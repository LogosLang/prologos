spec sum-to Int -> Int -> Int
defn sum-to [acc n]
  match [int-lt n 1]
    | true  -> acc
    | false -> [sum-to [int+ acc n] [int- n 1]]

def main : Int := [sum-to 0 10]

;; Sum 1..N via tail recursion. acc starts at 0 and accumulates n down
;; to 0. 1+2+...+10 = 55.
;; :expect-exit 55
