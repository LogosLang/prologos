# Pathological tests — see docs/tracking/ for details
# Format: filename  # reason
#
# Tests listed here are skipped by run-affected-tests.rkt by default.
# Use --no-skip to override, --skip-only to run only these tests.

# Reduction perf — still over the 120s batch-worker threshold.
# `test-reduction-perf-02-01.rkt` (fib 15 + ackermann, was ~51s) was
# brought back into the main suite at 7.9s after the looseBVarRange
# fix landed (commits 4f6b3f0 + 6f2e077). The two remaining ones
# below regressed instead of improving — root cause is the 449-arm
# match in whnf-impl/match (see profiler hot spot
# racket/match/compiler.rkt:559:40 = 44.5% CPU on fib 16).
# Investigation deferred; not OCapN.
test-reduction-perf-01-02.rkt  # fuel exhaustion (1M WHNF steps); now ~580s
test-reduction-perf-02-02.rkt  # fib 20 exponential recursion; now ~122s

# (No more OCapN tests skipped — the 7 interop tests previously
# listed here now hard-fail if Node deps are missing rather than
# silently exiting and crashing the batch-worker. Both test.yml
# and interop.yml install Node + npm interop deps.)
