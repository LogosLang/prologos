# Prior-Art Watch

Scheduled rescans + watched conferences/proceedings. Priority claims rot; this file is the discipline.

---

## Watched terms (monthly arXiv + Google Scholar rescan)

### Paper-B-critical

- `"free lattice"` + (`computation` | `parallel` | `distributed` | `propagator`)
- `"Whitman"` + (`decision` | `algorithm` | `parallel` | `complexity`)
- `"Nation Paolini"` (forward citations)
- `"FOTFL"` | `"first-order theory of free lattices"`

### Paper-A-critical

- `"stratified negation"` + (`category` | `bifibration` | `type system` | `effect`)
- `"layered recovery"` + (`monotone` | `lattice` | `fixpoint`)
- `"well-founded semantics"` + (`category` | `Galois`)
- `"Kan extension"` + (`stratified` | `program transformation`)

### Paper-Poly-critical

- `"polynomial functor"` + (`propagator` | `lattice` | `Sussman` | `Radul`)
- `"dependent optic"` + (`compiler` | `network` | `propagator`)
- `"Spivak"` + (`Poly` | `polynomial functor`)
- Topos Institute event listings; recent Spivak talks/slides

### Paper-C-critical

- `"lattice variety"` + (`parallel` | `optimal` | `Hasse` | `decomposition`)
- `"Garg"` + (`lattice-linear` | `predicate detection`)
- `"semidistributive"` + (`computational` | `algorithm` | `complexity`)

### Paper-A0-critical

- `"propagator network"` + (`compiler` | `IR` | `intermediate representation`)
- `"e-graph"` + (`IR` | `compiler`) (Tate equality saturation lineage)
- `"phase ordering"` + (`compiler` | `unified`)

### Cross-cutting

- `"CALM theorem"` (forward citations)
- `"Hellerstein"` + (`Bloom` | `monotone` | `coordination`)
- `"LVar"` + (`lattice` | `parallel`) (Kuper lineage)
- `"actor model"` + (`universal` | `substrate`) (Hewitt lineage)

---

## Watched conferences / proceedings

| Venue | Watch reason | Cadence |
|---|---|---|
| POPL, ICFP, OOPSLA, PLDI | Paper-A and Paper-A0 venues | Per-CFP + per-proceedings |
| LICS, FOCS | Paper-B and Paper-C venues | Per-CFP + per-proceedings |
| LMCS, JFP, JACM | Long-form paper venues | Quarterly (slow journals) |
| ACT, MFPS, CALCO | Paper-Poly venues | Per-CFP + per-proceedings |
| Topos Institute Berkeley + Oxford workshops | Spivak Poly programme | Per-event |

---

## Schedule (proposed; queue with `schedule_prompt` after charter approval)

- **Monthly**: Paper-B-critical + Paper-Poly-critical terms. Highest churn risk.
- **Quarterly**: Paper-A-critical + Paper-C-critical + Paper-A0-critical. Lower churn.
- **Bi-annually**: cross-cutting terms.
- **Per-conference-CFP**: targeted scan of accepted papers list for any of the watched terms.

---

## Scan log

| Date | Scanned terms | New hits | Notes |
|---|---|---|---|
| 2026-05-08 | (FL+UTM round-1+round-2) | (consolidated in `outputs/free-lattice-utm-parallel.md`) | Initial baseline established. Subsequent scans diff against this. |
