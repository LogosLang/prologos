#+TITLE: Standup — 2026-02-26
#+DATE: 2026-02-26


* Yesterday

** Logic Engine — Phases 4-7

| Phase   | Description                                                    |
|---------+----------------------------------------------------------------|
| Phase 4 | Persistent UnionFind as first-class Prologos type              |
| Phase 5 | Persistent ATMS — hypothetical reasoning as first-class type   |
| Phase 6 | SLG-Style Tabling — memoization for completeness               |
| Phase 7 | Relational language scaffolding — parser + elaborator          |

** Relational Language — Surface Syntax Design

Iterative design session covering the full relational language surface syntax:

- *Solver design*: =solve=/=explain= split. =solve= returns =Seq (Map Keyword Value)=
  (bare bindings, zero provenance overhead). =explain= returns =Seq (Answer Value)=
  with bundled bindings + derivation trees. Both have =-with= variants accepting
  ={...}= map merge overrides.
- *=solver= top-level form*: Configuration entity carrying execution, strategy,
  tabling, provenance, timeout. =default-solver= is module-level, shadowable.
  Completes the =spec=/=session=/=solver= triple.
- *Facts syntax*: =||= for disjunctive fact blocks within =defr=, =&>= for rules.
  Facts are bodiless clauses inside =defr=, not a separate top-level form.
- *Schema design*: =schema= as named closed-map type (like Rust structs / Clojure spec).
  Completes the =spec=/=schema=/=session= specification triple. Schema-annotated
  =defr= gets positional facts with type checking.
- *Logic variable conventions*: Bare names in rule bodies (no =?= prefix), mode
  contracts (=+in=/=-out=) only in argument signatures. Correct Mercury conventions
  (=+= = input, =-= = output).
- *Multi-arity =defr=*: =|= clause separator (matching =defn= multi-body pattern).
- *Relational → database isomorphism*: Facts ↔ rows, predicates ↔ tables,
  rules ↔ composable views, tabling ↔ materialized views, propagators ↔ incremental
  view maintenance, ATMS ↔ provenance.

Key design documents updated:
- Logic Engine Design doc (Phase 7 additions)
- =RELATIONAL_LANGUAGE_VISION.md=

** Benchmarking Framework + Type Inference Design

Two comprehensive design documents produced from parallel research:

1. =docs/tracking/2026-02-25_BENCHMARKING_FRAMEWORK_DESIGN.md= — Three-tier
   architecture (micro/integration/comparative), Lean4-inspired heartbeat counters,
   phase-level timing, A/B test harness, static HTML dashboard, property-based
   testing via rackcheck.
2. =docs/tracking/2026-02-25_TYPE_INFERENCE_ON_LOGIC_ENGINE_DESIGN.md= — Full
   isomorphism mapping (metavars→cells, unification→propagators, speculation→ATMS
   worldviews), 6-phase implementation plan, 12 comparative benchmark programs,
   risk analysis.

** Benchmarking Framework — Full Implementation (Phases A-G)

All 7 phases of the benchmarking framework design implemented and committed:

| Phase | Description                        | Key Deliverables                                                                     |
|-------+------------------------------------+--------------------------------------------------------------------------------------|
| A     | Heartbeat counters                 | =performance-counters.rkt= (277 lines), 12 counter types, 11 instrumentation points |
| B     | Phase-level timing                 | =time-phase!= macro, 6 phases, JSONL schema v3                                      |
| C     | Micro-benchmark harness            | =bench-micro.rkt= (243 lines), criterion-style CI, 4 benchmark suites               |
| D     | Multi-run baseline + memory        | GC time + memory delta tracking, =--runs N=, =--regression-check=                   |
| E     | Property-based testing             | =test-properties.rkt= (184 lines), rackcheck, 4 property suites                     |
| F     | Static HTML report                 | =benchmark-report.rkt= (483 lines), Vega-Lite charts, =--output= flag               |
| G     | Comparative A/B framework          | =bench-ab.rkt= (267 lines), 10 programs, paired t-test, Cohen's d                   |

Tests added: =test-perf-counters.rkt= (19), =test-phase-timing.rkt= (14),
=test-generators.rkt= (8), =test-properties.rkt= (10).

** Type Inference on Logic Engine — Design Critique + Refinement

- 12-point adversarial critique of the type inference design
- 4 accepted, 2 partially accepted, 7 rejected with rationale
- Design doc refined (commit =9847fe5=)

** Type Inference on Logic Engine — Implementation (Phases 0-6)

All 7 phases implemented and committed. 15 commits total.

| Phase | Description                          | New Files                              | Tests | Key Innovation                                                          |
|-------+--------------------------------------+----------------------------------------+-------+-------------------------------------------------------------------------|
|     0 | Benchmarking baseline                | baseline data                          |     — | Instrumented run: 191 files, 4147 tests, heartbeats + phase timing      |
|     1 | Type lattice module                  | =type-lattice.rkt= (230 lines)        |    24 | Pure structural unification via =try-unify-pure=; merge = unification   |
|     2 | Elaborator network                   | =elaborator-network.rkt= (227 lines)  |    19 | PropNetwork cells as type variables; unification propagators             |
|     3 | Shadow validation                    | =elab-shadow.rkt= (199 lines)         |    22 | Hook-based mirroring of meta-store → shadow network; validation reports |
|     4 | ATMS-backed speculation              | =elab-speculation.rkt= (186 lines)    |    18 | Worldview-based speculative type-checking; O(1) fork via CHAMP          |
|     5 | Always-on network + speculation      | =elab-speculation-bridge.rkt= (87 ln) |    17 | =with-speculative-rollback= wraps all 4 speculation sites               |
|     6 | Union type error enrichment          | — (modify only)                        |     5 | E1006 error: per-branch mismatch details for union type failures        |

*** Cumulative Impact

- *New source files*: 6 (=type-lattice.rkt=, =elaborator-network.rkt=,
  =elab-shadow.rkt=, =elab-speculation.rkt=, =elab-speculation-bridge.rkt=,
  =performance-counters.rkt=)
- *New test files*: 10
- *Lines added*: ~6,200 (source + tests + benchmarks + tools)
- *Total test count*: 4306 tests, 199 files, all pass (252.2s wall)

*** Key Decisions

- *Shadow mirroring, not full switchover*: The propagator network mirrors the
  meta-store via hooks. Meta-store remains primary for correctness; network
  validates and provides precise fork/restore for speculation. Full switchover
  (70+ call sites, 14 files) deferred.
- *=with-speculative-rollback= as single abstraction*: One helper wraps all 4
  speculation sites (3 in =typing-core.rkt=, 1 in =qtt.rkt=). Saves meta-state +
  forks network, runs thunk, restores both on failure.
- *Error enrichment at error layer*: E1006 union exhaustion errors produced by
  =check/err= in =typing-errors.rkt= — zero changes to =typing-core.rkt= or
  =qtt.rkt=. =whnf= on expected type, =flatten-union-local= to decompose nested
  unions.
- *Speculation failure recording*: =current-speculation-failures= collects branch
  failure labels per command. Infrastructure ready for future consumption.

*** Key Fixes

- =fresh-meta= returns =(expr-meta id)=, not raw symbol — must extract with
  =(expr-meta-id m)= before =solve-meta!=
- Union type surface syntax: =<A | B>= (angle brackets), not =[A | B]=
  (square brackets)
- =process-def-group=: re-init shadow + speculation tracking per clause to prevent
  teardown across multi-body defs
- =maybe-shadow-validate!=: defensive check on =net-box= before calling
  =shadow-validate!=


* Carry-Forward

** From This Session

- [X] Type Inference Phase 7: Performance optimization (micro-opts from design doc)
- [ ] Type Inference: Full switchover (replace meta-store with propagator network) — ~35 call sites, 8 files
- [X] Benchmarking: Post-switchover A/B comparison (baseline re-captured with actual heartbeats)
- [X] Benchmarking: CI integration for regression detection
- [X] Union type errors: Per-branch re-checking (now uses =with-speculative-rollback= per branch)

** Ongoing

- [ ] Relational language implementation (Phase 7 scaffolding done; full =defr=/=rel=/=solve= still ahead)
- [ ] Numerics Tower Phase 4: Float32/Float64
- [ ] Collections: Stage I transducer integration (deferred)
- [ ] Extended spec: Phase 2+ (example checking, property testing, refinement types)
- [ ] HKT partial application for =Map K= as =Type → Type= constructor
- [ ] Improved implicit resolution for trait accessor calls inside generic closures
- [ ] Homoiconicity Phase IV
- [ ] Editor-Assisted Interactive Programming design
- [ ] =schema= top-level form implementation


* Today (Feb 26)

** Type Inference Phase 7 — Performance Optimization + Benchmarking + CI

All 5 sub-phases implemented and committed:

| Sub-Phase | Description                          | Key Deliverables                                                                              |
|-----------+--------------------------------------+-----------------------------------------------------------------------------------------------|
| 7a        | Per-branch re-checking               | =with-speculative-rollback= per union branch in =check/err=; branch-specific E1006 messages   |
| 7b        | Performance micro-optimizations      | =eq?= fast path, ground unify skip-propagator, ground-atom extraction fast path               |
| 7c        | Fixed benchmarking framework         | =module+ main= in =driver.rkt=; re-captured baseline with actual heartbeats (11,900 total)    |
| 7d        | CI integration                       | =.github/workflows/test.yml=, =benchmark.yml=, =tools/ci-regression-check.rkt= (15% threshold) |
| 7e        | Full suite verification              | 4308 tests, 199 files, all pass (191.9s wall)                                                 |

Key fix: =bench-ab.rkt= ran =racket driver.rkt file.prologos= but =driver.rkt= had no =module+ main= — loaded as library and exited without processing. All prior comparative benchmarks had heartbeats=0.

Files changed: =typing-errors.rkt=, =type-lattice.rkt=, =elaborator-network.rkt=, =elab-shadow.rkt=, =driver.rkt=, =test-speculation-bridge.rkt=.
Files created: =.github/workflows/test.yml=, =.github/workflows/benchmark.yml=, =tools/ci-regression-check.rkt=.
