# CIU T6 F1b — Stage-3 Grounding Audit Synthesis (companion to the Options doc)

**Date**: 2026-07-16 · **Series/Track/Phase**: CIU Track 6, F1b (Stage-3 opening)
**Provenance**: `grounding-audit` workflow run `wf_0f364e4e` (6 HEAD-pinned read-only facets + adversarial completeness critic; 7 agents, ~1.01M tokens). All facets verified HEAD `02aad495` (docs-only `d3c37bd0` landed after; all `.rkt` coordinates hold).
**Main-session R-lens**: 11 load-bearing targets re-verified surgically before trusting (zonk pass-through; closed? single site; B3/C_Cons guards; underscore-key construction + the six merge surfaces; dormant expr-schema + 0 pnet hits; nested atomic-merge terminus; REFUSE leg; test-bound-args-01 [38 checks]; expr-solve→hole).
**Companion**: [F1b Stage-3 Options](2026-07-16_CIU_T6_F1B_STAGE3_OPTIONS.md) — the design-options panel this grounding fed.

> ⚠ Coordinates drift — re-grep before trusting any file:line below in a later session.

## §1 Facet: schema-substrate

**[1.0] (VERIFIED)** (a) schema-field struct carries exactly 4 fields: keyword, type-datum, default-val (#f if no :default), check-pred (#f if no :check). NO optional flag, NO presence slot.
  - *Evidence*: racket/prologos/macros.rkt:758 — "(struct schema-field (keyword type-datum default-val check-pred) #:transparent)"; property parsing at macros.rkt:852-867 handles only ':default' and ':check'

**[1.1] (VERIFIED)** (a) schema-entry carries (name fields closed? srcloc); registry is the parameter current-schema-registry (hasheq name→entry) with cell dual-write on register and cell-primary read.
  - *Evidence*: racket/prologos/macros.rkt:765 — "(struct schema-entry (name fields closed? srcloc) #:transparent)"; :768 "(define current-schema-registry (make-parameter (hasheq)))"; :770-773 register-schema! dual-writes via "(macros-cell-write! (current-schema-registry-cell-id) (hasheq name entry))"; :776-777 read-schema-registry cell-first; cell-id param at :534, wired :580

**[1.2] (VERIFIED)** (a) Registration happens in TWO preparse sites, never in the parser: Pass-0 pre-registration (forward refs) and the full public-schema handler which also ns-qualifies field type datums, registers auto-sub-schemas, auto-exports, and emits the opaque type def (def Name : (Type 0) (Type 0)). tree-parser hard-errors if a schema form reaches it.
  - *Evidence*: racket/prologos/macros.rkt:2415-2426 (Pass 0, register-schema! at :2426); :2744-2780 (main handler; qualify :2760-2768, register :2769-2770, type-def emit :2778); parse-schema-fields :789-846 (inline nested schema auto-register :836, name format parent__field :827-831); tree-parser.rkt:180 — "[(schema) (parse-error-result loc \"schema: consumed by preparse\")]"; WS tag registered surface-rewrite.rkt:123/:358

**[1.3] (VERIFIED)** (b) The closed? flag is set only by detecting ':closed immediately after the schema name (both preparse sites) and is CONSULTED at EXACTLY ONE production site: the map-assoc-vs-schema check arm — closed rejects unknown fields, open infers them. The design doc's 'exactly 1 site in check direction' claim HOLDS at HEAD; the historical coordinate typing-core:2444 is stale — it is now typing-core.rkt:2921.
  - *Evidence*: racket/prologos/macros.rkt:2421-2424 and :2752-2755 ("(if (and (pair? after-name) (eq? (car after-name) ':closed)) ..."); typing-core.rkt:2917-2923 — "(if field (check ctx v (schema-field-type->expr ...)) (if (schema-entry-closed? schema) #f (not (expr-error? (infer ctx v)))))"; repo-wide grep for schema-entry-closed? hits only macros.rkt:191 (provide) + typing-core.rkt:2921 in production

**[1.4] (VERIFIED)** (c) The schema projection template moved: now the expr-map-get infer arm at typing-core.rkt:1709-1722 (schema fvar + keyword literal → schema-lookup-field → schema-field-type->expr, else expr-error; non-keyword key → expr-error), with expr-get delegating at :1675-1678. schema-lookup-field is typing-core.rkt:397-400; schema-field-type->expr is typing-core.rkt:358-393 (built-in symbol table, fvar fallback :378, compound datum → nested expr-app :379-392). Historical coordinate 1536-1548 is STALE.
  - *Evidence*: racket/prologos/typing-core.rkt:1715-1719 — "[(expr-keyword kw-sym) (let ([field (schema-lookup-field schema kw-sym)]) (if field (schema-field-type->expr (schema-field-type-datum field)) (expr-error)))]"; qtt.rkt:1231-1242 delegates map-get result type for schema/selection to typing-core infer

**[1.5] (VERIFIED)** (d) NO schema→Map flow exists today. unify.rkt has zero schema mentions; subtype-predicate.rkt has zero schema mentions (its Record→Map α at :126/:210 covers structural rows only, not schema fvars); no typing-core check arm takes schema-actual/Map-expected. Behaviorally confirmed: passing p : Person to take-map : (Map Keyword String) -> String fails with inference-failed-error 'Could not infer type [probe::take-map probe::p]'.
  - *Evidence*: grep 'schema' unify.rkt → 0 hits; grep 'schema' subtype-predicate.rkt → 0 hits; subtype-predicate.rkt:126 — "[(and (expr-Record? t1) (expr-Map? t2)) (record-subtypes-map? t1 t2)]" (Record only); behavioral probe via process-file at HEAD

**[1.6] (VERIFIED)** (e) Map→schema WORKS in the check direction and is width-PARTIAL: map-assoc vs schema checks each provided field's type (:2910-2925), map-empty vs schema is unconditionally #t 'empty map is a valid partial schema' (:2942-2944), champ vs schema unconditionally #t (:2946-2948). Behaviorally: def p : Person := {:name \"alice\" :age 5} accepted, p.name → \"alice\" : String; MISSING fields are silently accepted on the annotation path — def r : Person := {:name \"carol\"} accepted with NO default fill and NO error; r.age types as Int but evaluates to <error>.
  - *Evidence*: racket/prologos/typing-core.rkt:2905-2948 (three arms + selection delegation :2927-2940); qtt.rkt:2330-2345 parallels; behavioral probes 1+2 via process-file at HEAD

**[1.7] (VERIFIED)** (f) The fill-or-error precedent exists ONLY on the constructor form via preparse rewrite: (SchemaName {$brace-params ...}) → inject-schema-defaults (macros.rkt:1193-1209) → (the SchemaName augmented) → wrap-schema-checks (macros.rkt:1243-1262, nested if/panic per :check field, underscore subst :1216-1220, operator normalization :1226-1239), at rewrite site macros.rkt:1779-1795. Behaviorally: [Person {:name \"bob\"}] → q.age = 30 : Int (default injected); top-level [Person {:name \"eve\" :age 0}] → panic: 'Person: field :age failed check (> _ 0)'. The annotation path (def x : Schema := {…}) gets NEITHER defaults NOR checks NOR missing-field errors.
  - *Evidence*: racket/prologos/macros.rkt:1788-1793 — "(let* ([augmented (inject-schema-defaults maybe-schema (cadr datum))] [the-form `(the ,(car datum) ,augmented)] [wrapped (wrap-schema-checks maybe-schema the-form)]) ...)"; behavioral probes 1-3 at HEAD

**[1.8] (FLAGGED)** (f) Check-pred panic does NOT surface when the violating constructor form is a def body: def bad := [Person {:name \"dave\" :age 0}] → 'bad : probe2::Person defined.' with no panic, while the identical top-level expression panics. Both behaviors observed; the mechanism (def body not forced vs error swallowed) was NOT traced — open item.
  - *Evidence*: behavioral probes 2 and 3 via process-file at HEAD 02aad495 (probe2: def path silent; probe3: top-level → #(struct:prologos-error #f panic: Person: field :age failed check (> _ 0)))

**[1.9] (VERIFIED)** (f) Second validation precedent: schema-typed defr fact rows are positionally type-checked against schema field types at registration — arity mismatch + per-field check with formatted errors; synthesized field-named params derive arity in the parser.
  - *Evidence*: racket/prologos/driver.rkt:413-439 check-relation-schema-rows (per-field "(check ctx-empty term ft)" :435), called at driver.rkt:745; parser.rkt:5461-5487 parse-defr-schema-typed (synth-params :5479); relation-info carries schema name, relations.rkt:426-428/:775-791

**[1.10] (VERIFIED)** A DORMANT second schema realization exists in the AST: expr-schema (name fields) 'named closed validated map' + expr-schema-type (name) at syntax.rkt:919-920, with full pipeline identity coverage (substitution/zonk/reduction/pretty-print/typing-core:2726/qtt:2145) but NO producer — nothing in parser/tree-parser/elaborator constructs them, and surf-schema (surface-syntax.rkt:1040) is never referenced outside its own definition/provide. They are ALSO absent from pnet-serialize.rkt (zero schema mentions) — unregistered per pipeline.md rule 6, latent vector-impostor if ever minted into a cached body.
  - *Evidence*: racket/prologos/syntax.rkt:919-920 — "(struct expr-schema (name fields) #:transparent) ; named closed validated map" / "(struct expr-schema-type (name) #:transparent)"; grep surf-schema across parser.rkt/tree-parser.rkt/elaborator.rkt → only surface-syntax.rkt:311,:1040; grep schema pnet-serialize.rkt → 0 hits; typing-core.rkt:2726 types it: "[(expr-schema nm fs) (for-each ...) (expr-schema-type nm)]"

**[1.11] (VERIFIED)** Schema types live in the env as OPAQUE fvar-typed defs (like data types): the emitted def is (def Name : (Type 0) (Type 0)); driver skips body elaboration for schema-named defs and residuation rejects them; known-type-name? includes lookup-schema; the cell/REPL path has its own emit site for the opaque def (two-context seam).
  - *Evidence*: racket/prologos/macros.rkt:2778 type-def emit; driver.rkt:1650-1672 (data-type-def? includes lookup-schema :1656-1657, opaque skip); driver.rkt:1334-1342 residuation reject; macros.rkt:6612 known-type-name?; form-cells.rkt:307-322 cell-path emit with comment "`schema` is NOT purely side-effect-only"

**[1.12] (VERIFIED)** Selections are a gating layer ON TOP of schemas and share every projection/check surface the seal will touch: selection map-get gating (typing-core.rkt:1692-1708), selection-field-type with sub-selection synthesis (:622-659), check-direction delegation to the parent schema (:2927-2940), and deep path validation against schema fields in the elaborator (elaborator.rkt:3725-3810).
  - *Evidence*: racket/prologos/typing-core.rkt:2931-2932 — "A selection at value level IS the parent schema — delegate to schema check"; elaborator.rkt:3749+ validate-path walks schema-entry-fields recursively

### §1.G Capture gaps

- The facet framing ('schema = registry + opaque fvar') under-counts the substrate: there is a SECOND, dormant AST realization — expr-schema/expr-schema-type (syntax.rkt:919-920, described as 'named closed validated map') plus never-produced surf-schema (surface-syntax.rkt:1040). F1b's seal design must either adopt these nodes (then complete pipeline.md's checklist, esp. pnet registration) or explicitly tombstone them; leaving them half-alive is the exact drift class workflow.md flags.
- pnet-serialize.rkt has ZERO schema registrations (expr-schema, expr-schema-type unregistered) — latent vector-impostor failure per pipeline.md rule 6 the moment any seal machinery mints one into a cached module body.
- def-path vs top-level asymmetry in check-pred enforcement: a violating constructor form inside a def body defines silently (probe2) while the same form at top level panics (probe3). Any fill-or-error seal that reuses wrap-schema-checks inherits this asymmetry — untraced mechanism, needs a targeted look at the def-body evaluation path (driver.rkt def case).
- Two stale historical coordinates in the design doc's framing: the closed? consultation is typing-core.rkt:2921 (not :2444) and the projection template is typing-core.rkt:1709-1722 (not :1536-1548).
- Two-context seam: schema has a dedicated cell/REPL-path emit (form-cells.rkt:307-322) that mirrors process-file's opaque-def emit; seal machinery added only on the preparse/process-file path would silently diverge on the cell/REPL path (pipeline.md Two-Context Audit).
- Selections (typing-core.rkt:622-659, :1692-1708, :2927-2940; elaborator.rkt:3725-3810) sit on the same schema surfaces the seal will modify — the facet prompt did not enumerate them; a Map↔schema seal that changes the schema check arms changes selection semantics for free.
- Defaults injection is PREPARSE-syntactic (macros.rkt:1779-1795 fires only on the literal (SchemaName {brace-params}) head-position shape) — it cannot see annotation-path literals, non-literal maps, or runtime-constructed maps; the F1b 'fill-or-error' seal cannot be a straight reuse of this mechanism.

### §1.R R-lens targets

- racket/prologos/typing-core.rkt:2921 — re-grep schema-entry-closed? to confirm the single-consultation claim before designing the seal's closed-row semantics
- racket/prologos/typing-core.rkt:2942-2948 — map-empty/champ vs schema unconditional #t arms: the width-partial acceptance the seal will tighten or preserve
- racket/prologos/macros.rkt:1779-1795 — the constructor-form-only boundary of defaults+checks (the fill-or-error precedent the seal generalizes)
- racket/prologos/syntax.rkt:919-920 + grep 'schema' pnet-serialize.rkt (expect 0 hits) — the dormant expr-schema family + missing pnet registration
- racket/prologos/subtype-predicate.rkt:126,144-155 — record-subtypes-map? key-domain gate: the existing Record→Map α the schema→Map free up-shift will parallel
- Behavioral re-probe if needed: def r : Person := {:name \"carol\"} is ACCEPTED at HEAD with r.age → <error> : Int (annotation path: no fill, no error) — one process-file run reproduces
- racket/prologos/driver.rkt:745 + :413-439 — check-relation-schema-rows as the second validation precedent (positional, ctx-empty, error-string protocol)

## §2 Facet: width-depth-surfaces

**[2.0] (VERIFIED)** D11 HOLDS in subtype-predicate.rkt: there is NO record-vs-record subtype judgment. subtype? has exactly two record arms — the Record→Map and Record→PVec Galois bridges — and compound-type? deliberately excludes expr-Record, so Record-vs-Record falls to `[else #f]`. Only equal?-identity passes.
  - *Evidence*: racket/prologos/subtype-predicate.rkt:126 `[(and (expr-Record? t1) (expr-Map? t2)) (record-subtypes-map? t1 t2)]`; :128 (Record→PVec); :108-113 compound-type? lists Pi/Sigma/app/PVec/Set/Map/Vec/Eq/pair/lam/Fin/suc — no expr-Record?; :135 `[else #f]`; :124 comment: 'This is the record→Map bridge, NOT a record<:record judgment (D11 preserved)'

**[2.1] (VERIFIED)** record-subtypes-map? (pure) is key-domain-gated and tail-BLIND by design: 'keyword rows need expr-Keyword? kt, 'nat rows need Nat/Int kt, and only KNOWN fields are checked against V — the site comment pins knowns-only as the C_ConsL absorption for dyn rows (§12.4), contrasting with typing-core's meta-refusing sibling.
  - *Evidence*: racket/prologos/subtype-predicate.rkt:144-155 `(and (case (expr-Record-key-domain rec) [(keyword) (expr-Keyword? kt)] [(nat) (or (expr-Nat? kt) (expr-Int? kt))] [else #f]) (andmap (lambda (f) (subtype? (record-field-type (cdr f)) vt)) (expr-Record-fields rec)))`; comment :139-143 'the knowns-only check IS the C_ConsL absorption for 'dyn-tailed rows … DELIBERATE dyn semantics'

**[2.2] (VERIFIED)** record-subtypes-pvec? (pure) is 'nat-domain-only and also tail-blind, but WITHOUT a dyn-posture comment at the site — the tuple/dyn absorption posture is implicit, not documented there.
  - *Evidence*: racket/prologos/subtype-predicate.rkt:160-164 `(and (eq? (expr-Record-key-domain rec) 'nat) (let ([at (expr-PVec-elem-type pv)]) (andmap (lambda (f) (subtype? (record-field-type (cdr f)) at)) (expr-Record-fields rec))))`

**[2.3] (VERIFIED)** The two pure bridges are ALSO reachable from nested covariant components via structural-subtype-ground?'s dedicated pre-tag arms (e.g. (List Record) <: (List Map)); a nested Record-vs-Record instead falls to the atomic-merge branch, where subtype-lattice-merge produces an incomparable union that fails `(equal? merged t2)` — a third width-failure site.
  - *Evidence*: racket/prologos/subtype-predicate.rkt:210 `[(and (expr-Record? t1) (expr-Map? t2)) (record-subtypes-map? t1 t2)]`, :212 (PVec), :236-240 atomic branch `(define merged (merge t1 t2)) (and (not ((sre-domain-contradicts? domain) merged)) (equal? merged t2))` with subtype-lattice-merge :409-411 `[else (build-union-type-with-absorption (list a b))]`

**[2.4] (VERIFIED)** There is NO function named check-subsumption at HEAD. The 'check-subsumption record arms' live in check's (typing-core.rkt:2762) CONVERSION FALLBACK arm; the only textual occurrence of the name is a comment in unify.rkt.
  - *Evidence*: grep 'check-subsumption' across racket/prologos/*.rkt hits ONLY unify.rkt:581 (comment '…falls through to check-subsumption's meta-solving arm at the top level'); the actual arms are typing-core.rkt:3217-3239

**[2.5] (VERIFIED)** check's conversion-fallback record arms at HEAD, in order after unify + cumulativity: Map-expected → record-<:-map?; PVec-expected → record-<:-pvec?; List-expected → record-<:-elem?; then the final subtype? leg (where record-vs-record width currently dies, since subtype? has no record-record arm).
  - *Evidence*: racket/prologos/typing-core.rkt:3227-3228 `[((? expr-Map? mt) (? expr-Record? rec)) (record-<:-map? ctx rec (expr-Map-k-type mt) (expr-Map-v-type mt))]`; :3231-3232 (PVec); :3235-3237 `[((expr-app f a) (? expr-Record? rec)) #:when (equal? f (list-type-fvar)) (record-<:-elem? ctx rec a)]`; :3239 `[(t-w t1-w) (subtype? t1-w t-w)]`

**[2.6] (VERIFIED)** The meta-V refusal (D16/Q4) is in record-<:-map?: a meta V may be solved from ⋃fields ONLY for a CLOSED row — `(and (eq? (expr-Record-tail rec) 'closed) (unify-ok? (unify ctx vt (record-value-union rec))))`; a dyn row returns #f, leaving the meta unsolved (Q4 display posture). Keys are checked via `check` of the label literal against K; concrete-V is knowns-only (C_ConsL absorption).
  - *Evidence*: racket/prologos/typing-core.rkt:534-554, refusal at :546-548 with comment :543-545 'a dyn row's remainder is unknown, so ⋃knowns would over-commit; REFUSE (the meta stays unsolved; Q4 display posture)'

**[2.7] (VERIFIED)** record-<:-elem? (shared PVec/List elem-side α, record-<:-pvec? is an alias) mirrors the refusal: meta elem type solves from ⋃positions ONLY for closed rows; empty closed row satisfies any elem meta WITHOUT solving it; concrete/union elem types accept a position when SOME branch fits, subtype?-first so ground positions don't stray-solve.
  - *Evidence*: racket/prologos/typing-core.rkt:476-497 (`[(expr-meta? at*) (and (eq? (expr-Record-tail rec) 'closed) (or (null? (expr-Record-fields rec)) (unify-ok? (unify ctx at (record-value-union rec)))))]`); alias :499-500 `(define (record-<:-pvec? ctx rec at) (record-<:-elem? ctx rec at))`

**[2.8] (VERIFIED)** check's union leg is generic (no record-specific arm): left branch rollback-probed, right branch bare, whole-union conversion only on the both-fail path (p3 perf-refit); records ride ordinary branch checks. checkQ mirrors it exactly.
  - *Evidence*: racket/prologos/typing-core.rkt:3176-3184 `[(_ (expr-union l r)) (or (with-speculative-rollback (lambda () (check ctx e l)) values "union-check-left") (check ctx e r) (let ([t1 (infer ctx e)]) …))]` + comment :3175 '(checkQ has the same shape — mirror them together.)'; qtt.rkt:2688-2702

**[2.9] (VERIFIED)** unify's classifier has Record→Map/PVec coercion arms firing in EITHER argument ordering, PURE (subtype-predicate helpers, no meta-solving): a meta-V map returns #f here and falls through to the check-fallback's meta-solving arm.
  - *Evidence*: racket/prologos/unify.rkt:582-586 `[(and (expr-Record? a) (expr-Map? b) (record-subtypes-map? a b)) '(ok)] [(and (expr-Map? a) (expr-Record? b) (record-subtypes-map? b a)) '(ok)]` (+ PVec pair); comment :579-581

**[2.10] (VERIFIED)** B3 row-vs-row arm: Record-vs-Record with SAME key-domain (eq?), SAME tail (eq?), SAME label-set (equal? on sorted label lists) decomposes to per-field 'sub goals zipping ONLY record-field TYPES (presence deliberately excluded); sorted-zip is label-correct because make-record canonicalizes field order.
  - *Evidence*: racket/prologos/unify.rkt:597-604 `[(and (expr-Record? a) (expr-Record? b) (eq? (expr-Record-key-domain a) (expr-Record-key-domain b)) (eq? (expr-Record-tail a) (expr-Record-tail b)) (equal? (map car …) (map car …))) (list 'sub (map (lambda (fa fb) (cons (record-field-type (cdr fa)) (record-field-type (cdr fb)))) …))]`; make-record canonicalization syntax.rkt:678-685

**[2.11] (VERIFIED)** C_Cons arm: same key-domain + at least one 'dyn tail; a CLOSED side must CONTAIN every other-side label (containment guards), dyn absorbs extras; shared labels decompose to per-field 'sub goals (solve-first D16 posture). Takes dyn-vs-closed and dyn-vs-dyn-with-differing-labels; dyn-vs-dyn same-label-set rides B3.
  - *Evidence*: racket/prologos/unify.rkt:617-631, guards :622-625 `(and (or (eq? (expr-Record-tail b) 'dyn) (andmap (lambda (l) (memv l lb)) la)) (or (eq? (expr-Record-tail a) 'dyn) (andmap (lambda (l) (memv l la)) lb)))`; goal emission :626-631

**[2.12] (VERIFIED)** EXACT width-mismatch failure terminus in unify (the sites erasure-mode width would relax): (1) closed-vs-closed with different label-sets — B3 guard :600-601 fails, C_Cons guard :619 fails (no dyn), SRE dispatch returns #f (Record has no ctor-desc), no hardcoded arm matches → `[else '(conv)]` at :775 → dispatch `['(conv) (conv-nf a b)]` at :802 → conv-nf deep structural equality FAILS. (2) dyn-vs-closed where the closed side misses a dyn-known label — C_Cons containment guard fails → same '(conv) terminus. The B3 comment names the different-label-set fall-through as 'the F1a-col flavor-B union-widen case, deferred'.
  - *Evidence*: racket/prologos/unify.rkt:775 `[else '(conv)]`, :802 `['(conv) (conv-nf a b)]`, comment :592; conv-nf = racket/prologos/reduction.rkt:3820-3838 (struct->vector deep equality, hole-wildcards, metas same-id only — NO record awareness)

**[2.13] (VERIFIED)** The ctor-desc structural walk is fixed-arity POSITIONAL everywhere and cannot host label-keyed recursion: the ctor-desc struct carries arity/extract-fn/component-lattices/component-variances all validated to positional length=arity; every consumer zips components positionally (sre-structural-classify `(map cons comps-a comps-b)`; structural-subtype-ground? for/and with positional variances; sre-decompose-generic positional sub-cells + `list-ref variances idx`; generic-merge positional for/list). The decomp registry stores `(tag . sub-ids)` ordered lists.
  - *Evidence*: racket/prologos/ctor-registry.rkt:85-96 (struct), :184-188/:198-215 (validation `(= (length components) arity)` etc.), :359-368 (generic-merge positional zip); unify.rkt:171-172; subtype-predicate.rkt:225-232; sre-core.rkt:2387-2440 (:2422 `list-ref variances idx`), :2310-2326 (sub-cells), :2372-2373 (positional reconstruct)

**[2.14] (VERIFIED)** expr-Record is DELIBERATELY unregistered in the ctor-desc registry and carries no prop:ctor-desc-tag — the struct-site comment already pins the F1b boundary: 'a keyed/variable-width row cannot register in the fixed-arity positional ctor-desc registry; width subsumption is F1b (erasure-mode), not the walk.' ctor-tag-for-value therefore returns #f for records (fast path needs the prop; slow path scans only data + extra domains).
  - *Evidence*: racket/prologos/syntax.rkt:650-659 (comment :652-653 + `(struct expr-Record (key-domain fields tail) #:transparent)` with no #:property); ctor-registry.rkt:277-294 (fast path :281-285, slow path :289-294 scans data-ctor-table + extra-domain-tables only); registered type ctors :426-569 = Pi/Sigma/app/Eq/Vec/Fin/pair/lam/PVec/Set/Map/suc — no Record

**[2.15] (VERIFIED)** typing-propagators.rkt hosts NO ctor-desc structural walk and NO expr-Record handling at HEAD — all its 'record' hits are the Track 4B attribute-record PU (per-position attribute maps, an unrelated concept). The 'generic structural decomposition' of pipeline.md lives in sre-core.rkt (sre-decompose-generic), unify.rkt (sre-structural-classify), and subtype-predicate.rkt (structural-subtype-ground?). expr-Record's typing is entirely imperative (typing-core infer/check + qtt inferQ/checkQ).
  - *Evidence*: grep 'ctor-desc|extract-fn|component-variances' typing-propagators.rkt → 0 hits; grep 'Record|record' typing-propagators.rkt hits only Track 4B attribute-record PU lines (:100, :360-540)

**[2.16] (VERIFIED)** A label-keyed depth-on-shared-fields variant has a natural slot: the classifier's dedicated Record arms (B3/C_Cons) already sit BEFORE the SRE ctor-desc dispatch and DO perform label-keyed recursion via 'sub goal emission — i.e., the label-keyed recursion precedent exists at the classifier layer, not in the walk. Extending width there (or adding a Record-vs-Record arm in subtype? before its `else #f`) are the structurally available attachment points; the positional walk itself offers none.
  - *Evidence*: arm ordering: unify.rkt:597/:617 (Record arms) precede :650-652 `[(and (current-sre-classify-enabled?) (sre-structural-classify a b)) => values]`; 'sub dispatch :807-815 (punify-dispatch-sub or sequential unify-core)

**[2.17] (VERIFIED)** qtt.rkt checkQ conversion fallback at HEAD is :2704-2731 (NOT the historical :2449-2463 — coordinates drifted) and mirrors check's fallback arm-for-arm: cumulativity, Map/Record via record-<:-map? (B2), PVec/Record (F1a-col-3 B2 completion), List/Record, final subtype?. The site comment states the mirror obligation explicitly.
  - *Evidence*: racket/prologos/qtt.rkt:2704-2731; :2716 'checkQ's fallback is a DUPLICATE of check's — mirror the arm'; :2717-2718 record-<:-map?, :2723-2724 record-<:-pvec?, :2725-2727 record-<:-elem?, :2728 subtype?

**[2.18] (VERIFIED)** expr-Record is internal-only ('inferred + displayed, NOT parsed' per the struct comment), so an expected-side Record can only arise from inference chains — record-vs-record width events funnel through unify (B3/C_Cons/conv), not through user annotations; check-side width attachment is via the Map/PVec/List container arms.
  - *Evidence*: racket/prologos/syntax.rkt:650 'Anonymous structural-row TYPE node (CIU T6 F1 — internal-only: inferred + displayed, NOT parsed)'

**[2.19] (VERIFIED)** record-value-union (the ⋃fields used by all meta-solving arms) hard-errors on EMPTY rows — callers must guard or route through record-value-bound (whose empty-closed arm mints a fresh meta, Q6). Any F1b arm reusing ⋃fields inherits this contract.
  - *Evidence*: racket/prologos/typing-core.rkt:464-468 `(when (null? fields) (error 'record-value-union "empty row — callers must guard or use record-value-bound (F1a.2 p2)"))`

### §2.G Capture gaps

- The framing names 'typing-core.rkt check-subsumption' — no such function exists at HEAD; the record arms live in check's conversion fallback (typing-core.rkt:2762 define, :3213-3239 arm). Only occurrence of the name is a comment at unify.rkt:581. Design docs referencing 'check-subsumption' should be read as 'check's conversion fallback'.
- The framing's historical qtt checkQ fallback coordinate ':2449-2463' has drifted — at HEAD 02aad495 it is qtt.rkt:2704-2731 (~255 lines of drift). Re-grep before trusting any stored qtt coordinates.
- The framing locates the generic structural decomposition in 'ctor-registry.rkt + typing-propagators.rkt' — typing-propagators.rkt has NO ctor-desc walk at HEAD (0 grep hits); the actual recursion sites are sre-core.rkt:2387-2440 (sre-decompose-generic), unify.rkt:142-174 (sre-structural-classify), subtype-predicate.rkt:205-240 (structural-subtype-ground?), plus ctor-registry.rkt:335-373 (generic-merge). Its 'record' hits are the unrelated Track 4B attribute-record PU.
- Under-counted width-failure site: besides the unify '(conv) terminus, NESTED record-vs-record inside covariant components fails in structural-subtype-ground?'s atomic-merge branch (subtype-predicate.rkt:236-240 — subtype-lattice-merge :409-411 unions incomparable records, `(equal? merged t2)` fails). An erasure-mode width relax reachable from components must handle this branch too, not just the classifier arms.
- record-subtypes-pvec? (subtype-predicate.rkt:160-164) is tail-blind with NO dyn-posture comment (unlike its map sibling :139-143) — the C_ConsL-absorption intent for 'dyn-tailed nat rows is undocumented at that site; confirm it is intended before F1b builds on it.
- The design pin 'width subsumption is F1b (erasure-mode), not the walk' is ALREADY recorded at the expr-Record struct site (syntax.rkt:652-653) — the F1b design doc should cite/honor that in-code pin rather than re-derive the boundary.

### §2.R R-lens targets

- unify.rkt:597-604 (B3 guards: eq? key-domain, eq? tail, equal? label-sets) + :617-631 (C_Cons containment guards) — the exact predicates erasure-mode WIDTH would relax; verify guard text before designing the relaxation
- unify.rkt:775 + :802 + reduction.rkt:3820-3838 — the '(conv)→conv-nf width-failure terminus (conv-nf has zero record awareness; width cannot land there without changes)
- syntax.rkt:650-659 — the in-code F1b design pin ('width subsumption is F1b (erasure-mode), not the walk') + expr-Record (key-domain fields tail) + record-field (type presence) shapes
- typing-core.rkt:534-554 — record-<:-map? meta-V refusal (:546-548), the Q4/meta-refusal surface F1b item (3) tightens; and :464-468 record-value-union empty-row error contract
- qtt.rkt:2704-2731 — checkQ fallback mirror obligation: ANY change to check's fallback arms (typing-core.rkt:3217-3239) must be mirrored here or the QTT pass diverges
- subtype-predicate.rkt:108-113 + :135 — compound-type? excludes expr-Record and subtype? ends `[else #f]`: if erasure-mode width is exposed through subtype?, a Record-vs-Record arm must be added BEFORE that else (and the nested atomic-merge branch :236-240 reconsidered)

## §3 Facet: tightening-surfaces

**[3.0] (VERIFIED)** (a) default-metas passes TYPE metas through unsolved — the DEFERRED.md claim 'no type-domain defaulting exists' holds at HEAD.
  - *Evidence*: racket/prologos/zonk.rkt:1053 — `[(expr-meta _ _) e]` (identity arm inside default-metas, defined :1050). Header comment :1048-1049: 'Walk an expression replacing all unsolved level-metas with lzero and all unsolved mult-metas with 'mw.' Confirmed by docs/tracking/DEFERRED.md:22-23: 'zonk-final leaves it DISPLAYED as a meta (no type-domain defaulting exists — `zonk.rkt` default-metas passes type metas through)'.

**[3.1] (VERIFIED)** (a) What default-metas DOES default: unsolved level-metas → lzero, unsolved mult-metas → 'mw, and (third domain, often omitted from the two-phase-zonking summary) unsolved numeric-literal alphas → notation-origin default.
  - *Evidence*: zonk.rkt:1052 `[(expr-Type l) (expr-Type (zonk-level-default l))]`; :1084-1085 `(expr-lam (zonk-mult-default m) ...)` / `(expr-Pi (zonk-mult-default m) ...)`. metavar-store.rkt:2527-2533 zonk-level-default: unsolved level-meta → `(lzero)`; metavar-store.rkt:2653-2657 zonk-mult-default: unsolved mult-meta → `'mw`. Num-lit arm zonk.rkt:1058-1066: `(or (collapse-num-lit val integral? resolved) (collapse-num-lit val integral? (num-lit-default-type origin integral?)))` — an existing per-node defaulting POLICY precedent inside default-metas.

**[3.2] (VERIFIED)** (a) freeze = zonk then default-metas; zonk-final is a deprecated alias of freeze.
  - *Evidence*: zonk.rkt:1040-1046 — `(define (freeze e) ... (define z (zonk e)) (default-metas z))` then `;; Backward compatibility alias (deprecated — use freeze)` `(define zonk-final freeze)`.

**[3.3] (VERIFIED)** (b) D19 mint site 1 — literal-key MISS on a 'dyn row mints a fresh meta via record-project's `miss` helper; 'closed keeps expr-error.
  - *Evidence*: typing-core.rkt:411-414 — `(define (miss) (if (eq? (expr-Record-tail rec) 'dyn) (fresh-meta ctx (expr-Type (lzero)) "dyn-row-projection") (expr-error)))`; invoked from the keyword-literal arm :417-418 `(if fld (record-field-type fld) (miss))` and the nat-literal arm :423-424.

**[3.4] (VERIFIED)** (b) D19 mint site 2 — dynamic key on a 'dyn row projects to ⋃knowns ∪ fresh via record-value-bound; the empty dyn row degenerates to the fresh meta alone (union of a singleton).
  - *Evidence*: typing-core.rkt:431-434 — `[(eq? (expr-Record-tail rec) 'dyn) (if (check ctx key key-ty) (record-value-bound ctx rec "dyn-row-dynamic-projection") (expr-error))]`; record-value-bound dyn arm :448-452 — `(build-union-type (cons (fresh-meta ctx (expr-Type (lzero)) src) (map ... (expr-Record-fields rec))))`; comment :429-430: 'the EMPTY dyn row still projects (fresh alone)'.

**[3.5] (VERIFIED)** (b) D19 mint site 3 — record-value-bound's empty-CLOSED arm mints a fresh meta alone (Q6; the old empty→Open arm is dead, record-value-union now ERRORS on empty rows).
  - *Evidence*: typing-core.rkt:456-457 — `[(null? (expr-Record-fields rec)) (fresh-meta ctx (expr-Type (lzero)) src)]`; record-value-union guard :466-467 — `(error 'record-value-union "empty row — callers must guard or use record-value-bound (F1a.2 p2)")`.

**[3.6] (VERIFIED)** (b) UNENUMERATED fourth mint surface — union-record-component-vt: a literal-key miss on a 'dyn COMPONENT of a union-typed access contributes a fresh meta (instead of Q5 filtering), and its dynamic-key arm routes through record-value-bound.
  - *Evidence*: typing-core.rkt:518-519 — `[(eq? (expr-Record-tail rec) 'dyn) (fresh-meta ctx (expr-Type (lzero)) "dyn-row-union-component")]`; dynamic-key arm :529 — `(record-value-bound ctx rec "dyn-row-union-component")` (empty-dyn admitted by the :523-524 guard `(or (pair? ...) (eq? (expr-Record-tail rec) 'dyn))`).

**[3.7] (VERIFIED)** (c) record-<:-map? meta-V REFUSAL leg is at typing-core.rkt:543-548 at HEAD — the historical :490-491 coordinate has DRIFTED (that region is now record-<:-elem?'s union-branch comment).
  - *Evidence*: typing-core.rkt:543-548 — `;; CIU T6 F1a.2 p1a (D16): a meta V may be solved from ⋃fields ONLY for a CLOSED row — a dyn row's remainder is unknown, so ⋃knowns would over-commit; REFUSE (the meta stays unsolved; Q4 display posture).` `[(expr-meta? (whnf vt)) (and (eq? (expr-Record-tail rec) 'closed) (unify-ok? (unify ctx vt (record-value-union rec))))]`. Concrete-V leg :549-554 comment: 'knowns-only IS the C_ConsL absorption for dyn rows (§12.4)'.

**[3.8] (VERIFIED)** (c) record-<:-elem? meta-elem REFUSAL leg (shared by record-<:-pvec?, an alias): meta elem solving from ⋃positions only for CLOSED rows; empty closed row satisfies any elem meta WITHOUT solving it.
  - *Evidence*: typing-core.rkt:482-487 — `[(expr-meta? at*) (and (eq? (expr-Record-tail rec) 'closed) (or (null? (expr-Record-fields rec)) (unify-ok? (unify ctx at (record-value-union rec)))))]` with comments ':480-481 (D16): meta-elem solving from ⋃positions only for CLOSED rows (dyn remainder unknown → refuse; §12.4)' and ':483-484 p1b: the EMPTY closed row satisfies any elem meta WITHOUT solving it'. Alias :499-500 `(define (record-<:-pvec? ctx rec at) (record-<:-elem? ctx rec at))`. Live call sites: typing-core.rkt:3228/:3232/:3237 + qtt.rkt:2718/:2724/:2727.

**[3.9] (VERIFIED)** (c) The PURE sibling record-subtypes-pvec? (subtype-predicate.rkt) has NO meta leg at all — it just andmaps subtype? over fields; likewise record-subtypes-map? is pure and deliberately tail-blind (knowns-only = C_ConsL absorption). All meta refusal logic lives ONLY in the typing-core meta-aware siblings.
  - *Evidence*: subtype-predicate.rkt:160-164 — `(define (record-subtypes-pvec? rec pv) (and (eq? (expr-Record-key-domain rec) 'nat) (let ([at (expr-PVec-elem-type pv)]) (andmap (lambda (f) (subtype? (record-field-type (cdr f)) at)) (expr-Record-fields rec)))))`; record-subtypes-map? :144-155 with comment :139-143 'tail-blindness here is DELIBERATE dyn semantics ... The meta-solving sibling record-<:-map? in typing-core, by contrast, REFUSES to solve a meta V from a dyn row'. Also used from unify.rkt:585-586 (classifier ok-arms).

**[3.10] (VERIFIED)** (d) record-value-bound is defined at typing-core.rkt:446-458 (provided :55) and has 16 call sites in typing-core + 13 in qtt = 29 at HEAD — NOT the historical 6+6.
  - *Evidence*: Definition typing-core.rkt:446 `(define (record-value-bound ctx rec [src "dyn-row-values"]) ...)`. typing-core call sites (grep, HEAD): :433, :529, :1855, :2119, :2137, :2154, :2217, :2262, :2281, :2298, :2380, :2406, :2997, :3008, :3042, :3060 (16). qtt.rkt call sites: :1551, :1577, :1597, :1649, :1673, :1696, :1713, :2447, :2473, :2497, :2556, :2581, :2609 (13).

**[3.11] (VERIFIED)** (e) An unsolved meta displays as `?<id>` (e.g. `?meta42`): pp-expr consults the cell-authoritative solution and falls back to the raw id format; the display path is pp-expr∘freeze in driver.rkt.
  - *Evidence*: pretty-print.rkt:135-139 — `[(expr-meta id cell-id) (let ([sol (meta-solution/cell-id cell-id id)]) (if sol (pp-expr sol names) (format "?~a" id)))]`. Display call sites: driver.rkt:598-599 `(nf (time-phase! zonk (freeze expr)))`, :623 `(pp-expr (time-phase! zonk (freeze ty)))`, :648 `(pp-expr (freeze expr))`.

**[3.12] (VERIFIED)** (f-hook-1) The natural attach point for a type-meta default-to-error policy is default-metas' expr-meta identity arm (zonk.rkt:1053) — the function already hosts three per-domain default policies (level :1052, mult :1084-1085, num-lit-by-origin :1058-1066). CAUTION: freeze is called in NON-display contexts too (stored types driver.rkt:1704, constraint message rendering :1579-1580/:1754-1755, capture-time :1349), so a policy at freeze/default-metas fires beyond REPL display.
  - *Evidence*: zonk.rkt:1040-1046 (freeze = zonk + default-metas; zonk-final alias); driver.rkt:1349 `[else (freeze type)]`, :1579-1580 `(pp-expr (freeze (constraint-lhs c)) names)`, :1704 `(unsolved-metas-to-holes (freeze type*))`.

**[3.13] (VERIFIED)** (f-hook-2) unsolved-metas-to-holes (driver.rkt:196-203) is a post-freeze 'remaining metas unacceptable here' policy precedent for STORED types — but its walk is SHALLOW: it recurses only Pi/Sigma/app/lam, so a D19 meta inside an expr-union (own struct, syntax.rkt:1134) or expr-Record field would SURVIVE the scrub. type-contains-meta? (driver.rkt:210-218) shares the same shallow shape.
  - *Evidence*: driver.rkt:196-203 — match arms `[(expr-meta _ _) (expr-hole)] [(expr-Pi ...)] [(expr-Sigma ...)] [(expr-app ...)] [(expr-lam ...)] [_ e]` — no expr-union/expr-Record arm; syntax.rkt:1134 `(struct expr-union (left right) #:transparent)` confirms unions are not app-encoded. Used at driver.rkt:1704 for the deferred-body stored-type path.

**[3.14] (VERIFIED)** (f-hook-3) Post-fixpoint sweep→error precedent exists for CONSTRAINTS (not type metas): check-unresolved-trait-constraints (trait-resolution.rkt:401) + all-failed-constraints (metavar-store.rkt:1388-1390), consumed at driver.rkt:592/:619/:729/:1555/:1565/:1724/:1738. Error srcloc recovery works only when meta-info-source is a meta-source-info struct — the D19 mint sites pass BARE STRINGS ("dyn-row-projection" etc.), so a source-discriminating error policy would have provenance tags but NO source locations today.
  - *Evidence*: trait-resolution.rkt:352-358 — `(define src (and minfo (meta-info-source minfo))) (define loc (if (and src (meta-source-info? src)) (meta-source-info-loc src) srcloc-unknown))`; typing-core.rkt:413 passes `"dyn-row-projection"` (a string) as source; metavar-store.rkt:1388-1390 `(define (all-failed-constraints) (filter (lambda (c) (eq? (constraint-status c) 'failed)) (read-constraint-store)))`.

**[3.15] (VERIFIED)** (f-precedent) NO annotation-derived-bound machinery exists at HEAD (FLAGGED, searched, not found). Closest precedents: (i) meta-info.type field ('expected type of the solution' — a KINDING slot; D19 mints pass (expr-Type (lzero)), not a checking-context subtype bound); (ii) the postponed-constraint store, which records EQUATIONAL obligations (lhs~rhs+ctx+source), not subtype bounds; (iii) meta-info.constraints field exists but is dead-threaded — only touched by two struct-reconstruction sites, never consumed. DEFERRED.md itself frames 'an annotation-derived bound once the Q_E(b) carrier change lands' as a FUTURE candidate, consistent with absence.
  - *Evidence*: metavar-store.rkt:305-313 meta-info struct — `type ;; expected type of the solution (Expr)`, `constraints ;; (listof any) — empty in Sprint 1, used by Sprint 2`; grep meta-info-constraints → only metavar-store.rkt:2068 + :2136 (copy sites); constraint struct :322-335 (lhs/rhs/ctx/source/status); DEFERRED.md:24-25 'TIGHTEN LATER (candidates: default-to-error at zonk-final, or an annotation-derived bound once the Q_E(b) carrier change lands). Home: F1b.'

**[3.16] (VERIFIED)** (context) DEFERRED.md's F1a.2 tightening entry also directs the same pass to revisit the engine refusal conservatism, matching the facet's (c) surfaces.
  - *Evidence*: DEFERRED.md:26-27 — 'Related: the row-comparison engine REFUSES meta-V solving from dyn rows (design doc §12.3) — the same tightening pass should revisit that conservatism.'

### §3.G Capture gaps

- record-value-bound call-site count has grown far past the prior framing's 'historically 6 typing-core + 6 qtt': at HEAD it is 16 (typing-core.rkt:433,529,1855,2119,2137,2154,2217,2262,2281,2298,2380,2406,2997,3008,3042,3060) + 13 (qtt.rkt:1551,1577,1597,1649,1673,1696,1713,2447,2473,2497,2556,2581,2609) = 29 — the F1a-col tuple/collection consumers (fold/map/filter/to-list/transient, duplicated across infer+check and inferQ+checkQ) multiplied the surface; any tightening that changes record-value-bound's dyn fresh-meta posture touches all 29.
- Coordinate drift: the record-<:-map? meta-V refusal leg is at typing-core.rkt:546-548 at HEAD, not the historical :490-491 (that region is now record-<:-elem?'s union-branch logic :488-497).
- The D19 mint-leg enumeration (literal-miss / dynamic-key / empty-dyn) missed a FOURTH mint surface: union-record-component-vt — literal-miss on a dyn component (typing-core.rkt:518-519, source "dyn-row-union-component") + its dynamic-key arm (:529). A meta-refusal/default-to-error tightening that keys on provenance strings must include this source tag.
- The tightening framing treats 'display of unsolved metas' as one surface, but HEAD has TWO divergent postures: REPL display leaves `?N` (pp-expr∘freeze, pretty-print.rkt:139), while stored types scrub metas to holes (unsolved-metas-to-holes, driver.rkt:1704) — and that scrub (plus type-contains-meta?, driver.rkt:210-218) is SHALLOW (no expr-union/expr-Record recursion), so D19 metas embedded in ⋃knowns∪?m unions or record field types survive into stored types. A default-to-error design has to reconcile both postures and the shallow-walk gap.
- D19 mint sites pass bare provenance STRINGS as meta-info source (typing-core.rkt:413 "dyn-row-projection"), while the existing sweep→error precedent (trait-resolution.rkt:353-358) recovers srclocs only from meta-source-info structs — a Q4 default-to-error at zonk-final would today produce errors with srcloc-unknown unless the mint sites are upgraded to structured sources.
- freeze/zonk-final is NOT display-only: it runs in stored-type registration (driver.rkt:1704), constraint error-message rendering (:1579-1580, :1754-1755), and capture-time zonking (:1349) — the DEFERRED.md candidate 'default-to-error at zonk-final' would fire in all these contexts, not just at output; the enumeration under-counts the blast radius of that hook.

### §3.R R-lens targets

- zonk.rkt:1053 — the `[(expr-meta _ _) e]` pass-through arm in default-metas (the load-bearing 'no type-domain defaulting' fact; one-line grep confirms)
- typing-core.rkt:546-548 — the record-<:-map? meta-V refusal leg at its DRIFTED coordinate (grep 'REFUSE' in typing-core.rkt)
- typing-core.rkt:518-519 + :529 — the unenumerated union-record-component-vt D19 mint surface (grep 'dyn-row-union-component')
- driver.rkt:196-203 + :1704 — unsolved-metas-to-holes' shallow walk and its stored-type consumer (verify no expr-union/expr-Record arm before relying on the scrub in the tightening design)
- metavar-store.rkt:2068 + :2136 — the only two meta-info-constraints touchpoints (verify the field really has no consumer before ruling out annotation-derived-bound precedent)
- typing-core.rkt:413 — fresh-meta source is a bare string at the D19 projection mint (verify before designing a source-discriminating or srcloc-carrying error policy)

## §4 Facet: presence-marks

**[4.0] (VERIFIED)** (a) The presence slot exists: record-field is a two-field transparent struct (type presence), with 'optional/'absent/'unknown explicitly reserved for F1b-era work.
  - *Evidence*: syntax.rkt:662 "(struct record-field (type presence) #:transparent)"; syntax.rkt:660-661 comment: "F1a writes presence='present only ('optional | 'absent | 'unknown reserved per D6 for schema-optional-keys / narrowing / presence-poly)"

**[4.1] (VERIFIED)** (a) Exactly 6 production record-field construction sites exist. 3 HARDCODE 'present: record-extend (syntax.rkt:691 "(record-field field-type 'present)"), heterogeneous list-literal 'nat row (typing-core.rkt:1940 "(cons i (record-field t 'present))"), heterogeneous pvec-literal 'nat row (typing-core.rkt:1976, same shape). 3 THREAD presence through: record-map-field-types (syntax.rkt:671-672 "(record-field (proc (record-field-type (cdr fld))) (record-field-presence (cdr fld)))"), pvec-map tuple rebuild (typing-core.rkt:2159-2160 "(record-field w (record-field-presence (cdr p)))"), map-map-vals record rebuild (typing-core.rkt:2305-2306, same shape). No struct-copy sites exist (grep exit 1).
  - *Evidence*: syntax.rkt:671-672, 691; typing-core.rkt:1940, 1976, 2160, 2306; grep '(record-field ' across racket/prologos production files returned only these + tests

**[4.2] (VERIFIED)** (a-adjacent) Two additional rebuild sites move whole (label . record-field) pairs WITHOUT reconstructing record-field — presence-transparent by construction: pvec-concat index-shift and pvec-slice renumber.
  - *Evidence*: typing-core.rkt:2069 "(cons (+ len1 (car f)) (cdr f))" (concat); typing-core.rkt:2104 "(cons (- (car f) lo-n) (cdr f))" (slice)

**[4.3] (VERIFIED)** (a) The elaborator mints only EMPTY-fields rows directly (no record-field construction): empty {} literal seeds (expr-Record 'keyword '() 'dyn), all-keyword literal seeds (expr-Record 'keyword '() 'closed) then grows via expr-map-assoc chain -> typing-core record-extend (which hardcodes 'present). Test constructions (test-record-node.rkt:28,41,42,46; test-dyn-rows.rkt:33) all write 'present.
  - *Evidence*: elaborator.rkt:2135 "(expr-Record 'keyword '() 'dyn)"; elaborator.rkt:2143 "(expr-Record 'keyword '() 'closed)"; typing-core.rkt:1640 "(record-extend rec kw vt)"

**[4.4] (VERIFIED)** (b) Exactly 3 reads of record-field-presence exist in production, ALL pass-through preservation in rebuilds — ZERO semantic readers. Display does NOT read presence at all: the pretty-print expr-Record arm formats only record-field-type. Unification explicitly excludes it by design comment.
  - *Evidence*: grep record-field-presence: syntax.rkt:672, typing-core.rkt:2160, typing-core.rkt:2306 only. pretty-print.rkt:435-445 reads only "(record-field-type (cdr fld))". unify.rkt:594-595: "Zip ONLY record-field TYPES; `presence` is not an expr and must never become a unification goal."

**[4.5] (INFERRED)** (b-implication) Because record-field is #:transparent, equal? on rows IS presence-sensitive — the unify ground-identical equal? fast-path (referenced at unify.rkt:590-591 "Ground-identical records already short-circuit at the equal? fast-path above") would stop short-circuiting for rows differing only in presence once non-'present values exist; conversely union-sort-key (union-types.rkt:92) keys on field TYPE only, ignoring presence.
  - *Evidence*: syntax.rkt:662 #:transparent; unify.rkt:590-591 comment; union-types.rkt:92 "(format \"~a=~a\" (car fld) (union-sort-key (record-field-type (cdr fld))))"

**[4.6] (VERIFIED)** (c) D20 drop-all-facts site 1 — dynamic-key dissoc: the arm keeps NO fields, returning the empty dyn row; its comment explicitly names presence marks as the F1b refinement. Site 2 — dynamic update-in on a record: same empty-dyn-row result.
  - *Evidence*: typing-core.rkt:1814-1819: "a dynamic-key removal may have deleted ANY known field — keeping the fields would be unsound; degrade to the empty dyn row {| _} (presence marks = F1b)" -> :1819 "(make-record (expr-Record-key-domain rec) '() 'dyn)". typing-core.rkt:1590-1597: "a dynamic deep-update on a record may have changed any field, so the only sound result is the empty dyn row {| _}" -> :1597 "(make-record (expr-Record-key-domain rec) '() 'dyn)"

**[4.7] (INFERRED)** (c) What 'unknown would change, grounded in the two arms' OWN soundness arguments: at the dissoc site only PRESENCE is uncertain (a surviving field keeps its type), so labels+types could be retained with per-field presence='unknown instead of dropping to '(). At the update-in site the applied fn may have TRANSFORMED any field's VALUE (comment: "may have changed any field"), so field TYPES are also uncertain — a presence-only mark recovers strictly less there. This is a semantic asymmetry between the two D20 sites the design should treat separately.
  - *Evidence*: typing-core.rkt:1815-1817 (dissoc: deletion-only uncertainty) vs typing-core.rkt:1591-1593 (update-in: value-transform uncertainty)

**[4.8] (VERIFIED)** (d) record-field serializes via the fully GENERIC struct->vector path; the reg2! registration's args '(expr-Nat) 'present' are dummy values used ONLY to create a sample instance for tag extraction. Deserialization is generic apply-ctor. Interned symbols pass through serialization untouched. Therefore a new presence VALUE ('optional/'absent/'unknown — all interned symbols) round-trips with ZERO serializer changes, no new registration, and no PNET_VERSION bump mechanically required.
  - *Evidence*: pnet-serialize.rkt:221 "(reg2! record-field (expr-Nat) 'present)"; :168-170 reg2! definition (tag from dummy instance, stores bare ctor); :110-111 generic struct serialization "(for/vector ([e (in-vector (struct->vector v))]) (deep-s->v e))"; :84 interned symbols returned as-is; :457 "(apply ctor fields)"

**[4.9] (VERIFIED)** (d) Stale-cache semantics are already covered without version churn: infrastructure-stale? invalidates every .pnet whose mtime predates compiled/driver_rkt.zo — landing presence-marks code (recompiling driver) auto-regenerates all caches by timestamp. PNET_VERSION is currently 2 (bumped 1->2 at F1a.2 p1b in the same commit as the Open mint-flip); pnet-stale? checks version + source mtime-hash + infrastructure timestamp.
  - *Evidence*: pnet-serialize.rkt:521-525 "(> (file-or-directory-modify-seconds driver-zo-path) (file-or-directory-modify-seconds pnet-path))"; :71 "(define PNET_VERSION 2)"; :527-538 pnet-stale?; :536 version check

**[4.10] (VERIFIED)** (e) Smart constructors: make-record (syntax.rkt:678-685) is presence-AGNOSTIC (takes pre-built (label . record-field) pairs; last-write-wins dedup at :680 + canonical sort). record-extend HARDCODES 'present (:691). record-remove (:699-702) filters pairs — presence-preserving for survivors. record-lookup-field (:695-696) returns the whole record-field (presence reachable) but every caller reads only -type (typing-core.rkt:418, 424, 514, 1763). record-map-field-types (:667-673) threads presence.
  - *Evidence*: syntax.rkt:678-702; typing-core.rkt:418 "(if fld (record-field-type fld) (miss))"

**[4.11] (VERIFIED)** (e-implication) record-extend on an EXISTING label replaces the whole pair and resets presence to 'present (make-record's right-priority last-write-wins) — under activation, any assoc-overwrite of an 'optional/'unknown field silently promotes it to 'present. Every keyword-record growth path funnels through record-extend (map-assoc :1640, pvec-push :1991, pvec-update via :2023).
  - *Evidence*: syntax.rkt:680 "(hash-set! ht (car f) f))  ;; last write wins" + :690-691 "new field LAST → wins on collision" with 'present; typing-core.rkt:1640, 1991, 2023 record-extend callers

**[4.12] (VERIFIED)** (f) The schema side has NO optional-keys notion today: schema-field is (keyword type-datum default-val check-pred) — no presence/optional slot; the only field-level properties parsed are :default and :check; grep for :optional across production code hits ONLY the reserved comment at syntax.rkt:661. De-facto near-optionality exists solely via :default — inject-schema-defaults appends missing defaulted fields into brace-params at elaboration (FILL semantics, not presence semantics). schema-entry does carry closed? (:765), a tail-adjacent notion.
  - *Evidence*: macros.rkt:758 "(struct schema-field (keyword type-datum default-val check-pred) #:transparent)"; macros.rkt:852-867 parse-field-properties (only ':default / ':check arms, else stop); macros.rkt:1193-1209 inject-schema-defaults; macros.rkt:765 "(struct schema-entry (name fields closed? srcloc))"

### §4.G Capture gaps

- The facet framing expected presence reads to be '~none / display only' — the DISPLAY half is wrong: pretty-print.rkt:435-445 never reads record-field-presence; the 3 reads that exist are thread-through rebuilds (syntax.rkt:672, typing-core.rkt:2160, 2306). A dyn row's tail displays (' | _' at pretty-print.rkt:442) but presence has NO display surface at all — activation needs to decide one.
- The construction enumeration frame (record-field constructor calls) under-counts presence-CARRYING dataflow: pvec-concat (typing-core.rkt:2069) and pvec-slice (typing-core.rkt:2104) move (cdr f) pairs wholesale and would silently propagate non-'present marks; record-remove and make-record likewise. These are presence-correct by accident, not by design review.
- record-extend's overwrite semantics (syntax.rkt:680, 691) resets an existing field's presence to 'present on assoc-overwrite — a semantic decision point for activation the design doc's smart-constructor discussion should own explicitly.
- equal?-sensitivity: record-field is #:transparent, so unify's ground-identical equal? fast-path (unify.rkt:590-591) and any equal?-keyed dedup become presence-DISTINGUISHING the moment two rows differ only in marks, while union-sort-key (union-types.rkt:92) ignores presence — a potential inconsistency class not in the F1b enumeration.
- typing-errors.rkt:157 uses record-lookup-field for missing-field diagnostics — the error-message surface would need presence-awareness ('optional miss vs hard miss) once marks activate; not in the enumeration.
- Schema alignment: the existing bit closest to 'optional is schema-field-default-val (fill-or-error via inject-schema-defaults macros.rkt:1193-1209); if F1b's seal-as-tabulation maps schema-entries to rows, whether a :default field seals as 'present (filled) or 'optional is an open design point with no current code anchor.

### §4.R R-lens targets

- syntax.rkt:662 + :691 — the presence slot + the single hardcoded-'present growth funnel (record-extend); grep '(record-field ' to confirm the 6-site enumeration still holds before designing
- typing-core.rkt:1814-1819 and :1590-1597 — the two D20 empty-dyn-row arms; note the asymmetry (dissoc = presence-only uncertainty; update-in = type+presence uncertainty) before choosing an 'unknown refinement
- unify.rkt:594-604 — the B3 record-vs-record arm zips ONLY types and its comment PINS 'presence must never become a unification goal'; presence-marks activation must either honor or explicitly amend this pin
- pnet-serialize.rkt:521-525 + :71 — infrastructure-stale? timestamp invalidation makes a PNET_VERSION bump mechanically unnecessary for new presence values; verify the driver-zo-path timestamp assumption holds in the batch-worker context if the design relies on skipping the bump
- macros.rkt:758 + :852-867 + :1193-1209 — schema-field has NO :optional; :default is elaboration-time FILL (inject-schema-defaults); the schema->presence mapping for seal-as-tabulation has no existing code anchor

## §5 Facet: solve-solutions

**[5.0] (VERIFIED)** (a) Solution-set reification happens in answers->prologos-expr: each answer becomes an expr-champ (CHAMP map) keyed by expr-keyword of the query-var symbol, and the set is a cons/nil Prologos List built by racket-list->prologos-list. A solution's runtime value IS a Map (expr-champ), keyed by keywords derived from logic-var names.
  - *Evidence*: racket/prologos/reduction.rkt:234-252 — '(define (answers->prologos-expr answers query-vars [bound-args '()]) (racket-list->prologos-list (for/list ([answer (in-list answers)]) ... (champ-insert c (equal-hash-code key) key pval))) ... (expr-champ champ-with-bound))' with key '(expr-keyword qv)' at :242; list builder reduction.rkt:77-81 folds '(expr-app (expr-app (expr-fvar 'cons) e) acc)'

**[5.1] (VERIFIED)** (a) run-solve-goal (reduction.rkt:521-589) is the reduction-level solve entry, invoked from nf at reduction.rkt:3160-3161 ('[(expr-solve goal) (run-solve-goal goal default-solver-config)]'). For expr-goal-app it calls stratified-solve-goal whose contract is '(listof hasheq) — each maps query var names to values' (stratified-eval.rkt:171), then answers->prologos-expr with bound-args at :533-534.
  - *Evidence*: reduction.rkt:527-534 — '(define-values (goal-args query-vars) (extract-query-info rel-args)) ... (stratified-solve-goal config store rel-name goal-args query-vars) ... (define bound-args (compute-bound-args-for-relation rel-name goal-args query-vars)) (answers->prologos-expr answers query-vars bound-args)'; stratified-eval.rkt:171 comment

**[5.2] (VERIFIED)** (a) All three solver strategies produce the uniform (listof hasheq keyed by query-var symbol) answer shape before reification: DFS solve-goal projects query vars at relations.rkt:1485-1487 ('(for/hasheq ([qv (in-list query-vars)]) (values qv (walk* subst qv)))'); propagator path dissolve-solver-pu at relations.rkt:2769-2774 and :2799-2803 ('(for/hasheq ([qv (in-list query-vars)]) ... (values qv (if (eq? val scope-cell-bot) qv val)))' — unresolved var maps to its own symbol); tier-1 fast path 'Returns: (listof hasheq)' (relations.rkt:2821).
  - *Evidence*: relations.rkt:1485-1487, :2769-2774, :2799-2803, :2821

**[5.3] (VERIFIED)** (b) relations.rkt:1377 and :1730 are NOT the display suffix — they are clause-variable FRESHENING for solver-internal renaming, producing names like x_g12345 (gensym text included), used via fresh-map/rename-goal-vars and never surfacing as answer keys.
  - *Evidence*: relations.rkt:1375-1379 — '(define (freshen name) (define key (string->symbol (format "~a_~a" name (gensym)))) (hash-set! fresh-map name key) key)'; identical shape at :1729-1732 in explain path

**[5.4] (VERIFIED)** (b) The ACTUAL underscore-key construction is compute-bound-args-for-relation (reduction.rkt:346-368): for each GROUND goal-arg position, it takes the RELATION'S DECLARED PARAM NAME (param-info-name, first variant only) and appends a single '_'. These pairs are merged into the same champ as query-var keys by answers->prologos-expr :245-251 ('Add bound (ground) args with _ suffix param names') and by run-solve-one-goal :615-621.
  - *Evidence*: reduction.rkt:366-368 — '(cons (string->symbol (string-append (symbol->string (param-info-name pi)) "_")) arg)'; champ merge :246-251 — '(define key (expr-keyword (car ba))) (define pval (ground->prologos-expr (cdr ba))) (champ-insert c ...)'

**[5.5] (VERIFIED)** (b) The function-narrowing analogue compute-bound-args (reduction.rkt:317-341) does the same '_' append using the defn param-name registry ('(string->symbol (string-append (symbol->string pn) "_"))' at :339-341), consumed by the narrow path at reduction.rkt:517-518. Any key-dropping design decision spans solve (:533), solve-one (:607), AND narrowing (:517) for uniformity.
  - *Evidence*: reduction.rkt:339-341, :517-518 — '(define bound-args (compute-bound-args func-name args-whnf)) (answers->prologos-expr solutions var-names bound-args)'

**[5.6] (VERIFIED)** (b/d) The underscore is in the RUNTIME VALUE'S keys, not a display transformation. pretty-print prints champ maps faithfully: expr-champ arm at pretty-print.rkt:448-459 joins '~a ~a' per entry; expr-keyword prints as ':~a' (pretty-print.rkt:420); the surrounding list prints as '[...] via the cons-chain arm (pretty-print.rkt:227 '(format "'[~a]" ...)'). There is NO display-side suffix logic anywhere.
  - *Evidence*: pretty-print.rkt:420 — '[(expr-keyword name) (format ":~a" name)]'; :448-459 champ arm; :227 cons-chain '[...]' format

**[5.7] (VERIFIED)** (c) At HEAD, a solve result carries NO type — expr-solve infers expr-hole in typing-core ('Solve/Explain → type-unsafe (hole)') and QTT mirrors it. No (List (Map Keyword ?)), no structural row, no meta is minted for solve results. Same for solve-with, solve-one, explain, and narrow.
  - *Evidence*: typing-core.rkt:2728-2729 — ';; Solve/Explain → type-unsafe (hole)' / '[(expr-solve g) (infer ctx g) (expr-hole)]' (+ :2730-2741 solve-with/solve-one/explain, :2744-2748 narrow); qtt.rkt:2149-2151 — '[(expr-solve g) (let ([r (inferQ ctx g)]) (match r [(tu _ u) (tu (expr-hole) u)] ...))]'; elaborator.rkt:3040-3044 wraps surf-solve → expr-solve with no type minting

**[5.8] (VERIFIED)** (d) Change-surface split: dropping ground-var keys touches ONLY the runtime value surface — remove/alter the bound-args merge (reduction.rkt:246-251, :616-621) or the compute-bound-args-for-relation call sites (:533, :607). Display needs no change (faithful printer). Type is untouched TODAY because expr-solve is a hole — the type surface only becomes involved if F1b newly mints a row type for solution maps (nothing to modify, only to add).
  - *Evidence*: reduction.rkt:246-251, :533, :607, :616-621; typing-core.rkt:2729 (hole)

**[5.9] (VERIFIED)** (e) The ground/free distinction exists at TWO points. Query build: extract-query-info (reduction.rkt:287-299) — '(cond [(expr-logic-var? a*) ... (set! query-vars (cons name query-vars))] [else (set! goal-args (cons a* goal-args))])' — logic vars enter goal-args as bare NAME SYMBOLS and query-vars; ground args enter goal-args as whnf'd exprs. Bound-arg computation: compute-bound-args-for-relation filters '#:when (not (symbol? arg))' (reduction.rkt:363) — symbols = logic vars per the above — plus a second guard '#:when (not (and (expr-logic-var? arg) (memq (expr-logic-var-name arg) query-vars)))' (:364-365).
  - *Evidence*: reduction.rkt:290-298, :360-365

**[5.10] (INFERRED)** (e) The second guard at reduction.rkt:364-365 appears redundant on the run-solve-goal path (logic vars arrive as symbols, already excluded by :363) — possibly defensive for other callers. Not traced to a caller where expr-logic-var structs reach this function.
  - *Evidence*: reduction.rkt:293-295 (logic vars pushed as name symbols) vs :364-365 (expr-logic-var? check)

**[5.11] (VERIFIED)** Unresolved query vars in answers display as the var name (symbol → expr-fvar via ground->prologos-expr :260), while a query var MISSING from the answer hasheq displays as 'none' ('(define pval (if val (ground->prologos-expr val) (expr-fvar 'none)))' reduction.rkt:243). Two distinct 'no value' presentations exist in solution maps.
  - *Evidence*: reduction.rkt:241-243, :257-260; relations.rkt:2774 ('(values qv (if (eq? val scope-cell-bot) qv val))')

**[5.12] (VERIFIED)** Bound-args silently vanish (return '()) on arity mismatch between params and goal-args, and use ONLY the first variant's params for relations — multi-variant relations with different param names get variant 1's names or nothing.
  - *Evidence*: reduction.rkt:330-331 ('[(not (= (length param-names) (length args-whnf))) '()]'), :356-358 ('(define params (variant-info-params (car variants))) ... [(not (= (length params) (length goal-args))) '()]')

**[5.13] (VERIFIED)** The anonymous-rel, unify-goal, and is-goal arms of run-solve-goal already pass '() as bound-args — 'no ground-param keys' is ALREADY the behavior there; only the named goal-app arms of solve/solve-one (and narrowing) add underscore keys.
  - *Evidence*: reduction.rkt:550 ('(answers->prologos-expr answers query-vars '())'), :576, :587

### §5.G Capture gaps

- Owner framing drift: the prompt describes the ':known_' keys as 'ground LOGIC-VAR keys' — the code shows they are the RELATION'S DECLARED PARAMETER NAMES (param-info-name from the defr, reduction.rkt:367) at ground ARGUMENT positions, holding the caller-supplied ground values. There is no notion of a 'ground logic var' at this boundary — extract-query-info (reduction.rkt:293) treats every expr-logic-var as free. A design that 'drops ground-var keys' is really dropping the bound-args echo feature ('so the user can see which concrete values were bound to which parameters', reduction.rkt:307-309 comment).
- The prompt's hypothesis that :1377/:1730 might be the display suffix is disconfirmed — those are gensym freshening; the real construction lives in reduction.rkt (compute-bound-args :339-341 and compute-bound-args-for-relation :366-368), a file the facet framing didn't name for surface (b).
- solve-one shares the underscore merge via a SEPARATE inline champ build (reduction.rkt:606-622), not via answers->prologos-expr — a change to answers->prologos-expr alone would leave solve-one's keys intact; both sites (plus narrowing at :517) must be enumerated in the design.
- The type surface is emptier than the framing assumed: no '(List (Map Keyword ?)) / rows / metas' exist for solve at HEAD — expr-solve is a bare hole in both typing-core and qtt. Path-Selection-over-solutions ergonomics therefore currently rides entirely on the RUNTIME champ value; any typed story is net-new F1b minting, and the ground-key question changes what row that new type would have to state.
- Multi-variant relations: bound-arg param names come from (car variants) only, and arity mismatch silently yields no bound keys (reduction.rkt:356-358, :330-331) — the current feature is already partial; worth surfacing in co-design since dropping the keys also deletes these edge cases.
- Two distinct 'unbound' representations coexist in solution maps (unresolved var → its own name as fvar; missing key → 'none', reduction.rkt:243 vs relations.rkt:2774) — relevant if solution rows get typed, since row value-types must cover both.

### §5.R R-lens targets

- reduction.rkt:346-368 — compute-bound-args-for-relation: the underscore-key construction (grep 'string-append (symbol->string (param-info-name' to confirm)
- reduction.rkt:234-252 — answers->prologos-expr: champ build + bound-args merge into the SAME map as query-var keys
- reduction.rkt:606-622 — run-solve-one-goal's separate inline bound-args merge (second site a key-drop must touch)
- typing-core.rkt:2728-2735 + qtt.rkt:2149-2161 — expr-solve/solve-one → expr-hole (no row/Map type minted for solve at HEAD)
- reduction.rkt:287-299 — extract-query-info: the only ground/free split (logic vars → name symbols + query-vars; ground → whnf'd exprs)
- reduction.rkt:517-518 — narrowing path uses compute-bound-args with the same '_' suffix (third site for uniformity)

## §6 Facet: rank2-hof-stdlib

**[6.0] (VERIFIED)** Prelude bare-name HOF surface (map/filter/reduce/length/any?/all?/find/to-list/etc.) is the GENERIC HKT trait-dispatched module, requiring (Seqable C)/(Buildable C)/(Reducible C) dicts keyed on the container type head — a row flowing here occurs both as container (row-list, C-position) and element (A := record, callback-domain position).
  - *Evidence*: namespace.rkt:841-849 '(imports [prologos::core::collections :refer [map filter reduce reduce1 length concat any? all? to-list find take drop ...]])' + core/collections.prologos:33 '(spec map {A B : Type} {C : Type -> Type} (Seqable C) -> (Buildable C) -> (-> A B) -> (C A) -> (C B))', :57 '(spec length {A : Type} {C : Type -> Type} (Reducible C) -> (C A) -> Nat)'

**[6.1] (VERIFIED)** Rank-1 prenex HOF families where a record instantiating the element variable lands in the callback's (monotype) domain — the width-subsumption-at-rank-2 exposure class: data/list.prologos foldr:20, reduce:28, map:46, filter:53, any?:128, all?:137, find:145, concat-map:207, take-while:239, drop-while:248, partition:269, zip-with:277, merge:334, sort:344, reduce1:359, foldr1:374, span:407, break:421, dedup:439, prefix-of?:447, suffix-of?:459, delete:465, find-index:487, count:501, scanl:511, iterate-n:519, sort-on:535; core.prologos compose:19, apply:24, flip:29, on:35; core/pvec.prologos pvec-any?:119, pvec-all?:124, pvec-zip-with:145; core/set.prologos set-map:105, set-any?:110, set-all?:115; core/generic-ops.prologos gmap:31, gfilter:36, gfold:41, gany?:56, gall?:61, seq-any?:97, seq-all?:107, seq-find:117; data/lseq-ops.prologos lseq-map:30, lseq-filter:37 (+ lseq-fold).
  - *Evidence*: data/list.prologos:46 'spec map [A -> B] [List A] -> List B'; :535 'spec sort-on [B -> B -> Bool] [A -> B] [List A] -> List A'; core/generic-ops.prologos:41 'spec gfold {A B : Type} {C : Type -> Type} [Foldable C] -> [A -> B -> B] -> B -> [C A] -> B'

**[6.2] (INFERRED)** NO stdlib spec declares a callback whose domain is literally Map- or Record-typed; the records-consuming-callback risky class arises only via instantiation. Nearest literal V-consuming cases (V := record when maps-of-records flow): map-merge-with '[V V -> V]' core/lattice.prologos:128, map-lattice-leq '[V V -> Bool]' core/lattice.prologos:138 (both prelude via :refer-all namespace.rkt:739), map-filter-vals '[-> V Bool]' core/map.prologos:48 (prelude namespace.rkt:769). The key-extractor class (sort-on data/list.prologos:535, on core.prologos:35) is the Path-Selection-over-solution-sets ergonomic shape (record consumed by the projection callback).
  - *Evidence*: core/lattice.prologos:128 'spec map-merge-with {K V : Type} [V V -> V] [Map K V] [Map K V] -> [Map K V]'; grep for Map-typed callback domains across lib/prologos found only direct Map arguments, no [[Map ...] -> ...] callbacks

**[6.3] (VERIFIED)** Known-safe set (Record arms in typing-core, INFER mode): map-fold-entries :2259-2265 (uniform view K=Keyword, V=record-value-bound), map-filter-entries :2279-2284 (result DEGRADES to Map Keyword ⋃), map-map-vals :2295-2325 (label-preserving rebuild), pvec-to-list :2118 ((List ⋃) escape hatch), pvec-fold :2135, pvec-map :2153 (position-preserving constant-W), pvec-filter :2216 ((PVec ⋃)), transient :2380/:2406. CHECK mode: pvec-fold :2994, pvec-map :3007, map-fold-entries :3038, map-map-vals :3058. Everything else in the stdlib enumeration relies on generic HM instantiation plus the α arms.
  - *Evidence*: typing-core.rkt:2259 '[(? expr-Record? rec)' under ';; map-fold-entries' with '(record-value-bound ctx rec "dyn-row-fold")'; :3038-3043 the check-mode mirror; :2153 closed-nat-row? arm under pvec-map

**[6.4] (VERIFIED)** The α (record satisfies expected container) has three reach points with DIFFERENT rank behavior: (1) check's conversion fallback — TOP-LEVEL ONLY (typing-core.rkt:3227-3237, Map/PVec/List vs Record); (2) unify's classifier — fires at ANY decomposed sub-problem including Pi domains, in EITHER argument ordering but with FIXED direction record→container (unify.rkt:582-586), justified by the comment 'In F1a a record is ALWAYS the inferred/value side and the Map the expected side' (unify.rkt:577-578) — an assumption that is untracked at rank-2 Pi-decomposed positions, making the classify arm variance-BLIND there; (3) the pure subtype walk — top level subtype-predicate.rkt:126/128, covariant components :210/:212, but CONTRAVARIANT components flip arguments (:230 '[(-) (structural-subtype-ground? domain c2 c1)]') with Pi domain declared contravariant (ctor-registry.rkt:435 "#:component-variances '(= - +)"), so the flipped check needs Map<:Record which has NO arm → #f at :234. Net: rank-2 contravariant record positions have no α in the pure walk and a variance-blind α in unify.
  - *Evidence*: unify.rkt:582 '[(and (expr-Record? a) (expr-Map? b) (record-subtypes-map? a b)) '(ok)]' + :583 the flipped-ordering twin; subtype-predicate.rkt:230 contravariant flip; ctor-registry.rkt:435 Pi variances

**[6.5] (VERIFIED)** Record-vs-Record unification admits NO width subsumption: B3 arm requires SAME key-domain + SAME tail + SAME label-set (unify.rkt:597-604), the dyn C_Cons arm requires a 'dyn tail with closed-side label containment (unify.rkt:617-631), and 'A DIFFERENT label-set falls through to [else (conv)]' (unify.rkt:591-592). subtype-predicate.rkt has no record<:record judgment ('This is the record→Map bridge, NOT a record<:record judgment (D11 preserved)' :124). D11 holds at HEAD — the F1b erasure-mode width work has a clean insertion boundary.
  - *Evidence*: unify.rkt:597-601 guards 'eq? key-domain', 'eq? tail', 'equal? (map car fields)'; subtype-predicate.rkt:124 quote above

**[6.6] (VERIFIED)** S10-at-list-level value-stall pin locations: typing-core.rkt:2116-2117 (pvec-to-list arm: 'a row-typed cons list expands the pinned S10-at-list-level value-stall surface; (List ⋃) keeps to-list the escape hatch'); tests/test-record-collections.rkt:157-163 (type-only pin — 'length over a row-list types Nat; the VALUE may print as a stuck term when implicit/dict resolution can't see a row instance head (S10 posture at the list level; CIU T3/T5 turf)') and :198-203 ((List ⋃) value-reduces fine — the stall is ROW-lists only); design doc 2026-07-06_CIU_T6_F1_STRUCTURAL_RECORDS_DESIGN.md:36 ('KNOWN v1 LIMIT: generic folds over row-lists type-sound but value-stalls (S10-at-list-level; CIU T3/T5 home; α = escape hatch)'); handoff 2026-07-16_CIU_T6_F1a2_close_handoff.md:58.
  - *Evidence*: typing-core.rkt:2116-2117 and test-record-collections.rkt:157-163 quoted above

**[6.7] (INFERRED)** The S10 pin BOUNDS this audit partially: for the prelude bare-name surface (the collections generics), dict resolution on a row container head stalls the VALUE regardless of any rank-2 typing outcome — S10 gates BEFORE the subsumption boundary. The rank≤2 boundary is independently live for three classes S10 does not cover: (i) non-shadowed rank-1 named HOFs (foldr/sort/sort-on/zip-with/scanl/partition etc.) where records-as-elements meet callback domains, (ii) width mismatch between a row element and a callback expecting a NARROWER row (the D11 gap, contravariant position — no α, no width), (iii) transducers (already rank-2 types).
  - *Evidence*: test-record-collections.rkt:157-163 (the stall is at implicit/dict resolution) + core/collections.prologos:33-97 (dict-first specs) + unify.rkt:591-592 (different label-set → conv fallthrough)

**[6.8] (FLAGGED)** OPEN ITEM (flag, not guess): the runtime MECHANISM of the S10-at-list-level stall is attributed to implicit/dict resolution only by the test comment (test-record-collections.rkt:158-160); I did not trace the evaluator, and I did not trace how '(C A)' unifies with a row type such that 'length xs' still TYPES as Nat in the col-2 pin. Also unverified: full checkQ (qtt.rkt) Record-arm parity with typing-core — only qtt.rkt:2727 (record-<:-elem? call) was spot-verified; the design doc claims 'BOTH checkers' (F1a-s3 tracker row) but I did not enumerate the qtt arms.
  - *Evidence*: test-record-collections.rkt:158-160 comment; qtt.rkt:2727 '(record-<:-elem? ctx rec la)'

**[6.9] (VERIFIED)** Trait-method dict types are rank-2-SHAPED (Pi-over-Type nested inside the dict value): Foldable fold, Reducible reduce, Functor fmap (core/collection-traits.prologos:46-73). Dispatch keys on the container constructor C, not the element, so records-as-ELEMENTS pass through dict dispatch; records-as-CONTAINERS hit the S10 no-Record-instance-head posture (design doc :240: 'record types match no Map-headed instance head in F1a — expr->impl-key-str has no Record case').
  - *Evidence*: core/collection-traits.prologos: 'fold : [Pi [A :0 <Type>] [Pi [B :0 <Type>] [-> [-> A [-> B B]] [-> B [-> [F A] B]]]]]' under 'trait Foldable {F : Type -> Type}'

### §6.G Capture gaps

- The facet framing locates 'map-fold-entries/map-map-vals consumers' in the stdlib — they are NATIVE parser keywords, not stdlib specs (namespace.rkt:757-760: 'map-fold-entries, map-filter-entries, map-map-vals are now native parser keywords — no need to import'); the stdlib only WRAPS them (map-filter-vals core/map.prologos:47-50, map-merge core/map.prologos:64-67, map-merge-with core/lattice.prologos:128-133). The rank-2 audit surface for these is typing-core arms, not .prologos specs.
- PRELUDE SHADOWING under-counted by the facet framing: the prelude's LAST import (namespace.rkt:835-849, 'MUST BE LAST — shadowing depends on ordering') replaces bare map/filter/reduce/reduce1/length/concat/any?/all?/find/take/drop/to-list/head/first/second/rest with GENERIC trait-dispatched versions from core/collections.prologos:33-97 ('(spec map {A B : Type} {C : Type -> Type} (Seqable C) -> (Buildable C) -> (-> A B) -> (C A) -> (C B))'). Any F1b claim about 'map over a solution set' in a prelude namespace hits the HKT generics + S10 dict resolution, NOT data::list map. The rank-1 data::list HOFs survive only under non-shadowed names (foldr, sort, sort-on, zip-with, scanl, dedup, span, break, partition, etc., namespace.rkt:711-722).
- Transducers are ALREADY genuinely rank-2 in-tree — data/transducer.prologos:31/37/43/57/79/86/94 carry explicit Pi-over-Type argument types ('spec transduce {A B R : Type} <(S :0 Type) -> [S -> B -> S] -> S -> A -> S> [R -> B -> R] R [List A] -> R'). The §3b 'rank≤2 boundary' discussion has a concrete existing instance class to name; not prelude-imported (absent from namespace.rkt:684-849), so exposure is qualified-import only.
- book/* chapters DUPLICATE every HOF spec (book/lists.prologos:45-536 mirrors data/list.prologos:20-535; book/maps.prologos, book/lattices.prologos, book/persistent-vectors.prologos, book/characters-and-strings.prologos likewise). Not prelude-imported. Any F1b spec-surface change must touch both or knowingly skip the book mirrors.
- Check-mode Record-arm ASYMMETRY not in prior framing: pvec-fold/pvec-map/map-fold-entries/map-map-vals have explicit check-mode Record arms (typing-core.rkt:2994, 3007, 3038, 3058) but pvec-filter (typing-core.rkt:3011-3013) and map-filter-entries (typing-core.rkt:3046-3048) have NONE in check mode — their record subjects rely on routing through check's conversion-fallback α instead. If F1b touches check-mode subsumption, these two arms are the un-canaried edge.

### §6.R R-lens targets

- unify.rkt:576-586 — the either-ordering, fixed-direction Record↔Map/PVec classify arms + the 'record is ALWAYS the inferred/value side' comment: verify (targeted construction or grep of Pi-decomposition callers) whether a Pi-decomposed sub-problem can present the FLIPPED ordering today (Map-as-actual vs Record-as-expected inside a callback domain), since the F1b width design will sit exactly on this variance-blind seam.
- subtype-predicate.rkt:229-234 + ctor-registry.rkt:435 — contravariant Pi-domain walk flips to Map<:Record which has no arm (silent #f): confirm this is the intended D11 posture to build on, not an accidental dead end, before choosing the F1b width insertion point.
- namespace.rkt:841-849 + core/collections.prologos:33-97 — prelude bare-name map/filter/length are HKT trait-dispatched: confirm the owner's Path-Selection-over-solution-sets ergonomics scenario is evaluated against THESE (S10-gated) rather than data::list map, since that changes whether F1b needs a rows-Foldable story or only the rank-1 named-HOF story.
- typing-core.rkt:3227-3237 — the conversion-fallback α is top-level-only: confirm that this is the deliberate erasure-mode rank-1 boundary line (the place F1b's 'weak preservation' doc should cite).
- qtt.rkt checkQ Record-arm parity — enumerate the qtt.rkt Record arms against typing-core's known-safe set (only qtt.rkt:2727 spot-verified here); an asymmetric qtt gap is the issue-#76 failure class.
- typing-core.rkt:3011-3013 + :3046-3048 — pvec-filter and map-filter-entries check-mode arms have NO Record case (rely on fallback-α routing): verify a record subject in check mode actually lands, or add canaries before F1b touches check-mode.

## §7 Adversarial completeness critic

**[C.0] (VERIFIED)** C1 CONFIRMED: subtype-predicate.rkt has NO record-vs-record subtype judgment (D11 holds). My greps: 0 'schema' hits in unify.rkt and subtype-predicate.rkt; no Record-vs-Record arm pattern exists. Facet 2's caveat stands: the design's 'clean insertion boundary' under-counts — NESTED record-vs-record inside covariant components dies in structural-subtype-ground?'s atomic-merge branch (a THIRD width-failure site beyond the two unify termini).
  - *Evidence*: subtype-predicate.rkt:124 comment 'NOT a record<:record judgment (D11 preserved)'; :126/:128 only Record arms (→Map/→PVec); :108-113 compound-type? excludes expr-Record; :135 [else #f]; nested failure at :236-240 (subtype-lattice-merge :409-411)

**[C.1] (VERIFIED)** C2 CONFIRMED (the 'cannot host' half): the ctor-desc walk is fixed-arity positional everywhere and expr-Record deliberately carries no prop:ctor-desc-tag — I read the in-code F1b pin myself. The 'F1b adds a label-keyed variant' half is future work with nothing to verify; the code fact is that label-keyed recursion ALREADY exists at the unify classifier layer (B3/C_Cons 'sub-goal emission), so the natural attachment points for Q1 are the classifier arms and/or a new subtype? arm — NOT the walk.
  - *Evidence*: syntax.rkt:652-653 'a keyed/variable-width row cannot register in the fixed-arity positional ctor-desc registry; width subsumption is F1b (erasure-mode), not the walk' (read directly); ctor-registry.rkt:85-96/:184-215 positional validation per facet 2; unify.rkt:597-604 (B3) + :617-631 (C_Cons) label-keyed 'sub goals (read directly)

**[C.2] (VERIFIED)** C3 CONFIRMED: default-metas passes type metas through unsolved — read the arm directly. Facet 3's two load-bearing riders verified: (i) default-metas already hosts THREE per-domain defaulting policies (level→lzero :1052, mult→'mw, num-lit-by-origin :1054-1066), so a Q4 default-to-error has an in-function precedent; (ii) freeze (=zonk+default-metas, zonk-final is its alias) fires in NON-display contexts (stored types driver.rkt:1704, constraint rendering :1579-1580, capture :1349) — the 'default-to-error at zonk-final' candidate has a wider blast radius than display.
  - *Evidence*: zonk.rkt:1050-1053 '(define (default-metas e) (match e [(expr-Type l) ...] [(expr-meta _ _) e]' (read directly); DEFERRED.md:18-31 at HEAD (entry intact, STATUS 'ACTIVE — in scope of the F1b Stage-3 co-design' added by d3c37bd0)

**[C.3] (VERIFIED)** C4 CONFIRMED with drift verified: schema-entry-closed? is consulted at EXACTLY one production site — typing-core.rkt:2921 (the map-assoc-vs-schema check arm: closed rejects unknown fields, open infers them). The historical :2444 coordinate is stale. Only other hit is the provide (macros.rkt:191). Adjacent design fact the seal must weigh: map-empty-vs-schema (:2942-2944) and champ-vs-schema (:2946-2948) are UNCONDITIONALLY #t — closed? is not consulted there, so width-partial acceptance bypasses closed? entirely on those shapes (read directly).
  - *Evidence*: grep schema-entry-closed? → macros.rkt:191 + typing-core.rkt:2921 only; typing-core.rkt:2917-2923 '(if (schema-entry-closed? schema) #f (not (expr-error? (infer ctx v))))'; :2942-2948 unconditional arms (read directly)

**[C.4] (INFERRED)** C5 CONFIRMED: schema-field carries (keyword type-datum default-val check-pred) — defaults + check-preds live at the schema layer, NO optional/presence slot (polarity split holds; presence lives only on record-field). CRITICAL caveat for Q2 (facet 1, corroborated by facet 4): the existing fill-or-error precedent is PREPARSE-SYNTACTIC and constructor-form-only — inject-schema-defaults + wrap-schema-checks fire only on the literal (SchemaName {brace-params}) head shape (macros.rkt:1779-1795); the annotation path (def x : Schema := {...}) gets NO defaults, NO checks, NO missing-field errors (behaviorally probed by facet 1). Seal fill-or-error CANNOT be a straight reuse.
  - *Evidence*: macros.rkt:758 struct; :852-867 only ':default/':check parsed; :1193-1209 inject-schema-defaults; :1243-1262 wrap-schema-checks; :1779-1795 rewrite site; facet-1 probes: def r : Person := {:name "carol"} accepted, r.age → <error> : Int

**[C.5] (VERIFIED)** C6 CONFIRMED: the row-comparison engine refuses meta solving from dyn rows in both legs — record-<:-map? (meta-V solves from ⋃fields only when tail='closed; REFUSE comment at :545) and record-<:-elem? (shared PVec/List core, :482-487 meta-elem leg, record-<:-pvec? is an alias :499-500). Coordinates verified by direct grep; facets 2 and 3 agree (:543-548 vs :534-554 are the same arm at different granularity — no disagreement). Rider for Q3: record-value-union hard-errors on empty rows (:466-467) — any F1b arm reusing ⋃fields inherits that guard contract.
  - *Evidence*: typing-core.rkt:534 (define record-<:-map? ...), :545 'over-commit; REFUSE (the meta stays unsolved; Q4 display posture)', :476 record-<:-elem?, :499-500 alias (grep-verified); consumers :3228/:3232/:3237 + qtt.rkt:2718/:2724/:2727

**[C.6] (VERIFIED)** C7 CONFIRMED (facets did not cover this directly — verified by me): (Map K V) annotations parse via parser.rkt:2080-2087 → surf-map-type → elaborator.rkt:2106-2111 → (expr-Map ek ev); NO row is minted on the annotation path. Rows are minted ONLY in the map-LITERAL arm (elaborator.rkt:2113+). Map String V is structurally inexpressible as a row: key-domain is 'keyword | 'nat only (syntax.rkt:655). Load-bearing nuance for Q2/the seal: the empty-{} dyn row rides expr-map-empty's V-TYPE SLOT with a fresh K META precisely so the annotation escape hatch ((Map String V) := {}) can claim it through the B1 empty-seed arm 'which unifies ONLY the key types' (elaborator comment) — a Map↔schema seal must preserve this arm's contract.
  - *Evidence*: parser.rkt:2080-2087 '[(Map) (or (check-arity 'Map args 2 loc) ... (surf-map-type k v loc))]'; elaborator.rkt:2106-2111 '[(surf-map-type k v loc) ... (expr-Map ek ev)]'; :2128-2135 'the seed's K slot stays a fresh META so the annotation escape hatch ((Map String V) := {}) can still claim it through the B1 empty-seed arm' + '(expr-map-empty (fresh-meta ...) (expr-Record 'keyword '() 'dyn))' (all read directly)

**[C.7] (VERIFIED)** C8 CONFIRMED-with-REFRAMING: the underscore keys are the RELATION'S DECLARED PARAM NAMES (param-info-name, FIRST variant only) at ground argument positions, suffixed '_' — NOT 'ground logic-var keys' (owner framing refuted; extract-query-info treats every expr-logic-var as free). Exact construction sites verified: compute-bound-args-for-relation reduction.rkt:346-368 (solve/solve-one/explain) and compute-bound-args :317-341 (narrowing). Facet 5's merge-site enumeration was UNDER-COUNTED: beyond answers->prologos-expr :245-251, solve :533, solve-one inline :606-622, and single-candidate narrowing :517-518, there are ALSO multi-dispatch narrowing (:506-509) and the ENTIRE explain path (:704 compute + :709 answer-result->prologos-expr with merge at :781-787, plus two legacy inline merges :722-730 and :752-757). Any key-drop decision spans SIX merge surfaces. Edge behavior confirmed: arity mismatch or missing relation → silently '() (:330-331, :356-358); anonymous-rel/unify-goal/is-goal/constructor-inversion already pass '() (:493, :550, :576, :587).
  - *Evidence*: reduction.rkt:366-368 '(cons (string->symbol (string-append (symbol->string (param-info-name pi)) "_")) arg)' (read directly); grep answers->prologos-expr → :493, :509, :518, :534, :550, :576, :587; grep compute-bound-args-for-relation → :533, :607, :704; answer-result->prologos-expr :770 with with-bound merge :781-787 (read directly)

**[C.8] (VERIFIED)** C9 CONFIRMED: record-field is (type presence) #:transparent with 'present the only written value ('optional/'absent/'unknown reserved per D6 in the struct comment — read directly). My grep confirms the 3 hardcode sites (syntax.rkt:691, typing-core.rkt:1940, :1976); facet 4's 3 thread-through sites + 2 pair-moving sites accepted. The unify pin verified directly: 'presence is not an expr and must never become a unification goal' (B3 comment). Q4 answer per code facts: activation needs NO carrier/serializer change — record-field is pnet-registered (reg2! :221) with generic struct->vector serialization and interned-symbol pass-through, so new presence VALUES round-trip free; the work is new WRITES (D20 arms at typing-core.rkt:1814-1819 dissoc / :1590-1597 update-in — both read directly, and note their asymmetry: dissoc = presence-only uncertainty, update-in = type+presence uncertainty) + new READS (currently ZERO semantic readers; display never reads presence).
  - *Evidence*: syntax.rkt:660-662 (read directly); grep '(record-field ' → syntax.rkt:691, typing-core.rkt:1940, :1976 hardcoded 'present; unify.rkt:594-596 presence pin (read in B3 block); pnet-serialize.rkt:218/:221/:222 expr-Map/record-field/expr-Record all registered (grep-verified); typing-core.rkt:1814-1819 + :1590-1597 D20 arms (read directly)

**[C.9] (VERIFIED)** Cross-facet disagreements: NONE material — the six facets are mutually consistent. The real conflicts are facets-vs-FRAMING, all resolved in the facets' favor per ground truth: (1) 'check-subsumption' does not exist as a function — the record α arms live in check's conversion fallback (typing-core.rkt:3227-3237, grep-verified) with the qtt mirror at :2704-2731; Q1's wording must be corrected. (2) Owner's 'ground logic-var keys' refuted (see C8). (3) map-fold-entries/map-map-vals are NATIVE parser keywords, not stdlib specs (facet 6, namespace.rkt:757-760). (4) pipeline.md's 'generic structural decomposition via typing-propagators.rkt' locus is wrong — 0 ctor-desc hits there; the walk lives in sre-core/unify/subtype-predicate (facet 2). Minor coordinate variances between facets 2 and 3 on the refusal legs are granularity differences, both correct.
  - *Evidence*: typing-core.rkt:3228/:3232/:3237 (grep-verified this session); unify.rkt:581 only textual 'check-subsumption' occurrence (facet 2); reduction.rkt:366-368 (read directly)

**[C.10] (FLAGGED)** Q6 boundary status (flagged, per facet 6 — not independently re-verified): the rank≤2 boundary is live TODAY on three classes S10 does not gate (non-shadowed rank-1 named HOFs with records-as-elements; contravariant callback-domain width, where the pure walk flips to Map<:Record with no arm → silent #f at subtype-predicate.rkt:230-234; transducers already rank-2). Prelude bare-name map/filter/length are HKT trait-dispatched and S10-gates BEFORE subsumption. Facet 6's open items stand: the S10 stall mechanism was not traced, and full qtt Record-arm parity was only spot-verified (qtt.rkt:2727).
  - *Evidence*: facet 6 citations: namespace.rkt:841-849, core/collections.prologos:33-97, subtype-predicate.rkt:230 + ctor-registry.rkt:435, typing-core.rkt:3011-3013/:3046-3048 missing check-mode Record arms

### §7.G Critic capture gaps

- HEAD moved past the pin: d3c37bd0 (docs-only) sits atop 02aad495. All .rkt coordinates hold, but DEFERRED.md citations shifted (+2; the Q4 entry now carries 'STATUS 2026-07-16: ACTIVE — in scope of the F1b Stage-3 co-design' at :20) and the d3c37bd0 triage moved 'Type Narrowing for map-get' to DEFERRED_COMPLETE.md as RESOLVED-by-F1a — the co-design should cite the triaged state, not the facets' snapshot.
- C8/Q5 merge-surface under-count (missed by facet 5, found this session): the EXPLAIN path also builds underscore-keyed solution champs — reduction.rkt:704 (compute-bound-args-for-relation) feeding answer-result->prologos-expr (:770, merge :781-787) plus two legacy inline merges (:722-730 wf-explained-answer, :752-757 answer-record); and multi-dispatch narrowing computes bound-args at :506-509. A key-drop decision spans SIX merge surfaces, not the three facet 5 enumerated.
- Reserved solution-map keys missed by all facets: explain results merge :certainty (Keyword) and :cycle (List String) into the SAME champ key namespace as query-var and param_ keys (reduction.rkt:730-740, :790-812 — read directly). Two Q5 consequences to surface in co-design: (i) a typed per-solution row must cover OPTIONAL fields — solution rows are the first natural client of the reserved presence marks; (ii) a relation param literally named 'certainty' would collide with the reserved key under the current '_'-suffix scheme only by luck of the suffix.
- Consumer/regression surface for the key-drop, unenumerated anywhere: tests/test-bound-args-01.rkt is a DEDICATED 8-test-case file asserting :y_/:x_/:a_ both present and absent (narrowing AND solve), and underscore-key references appear in examples/2026-03-10-surface-ergonomics.prologos, examples/2026-03-14-wfle-acceptance.prologos, examples/2026-03-19-punify-acceptance.prologos, examples/audit/audit-08-narrowing-logic.prologos. Meanwhile grep for bound-args/underscore in the F1 design doc + F1a.2 handoff = 0 hits — confirming the owner concern is ungrounded in docs; these five artifacts are the change's blast radius.
- C7 nuance no facet surfaced: the empty-{} literal's dyn row rides expr-map-empty's V-TYPE SLOT while the K slot stays a fresh META, explicitly so the (Map String V) := {} annotation escape hatch claims it through the B1 empty-seed arm 'which unifies ONLY the key types' (elaborator.rkt:2124-2135). Both the seal (Q2) and any Q1 width change touching Map↔row coupling must preserve this arm's contract — it is the load-bearing joint between C7 and the seal.
- The dormant expr-schema/expr-schema-type family (facet 1: syntax.rkt:919-920, no producer, pnet-UNREGISTERED) remains the biggest seal-design enumeration hazard; by contrast the LIVE row family is fully pnet-registered (pnet-serialize.rkt:218 expr-Map, :221 record-field, :222 expr-Record — grep-verified this session), so the vector-impostor risk is confined to the dormant family. Adopt-or-tombstone is a forced seal-design decision.
- Q1 framing correction inherited by the prompt itself: the 'four-α-point topology (check-subsumption / qtt fallback / subtype-predicate pure helpers / unify classify)' names a nonexistent function AND under-counts the width-failure sites — there are THREE termini to cover (unify '(conv)→conv-nf; the C_Cons containment fall-through; nested structural-subtype-ground? atomic-merge at subtype-predicate.rkt:236-240), and conv-nf (reduction.rkt:3820-3838) has zero record awareness.

### §7.R Critic R-lens targets (all re-verified or probe-queued by the main session)

- typing-core.rkt:2921 — single production schema-entry-closed? consultation (one-line grep re-confirms), PLUS the unconditional map-empty/champ-vs-schema arms :2942-2948 that bypass closed? — the exact acceptance the seal tightens or preserves
- reduction.rkt:346-368 (+ :317-341) and the SIX merge surfaces :245-251/:533/:606-622/:506-518/:704-757/:770-787, with tests/test-bound-args-01.rkt as the regression gate — the full bound-args enumeration before any key-drop decision
- unify.rkt:597-604 + :617-631 (B3/C_Cons guards — the predicates erasure-mode width would relax), :775+:802 conv terminus, and subtype-predicate.rkt:236-240 (nested atomic-merge — the third width-failure site) — Q1 must cover all three
- typing-core.rkt:534-554 + :476-497 (refusal legs) + :464-468 (record-value-union empty-row error) + zonk.rkt:1053 + freeze's non-display callers driver.rkt:1349/:1579-1580/:1704 (+ the shallow unsolved-metas-to-holes walk :196-203) — the complete touch-set comparison for Q3's two candidates
- macros.rkt:1779-1795 (constructor-form-only defaults/checks boundary) + the def-body-vs-top-level check-pred asymmetry (facet 1 probes, mechanism UNTRACED — flag) + form-cells.rkt:307-322 (cell/REPL-path schema emit, the two-context seam) — Q2's re-derivability question
- elaborator.rkt:2106-2111 + parser.rkt:2080-2087 + :2124-2135 (B1 empty-seed escape-hatch contract) — the C7 joint any Map↔schema/row change must not break
- qtt.rkt:2704-2731 mirror obligation + the unenumerated qtt Record-arm parity vs typing-core's known-safe set (facet 6 spot-verified only :2727) — the issue-#76 divergence class before F1b touches any check arm
- syntax.rkt:919-920 + grep 'schema' pnet-serialize.rkt (expect 0) — dormant expr-schema family: adopt-or-tombstone decision forced at seal design time
