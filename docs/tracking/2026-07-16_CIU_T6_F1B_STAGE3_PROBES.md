# CIU T6 F1b — F1b.0 Probe Bundle Results (companion to Grounding + Options)

**Date**: 2026-07-16 · **Provenance**: probe workflow `wf_7d2900e9` (8 read-only probes/traces at HEAD `d3c37bd0`; probe `.prologos` files in session scratchpad, outputs quoted in evidence). **Companions**: [Grounding](2026-07-16_CIU_T6_F1B_STAGE3_GROUNDING.md) · [Options](2026-07-16_CIU_T6_F1B_STAGE3_OPTIONS.md).

**What the bundle decided** (digest — full verdicts below):
- **P1/P2/P3/P8 (cluster W)**: the canonical width events are BARELY EXPRESSIBLE at HEAD — record types unwritable, spec-less defn params are HOLES (never narrowed by calls), projection on unannotated params dies at defn-time. Option C's contravariant cornerstone confirmed **shape-without-benefit** (ground record-domain Pis essentially never reach the fallback `subtype?` leg). A width rule inside unify/classify would **corrupt D15 literal-extent semantics** (homogeneity probes RELY on row-vs-row unify failing). → W collapses to check-fallback attachment (A/E) with named forward carve-outs.
- **P1 (NEW soundness gap)**: on-network annotation ADOPTION — inline `[use-m {:a 2 :b "x"}]` vs `(Map Keyword Int)` PASSES; `(the Int {:a 1})` → `{:a 1} : Int`. Driver types top-level exprs on-network first; the ann rule adopts the annotation and map-literal chains stay ⊥ (rule-fn #f) so no contradiction ever fires. Imperative strict arms never run on those routes. Route-sensitive (def-bound + annotated-def routes reject correctly).
- **P4 (cluster S)**: the def-body :check asymmetry is a FORCING asymmetry — `expr-panic` is an ordinary stuck value; only the top-level eval arm converts top-node panics. Worse: projection SWALLOWS panics to `none` (`bad.name` → `none : String`, zero error) because `definitely-not-map?` exempts `expr-error` but not `expr-panic`. → the seal must NOT be realized as reduction-level panic desugar; typing/elaboration-layer errors avoid the whole class.
- **P5 (cluster S)**: champ-vs-schema arm is DEAD CODE (no production flow feeds it; blanket-accepts wrong-field champs) — retirement is safe. Width-partial census: **zero live missing-required sites** — fill-or-error on the annotation path breaks nothing, EXCEPT the 10 selection-typed sites (width-partial BY DESIGN; fix = check against the selection's field subset). Direct `the Schema {…}` bypasses defaults AND :check. Aspirational commented-out sites already expect FILL semantics.
- **P6 (cluster P)**: B+ premise CONFIRMED-with-guards — update-in cannot delete SPINE keys on non-empty paths (labels ⊇-stable), so the update-in D20 arm needs NO presence machinery IF (i) zero-segment paths become a runtime error and (ii) the result row keeps a dyn tail (missing-key paths INSERT). Presence activation is dissoc-only. Also found an EVALUATION BUG: update-in/get-in missing from `definitely-not-map?` → map-get over dynamic-update-in results silently returns `none`. *(✏ FIXED 2026-07-16, commit `06e10d25` — whnf arms + exemptions + nf spine normalization; semantics preserved incl. the empty-path and insert-on-miss behaviors, which remain D20 co-design questions; the QTT inline-lambda flag stays open.)*
- **P7 (cluster T)**: commitment boundaries CONFIRMED = the def/defr/annotated store sites (`:1598/:735/:1782`); the `:1704` deferral store is TRANSIENT (replaced on completion via per-component LWW) — erroring there is premature-by-construction. The raw-meta leak is REAL and route-sensitive: open-row projection metas store verbatim (`x : ?meta1610 defined.`) and dangle silently; closed-row misses error correctly. → escape-boundary posture (A-prime mechanism + severity gate at the store sites, def-srcloc free) is the supported ruling.

---

## P1-W-widthfunnel

**HEAD verified**: d3c37bd03e04b08024afa41f6f9e922fe3ab06cb (d3c37bd0, as expected; `git rev-parse HEAD` run first)

**Verdict**: The P1 width event as posed is UNREACHABLE at HEAD: an unannotated defn whose body projects the record param (`defn use-a [r] [+ r.a 1]`) is rejected at DEFINITION time with "ERROR: Type mismatch" — no first call ever runs, so no "second wider call" exists. The defn dies in infer's expr-map-get fallthrough `[_ (expr-error)]` (typing-core.rkt:1748; expr-get twin :1683): the spec-less param's type is an expr-hole (never a meta), whnf can't resolve it to Record/Map, and no meta/hole arm exists — it never reaches unify's B3 guard nor check's conversion fallback. Question (3) is therefore NO on a stronger ground: there is no param meta to solve — `defn id2 [x] x` types as `_ -> _`, and `[id2 1]` then `[id2 "s"]` BOTH pass with result type `_` (the hole is never narrowed by a first call). Where a wide second call IS expressible (the only user-writable annotation forms are `(Map K V)`/schema — record types are pinned "not user-writable", design doc :152), WIDTH IS ADMITTED by design via the record→Map α (`[use-m {:a 2 :b 3}]` and def-bound both pass against `(Map Keyword Int)`); only a value-type misfit fails, and ONLY on the def-bound route: `[use-m wides]` with `wides : {:a Int :b String}` fails "ERROR: Could not infer type", dying in check's conversion-fallback legs (unify classify's record-vs-map guard `record-subtypes-map?` at unify.rkt:582-583 refuses → check fallback `record-<:-map?` typing-core.rkt:3227-3228 fails String</:Int at :551-553 → `subtype?` leg :3239 fails) — NOT at B3, which only matches Record-vs-Record with identical label-sets (unify.rkt:597-604). BONUS FINDING (soundness gap): the same call with the INLINE literal `[use-m {:a 2 :b "x"}]` PASSES (`3 : Int`), as do `(the (Map Keyword Int) {:b "x"})` and even `(the Int {:a 1})` → `{:a 1} : Int`. Mechanism (verified): the driver types top-level expressions on-network FIRST (driver.rkt:584-588); the on-network ann rule adopts the annotation as the root type and only contradicts if the term position reaches type-top (typing-propagators.rkt:2705-2732); map-literal chains are registered with rule-fn #f (typing-propagators.rkt:2362-2372) so their position stays ⊥, which merges trivially with the annotation — the imperative per-entry strict arms (typing-core.rkt:2896-2908, which correctly reject `def z1 : (Map Keyword Int) := {:a 2 :b "x"}` with "Type mismatch" via driver.rkt:561) never run on the inline/the-form routes. Def-bound terms carry a concrete Record type on-network, hit top, fall back imperatively, and reject correctly ((the (Map Keyword Int) wides) errors; homogeneous widei passes via the α).

**Route matrix**: UNANNOTATED — defn param + projecting body ([+ r.a 1] or bare r.a): REJECTED at defn, "ERROR: Type mismatch" (both variants; no call reachable). defn param, non-projecting (id2): 1st call Int passes, 2nd call String passes, type stays `_` — hole never solved. def-bound unannotated lambda (def g := [fn [r] r.a]): "ERROR: Could not infer type" (infer lam hole-domain arm, typing-core:2856). Inline beta-redex ([[fn [r] r.a] {:a 1}]): PASSES `1 : Int` (per-application arg-type flow, nothing persists). ANNOTATED — record-type annotation ({:a Int} in spec or fn binder): inexpressible — "Type mismatch"/"Could not infer type" (design pin: not user-writable; (Map K V)/schema are the annotation forms). Map spec (use-m : (Map Keyword Int) -> Int): narrow inline PASS (2:Int); WIDE inline same-V {:a 2 :b 3} PASS (width admitted, α); WIDE inline hetero-V {:a 2 :b "x"} PASS — SOUNDNESS GAP (on-network ann/app leniency); WIDE def-bound same-V PASS; WIDE def-bound hetero-V FAIL "Could not infer type" (check conv-fallback legs, not B3). Annotated def (def z : (Map Keyword Int) := {:a 2 :b "x"}): FAIL "Type mismatch" (imperative per-entry arm). the-form: inline literal LENIENT vs any annotation incl. (the Int {:a 1}); def-bound fvar STRICT (rejects hetero, admits homogeneous width).

**Open flags**:
- SOUNDNESS GAP (main discovery, VERIFIED end-to-end): inline map-literal terms bypass ALL value/key checking against annotations on the on-network route — `(the Int {:a 1})` types as `{:a 1} : Int`; `[use-m {:a 2 :b "x"}]` against `(Map Keyword Int)` passes and RUNS (`3 : Int`). Chain: driver.rkt:584-588 network-first + typing-propagators.rkt:2705-2732 ann-adopts-annotation-unless-term-hits-top + typing-propagators.rkt:2369 map-assoc rule-fn #f (term stays bot, never top). The typing-core:2899 comment 'Per-entry strictness is preserved by the map-assoc-vs-Map arm as the chain unwinds' is TRUE only on imperative routes (annotated def) — the arm is dead code for top-level the-forms and inline call arguments. No acceptance canary covers the should-fail direction (;;14/;;16 test only passing cases).
- Cluster W premise gap: 'defn param meta solved narrow by first call' is inexpressible at HEAD — spec-less defn params are expr-holes (not metas), never solved by calls (id2 evidence), and a projecting body kills the defn outright at typing-core:1748. If cluster W wants this width event, it needs the hole→row-meta minting (or an expr-get meta arm) to exist first.
- Error-text quality: the defn rejection and the def-bound wide-call rejection surface as bare 'Type mismatch' / 'Could not infer type' with no mention of the record/field/annotation involved (contrast the rich closed-row-miss message at acceptance ;;28).
- INFERRED (not read): the exact on-network app-rule mechanism for the inline-argument leniency (arg position bot merges with domain write) — inferred from the verified ann-rule mechanism + observed outcomes (probe-diag3 d6 vs d10, probe-diag7 h0); the ann-rule leg itself is VERIFIED.

**Evidence**:
- HEAD: d3c37bd03e04b08024afa41f6f9e922fe3ab06cb (git rev-parse HEAD)
- PROBE probe-p1a.prologos (/private/tmp/claude-501/-Users-avanti-dev-projects-prologos/e11f1cd7-a020-4225-91af-fdf4803e15a8/scratchpad/): result 0 `ERROR: Type mismatch` for `defn use-a [r] [+ r.a 1]`; all calls downstream `ERROR: Unbound variable` — VERIFIED (ran)
- PROBE probe-diag.prologos: `0: id1 : _ -> _ defined.` / `1: 1 : _` / `2: ERROR: Type mismatch` (defn geta [r] r.a) — projection alone kills the defn — VERIFIED (ran)
- PROBE probe-diag3.prologos: `1: 1 : _` `2: "s" : _` (id2 both calls pass, hole unsolved); `4: 2 : Int` `5: 3 : Int` `6: 3 : Int` (use-m narrow / wide-same-V / wide-HETERO all pass inline); `8: 3 : Int` (def-bound wide same-V passes); `10: ERROR: Could not infer type` (def-bound wide hetero fails) — VERIFIED (ran)
- PROBE probe-diag4.prologos: `0: ERROR: Type mismatch` (def z1 : (Map Keyword Int) := {:a 2 :b "x"}) vs `4: {:a 2, :b "x"} : [Map Keyword Int]` ([idmi {:a 2 :b "x"}]) and `5: {:a 2, :b "x"} : [Map Keyword Int]` (the-form) — VERIFIED (ran)
- PROBE probe-diag5.prologos: `0: ERROR: Could not infer type` ((the Int "x") — plain check strict) vs `1: {:b "x"} : [Map Keyword Int]` — VERIFIED (ran)
- PROBE probe-diag6.prologos: `4: {:a 1} : Int` ((the Int {:a 1}) PASSES — full annotation adoption on literal terms); 0-3 all pass incl. key mismatches — VERIFIED (ran)
- PROBE probe-diag7.prologos: `1: ERROR: Could not infer type` ((the (Map Keyword Int) wides) — def-bound fvar term REJECTED) / `4: {:a 2, :b 3} : [Map Keyword Int]` (homogeneous width admitted) — mechanism discriminator — VERIFIED (ran)
- typing-core.rkt:1660-1683 expr-get / :1684-1748 expr-map-get: match on whnf'd subject type ends `[_ (expr-error)]` — no arm for hole/meta subjects — VERIFIED (read)
- typing-core.rkt:2762-2764: `(define (check ctx e t) ... (match* (e (whnf t))` — scrutinee not whnf'd, target is — VERIFIED (read)
- typing-core.rkt:2896-2908: B1 empty-seed arm (keys-only unify for empty closed Record v1) + map-assoc-vs-Map arm `(and (check ctx m ...) (check ctx k kt) (check ctx v vt))` — the per-entry strict arm, live only on imperative routes — VERIFIED (read)
- typing-core.rkt:3213-3239 conversion fallback: `(unify ctx t t1)` then cumulativity, `record-<:-map?` (:3227-3228), record-<:-pvec?, List-elem, `(subtype? t1-w t-w)` (:3239) — VERIFIED (read)
- typing-core.rkt:534-554 record-<:-map?: labels checked vs kt; concrete V: per-field `(or (unify ...) (subtype? ft vt))` (:551-553) — width admitted, value misfit rejected — VERIFIED (read)
- unify.rkt:582-583 classify record-vs-map coercion arms gated on `record-subtypes-map?`; :597-604 B3 arm requires same key-domain + same tail + `(equal? (map car fields-a) (map car fields-b))` — never matches Record-vs-Map or differing label-sets — VERIFIED (read)
- subtype-predicate.rkt:144-155 record-subtypes-map?: key-domain gate + `(andmap ... (subtype? (record-field-type (cdr f)) vt))` — per-field strict — VERIFIED (read)
- driver.rkt:584-588: top-level expr typing = `infer-on-network/err` first, `infer/err` only if the network result is a prologos-error — VERIFIED (read)
- driver.rkt:561: annotated def path = `(check/err ctx-empty expr type)` imperative — VERIFIED (read)
- driver.rkt:1715: defn body path = `(check/err ctx-empty body type*)` imperative — VERIFIED (read)
- typing-propagators.rkt:2705-2732 on-network expr-ann rule: `type-map-write net2 tm-cid e type-expr` (annotation IS the root type) + `type-map-write-unified` downward + contradiction fires ONLY `(if (type-top? term-type) ...)` — a bot term position never contradicts — VERIFIED (read)
- typing-propagators.rkt:2362-2372: `Registered with return-type=#f — falls back to imperative` for expr-map-get/expr-map-assoc/expr-map-dissoc/... — map chains stay bot on-network — VERIFIED (read)
- typing-propagators.rkt:3110-3119 infer-on-network/err falls back only when ROOT type is bot/top/unsolved-meta — an ann node masks a bot term — VERIFIED (read)
- docs/tracking/2026-07-06_CIU_T6_F1_STRUCTURAL_RECORDS_DESIGN.md:152: record types are `(3) not user-writable — (Map K V)/schema remain the annotation forms` — VERIFIED (read)
- elaborator.rkt:2136-2153: all-keyword literal elaborates to map-assoc chain on `(expr-map-empty (expr-Keyword) (expr-Record 'keyword '() 'closed))`; elaborator.rkt:1297-1303: (the T e) elaborates to plain `(expr-ann e t)` — VERIFIED (read)
- typing-core.rkt:854-862 infer expr-lam: `[(expr-hole? dom) (expr-error)]` — why `def g := [fn [r] r.a]` gives Could-not-infer — VERIFIED (read)

## P2-W-nestedwidth

**HEAD verified**: d3c37bd03e04b08024afa41f6f9e922fe3ab06cb

**Verdict**: At HEAD d3c37bd0 the P2 probe as written FAILS, but earlier than the intended event: `defn first-a [xs] [+ [head xs].a 1]` dies at DEFINITION time ("Type mismatch … Got: <could not infer>") because projection (`map-get x :a`) on a meta-typed unannotated param cannot infer (no row-constraint machinery; isolated: `defn proj-a [m] m.a` alone fails) — both calls then fail "Unbound variable: first-a", so the "first call solves the param" premise never materializes (and prelude `head` returns `Option A` anyway, collections.prologos:126). Realizing the true cluster-W event ii via a shared spec variable (`spec both [List A] [List A] -> List A`) shows nested width FAILS in both directions and both routes (inline + def-bound): the implicit solves to the first list's row, the second list's check dies with generic "Could not infer type". Where it dies: the app arg check (typing-core.rkt:924-927) reaches the conversion fallback (:3213-3239); UNIFY decomposes List (SRE app ctor-desc, variances `(= +)`) to the element-row goal `{:a Int :b Int}` vs `{:a Int}`, which matches NO Record arm — B3 (unify.rkt:594-607) needs equal label sets, the p1a arm (:608-633) needs a 'dyn tail — and falls to `[else '(conv)]` (:775) → conv-nf fails; the fallback's last-resort `subtype?` (:3239) ALSO refuses via structural-subtype-ground?: records are tag-less (ctor-tag-for-value → #f, verified), so the row pair lands in the atomic-merge arm (subtype-predicate.rkt:236-240) → #f. So: primary death in unify's classify '(conv), secondary refusal in the subtype-predicate atomic-merge — there is simply no row-width rule in either engine (only Record→Map/PVec/List αs, which is why the same wide-row list PASSES against a `(List (Map Keyword Int))` annotation, both routes). BONUS: `'[{:a 1} {:a 1 :b 2}]` does NOT stay a List — the heterogeneous list literal mints a closed 'nat row of rows `⟨{:a Int} {:a Int :b Int}⟩` (typing-core.rkt:1925-1941), inline and def-bound — so nested width events never arise from literals; they arise only when two separately-inferred row-lists meet through a shared type variable.

**Route matrix**: Nested (List-level) width via shared spec var (spec both [List A] [List A] -> List A): inline literals FAIL both orders; def-bound FAIL both orders — route-insensitive, error 'Could not infer type' with the implicit shown solved to the FIRST arg's row. Nested width vs annotated (List (Map Keyword Int)): inline PASS, def-bound PASS (Record→Map α fires at the element position under both unify-classify '(ok)' and the subtype walk :210). Row-typed annotation (List {:a Int}): unwritable — parses as a value, 'not a valid type'. Element-level width (cons into a solved-row list): inline literal PASS in BOTH directions (width ignored; final row = the list's row); def-bound record FAIL both directions. Original P2 defn route (unannotated param + projection): fails at DEFN definition time (annotated defn param via spec is the only working defn route, and only Map-elem — row-elem specs are unwritable).

**Open flags**:
- SUSPICIOUS (possible static-soundness gap, mechanism untraced): inline record-literal elements into cons are width-tolerant in BOTH directions — [cons {:a 9} wa] types as (List {:a Int :b Int}) although the head VALUE lacks :b (projecting .b off that element would typecheck but miss at runtime). Def-bound records fail the same positions. No typing-core check arm explains it (map-assoc-vs-Record has no arm; the conversion fallback would reject); the elaborator's implicit/constraint pipeline is the suspected mechanism. Deserves its own probe/audit.
- INFERRED (not step-traced): that the [both na wa] failure's exact failing frame is typing-core's check conversion fallback (unify then subtype?, both refusing) — grounded by the error shape (infer/err wraps infer's expr-error) plus component-level harness verification of every reconciliation primitive on the exact type pair; a debugger-level trace of the live call was not run.
- Observed in passing: calling an unannotated head-wrapping defn ([get-h '[1 2]]) prints a STUCK reduce with unsolved metas ('[reduce [?meta1505 …]] : Option _') — the Seqable dict meta appears unsolved at the call site; unrelated to P2 but odd.
- UX gap: the nested width failure surfaces as bare 'Could not infer type' naming the whole application — no mention of the row-width conflict; the S7 closed-row-miss hint covers projection misses only, not row-vs-row width refusals inside containers.
- The unannotated-defn route for P2's premise is DOUBLY dead: (a) projection on a meta-typed param can't infer (no row constraints), so the defn never defines; (b) even a defn that DOES infer (identity) keeps its param as '_' — the first call does not persistently solve the param meta, so cross-call narrow-then-wide events cannot arise via defn params at HEAD.

**Evidence**:
- git rev-parse HEAD → d3c37bd03e04b08024afa41f6f9e922fe3ab06cb (docs-only atop 02aad495, as expected)
- PROBE /private/tmp/claude-501/-Users-avanti-dev-projects-prologos/e11f1cd7-a020-4225-91af-fdf4803e15a8/scratchpad/probe2.prologos — original P2 shape: result 0 = 'Type mismatch / Expected: [Pi [x <[?meta1504 ?meta1503]>] ?meta1501] / Got: <could not infer> / In expression: [fn [x <_>] [+ [map-get [prologos::core::collections::head ?meta1503 ?meta1504 ?meta1505 x] :a] 1]]'; results 1-2 = 'Unbound variable: first-a' (both calls dead)
- PROBE probe2iso.prologos — isolation: 'defn get-h [xs] [head xs]' infers fine ('get-h : [_ _] -> [Option _] defined.'), but 'defn proj-a [m] m.a' fails at definition ('[fn [x <_>] [map-get x :a]] … <could not infer>') → projection on a meta-typed param is the blocker; no row-constraint inference exists
- racket/prologos/lib/prologos/core/collections.prologos:126 — '(spec head {A : Type} {C : Type -> Type} (Seqable C) -> (C A) -> (Option A))' — prelude head returns Option A (P2's premise assumed A)
- PROBE probe2unify.prologos — THE nested-width event via shared spec var ('spec both [List A] [List A] -> List A'): [both narrow wide] FAILS 'Could not infer type / Expression: [both {:a Int} …]' (A solved to first arg's row); reverse order FAILS with A={:a Int :b Int}; def-bound args [both na wa]/[both wa na] FAIL identically → inline vs def-bound route-INSENSITIVE at List level
- PROBE probe2mixed.prologos — BONUS: '[{:a 1} {:a 1 :b 2}] types as ⟨{:a Int} {:a Int :b Int}⟩ (closed 'nat row of rows), inline AND def-bound — no List join, no error
- PROBE probe2ann.prologos — wide-row list vs (List (Map Keyword Int)) annotation: inline PASSES, def-bound PASSES ('ys : [List [Map Keyword Int]] defined.') → nested Record→Map α works through the List decomposition
- PROBE probe2row.prologos — row annotation (List {:a Int}) NOT writable: '{:a Int}' in type position elaborates as a value → 'Expression is not a valid type / [List [map-assoc {} : (Map Keyword {}) :a Int]]'
- PROBE probe2cons.prologos + probe2cons2.prologos — element-level split: inline literal element PASSES BOTH directions ([cons {:a 1 :b 2} na] : List {:a Int}, value keeps :b; [cons {:a 9} wa] : List {:a Int :b Int}, head value LACKS :b); def-bound record element FAILS both directions ('Could not infer type / '[nr | wa]')
- HARNESS rowharness.rkt (direct classify/subtype on constructed rows): 'classify wide-vs-narrow: (conv)' / 'classify narrow-vs-wide: (conv)' / 'subtype? wide<:narrow: #f' / 'subtype? narrow<:wide: #f' / 'subtype? wide<:Map: #t' / 'classify wide-vs-Map: (ok)'
- HARNESS listharness.rkt (container level): 'ctor tag of (List row): #(struct:ctor-desc app 2 … (= +) …)' (arg covariant); 'subtype? (List wide)<:(List nar): #f' both directions; 'subtype? (List wide)<:(List Map): #t'; 'classify (List wide) vs (List nar): (sub ((List . List) (wide-Record . narrow-Record)))' — List DOES decompose to the element-row goal
- HARNESS tagharness.rkt: 'ctor-tag-for-value on a closed keyword row: #f' (with driver registrations loaded) → expr-Record is tag-less to the SRE ground walk
- racket/prologos/unify.rkt:594-607 — B3 Record-vs-Record 'sub arm requires SAME key-domain + SAME tail + SAME label-set; its own comment (:592): 'a DIFFERENT label-set falls through to [else '(conv)] (the F1a-col flavor-B union-widen case, deferred)'
- racket/prologos/unify.rkt:608-633 — p1a dyn-tail arm requires 'at least ONE 'dyn tail'; both probe rows are 'closed → arm skipped
- racket/prologos/unify.rkt:775 '[else '(conv)]' + :802 "['(conv) (conv-nf a b)]" — the fall-through that kills the element-row goal
- racket/prologos/subtype-predicate.rkt:205-240 — structural-subtype-ground?: :210 Record→Map arm (fires only for Map-expected); :218-232 same-tag component walk with variance; :236-240 atomic-merge arm '(and (not …contradicts?…) (equal? merged t2))' — the arm tag-less Record-vs-Record lands in
- racket/prologos/typing-core.rkt:922-928 — general app: '(check ctx e2 (expr-Pi-domain t1))', failure → expr-error
- racket/prologos/typing-core.rkt:3213-3239 — check conversion fallback: unify, then Record→Map/PVec/List α arms (Map:3227, PVec:3231, List:3235 — all require the ACTUAL to be a Record, not a List-of-Record), then last-resort '(subtype? t1-w t-w)' (:3239)
- racket/prologos/typing-errors.rkt:166-176 — infer/err wraps infer's expr-error as inference-failed-error 'Could not infer type' (the exact user-visible message observed)
- racket/prologos/typing-core.rkt:1925-1941 — expr-list-literal all-at-once: homogeneous (rollback-probed pairwise unify) → delegate to chain infer; heterogeneous → make-record 'nat … 'closed (the BONUS tuple-minting arm)
- racket/prologos/elaborator.rkt:2113-2153 — all-keyword map literal elaborates to a map-assoc chain on an empty-closed-record seed (record seed), non-keyword → expr-map-literal

## P3-W-contravariant-groundness

**HEAD verified**: d3c37bd03e04b08024afa41f6f9e922fe3ab06cb (d3c37bd0, as expected; docs-only atop 02aad495)

**Verdict**: The canonical contravariant-callback case cannot even be CONSTRUCTED unannotated at HEAD: `defn get-a [r] r.a` and `defn apply-f [f x] [f x]` both die at defn-time ("Type mismatch / Expected: [Pi [x <?meta1506>] ?meta1507] / Got: <could not infer>") because projection and application on meta-typed params mint no structure — and the partial section `[map-get _ :a]` dies identically at def-bind. Every annotated variant (spec A1-schema or (Map Keyword Int) callback, polymorphic `{A B} [A -> B] A -> B` HOF, inline or def-bound wide record) PASSES — but via meta-solving unify (the expected callback Pi is `[?A -> ?B]` instantiation metas) plus unify's SYMMETRIC Record↔Map absorption arms (unify.rkt:582-586), never via the fallback subtype? leg. THE RIDER: a ground Pi DOES reach typing-core.rkt:3239's subtype? leg and the leg genuinely works — proven behaviorally by direction-sensitivity on flat atoms ([hofi get-rat 1] contravariant-widening PASSES, [hofr get-int 1] unsound direction REJECTED, ground m1-vs-mw mult REJECTED): only the variance walk is direction-sensitive; unify's arms are symmetric. BUT a ground Pi with a RECORD domain essentially never reaches it: rows are unspellable in specs, schemas stay nominal fvars that neither whnf-unfold in the fallback arms (p3f: `def mv : (Map Keyword Int) := a1v` fails "Got: p3f::A1") nor relate in the walk's atomic-merge leg, row-domain provided-side function types can't be constructed (unannotated dies at defn), and row-vs-Map pairs entering via solved metas are consumed by unify BEFORE the fallback. Both record-flavored ground contravariant directions fail with the uninformative inference-failed "Could not infer type" (no Expected/Got). So walk-arm record-WIDTH in structural-subtype-ground? would receive zero traffic from the contravariant-callback flows at HEAD — shape-without-benefit — unless rows become spellable, schemas unfold, or unify's symmetric record arms are re-routed through the variance walk. Refinement of the grounding claim: subtype? does NOT hard-die on TYPE metas — subtype-lattice-merge keeps the concrete side (subtype-predicate.rkt:406-408), so a meta component passes vacuously as sub-side and fails as super-side; the strict equal? kill is only the mult '=' component (subtype-predicate.rkt:231, Pi variances '(= - +) ctor-registry.rkt:426-435), and it does not bite def-bound callbacks in practice because stored spec-less types are mult-ground after final zonk ([hofi add1i 1] passes; spec arrows are born 'mw at elaborator.rkt:1087).

**Route matrix**: CALLBACK unannotated × def-bound (defn get-a [r] r.a; def getp := [map-get _ :a]): FAILS AT DEFN (meta-laden expected Pi, "<could not infer>") — call-site never reached. CALLBACK unannotated × inline (section [map-get _ :a] straight into ground HOF): PASSES (checked lam-vs-Pi arm typing-core.rkt:2780, no fallback). CALLBACK annotated (spec A1 or Map) × polymorphic HOF × inline wide literal: PASSES (1 : Int). × def-bound wide: PASSES. CALLBACK annotated × ground monomorphic HOF: exact-domain PASSES; record-contravariant mismatch FAILS both directions ("Could not infer type"); flat-atomic contravariant (Rat-consumer where Int expected) PASSES; unsound flat direction REJECTED. WIDE RECORD inline-literal vs def-bound: identical (both pass) in apply-f flows; DIVERGES under prelude map with schema callback — `map get-s '[{:a 1 :b 2}]` inline FAILS, `map get-s xs` def-bound PASSES (mechanism unattributed, flagged); also diverges at schema annotations — literal `def a1y : A1 := {:a 1 :b 2}` PASSES, def-bound `def a1x : A1 := wide` FAILS.

**Open flags**:
- UNRESOLVED MECHANISM: `map get-s xs` (p3d:5) passes while the same (List row)-value-vs-(List A1) comparison fails in def-annotation position (p3j:2) — the app-argument check route diverges from the def-annotation route (speculation/dict machinery suspected); needs a targeted trace before any design leans on it
- Attribution of the flat ground-Pi acceptance to typing-core.rkt:3239 specifically is behavioral+code-elimination (direction-sensitivity rules out unify's symmetric arms), not debugger-stepped; another variance-aware subtype? caller (e.g. qtt/checkQ) could be the accepting frame — same predicate and walk either way
- The `[x <T>]` defn-param annotation form named by the parser error was not exercised (only the rejected `[x : T]` form and the spec route were); a defn-local angle-annotation route could add one more matrix cell
- Error-text meta display reflects error-CONSTRUCTION time (typing-errors.rkt:276-282), immediately after the failing check in the same command — faithful proxy for check-time meta state, but not literally instrumented at the subtype? call site
- Grounding-doc claim 'subtype? dies on metas at the atomic-merge equal? :226-240' is imprecise at HEAD: type-meta components are vacuously ACCEPTED sub-side / rejected super-side (merge keeps concrete side, subtype-predicate.rkt:406-408); only the mult '=' equal? (:231) is a hard kill, and probed flows never hit it (stored types mult-ground post-zonk)
- Prompt's coordinate 'typing-core ~:3238' is the comment line; the subtype? leg is :3239 at this HEAD

**Evidence**:
- typing-core.rkt:3213-3239 — conversion fallback; final arm :3239 `[(t-w t1-w) (subtype? t1-w t-w)]`; record-α arms :3227 (Map), :3231 (PVec), :3235 (List) require t1 to be expr-Record after whnf
- subtype-predicate.rkt:115-135 — subtype? pure; expr-Record NOT in compound-type? (:108-113); Record→Map arm :126 one-directional
- subtype-predicate.rkt:205-240 — structural-subtype-ground?: variance loop :225-232, '=' → equal? at :231; atomic-merge leg :236-240 requires (equal? merged t2)
- subtype-predicate.rkt:397-411 — subtype-lattice-merge keeps CONCRETE side on metas (:406-408 'Meta handling (F2): keep concrete side'), no hard die
- ctor-registry.rkt:426-435 — Pi ctor-desc #:component-variances '(= - +) (VERIFIED at exactly :435)
- unify.rkt:582-586 — SYMMETRIC Record↔Map + Record↔PVec unify arms: `[(and (expr-Record? a) (expr-Map? b) (record-subtypes-map? a b)) '(ok)]` AND the flipped orientation — width absorption both ways at unify, bypassing variance
- elaborator.rkt:1087 — spec arrows born ground: `(or raw-mult 'mw)`; elaborator.rkt:1131 — unannotated lambda params mint fresh-mult-meta
- typing-errors.rkt:276-282 — type-mismatch-error pp's expected/actual AT CONSTRUCTION time; pretty-print.rkt:135-139 prints ?N only if meta unsolved at that moment (the meta-visibility proxy)
- typing-core.rkt:2780 — check's lam-vs-Pi arm `[((expr-lam m a body) (expr-Pi m2 t-dom b))` (inline lambdas never hit the fallback)
- probe p3a (scratchpad/p3a-unannotated.prologos): `defn get-a [r] r.a` → 'Type mismatch / Expected: [Pi [x <?meta1506>] ?meta1507] / Got: <could not infer> / In expression: [fn [x <_>] [map-get x :a]]' — meta-laden expected at defn-time
- probe p3b (p3b-annotated.prologos): [apply-f get-s {:a 1 :b 2}] → '1 : Int'; [apply-f get-m wide] → '1 : Int' (all annotated routes pass)
- probe p3c (p3c-ground-hof.prologos): [hofm get-s {:a 1 :b 2}] → 'Could not infer type'; [hofs get-m {:a 1}] → 'Could not infer type' (both ground record-contravariant directions fail, uninformative error); exact matches → '1 : Int'
- probe p3f (p3f-ground-leg.prologos): [hofi get-rat 1] → '7 : Int' (ground contravariant flat PASSES); [hofi get-nat 1] → '0N : Int'; [hofi get-lin 1] → FAILS (ground m1 vs mw); def mv : (Map Keyword Int) := a1v → 'Expected: [Map Keyword Int] / Got: p3f::A1' (schema whnf-opaque, ground meta-free failing comparison)
- probe p3g (p3g-direction.prologos): [hofr get-int 1] → REJECTED vs [hofi get-rat 1] → '7 : Int' — direction-sensitivity proves variance-walk attribution; [hofm [fn [r : A1] r.a] {:a 1}] → '1 : Int' (inline lambda checked route)
- probe p3h (p3h-mult-meta.prologos): [hofi add1i 1] → '2 : Int' — spec-less def-bound stored types are mult-ground; no '=' mult-meta kill on def-bound comparanda
- probe p3d (p3d-map-prelude.prologos): map get-s '[{:a 1 :b 2}] → 'Could not infer type' (elaborated form shows ?A solved to p3d::A1) vs map get-s xs → ''[1] : List Int' — inline/def-bound SPLIT
- probes p3i/p3j: def a1x : A1 := wide → 'Expected: p3i::A1 / Got: {:a Int :b Int}' FAILS; def a1y : A1 := {:a 1 :b 2} PASSES (literal-only schema checking); def ys : (List A1) := xs → FAILS (walk rejects row-vs-schema)
- probe p3k (p3k-partial.prologos): def getp := [map-get _ :a] → 'Could not infer type' on desugared [fn [x <_>] [map-get x :a]]; inline [hofm [map-get _ :a] {:a 1}] → '1 : Int'
- probe p3e (p3e-specless.prologos): defn id1 [x] x → 'id1 : _ -> _ defined.'; defn add1i [x] [int+ x 1] → 'Int -> Int' (spec-less defn viable; projection/application bodies are what die)
- harness: scratchpad/run-verbose.rkt (process-file + format-error for full Expected/Got text)

## P4-S-defbody-asymmetry

**HEAD verified**: d3c37bd03e04b08024afa41f6f9e922fe3ab06cb (d3c37bd0, as expected; docs-only atop 02aad495)

**Verdict**: REPRODUCED + MECHANISM TRACED. The asymmetry is a forcing asymmetry in driver.rkt, not an error-swallow: the schema :check desugar (macros.rkt wrap-schema-checks) lowers the constructor form to `(let tmp (the Schema map) (if pred tmp (panic msg)))`, which elaborates to a beta-redex `[[fn [x] [boolrec ... x (panic msg) [< 0 [map-get x :age]]]] (the Person {...})]`. `expr-panic` is an ordinary STUCK VALUE (not a Racket exception, not a prologos-error): typing accepts it anywhere (infers as expr-error, typing-core.rkt:2391; check-mode always succeeds, :2773), and reduction leaves it stuck (reduction.rkt:3311). The ONLY panic→error conversion in the whole pipeline is the top-level `(list 'eval expr)` arm, driver.rkt:596-607, which calls `nf` on the expression and then checks `(expr-panic? val)` on the TOP node, converting it to `prologos-error "panic: ..."`. Both def paths — inferred (driver.rkt:1588-1606) and annotated (:1766-1791) — do `zonk`+`freeze`+`rewrite-specializations` but NEVER call `nf` and NEVER check `expr-panic?`; `global-env-add` (global-env.rkt:161-162) stores the body verbatim. So the def is LAZY: the panic value is never even computed at definition time (verified by direct inspection of the stored value — it is the unreduced redex with the condition unevaluated), and the def prints "bad : Person defined." silently. On later bare USE, whnf's fvar case (reduction.rkt:3237-3242) delta-unfolds the stored redex, the if reduces to the panic branch, nf returns expr-panic at top, and driver.rkt:601 converts it to the error — so the panic fires on use, but ONLY when the panic lands at the top of the nf'd value. Worse: under an elimination context (map-get / dot-access), the panic is SWALLOWED into a wrong value — `definitely-not-map?` (reduction.rkt:1393-1407) exempts `expr-error?` (:1407) but NOT `expr-panic?`, so the map-get whnf arm's "graceful degradation" (:2726-2733) returns `(expr-fvar 'none)`: `[map-get bad :name]` and `bad2.name` both print `none : String` with zero error. CONSEQUENCE FOR THE SEAL: a seal realized as this panic-in-branch desugar inherits all three defects — silent definition (annotated AND unannotated), fire-only-on-bare-top-level-use, and silent `none`-conversion under projection. Def-time enforcement would require either forcing (nf + expr-panic check mirroring driver.rkt:600-607) at def commit, or raising at the typing/elaboration layer as a prologos-error instead of a reduction-level panic value.

**Route matrix**: inline top-level constructor (preparse self-wraps with `the`, so effectively annotated): panic → ERROR at driver.rkt:601 (Probe A). | def-bound UNANNOTATED `def bad := [Person {...}]`: silent 'defined.' — body stored unreduced, no nf/panic check on the inferred path driver.rkt:1588-1606 (Probe B r1). | def-bound ANNOTATED `def bad2 : Person := [Person {...}]`: silent 'defined.' — same gap on the annotated path driver.rkt:1766-1791 (Probe C r1). | bare USE of def-bound name (both annot/unannot): panic fires at use via fvar delta-unfold + top-level nf + driver.rkt:601 (Probe B r2, C r2). | USE under elimination context (map-get / dot-access, both annot/unannot): panic SWALLOWED to `none : String` via definitely-not-map? + map-get graceful degradation, reduction.rkt:1393-1407 + :2726-2733 (Probe B r3, C r5). | Passing-check def-bound: reduces to the record at use, normal (Probe C r4).

**Open flags**:
- SWALLOW-IS-WORSE-THAN-SILENCE (VERIFIED): projection through map-get/dot-access converts a failed :check into `none : String` — a wrong VALUE with a value/type mismatch (none at type String), no error anywhere. Any seal desugar realized as panic-in-branch inherits this, so a def-bound sealed value used only via projections would NEVER surface the violation.
- TOP-NODE-ONLY panic detection (INFERRED, not probed): driver.rkt:601 checks expr-panic? only on the top node of the nf'd value — a panic nested inside a constructor result (e.g., a pair/list containing the sealed value) would print as a stuck term rather than convert to ERROR. Worth one probe if the seal can flow into containers.
- nil-safe-get swallows to nil analogously (reduction.rkt:2734-2741, VERIFIED read, not probed).
- expr-error (unlike expr-panic) IS exempted in definitely-not-map? (:1407 'error propagation') — map-get on expr-error stays STUCK rather than degrading to none; a seal realized as a typing/elaboration-layer prologos-error (or an expr-error value) would not hit the none-swallow class, one argument for lifting the seal check out of reduction-level panic.
- QTT/type-check pass cleanly on the panic-bearing body (expr-panic types as expr-error/anything, typing-core.rkt:2391+2773) — no def-time gate exists at ANY pipeline stage for reduction-level check failures.

**Evidence**:
- HEAD: git rev-parse HEAD = d3c37bd03e04b08024afa41f6f9e922fe3ab06cb (VERIFIED)
- Desugar shape: racket/prologos/macros.rkt:1243-1262 wrap-schema-checks — `(if ,pred-norm ,inner (panic ,msg))` under `(let ,tmp ,base-form ,body)`; constructor-form preparse rewrite macros.rkt:1779-1793 wraps `(the SchemaName brace)` + inject-schema-defaults + wrap-schema-checks (VERIFIED read)
- :check parsing: macros.rkt:852-867 parse-field-properties consumes `:check pred` (VERIFIED read)
- Typing accepts panic everywhere: racket/prologos/typing-core.rkt:2391 `[(expr-panic _) (expr-error)]` (infer) and :2773 `[((expr-panic msg) _)` (check) — both routes type-check clean (VERIFIED read)
- Panic is a stuck value in nf: racket/prologos/reduction.rkt:3311 `[(expr-panic msg) (expr-panic (nf msg))]  ;; reduce msg, stay stuck` — only 'panic' mention in reduction.rkt (VERIFIED grep+read)
- SOLE panic→error conversion: racket/prologos/driver.rkt:596-607 — eval arm computes `(nf (rewrite-specializations (zonk (freeze expr))))` then `(if (expr-panic? val) (prologos-error #f (format "panic: ~a" ...)) ...)`; grep confirms no other expr-panic? site in driver.rkt (VERIFIED)
- Inferred def path stores unforced: driver.rkt:1588 `(define zonked-body (rewrite-specializations (time-phase! zonk (freeze body))))` + :1598 `(global-env-add name zonked-type zonked-body)` — no nf, no expr-panic? check (VERIFIED read)
- Annotated def path identical: driver.rkt:1766 zonked-body + :1782 global-env-add — no nf, no panic check (VERIFIED read)
- Verbatim storage: racket/prologos/global-env.rkt:161-162 `(define (global-env-add name type value) (mnr-add-or-update! name (cons type value)))` (VERIFIED read)
- Use-site delta unfolding: reduction.rkt:3237-3242 whnf fvar case `(let ([val (global-env-lookup-value name)]) (if val (whnf val) e))` (VERIFIED read)
- Swallow classifier: reduction.rkt:1393-1407 definitely-not-map? — exception list includes `(expr-error? e)  ;; error propagation` but NOT expr-panic?; map-get whnf arm :2726-2733 `[(definitely-not-map? m*) (expr-fvar 'none)]` (graceful degradation); nil-safe-get :2734-2741 swallows to nil analogously (VERIFIED read)
- Probe A (top-level inline): /private/tmp/claude-501/-Users-avanti-dev-projects-prologos/e11f1cd7-a020-4225-91af-fdf4803e15a8/scratchpad/probe-a-toplevel.prologos — output `1: ERROR: panic: Person: field :age failed check (> _ 0)` (VERIFIED ran)
- Probe B (unannotated def + uses): .../scratchpad/probe-b-def.prologos — output `1: bad : probeb::Person defined.` (SILENT), `2: ERROR: panic: Person: field :age failed check (> _ 0)` (bare use fires), `3: none : String` ([map-get bad :name] swallowed) (VERIFIED ran)
- Probe C (annotated def + dot access): .../scratchpad/probe-c-annotated.prologos — output `1: bad2 : probec::Person defined.` (SILENT), `2: ERROR: panic: ...` (bare use fires), `4: {:name "ann", :age 33} : probec::Person` (passing check reduces at use), `5: none : String` (bad2.name swallowed) (VERIFIED ran)
- Stored-value inspection: .../scratchpad/inspect-stored-body.rkt — `stored value pp: [[fn [x <_>] [boolrec ... x (panic "Person: field :age failed check (> _ 0)") [< 0 [map-get x :age]]]] [the Person [map-assoc [map-assoc {} ...] :age 0]]]` — the UNREDUCED redex, condition unevaluated: lazy storage, NOT a stored error value (VERIFIED ran)

## P5-S-censuses

**HEAD verified**: d3c37bd03e04b08024afa41f6f9e922fe3ab06cb (d3c37bd0, as expected; docs-only atop 02aad495)

**Verdict**: (a) The champ-vs-schema check arm (typing-core.rkt:2946-2948) is fed by NO production flow at HEAD — it is defensive dead code from Phase 1b (commit f9170a0e0). Structural reason (VERIFIED): `check` dispatches on `(match* (e (whnf t)))` — only the EXPECTED type is whnf'd, never the subject — and every whnf inside check (lines 2764/2985/3002/3029/3051/3162/3221) applies to types; expr-champ is constructed ONLY in reduction.rkt (map-op reduction :2338-2342, solve-answer conversion :234-252), which runs post-typecheck; def bodies are stored zonked-UNREDUCED (driver.rkt:1766,1782), so delta-unfolding mints champs only inside unify/reduction, never as check subjects; all-keyword literals elaborate to map-assoc chains (elaborator.rkt:2140-2145) hitting arms 2910/2942, not 2946. Candidate flows individually excluded: `the Schema x` (expr-ann → check on unreduced e1, typing-core.rkt:837-849); annotated defs (check/err on elaborated body, driver.rkt:1715); solve results (champs born at reduction, displayed but never re-checked, driver.rkt:584-599); .pnet caches (expr-champ has NO pnet-serialize registration — a cached champ would deserialize as a raw-vector impostor, not reach the arm); REPL/cell path (form-cells.rkt:307-322 emits surfs only); fact rows (driver.rkt:435 checks zonked unreduced terms); on-network typing (zero champ handling in typing-propagators.rkt). Direct-API probe VERIFIED the arm fires and BLANKET-ACCEPTS — even a wrong-field-typed champ ({:name 42 :age "bad"} vs Person) returns #t; qtt twins (qtt.rkt:2329-2331, 2348-2350) share both the dead status and the blanket-accept. (b) Width-partial census: 30 def-annotation sites (def x : Schema) across tests/examples/lib — 27 FULL, 3 constructor-route sites relying on default-fill (test-schema-properties.rkt:314,339,408; full at check time post-preparse), ZERO live width-partial-missing-required sites, ZERO {}-seed-then-assoc sites. Constructor/the-route sweep: 37 FULL, 3 missing-defaulted (fill saves them), 0 missing-required. So fill-or-error on the annotation path breaks NOTHING live for plain schemas — with one load-bearing carve-out: 10 selection-typed def sites (test-selection-typing.rkt:98,112,124,167,181,234; test-selection-compose.rkt:240; test-selection-paths.rkt:202,211,223) are width-partial vs the parent schema BY DESIGN, and the selection arms DELEGATE to the parent-schema check (typing-core.rkt:2927-2940) — fill-or-error must check against the selection's field subset or all 10 break. Bonus: punify-p3-acceptance.prologos:264,267 carry commented-out ASPIRATIONAL sites expecting FILL semantics (`def default-cfg : Config := {}` => all defaults filled). (c) VERIFIED at HEAD: `def m := {:name "x" :age 1}` then `[Person m]` → "ERROR: Could not infer type" — the preparse rewrite (macros.rkt:1779-1795) requires the literal `(SchemaName ($brace-params ...))` shape (:1783-1787), so `[Person m]` elaborates as application of the opaque (Type 0) fvar and fails inference, as expected. (d) VERIFIED at HEAD: `[the Person {:name "y" :age 2}]` type-checks (per-field, arms 2910→2942) but direct user `the` gets NO default injection — `[the Config {:port 9000}]` yields `{:port 9000} : Config` (no :host) while constructor `Config {:port 9000}` yields `{:host "localhost", :port 9000}`; inject-schema-defaults + wrap-schema-checks run only inside the constructor rewrite (macros.rkt:1790-1792), so direct `the` bypasses both defaults and :check assertions (defaults VERIFIED by probe; :check bypass verified by read).

**Route matrix**: Probed at HEAD d3c37bd0 (probe census-p5-cd.prologos, results 0-12): INLINE LITERAL x annotated-the ([the Config {:port 9000}]) = OK, per-field checked, NO defaults/:check (res 4,6). INLINE LITERAL x annotated-def (def c1 : Config := {:port 9000}) = OK, width-partial ACCEPTED, no fill (res 7-8). INLINE LITERAL x constructor (Config {:port 9000}) = OK, defaults injected via preparse rewrite (res 5). INLINE LITERAL x unannotated (def m := {...}) = infers closed record type {:age Int :name String} (res 2,9). DEF-BOUND x annotated-the ([the Config mm]) = ERROR 'Could not infer type' (res 10; conversion fallback: record type vs schema fvar has no subsumption arm — mechanism INFERRED from typing-core.rkt:3217-3239 read, error text VERIFIED). DEF-BOUND x constructor ([Person m]) = ERROR 'Could not infer type' (res 3; rewrite shape-gated at macros.rkt:1783-1787). CHAMP-PRODUCING EXPR x annotated-def (def s : Person := [map-assoc {:name \"x\"} :age 1]) = OK via map-assoc arm 2910 — champ never exists at typing time (res 11-12). Not probed: def-bound var directly under def-annotation (def x : Config := mm).

**Open flags**:
- expr-champ has NO pnet-serialize registration (pipeline.md core-pipeline item 6 gap) — currently latent because def bodies cache zonked-UNREDUCED, but any future nf-before-cache or value-caching change detonates the raw-vector-impostor class; same for expr-tchamp (not checked)
- The champ-vs-schema arm blanket-accepts with ZERO field validation (VERIFIED: wrong-field-typed champ → #t) — dead today, but a silent type-hole if any future flow re-checks reduced values (e.g. solve-result annotation, cached-value re-check); qtt.rkt:2329/2348 twins identical
- Fill-or-error migration MUST rework the selection delegation arms (typing-core.rkt:2927-2940 delegate to PARENT schema) or the 10 width-partial-by-design selection sites break; checking against the selection's field subset is the natural fix
- Route asymmetry: def-bound map under `the Schema` errors today ('Could not infer type') — record-<:-map subsumption exists for (Map K V) (typing-core.rkt:3227-3228) but no record-<:-schema arm; an annotation-path redesign should decide this route deliberately
- Direct `the Schema {..}` bypasses BOTH inject-schema-defaults AND wrap-schema-checks (:check assertions) — defaults bypass VERIFIED by probe; :check bypass verified by read only (macros.rkt:1790-1792 scope), no runtime :check probe run
- census_b.py field extraction is regex-based (WS layout + sexp heuristics); nested-brace literals and exotic schema layouts may be under-counted — the 0-missing-required conclusion was cross-checked by eyeball on the schema test files but is not parser-grade
- solve-results-annotated flow excluded by code-read only (reduction.rkt:234-252 runs post-typecheck; driver.rkt:596-599 display-only) — no runtime solve probe was run

**Evidence**:
- HEAD: git rev-parse HEAD = d3c37bd03e04b08024afa41f6f9e922fe3ab06cb
- typing-core.rkt:2762-2764 — (define (check ctx e t)) (match* (e (whnf t)) — subject NOT whnf'd; only whnf calls inside check body are at 2764,2985,3002,3029,3051,3162,3221, all on types
- typing-core.rkt:2946-2948 — [((expr-champ _) (expr-fvar schema-name)) #:when (lookup-schema-by-name schema-name) #t] — blanket #t, no field validation; added by commit f9170a0e0 'Phase 1b: Schema as named type'
- typing-core.rkt:2910-2925 (map-assoc vs schema), 2942-2944 (map-empty vs schema #t), 2927-2940 (selection arms delegate to parent schema check)
- typing-core.rkt:1619 — [(expr-champ _) (expr-error)] ;; champ needs checking context (infer of champ always errors → conversion fallback can never accept a champ)
- typing-core.rkt:837-849 — expr-ann infer arm: (check ctx e1 t) with e1 unreduced; parser.rkt:1021-1031 (the T e) → surf-ann; elaborator.rkt:1298-1303 surf-ann → expr-ann
- elaborator.rkt:2136-2153 — all-keyword literal → expr-map-assoc chain over (expr-map-empty (expr-Keyword) (expr-Record 'keyword '() 'closed)); non-keyword → expr-map-literal; NEVER expr-champ (grep: expr-champ constructed only in reduction.rkt + registered in syntax.rkt)
- reduction.rkt:2338-2342 — champ minting: (expr-map-empty _ _) → (expr-champ champ-empty); map-assoc on champ → champ-insert
- reduction.rkt:234-252 — answers->prologos-expr: solve answers become literal (expr-champ champ-with-bound) — but only during reduction
- reduction.rkt:3237-3242 — whnf delta-unfolds global fvars ((global-env-lookup-value name) → whnf val) — champs from whnf surface in unify/reduction, not as check subjects
- driver.rkt:1766,1782 — zonked-body = rewrite-specializations (zonk (freeze body)); global-env-add name zonked-type zonked-body — def values stored UNREDUCED
- driver.rkt:584-599 — eval path: infer first (:584), nf for display only (:596-599), reduced value never re-checked
- driver.rkt:556-563 (top-level check command), 1467 (residuation finalize checks (expr-fvar referent)), 1715 (def body check/err), 413-439+745 (fact-row terms checked from zonked defr body) — full check/err caller set; typing-errors.rkt:207,222; typing-sessions.rkt:144-152 (proc payloads)
- pnet-serialize.rkt — grep 'champ' = only comments at :106,:424; NO reg0!/reg1!/regN!/auto-cache! for expr-champ; dynamic-ctor-cache populated only by explicit auto-cache! calls (:335-344); unknown tag → raw vector (:459-466 [else v]); champ.rkt:12-41 provides functions + tchamp-root only (champ-node struct not exported)
- typing-propagators.rkt — grep 'champ' = zero matches (no on-network champ handling)
- qtt.rkt:2329-2331, 2348-2350 — checkQ champ-vs-schema/selection twins, fed by zonked (unreduced) bodies at driver.rkt:1774 — same dead status
- macros.rkt:1779-1795 — constructor rewrite: fires only on (SchemaName ($brace-params ...)) with exactly one brace arg (:1783-1787); inject-schema-defaults (:1790) + wrap-schema-checks (:1792) live ONLY inside this rewrite
- form-cells.rkt:307-322 — cell path emits (def Name : (Type 0) (Type 0)) surf for schema forms; all cell-path check subjects are elaborated surfs, no champ feeder
- PROBE 1 (parts c,d): /private/tmp/claude-501/-Users-avanti-dev-projects-prologos/e11f1cd7-a020-4225-91af-fdf4803e15a8/scratchpad/census-p5-cd.prologos — key output lines: '3: ERROR: Could not infer type' ([Person m]); '4: {:port 9000} : censusp5::Config' ([the Config {:port 9000}] — no defaults); '5: {:host "localhost", :port 9000} : censusp5::Config' (Config {:port 9000} — defaults injected); '6: {:name "y", :age 2} : censusp5::Person'; '7: c1 : censusp5::Config defined.' + '8: {:port 9000} : censusp5::Config' (annotation path accepts width-partial, no fill); '10: ERROR: Could not infer type' ([the Config mm], def-bound); '12: {:name "x", :age 1} : censusp5::Person' (def s : Person := [map-assoc {:name "x"} :age 1] accepted via arm 2910, not 2946)
- PROBE 2 (part a, direct arm): /private/tmp/claude-501/-Users-avanti-dev-projects-prologos/e11f1cd7-a020-4225-91af-fdf4803e15a8/scratchpad/census-p5-arm-probe.rkt — output: 'check champ{name:"x" age:1} vs Person: #t'; 'check champ vs unknown fvar (control): #f'; 'check EMPTY champ vs Person: #t'; 'check WRONG-field-typed champ vs Person: #t'
- CENSUS scripts (part b): /private/tmp/claude-501/-Users-avanti-dev-projects-prologos/e11f1cd7-a020-4225-91af-fdf4803e15a8/scratchpad/census_b.py + census_b2.py — 34 schemas; 30 def-annotation sites (27 FULL, 3 constructor-route defaulted); constructor/the sweep: 37 FULL, 3 MISSING-DEFAULTED (test-schema-properties.rkt:314,339,408), 0 MISSING-REQUIRED (after per-file schema scoping)
- Selection width-partial-by-design sites (10): tests/test-selection-typing.rkt:98,112,124,167,181,234; tests/test-selection-compose.rkt:240; tests/test-selection-paths.rkt:202,211,223 — e.g. '(def u : NameOnly (the NameOnly ($brace-params :name \"alice\")))' vs parent User(name,age,email,address)
- examples/2026-03-20-punify-p3-acceptance.prologos:261-268 — ';; --- D9: Construct with defaults (omit defaulted fields) --- ;; ASPIRATIONAL — default-fill on construction ;; def default-cfg : Config := {}' (+ :921 ';; def server-config : DatabaseConfig') — commented sites expecting FILL semantics on the annotation path

## P6-P-updatein-removal

**HEAD verified**: d3c37bd03e04b08024afa41f6f9e922fe3ab06cb

**Verdict**: The D20 update-in-dynamic arm's spine labels are stable-under-shrink for every NON-EMPTY dynamic path — the fn receives only the VALUE at the path (reduction.rkt:3579) and every spine level is rebuilt with map-assoc/champ-insert (:3584, :2340-2342), which structurally cannot delete a key (probe-verified end-to-end: a dissoc-ing fn shrank only the nested map; the sibling spine key survived) — so a fn like [fn [sub] [map-dissoc sub :k]] shrinks only the NESTED map at the path, which the per-field fresh metas already cover. BUT 'labels + per-field fresh metas + presence=present' is NOT unconditionally sound, for two probe-verified reasons: (1) SHRINK via the empty path — `path::tail`/`from-segments` produce `#p()` at runtime (path-ops.rkt:120-122; the literal `#p()` doesn't parse, so this is exclusive to the dynamic arm), and with zero segments the arm applies fn to the WHOLE map and returns fn's output as the entire result (:3578-3579): probe showed {:a {:x 1 :y 2} :b 3} → {:a {:y 2, :x 1}} with top-level :b DELETED and projection on :b a runtime <error> — presence=present would type that projection as safe; (2) GROWTH on a missing-key path — map-assoc inserts the path key if absent (probe: :zzz appeared with the fn's result 42), so the label set is only ⊇-stable; harmless if the posture keeps a 'dyn tail, but it breaks any closed-row/exact-labels reading and inserts silently on what is arguably an invalid path. CONCLUSION: the proposed posture is sound iff the empty-path case is closed out (e.g., expr-update-in raises on zero-segment paths at runtime — consistent with path-head/path-tail already erroring on empty, path-ops.rkt:110/:122) AND the result row keeps a dyn tail to absorb missing-key growth; with those two guards, the second D20 arm needs NO presence machinery (presence=present is then provably correct for the spine), and all type-uncertainty is confined to the per-field fresh metas.

**Route matrix**: Static-literal path (inline or def-bound result): desugars at elaborator (:2318-2369), fully reduces, typing tracks structure exactly — result typed {:a [Map Keyword Int] :b Int}, keys '[:a :b]. Dynamic path × annotated fn: types { | _} (D20 arm), runs. Dynamic path × UNANNOTATED fn: identical — no type error, { | _}; update-in's typing rule ignores fn's type entirely (typing-core:1589 infer-only). Inline-expression vs def-bound TARGET: no difference (p6-full results 15/16 identical). EXCEPTION route: `def r := [update-in m p <inline-lambda>]` fails with 'Multiplicity violation' regardless of annotation form ([Map Keyword Int] and record-type both), while hoisting the same lambda to its own def works — QTT route-sensitivity, flagged not diagnosed. Consumption routes: whnf-level consumers (map-keys/map-size, static-get-in→map-get chains) over a dynamic update-in result are STUCK or silently yield `none`; dynamic get-in (expr-get-in nf arm, which nf's its target first) forces full reduction and was used for all end-to-end evidence.

**Open flags**:
- EVALUATION BUG (silent value loss): expr-update-in and expr-get-in have NO whnf case (nf-only, reduction.rkt:3561/:3570) and are MISSING from definitely-not-map?'s exception list (:1393-1407), so map-get over a whnf-stuck dynamic update-in result returns `none` instead of the value (p6-edge #8: expected 3, got none; also poisons static get-in over dynamic update-in). Exactly the pipeline.md new-AST-node checklist failure mode (reduction.rkt step: 'check definitely-not-map? if the node could appear as a value').
- EVALUATION WART: the nf dynamic-update-in arm returns its map-assoc spine UN-normalized (`(expr-map-assoc base key updated)` at :3584 and `(build nt segs)` at :3585 lack an nf/whnf wrap), so even top-level results print as stuck `[map-assoc {…} :a {…}]` terms; map-keys/map-size over them stay stuck through def hops (p6-stuck #8).
- QTT: `def r := [update-in m p [fn …]]` with an INLINE lambda → 'Multiplicity violation'; the same lambda def-bound first works (p6-edge #4 vs p6-qtt #5). Not diagnosed further (out of P6 scope).
- SEMANTICS QUESTION for D20: missing-key dynamic update-in silently INSERTS the key with fn applied to the miss artifact (p6-force #10: :zzz→42) — is insert-on-miss intended, or should an invalid dynamic path be a runtime miss/error? Interacts with the growth caveat in the verdict.
- Empty-path closure needed if the posture lands: expr-update-in should reject zero-segment paths at runtime (consistent with path-head/path-tail erroring on empty, path-ops.rkt:110/:122); until then the { | _} degrade is the only sound typing for the dynamic arm.
- Housekeeping: path-ops.rkt .zo was linklet-stale at session start (foreign import of prologos/path-ops failed); fixed by `raco make path-ops.rkt`. Also `map-has-key` at a prelude ns errored 'Unbound variable' in one probe (p6-full #10) — not chased.

**Evidence**:
- git rev-parse HEAD = d3c37bd03e04b08024afa41f6f9e922fe3ab06cb (expected d3c37bd0) — VERIFIED
- elaborator.rkt:2312-2369 — static-path update-in desugars at elaboration: leaf `[(null? segs) (expr-app ef base)]` (:2345), spine `(expr-map-assoc base key updated)` (:2350); dynamic path → `(expr-update-in et elab-path ef)` (:2352-2353) — VERIFIED (read)
- reduction.rkt:3570-3586 — the ONLY evaluation site for expr-update-in (nf-whnf arm; grep across racket/prologos/*.rkt confirms no whnf case): `(define (build base segs) (cond [(null? segs) (nf (expr-app nf-fn base))] [else ... (define sub (nf (expr-map-get base key))) (define updated (build sub (cdr segs))) (expr-map-assoc base key updated)]))` — fn applied ONLY at the leaf; every spine write is map-assoc; arm output NOT re-normalized — VERIFIED (read)
- reduction.rkt:2340-2342 — `(expr-map-assoc (expr-champ c) k v)` → `champ-insert` (insert-or-replace, never deletes); :2405-2407 map-dissoc → champ-delete — VERIFIED (read)
- path-ops.rkt:114-122 — `path-tail` on 1-segment path returns `(expr-path (list (cdr segs)))` = `(expr-path '(()))` — the EMPTY path; :46-81 path-from-segments on nil likewise — VERIFIED (read + probe p6-smoke result 5: `p-empty : Path` prints `#p()`)
- probe p6-smoke.prologos result 1 (STATIC path + dissoc-ing fn): `{:a {:y 2}, :b 3} : {:a [Map Keyword Int] :b Int}` — nested map shrank, spine intact — VERIFIED (ran)
- probe p6-force.prologos results 7/8 (DYNAMIC path, forced via dynamic get-in): `get-in r-dyn p-a` → `{:y 2}`; `get-in r-dyn p-b` → `3` — spine sibling SURVIVES a dissoc-ing fn — VERIFIED (ran)
- probe p6-force.prologos results 10/11 (missing-key GROWTH): `def rz := [update-in m p-z const42]` with p-z=#p(zzz) absent from m; `get-in rz p-z` → `42` — the key was INSERTED — VERIFIED (ran)
- probe p6-empty2.prologos results 6/8 (EMPTY dynamic path SHRINKS THE SPINE): `def re := [update-in m p-empty dissoc-b]` → re = `{:a {:y 2, :x 1}}` (top-level :b DELETED); `get-in re p-b` → `<error>` (runtime miss on a spine label) — VERIFIED (ran)
- probe p6-emptylit.prologos: `#p()` literal does NOT parse (Unbound variable) — empty path is dynamic-arm-only — VERIFIED (ran)
- typing-core.rkt:1586-1598 — current D20 update-in arm: dynamic-only comment + `(make-record (expr-Record-key-domain rec) '() 'dyn)` = {| _}; fn's inferred type IGNORED (`(define _ft (infer ctx fn))` :1589, no domain/codomain unification) — VERIFIED (read)
- typing-core.rkt:1807-1819 — D20 map-dissoc dynamic-key arm degrades to {| _} — VERIFIED (read)
- reduction.rkt:1393-1407 — `definitely-not-map?` exception list has NO expr-update-in/expr-get-in entry; :2726-2732 map-get over whnf-stuck subject → `(expr-fvar 'none)`; probe p6-edge result 8: inline `[map-get [update-in m p-a f] :b]` → `none` (expected 3) — SILENT VALUE LOSS — VERIFIED (read + ran)
- probe files: /private/tmp/claude-501/-Users-avanti-dev-projects-prologos/e11f1cd7-a020-4225-91af-fdf4803e15a8/scratchpad/{p6-smoke,p6-full,p6-edge,p6-qtt,p6-stuck,p6-emptylit,p6-force,p6-empty2}.prologos
- session note: path-ops.zo was linklet-stale vs syntax.rkt (F1a-era drift); refreshed via `raco make path-ops.rkt` (compiled cache only, no source edits)

## P7-T-commitment-boundaries

**HEAD verified**: d3c37bd03e04b08024afa41f6f9e922fe3ab06cb

**Verdict**: The real commitment boundaries are :735/:739 (defr), :1598/:1604 (unannotated def), :1782/:1788 (annotated def), plus :3062/:3069 (foreign) and :1664/:1670 (data-type) — all freeze'd but only :1782 conditionally scrubbed (has-holes? gate) and :1598/:735 never scrubbed; :1704/:1707's deferral store is the ONLY unconditional unsolved-metas-to-holes site and it is deliberately TRANSIENT: when the placeholder completes, the sweep (run-general-residuation-sweep! :1262-1287, fired at file-end via run-residuation-fixpoint! :2391 and on demand via process-command/demand :2374) re-runs process-command on the stored ORIGINAL surf (:1277), which traverses the full process-def path and re-commits through :1641 then :1782/:1788 (or :1598/:1604), REPLACING the :1704 safe-ty because def-entry-merge is per-component LWW with ':type new-wins' (definition-entry.rkt:96) — empirically proven by the deferral probe where completed g/h yield real values (only producible by the full re-commit, since :1704 stores value=#f and def-value-lww keeps old only when new=#f). So :1704 does NOT persist as final on any completing path (it persists only as a 'ground-with-#f ghost when the placeholder NEVER completes, alongside a file-end unbound error) — making :1704 the wrong place to error on a scrubbed meta: the type there is a mid-file read surface, not the commitment. The RAW-meta leak is empirically CONFIRMED at the :1598 boundary on the open-row route: `def x := [map-get m :c]` over a map-assoc-built map stores `x : ?meta1610` verbatim (freeze passes unsolved expr-metas through, zonk.rkt:1053), and a later command shows the dangling `?meta1609` with zero prologos-errors; the closed-row literal route errors correctly instead.

**Route matrix**: Open-row map (map-assoc chain, m : {:a Int :b String | _}): inline unannotated `[map-get m :c]` → `<error> : ?meta1605` printed, NO prologos-error; def-bound unannotated `def x := [map-get m :c]` → `x : ?meta1610 defined.` (RAW meta committed at :1598); later-command `x` → `<error> : ?meta1609` (dangling raw meta, still 0 errors). Closed-row map (literal, lit : {:a Int :b String}): inline unannotated → proper error 'field :c is not present... available fields: :a :b'; def-bound unannotated → same error, nothing stored (later `y` = Unbound variable); def-bound annotated `def z : Int` → Type mismatch, nothing stored. So the RAW-meta leak is ROUTE-SENSITIVE: only the open-row (D19 projection-meta) route leaks; closed-row misses error correctly on all routes. Annotated-over-open-row: not probed (flagged).

**Open flags**:
- unsolved-metas-to-holes (driver.rkt:196-203) recurses ONLY through Pi/Sigma/app/lam — an expr-meta nested inside any other compound type node (record/map rows, unions, tycon apps) survives even where the scrub IS applied; the scrub is shallow (read-verified, not probed)
- the :1782 annotated-commit scrub is gated on has-holes? — annotated types carrying metas from implicit-param elaboration (the very case type-contains-meta? at :1627 acknowledges) are stored UNscrubbed; unprobed
- raw-meta stored types propagate verbatim into module capture (driver.rkt:2682/2711) and .pnet snapshot reconstruction (namespace.rkt:392-398) — a library def with an open-row projection would carry the dangling ?meta cross-module; unprobed
- never-completed annotated deferral leaves the :1704 'ground-with-#f type-only ghost in the mnr (finalize-residuations :1451-1460 emits the error but performs no remove-failed-definition!) — benign per the :1691-1693 comment, but the entry outlives the error
- capability cap-type (:775, built elaborator.rkt:3854-3874) is elaborated but never freeze'd/scrubbed — raw-meta risk if a param type elaborates to a meta (low likelihood, param types typically concrete)
- the raw-meta leak run exits '--- 0 errors ---' with a stuck '<error>' VALUE — no prologos-error is raised anywhere on the open-row missing-key path, so the leak is silent at the file gate
- annotated def over the OPEN-row (map-assoc-built) route not probed (closed-row annotated route errors with Type mismatch); the later-reference meta-id drift (?meta1610 stored vs ?meta1609 displayed) not root-caused — both are dangling either way

**Evidence**:
- git rev-parse HEAD → d3c37bd03e04b08024afa41f6f9e922fe3ab06cb (docs-only atop 02aad495, as expected)
- driver.rkt:733-735 (defr commit): '(let ([zonked-body (time-phase! zonk (freeze expr))] [zonked-type (time-phase! zonk (freeze ty))]) (global-env-add name zonked-type zonked-body)' — freeze'd, NOT scrubbed; FQN twin :739
- driver.rkt:1588-1589+1598/1604 (unannotated commit): '(define zonked-type (time-phase! zonk (freeze inferred-type)))' → '(global-env-add name zonked-type zonked-body)' — freeze'd, NEVER unsolved-metas-to-holes'd
- driver.rkt:1767-1768+1782/1788 (annotated commit): '(define zonked-type (if has-holes? (unsolved-metas-to-holes zonked-type-raw) zonked-type-raw))' — scrub CONDITIONAL on has-holes? only
- driver.rkt:1704-1710 (deferral placeholder store): '(define safe-ty (unsolved-metas-to-holes (freeze type*))) (global-env-add-type-only name safe-ty)' — the ONLY unconditional freeze+scrub site; comment :1694-1696 'it is RE-WRITTEN META-FREE below: type* may hold holes→metas that dangle after reset-meta-store! (the stored-type discipline)'
- driver.rkt:1641/1645 (annotated pre-register): '(global-env-add-type-only name type*)' — type* = holes-to-metas output (:1634), un-freeze'd, un-scrubbed, transient
- driver.rkt:1663-1670 (data-type defs): '(let ([zonked-type (time-phase! zonk (freeze type))]) (global-env-add-type-only name zonked-type)' — freeze'd, not scrubbed
- driver.rkt:764/766, 883/887, 922/926, 942, 952: constant '(expr-Type 0)' stores (selection/session/defproc) — meta-free by construction; typing-core.rkt:660 same with '(expr-Type (lzero))'
- driver.rkt:775/777 (capability): stores cap-type built at elaborator.rkt:3854-3874 (elaborated Pi chain over param types) — NOT freeze'd, NOT scrubbed
- driver.rkt:3002+3062/3069 (foreign): '(define zonked-type (freeze type-expr))' → full-type (+ m0 cap Pi binders :3009-3015) → '(global-env-add prologos-name full-type val)' — freeze'd, not scrubbed; elaborator.rkt:3375 foreign-block twin
- driver.rkt:2682+2711 (module capture): '(set! mod-env (global-env-snapshot))' → '(module-network-add-definition mnr name entry)' — verbatim re-store, inherits scrub state; namespace.rkt:392-398 module-network-from-snapshot = verbatim .pnet reconstruction
- definition-entry.rkt:94-97 (LWW proof): '[(and (def-entry? old) (def-entry? new)) (def-entry (def-entry-type new) (def-value-lww (def-entry-value old) (def-entry-value new)))]' — :type new-wins; :79-80 def-value-lww '(if (eq? new-v #f) old-v new-v)'
- global-env.rkt:121-124+161-169: global-env-add/-type-only → mnr-add-or-update! 'module-network-write (merge-replace = last-write-wins)'
- driver.rkt:1277 (sweep re-run): '(define result (process-command (general-body-placeholder-surf ph)))' inside run-general-residuation-sweep! :1262-1287; gated on no referent still 'pending (:1273-1275)
- driver.rkt:2374 (demand trigger): '[result (process-command/demand surf)]' + :1211-1224 residuation-demand-name; :2391 '(run-residuation-fixpoint!)' file-end drive+sweep fixpoint; :2397 finalize-residuations
- driver.rkt:1451-1460 (never-completes): general-body-placeholder slot → unbound-variable-error from completions-map miss; NO remove-failed-definition! → :1704 type-only ghost persists in the mnr
- driver.rkt:196-203 unsolved-metas-to-holes: '[(expr-meta _ _) (expr-hole)]' but recursion ONLY through expr-Pi/Sigma/app/lam — metas nested in other compound nodes pass unscrubbed
- zonk.rkt:1040-1043+1053: freeze = zonk + default-metas; default-metas '[(expr-meta _ _) e]' — unsolved expr-metas SURVIVE freeze unchanged (only level/mult/num-lit defaulted)
- PROBE /private/tmp/claude-501/-Users-avanti-dev-projects-prologos/e11f1cd7-a020-4225-91af-fdf4803e15a8/scratchpad/p7-raw-meta-leak.prologos → '0: m : {:a Int :b String | _} defined.' / '1: <error> : ?meta1605' / '2: x : ?meta1610 defined.' / '3: <error> : ?meta1609' / '--- 0 errors ---'
- PROBE .../scratchpad/p7-routes.prologos (literal/closed-row) → '1: ERROR: Could not infer type — field :c is not present in the record {:a Int :b String}; available fields: :a :b' / '3: ERROR: Unbound variable' / '4: ERROR: Type mismatch'
- PROBE .../scratchpad/p7-defer-lifecycle.prologos → '0: g : Int defined.' / '1: h : Int defined.' / '2: f : Int -> Int defined.' / '3: 1 : Int' / '4: 2 : Int' — deferred-then-completed g yields a real VALUE, only producible by the re-run's full global-env-add (:1704 stores value=#f; def-value-lww keeps old only on #f-new)

## P8-W-unify-internal-reality

**HEAD verified**: d3c37bd03e04b08024afa41f6f9e922fe3ab06cb (matches expected d3c37bd0; git rev-parse HEAD run first)

**Verdict**: There is NO live production population of closed-row-vs-closed-row width events at HEAD that bypasses check — check-side attachment (A/E/C) is empirically sufficient TODAY, but only because the width event is barely expressible: record-type annotations do not parse ("Expression is not a valid type", probe 1), row-typed specs/lam-domains are unwritable, and bare `defn f [r] r.a` fails, so both-sides-concrete-row unifications arise almost exclusively at the D15 literal-homogeneity probes (typing-core.rkt:1933/:1969) where the width FAILURE is load-bearing semantics (heterogeneous → tuple: `@[{:a 1} {:a 2 :b 3}]` → `⟨{:a Int} {:a Int :b Int}⟩`, verified). Per enumerated path: (a) L2 constraint retry (metavar-store.rkt:1663 process-resolution → resolution.rkt:287 → retry-unify-constraint-pure:196 unify-core) never re-enters check, but its constraints are minted at exactly ONE production site (unify.rkt:917, flex-app Miller-pattern failure) with lhs = applied meta — lhs whnfs to a row only via a record-returning type function, which no surface can write; empirically the full records acceptance file runs with constraint_count=0, constraint_retries=0. (b) ALL unify-internal recursion ('sub/'pi/'binder/'union/'retry, unify.rkt:814/:824-841/:844/:872) re-enters unify-core→unify-whnf→classify-whnf-problem, so classify's record arms (B3 same-label 'sub :599-607, C_Cons ≥1-dyn :617-631, record→Map/PVec '(ok) :582-585) apply at every depth — but a width rule attached only in check applies at NO nested depth: closed-vs-closed different labels falls to '(conv)→conv-nf→#f by design ("falls through to [else '(conv)] ... deferred"). (c) Trait/instance matching never uses unify: monomorphic = string-key lookup (trait-resolution.rkt:133-141), parametric = match-one with plain equal? (:172-217) — no width semantics, no check involvement, population zero (expr->impl-key-str has no Record case). (d) punify is OFF: `(make-parameter #f)` at ctor-registry.rkt:390 (flip attempt reverted per comment); sole enablement is test-only (tests/test-punify-integration.rkt:84) — not a production path. (e) B1 empty-seed (typing-core.rkt:2896-2903) unifies v1~v2 only when v1 is NOT an empty-closed record, but elaboration mints seeds exclusively with v1 ∈ {empty 'dyn row, empty 'closed row, fresh meta} (elaborator.rkt:2133-2151) — row-vs-row population zero. THE REAL GAP is forward-looking, not current: the moment record-type annotations become writable, three direct-unify surfaces that never reach check's conversion fallback go live simultaneously — (1) check's lam-vs-Pi arm unifies annotated lam domain vs expected Pi domain directly (typing-core.rkt:2801; arm-match precludes fallthrough to the fallback), (2) HOF infer arms unify Pi-domain vs element type and return expr-error (typing-core.rkt:2176/:2199/:2313-2344), (3) nested rows in containers/Pi components/unions resolve entirely inside unify (per (b)), plus the α-helpers' internal field-vs-elem unify∨subtype? steps (typing-core.rkt:495/:553) where subtype? has NO record<:record arm (subtype-predicate.rkt:115-135, "NOT a record<:record judgment (D11 preserved)"). Additionally any check-side attachment carries a mandatory QTT-twin co-attachment (qtt.rkt:2238 lam-vs-Pi twin, :2700-2711 union+conversion-fallback twins — the B2 record-α mirror precedent exists). Finally, a width rule placed INSIDE unify/classify instead of check would CORRUPT D15 literal-extent semantics (homogeneity probes rely on row-vs-row unify failing) — an argument FOR the check-side placement, with the enumerated forward carve-outs named as explicit non-coverage.

**Route matrix**: Typing probes ran (route sensitivity): [inline literal × annotated] `def r2 : {:a Int} := {:a 1 :b 2}` → ERROR 'Expression is not a valid type' (record-type annotation unparseable — width event not expressible); [def-bound × annotated] `def r3 : {:a Int} := wide` → same parse error; [inline literal × unannotated] `@[{:a 1} {:a 2 :b 3}]` → `⟨{:a Int} {:a Int :b Int}⟩` and `'[{:a 1} {:a 2 :b 3}]` → same tuple (width failure at typing-core:1933/:1969 consumed as D15 heterogeneity — deliberate semantics, not a gap); [homogeneity control] `'[{:a 1} {:a 2}]` → `List {:a Int}` (classify B3 same-label 'sub arm works); [unannotated match arms, wide AND same-shape controls] both → 'Could not infer type' (generic infer-has-no-expr-reduce failure, not width-specific); [unannotated defn over projection] `defn getA [r] r.a` → 'Type mismatch' (row-domain functions unwritable). Net: no writable route produces a closed-vs-closed different-label row unify outside the literal-heterogeneity detectors.

**Open flags**:
- On-network Role-B row events: try-unify-pure (type-lattice.rkt:401) has NO expr-Record arm, so ANY row-vs-row on a unify-propagator cell (even same-label, B3-decomposable) → #f → type-top. Whether any production elaboration actually installs unify propagators whose cells carry expr-Record values is UNVERIFIED (record typing appears to ride the imperative path via the driver.rkt:585 op-⊥ fallback per typing-core comments) — needs a targeted grounding before F1b lands record annotations.
- resolution-execute-action! (impure twin, resolution.rkt:151-173) is provided but has zero production callers found (only the pure variant is wired at driver.rkt:3304) — dead-code candidate; checked non-test/non-benchmark .rkt only.
- The 'retry classification arm (unify.rkt:844) re-enters unify-whnf directly WITHOUT the unify-core re-zonk — inconsequential for rows today (normalize-for-resolution never produces records, unify.rkt:197-211) but a re-dispatch asymmetry worth knowing.
- expr->impl-key-str (trait-resolution.rkt:80-127) has no expr-Record case → record instance heads would key via pp-expr strings; population zero today, a latent foot-gun once rows become annotatable in impl heads.
- Width-event population is gated on record-type annotation syntax: the moment it lands, the three named direct-unify surfaces (check lam-vs-Pi typing-core:2801, HOF infer arms :2176/:2199/:2313-2344, nested rows inside unify recursion + α-helper internals :495/:553) and the QTT twins (qtt.rkt:2238/:2700) all go live at once — any check-side option A/E/C should carry these as explicit named non-coverage or co-attachment obligations, and D15 heterogeneity probes (typing-core:1933/:1969) must be pinned as intentionally width-failing.

**Evidence**:
- HEAD: d3c37bd03e04b08024afa41f6f9e922fe3ab06cb (git rev-parse HEAD)
- (a) racket/prologos/resolution.rkt:67 + :196 — `(define result (unify-core (constraint-ctx c) lhs rhs))` on zonked constraint lhs/rhs; outcome written to constraint status ('solved/'failed), no check re-entry [VERIFIED read]
- (a) racket/prologos/metavar-store.rkt:1663-1682 process-resolution + :1684-1687 `(register-stratum-handler! resolution-stratum-request-cell-id process-resolution #:tier 'value ...)`; executor wired at driver.rkt:3304 `(current-resolution-executor-pure resolution-execute-action-pure)` [VERIFIED read]
- (a) racket/prologos/unify.rkt:917 — the ONLY production add-constraint! site (grep over non-test .rkt): flex-app Miller-pattern failure, provenance string "pattern condition failed for applied metavariable"; lhs is always an applied meta at creation [VERIFIED grep+read]
- (a-empirical) run of examples/2026-07-06-ciu-t6-f1-records.prologos: PERF-COUNTERS constraint_count=0, constraint_retries=0, unify_steps=101 — record-heavy corpus creates zero postponed constraints [VERIFIED ran]
- (b) racket/prologos/unify.rkt:490-513 unify-core (zonk + unify-whnf), :848-851 `(dispatch-unify-whnf ctx a b (classify-whnf-problem a b))`, sub-goal recursion all via unify-core: :814 ('sub), :824-829 ('pi), :836-841 ('binder), :844 ('retry → unify-whnf), :872 ('union); unify-spine :531-537 → unify-core; solve-flex-rigid already-solved arm :457 → unify-core — NO chain bypasses classify [VERIFIED read]
- (b) racket/prologos/unify.rkt:582-585 record→Map/PVec coercion '(ok) arms; :599-607 B3 same-label 'sub arm with comment "a DIFFERENT label-set falls through to [else '(conv)] (the F1a-col flavor-B union-widen case, deferred)"; :617-631 C_Cons ≥1-'dyn arm; reduction.rkt:3820 conv-nf = deep structural equality → different-label closed rows fail [VERIFIED read]
- (c) racket/prologos/trait-resolution.rkt:133-141 try-monomorphic-resolve = string impl-key lookup; :172-217 match-one datum matcher with `(define (expr-equal? a b) (equal? a b))`; expr->impl-key-str (:80-127) has NO expr-Record case (falls to pp-expr) — trait/instance matching never calls unify [VERIFIED read]
- (d) racket/prologos/ctor-registry.rkt:390 `(define current-punify-enabled? (make-parameter #f))` + comment "Track 10B Phase A5: Toggle flip ATTEMPTED ... Reverted to #f"; sole enablement tests/test-punify-integration.rkt:84; relations.rkt:1479 is term-level solver env, not type rows [VERIFIED grep+read]
- (e) racket/prologos/typing-core.rkt:2896-2903 B1 arm — `(if (and (expr-Record? v1) (null? (expr-Record-fields v1))) (unify ctx k1 k2) (and (unify ...k...) (unify ctx v1 v2)))`; elaborator.rkt:2133-2151 mints map-empty seeds only with empty-'dyn row / empty-'closed row / fresh meta as v1 [VERIFIED read]
- (e) typing-core.rkt:2781-2801 check lam-vs-Pi arm: `[(not (unify-ok? (unify ctx a t-dom))) #f]` — direct unify of annotated lam domain vs expected Pi domain, arm-match precludes conversion-fallback fallthrough [VERIFIED read]
- (e) typing-core.rkt:2176/:2199 (pvec-map infer) + :2313-2344 twins — `(unify ctx dom v)`/`(unify ctx dom a)` dom-vs-elem, failure → (expr-error), no check re-entry [VERIFIED read]
- (e) typing-core.rkt:1927-1941 (list-literal) + :1964-1980 (pvec-literal) D15 homogeneity probes — `(unify ctx (car tys) ti)` under with-speculative-rollback; failure → `(make-record 'nat ... 'closed)` tuple [VERIFIED read]
- (check fallback) typing-core.rkt:3216-3239 conversion fallback: `(unify ctx t t1)` then Map/PVec/List←Record α arms then `(subtype? t1-w t-w)`; general app path :923-925 routes args through check [VERIFIED read]
- (no record<:record) subtype-predicate.rkt:115-135 — subtype? arms: equal?/flat/Record→Map α/Record→PVec α/SRE; comment "This is the record→Map bridge, NOT a record<:record judgment (D11 preserved)"; compound-type? (:108-113) excludes expr-Record [VERIFIED read]
- (α-helper internals) typing-core.rkt:495 `(or (subtype? ft br) (unify-ok? (unify ctx ft br)))` in record-<:-elem?; :553 `(or (unify-ok? (unify ctx ft vt)) (subtype? ft vt))` in record-<:-map? — nested field-vs-elem width surface [VERIFIED read]
- (QTT twins) qtt.rkt:2238 lam-vs-Pi `(unify ctx a t-dom)`; :2700 union both-fail `(unify ctx (expr-union l r) t1)`; :2704+ QTT conversion fallback with "CIU T6 F1 (B2): reach the Record<:Map subsumption from the QTT pass" mirror precedent [VERIFIED read]
- (on-network adjacent) type-lattice.rkt:203-220 type-unify-or-top → try-unify-pure (:401); try-unify-pure has NO expr-Record arm (grep 'Record' in type-lattice.rkt hits only :117-118 has-unsolved-meta?) — row-vs-row on Role-B unify propagators (elaborator-network.rkt:143/:196, typing-propagators.rkt:653) → #f → type-top even for B3-decomposable same-label rows [VERIFIED read+grep]
- (probe files) /private/tmp/claude-501/-Users-avanti-dev-projects-prologos/e11f1cd7-a020-4225-91af-fdf4803e15a8/scratchpad/p8-probe1.prologos — `def r2 : {:a Int} := {:a 1 :b 2}` → 'ERROR: Expression is not a valid type' (both inline and def-bound routes) [VERIFIED ran]
- (probe files) .../scratchpad/p8-probe2.prologos + p8-probe3.prologos — `@[{:a 1} {:a 2 :b 3}]` → '⟨{:a Int} {:a Int :b Int}⟩'; `'[{:a 1} {:a 2 :b 3}]` → tuple; control `'[{:a 1} {:a 2}]` → '[prologos::data::list::List {:a Int}]' (B3 succeeds); match arms fail 'Could not infer type' for BOTH same- and different-shape records (generic, not width-specific) [VERIFIED ran]
- (probe files) .../scratchpad/p8-probe4.prologos — `defn getA [r] r.a` without spec → 'ERROR: Type mismatch'; row-domain functions unwritable at HEAD [VERIFIED ran]
