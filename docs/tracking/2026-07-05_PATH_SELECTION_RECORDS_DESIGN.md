# Path Selection & Anonymous-Record (Map) Typing — Design (Stage 0/1)

**Status**: F1 (record typing) co-design **CONVERGED** 2026-07-06 (D1–D12 locked) → Stage-3 D.1 at [`2026-07-06_CIU_T6_F1_STRUCTURAL_RECORDS_DESIGN.md`](2026-07-06_CIU_T6_F1_STRUCTURAL_RECORDS_DESIGN.md); V2–V4 selection-syntax threads remain design-open. **CIU Series, Track 6** (Anonymous Records & Path Selection; owner-designated 2026-07-05). Linked from [`2026-03-21_CIU_MASTER.md`](2026-03-21_CIU_MASTER.md).
**Date opened**: 2026-07-05
**Owner**: Zee Larson
**Motivating incident**: `m.{a.a1 b.b1}` reported a misleading "`.{ }` retired for mixfix" error (the `.{`→`.(` mixfix migration clobbered path-selection `.{`). Immediate fix landed (message reworded, [issue context](#immediate-fix)); the deeper desire is to **revisit Path Selection — gaps, ergonomics, and language-design changes** — together with **how anonymous records (`Map`) are typed**.

---

## §1 Purpose & scope

Two intertwined threads:

1. **Anonymous-record (`Map`) typing** — make `Map` behave as a first-class *anonymous record* with *structural / observational* typing (the type of `{:a 1}` is `{:a Int}`, so `{:a 1}.a : Int`), retiring the current opaque `Open` value type. `schema` remains the *closed / exact* record form; `Map` is the *open / anonymous* form.
2. **Path Selection** — a redesigned, ergonomic surface for selecting sub-trees out of nested records/collections in one line, with control over the *shape* of the result (leaves only / paths preserved / renamed / everything-at-a-level), postfix syntax, broadcast, and a possible **Array ⇄ Map** unification.

This document is the co-design home; it will grow through a grounding audit → design dialogue → phased implementation.

## §2 Owner's vision (captured 2026-07-05)

Faithful capture; reflections in §3.

- **V1 — Map = anonymous record; coinductive/observational typing.** `Map` should work like a dynamic-language record. The type of an anonymous record = the *observed* types/type-literals of the values at its keys. `Open` is "too weird" a name and, more importantly, erases the field types. `schema` already provides the closed/exact form. *Concrete goal*: `+ {:a 1}.a 1  ;; => 2 : Int` (today `:a` is typed `Open`, not the observed `Int`).
- **V2 — `_{ … }` postfix-juxtaposition selection syntax.** Replace the `.{ … }` surface with postfix juxtaposition: `{:a {:a1 1 :a2 2} :b {:b1 11}}{a.a2 b.b1}` — like postfix array indexing. Deep note: **Arrays/Vectors and Maps/Records are different perspectives on the same class of data structure** — a candidate for *unification* (indexed access over Nat-keys vs Keyword-keys).
- **V3 — broadcast selection.** Same postfix-juxtaposition syntax. Two ideas: (a) an explicit broadcast marker in first position inside the brackets — `'[{:a 1 :b 11} {:a 2 :b 22}]{+ a}  ;; => '[{:a 1} {:a 2}]`; (b) **implicit** broadcast — path-selection on a *collection of records* automatically maps over the collection (array-programming style).
- **V4 — result shape: keep the path/keys or not (owner genuinely unsure).** Selection should ergonomically preserve *any* portion of sub-trees: sometimes just leaf key/values; sometimes the *path* preserved alongside the leaf; sometimes everything at a level; sometimes a renamed key (`^`). **Open question: what syntax designates "keep the path/keys with the leaf" vs "flatten to leaves"?**
- **V5 — the typing payoff.** `{:a 1}.a` should observe `Int` (structural field projection), enabling `+ {:a 1}.a 1 => 2 : Int`. Ties V1 to a concrete, testable target.

*Background reading the owner flagged*: `haskellforall.com/2026/06/record-type-inference-for-dummies` (record type inference; row/structural records). *(Couldn't fetch — 403; anchor to it once the owner shares excerpts.)*

## §2a Locked decisions (owner, 2026-07-05)

- **D1 — `Map` IS the anonymous, open record type.** Structural/observational typing: a `Map` literal's type is the observed field types (`{:a 1}` : a record with `:a → Int`). **Retire the internal `Open` type entirely** — the structural record type replaces it.
- **D2 — `Map` ⇄ `schema` interop, both directions.** An anonymous `Map` record and a named `schema` record inter-operate (a `Map` flows where a compatible `schema` is expected, and vice versa).
- **D3 — two axes: named/anonymous × closed/open.** `schema` = named (already carries a `closed?` flag); `Map` = anonymous open. Reconcile as *one* structural-record notion, not two mechanisms.
- **D4 — home = CIU Series, Track 6.** Relates to CIU Track 2 (dot-brace / sugar normalization) + Track 3 (Indexed/Keyed access unification, the Array⇄Map piece).
- **D5 — sequencing: fundamentals first**; **WS-first** — every feature wired all the way through to WS (the usability design target) and **regression-tested in WS `.prologos` syntax directly** (not sexp/`.rkt`). A `test-first-class-paths` WS test file is part of this.

### Locked 2026-07-06 (post frontier-research co-design — see §3b)

- **D6 — the record-type carrier is born ROW-SHAPED.** One new record type node: label-keyed **per-field slots** + an explicit **3-state tail** (`closed | dyn | ρ-reserved`). The per-field slot is a small struct with room for a **presence mark** (`present / optional / absent / unknown`) — F1a writes only `present`; the slot exists so Malli-style optional keys (owner vision) + Typed-Clojure-style absent-key narrowing + presence-polymorphic merge typing land without a carrier change. Literals get **ground (closed-tail) rows** — `{:a 1}.b` is a *closed-row miss*, not a policy error. Type-level merge of record types = record-meet (field-set union + per-field unify; Reynolds/MLstruct algebra). **Malli-concern polarity split**: optionality → type layer (presence marks); defaults + check-preds → schema/seal layer (already in `schema-field`; discharged at seal-as-tabulation = "fill-or-error", where defaults are the *fill*). Schema implementation NOT scoped into F1; the carrier just leaves it room.
- **D7 — D1 refined: "retire `Open`" = RELOCATE, then delete.** Today's `Open` is semantically `&{}` (top of the negative layer) and behaviorally Sekiyama–Igarashi's `★` dyn row tail (its `C_ConsL`/`C_ConsR` absorption IS the verified `{} : (Map K T)`-succeeds-via-`unify:574` behavior). **D1-a** (F1a): literals stop defaulting to `Open`. **D1-b** (F1a.2): `Open`'s wildcard semantics relocate into the row's `dyn` tail state (transcribe the S–I absorption rules into unify), then the nullary node is deleted. No deletion before relocation — the seeded `Open` is load-bearing for ordinary annotation satisfaction (`typing-core:2424-2431`).
- **D8 — QTT pin.** Map/record fields are `mw` for all of F1 (QTT×rows is a literature void; per-field `m1` is incoherent while a remainder is unknown). Seal = the future multiplicity-discharge point if linear fields are ever wanted.
- **D9 — research posture.** The rows/coalgebra/propagator research question is captured as a **brief research note** (`docs/research/2026-07-06_ROWS_COALGEBRA_PROPAGATOR_NOTE.md`) serving engineering grounding only — no paper ambitions now, never gating the engineering phases.

### Locked 2026-07-06 (merge/refinement/codata co-design, round 2)

- **D10 — merge scope (M-staging).** Value semantics: **right-priority overwrite**; deep variant recurses **iff both sides are maps** (Clojure-`merge-with`-family; Nix/CUE make different choices — ours is named). GROUNDED: no map `merge` exists today (only list merge-sort; map combination = `assoc` chains = `expr-map-assoc`), so F1a types `assoc` extension exactly via the record arm, and `merge`/`deep-merge` arrive as NEW node-backed operations: **M1** ground×ground exact typing (elaboration-time computation of the KT formula — result row = right's fields + left's fields disjoint from right — shallow AND deep; rows are finite trees, termination trivial); **M2** one-side-ground refinement via presence flags; **M3** `Concat l r u` / `DeepConcat` as residuated relational constraints in the ONE solver at F-row (fundep-directional 3-cell propagator; `DeepConcat` user-expressible at the SRE-5 dependent-matching junction). Unconstrained-principal-simple-typed polymorphic merge: **never** (Wand 1991 finite typing sets; Palsberg–Zhao 2003 NP-complete — the case-split over `:a ∈ m2` needs m2's ABSENCE facts, which coinductive observation cannot supply monotonically; the relation residuates until they arrive).
- **D11 — refinement-first; no record-subtype judgment, ever.** A row-typed record IS a refinement type: a conjunction of per-label observation facts (`has(:a,Int) ∧ … ∧ tail-state`); meet = conjunction; uninhabited = ⊥ = harmless (owner: subtyping-as-logical-inference is compositionally fragile; conjunctive constraints are arbitrarily extensible + safe-if-uninhabited). Width acceptance = erasure-mode fact-subset discharge inside existing check+unify; Map→schema = named seal; `HasField`/`Lacks`/`Concat` shaped as constraint kinds for the one solver (UCS-5 junction). The row node = the type-producer CARRIER (type-level data); constraints stay dictionary-witnessed propositions — recorded as a data point for [`2026-06-30_TRAITS_AS_REFINEMENT_TYPING_NOTE.md`](2026-06-30_TRAITS_AS_REFINEMENT_TYPING_NOTE.md) §7 Q1 (the sibling-producer answer).
- **D12 — codata posture.** F1 ships **substrate-level codata only**: observation-indexed carrier + tabulation boundary (seal = finite forced observation, fill-or-error = existing schema validation re-derived). No surface codata/copatterns/coinductive equality. Records = the worked example for future codata features (session types are the other negative type in the vision). The co-GADT generalization (record interface = indexed family of observations `.l : Rec r → r(l)`; `DeepConcat` = dependently-matched type-level relation) is homed at the SRE-5 junction, NOT CIU T6.

### Locked 2026-07-06 (collection reframe, round 3 — after D.3 critique + frontier research)

- **D13 — anonymous COLLECTION typing is a first-class design goal (the record work generalizes).** Grounding: `@[1 "a"]` (heterogeneous PVec literal) ERRORS today — `expr-PVec` has one uniform `elem-type` slot, exactly parallel to `expr-Map`'s one `v-type` slot. So heterogeneous collections are NET-NEW capability, not a regression. A record is a structural collection keyed by **Keyword**; a tuple is one keyed by **Nat (position)**; a dictionary/array is the **dyn-tailed uniform** instance of the same carrier (owner V2: "Arrays/Maps are different perspectives on the same class of data structure" — now precise). Owner-blessed decisions:
  - **Q_A → ONE carrier, TWO surface nodes, one key-domain axis.** Generalize the row carrier's key domain to `Key = Keyword | Nat`; reuse the entire D6/D10/D11 + OSF-meet + one-solver machinery; keep distinct **surface** presentations (record `{…}` / tuple `@[…]`) with a key-domain tag selecting the law-set (order-significance, dense-prefix, width-applicability). Matches the frontier: everyone unifies the row *substrate* (Morris–McKinna monoidal rows; Ur/Web tuples-as-Nat-records; Roc same runtime rep), nobody unifies the *surface*.
  - **Q_B → forbid mixed-key rows** (a row is all-Keyword or all-Nat; homogeneous-key-domain invariant). ψ-terms admit named+positional; every shipping language forbids it; revisit only on a concrete use case.
  - **Q_C → v1 = strictly CLOSED tuples + the separate union-element list for length-polymorphism; variadic tuples DEFERRED** (open Nat-tail; TS/Scala/PEP-646 show variadic inference is subtle). Tail stays expr-ready (not a one-way door). *(Owner note: a dependent-typing route may be cleaner; fun to revisit in later CIU work — out of scope now.)*
  - **Q_D → tuple-by-default for heterogeneous fixed literals** (Roc-like precision-by-default; the opposite of TS array-by-default, whose `as const` ceremony is a documented friction point — array-by-default discards per-position facts you already proved). Widening tuple → `PVec`-of-union is a monotone Galois projection available on demand.
  - **Q_E → end-state (b): `expr-Map`/`PVec` are the DYN-TAILED / UNIFORM instances of the one carrier** (one notion at the carrier, species-split at the surface). The F1a `Record<:Map` subsumption arm is transitional plumbing (F1a.2 reworks it); the dyn tail must eventually carry `(K,V)` bounds — representable only because the tail slot is expr-ready.
- **D14 — two heterogeneity FLAVORS, two code paths (the classifier is the correctness pivot):** *Flavor A* = fixed-arity heterogeneous positional product (**tuple**, `@[1 "a" true]` : per-position types), the positional dual of a record; order-significance is FREE from position-being-the-key (`⟨Int,String⟩` and `⟨String,Int⟩` are different slot-maps by construction — no non-commutative-monoid needed for identity/meet). *Flavor B* = variable-length container with heterogeneous ELEMENTS = `List`/`PVec` with a **union** element type (`'[{:a 1} {:b 2}]` : `List <{:a Int} | {:b Int}>`); the critique's "regression" becomes a feature via union-widening (verified: union-element PVec + `pvec-nth v 0N` projection already work today; only the inference-time widen-on-unify-failure is net-new). Mis-classification is the single source of BOTH today's `@[1 "a"]` error (A misrouted to B) AND the critique's record-of-records regression (B misrouted to a single meet).
- **Scope**: no change to any D1–D12 *judgment* — only *generalization*: D6 carrier key domain → `Keyword | Nat`; D10 `Concat` applies to tuple concat (Nat-key monoid = prefix-append+index-shift, inherits the residuated-constraint solution, F-row); D11 no-subtype-judgment extends verbatim (conjunction of per-position facts). **Verified-deferred with existence proofs** (Ur/Web; the concat principal-types issue is key-domain-agnostic = the record concat issue already solved by D10 residuation): tuple concat, dense-prefix maintenance under unification, variadic, η-on-closed-tuples — each homed at the phase that needs it (F-row/F1b), none blocking F1a.

### Locked 2026-07-13 (F1a-col co-design, round 4)

- **D15 — observational literal typing for anonymous collections (revises D14's flavor-B wording).** Owner: *"the type is what the literal is observed to be."* Heterogeneous literals mint **per-position rows in BOTH bracket forms** — `@[1 "a"]` AND `'[{:a 1} {:a "x"}]` → closed `'nat` rows (`⟨{:a Int} {:a String}⟩`) — because the union `List <r1|r2>` is NOT the observation: it forgets which position holds which type. The union-element list is the **derived α** (uniform-bound view, `A := ⋃positions`, the exact `record-<:-map?` analog), reached at subsumption sites when a row flows where a uniform `List A`/`PVec A` is expected — never minted at the literal. Homogeneous literals stay `(PVec T)`/`(List T)` exactly as today. Same-label-different-type lists raise no union question at the literal (they're rows). v1 consumer dispositions: `cons`/match-pattern over row-typed values α-degrade (exact prepend-with-index-shift = D10 `Concat`-family, later). Mechanism (owner-blessed Q_col-A): **literal-extent AST nodes** (`expr-pvec-literal`, `expr-list-literal`) typed all-at-once — contract-safe (one union solve, no meta widening), order-robust, mantra-aligned; explicit `pvec-push`/`cons` chains keep today's semantics (decomplected, like record-literals vs `map-assoc`). Display: space-separated `⟨Int String⟩` (owner-confirmed Q_col-D).

### Locked 2026-07-15 (F1a.2 co-design, round 5 — dyn tail + Open deletion)

Grounding: audit `wf_2b58a7e2` (5 Open producers, not 2; key-domain cannot express `(Map String V)`; the `unify:579` wildcard is strictly coarser than S–I C_Cons; 2 live QTT bugs on the reworked flows) + options panel `wf_71c14484` (4 clusters, propose/critique/synthesize).

- **D16 — F1a.2 dyn-tail architecture.** The live dyn tail is the **bare `'dyn` symbol** (bounds-free ★, S–I-faithful; zero carrier change — the twelve tail-blind walkers stay structurally safe, pnet rides the free symbol path, the `eq?` tail guards survive). **`expr-Map` SURVIVES** as the annotation/dictionary type — `(Map K V)` annotations never mint rows this phase (forced: key-domain `{'keyword,'nat}` cannot express `(Map String V)`; Q_E(b) carrier unification = one coherent future change bundling bounds + key-domain generalization + expr-Map dissolution, F1b+). Mint scope = exactly the five Open producers. The two dynamic-key degrade arms flip to **dyn rows keeping their known fields** (`{known… | _}` — strictly more informative than `(Map Keyword Open)`); D6's "(K,V)-bounds on the dyn tail" parenthetical is AMENDED to record the sequencing. Dyn-row-vs-Map/row/PVec semantics live in ONE **row-comparison engine** with the five existing α helpers delegating (drift-proof: an extend-in-place `meta-V := ⋃known` over-commit in one helper would silently undo the kept-fields win); written simplicity budget + fallback-to-extend trigger. Absorption ordering = **solve-first** (per-field goals decompose and solve metas; the tail absorbs only the remainder — fixes the `:579` absorbs-without-solving wart; canaries both directions).
- **D17 — bare `{}` = keyword-committed empty dyn row** (owner-signed SEMANTICS change): `{}` seeds `(expr-Record 'keyword '() 'dyn)` — keyword assoc chains grow exact rows (`[map-assoc {} :a 1].a : Int`); **string-keyed assoc onto unannotated `{}` stops inferring** (works today via the open `?km` — the escape hatch is the annotation form `def m : (Map String V) := {}`, which stays a Map and works). Needs its own acceptance pins of BOTH the new behavior and the escape hatch. *(Open design space, deferred by owner: heterogeneous key-types for Records/Maps — string/map/arbitrary keys as first-class row key-domains. Larger issue; F1a.2 is forward-compatible: the key-domain slot is where it lands, `{}`'s narrowing is one recoverable seed site.)*
- **D18 — mixed-key literal seed value slot = `⋃observed-values`** (D15-flavored: the observation as the uniform bound). NO global Map-assoc V-widening — dictionaries stay honest; open accumulation = the annotation form.
- **D19 — projection on a dyn tail = fresh meta ONLY; observation recording DESCOPED to F-row** (owner-signed; the meta IS the observation; the F-row pins — per-command observation cell keyed (row-id × label) w/ hash-union merge, collision-unify in the propagator — are recorded in design doc §12 so the descope is visible, not invisible deferral). Unsolved projection metas **display as metas** at zonk-final (no new defaulting; TIGHTEN LATER — DEFERRED.md entry).
- **D20 — S9 folded into dyn semantics**: the designed `(Map Keyword Open)` degrade for dynamic update-in is NEVER implemented; dynamic update-in on a record → **`{| _}`** (empty dyn row — drop all field facts; the only sound option: a dynamic deep-update may have changed any field). Same posture for dynamic-key **dissoc** (a known field may have been removed — presence marks are the F1b refinement).

### Locked 2026-07-16 (F1b co-design, round 6 — in progress)

Grounding: audit `wf_0f364e4e` (6 facets; [GROUNDING](2026-07-16_CIU_T6_F1B_STAGE3_GROUNDING.md)) + options panel `wf_b47e23d4` ([OPTIONS](2026-07-16_CIU_T6_F1B_STAGE3_OPTIONS.md)) + F1b.0 probe bundle `wf_7d2900e9` ([PROBES](2026-07-16_CIU_T6_F1B_STAGE3_PROBES.md) — 8 probes; W's Option C refuted at its cornerstone; unify-attached width would corrupt D15; three by-catch defects).

- **D21 — erasure-mode width discharge = expected-tail relaxation into C_Cons (Option E); equality-depth on shared fields.** ONE new arm in check's conversion fallback (before the final `subtype?` leg) + the qtt checkQ-fallback mirror, both calling ONE shared predicate: when the actual is a CLOSED keyword row and the expected is a CLOSED keyword row, relax the EXPECTED row's tail `'closed→'dyn` (on a copy) and delegate to the existing unify/C_Cons machinery under the rollback discipline (`union-check-left` precedent; success path stays BARE per the F1a.2 p3 refit lesson). The principled reading: **width discharge = viewing the expected row through the open-row lens** — Tang upcast-erasure erases the expected side's closure fact for the comparison; extras absorb via C_Cons (that IS the erasure); shared labels decompose to `'sub` goals (unify; metas solve). unify/classify itself is UNTOUCHED — the D15 literal-homogeneity probes (which RELY on row-vs-row unify failing) are structurally safe. Pins: (i) failure rolls meta state back; success-path field-meta solves are the accepted residue; (ii) expected-side closure is ERASED by the discharge — an empty-closed EXPECTED row accepts any record; a site needing exactly-empty force must not route through width. **Covariant/label-keyed depth DEFERRED with named triggers**: (a) row-type annotations become user-writable (the P8 forward surfaces), (b) supertype-typed fields (e.g. the future generic `Num`, unions) can appear in EXPECTED-row positions (owner example, 2026-07-16). The upgrade is LOCAL — the same fallback arm swaps realization E→per-field-helper (`unify ∨ subtype?`, the `record-<:-map?` idiom); it can NEVER ride C_Cons (classify `'sub` goals are pure unify pairs — F1a.2 p1a audit). **Boundary invariant (binds the seal design)**: the width discharge answers ONLY the structural/label question; per-field VALUE-TYPE satisfaction at rich boundaries (the Map→schema seal, annotations) stays at CHECK-strength (full conversion fallback: unify → αs → subtype? → union legs) — the seal must NOT be realized as schema→row projection + row-vs-row width discharge (else `:x Num` would wrongly reject `{:x Int}`). Entails: **Q2 resolved** — the syntax.rkt:652-653 pin stands under its STRICT reading (check-fallback only, not the walk), now with the D15-corruption argument added at the pin; **Q3 resolved** — no seal flow needs record-record width. D11 intact: the discharge is an erasure move inside check, not a subtype judgment.

- **D22 — the Map→schema SEAL (static face) + schema→Map up-shift.** (1) **The constructor form IS the seal**: the preparse rewrite generalizes from literal-braces-only to any `[SchemaName e]` head — `[Person m]` on a non-literal becomes a seal application (today: bare inference error); the annotation routes (`def x : Person := e`, mid-expression `the Person e`) route through the SAME discharge at their elaboration boundaries. One engine, four doors; NO new keyword (a distinct `seal` form = dual-path, killed in critique). (2) **Discharge**: per-field at CHECK-strength (the D21 boundary invariant — full conversion fallback, so supertype fields like the future generic `Num` accept subtype fields); the RESIDUAL scan (defaults FILL; missing-without-default → elaboration-time prologos-error with srcloc; closedness scan iff `:closed`) runs ONCE at the seal boundary, NEVER inside the check arms — selections stay exempt via delegation, with the explicit obligation that selection delegation checks against the SELECTION's field subset (the 10 by-design width-partial census sites). (3) **Annotation-path tightening**: missing-required errors; defaults fill (the aspirational `def default-cfg : Config := {}` semantics). Census-verified safe (0 live breaks; PROBES P5) — lands as its OWN gated commit citing the census. (4) **`:check` posture = minimal-repair NOW + the F1b.5 retirement pin** (owner-ruled after the (i)-vs-(iii) split): check-preds are the RUNTIME residue and their principled channel is the Result-returning `validate` face (F1b.5) — building a fourth error mechanism now would race it. Repairs now: `expr-panic` joins the `definitely-not-map?` exemptions (kills the P4 swallow-to-`none` class, message preserved) + **seal-scoped def-forcing** (a def whose body is a seal application is nf'd at commit; top-node panic → prologos-error — tabulation MEANS forcing; scoping preserves the language's laziness posture elsewhere). The panic-desugar bridge is NAMED SCAFFOLDING; its retirement decision (delegate: seal = static-seal + validate + unwrap-or-error; vs keep as explicit assertion sugar) is pinned AT F1b.5 alongside validate's witness-strength policy (refuse vs skip non-runtime-checkable fields). Named out-of-scope: compile-time literal pred-folding (crosses the elaboration/evaluation two-context seam + free-ordering). (5) **Tombstone** `expr-schema`/`expr-schema-type`/`surf-schema` (owner-ruled): no producers, pnet-unregistered (latent vector-impostor); **type-as-witness stands** — sealedness is a fact of the typing derivation, values stay uniform champs (a wrapper node would fork the value representation = decomplection violation, fight the MLstruct nominality pin, and duplicate fact-carrying the row TYPES already do — e.g. dissoc on a sealed value degrades gracefully via the row machinery with zero value-level story). History docstring at the deletion site (the expr-Open pattern). (6) `:default`-filled fields seal as **`'present`** (`'optional` stays writer-less until schema optional-keys). (7) **Up-shift** = a schema→row projection feeding the EXISTING record→Map α (the free direction). (8) The dead champ-vs-schema blanket-accept arms (typing-core + the qtt twins) retire with a LOUD refusal naming validate — not silently.

- **D23 — Q4 tightening = ESCAPE-BOUNDARY posture, hard error.** Exploration stays permissive (a bare top-level projection on a dyn row displays its meta — D19's "the meta IS the observation" honored for reads). The error line is **escape into a STORED type**: at the def/defr/annotated commit boundaries, a type containing an undischarged D19 projection meta is a **HARD ERROR** with def-srcloc (a stored type is a commitment; a raw meta in it is asserted knowledge the compiler doesn't have — and P7 proved stored raw metas DANGLE after meta-store reset and would propagate into module capture/.pnet). Escape hatch = explicit annotation (discharges the meta). The one foreclosed idiom is `def x := m.c` as a way of NAMING an open observation — owner-accepted. Mechanism = the panel's **A-prime**: a filter over the existing unsolved-metas path mirroring `check-unresolved-trait-constraints`, consulted at the store boundaries; NO second store (fresh-meta already records provenance at mint). **Groundwork (posture-independent, lands first)**: structured meta-source-info at the FOUR D19 mint sites (incl. `union-record-component-vt`; loc=#f interim, honestly named); deepen the SHALLOW scrub walk (unsolved-metas-to-holes recurses only Pi/Sigma/app/lam today — add expr-union/expr-Record recursion); route ALL stored-type paths through the guard (`:1598`/`:735` are unguarded TODAY; `:1704` is TRANSIENT per P7 — not a commitment point, never error there); DEFERRED.md rejected-with-reason entries (freeze-wide default-to-error — blast radius through constraint rendering + capture; the constraint-store realization — equational rendering doesn't fit an obligation). **The engine's meta-V-refusal-from-dyn STAYS** (it prevents exactly the absorb-without-solving wart the Open deletion killed); the relax option (`V := ⋃knowns ∪ fresh`) is deferred to the Q_E(b) era with a union-behavior probe pinned at the refusal-leg comments as its entry gate. **Sequencing pin**: the posture flip lands AFTER presence activation (the Q7 gradient changes what "undischarged" means); re-trigger pinned in DEFERRED.md.

- **D24 — presence-marks activation: dissoc-only `'unknown`, gated-identically gradient, ONE joint grid amendment.** (1) **Activation scope**: the dissoc-dynamic arm keeps labels + types, marks every field `presence='unknown`, dyn tail (replacing the drop-all `{| _}`; strictly more informative). The **update-in arm gets NO presence machinery** (P6): labels + per-field fresh metas + `'present` + dyn tail, guarded by (i) zero-segment dynamic paths → runtime error (consistent with path-head/tail erroring on empty) and (ii) the dyn tail absorbing insert-on-miss growth. `'optional` stays writer-less until schema optional-keys (D22's `:default`→`'present` holds). (2) **Landing discipline**: ONE co-signed §12.3/§12.4 grid amendment WITH D21's width discharge (never as separate salami slices on the locked grid); the **acceptance-preserving guard is REQUIRED, not optional** — a marked row in EXPECTED position treats its `'unknown` labels as non-required in C_Cons containment (without this, retained-marked rows are MORE restrictive than today's `{| _}` — a regression). The unify pin text is amended in the same commit: presence never becomes a unification GOAL; presence-consulting GUARDS inside arms are the sanctioned mechanism. (3) **Gradient (Q7) = gated-identically**: projection on an `'unknown` field mints a fresh meta exactly like the bare tail; D23's escape-boundary applies unchanged — one gradient, no special lane. Courtesy-upgrade REJECTED (typing the projection at the retained type asserts presence the compiler doesn't have — the same unsoundness shape D22's annotation-path fix closed). The marks pay via: comparison precision (retained types participate in shared-label `'sub` goals), diagnostics (miss/escape errors name the dissoc provenance), and the future `has-key?` narrowing hook (`'unknown`→`'present` on evidence — where courtesy arrives SOUNDLY, as narrowing). `record-extend`'s overwrite-to-`'present` is CORRECT under this reading (assoc = positive evidence), not a bug. (4) **Mechanics (Q9)**: presence-aware union ABSORPTION ({P} ⊂ {P,A} with equal types → the `'present` branch absorbs into the `'unknown` branch) + the presence tag added to `union-sort-key` as deterministic tiebreak; display = **label suffix** `{:a? Int | _}` (no `?metaN` collision; pre-commits the natural future optional-field surface syntax); the points-map + joins DECLARED at the struct site: present={P}, absent={A}, unknown/optional={P,A} (provenance distinguishes), contradiction={}; evidence-narrowing = intersection, row-merge join = union. (5) Bench rider: the close-gate bench matrix covers the D21+D22+D24 fast-path touches TOGETHER (the aggregate microbench obligation).

- **D25 — solve solution-set shape.** (1) **The bound-args echo is DELETED** (the F1b first-green slice): all SIX merge surfaces (the `answers->prologos-expr` bound-args merge; solve; solve-one's inline build; explain's three merge sites; both narrowing paths) stop echoing param-name keys — solutions carry ONLY query-var keys (`'[{:unknown "solution"}…]`, the owner's original target). `test-bound-args-01.rkt` INVERTS to a keys-ABSENT negative gate (protects against echo resurrection); 4 example files updated; `compute-bound-args`/`-for-relation` retire with the echo. The deletion structurally erases the echo's three inconsistencies (first-variant-only param names; arity-mismatch silent-vanish; anonymous-rel/unify/is-goal arms were already echo-free). (2) **Explain reserved-key clobber** (live silent-data-loss bug): an interim guard lands IN the echo commit window (reject-or-rename query vars named `certainty`/`cycle` on the explain path); pinned for inheritance by the future explain restructure (wrapper-record shape `{:solutions […] :certainty …}` — provenance beside the rows, not merged into them). (3) **Typed solution rows = their OWN mini-track, chartered at F1b close** (not inside F1b — the labels are NOT statically derivable in general: whnf'd args + gensym'd `_` vars). Charter entry gates: define the typeable-goal fragment; ONE shared ground/free predicate consumed by reduction AND typing; the two-context/relation-registry audit (incl. relations from cached `.pnet` bodies); the unbound-representation reconciliation (own-name fvar vs `none` — presence-`'optional`/Option are the candidates; explain's reserved keys = the first `'optional` clients); display posture vs D23 (untyped relations ⇒ rows of metas). (4) **`solve-one` returns the BARE solution map**, `none` on no-solution — NEVER `{}` (ambiguous with the D17 empty dyn row); `[solve-one q].unknown` projects directly. `solve` stays a list. (Current solve-one return shape gets its one-line verification at implementation.)

### OPEN — Path Selection unified surface (owner direction 2026-07-13; NEEDS its own deep co-design BEFORE any surface commitment)

Owner (from live exploration, `foray.prologos` scratch `@[{:a 1} {:b 2}][1.b]`): the V2–V4 selection surface is converging on —
1. **Postfix `coll[…]` = unified path SELECTION** — indexing is the degenerate single-segment case (`v[0]`); the bracket content generalizes to PATHS (`v[1.b]`). This **supersedes the earlier `coll{selector}` / `_{…}` postfix-BRACE direction** (§2 V2) — brackets, not braces.
2. **Broadcast selection = `.` followed by brackets** (`coll.[…]`) — the V3 explicit-marker question re-lands here.
3. **Result shape (V4) remains the open crux**: how to specify k+v-preserving vs value-only returns.
Sequencing: a **dedicated design conversation** (grounding pass over the reader surfaces + result-shape co-design) when the owner opens it — the rows/projection/α machinery now in place makes V4 substantially more tractable than at track opening. **F1a-col deliberately ships ONLY the degenerate case** any selection design must contain: `v[i]` literal-index positional projection (Nat-or-Int, mirroring PVec's gate), composing as `v[1].b` (chained postfix-then-dot). `v[1.b]` (path-in-brackets) and `coll.[…]` are NOT built until this conversation happens.

**Co-design CONVERGED 2026-07-06** (records + collections; D15 + selection notes added 2026-07-13) → Stage-3 **D.2**: [`2026-07-06_CIU_T6_F1_STRUCTURAL_RECORDS_DESIGN.md`](2026-07-06_CIU_T6_F1_STRUCTURAL_RECORDS_DESIGN.md) (D.1 → D.3 critique → D.2 amendments + collection reframe). F1a-core COMPLETE (s1–s5); F1a-col in progress.

## §2b Fundamentals status (F2/F3/F4 ✅ 2026-07-05)

- **F2 — `.pnet` path-node registration ✅** (`af161de7`). Registered the 5 path AST nodes in `pnet-serialize`; the two-run `core/path` cache repro no longer crashes.
- **F3 — WS `^`-rename tokenizer ✅** (`af161de7`). `recognize-keyword` now includes `^` (aligned with `ident-continue?`); `get-in u :user-name^name` works in WS. *(Branching/rename → projected map, `#p(user-name^name)`, is result-shape/V4 — deferred to F1's design.)*
- **F4 — WS regression test file ✅** (`142da071`). `test-first-class-paths.rkt`, WS-syntax coverage for dot-access / `#p(…)` / get-in / update-in / `^`-rename. **Perf lesson**: a WS `process-file` test that loads the *full prelude* balloons to ~48s under 10-worker suite contention — use `:no-prelude` when the feature is language-level (kept the file at ~0.6s). (Broadcast `.*` needs `'[…]` = prelude; verified via probe, re-add later.)
- **F1 — retire `Open` → structural `Map` typing** — next; mini-design below/§5.

## §3 Initial design reflections (Claude — for dialogue, not decisions)

- **V1/V5 is structural / row-typed records.** The "observed types of the values at the keys" reading is exactly **structural record typing**, and open records are the **row-polymorphic** case (`{:a Int | ρ}`). This is well-trodden theory (the "for dummies" post is an accessible treatment), and it's the right frame: `Open` → a structural record type carrying per-key field types, with a row variable for the open tail. `schema` = a *closed* record (no row tail); `Map` = *open* (row tail). Field projection `m.a` then has the field's type, not `Open`. The design work is *how this hooks into Prologos's existing type lattice* (union types, `schema` registry, the quantale type lattice) without reintroducing the fragilities the tower avoided.
- **V2's Array⇄Map unification resonates with CIU.** Arrays are maps `Nat → v`; records are maps `Keyword → v`; both are indexed collections. A unified *indexed-selection* interface (postfix `coll{selector}`) is elegant and connects to the **CIU (Collection Interface Unification)** series' thesis. Worth checking whether the selection surface should be *one* mechanism dispatched by key-type (Nat-index vs Keyword-path) rather than two.
- **V3's explicit-vs-implicit broadcast is a real tension.** Implicit auto-broadcast (selection on a collection auto-maps) is array-programming-ergonomic (APL/J/K) but risks ambiguity (select-over-elements vs select-on-the-collection-itself). An explicit marker (`{+ a}`) is predictable but adds syntax. Prior art: jq's `.[]`, GraphQL selection sets, APL rank. Likely both are wanted (explicit as the always-available form; implicit as sugar where unambiguous).
- **V4 (result shape) is the crux, and it has rich prior art.** "Which sub-trees, and in what shape" is the same problem jq, GraphQL selection sets, XPath, and optics/lenses solve. The shape axis (flatten-to-leaves vs preserve-nesting vs preserve-path vs rename) wants a small, composable notation. One natural lever: *nesting in the selector mirrors nesting in the result* — `{a.b}` flattens the path to the leaf, `{a.{b}}` preserves one level of nesting — so the selector's shape *is* the result's shape. Rename `^` already exists. This is the piece to design most carefully.
- **The `.{`→`_{ }` move also resolves the collision** we hit: freeing `.{` from mixfix and moving selection to postfix `_{ }` removes the syntactic contention entirely.

## §3b Frontier-research synthesis (2026-07-06) — rows, codata/polarity, OSF, gradual

Owner-directed research pass over the 2026-06-29 standup conversation (`docs/standups/standup-2026-06-29.org` § Conversations → "Maps<->Schema as co-data..."): 7 parallel literature threads + synthesis, primary sources fetched. Full detail in the session transcript; grounding note at `docs/research/2026-07-06_ROWS_COALGEBRA_PROPAGATOR_NOTE.md`. What it settled for THIS design:

- **The carrier must be row-shaped from birth** (→ D6). Three threads converge: Tang et al. (OOPSLA 2023) cofinite-label rows; Sekiyama–Igarashi's `★` dyn tail; Elixir/CFT closedness-as-orthogonal-addon. Retrofit of a tail slot is the expensive path.
- **`Open` is formalized prior art, not debt** (→ D7): it is S–I's dynamic row `★` (absorption rules = our `unify:574-575` behavior) and `&{}`-top (Polarized Subtyping, ESOP 2022). Relocate into the `dyn` tail, then delete.
- **Subtyping strategy = erasure-mode, not engine-retrofit**: full record width/depth subsumption in a rank-1 HM regime is recoverable by *upcast erasure* — let inference instantiate row/presence metas (Tang §6) — in check+unify fallback, NO dedicated subtype relation. Boundaries to document: rank≤2 record positions (audit HOF-heavy stdlib before F1b); weak preservation (inferred types only subtype-related to annotations). The fixed-arity POSITIONAL ctor-desc walk cannot host label-keyed width (confirmed structural); F1b adds a label-keyed variant for depth-on-shared-fields only.
- **Interop is polarity-shaped** (validates the conversation's architecture): schema→Map = free up-shift (width subsumption, no coercion machinery). Map→schema = **named explicit seal** — theory *predicts* it can't be silent (no type-only encoding of full record subtyping into rows+presence, Tang §5.4; OCaml's rows+upcasts rationale §7.2). Seal = **tabulation** (Codata in Action, ESOP 2019: force all observations, fill-or-error, positive witness) + elaboration-time residual set (schema fields − observed fields) + closedness scan iff `closed?`. Ship Elixir-style safe-erasure first; **monomorphic seals only** (gradual guarantee + blame for polymorphic rows unproven anywhere through 2026).
- **MLsub/MLstruct: steal locally, don't adopt.** Steal: record-meet algebra (the D6 merge spec); Simple-sub's monotone-bounds-growth as the published inference-as-lattice-fixpoint precedent; MLstruct's nominality discipline (schema registry stays nominal/positive, OUT of the structural lattice — disfavors the anonymous-schema-registry carrier). Don't adopt: biunification (conflicts with the `solve-meta!` single-solution contract), Boolean type lattice (co-NP-hard; Boolean structure stays in the ATMS worldview bitmask where it lives today).
- **On-network endgame licensed + priced**: row solving = monotone positive fragment (presence facts, containment, fundep firing — S0 cells, hash-union) + non-monotone residue (closedness/seal, `Lacks`, ambiguity check — stratum boundaries). Rows + traits belong in **ONE solver** (Toohey et al., POPL 2026, Lean-mechanized; PureScript fundep row-solving in production). F1a forward-compat obligations: label-keyed carrier w/ tail (D6); record obligations through the EXISTING constraint store; cell-level per-field solving bridges `solve-meta!` (PUnify precedent); per-field types = ordinary type metas (NO new meta domain); when ρ arrives, spike **row-KINDED meta in the existing type universe** first (PureScript `Row k`); pin the simple commutative unique-label row theory (unordered hasheq gives it natively).
- **Risks the pre-research fork missed**: merge is the expensive commitment (no principal typing for unconstrained polymorphic merge ANYWHERE — Wand 1991 finite typing sets; Palsberg–Zhao 2003 NP-complete with subtyping; workaround = constraint residuation, §5 item 9); Jones fundep-closure ambiguity check at generalization = a *scheduler-independence* requirement on-network; closedness checking is the empirical perf trap (CUE evalv3) — seal-time only, never the hot merge path; variants are not free-free (Tang §5.3 asymmetry; obligation flips to match sites → pattern-compiler budget).
- **Revised phasing**: **F1a** (ground rows, one row-shaped node through the full pipeline + `compound-type?` + qtt:1213 co-edit; `{:a 1}.a : Int`; D1-a) → **F1a.2** (dyn tail live; Open relocation; D1-b closes) → **F1b** (erasure-mode width + label-keyed depth; free up-shift; named monomorphic seal) → **F-row** (ρ spike-first; extension typing; M-staging per §5.9; ambiguity check) → **F-variant** (extensible sums, own budget) → optional **F-polarity/gradual** (blame as monotone failure-set latch). R-note runs beside, never gating.

Reading list (highest-value 8): Tang et al. OOPSLA'23 ([2304.08267](https://arxiv.org/abs/2304.08267)); Morris–McKinna POPL'19 ([doi:10.1145/3290325](https://doi.org/10.1145/3290325)); Toohey et al. POPL'26 ([pdf](https://mtoohey.com/papers/popl26.pdf)); Castagna–Peyrot OOPSLA'25 ([2404.00338](https://arxiv.org/abs/2404.00338)); Parreaux Simple-sub ([exposition](https://lptk.github.io/programming/2020/03/26/demystifying-mlsub.html)); Codata in Action ESOP'19 ([pdf](https://simon.peytonjones.org/assets/pdfs/codata-in-action.pdf)); Sekiyama–Igarashi gradual rows ([1910.08480](https://arxiv.org/abs/1910.08480)); CUE impl+evalv3 notes ([impl.md](https://raw.githubusercontent.com/cue-lang/cue/master/doc/ref/impl.md)).

## §4 Grounded current state (HEAD `4e56da1d`, verified)

- **Single-path dot-access works**: `m.a.a1` → `1`, `m.b` → `{:a1 1}`. But selected values are typed **`Open`** (`m.a.a1 : Open`), not the observed value type — the V1/V5 gap.
- **`.{ … }` postfix multi-select never worked**: it was *only ever* the mixfix form; the First-Class Paths example marks `app-config.{…}` with `;; ERROR ❌`. The recent `.{`→`.( )` mixfix migration then made `.{` emit a misleading "retired for mixfix" hard-`raise` (`parse-reader.rkt:2510` → `macros.rkt:5746`). **Immediate fix landed**: reworded to "`.{ … }` is not currently supported — postfix path-selection is under redesign" (`macros.rkt`), test updated (`test-mixfix-01.rkt`). <a name="immediate-fix"></a>
- **First-Class Paths** (design `2026-03-20_FIRST_CLASS_PATHS_DESIGN.md`) shipped Phases 0–7c: dot-access, broadcast `.*`, renaming `^`, `#p(…)` path literals, `get-in`/`update-in`/`selection`. Phase 8 (**Lens**) was deferred. Postfix object multi-select `expr.{…}` was left incomplete.

### §4a Grounding audit findings (2026-07-05, run `wf_b5bda45e`; HEAD 4e56da1d)

**Scope verdict — the two threads are separable, with a clear lift ordering.**

- **Selection syntax (V2/V3/V4) is the cheaper thread, with working precedent.** Postfix juxtaposition ALREADY exists: `arr[i]` (no space) emits `$postfix-index` via a reader *adjacency* check (`parse-reader.rkt:2461-2487`) → `(get …)` = `expr-get`. The hook for `coll{selector}` is a *single* mirror of that adjacency check on the `{`-branch (`parse-reader.rkt:2500-2506`, which today unconditionally emits `$brace-params`) → a new `$brace-select` sentinel + one fold arm in `rewrite-dot-access` (`macros.rkt:5107`). **No ambiguity** (map literals disambiguate positionally at the parser: head → literal; application uses `[]`, a different delimiter; the adjacency decision is made at the reader). This validates the `_{…}` / `coll{…}` idea AND echoes the Array/Map instinct — it is the *same* reader mechanism as `[i]`.
- **Map structural typing (V1/V5) is the bigger, net-new lift.** There is **zero** row-polymorphism / extensible-record machinery; `expr-Map` carries one uniform value type (no per-key slot); `Open` is minted at *elaboration* (`elaborator.rkt:2127/2132`), and `Open` is an α-semantic wildcard so projection absorbs to `Open`. The reusable substrate is **`schema`** — already a *named closed structural record* with per-field projection (`typing-core:1536`), and it **already carries a `closed?` flag** (open-vs-closed exists *inside* schema; the "schema=closed / Map=open" split is partly built). The design ≈ generalize schema's per-field projection to anonymous literals: synthesize an inline field-map at elaboration + teach `map-get` to read it (mirror in `qtt` — its `expr-map-get` has NO `Open` arm today, an existing asymmetry).

**~~Blocker~~ — REFUTED (main-session R-lens, 2026-07-05): schema-typed dot-access WORKS.** The audit's "broken" finding was a **probe artifact** — it used the non-canonical `schema Person := {…}` inline form. With the canonical *block* form (`schema Person` / `:name String` / `:age Int`), `def alice : Person := {…}` then `alice.age` → **`30 : Int`** (0 errors, via `process-file`). The schema per-field-projection substrate (`typing-core:1536`, `schema-lookup-field` → `schema-field-type->expr`) **functions** — V1 is *de-risked*: the plan is to generalize this proven template to anonymous literals. *(Lesson: re-verify every audit "broken" finding with canonical syntax before trusting.)*

**Array⇄Map unification (V2 deep) is a CIU concern.** `expr-get` already unifies PVec/Map/List access at the *node* level (`typing-core:1484-1508`; value-directed in `reduction`), but they are disjoint at the *trait* level: `Indexed {C : Type -> Type}` vs `Keyed {C : Type -> Type -> Type}` — a **kind-arity mismatch** blocking a common supertrait. **CIU Track 3 (Trait-Dispatched Access, ⬜ pending)** is the live owner of exactly this unification (makes `expr-get` vestigial, routes `[i]`/`.k` through Indexed/Keyed constraints). This thread must **reconcile with CIU Track 3** — subsume / supersede / sequence — not parallel-design.

**Latent bugs surfaced** (First-Class Paths is more broken than its tracker says — separable, worth filing):
1. **`.pnet` gap** — the 5 path AST nodes (`expr-path`/`expr-Path`/`expr-get-in`/`expr-update-in`/`expr-broadcast-get`) are unregistered in `pnet-serialize` → **vector-impostor detonation** (the `core/path` lib crashes under a stale cache). pipeline.md item #6.
2. **schema-typed dot-access broken** (above).
3. **`^` rename broken in WS-file mode** (the WS keyword tokenizer excludes `^`, `parse-reader.rkt:~519`) despite Phase 7c marked ✅.
4. **No `test-first-class-paths.rkt`** — zero Level-3 regression coverage (why 1–3 went unnoticed).

**Correction to §3**: the quantale structure is confined to the QTT multiplicity layer + SRE tensor, NOT the value-type lattice where records live (flat lattice `bot→T→top` + variance subtyping). Don't over-index on quantale-for-records.

*(rlens targets for when we implement: `typing-core.rkt:397-401` + the alice.age failure [root-cause the schema break — highest priority]; `elaborator.rkt:2113-2145` [Open mint site]; `parse-reader.rkt:2500-2506` [the postfix hook]; `pnet-serialize.rkt` [the 5-node gap]; `collection-traits.prologos:121/137` [the Indexed/Keyed kind mismatch].)*

## §5 Open design questions

*(Status 2026-07-06: items 1, 2, 7 RESOLVED by D6/D7 + §3b — row-shaped carrier; Open→dyn-tail relocation; erasure-mode subsumption; up-shift free / seal named. Item 9 added: the live co-design question.)*

1. ~~**Structural record types**~~ — RESOLVED (D6/D7): row-shaped node (per-field slots + 3-state tail); `Open` relocates to the `dyn` tail state, then the node is deleted.
2. ~~**`schema` (closed) ⇄ `Map` (open)**~~ — RESOLVED in shape (§3b polarity): schema→Map free up-shift; Map→schema = named explicit seal (tabulation + residual set), monomorphic in F1; mechanics land in F1b.
3. **Selection surface** — `coll{selector}` postfix juxtaposition: grammar, precedence, interaction with application `[f x]` and existing `.` dot-access; does `.` single-access unify into it?
4. **Result shape (V4)** — the notation for flatten-to-leaves vs preserve-nesting vs preserve-path vs rename. **The hardest/most-open piece.**
5. **Broadcast (V3)** — explicit marker vs implicit auto-broadcast; how the selector distinguishes "over the collection" from "on the collection."
6. **Array ⇄ Map unification (V2)** — one indexed-selection mechanism dispatched by key type, or two surfaces? Relation to CIU + PVec.
7. ~~**Field-projection typing (V5)**~~ — RESOLVED (D6): per-field lookup on ground rows; known field → observed type; `{:a 1}.b` = closed-row miss error; dyn-tail projection (F1a.2) mints a fresh observation, never silent absorption, never hard error.
8. **Migration** — retire `.{` cleanly (it's now under-redesign); `#p(…)`, `selection`, `get-in`/`update-in` continuation.
9. ~~**Merge/concat typing scope (M0–M3)**~~ — **RESOLVED (D10, 2026-07-06 round 2)**: right-priority value semantics; no map `merge` exists today (grounded) so `assoc` types exactly in F1a and `merge`/`deep-merge` ship as new node-backed ops with M1 ground typing; M3 residuated `Concat` at F-row. Original framing preserved below for the record:
   **(historical)** Guard rails from the research: *unconstrained principal simple typing of polymorphic merge does not exist* (Wand 1991: finite typing sets; Palsberg–Zhao 2003: NP-complete with subtyping); the principled workaround is **constraint residuation** — `merge : {l r u} (Concat l r u) => Rec l -> Rec r -> Rec u`, a 3-place relational row constraint with fundeps, principal at the qualified level (Rose POPL'19; Toohey POPL'26 mechanized; PureScript `Union` in production). In Prologos this is trait-shaped (one solver) AND a 3-cell propagator whose firing direction emerges from information availability — the most mantra-literal constraint in the design. Three value-level merges must never be conflated: right-priority overwrite (Clojure, THE user op), symmetric/disjoint (needs `Lacks`), unification-merge (CUE — stays a TYPE-lattice op, record-meet, not a user op). Staging spectrum: **M0** dyn-tail merge result (F1a-era, coarse); **M1** ground×ground exact right-priority union (cheap, principal, covers literal-heavy config code); **M2** one-side-ground refinement via presence flag-flip (needs D6's presence marks); **M3** full `Concat` residuation at F-row (+ Jones ambiguity check = scheduler-independence). Awaiting owner input: which idioms carry weight (config merge / assoc-update chains / bulk `into` / deep-merge — deep-merge is a *different* semantics needing its own guarded look); annotation-free vs annotated merge seams.

## §6 Proposed approach

1. **Grounding audit** (workflow) of the current subsystem: Map/`Open` typing, First-Class Paths implementation, the reader path-grammar surfaces, PVec-vs-Map representation + CIU, and any structural-record/row-type support — to establish the baseline the design builds on.
2. **Design dialogue** (main-session, iterative) on §5, anchored by the owner's vision + the record-type-inference background.
3. **Stage-3 design doc + phased roadmap**, then implementation.

## §7 References

- First-Class Paths: `2026-03-20_FIRST_CLASS_PATHS_DESIGN.md` (Phases 0–7c; Lens Phase 8 deferred).
- CIU Series (Collection Interface Unification) — Array⇄Map unification angle: `MASTER_ROADMAP.org` § CIU.
- Record type inference background: `haskellforall.com/2026/06/record-type-inference-for-dummies` (row/structural records).
- Reader surfaces: `parse-reader.rkt` (`recognize-dot-lbrace` :748, dot-lbrace emission :2510), `macros.rkt` (`expand-mixfix-retired` :5744).

---
*Stage 0/1, opened 2026-07-05. Standalone tracking doc. Next: grounding audit of the Map-typing / path-selection subsystem, then co-design of §5.*
