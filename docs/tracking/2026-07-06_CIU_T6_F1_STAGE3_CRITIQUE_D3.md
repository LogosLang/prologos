# CIU T6 F1 Stage-3 — Independent Adversarial Critique Record (D.3)

**Date**: 2026-07-06 · **Design under critique**: [`2026-07-06_CIU_T6_F1_STRUCTURAL_RECORDS_DESIGN.md`](2026-07-06_CIU_T6_F1_STRUCTURAL_RECORDS_DESIGN.md) (D.1 + Pre-0)
**Method**: 5 independent lens critics (P principles / R code-reality / M propagator-mindspace / S structural-SRE / U adversarial-user+WS), read-only, HEAD-pinned (`6d6a24ca`), with live probes; then an adjudicator re-verifying every BLOCKING/SIGNIFICANT finding against code before ranking. 2 BLOCKING claims REFUTED at adjudication (recorded below so scope is not falsely descoped).

## Verdict (adjudicator)

**D.1 does NOT survive as-written → focused D.2 amendment round (not structural revision).** Survives verification: the `expr-Record` carrier (sorted-assoc + presence slot + expr-ready tail), the seed-riding-the-v-slot elaboration mechanism, the projection-disposition approach, the D11 realization (`subtype-predicate.rkt` untouched is genuinely safe), qtt-delegation strategy, the Galois α framing of Record<:Map, the honest zero-propagator NTT answer, all §3 code coordinates, and the Pre-0 instruments. The failures are concentrated in *reasoning from* those facts. **No implementation slice starts before findings B1–B4 are amended** (they change s2's file list and the s4 gate contents).

## CONFIRMED BLOCKING (4)

- **B1 — The record seed breaks annotated non-empty literal checking** (5/5 critic convergence + probes). Term-directed checking of `{:a 1}` vs `(Map K V)` recurses to the seed; the map-empty-vs-Map arm (`typing-core:2424-2426`) unifies the seed's v-slot; with `(expr-Record '() 'closed)` there, unify fails (no Record case) — or worse, flex-rigid **solves `?V := empty-record`** for polymorphic specs (probe: `map-merge {:a 1} {:b 2}` works today, would break). §4.2's "never consults the seed" is FALSE; D7's caution applied to seed *replacement*, not just deletion. **Fix**: a third co-edited check arm (s2, not s3) — empty-closed-record seed ⇒ unify keys only, skip v-unify (the seed asserts no fields; per-entry strictness preserved as the chain unwinds at `:2431`). + annotated-literal/annotated-list/polymorphic-spec canaries.
- **B2 — The Record<:Map subsumption arm is unreachable from qtt**. `checkQ`'s app arm falls to qtt's OWN duplicated conversion fallback (`qtt:2449-2463`), which never calls typing-core's `check` — so THE regression canary `[sum-vals r]` fails the QTT pass even after the typing-core arm lands. The §6-cited divergence bug class, fallback instance. **Fix**: co-edit the qtt fallback (preferred: delegate type comparison to typing-core `check`, keep usage local); name the meta-V solve's `solve-meta!` routing (pipeline.md Known Coupling).
- **B3 — Record-vs-Record unification entirely undispositioned** (probe: `'[{:a 1} {:b 2}]` and `'[{:a 1} {:a "x"}]` both type today as `(List [Map Keyword Open])`; post-F1a both hard-error via list-element meta solving + no classify case). Invisible to every F1a gate (both test artifacts are `:no-prelude`; list literals need the prelude). Pipeline file 14 was omitted from s1 despite §3 counting 2 unify sites. **Fix**: hardcoded `classify-whnf-problem` case — closed×closed, equal labels+tails ⇒ `'sub` per-field goals (sorted zip); label mismatch ⇒ `'conv` with the closed-row-miss diagnostic. Same-shape/meta-bearing collections survive; heterogeneous-SHAPE stays a **named regression** with the annotation escape hatch (works only after B1's fix) → owner Q1. + one prelude-loaded WS test file so the class is suite-gated.
- **B4 — Dynamic-key degrade rows drop the key check** (probe: `map-assoc q "strkey" 2` is a type ERROR today; the disposition as written accepts it and types `(Map String Open)` for a map containing keyword keys — unsound `map-keys`). **Fix**: prepend `(check ctx k (expr-Keyword))` to every non-literal-key disposition (fail ⇒ error, preserving today's rejections); qualify "byte-identical"; + two dynamic-key canaries.

## CONFIRMED SIGNIFICANT (12, condensed — each with its minimal D.2 change)

1. **union-sort-key has no Record case** (4-way convergence) → record-containing unions non-canonical (commutativity/idempotence fail at realization; display order-dependent; type-cell merges order-dependent = scheduler-independence violation). Add `union-types.rkt` to s1 + recursive sort-key + order-determinism test.
2. **Generic meta-walkers blind to metas inside the fields list**: `occurs?` (`unify:234-245`), `collect-meta-ids` (`metavar-store:915-922`), `ground-expr?` (`[_ #t]`) recurse on `struct?` only — a raw assoc list hides field metas (occur-check holes; constraint-retry misses; pipeline.md meta-coupling class). Add s1 walker-audit co-edit list (~6 grep-able walkers) + occurs/retry regression tests + propose the pipeline.md checklist addition.
3. **Map-op disposition under-count**: `fold-entries`/`filter-entries`/`map-map-vals` (`typing-core:1836-1886`) fall to `(expr-error)` on Records (regress from working); `nil-safe-get`'s OWN union loop (`:1587-1604`) drops Record components (wrong Nil-only types); union-arm miss policy unchosen. Add rows (uniform-view operation; accept Record components; miss policy → owner Q5).
4. **qtt co-edit under-count**: `map-dissoc` (`qtt:1244-1250`) propagates the un-dissoc'd type (divergence verbatim); map-assoc delegation must be stated unconditional. Verified free: map-keys/vals/nil-safe-get/get already delegate.
5. **s4 census 2× under-budget**: `test-mixed-map.rkt` is the PPN 4C T-2 "Open by Design" CONTRACT file with ~10 colon-less Open assertions invisible to the display grep — s4 is flip-or-retire *decision* work + T-2 supersession note. Re-census semantically; per-case table.
6. **§3/D10 grounding fact FALSE: stdlib `map-merge` EXISTS** (`book/maps.prologos:136-141`, `core/map.prologos:62-66`, + `map-merge-with` feeding `impl Lattice (Map K V)`; probe: works today, right-priority). D10's semantics lock survives (it already implements right-priority); the "no legacy" clause doesn't — merge-node mini-design must disposition it (owner Q4); its polymorphic flow hits B1's flex-rigid case → canaries.
7. **Closed-row-miss diagnostic has no transport**: `expr-error` is nullary; §4.6.2's promised message needs a mechanism (rec: `typing-errors.rkt` hint arm, Issue-#70 walk precedent, field list capped) or the het.z assertion downgrades to "is a type error".
8. **Third literal-shaped mint site**: multi-path `get-in` projection maps (`elaborator:2280-2293`) — concrete leaf types now solve the shared `?vm`; heterogeneous-leaf projections regress. Extend the record-seed rule there (keys are keywords by construction) or name the regression + canary.
9. **Dynamic-path `update-in` becomes unsound under precision** (`typing-core:1429-1434` discards the fn type; today vacuously safe via Open). Rec: degrade result to dictionary view in F1a (owner Q7). Literal paths are SAFE (desugar — see refutations).
10. **Trait-resolution surfaces undispositioned**: `expr->impl-key-str` Record → unmatchable pretty-printed key; `ground-expr?` `[_ #t]` misreports meta-bearing records. Add ground-expr? arm (s1) + a posture row ("record types match no Map-headed instance heads in F1a; annotation escape hatch") + baseline probe (blast radius unresolved — coverage gap 7).
11. **Empty-record ⋃∅ corners**: `build-union-type('())` = `(expr-error)`; `dissoc q :a : {}` is an acceptance target → `map-vals {}` leaks a malformed type; `{} <: Map K ?V` must succeed WITHOUT solving V. Spec corners (owner Q6); add `map-vals (map-dissoc q :a)` canary.
12. **record-meet spec gaps + canonicalization self-contradiction**: no tail-meet table (closed⊓closed-different-support: D6 formula vs D11 conjunction reading CONFLICT → owner Q3); conflates lattice-meet / side-effecting unify / assoc right-priority overwrite (rename the latter "row extension"); §4.1 "constructor-enforced" vs §7.2 "not per-extension" contradict (smart constructor wins; user assoc builds rows post-literal). **BONUS RESOLVED**: §8's distributivity precondition — per-field `type-lattice-meet` distributes over unions BY CONSTRUCTION (`type-lattice.rkt:263-274`), conservative-⊥ on metas; ill-posed for the "per-field unify" reading; equal?-level verification additionally needs the sort-key fix (S1).

**MINOR cluster** (confirmed): elaborator seed-before-entries hoist (§4.2 needs entries-first restructure paragraph); acceptance F1a display-target strings don't match `pp-expr`'s bare `~a | ~a` union convention (`pretty-print:718-719`); subsumption first-solve-wins order-dependence (document + canary); `;;N=>` insertion renumbering (append het.z at end); pipeline items 9–15 need one-line disposition rows (13 verified n/a — map ops are `#f`-registered at `typing-propagators:2366-2372`; **14 is REQUIRED** per B3).

## REFUTED at adjudication (2) — do NOT descope on their strength

- "get-in expectation flips undeliverable" + "literal-path update-in unsound" (critics 3/5, rated BLOCKING): **wrong** — literal `#p` get-in/update-in are *inlined by the elaborator* to `map-get`/`map-assoc` nests (`elaborator:2242-2252`, `:2320-2335`); the nodes are emitted only for dynamic paths. The s4 flips stand; literal update-in types correctly via the assoc extension.

## Owner adjudication needed (7) — carried to co-design

Q1 heterogeneous-shape collections: named regression + annotation escape (rec) vs join/degrade fallback. Q2 D3 end-state: expr-Map permanent dictionary type vs annotation-alias-over-dyn-rows (wording of §4.5(i) depends; reading (b) needs a parameterized-dyn tail eventually — slot is expr-ready). Q3 record-meet closed⊓closed different supports: D6 formula vs D11 conjunction (⊤) — rec: D11 for closed×closed, D6 formula for dyn-involved. Q4 stdlib map-merge: retire-into-node / alias / coexist. Q5 union-arm miss policy: filter (rec, precedent-consistent) vs strict error. Q6 empty corners: `map-vals {}` result; `{}<:Map` no-solve (rec); dissoc-absent = identity (rec); dynamic-get-on-{} = miss error (rec). Q7 dynamic-path update-in: degrade (rec) vs precise-but-unsound + note.

## Critique coverage gaps (honest — candidates for the re-verification pass)

reduction.rkt whnf/nf/definitely-not-map? treatment of the node (assumed, unexamined); **cross-module .pnet consumer path** (the F2 detonation surface — no cross-module canary exists; `:no-prelude` single-file acceptance cannot exercise it); pattern-matching/narrowing over record-typed scrutinees (probably nil, unasserted); branch-join baseline unpinned (match-arm probe ERRORS today; if/meta joins unprobed — sizes the Q1 census); record-TYPE vs map-VALUE display collision in mixed contexts (errors/hover/REPL); perf of new arms under adversarial literals (uninterrogated beyond Pre-0); trait dispatch success on `(Map ?km Open)` literals today (2-line probe would settle S10's blast radius).

---
*Next: resolve Q1–Q7 in co-design → apply all amendments as D.2 → light re-verification of amended dispositions (probes/line-anchors reusable) → Stage-4.*
