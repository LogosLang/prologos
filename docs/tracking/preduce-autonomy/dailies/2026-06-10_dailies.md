# PReduce Autonomy — Phase B Dailies — 2026-06-10

The loop's own dailies stream (charter §6; separate from main-session dailies in
docs/tracking/standups/). Interval discipline: creation-to-creation.

## Carried forward (from the interactive Phase A arc, same day)

- **Phase A CLOSED**: Tracks 0.1 (D.1) / 0.2 (D.2) / 0.3 (D.3) all locked; merged to
  main; main == branch at `d7f9daf2` at loop start.
- **Implementation queue** (handoff order): SM1.1 production-merge commit + shape-P +
  comment fix → pce.rkt (PCE/1 + golden vectors) → D5 probe (singleton-fraction FIRST)
  → term carrier + 'eclass-refine → e-class cells + hashcons + union-emitter (green
  slice) → #:after substrate → congruence → effect-safety guard (BLOCKING Track 2 P0).
- **Blocking obligations**: #:after handler ordering + keep-pending (before promotion);
  effect-safety guard (before β; covers ι instantiation).
- **Watching** (per workflow.md dailies discipline):
  - Panel verdicts need main-session R-lens before binding (8 ledger data points;
    pattern held all day — kill-shots, compositions, AND compressed option labels).
  - The §5.8 perf curve honesty: Tracks 1-3 may regress wall-clock; record, don't panic.
  - Two-tier drift risk: tier-2 "absorbed in metadata only" must keep being said.

## Iteration 0 (shakeout) — this entry

- **Baseline**: cold compile + full suite launched in background in the worktree
  (results land next firing; timings.jsonl entry + local green = the gates' floor).
  §5.8 perf baseline (bench-ab --output + reduction-share profile) queued behind the
  compile — NOTE: reduction-share measurement method needs a decision (existing
  time-phase! instrumentation vs profiler run) — flagged for iteration 1, do not
  improvise instrumentation in a shakeout.
- **DEFERRED.md triage (the A.0 leftover, DONE)**: 3 PReduce-relevant items:
  (1) "Propagator-First Phase 3e: Reduction Cache Cells" → marked ABSORBED into
  PReduce (superseded by e-class substrate; independent implementation would violate
  belt-and-suspenders) — annotated in place. (2) Allocation Efficiency audit + the
  per-command transient-allocation finding (~1100 cells/run) → cross-referenced as
  NAMED INPUTS to Track 1's allocation cost model + D5 interpretation — annotated.
  (3) bench-ab --refs multi-way extension (Issue #63) → still valid, consumed at
  Track 4; no action now.
- **Dailies file**: created (this file).

## Iteration 0 (continued) — baseline ingested

- **SUITE BASELINE GREEN**: 8380 tests / 428 files / 131.2s wall / ALL PASS, cold
  worktree compile clean; timings.jsonl entry recorded (branch preduce-autonomy,
  commit 41d222d7, 10 batch workers, macosx-aarch64). The gates' floor is established.
- **bench-ab Phase B baseline**: launched in background (10 runs, comparative suite,
  saved to data/benchmarks/preduce-phaseb-baseline-41d222d7.json). Iteration 0 closes
  when it lands; iteration 1 = the §5.8 reduction-share measurement METHOD decision +
  the SM1.1 mini-audit opening (grounding-audit workflow per charter budget posture).

## Iteration 1 — the denominator does not (yet) match the assumption

- **v1 measurement (16 comparative benchmarks, self-time protocol)**: reduction.rkt
  self-share 0-10.6% (median ~2%; max combined with substitution.rkt = 12.7% on
  church-folds). FAR below the owner-reported ~50-60%.
- **Discipline applied — instrument skepticism before conclusion** (the same rule as
  panel verdicts): two protocol gaps could explain the gap: (a) SELF-time
  under-attributes the reduction CALL TREE (whnf's work lands in callee modules —
  match compiler, performance counters, champ); (b) the comparative corpus was BUILT
  to stress elaboration/typing/registry paths (Track-7 adversarial), not evaluation.
- **v2 protocol running**: harness extended with REDUCTION-TREE-BOUND (call-tree
  totals for reduction.rkt nodes; recursion-merged by the analyzer) + corpus extended
  with acceptance files (wfle, track4, ppn-track4c). Ingest next firing; only then
  does the §5.8 denominator get recorded.
- **Watching**: if v2 confirms low single-digit shares even by tree-bound on
  acceptance files, the §5.8 "across the board" hope needs an owner conversation
  about WHICH workloads the 50-60% figure came from — that would be a
  notification-worthy finding (charter doorbell), not a silent ledger line.

## Iterations 2-9 digest (the loop's first implementation day)

**Landed, in order** (each with full ledger entries — this is the compact digest):
- iter 2-3: **SM1.1a** (`2c48d6b8`) — 4 internal reduction facets, closed dispatch,
  scoped bot-filter, arity-2 filter. Gate caught the unscoped filter breaking legacy
  :type bot-write WAKE-SIGNAL duty (the audit's one untraced flag — data point #9).
  Plus the §5.8 denominator RECONCILED: phase-accounting reduce_ms ≈ 75% on
  ppn-track4c (the owner's 50-60% holds under the phase lens) vs 25.4%
  whole-process tree-bound; both baselines standing.
- iter 4: **SM1.1b shape-P** (`5ae86ccc`) — precise O(|delta|) changed-paths,
  ~420× at N=5000, FLAT. The gate REFUTED my "superset over-fire is CALM-safe"
  reasoning: over-fire CONSUMES fire-once dependents (data point #10; Watching:
  "changed-path precision is a soundness contract wherever fire-once exists").
- iter 5: **flake audit** (`86b928ce`) — mechanism found (module-load parameter
  mutation, missing batch-worker restore entry), conformance patch landed, fix
  hypothesis REFUTED by experiment, flake classified+tracked in DEFERRED.md with
  gate policy (data point #11: no sunk-cost spiral). Structural fix = SM3 registry.
- iter 6: **Track 1 acceptance file** (`e1e35675`) — reduction-kind-organized
  (D.2 taxonomy), Level-3 clean; Phase-0 gate discharged.
- iter 7: **pce.rkt** (`98972546`) — PCE/1 single hasher + golden vectors;
  cross-process determinism verified.
- iter 8: **eclass-cell.rkt + 'eclass-refine** (`cce768e9`) — the SM2 product
  merge + 'term carrier + relation through the public SRE dispatch table.
- iter 9: **THE GREEN SLICE** (`ba22d1e1`) — eclass-graph.rkt hashcons + intern +
  union-emitter; the D.1 §2.4 literal trace EXECUTES (41 first-run-green checks:
  racing unions → one CALM fixpoint; consuming read via :eclass-link; three-key
  separation asserted). Mid-unit design fix: ':alts equal?-based (digest bytes).

**Loop process observations**: gates caught 2 real regressions + refuted 1 fix
hypothesis + adjudicated 2 flake occurrences — the gate discipline is doing
co-design's verification half. One self-inflicted shell lesson: `grep -c` with 0
matches exits 1 and broke an && chain ahead of a suite run (silently skipped gate,
caught at next firing by the missing timings entry).

**Watching**: (1) over-fire × fire-once precision contract (1 more data point →
propagator-design.md); (2) test-sre-coverage tracked flake occurrences (3 so far,
all pass individually); (3) the &&-chain-before-gate shell pattern — banned, use `;`.

## Iterations 10-17 digest

- iter 10: **#:after + keep-pending substrate** (`01044d05`) — stratification.md
  CAUTION discharged; D5 probe honestly deferred (observer-hook design needed).
- iter 11: **congruence ENGINE** (`8f10fcff`) — sound-if-stale argument;
  asymmetric-staleness property learned at the gate (data point #12).
- iter 12: **congruence WIRING** (`a1e6b9ef` + census sweeps `ad21e550`/`c19dc3a0`)
  — cells 20/21; reactive two-level cascades; census-class lesson (#13).
- iter 13: **effect-safety FLOOR** (`e350a8c0`) — :opaque (the missed 5th facet,
  honest-recorded), guards, deterministic epoch×path kind-2 keys.
- iter 14: **TRACK 1 CLOSED** (`fc5c55a1`/`dd17d2f2`) — close-out audit (D.1 §9),
  PIR (16 questions; §15 OWNER-PROVISIONAL fine-NTT-at-Track-2; §16 codify-NOW),
  bench-ab neutral (±2.5% large; pattern-matching +9.6% watch-listed), doorbell.
- iter 15: **SM3 registry 15a** (`bd546f61`) — schema + dedup-or-error +
  propagator-maintained tag-index; precision contract CODIFIED in
  propagator-design.md.
- iter 16: **SM3 15b** (`b6684a89`) — kernel seed pour live in driver init;
  SM3 COMPLETE.
- iter 17: **Track 2 design OPENED** — HVM2 owner package (B+C recommended,
  OWNER-PROVISIONAL), Phase-0 guard sketch, seed enumeration; doorbell.

**Watching (rollup)**: tracked flakes (sre-coverage occurrences now ~6, module-
network ~2 — all pass individually every time; structural fix = the named
consumer-read migration); pattern-matching bench +9.6% (re-check at next phase
bench); census-class checklist entry (codify on next preallocation); the
&&-chain-before-gate ban (holding).

## Iterations 18-43 digest + THE DAY'S CLOSE

- iters 17-26: **Track 2 designed + CLOSED** (HVM2 package; the guard; the seed
  FIRES; δ; guarded β; PRN §2 CONFIRMED; the A/B caught a real crash + rendered
  the no-flip verdict).
- iters 27-32: **Track 4 designed + CLOSED** (extraction as a propagator
  fixpoint; the cache lattice; residuation; the curve-not-turned verdict with
  levers named).
- iter 33: **the selectivity finding** — ingestion can be FREE.
- iters 34-39: **Track 5 designed + CLOSED** (the .pnet/2 container realized;
  warm-start exists; warm ≈ cold pre-registered; the prn residue finding).
- iters 40-42: **Track 3 opened, verified, hooked, CLOSED** — and THE SERIES
  VERDICT: recording wall-clock-free; memo value ≤ cost on Racket; the
  pre-registered boundary fired → the SH/Zig lowering case, with numbers.
- iter 43: the charter §9 RETRO written; **the loop HALTS per §8** — the queue's
  frontier is owner-sequencing-shaped (the lowering arc; the registries-as-cells
  migration; D5; the residue fix; the provisionals).

**The day**: 43 loop iterations · ~50 commits · 5 tracks closed with PIRs ·
2 substrate findings · ~20 autonomy data points · 3 codified disciplines ·
every gate green or adjudicated · every deviation ledgered · the verdict
honest. See RETRO.md.

## Post-halt addendum (2026-06-11, owner-interactive)

- **The registry-visibility flake family's MECHANISM CONFIRMED + member 2 FIXED**
  (owner-reported test-module-network-01 failure; owner invited the fix).
  Root cause: `current-domain-classification-lookup` was a `make-parameter`
  wired by module-load-time assignment — parameter writes land only in the
  instantiating THREAD's cell; batch workers run each test file in a fresh
  thread (per-file timeout), so `enforce-component-paths!`'s `(when lookup ...)`
  silently skipped whenever the wiring module first instantiated off the worker
  main thread. Partition-order dependence explains the intermittence AND the
  iter-5 refuted-hypothesis (save/restore can't fix what the capturing thread
  never saw). Fix: parameter → module-level box + `set-domain-classification-lookup!`
  setter; injection pattern (cycle-break) unchanged; no scoped parameterize
  consumers (verified). Targeted family tests 99/99; full suite 8655/439 all
  pass. DEFERRED.md family entry updated; same fix shape proposed for
  `current-typing-domain` (member 1) — owner-sequenced.

## Post-halt addendum 2 (2026-06-11): the flake family — 2 of 3 CLOSED, morning claim corrected

- **Owner go-ahead on the family sweep** → ran a 21-agent audit workflow (4 trace
  facets + adversarial verification per finding). The adversarial layer earned its
  keep twice: (1) it refuted the morning's member-2 mechanism claim (driver.rkt's
  static require graph instantiates the wiring on the worker MAIN thread — the
  thread-cell story cannot produce the batch flake under current runners); (2) it
  caught a second destroyer file (test-merge-fn-registry.rkt, 8 reset calls) the
  trace missed, with a controlled repro proving the mechanism.
- **Unified mechanism CONFIRMED for members 2+3**: test files calling
  `reset-merge-fn-registry!` hash-clear! the shared load-time-populated registry;
  module-instance caching means later co-located files can never re-register →
  cells stop inheriting domains → member 3's #f lookups AND member 2's silent
  enforcement skip. Deciding repro: [test-merge-fn-registry →
  test-module-network-01] failed deterministically EVEN WITH the box fix in place
  — the morning's "FIXED" was a lucky partition. Corrected in DEFERRED.md and in
  propagator.rkt's comment (honesty note).
- **Fix**: 18 reset calls deleted across both test files; absolute size assertions
  → delta-style; `reset-merge-fn-registry!` RETIRED. Verified: 6 pair-orders all
  pass + full suite 8655/439 green.
- **Member 1 (test-sre-coverage 'type-bot) remains OPEN** — both candidate
  mechanisms now refuted for its signature (thread-cell: static-require argument +
  iter-5 experiment; registry-clear: destroyer→sre-coverage repro PASSES). Next
  step requires a reproduced failing partition; recorded in DEFERRED.md with the
  audit residue (typing-domain fix plan unapplied for lack of mechanism; ~16
  dead-wiring parameter retirement candidates for PM Track 12; doc-rot list).
- **Lesson (codify-watch)**: "a green suite run does not confirm a flake fix —
  reproduce the failure deterministically first, then show the fix kills the
  repro." The morning fix skipped the repro step; the afternoon audit built the
  repro and the claim flipped within one day. 2nd data point for the
  falsify-first family (with iter-5's refuted restore patch).

## Post-halt addendum 3 (2026-06-11): owner review Q&A — two substrate corrections found

Owner deep-dive questions (reordering / prn / persistence fidelity / flat-vs-
decomposed / Track 8 path) ran as a 5-agent grounded audit; two findings CORRECT
the tracked record (DEFERRED.md amended, code claims spot-checked main-session):

1. **The prn "worklist residue" is a PERMANENT CONTRADICTION, not stale ids.**
   infer-on-network's 200-fuel bounded typing run poisons the prn when one
   command exceeds the budget: the fuel on-write-check sets the network
   contradiction; net-cell-reset restores only the cell value; nothing ever
   clears prop-network-contradiction; both schedulers short-circuit on it.
   All ~112-156 worklist pids are LIVE. Post-poisoning, on-network typing
   silently falls back imperative (probe: 28/3 → 2/12) and PReduce dispatch/
   congruence/extraction go dead. The recorded fix candidate (runner removes
   unfireable ids) targets the wrong layer and would amputate live work. Correct
   fix: clear/scope the bounded run's contradiction at the restore boundary, or
   fork the bounded run.
2. **Warm-start key-survival = 0 (re-pour drops the persisted digest).** The
   loader re-interns best-FORM and recomputes its digest; memo entries key by
   BODY digest → 15/15 real-artifact entries unreachable on reload. Cross-
   session hits were structurally impossible during the iter-39 warm≈cold A/B —
   the pre-registered expectation masked the defect (zero hits ≈ small share).
   ~5-line fix shape recorded; cross-module .pnetx consult absent (wiring, not
   schema); extract/cached still zero production callers.

Lesson (3rd data point for the falsify-first family): a pre-registered negative
expectation can MASK a defect that produces the same observable — when the
pre-registered outcome fires, verify the mechanism (one probe) before attributing.
