# The PReduce Autonomy Experiment — Charter §9 Retro (2026-06-10)

One calendar day. Phase A (interactive co-design): Tracks 0.1–0.3 closed through
six panel rounds and fourteen owner decisions. Phase B (the autonomous loop):
**43 iterations, ~50 commits, FIVE implementation tracks closed** (1, 2, 4, 5, 3
— each with a 16-question PIR), the SM3 registry, two §5.8 instruments, and the
series verdict rendered against a boundary pre-registered before the data
existed.

## What the loop produced (the engineering ledger)

- **Track 1**: the complete e-class substrate — facets, shape-P (~420×), PCE/1
  identity + golden vectors, the product cell + 'eclass-refine, hashcons +
  union, #:after/keep-pending, reactive congruence, the effect-safety floor.
- **Track 2**: the rule ladder — the guard (BLOCKING, tests-first), 15
  declarative folds, δ, guarded β; **the PRN §2 prediction CONFIRMED**
  (reduction as DPO rewriting on the propagator substrate, in the PRN master's
  Confirmed Findings).
- **Track 4**: extraction as a propagator fixpoint (the conjecture's most
  concrete exhibit), the question-keyed cache lattice, residuation's first
  consumer.
- **Track 5**: the .pnet/2 container's first realization; warm-start
  compilation exists; the prn worklist-residue substrate finding.
- **Track 3**: the blocking NAC verification dissolved by one static trace;
  ι on the recursion carriers; **the series verdict**.

## The verdict (what §5.8 was for)

The e-graph recording layer is **wall-clock-free** at full ι+δ/β coverage —
itself a result — but memo value ≤ cost on the Racket substrate at every
workload tested, because memoizable-redex RECURRENCE, not coverage, binds.
The pre-registered boundary fired: **the answer is SH/Zig lowering**, handed
off with the complete dossier (the +52µs floor, every curve, every mechanism
proven). No spin was needed at any point because the charter forbade it.

## What the apparatus got right

1. **Gates as the co-design partner**: 6 real regressions caught, 2 fix
   hypotheses refuted by experiment, 3 test-author errors caught, 2 substrate
   findings surfaced (the residue; the registry-visibility family) — the
   full-suite-per-commit discipline did the verification half of co-design.
2. **Pre-registration**: every A/B carried its failure interpretation BEFORE
   the data; three corpus premises were falsified in single runs and recorded
   as findings, not buried.
3. **The file spine** (charter/HANDOFF/LEDGER/dailies): 43 iterations of
   continuity with zero context loss across ~20 background-gate round-trips.
4. **Scoped units + honest descoping**: every deviation named in the ledger;
   nothing silently widened or narrowed.

## What it got wrong (and what was learned)

- Three shell/scripting self-inflictions (grep -c exit codes; str.replace-all;
  weak acceptance gates) — each now a standing discipline (#16: exit codes;
  asserted-match scripted edits).
- The runtime-probe reflex cost two dead-ends where one static trace answered
  (twice); judgment lesson, recorded.
- The panel machinery was used ZERO times in Phase B — every design round
  resolved with in-context 3-column rationale + recorded panel-skip reasons.
  Honest assessment: correct for THIS phase (the loop had authored every
  adjacent surface), but a longer arc on unfamiliar code would need them.

## Codify-NOW (ripened patterns)

1. **"Falsify the workload premise before building the benchmark"** — 3 points.
2. **"Changed-path precision is a soundness contract where fire-once exists"**
   — codified at iter 15 (propagator-design.md); held since.
3. **The set-comparator family** ("choose comparators by member identity
   semantics; test value-equality across constructions") — 2 points, ripe.

## The owner's queue at halt

- 8 doorbells (Track closes ×5, the denominator, design opens).
- 2 provisionals proceeding on defaults: HVM2 B+C; fine-NTT-at-Track-2.
- The natural next arcs (owner-sequencing calls, which is WHY the loop halts):
  (a) the SH/Zig lowering case (the verdict's successor); (b) the
  registries-as-cells migration (kills the 3-member flake family);
  (c) D5 with the observer hook; (d) the prn residue fix; (e) Phase 2/boolrec
  completeness riders.

## Recommendation on the experiment itself

The apparatus works. The single day produced more verified, honestly-measured
track-closure than the methodology's typical week — AND the negative verdict
it produced is more valuable than a false positive would have been: the
lowering case now exists with numbers instead of hopes. Recommend: re-arm the
loop for the next arc AFTER the owner reviews this halt state and rules on
sequencing; the charter needs no amendment beyond folding in the standing
disciplines named above.
