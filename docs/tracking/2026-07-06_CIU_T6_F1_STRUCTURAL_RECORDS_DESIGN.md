# CIU Track 6, F1 — Structural Record & Collection Typing (Stage-3 Design, D.2)

**Status**: **Stage-3 D.2** — Pre-0 done (§7); independent adversarial critique done ([D.3 record](2026-07-06_CIU_T6_F1_STAGE3_CRITIQUE_D3.md), 4 BLOCKING + 12 SIGNIFICANT confirmed); collection reframe co-designed + owner-locked (D13/D14). This D.2 folds ALL of the above. Next: light re-verification of amended dispositions (§7b) → Stage-4.
**Date**: 2026-07-06 · **Owner**: Zee Larson · **Series/Track**: CIU Track 6, phase F1
**Co-design home**: [`2026-07-05_PATH_SELECTION_RECORDS_DESIGN.md`](2026-07-05_PATH_SELECTION_RECORDS_DESIGN.md) (§2a locked D1–D14; §3b rows research; §4a grounding). Research notes: [rows/coalgebra](../research/2026-07-06_ROWS_COALGEBRA_PROPAGATOR_NOTE.md).
**Verified-at**: HEAD `6d6a24ca` (coordinates re-verified there by main-session R-lens + the D.3 adjudicator; re-grep after drift).

## §1 Goal, scope, and the reframe

**One-sentence F1a job**: replace the elaborator's hardcoded `Open` value-type for unannotated keyword literals with a **ground row**, and teach the map-op typing arms to consume it — so `{:a 1}.a : Int` and `+ {:a 1}.a 1 ⇒ 2 : Int` (owner V5). The projection machinery already works when the value type is concrete (verified: `def am : (Map Keyword Int) := {:a 1}` → `am.a : Int` today).

**The reframe (D13/D14)**: this is not only about records. A record is a structural collection keyed by **Keyword**; a **tuple** is one keyed by **Nat (position)**; a **dictionary/array** is the **dyn-tailed uniform** instance of the same carrier. `@[1 "a"]` (heterogeneous vector) ERRORS today for the *same* reason `{:a 1 :b "x"}` would (one uniform element/value slot) — so heterogeneous collections are **net-new capability**, the positional dual of the record work. The 2×2:

|  | closed / heterogeneous (per-slot types) | dyn / uniform (one type + open tail) |
|---|---|---|
| **Keyword-keyed** | `{:a Int :b String}` — anonymous **record** | `(Map Keyword V)` — **dictionary** |
| **Nat-keyed** | `⟨Int, String, Bool⟩` — **tuple** (flavor A) | `(PVec V)` / array — homogeneous vector |

Plus **flavor B**: a variable-length container whose *elements* differ → `List`/`PVec` with a **union** element type (`'[{:a 1} {:b 2}]` : `List <{:a Int} | {:b Int}>`). *(✏ REVISED by D15, 2026-07-13: heterogeneous literals mint per-position **rows** in BOTH bracket forms — `'[{:a 1} {:b 2}]` : `⟨{:a Int} {:b Int}⟩`; the union-element container is the DERIVED α only, reached at subsumption sites, never minted at the literal.)*

**F1a-core scope** (this slice): the Keyword-keyed closed row (records) + its full disposition + the D.3 blocker fixes. **F1a-col scope** (sibling slice, same CIU-T6 phase): flavor A (tuple minting + classifier) + flavor B (union-widening) *(✏ D15 revised flavor B to row-minting; see §11d for what actually landed)* — turns the critique's list-of-records "regression" into the feature. **Deferred**: dyn tails + `Open` deletion (F1a.2); width + Map↔schema seal (F1b); ρ/`Concat`/`DeepConcat` (F-row); variadic tuples, η, variants (later). No user-writable record/tuple *type* syntax in F1 (inferred + displayed only).

## §2 Progress Tracker

| Phase | Description | Status | Notes |
|---|---|---|---|
| Pre-0 | Probes + baseline + acceptance file + `--check` runner | ✅ | §7; `record-field` struct SAFE; churn small; acceptance 26/26 baseline-green |
| D.3 | Independent adversarial critique (P/R/M/S/U) + adjudication | ✅ | [record](2026-07-06_CIU_T6_F1_STAGE3_CRITIQUE_D3.md); 4 BLOCKING + 12 SIGNIFICANT; 2 REFUTED |
| D.2 | Fold critique amendments + collection reframe | ✅ | this doc |
| re-verify | Light re-verification of amended dispositions (§7b) | ⬜ | probes/anchors reusable from D.3 |
| F1a-s1 | Carrier struct(s) through core pipeline (files 1–8 + `union-types.rkt` + walkers) + display | ✅ | 13 files; `expr-Record`/`record-field` + `record-map-field-types` helper; S1 union-sort-key + S2 walker audit done; `tests/test-record-node.rkt` (15); suite GREEN 8530/448/0; no behavior yet (nothing mints). unify classify (B3) + minting = s2/s3 |
| F1a-s2 | Literal inference + projection + **B1** + **B2** + Record<:Map subsumption + **the full map-op surface** (pulled fwd from s3) + Record↔Map coercion in unify/subtype (NEW, see below) | ✅ | `{:a 1}.a : Int` ✓, V5 ✓, heterogeneous per-field ✓, `idm r` subsumption canary ✓, exact assoc/dissoc/keys/vals/has-key/nil-safe-get ✓, closed-row-miss error ✓. Suite GREEN **8545/449/0**; acceptance 27/27 (`test-f1-records-acceptance.rkt` = suite gate); 4 display-churn tests flipped (incl. the T-2 contract file supersession, D7). Elaborator classifier + record-seed mint; smart-constructor helpers (`make-record`/`record-extend`/`record-lookup-field`/`record-remove`). See §11a. |
| F1a-s3 | Remaining record dispositions | ✅ | Grounding audit `wf_2d535113` → **B3 ✅ `a5c546c8`** (same-shape classify; prelude WS test) · **S10 ✅ already-s2** (`ground-expr?` arm; posture §11b) · **S3+B4 ✅ `83bd416f`** (fold/filter/map-vals Record arms BOTH checkers; both union arms; helpers; whnf wrappers; B4 canaries) · **S7 ✅ `d08e14bf`** (rich closed-row-miss via the #70-precedent hint walk; acceptance asserts the message) · **.pnet cross-module canary ✅ `d08e14bf`** (`test-record-pnet-cache.rkt`, the F2 two-run repro as a permanent test) · non-reachability notes §11b. Acceptance **37/37**; suite GREEN **8586/451/0** |
| F1a-s4 | (absorbed into s2/s3 incrementally) acceptance flips + `het.z` + census/T-2 + path-test flips + `.pnet` canary + prelude WS test + `--check` suite wrapper | ✅ | All landed within the s2/s3 commits; acceptance 37/37 |
| F1a-s5 | Full-suite gate + perf verification + PIR-lite | ✅ | §11c. Suite 8586/451/0; like-for-like: net FASTER (~125→~117ms; tc −25%, qtt +13ms watch item); bench-ab all noise-level, reference saved |
| F1a-col | **D15 observational literal typing**: literal-extent nodes, all-at-once typing — homogeneous → `(PVec T)`/`(List T)` as today; heterogeneous → closed `'nat` rows. Union = derived α only | ✅ | **col-1 ✅ `cf034b56`**: `expr-pvec-literal` through the pipeline; `@[1 "a" true] : ⟨Int String Bool⟩`; unification-based homogeneity (`@[none [some 1]]` still `PVec (Option Int)`); positional projection incl. bare-Int (`tr[1].b ⇒ 2 : Int`, the owner-scratch idiom); Tuple→PVec α (union-branch aware) at all 5 entry points; `'nat` key-gate MUST-FIX landed; acceptance **48/48**; suite GREEN **8601/452/0**. **col-2 ✅ `c6c3ef3a`**: `expr-list-literal (elems chain)`; `'[{:a 1} {:b 2}] : ⟨{:a Int} {:b Int}⟩` (named regression CLOSED); route discovery — WS files use the PREPARSE sentinel, fixed via `$list-literal-parse` parser handoff (tail-syntax/`'[]`/varargs/manual-cons legacy by design); `record-<:-elem?` shared α + (List A) fallback arm; qtt checkQ list-vs-List arm (CHECK-mode elements — the issue-#76 operator class); KNOWN v1 LIMIT: generic folds over row-lists type-sound but value-stalls (S10-at-list-level; CIU T3/T5 home; α = escape hatch); canary now cleans its cache file (pregen-heuristic poisoning found+fixed, tools hardening flagged); suite GREEN **8604/452/0**. **col-3 ✅ `f6716b3a`**: Record arms across ALL pvec ops (grounding `wf_d09c38a7` re-grepped arms to `typing-core:1919-2014` + surfaced push/length/from-list/transient as same-class gaps): EXACT-where-static (nth via record-project, push/update/pop grow-replace-shrink the row, concat ground×ground renumbered append, slice clamped sub-row, from-list row-identity) / uniform-⋃ elsewhere (to-list = escape-hatch List-view NOT row-identity, map position-preserving constant-W, filter (PVec ⋃), transient (TVec ⋃)); 11-arm whnf discipline landed (aliased subjects newly admitted — col-4 canary); **Nat-only index discipline** (pvec-* runtime is nat-value-only → Int literals rejected; `v[i]`/expr-get keeps col-1 Int tolerance); check-mode fold/map arms BOTH checkers (issue-#76 class); qtt §6-class delegation for the now-type-changing echo arms + to-list silent-wrong-type fallback fix + checkQ-fallback Tuple→PVec/List α mirrors (B2 completion); `closed-nat-row?` guard (B3 discipline). `tests/test-tuple-ops.rkt` (21). Suite GREEN **8625/453/0**; acceptance 48/48. (One transient full-suite mass-stall observed then not reproduced — logged as a `task_5e3ec9c2` hardening data point; the pregen hardening landed as `c457f479`.) **col-4 ✅ (col CLOSED)**: acceptance ;;50–65 tuple-op showcase (**64/64** — nth/push/pop/concat/slice/update EXACT, to-list/map/filter views, from-list∘to-list round-trip type-loss pin, **aliased-PVec whnf canary** ;;61–63, static-miss + Int-index-discipline errors); `test-record-collections.rkt` +5 prelude surfaces (keyword from-list = row IDENTITY, from-list∘nth composition, wrapper `pvec-from-list-fn` α-degrades DEF-BOUND rows to `(PVec ⋃)` while INLINE literals stay per-element-check errors — the C2 class, both pinned; `(List ⋃)` value-reduces through prelude `length`, the S10 stall is ROW-lists only); **doc route correction** (§1/§3-B3/§4.2/§4.6/§9 dated ✏ amendments + new **§11d** col close notes); **bench gate**: every comparative program −0.6%…−13.6% vs the f1a-core reference (no regression; attribution spans col + test-infra, see §11d). Whnf-admission precision (audit `wf_80ef80da`): the NEW aliased-subject admission = the 11 PVec-subject arms ONLY (from-list/transient whnf'd pre-col-3). Selection surface = OPEN design conversation (track doc) |
| F1a.2 | dyn tail live; `Open` relocation + node deletion (D1-b/D7) | ✅ | **Mini-design = §12** (D16–D20 locked 2026-07-15; grounding `wf_2b58a7e2`, panel `wf_71c14484`). Ladder: p0 docs+bug-localization+pins → p1a consumers (dead-code-safe) → p1b atomic mint-flip + PNET_VERSION bump → p2 mechanical deletion → p3 gate |
| — p0 | doc batch · QTT-bug localization (time-boxed; default fix-before) · characterization + working-flow pins | ✅ | docs `767bbcae`; fixes `803a57e4` — BOTH bugs root-caused (inferQ missing expr-meta arm; union-expected branch-split-only in BOTH checkers — a THIRD site, typing-core check, surfaced during verification) + fixed-before (shared machinery); `test-qtt-union-meta.rkt` (6) + acceptance ;;66–73 (incl. the D17 flip target). Suite 8660/454/0. The p1b PNET-bump gate condition (only the 5 producers emit the Open tag) is discharged by the census itself |
| — p1a | row-comparison engine + §12.4 per-op dyn table + 5 helpers delegate + C_Cons arms + record-project dyn hook + negative probes | ✅ | `bec0c058` — §12.4 written AS SPEC first (audit `wf_2687a16e` corrected the engine shape: pure fragment in subtype-predicate [tail-blindness = deliberate C_ConsL absorption, comment-only] + typing-core ctx adapters; classify 'sub goals are pure pairs → row-vs-Map rides the existing α arms, only row-vs-row got a new arm; solve-first is structural — all arms both-sides-concrete). meta-V/elem REFUSED from dyn; `record-value-bound` at 6+6 enumerating sites; record-project/union-component/nil-safe dyn legs; S7 tail guard. `test-dyn-rows.rkt` (16 synthetic incl. NEGATIVE wildcard-differentiating probes). Suite **8676/455/0**. **Status: validated (NOT deployed) — nothing mints 'dyn until p1b** |
| — p1b | atomic mint-flip (5 producers per §12.2) + PNET_VERSION 1→2 same commit + canary/pin flips | ✅ | `07c0e70f` (p1b-pre: `expr-map-literal` node dead-code — the D18 ⋃observed mechanism needs the D15 literal-extent recipe, NOT a fresh-meta value slot) + `6a2bc9d5` (THE flip). Probe caught 2 pre-commit: the seed's K slot must stay a META (concrete Keyword broke the D17 annotation escape hatch through B1); qtt map-empty inferQ must delegate the type when the v-slot is a Record (checkQ-top needs the ROW, not (Map ?km row)). Canaries ;;35/;;37/;;66 flipped + ;;74–79 payoff pins. Acceptance **78/78**; suite **8682/455/0**; ZERO caches carry the Open tag — the `:579` wildcard + 9 scrutinee arms are dead code; p2 is mechanical |
| — p2 | Open deletion (~44 sites + wildcard + 9 arms + pnet dereg + test-mixed-map rewrite + two-role docstring) | ✅ | `e4fad7d7` — net −77 lines; grep = 0 code hits; tombstone carries the two-role history; RVU non-empty-by-contract; suite 8682/455/0 |
| — p3 | full gate + close notes | ✅ | `4892fda3` — bench incident diagnosed AMBIENT (worktree A/B + interleaved runs = parity, new marginally faster; §12.7); union-arm refit (bare right branch, zero hot-path cost); close notes §12.7. **F1a.2 CLOSED — D1-b done** |
| F1b | Erasure-mode width + depth; Map↔schema seal; Q4 tightening; presence marks; solve-shape; route-soundness | 🔄 | **Stage-3 CLOSED 2026-07-17 (D21–D26 locked)**: [GROUNDING](2026-07-16_CIU_T6_F1B_STAGE3_GROUNDING.md) · [OPTIONS](2026-07-16_CIU_T6_F1B_STAGE3_OPTIONS.md) · [PROBES](2026-07-16_CIU_T6_F1B_STAGE3_PROBES.md) · rulings = track doc §2a round 6; mechanics = **§13**; ladder = **§13.6**; implementation opens at F1b.1 (first green) |
| F-carrier | The Q_E(b) carrier unification: bounded dyn tails `(K,V)`, key-domain generalization (heterogeneous key-types), `expr-Map` dissolution | ⬜ | **Stub charter only (D26)** — the consolidated retirement home for the round-6 deferrals; entry list + gates in DEFERRED.md § F-carrier; sequencing vs F-row decided by whichever opens first |

## §3 Grounded code facts (VERIFIED at `6d6a24ca`; ✏ = corrected from D.1 by the D.3 critique)

| Fact | Where |
|---|---|
| `Open` minted at exactly 2 sites, both the unannotated keyword-literal seed | `elaborator.rkt:2127`/`:2132`, comment `:2117` |
| Literal entry key/val exprs in-hand at mint (keys are `expr-keyword`) — but the seed is built at `:2129-2132` BEFORE entries elaborate (`:2138`): the all-keyword classifier needs an **entries-first** scan (§4.2) | `elaborator.rkt:2129-2145` |
| `{:a 1}` infers `(expr-Map ?km (expr-Open))` → the **Map arm** `:1512` (returns `vt`), NOT the schema arm `:1536` | `typing-core.rkt:1465-1473`, `:1512-1513` |
| **✏ B1**: annotated-literal checking is TERM-directed AND recurses to the seed: `map-assoc`-vs-`Map` (`:2428-2431`) → `map-empty`-vs-`Map` (`:2424-2426`) `(unify v1 v2)`; today `v1=Open` passes via `unify:574`. A Record seed there breaks it / flex-rigid-poisons `?V` | `typing-core:2424-2431`; `unify.rkt:574/583` |
| **✏ B2**: qtt has its OWN duplicated conversion fallback (`unify`→cumul→`subtype?`) that never calls typing-core `check` — a Record<:Map arm in typing-core is unreachable from `checkQ` | `qtt.rkt:2449-2463`; app-arm `:316-325` |
| **✏ B3**: `classify-whnf-problem` has NO Record case → Record-vs-Record unify falls to `'conv` → fails. Reached by list-element meta solving. `'[{:a 1} {:b 2}]` types today as `(List [Map Keyword Open])` (probe) *(✏✏ col-2 ROUTE CORRECTION, 2026-07-14: "elaborator list = cons chains" was true at `6d6a24ca` but the ROUTE claim misled the col grounding audit — in WS FILES `'[…]` lowers via the PREPARSE `$list-literal` sentinel → `$list-literal-parse` parser handoff (mirroring `$vec-literal`), NOT the tree route; post-col-2 both routes produce `expr-list-literal`, and cons chains remain only for tail-syntax `'[a b \| rest]`, empty `'[]`, varargs, and manual cons — legacy by design. See §11d.)* | `unify.rkt:558-719`; `elaborator` list = cons chains |
| **✏ B4**: today's dynamic-key path DOES check the key (literal keys solve `?km:=Keyword`): `map-assoc q "str" 2` ERRORS today (probe) | `typing-core:1465-1473` |
| **✏ S1**: `union-sort-key` sends unknown nodes to `"9:other"`; `dedup` merges only ADJACENT equals → record-containing unions non-canonical (commutativity/idempotence fail) | `union-types.rkt:43-95`, `:103-111` |
| **✏ S2**: generic meta-walkers recurse on `struct?` only — `occurs?` (`unify:234-245`), `collect-meta-ids` (`metavar-store:915-922`), `ground-expr?` (`[_ #t]`) are BLIND to metas inside a raw assoc-list fields slot | those + `type-lattice.rkt has-unsolved-meta?` |
| **✏ S6**: stdlib **`map-merge` EXISTS** (right-priority), prelude-reachable, feeds `impl Lattice (Map K V)`; `map-merge {:a 1} {:b 2}` → `{:a 1 :b 2}` today (probe). D.1's "no map merge exists" was FALSE (D10 semantics lock unaffected) | `lib/prologos/book/maps.prologos:136-141`, `core/map.prologos:62-66`, `core/lattice.prologos:128/229-233` |
| **collection grounding** (probed): `@[1 2 3]`→`[PVec Int]`; `@[1 "a"]`→ERROR (single elem meta, `syntax.rkt:700`); `expr-Vec (elem-type length)` `:414`; `def v:(PVec <Int\|String>):=@[1 "a"]` CHECKS → `[PVec Int\|String]`; `pvec-nth v 0N`→`1 : Int\|String` (projection works; `0` Int errors — index-type, not union); union normalization FLATTENS (`<Int\|<String\|Bool>>`≡`Int\|String\|Bool`) | probes at HEAD |
| schema projection template + datum→expr; `closed?` consulted at 1 site (check dir) | `typing-core:1536-1548`, `:390-393`, `:351-386`, `:2444`; `schema-field` `macros.rkt:758` |
| `m.a`→`(map-get m :a)`; `m[i]`→`(get m i)`; `expr-get` (both typing-core `:1491`+qtt `:1221`) DELEGATES to infer (free); `map-get`'s `(expr-Map _ vt)` qtt arm `:1213` is INLINE | `macros.rkt:5064/5096/5119` vs `:5112` |
| literal `#p` get-in/update-in **desugar** to `map-get`/`map-assoc` nests at the elaborator (only DYNAMIC paths emit `expr-get-in`/`expr-update-in`) — REFUTES the "path-node flips undeliverable" critique claims | `elaborator.rkt:2242-2252`, `:2320-2337` |

## §4 The design

### §4.1 The carrier — one row, two surface presentations (D6 + D13)

```racket
;; syntax.rkt — the anonymous structural-row TYPE node (internal-only in F1: inferred + displayed, not parsed)
(struct expr-Record (key-domain fields tail) #:transparent)
;;   key-domain : 'keyword | 'nat        (D13 forward-compat: generalize NOW; F1a-core mints 'keyword only,
;;                                        'nat = tuples land in F1a-col. Q_B: a row is ALL-keyword or ALL-nat —
;;                                        homogeneous-key-domain invariant, enforced by the smart constructor.)
;;   fields     : canonical assoc ((label . record-field) ...)
;;                label = keyword-symbol (keyword-domain) | Nat (nat-domain);
;;                sorted by symbol<? / < per domain (smart-constructor-enforced canonical form).
;;   tail       : 'closed | 'dyn         (F1a mints 'closed. 'dyn = F1a.2 (+ must carry (K,V) bounds for the
;;                                        Q_E dictionary reading). ρ = row-kinded meta at F-row. Slot is expr-ready.)
;;   ✏ D16 amendment (2026-07-15): F1a.2's live 'dyn is the BARE SYMBOL, bounds-free (S–I ★).
;;   The (K,V)-bounds reading is deferred to the ONE coherent carrier change that also
;;   generalizes key-domain + dissolves expr-Map (Q_E(b), F1b+) — partial bounds now would
;;   trigger the tail-blind-walker class without unlocking Map absorption (key-domain
;;   cannot express Map String V). See §12.

(struct record-field (type presence) #:transparent)
;;   type     : an ORDINARY type expr/meta  (NO new meta domain)
;;   presence : 'present                    (F1a writes 'present only; 'optional|'absent|'unknown reserved,
;;                                           per D6 — the Malli-optional-keys + narrowing + presence-poly hook)
```

**Two surface nodes, one carrier (Q_A)**: the struct is the *carrier*; the *surface* distinction (record `{…}` vs tuple `@[…]`, and the two displays) is driven by `key-domain`. Whether the nat-domain tuple later gets a physically-distinct `expr-Tuple` struct sharing the field/tail helpers, or rides `key-domain='nat` on this struct, is an F1a-col call — both satisfy "two surface nodes"; the tag is less machinery. F1a-core commits the `key-domain` field so either lands without rework (the forward-compat crux — retrofitting a key-domain parameter after a keyword-only carrier hardens is the rework risk).

**Order-significance is FREE** (D14, verified by construction): `⟨Int,String⟩` = `{0↦Int, 1↦String}` and `⟨String,Int⟩` = `{0↦String, 1↦Int}` are different slot-maps ⇒ unequal — no non-commutative monoid needed for identity/meet. The `key-domain` tag's job is narrow: canonical form (keyword→sort-by-name; nat→already positional), the dense-prefix well-formedness check (nat only, trivially satisfied by literals), and width-applicability (tuples exact/no-width; records get width at F1b).

**Sorted assoc, not hasheq** (Pre-0 #2): determinism for serialization / display / structural `equal?`; n small. Canonicalize ONCE at row completion via a **smart constructor** (the only row producer) — supersedes D.1's "not per-extension" note (S12): user `assoc` builds rows post-literal, so per-op canonical maintenance is unavoidable and the smart constructor owns it.

**Well-formedness**: `is-type (expr-Record kd fs t)` at `Type 0` iff every field type `is-type` (mirrors `expr-Map` `:1445-1448`); `infer-level`→`lzero`. Duplicate labels impossible by construction.

**Record-meet (spec; F1a has ZERO call sites — F1a.2/M1 consume it; S12)**: split honestly into (a) pure-ground: per-field `type-lattice-meet` under the orientation flip (OSF generality-meet = our information-join); (b) meta-bearing: residuated (metas → `type-bot`, conservative). Field-*extension* (assoc right-priority overwrite) is a DIFFERENT non-monotone operation — name it "row extension", never "merge", to keep it clear of the future cell-merge. **Distributivity precondition RESOLVED** (S12/§7b): per-field `type-lattice-meet` distributes over unions BY CONSTRUCTION (`type-lattice.rkt:263-274`); equal?-level canonicity of the distributed form additionally needs the S1 `union-sort-key` fix.

**Records vs dictionaries (a load-bearing split; §6 challenges it)**: a *record/tuple* is a literal with a syntactically-known key set (keyword-literal keys / fixed positions); a *dictionary/array* is uniform with computed keys. F1a gives literals row types and leaves dictionaries/arrays as today; a non-literal key **degrades** to the dictionary view (§4.3, B4-gated). Per Q_E this is end-state (b): `expr-Map`/`PVec` are the dyn-tailed/uniform instances of the one carrier — the split is a *refinement* of D3's "one notion", not a third mechanism; the F1a `Record<:Map` arm is transitional (F1a.2 reworks it onto the dyn tail).

### §4.2 Elaboration + the classifier (the correctness pivot; B1)

`surf-map-literal` (`elaborator.rkt:2113-2145`) — restructure to **scan keys first** (S12 minor: the seed is currently built before entries elaborate; scan the surface keys before choosing the seed):

- **all keys keyword-literal** (non-empty) → **record seed** `(expr-map-empty (expr-Keyword) (expr-Record 'keyword '() 'closed))`. Zero new *term* nodes; the row rides the existing v-type slot; `infer` grows + returns it (§4.3). Empty `{}` → **unchanged** (today's Open seed; the empty-record reading waits for dyn tails, F1a.2 — deliberate).
- **any non-keyword key** → unchanged Open/dictionary seed.

`surf-pvec-literal` (`:2448`) — the **classifier** (F1a-col): homogeneous → `expr-PVec` (today); heterogeneous fixed → tuple (nat-domain row, flavor A, **tuple-by-default** per Q_D). Widening tuple→`PVec`-of-union is a monotone Galois projection on demand. *(✏ SUPERSEDED by D15/Q_col-A as landed (col-1/col-2): there is NO elaborator-level classifier — the elaborator mints literal-extent nodes unconditionally for non-empty literals (`expr-pvec-literal` at `elaborator.rkt:2472`, `expr-list-literal` at `:2458`; `:2448` is now `surf-pvec-type` — coordinates drift, re-grep) and classification happens ALL-AT-ONCE at typing: unification-based homogeneity probe (rollback; success commits) → `(PVec T)`/`(List T)`, else a closed `'nat` row. See §11d.)*

**B1 fix** (the seed must not break annotated literals): add a check arm at the `map-empty`-vs-`Map` site (`typing-core:2424`): when `v1` is an **empty closed `expr-Record`** seed, unify the KEY types only and **skip the value-unify** — the empty row asserts no fields, so it satisfies any `(Map K V)` and does not flex-rigid-poison a meta `?V`. Per-entry strictness is preserved by the `map-assoc`-vs-`Map` arm's `(check ctx v vt)` as the chain unwinds. This transcribes the seeded-`Open`'s annotation-satisfaction duty for the record seed, one arm. **Corrects D.1 §4.2's false "never consults the seed" claim.** Gated by acceptance canaries 9/14/16 + new `map-merge` canary at s2 (NOT s5).

### §4.3 Typing-arm disposition (F1a-s2/s3) — with all D.3 fixes

`R` = whnf-inferred `(expr-Record kd fs 'closed)`. All `typing-core` `infer` unless noted.

| Site | Disposition on `R` | Fix |
|---|---|---|
| `map-empty` `:1450` | v-type `expr-Record` → return it | |
| `map-assoc` `:1465` | keyword-lit key → `infer` v, **row-extend** right-priority (D10 exact extension). Non-literal key → **first `(check ctx k (expr-Keyword))`**; on fail `expr-error` (preserves today's rejection); on pass degrade `(expr-Map (expr-Keyword) (expr-Open))` | **B4** |
| `map-get` `:1512` / `get` `:1491` | keyword-lit: present → field type (**the goal**); absent → `expr-error` + closed-row-miss diagnostic. Non-literal: `(check k Keyword)` then `⋃fields` | B4 |
| `nil-safe-get` `:1585` + its **own union loop `:1587-1604`** | present → `field\|Nil`; absent+closed → `Nil`; **union loop must accept Record components** (drops them today → wrong Nil-only) | **S3** |
| `map-dissoc` `:1619` | keyword-lit → `R` minus field (exact); non-literal → B4-gated degrade | B4 |
| `map-size` `:1626`/`has-key` `:1634` | `Nat`/`Bool` (+ `R` arm) | |
| `map-keys` `:1643`/`vals` `:1651` | `(List Keyword)`/`(List ⋃fields)` | |
| **`map-fold-entries` `:1836` / `map-filter-entries` `:1850` / `map-map-vals` `:1861`** | operate through the derived uniform view (`K=Keyword`, `V=⋃fields`); today `[_ (expr-error)]` → Record regresses to hard error | **S3** |
| union arm of `map-get` `:1549-1568` | accept `expr-Record?` components; **miss policy = filter** the missing component (Q5; matches the arm's existing Map precedent; all-miss already errors). Do NOT copy `with-speculative-rollback` into new arms (§8) | S3/Q5 |
| **check-subsumption `:2700-2706`** (NEW) | `R <: (Map K V)`: labels vs `K`; `V` meta → solve `V := ⋃fields` (Galois α; empty row → succeed WITHOUT solving `V`, Q6); `V` concrete → per-field check | Q6 |
| **`unify.rkt` classify `:558-719`** (NEW, #14) | Record-vs-Record: equal key-domain + labels + tails → `(list 'sub per-field-goals)` (sorted zip); label mismatch → `'conv` + closed-row-miss. **Different-shape → union (F1a-col flavor B), NOT here** | **B3** |
| **`expr-update-in` `:1429`** (dynamic path) on `R` | **degrade** result to `(expr-Map Keyword Open)` (F1a; dyn tail subsumes F1a.2) — precise-but-unsound rejected (Q7). *Literal* update-in desugars → safe (no arm needed) *(✏ 2026-07-15: NEVER IMPLEMENTED as written (HEAD returns the target type unchanged — the F1a.2 audit caught the design-vs-HEAD drift) and now SUPERSEDED by D20: dynamic update-in on a record → `{\| _}` empty dyn row; the `(Map Keyword Open)` degrade must never land — it would ADD an Open mint during the deletion phase)* | **S9**/Q7 |
| **`expr-get-in` / `expr-broadcast-get`** (dynamic) | unchanged (fresh meta, Record-indifferent) — one-line "unchanged" rows | S9 |
| **trait-resolution** `ground-expr?` + `expr->impl-key-str` | add `ground-expr?` Record arm (meta-bearing → not ground); posture: record types match no Map-headed instance head in F1a — annotation is the escape hatch (α-bridge is F1b/UCS-5) | **S10** |
| **qtt** `map-get` `:1213` + `map-assoc` `:1196` + **`map-dissoc` `:1244`** + the **conversion fallback `:2449-2463`** | delegate the TYPE to `(infer ctx e)` (the `:1215` schema-arm pattern), usage local; the fallback delegates type-comparison to typing-core `check` so the Record<:Map arm is reachable. map-assoc delegation is **unconditional**. Route any cell-level meta-solve through `solve-meta!` (pipeline.md Known Coupling) | **B2/S4** |

**Diagnostic transport (S7)**: `expr-error` is nullary — the closed-row-miss message needs a channel. Mechanism: a `typing-errors.rkt` hint arm matching `map-get`-on-Record-with-absent-keyword (Issue-#70 walk precedent), field list capped (~6 + "+N more"), typo-suggestion optional. If descoped, the `het.z` acceptance assertion downgrades to "is a type error".

**Generic-walker audit (S2, s1)**: extend `occurs?`, `collect-meta-ids`, `ground-expr?`, `has-unsolved-meta?` (+ grep the ~6 `struct->vector` walkers) with a pair?/list? recursion into `fields`, OR make the fields spine struct-based so the generic walk works unmodified. Regression tests: occurs-check meta-in-field; constraint-retry with a record-embedded meta. Propose the pipeline.md checklist addition ("sub-exprs inside a non-struct container → audit every generic walker").

**Untouched by F1a-core**: `unify:574-575` Open wildcard (D7 relocation = F1a.2); `subtype-predicate.rkt` (no record-subtype judgment, D11; width = F1b erasure-mode); the two term-directed check arms except the B1 seed guard; reduction/runtime (values stay CHAMP). **Coverage-gap check to close at s1** (D.3): `reduction.rkt` `whnf`/`nf`/`trivially-whnf?`/`definitely-not-map?` treatment of the new node.

### §4.4 `merge` / `deep-merge` (D10; corrected grounding S6)

**Correction**: stdlib `map-merge` (right-priority) **exists** and feeds `impl Lattice (Map K V)` — D10's *semantics* lock is unaffected (it already implements right-priority), but the merge-node mini-design must **disposition the live stdlib function** (Q4: retire-into-node — recommended — vs alias vs coexist). M1 typing (both sides `R`-ground): result row = right's fields + left's fields with labels absent-on-right (deep: recurse where both field types are records — finite trees, terminating). Non-ground → dyn-tail (F1a.2). M3 (F-row): `Concat`/`DeepConcat` residuated relational constraints, 3-cell fundep-directional propagator in the one solver. `map-merge {:a 1} {:b 2}` becomes a subsumption-arm canary (its polymorphic `(Map K ?V)` flow exercises the B1 flex-rigid path) — but **`map-merge` is prelude/stdlib** (verified §7b: unbound under `:no-prelude`), so this canary lives in the **prelude-loaded** WS test, not the `:no-prelude` acceptance file.

### §4.5 Open relocation (D7 — F1a.2 summary)

F1a leaves every non-literal `Open` path byte-identical. F1a.2: `'dyn` tail live ((Map K V)-annotations + dynamic-key degradations produce dyn-tailed rows carrying (K,V) bounds); transcribe Sekiyama–Igarashi `C_ConsL`/`C_ConsR` absorption into unify for dyn tails (`unify:574`'s duty, principled home); unknown-field projection on a dyn tail mints a fresh meta + records the observation; then delete `expr-Open` (~38 sites/11 files) with the two-role history recorded (docstring). D1-b closes there. *(✏ SUPERSEDED 2026-07-15 by the F1a.2 co-design — §12. The grounding revised the premises: FIVE producers not two; ~44 code sites; `(Map K V)` annotations do NOT mint rows (key-domain can't express `Map String V` — D16); the tail is bounds-FREE (bare `'dyn`, D16); recording is descoped to F-row (D19); coordinates drifted (unify 574→579-580).)*

### §4.6 WS impact

No reader/parser/preparse change in F1a-core (`{:a 1}.a` already parses). *(✏ F1a-col DID change preparse/parser: col-2's `$list-literal-parse` handoff — §11d.)* Surface: **(1) display** — record → `{:a Int :b String}` (canonical order); nested nests; tuple (F1a-col) → `⟨Int, String⟩` (delimiter TBD) *(✏ resolved space-separated `⟨Int String⟩`, owner Q_col-D)*; dyn tail (F1a.2) → `{:a Int | _}`; `expr-Map` unchanged. **Union display fix (S-minor)**: `pp-expr` prints unions bare `~a | ~a` (`pretty-print:718`) — the acceptance `;;N=>` targets `(List <Int | String>)` must use the ACTUAL convention (`Int | String`), not delimited. **(2) diagnostics** — closed-row miss names available fields (§4.3 S7). **(3) not user-writable** — `(Map K V)`/`schema` remain the annotation forms.

### §4.7 Acceptance (EXISTS, baseline-green 26/26; §7.5)

`examples/2026-07-06-ciu-t6-f1-records.prologos`, `:no-prelude`, self-verifying via `run-file.rkt --check`. s4 flips `;;N=>` to F1a targets + adds to the `:no-prelude` file: `het.z` closed-row miss (**append at file END** to avoid index renumber — S-minor); two dynamic-key canaries (Keyword key → degrade; String key → error, covering B4). **NEW prelude-loaded WS test file** (surfaces need `'[…]` list literals + stdlib `map-merge` = prelude): `'[{:a 1} {:a 2}]` same-shape (B3) + the different-shape named-regression/escape-hatch; `map-merge {:a 1} {:b 2}` + two-record `map-merge` (subsumption canary, §4.4); a `.pnet` **cross-module canary** (a Record-typed def in a lib module consumed downstream — the F2 detonation surface the single-file `:no-prelude` acceptance cannot reach; D.3 coverage gap). Plus `test-first-class-paths.rkt` `"1 : Open"`→`"1 : Int"` flips (`:113/:127`). **S10 blast-radius** (does trait dispatch on `(Map ?km Open)` literals succeed today?) quantified here via a prelude probe — the design posture (§4.3 S10) does not depend on the count.

## §5 SRE lattice lens (records + collections)

1. **Classification**: STRUCTURAL — labeled product of per-field/-position VALUE lattices, indexed by a support set + key-domain + tail.
2. **Algebraic properties**: per-slot = flat type lattice (join-semilattice + ⊤/⊥); support = powerset (Boolean); tail = 3-point info order. Whole = OSF-family feature lattice. **Meet vs union are DIFFERENT operations** (Q3 resolution): *meet* (same object) = D11 conjunction-of-facts (conflict ⇒ ⊤/contradiction — for tuples, position-i-Int ∧ position-i-String = ⊥); *collection-element* context = union (no meet taken). D6's "field-union formula" conflated them; now separated. **Distributivity RESOLVED** (§7b).
3. **Bridges**: (a) per-slot ↔ flat lattice (Galois, trivial). (b) row ↔ `(Map/PVec K V)` = the uniform-bound α(R) = `(… ⋃fields)`; the §4.3 subsumption arm IS this α. (c) row ↔ schema = up-shift embedding; seal = checked partial inverse + residual (F1b), NOT a lattice morphism (polarity boundary).
4. **Composition**: field types may be unions (normalized) or nested rows; least fixpoint of the labeled-product functor; no cycles (ground literals finite trees).
5. **Primary/derived**: literal ground row = PRIMARY; `(Map/PVec …⋃…)` view = DERIVED (α); tuple→PVec-of-union widening = a Galois projection (SRE Q5), realized as an on-network propagator NOT an ad-hoc cast.
6. **Hasse**: nodes = (support, per-slot assignments, key-domain, tail); edges = single-fact refinements. Per-slot independence ⇒ the diagram factors as a product = the parallel decomposition (per-slot ↔ compound-cell components at F-row; set-latch/broadcast per key). F1a: documentation; F-row: operational.

## §6 Principles gate (challenge column)

| Decision | Serves | Challenge |
|---|---|---|
| One carrier, two surface nodes, key-domain tag (Q_A) | Most-Generalizable-Interface + Decomplection (laws decomplected by the tag) | More-aligned than pure-one-node (leaks laws across key-domain) AND than two-fully-separate (duplicates machinery + reopens tuple-subtype). Matches frontier (unify substrate, split surface). **Active enforcement**: a law reading the carrier without checking the key-domain tag = latent law-crossing bug. |
| Records vs dictionaries split | Decomplection | Challenged "too syntactic?": the *literal* is the one place the key set is a fact (TS fresh-literal / Typed-Clojure complete-HMap precedent). Q_E frames it as a D3 refinement (dyn-tailed instance), not a 3rd mechanism. |
| Tuple-by-default (Q_D) | First-Class-by-Default | Records already first-class per-field; array-by-default (TS) discards proven per-position facts + needs `as const` ceremony. Widening is a monotone Galois step, so no expressiveness lost. |
| Degrade-to-dictionary on dynamic keys (B4-gated) | Honest scoping | Now key-CHECKED (B4) — not "byte-identical" (that was false); preserves today's rejections. F1a scope boundary, D7 relocation scheduled. |
| No record/tuple-subtype judgment (D11) | Decomplection; compositional safety | Extends to tuples verbatim (per-position conjunction; Tang non-existence warrants refinement-only). `subtype-predicate.rkt` untouched — verified safe (compound-type? excludes the node → `subtype?` degrades to `equal?`, no unsoundness). |
| Union-widening VISIBLE not silent (flavor B) | Correct-by-Construction | Tang: unbounded widening breaks principality — keep `List<Int\|String>` a user-visible outcome; run the SRE lattice check on the union merge; union is a monotone join (CALM-safe). |

**NTT-relevance**: F1a adds **zero propagators, zero cells** — value-type-lattice representation inside imperative `infer`/`check` (same status as all typing-core; on-network typing is PPN's turf). Network Reality Check honestly returns "function-call chain" — correct for this layer (verified: map ops are `#f`-registered imperative returns, `typing-propagators:2366`; ctor-desc is positional so the row node can't ride it). The NTT-model obligation attaches at **F-row** (rows-as-cells, `Concat` 3-cell propagator, set-latch per-key). F1a carries only the forward-compat obligations (§4.1 key-domain generalization; ordinary metas; per-slot components) that keep F-row's path clear.

## §7 Pre-0 results + §7b re-verification

**§7 Pre-0** (unchanged from D.1, executed 2026-07-06): (1) serializer probe ✅ `record-field` struct SAFE (generic walk + empirical round-trip; `reg2!` each). (2) field-rep micro ✅ not perf-driven (<0.05µs lookup; sorted-assoc on determinism). (3) census ✅ small on the display grep — **corrected by S5**: `test-mixed-map.rkt` is the PPN 4C T-2 "Open by Design" contract file with ~10 colon-less `"Open"` assertions the display grep MISSED → s4 is flip-or-retire *decision* work (~2× budget) + a T-2-supersession note. (4) baseline ✅ elaborate 16 / type_check 37 / qtt 6 / zonk 22 / reduce 45 ms. (5) acceptance ✅ Level-3 WS wiring verified. (6) `run-file.rkt --check` ✅ shipped.

**§7b re-verification (D.2, executed 2026-07-06)** — the D.3-amended dispositions, probed at HEAD:
- Flavor-B projection ✅ **de-risked**: `pvec-nth (v:(PVec <Int|String>)) 0N` → `1 : Int | String`. Earlier "gap" was `0`(Int)-vs-`0N`(Nat) index, not the union. Flavor B needs only inference-widening, no new projection arm.
- Union normalization ✅ associative/flattening (`<Int|<String|Bool>>` ≡ `<<Int|String>|Bool>` ≡ `Int|String|Bool`); record-containing unions need the S1 `union-sort-key` case for commutativity (on the amendment list).
- Order-from-position ✅ (by construction: `⟨Int,String⟩`≠`⟨String,Int⟩` are distinct slot-maps) — no non-commutative monoid needed for F1a.
- Ordered-rows/concat principal-types: no impossibility (Wikipedia/Wand/Morris–McKinna); the concat principal-types issue is key-domain-agnostic = the record-concat issue already solved by D10 residuation; Ur/Web existence proof. **Deferred to F-row, inherits D10.**
- Distributivity ✅ RESOLVED (holds by construction, `type-lattice.rkt:263-274`).

## §8 Risks (updated) + coverage gaps

- ~~pnet nested-struct~~ ✅ (Pre-0 #1). ~~distributivity~~ ✅ (§7b). ~~map-merge-exists~~ ✅ folded (S6).
- **Union-arm `with-speculative-rollback` inconsistency** (`:1549-1568`, the "retired" mechanism): F1a extends this arm (accept Record components) but must NOT propagate the shape into new arms; excise-or-defer is an s3 call (tracking note if deferred).
- **Union-widening principality** (flavor B): keep VISIBLE; SRE lattice check on the union merge; not-yet-probed that a computed record-union stays non-⊤ (F1a-col gate).
- **Heterogeneous projections become precise** → code silently flowing `Open` into arithmetic may surface real errors (strictly more sound; suite quantifies).
- **Different-shape record collections** = named regression in F1a-core (escape hatch: `: (List <r1|r2>)`, verified to check), closed by F1a-col; prelude-loaded WS test pins it.
- **Dense-prefix / variadic / η** = F-row/F1b (existence-proofed; not F1a).
- **Coverage gaps to close** (from D.3, s1/s4): `reduction.rkt` whnf treatment of the node; cross-module `.pnet` consumer canary; pattern-match/narrowing over record scrutinees (probably nil — types not patterns — but assert); record-TYPE vs map-VALUE display collision in error/REPL/LSP contexts; trait dispatch on `(Map ?km Open)` literals today (2-line probe to settle S10 blast radius).

## §9 Deferred (explicit)

~~Flavor A tuple mint + Flavor B union-widen (F1a-col, still CIU-T6)~~ *(✏ LANDED as D15 row-minting: col-1 `cf034b56`, col-2 `c6c3ef3a`, col-3 `f6716b3a` — §11d)*; dyn tails + Open deletion (F1a.2); width + seal + `closed?` scan + rank≤2/weak-preservation docs (F1b); `merge`/`deep-merge` nodes + M1 (F1a.2-era); ρ/`Concat`/`DeepConcat`/ambiguity-check (F-row; UCS-5+SRE-5); variadic tuples + η-on-closed (later CIU, possibly dependent-typed per owner Q_C note); variants (F-variant); presence marks beyond `'present` (with schema optional-keys); user-writable record/tuple type syntax; V2–V4 selection syntax (track doc).

## §10 The seven questions — resolutions (co-design, 2026-07-06)

1. **Q1 heterogeneous collections** → DESIGN GOAL (D13/D14): flavor A (tuple, net-new capability, fixes `@[1 "a"]` error) + flavor B (union-element list, turns the "regression" into a feature). Classifier is the correctness pivot. (F1a-col.)
2. **Q2 D3 end-state** → **(b)** `expr-Map`/`PVec` = dyn-tailed/uniform instances of the one carrier; F1a `Record<:Map` arm transitional; dyn tail carries (K,V) at F1a.2.
3. **Q3 record-meet different supports** → tension DISSOLVED: meet = D11 conjunction (contradiction on conflict, tuples too); collection-element = union (no meet). D6-vs-D11 was meet-vs-union conflation.
4. **Q4 stdlib map-merge** → retire-into-node (recommended) when the merge node lands; rides Q2(b). D10 semantics unaffected.
5. **Q5 union-arm miss** → union-PRESERVE (neither filter nor error for the collection case); field present-in-some-arm → `Option`/refinement.
6. **Q6 empty corners** → empty-record = empty-tuple = unit (row-monoid identity); `{} <: (Map K ?V)` succeeds WITHOUT solving V; `map-vals {}` → `(List fresh-meta)`; dissoc-absent = identity; dynamic-get-on-`{}` = miss error.
7. **Q7 dynamic update-in** → degrade to dictionary view (sound); tuple case admits a sharper bounds-checked answer via known length (small win). Literal paths safe (desugar).

Plus the collection forks: **Q_A** one-carrier/two-nodes ✅; **Q_B** forbid mixed-key ✅; **Q_C** closed tuples v1, variadic deferred ✅; **Q_D** tuple-by-default ✅; **Q_E** end-state (b) ✅.

## §11 D.3 critique disposition (finding → resolution)

**BLOCKING** (all CONFIRMED): **B1** seed breaks annotated literals → §4.2 seed-check arm (s2). **B2** subsumption unreachable from qtt → §4.3 qtt-fallback delegate (s2). **B3** Record-vs-Record unify undispositioned → §4.3 classify case (same-shape, s3) + union-widen for different-shape (F1a-col) + prelude WS test. **B4** dynamic-key drops key check → §4.3 Keyword gate (s3).
**SIGNIFICANT** (all CONFIRMED, folded): S1 union-sort-key (§4.1/s1) · S2 walker audit (§4.3/s1) · S3 fold/filter/map-vals + nil-safe-get union (§4.3/s3) · S4 qtt dissoc (§4.3) · S5 census re-budget + T-2 (§7) · S6 map-merge exists (§3/§4.4) · S7 diagnostic transport (§4.3) · S8 third mint site (get-in projection maps) → extend the seed rule or name-the-regression (s2) · S9 dynamic update-in degrade (§4.3/Q7) · S10 trait-resolution posture (§4.3) · S11 empty corners (§4.3/Q6) · S12 record-meet spec split + smart-constructor + distributivity-resolved (§4.1).
**REFUTED** (do NOT descope): literal-path get-in/update-in flips ARE deliverable (elaborator desugars — §3 last row); the s4 flips stand.
**Coverage gaps** → §8.

## §11a F1a-s2 implementation notes (what shipped + the design refinement it revealed)

**Delivered (commit at s2 close)**: elaborator classifier (all-keyword literal → record seed `(map-empty Keyword (Record 'keyword '() 'closed))`, growing via the map-assoc infer arm); projection (`map-get`/`get` → `record-project`); B1 (empty-record seed-check arm, `typing-core:~2485`); Record<:Map subsumption (`record-<:-map?`, in check-subsumption + qtt fallback = B2); qtt map-assoc/map-get/dissoc delegation (S4); the **full map-op surface pulled forward from s3** (dissoc/keys/vals/size/has-key/nil-safe-get Record arms — minting records makes these encounter records, so leaving them erroring would be a regression); smart-constructor helpers in `syntax.rkt` (`make-record` canonicalizes right-priority + sorted; `record-extend`/`record-remove`/`record-lookup-field`). 4 display-churn test files flipped (`test-first-class-paths`, `test-mixed-map` [the T-2 "Open by Design" contract file — SUPERSEDED per D7, its α-Open-trust tests now assert the sound static behavior], `test-path-expressions`, `test-postfix-index-03`).

**DESIGN REFINEMENT (the implementation revealed, per the per-phase discipline)**: D.2 §4.3 said the Record→Map α is "a local **check** arm" and "F1a leaves `subtype-predicate.rkt`/`unify` untouched." **That was insufficient.** The α must ALSO be reachable from:
- **`subtype?` + `structural-subtype-ground?`** (`subtype-predicate.rkt`) — for **nested** subsumption: `(List Record) <: (List Map)` recurses through the covariant List walk (`structural-subtype-ground?`, NOT `subtype?` at top), so `record-subtypes-map?` (pure structural, V concrete) lives in both.
- **`classify-whnf-problem`** (`unify.rkt`) — for **polymorphic** subsumption: `'[{:a 1}] = (cons {:a 1} nil)` commits `cons`'s element param to the record, which then meets the `(List (Map K V))` annotation through `unify` (not check). A directional `(Record,Map)`+`(Map,Record)` coercion (always `record-subtypes-map?(record,map)`) reconciles the concrete-V case here; meta-V falls through to check-subsumption's meta-solving arm.

This is a **scope refinement, not a principle violation**: **D11 (no record<:record judgment) is preserved** — this is the record→Map **α bridge** (a record projected to its uniform-Map view, Q_E), reachable wherever a record meets a Map in a subsumption context. Concretely: `record-subtypes-map?` (pure, in `subtype-predicate.rkt`, provided to `unify`) for structural/nested; `record-<:-map?` (in `typing-core`, solves meta-V) for the top-level check arm. Regression that forced this: `postfix-index-03`'s `def xs : (List (Map Keyword Nat)) := '[{:age 25N} …]` (before s2 it worked via `(Map K Open)`'s `Open` unifying with `Nat`; records don't, so the bridge is required). **Amend D.2 §4.3's "untouched" claim accordingly.**

**Deferred to s3 (genuinely remaining)**: B3 Record-vs-Record same-shape unify classify case + the prelude-loaded WS test; B4 the acceptance dynamic-key canaries; S3 fold-entries/filter-entries/map-map-vals arms + the union-arm Record components; S7 the rich closed-row-miss diagnostic (currently a plain "Could not infer type"); S10 trait-resolution ground-expr? arm + posture; the cross-module `.pnet` canary. Flavor A/B = F1a-col.

## §11b F1a-s3 close notes (2026-07-06)

**Shipped**: B3 `a5c546c8` · S3+B4 `83bd416f` · S7+canary `d08e14bf` (details in the tracker row + commit messages). Suite GREEN 8586/451/0; acceptance 37/37.

**S10 posture (documented)**: record types match **no Map-headed instance head** in F1a — `expr->impl-key-str` has no Record case, so trait dispatch on a record-typed value yields a clean no-instance error, never a crash; `ground-expr?` has the Record arm (s1) so meta-bearing records don't misreport ground. The escape hatch is annotation (the record subsumes into `(Map K V)` via the α, and the Map-headed instance then matches). The α-bridge at constraint-argument position is F1b/UCS-5 material.

**B3 known limitation (documented in `a5c546c8`)**: a degenerate cross-command flow — a `def` that leaves an *unsolved meta* inside a record field's type, later force-solved by same-shape unification in a NEW command — hits `solve-meta!`'s unknown-metavariable path (pre-existing cross-command meta-staleness, surfaced not caused by B3; single-command flows are clean; zero suite hits).

**Non-reachability notes (asserted, not assumed)**:
- *Narrowing / match*: record-typed scrutinees flow through `match` cleanly (probed: literal-arm dispatch on a projected field + wildcard on the record value itself — 0 errors). Records are TYPES; map values aren't constructor patterns; no pattern-compiler arms needed in F1a.
- *Transients*: the transient-builder path (`expr-trrb`/`expr-tchamp` → persist) is dictionary-flow; records never arise there (record types mint only at the keyword-literal seed).
- *reduction sets*: `expr-Record` joined `trivially-whnf?`/nf-identity at s1 (a TYPE node, never in value position; runtime values remain champs).

**S7 check-path note**: the rich message rides `infer/err` (the #70 precedent). The `check/err` path (e.g. an annotated def whose body contains the miss) still gets the plain failure — extend the hint there only if it proves annoying in practice.

## §11c F1a-core s5 gate — PIR-lite (2026-07-06)

**Objectives vs delivered**: the F1a-core objective (§1 — literals mint ground rows; projection returns observed types; every consumer arm dispositioned; the D.3 blockers fixed) is DELIVERED across s1 (`7330b582`), s2 (`589fb067`), s3 (`a5c546c8`/`83bd416f`/`d08e14bf`). Suite GREEN **8586/451/0**; acceptance **37/37** self-verifying at Level 3.

**Perf verdict (microbench-claim verification per workflow.md)** — like-for-like instrument = the EXACT Pre-0 26-form acceptance file (via `git show 6d6a24ca:…`) on the post-s3 compiler vs the Pre-0 numbers:
- **type_check 37 → 27-29ms (−25%)** — direct field lookup replaces Open-wildcard churn on record-dense code.
- **qtt 6 → 18-19ms (+13ms, 3×)** — the honest cost of the qtt DELEGATION pattern (record-op arms re-run typing-core `infer` for the type while keeping usage local). **WATCH item**: correct-by-construction beats the divergence bug class at these magnitudes; if it ever matters, the fix is cross-checker infer memoization (not warranted now).
- elaborate +2-3ms (entries-first scan + seed mint); zonk/reduce ~flat. **Net wall ~125 → ~117ms: F1a-core is net FASTER on record-dense code.**
- Suite-level: 131.4-137.4s across the three s3 gates vs ~130-135s pre-F1a — inside normal variance, no 1.2×-median signal.
- `bench-ab --runs 10 benchmarks/comparative/` at post-s3 HEAD: **all comparative deltas noise-level** (±1.5%, every p>0.2, CVs 0.8-5.7% — constraints/solve/type-adversarial + simple-typed all clean); saved as `data/benchmarks/f1a-core-post-s3-2026-07-06.json`, the F1a-core reference for future A/B (no `--ref` checkout run: owner WIP in the working tree makes checkout-based A/B unsafe this session).

**What went well**: probe-first caught 2 bugs before any test (the `map` pattern-var shadowing; the earlier B1-class seed behavior); the grounding audit corrected the s3 enumeration before code (both union arms, check-side arms, whnf wrappers); the incremental acceptance-flip discipline meant s4 dissolved into the slices.

**What surprised**: S10 was already done (s2 pulled it forward silently — the audit caught the tracker drift, not the code); `map-merge` being prelude-only moved a canary; the qtt delegation cost showing up 3× on a microinstrument while invisible at suite level.

**Debt carried (named)**: B3's degenerate cross-command stale-meta limitation (pre-existing class, documented in `a5c546c8`); S7 not on the check-path; the union-arm's inherited `with-speculative-rollback` shape (excise decision deferred with tracking note, §8); `Open` still present everywhere non-literal (BY DESIGN — D7 relocation is F1a.2's whole job).

**Right problem?** Yes — the V5 goal works end-to-end in WS, and the collection reframe (D13/D14) means the same carrier now pays for tuples + heterogeneous collections next (F1a-col), exactly as designed.

## §11d F1a-col close notes (col-1…col-4, 2026-07-13/15)

**Commits**: col-1 `cf034b56` (tuples) · col-2 `c6c3ef3a` (lists under D15) · col-3 `f6716b3a` (tuple-op dispositions) · col-4 (canaries + this route correction). Per-slice detail in the §2 tracker row; D15 + the Q_col decisions in the track doc §2a.

**What landed vs §4.2's design**: D15 (owner, 2026-07-13) replaced the elaborator-classifier + union-widening design with **literal-extent nodes typed all-at-once** — the elaborator mints `expr-pvec-literal` (`elaborator.rkt:2472`) / `expr-list-literal` (`:2458`) unconditionally for non-empty literals; TYPING runs the unification-based homogeneity probe (rollback; success commits) → `(PVec T)`/`(List T)` byte-identical to pre-F1a, else a closed `'nat` row. The union container is the derived α only (`record-<:-elem?`), never minted. Legacy-by-design lowerings keep meta-seeded/cons semantics: empty `@[]`/`'[]`, tail-syntax `'[a b | rest]`, varargs, explicit `pvec-push`/manual cons.

**The route correction (col-2's load-bearing discovery)**: WS FILES lower `'[…]` through the **preparse sentinel**, not the tree route the col grounding audit read — `parse-reader.rkt` emits `$list-literal` → preparse macro `expand-list-literal` (`macros.rkt:4723-4731`): no-tail literals hand off as `($list-literal-parse elems…)` → `parser.rkt` → `surf-list-literal` (mirroring `$vec-literal`); tail-syntax keeps the cons foldr; empty → `'nil`. Both routes converge on `expr-list-literal (elems chain)`. **Lesson (handoff §4.1): when a literal's typing changes route-dependently, instrument the driver to see WHAT NODE arrives — don't trust route claims.** (Coordinates cited here drift — re-grep.)

**col-3 dispositions (one line each; full table in the tracker row + `tests/test-tuple-ops.rkt` pins)**: EXACT where the position structure is static — nth (record-project), push (grow), update (literal in-bounds replace; out-of-bounds = static miss), pop (drop last), concat (ground×ground renumbered append; residuated `Concat` stays F-row), slice (clamped half-open sub-row renumbered), length, from-list (**row identity** — rows carry no container tag). Uniform-⋃ where not — to-list (`(List ⋃)`, deliberately NOT row-identity: a row-typed cons list would grow the pinned S10-at-list-level stall surface; to-list IS the escape hatch), fold, map (**position-preserving constant-W**, the map-map-vals mirror), filter (`(PVec ⋃)`), dynamic index/bounds, transient (`(TVec ⋃)`). Check-mode fold/map arms in BOTH checkers (the issue-#76 class); qtt delegation-to-infer for the now-type-changing echo arms (the §6 divergence class) + to-list's silent row-echo fallback fixed + checkQ-fallback Tuple→PVec/List α mirrors (B2 completion); `closed-nat-row?` guards every EXACT arm ('nat + 'closed explicit — B3 forward-necessary).

**Whnf-discipline scope (precision the col-3 framing missed)**: the NEW aliased-type admission is exactly the **11 PVec-subject arms** (bare `(infer ctx v)` → `(whnf …)`); `pvec-from-list` and the generic transient dispatch whnf'd their subjects BEFORE col-3. Pinned: `def MyVec := (PVec Int)` subjects flow through the ops (acceptance ;;61–63).

**Index discipline (col-3 decision)**: pvec-* ops are **Nat-only** on tuples as on PVecs — the pvec-* runtime is `nat-value`-only, so admitting Int literals at typing (as `record-project` does) would type-exactly-then-VALUE-STALL. Int tolerance lives where col-1 put it: `v[i]`/`expr-get` (runtime `index-value` handles Int). Pinned: acceptance ;;65.

**Route-sensitivity of the uniform-wrapper flows (col-4 finding)**: `pvec-from-list-fn '[1 "a"]` INLINE errors (the literal elaborates in CHECK mode against `(List ?A)` — each element vs the ONE meta, the C2 annotated-literal behavior), while the DEF-BOUND row α-degrades cleanly (`(PVec Int | String)` — the row's type meets `(List ?A)`, A := ⋃positions). Both pinned in `test-record-collections.rkt`; keyword `pvec-from-list` on a def-bound row-list is row-identity (exact).

**Canary inventory (col-4)**: acceptance ;;50–65 (tuple-op showcase incl. aliased-PVec + round-trip type-loss + static-miss/Int-index errors; 64/64, suite-gated); `tests/test-tuple-ops.rkt` (21, :no-prelude, exhaustive op pins); `test-record-collections.rkt` +5 prelude surfaces (from-list row-identity, from-list∘nth composition, wrapper α-degrade def-bound vs inline-error, `(List ⋃)` value-reduces through prelude `length` — the S10 stall is ROW-lists only).

**Perf (col close gate)**: `bench-ab --runs 10 benchmarks/comparative/` at col close (saved `data/benchmarks/f1a-col-close-2026-07-15.json`, commit `016251dd`) vs the f1a-core reference (`f1a-core-post-s3-2026-07-06.json`, commit `6ad07de8`): **every program faster, −0.6%…−13.6%** (adversarial tier −5%…−11%, CVs ≤1%; within-run A/B all noise as expected). NO regression from the col arc. Attribution caveat: the delta spans col-1…col-3 PLUS two test-infra commits (`c457f479` pnet-pregen hardening likely explains much of the ambient gain) and 9 days of machine drift — treat as "no regression", not as a col speedup claim. Note `bench-ab.rkt` has no saved-JSON compare flag and `--ref` is documented-but-unimplemented (workflow.md's `--ref HEAD~1` advice is stale) — comparison was scripted median-vs-median.

**Current coordinates (verified at col-4 close; they DRIFT — re-grep before trusting)**: pvec infer arms `typing-core.rkt:1923-2148` + from-list `:2289` + transient `:2308-2344`; check-mode arms `:2910-2942`; literal mint sites `elaborator.rkt:2458/:2472`; route chain `parse-reader.rkt:2528-2535` → `macros.rkt:4723-4731/:5774` → `parser.rkt:778-781`. The col-3-era citation "1919-2014" is stale.

## §12 F1a.2 mini-design — dyn tail live + Open deletion (D.1, 2026-07-15)

**Decisions**: D16–D20 (track doc §2a round 5, owner-locked 2026-07-15). **Grounding**: audit `wf_2b58a7e2` at `a3b38e97` (census + roles + carrier readiness + flows); options panel `wf_71c14484` (4 clusters propose/critique/synthesize). Coordinates below verified at `a3b38e97` — they DRIFT, re-grep.

### §12.1 Revised charter (what the audit changed)

FIVE Open producers (design §4.5 implied two): elaborator empty-`{}` seed (`:2126-2129`), elaborator mixed-key seed (`:2137-2140`), `record-value-union` empty corner (typing-core `:425-429`), map-assoc dynamic-key degrade (`:~1585`), map-dissoc dynamic-key degrade (`:~1763`). ~44 code sites / 11 files total (not 38). The `unify:579-580` wildcard is strictly COARSER than S–I C_Cons (unconditional, not row-scoped, absorbs metas WITHOUT solving — it sits BEFORE the flex-rigid arms). `subtype-predicate.rkt` has ZERO Open handling (the pure route never absorbed — fresh decision, §12.3). `(Map K V)` annotations do NOT mint rows (D16; key-domain fact). TWO LIVE QTT BUGS on these exact flows (`def m0 := {}` and heterogeneous-record dynamic-key map-get, both "Multiplicity violation"; homogeneous passes; suspected checkQ-side, upstream of typing-errors' generic reporter) — p0 owns the time-boxed localization; pre-declared default = fix-BEFORE if inconclusive; branch rule: "flow-local" (the failing computation dies with the deleted producers) → fix-WITH at p1b.

### §12.2 Disposition table (per-site replacement)

| Site | Today | F1a.2 mints/does | Decision |
|---|---|---|---|
| elaborator empty-`{}` seed | `(expr-map-empty ?km (expr-Open))` — key-domain OPEN (string assoc works) | `(expr-Record 'keyword '() 'dyn)` — keyword-committed ⟨\| _⟩; assoc chains grow exact rows; string keys = annotation escape hatch (pins BOTH) | **D17** (owner-signed semantics change) |
| elaborator mixed-key seed | `(expr-map-empty ?km (expr-Open))` | `(expr-Map ?km ⋃observed-values)` — stays Map (non-keyword keys by construction) | **D18** |
| `record-value-union` empty corner | `(expr-Open)` | fresh meta (Q6 map-vals-`{}` precedent); callers with a different empty policy still guard BEFORE calling | D19-adjacent |
| map-assoc dynamic-key degrade | `(expr-Map Keyword Open)` | dyn row **keeping known fields** `{known… \| _}` (inserted value's type not recorded — named cost, bounds-free tail) | D16 |
| map-dissoc dynamic-key degrade | `(expr-Map Keyword Open)` | **`{\| _}`** empty dyn row — a KNOWN field may have been removed; keeping fields is unsound under removal (presence marks = the F1b refinement) | D20 posture |
| dynamic update-in on a record | returns target type UNCHANGED (S9 never landed) | **`{\| _}`** (drop all facts — only sound option; never mint `(Map Keyword Open)`) | **D20** |
| unknown-field projection on `'dyn` | n/a (no dyn rows exist) | fresh unconstrained meta; NO recording (descoped, §12.5); record-project gets the tail dispatch (literal miss on `'dyn` ≠ error) | **D19** |
| `unify:579-580` wildcard | absorbs ANY type, both directions, pre-meta | dies at p2; replaced by C_Cons arms (§12.3) | D16 |
| check `typing-core:3053-3054` + checkQ `qtt:2256-2257` Open arms | absorb both directions | die at p2 (dyn rows ride normal paths) | |
| 9 Open-scrutinee map-op infer arms (`:1599-1813`) | dictionary-of-Open flows | dead code post-p1b (nothing types as Open); deleted at p2. Their SUBJECTS become dyn rows → the existing s2/s3 Record arms need **tail guards + per-op dyn dispositions** (§12.4) | |
| B1 chain `typing-core:2833-2845` | empty-Record seed skips value-unify; Open seed rides the wildcard | the empty-DYN-row seed is handled by the same empty-record branch (tail-blind — verify at p1b, T6); the Open leg dies | |
| pipeline identity sites (shift/subst/zonk×3/nf/pp/pnet reg0!/union-sort-key/definitely-not-map?) | carry Open through | deleted at p2 with the node | |

### §12.3 The row-comparison engine (D16)

ONE home for dyn-row comparison semantics; the five tail-blind α helpers (`record-subtypes-map?`/`-pvec?`, `record-<:-map?`/`-elem?`, `union-record-component-vt`) DELEGATE (drift argument: an extend-in-place `meta-V := ⋃known` over-commit in one helper silently un-does the kept-fields win, invisible until something mints `'dyn`). Semantics grid (C_Cons transcription, solve-first ordering):

- **dyn-row vs (Map K V)**: labels vs K per key-domain; per-known-field goals vs V (solve-first — field metas decompose and solve); the `'dyn` tail absorbs the remainder. Concrete-V: per-field checks. **Meta-V from a DYN row: REFUSED** (cannot honestly solve V := ⋃known when the remainder is unknown; the meta stays unsolved → D19/Q4 display posture; canary pinned; tighten at F1b). Meta-V from a CLOSED row: unchanged (solve V := ⋃fields — the existing α).
- **dyn vs closed (same key-domain)**: shared labels → per-field goals; expected-side known-but-missing label → fail; value-side extras vs the expected `'dyn` tail → absorb. Closed-expected + dyn-value → fail (can't verify absence). D11 intact: this is C_Cons absorption at unify (gradual consistency), NOT a record-subtype judgment; width-on-CLOSED stays F1b erasure-mode.
- **dyn vs dyn**: shared labels per-field; both tails absorb the symmetric difference.
- **dyn vs meta**: SOLVE-first (flex-rigid binds the meta to the row) — never absorb-without-solving (the `:579` wart, fixed). Canaries both directions.
- **'nat dyn rows**: NOT minted in F1a.2 (no producer); the engine guards key-domain explicitly (B3 discipline) and `closed-nat-row?`'s 23 exact-arm guards already exclude them.
- **Pure-subtype? route**: `record-subtypes-map?` gets the tail read (dyn → known-fields-vs-V + absorb; it never absorbed Open, so this is NEW, conservative semantics, not a relocation). `structural-subtype-ground?` recursion inherits via the same helper.
- Budget: if the engine needs more than a dispatch + the grid above (goes baroque), FALLBACK to extend-in-place with a per-helper canary matrix — trigger written here so invoking it is cheap and visible. Home: typing-core helper beside record-project (ctor-desc can't host: no prop:ctor-desc-tag on expr-Record, by design).

**✏ F1b.3 GRID AMENDMENT (2026-07-17, the ONE co-signed change — D21+D24; commits at the §13.6 row):**
(1) **D21 width**: `record-width-applicable?`/`record-width-discharge?` (typing-core, shared with the qtt fallback twin) — both-closed-keyword + label-containment pre-filter (`#:when`, so guard-failing pairs still reach the `subtype?` leg), then expected-tail `'closed→'dyn` relaxation delegated to the C_Cons arm under rollback (bare success path). Closure-erasure is KEYWORD-DOMAIN-scoped (an empty-closed keyword expectation accepts any closed keyword record; cross-domain pairs never fire). unify/classify untouched. (2) **D24 presence**: the dissoc-dynamic arm mints `record-mark-all-unknown` rows (labels+types kept, all-`'unknown`, dyn tail — supersedes the D20 drop-all); the update-in-dynamic arm mints labels + per-field fresh metas (`'dyn-row-update-in`) + `'present` + dyn tail, KEYWORD-gated ('nat targets keep the empty-dyn degrade), guarded by the reduction-layer ZERO-SEGMENT runtime error (both arms; the path-ops host-error precedent); C_Cons containment REQUIRES only presence≠`'unknown` labels (both symmetric conjuncts — acceptance-preserving); the shared-label zip still emits type goals for `'unknown` labels (type-if-present is a fact — co-signed); B3 stays presence-BLIND (pin text amended in unify.rkt); Q7 gated-identically at THREE hit sites (record-project, union-record-component-vt, nil-safe-get); union mechanics = presence sort-key tiebreak (repairs the S1 determinism invariant) + `presence-absorb-adjacent` ({P}→{P,A} same-type absorption); display `{:a? Int | _}` (the `?`-suffix/predicate-name ambiguity accepted, noted at the struct site); the points-map/joins declared at syntax.rkt's record-field. (3) **qtt divergence repairs** (audit C4 refutation): the dissoc + update-in twins now DELEGATE to infer (they previously returned subject/target types unchanged — the dissoc one made def-bound dissoc results fail QTT with a miscategorized Multiplicity violation, fixed). (4) The width/update-in acceptance canaries live in `examples/2026-07-17-ciu-t6-f1b3-width.prologos` (own suite gate): appending polymorphic apps or lambda defs to the MAIN file's accumulated state triggers the PRE-EXISTING union-state BSP hang (DEFERRED.md § union-type hang, worktree-verified; the main file keeps the ;;79 flip + wdd canaries).

### §12.4 The per-op dyn disposition table (p1a SPEC — grounded by audit `wf_2687a16e` at `98a147aa`)

**Architecture corrections from the p1a audit** (supersede two §12.3 phrasings): (1) **the engine splits by the module topology** — `subtype-predicate.rkt` deliberately requires neither typing-core nor unify (its extraction rationale), and unify requires IT; so the PURE fragment (tail-aware `record-subtypes-map?`/`-pvec?`) lives in subtype-predicate and the ctx/meta adapters (`record-<:-map?`/`-elem?`, `union-record-component-vt`, `record-project`, `record-value-bound`) live in typing-core. There is no single-function engine; there is ONE semantics (this table) realized at two layers. (2) **classify's `'sub` goals are pure unify-equality pairs, no rollback, no subtype kind** — so row-vs-Map C_Cons semantics ride the EXISTING pure α arms (`:588-592`) becoming tail-aware in place; only **row-vs-row** gets a new goal-emitting arm (B3-adjacent). "Solve-first vs metas" is STRUCTURAL: every record arm is both-sides-concrete, so bare metas hit flex-rigid (`:618`) and applied metas hit flex-app — no arm placement after flex-rigid needed.

**The one new value-bound notion**: `record-value-bound ctx rec` = closed → `⋃fields` (record-value-union, today); **dyn → `⋃fields ∪ fresh-meta`** — the remainder's values must be absorbed by any consumer of "all values". Fresh-meta-per-call is the accepted D19 posture (no reconciliation); speculative branches roll metas back via the existing save/restore.

| Surface | closed (today, unchanged) | 'dyn disposition |
|---|---|---|
| `record-subtypes-map?` (pure) | labels-vs-K + every field <: V | SAME on knowns; remainder absorbed (C_ConsL at the pure layer — deliberate: the `:588-592` classify arms inherit this) |
| `record-subtypes-pvec?`/`-elem?` meta-elem leg | solve elem := ⋃fields | **REFUSE** (meta stays unsolved) when tail ≠ 'closed |
| `record-<:-map?` meta-V leg (`:490-491`) | solve V := ⋃fields | **REFUSE** when tail ≠ 'closed; concrete-V leg unchanged (knowns-only IS the absorption) |
| unify row-vs-row (NEW arm, B3-adjacent) | B3: equal tails + labels → per-field 'sub | ≥1 dyn tail + same key-domain: shared labels → 'sub goals (metas solve); a CLOSED side must CONTAIN every dyn-known label (it asserts total knowledge); dyn tails absorb extras. dyn-dyn-same-labels already rides B3 (tail eq). No-rollback on partial goal failure = the pre-existing B3 posture |
| `record-project` keyword/nat literal MISS | expr-error (+S7) | **fresh meta** (D19) |
| `record-project` dynamic-key leg | ⋃fields (empty → error) | ⋃knowns ∪ fresh; **empty dyn → fresh alone** (the remainder may hold it) |
| `nil-safe-get` inline literal leg (`:1714-1716`) | miss → Nil | miss → `<fresh \| Nil>` |
| `union-record-component-vt` literal leg (`:466-468`) | miss → #f (Q5 filter) | miss → **fresh meta** (the component may hold the field in its remainder — filtering would silently drop it) |
| `union-record-component-vt` dynamic leg | ⋃fields | ⋃knowns ∪ fresh |
| map-assoc/map-dissoc LITERAL-key arms | record-extend / record-remove | UNCHANGED — both preserve the tail (extend on dyn = grow the known set; remove a known = sound, remainder untouched) |
| map-size / map-has-key / map-keys | Nat / Bool / (List Keyword) | UNCHANGED — sound as TYPES regardless of remainder |
| map-vals (`:1809`) | (List ⋃fields) | (List **bound**) |
| fold-entries / filter-entries / map-map-vals (infer `:2199/:2219/:2234` + check `:2968/:2988`) | f consumes ⋃fields; filter → (Map Keyword ⋃); map-vals → label-preserving rebuild | f consumes **bound**; filter → (Map Keyword bound); map-vals rebuild UNCHANGED (knowns := W, tail preserved — remainder stays unknown, sound) |
| qtt mirrors (fold `:1631` / filter `:1655` / map-vals `:1677` + checkQ `:2527/:2554/:2581`) | ⋃fields | **bound** (same helper; qtt has ctx) |
| qtt delegating arms + checkQ fallback α | delegate / record-<:-* | inherit the typing-core dispositions free |
| pvec EXACT ops (23 `closed-nat-row?` guards) | exact | fall through to error arms — verified fail-safe, no change |
| pvec-nth (`:1942`, unguarded delegate) | record-project | inherits the hook |
| B1 empty-seed arm (`:2837`) | skips value-unify for ANY empty record | tail-blind = correct for the empty dyn seed too (T6 resolved: subsumed) |
| S7 hint (`typing-errors:150-152`) | closed-row-miss message | **tail guard added** — a dyn-row miss is legal (D19), the hint must not fire |
| union-sort-key / pnet / pp | tail in sort key; symbol round-trips; `{… \| _}` display shipped | verified — no p1a work |

**Exports added for testability**: `record-project`, `union-record-component-vt`, `record-value-bound` (export-surface change → targeted-runner scoped precompile discipline).

### §12.5 Descopes + F-row pins (W1 honesty)

F1a.2 ships ZERO on-network computation — recorded deliberately (typing goes on-network at PPN; rows-as-cells at F-row). The charter's "record the observation" is DESCOPED (D19, owner-signed). **F-row inherits, pinned here**: (1) the per-command observation cell — compound cell keyed `(row-id × label)`, hash-union merge, collision-unify in the propagator, keyspace = the F-row cell's component keyspace so retirement of the descope is mechanical; (2) the ρ row-meta sketch (row-KINDED meta in the existing type universe, PureScript `Row k` precedent per §3b); (3) the bounded-tail Galois end-state (`'closed ⊐ (dyn K V) ⊐ (dyn K ?)`) for the Q_E(b) carrier change; (4) the tail-blind-walker audit checklist (salvaged from the rejected bounds-now option). Q4 tighten-later (unsolved projection metas display as metas; no zonk-final defaulting) → DEFERRED.md.

### §12.6 Phase ladder + gates

| Phase | Content | Gate |
|---|---|---|
| p0 | Doc batch (this section + D16-D20 + amendments, ONE commit) · time-boxed QTT-bug localization (instrument checkQ on the two repros; default fix-before if inconclusive; flow-local → fix-with) · characterization pins (paired expected-fail + passing-sibling probes) · acceptance pins of today's WORKING flows (annotated-empty def, string-keyed assoc onto `{}` [the D17 flip target], homogeneous dynamic-key get) | suite green; pins documented |
| p1a | Consumers-first, DEAD-CODE-SAFE: the engine + §12.4 table + 5 helpers delegated (one per commit) + C_Cons arms (solve-first) + record-project dyn hook + synthetic dyn-row unit tests + **NEGATIVE differentiating probes** (bound-rejection cases the `:579` wildcard structurally cannot fake — it only returns ok) | suite green throughout; zero mints; if a session ends here the tracker says "validated (NOT deployed)" |
| p1b | THE atomic mint-flip (smallest possible diff): all five producers flip per §12.2 + **PNET_VERSION 1→2 in the SAME commit** (all caches regenerate Open-free; the wildcard + 9 arms become dead code; gated on the p0 grep that only the five producers can emit the Open tag into a fresh cache) + acceptance canaries ;;35/;;37 flip + D17 pins flip + expected-fail pins resolve | suite green; acceptance green |
| p2 | Mechanical deletion: ~44 Open sites, the 9 scrutinee arms, the `:579-580` wildcard, check/checkQ arms, pnet `reg0!` dereg, `test-mixed-map.rkt` rewrite (hand-builds `(expr-Open)`), the two-role docstring history (S–I ★ + polarized `&{}` negative-top) at the deletion commit | `grep expr-Open` = 0 production hits |
| p3 | Full suite + acceptance + probe-diff + bench sanity + close notes (§12.7) + tracker/dailies | the col-close gate recipe |


### §12.7 F1a.2 close notes (p0–p3, 2026-07-15/16) — D1-b CLOSED

**Commits**: p0 `767bbcae`+`803a57e4` · p1a `bec0c058` · p1b `07c0e70f`+`6a2bc9d5` · p2 `e4fad7d7` · p3 `4892fda3`. Final: suite **8682/455/0**; acceptance **78/78**; `grep expr-Open` = 0 code hits (tombstone + historical comments only). **`expr-Open` no longer exists; the dyn tail carries both of its formalized roles** (the two-role history at the syntax.rkt tombstone: S–I's ★ dyn row tail → the row-scoped C_Cons arms + knowns-only pure α; the polarized `&{}` negative top → the empty dyn row `{| _}` that bare `{}` seeds).

**What the phase delivered beyond the charter**: the two p0 QTT fixes (inferQ expr-meta arm; the union whole-type conversion — a THREE-checker bug class invisible to the suite because union results were never def'd); `expr-map-literal` (D18's ⋃observed needed the D15 literal-extent recipe — the assoc-chain seed structurally cannot carry an observed union); the empty-closed-row Q6 corner (fresh meta via `record-value-bound`; RVU is non-empty-by-contract now).

**Two probe catches at the point of no return** (both would have been muddy suite failures): the empty seed's K slot must stay a META — a concrete `(expr-Keyword)` broke the D17 annotation escape hatch through the tail-blind B1 arm; and qtt's map-empty must DELEGATE the type when the v-slot is a Record (checkQ-top compares against the ROW, and the pure α key-gate rightly refuses `(Map ?km row)`).

**The p3 bench incident (recorded for the next close)**: the close bench showed +9–19% vs the col-close reference — DIAGNOSED as ambient, not code: a pinned worktree at `016251dd` benched the OLD compiler in the same window (+6.3% vs its own then-numbers); phase timings identical; cache sizes identical; **interleaved ABAB wall runs showed parity with the new compiler marginally faster**. The machine had degraded across a 15+-hour session (bench CVs 3–9% vs ≤1% at col close; the suite wall itself recovered 146→130s as load wound down). *Lessons*: (1) sequential bench invocations are NOT an A/B under long-session ambient — interleave or use a pinned worktree; (2) the worktree-for-perf-A/B pattern is the safe form of `bench-ab --ref` when owner WIP occupies the main tree; (3) the saved close benches (`f1a2-close*`) carry this caveat — re-bench fresh before any cross-session comparison. The union-arm refit found during diagnosis stayed on principle (bare right branch restored; whole-union conversion on the both-fail path only — zero hot-path cost).

**Debt carried (named)**: unsolved projection metas display as metas (Q4/D19 — DEFERRED.md, tighten at F1b); the engine's meta-V-refusal-from-dyn is deliberately conservative (same tightening pass); dissoc-dynamic drops all facts pending presence marks (F1b); the mixed-key literal's chain seeds a fresh value meta that zonk-final leaves unsolved if the literal is never used (harmless, displays only under `infer` of the raw chain); heterogeneous key-domains = the owner-flagged open design space (DEFERRED.md).

**F-row pins carried forward** (§12.5): the per-command observation cell keyed `(row-id × label)`; the ρ row-meta sketch; the bounded-tail Galois end-state; the tail-blind-walker audit checklist.

## §13 F1b mini-design (Stage-3, 2026-07-16 — rulings land here as they close)

**Provenance**: grounding `wf_0f364e4e` ([GROUNDING](2026-07-16_CIU_T6_F1B_STAGE3_GROUNDING.md)) → options panel `wf_b47e23d4` ([OPTIONS](2026-07-16_CIU_T6_F1B_STAGE3_OPTIONS.md)) → F1b.0 probe bundle `wf_7d2900e9` ([PROBES](2026-07-16_CIU_T6_F1B_STAGE3_PROBES.md)) → owner co-design (track doc §2a round 6 = the D-number record; this section = the mechanics). The composed architecture (panel synthesis §6.1): **the F1a row-comparison engine stays the sole comparison authority** — width extends it check-side; presence instruments the same arms; the seal delegates via one schema→row projection through the existing αs; tightening polices the emitted metas; solve-shape purifies the reification boundary.

### §13.1 W — erasure-mode width discharge (D21 ✅ LOCKED 2026-07-16)

Ruling text = track doc §2a D21. Implementation shape:

- **Attachment**: ONE arm in check's conversion fallback (typing-core, before the final `subtype?` leg — coordinates drift, re-grep `record-<:-map?` consumers) + the qtt checkQ-fallback mirror; both call ONE shared predicate (caps the issue-#76 mirror-drift class).
- **Mechanism**: actual = closed keyword row, expected = closed keyword row → relax expected's tail `'closed→'dyn` on a copy → delegate to existing unify/C_Cons under rollback (`union-check-left` precedent; bare success path per the p3 refit lesson). Extras absorb (the erasure); shared labels = `'sub` goals (unify; metas solve = equality-depth).
- **Pins**: failure rolls back metas; success-path field-meta solves = accepted residue. Expected-side closure is ERASED (empty-closed-expected accepts any record; exactly-empty force must not route through width). unify/classify untouched → D15 homogeneity probes structurally safe.
- **Deferred (named carve-out, non-coverage honest)**: covariant/label-keyed depth — triggers (a) rows become user-writable (P8's three direct-unify surfaces go live), (b) supertype-typed fields (generic `Num`, unions) in expected-row positions (owner, 2026-07-16). Upgrade = local realization swap at the same arm (per-field `unify ∨ subtype?` helper); can NEVER ride C_Cons (`'sub` goals are pure unify pairs). Also non-covered by design: unify-internal row events (P8: population zero today; D15 semantics depend on the failure) + nested-container width (P2: zero traffic; dies in unify `'(conv)` / atomic-merge).
- **Boundary invariant (binds §13.2)**: width discharge = the structural/label question ONLY; per-field value-type satisfaction at rich boundaries (seal, annotations) stays at CHECK-strength — the seal must NOT be realized as row-vs-row width discharge (else `:x Num` wrongly rejects `{:x Int}`).
- **Pin amendments in the landing commit**: syntax.rkt `expr-Record` pin gains a dated note (strict reading CONFIRMED + the D15-corruption argument); the unify B3/C_Cons comments gain a pointer to the discharge arm (the one sanctioned tail-relaxation caller).

### §13.2 S — the Map→schema seal + up-shift (D22 ✅ LOCKED 2026-07-16)

Ruling text = track doc §2a D22. Implementation shape:

| Route | Today | Under D22 |
|---|---|---|
| `[Person {…}]` literal constructor | preparse rewrite: defaults + panic-wrapped checks | SAME door, seal discharge: static seal (labels/types/fill/residual/closedness) + the repaired `:check` bridge |
| `[Person m]` non-literal constructor | inference error (opaque fvar applied) | seal application (the rewrite generalizes to any `[SchemaName e]` head) |
| `def x : Person := e` | per-field check; missing fields SILENTLY accepted (the live gap) | same discharge at the def boundary: fill-or-error (own gated commit, census cited) |
| `the Person e` mid-expression | per-field check; NO defaults, NO checks | same discharge at the `the` boundary |
| runtime | — | `validate` face = F1b.5 (Result-returning tabulation; witness-strength policy decided there) |

- **Static discharge**: per-field CHECK-strength (D21 invariant); residual scan ONCE at the seal boundary (fill → missing-required prologos-error w/ srcloc → closedness iff `:closed`). Selections exempt via delegation + the subset-check obligation.
- **`:check` bridge repairs (this slice)**: `expr-panic` → `definitely-not-map?` exemption list; seal-scoped def-forcing (nf at def commit when the body is a seal application; top-node panic → error, mirroring the top-level eval arm). Bridge = NAMED SCAFFOLDING, retirement decision at F1b.5.
- **Tombstone slice**: delete `expr-schema`/`expr-schema-type` (syntax.rkt:919-920) + `surf-schema` + the ~6 pipeline identity arms; history docstring at the site. No producers → pure mechanical delete, no flip needed.
- **Up-shift**: schema→row projection (schema-entry fields → closed keyword row) feeding the existing `record-subtypes-map?`/`record-<:-map?` αs; also gives the record-vs-schema comparison route (`the Person m` on a def-bound map) its arm.
- **Champ-arm retirement**: typing-core:2946-2948 + qtt twins → loud refusal naming validate (closes the blanket-accept hole).
- QTT mirrors: every new/changed check arm lands with its checkQ twin in the same commit (shared predicates where possible).

**✏ F1b.4 CLOSE NOTES (2026-07-17; commits at the §13.6 row; D22 amendments = track doc §2a ✏).** What landed vs this spec: **4-pre** made the F1b.2a scan UNIVERSAL (`infer-on-network/full` returns the scan net + cell id — the mini-audit's two-context soundness headline: wrong-typed sealed literals passed silently in process-string contexts). **4a** = `schema->row` (one-level) + `record-<:-schema?` (per-field CHECK-strength, closed? consultation) + five fallback arms both checkers + `field-type-satisfies?` (closed the PRE-EXISTING union-V α gap) + champ arms retired loud + the SELECTION VIEW correction (test-oracle: extra parent fields accepted). **4b** = the constructor generalization, literal conjunct dropped; **M2's runtime-conditional fill probe-REFUTED** (type-directed; preparse type-free) → non-literal seals TYPE-ONLY, fill/checks at validate. **4c** = `expr-panic` → `definitely-not-map?` exemption (one edit kills both swallow arms) + `seal-application-body?` shape-gated def-forcing at both commits (the P4 matrix flipped; laziness outside seals pinned). **4d** = the tombstone (12 files; the shared-line `expr-logic-var` collateral caught + restored; zero residual hits). **4e** = `check-seal-chain` (the boundary walk; ONE residual with union knowledge) + `schema-seal-residual-ok?` replacing the map-empty blanket-#t (co-landed = THE FLIP) + preparse fill on the annotation-literal routes (`($angle-type S)` discovered by instrumentation) + named-field errors on BOTH error routes (infer/err hint + check/err seal-msg) + the SECOND selection test-oracle correction (`:requires` = read-capability → **closedness-only selection residual**). **4f** = the clean-state seal acceptance file (29/29 first-run) + gate. Suite arc: 8733→**8790/459/0**. Debt named: nested-in-container seals (top-node class) + non-literal fill/checks + partial-view runtime misses + the `:check` bridge retirement = **validate (F1b.5)**; the bench matrix = F1b.close.

### §13.3 T — Q4 tightening (D23 ✅ LOCKED 2026-07-16)

Ruling text = track doc §2a D23. Implementation shape: **groundwork slice** (posture-free) = structured `meta-source-info` at the four D19 mint sites (record-project literal-miss + dynamic-key, record-value-bound empty-closed, union-record-component-vt; tags replace the bare strings, loc=#f interim) · deepened meta-walk (unsolved-metas-to-holes + type-contains-meta? gain expr-union/expr-Record recursion — consider deriving both from one generic expr meta-fold) · ALL stored-type producer sites routed through the guard (def `:1598`, defr `:735`, annotated `:1782`, foreign `:3062`, data `:1664` — coordinates drift, re-grep; `:1704` deferral store explicitly EXEMPT, it is transient) · DEFERRED.md rejected-with-reason entries. **Posture slice** (AFTER §13.4 presence activation; re-trigger pinned in DEFERRED.md) = `check-unresolved-projection-metas` filter over the unsolved-metas path, consulted in the existing driver error block at the store boundaries; hard error w/ def-srcloc; qtt untouched (the guard is a driver-boundary concern, not a checker arm).

### §13.4 P — presence-marks activation (D24 ✅ LOCKED 2026-07-16)

Ruling text = track doc §2a D24. Implementation shape: **writes** = the dissoc-dynamic arm (labels+types kept, all-`'unknown`, dyn tail) — the ONLY non-`'present` writer this slice; the update-in arm ships its P6 posture (labels + fresh metas + `'present` + dyn tail) with the zero-segment runtime-error closure. **Reads** = the C_Cons containment guards (acceptance-preserving: `'unknown` labels non-required on the expected side), the S7/typing-errors miss diagnostics (provenance naming), presence-aware union absorption + sort-key tiebreak, pp-expr `{:a? Int | _}` display. **Non-reads pinned**: projection (gated-identically — fresh meta, D23 applies), qtt (fields stay `mw`, D8). Lands as ONE grid-amendment commit with D21's discharge arm; unify pin text + struct-site lattice spec (points-map/joins) in the same commit. No serializer/PNET change (interned symbols ride free — P audit).

### §13.5 Q5 solve-shape (D25 ✅ LOCKED 2026-07-16)

Ruling text = track doc §2a D25. Implementation shape: **the echo-deletion slice is F1b's FIRST GREEN** (decision-light, blast radius verified closed by the panel's independent sweep) — the six reduction.rkt merge surfaces + `test-bound-args-01.rkt` inversion (keys-ABSENT gate) + 4 example touch-ups + the explain reserved-key interim guard + solve-one → bare-map-or-`none` (verify current shape first), all in one gated commit window. DEFERRED.md entries in the same commit: the typed-solution-rows mini-track charter home (entry gates per D25.3) + the explain-restructure home (wrapper-record shape, inherits the clobber pin). Runs while later slices are still in design — proves the track's commit cadence at zero design risk.

Queue + collapsed state per the OPTIONS doc §0 ✏ note: T = escape-boundary posture (P7: `:1704` transient; raw-meta leak confirmed route-sensitive) + severity + flip-after-presence; P = dissoc-only `'unknown` (P6: the update-in arm needs NO presence machinery given zero-segment-path closure + dyn tail) + gradient (Q7) + mechanics (Q9); Q5 = echo deletion (first green) + clobber guard + typed-rows mini-track charter. By-catch defects pending placement (Q11): the on-network annotation-adoption gap; the inline-cons width-tolerance audit; (the panic-swallow class is now ADDRESSED by D22's bridge repairs; the update-in value-loss bug is chipped `task_92fc799d`).

### §13.6 The F1b ladder (Stage-3 CLOSED 2026-07-17; D21–D26 all owner-locked)

Every slice per-change-gated (check-parens → raco make → PROBE-FIRST → targeted → full suite → commit) + extends the F1 acceptance file incrementally (`examples/2026-07-06-ciu-t6-f1-records.prologos`, new F1b sections per slice). Validate = F1b.5 per the D22 pin.

| Slice | Content | D | Gate / liveness proof |
|---|---|---|---|
| F1b.1 **FIRST GREEN** ✅ `83784ef9` | Echo deletion (six reduction.rkt merge surfaces) · `test-bound-args-01` INVERTED to the solution-shape gate (11 cases incl. 2 NEW explain cases + the clobber-guard probe) · explain reserved-key guard (**3 keys**: certainty/cycle/provenance — provenance is the live one; binding-wins-on-collision) · solve-one → bare-map-or-`none` (**verified shape: was Option-wrapped**, so the change = some-unwrap ×4 arms) · dead legacy explain arms (wf-explained-answer/answer-record, zero producers) DELETED · acceptance ;;80–84 (82/82) · suite 8694/455/0. Mini-audit `wf_b4faf5ef`. Descoped WITH the audit's blessing: the 4 example-file comment touch-ups (comment-only, not gated; 2 owner-WIP-entangled — doc-truth sweep deferred to F1b.close) | D25 | ✅ solutions display `'[{:y "bob"}…]`; keys-absent gate green; suite green |
| F1b.2 **SOUNDNESS GROUNDWORK** ✅ `3153319b`+`1801d377` | Mini-audit `wf_8390b549` REWROTE the fix shape: the term position does NOT stay ⊥ (the ann rule's downward write fills it — the post-quiescence term-⊥ read was unrealizable as pinned); the cons case reaches through the APP rule (no ann node) so only a **post-quiescence untyped-interior SCAN** covers the class — landed as the 5th refusal check in `infer-on-network/err` (roster-free generic walk over the binder-free spine; expr-meta excluded; scheduler-independent; over-approximation SAFE → imperative re-route). One choke point = all four driver gates + REPL/LSP. Also closed: the no-net-box bare-symbol trap; the inline-cons tolerance (SAME adoption class — P2's elaborator suspicion refuted); the QTT-backstop skip-hole (`def y := (the Int (match …))` PERSISTED unsound types — critic find, probe-confirmed). (b) D23 groundwork: `dyn-row-source` structured provenance (NINE tags / TWELVE sites — the audit's funnel correction, not 4); **generic reflection-based deep walkers** in zonk.rkt (transformer + 2 detectors — zero per-node arms, S2-drift-immune); blanket deep scrub at all SIX store sites with the `has-holes?` gate REMOVED (it stored implicit metas RAW — a leak, not a protection); the recursion pre-register untouched; `holes-to-metas` deliberately shallow (documented). `test-route-soundness-01.rkt` (11 cases); acceptance ;;85–88 (**86/86**); suites 8706/456/0 + 8704/456/0. Watch: the run-ns-ws-last fixture×speculation crash; one NON-reproducing transient mass-stall (2nd data point of the F1a-col-3 infra class) | D26 + D23g | ✅ the P1 probe matrix FLIPPED (all five shapes error); the P7 leak stores holes; suite green ×2 |
| F1b.3 **THE GRID AMENDMENT** ✅ `352fc28b` | Landed as designed + the audit's widenings: D21 discharge (`#:when` static guards + rollback delegation; keyword-scoped closure-erasure) at check fallback + qtt twin via shared predicates; D24 dissoc-`'unknown` retention + KEYWORD-gated update-in fresh-meta posture + zero-segment runtime guard (both reduction arms) + acceptance-preserving C_Cons relaxation (both conjuncts) + Q7 gates at THREE hit sites + union tiebreak/absorption (repairs the stated S1 invariant) + `{:a? T | _}` display + struct-site lattice spec + pin-text amendments; the qtt dissoc/update-in divergence REPAIRS (audit C4 — fixed a live miscategorized-Multiplicity bug on def-bound dissoc). **Canary split** (worktree-verified pre-existing union-state hang — DEFERRED widened): width+update-in canaries = the NEW clean-state `2026-07-17-ciu-t6-f1b3-width.prologos` (6/6, own gate); main file = ;;79 flip + wdd canaries (89/89). test-dyn-rows → 31 | D21+D24 | ✅ envelope: 86 pre-existing markers byte-identical; width flips + negatives verified; suite 8733/457/0 |
| F1b.4 **THE SEAL** ✅ `eaf115d6`→`1a9cd0de` | Landed as spec'd + three ✏ amendments (track doc §2a): **4-pre** `eaf115d6` scan universal (two-context gap closed) · **4a** `0e6901ef` up-shift + discharge + union-V gap + champ retirement + selection-VIEW correction · **4b** `dd37efd6` constructor generalization (M2 fill probe-REFUTED → type-only; validate inherits) · **4c** `7269dfb9` panic exemption + seal-scoped def-forcing (P4 flipped) · **4d** `f77e646d` tombstone (12 files, 0 residual) · **4e** `4082ec70` THE FLIP (chain-walk residual + map-empty retirement co-land; annotation-route fill; named-field errors; selections = closedness-only residual) · **4f** `1a9cd0de` seal acceptance 29/29 + gate | D22 | ✅ P4/P5 matrices FLIPPED; `probe-ctx` two-context canary green; suite 8733→**8790/459/0** |
| F1b.5 **VALIDATE** 🔄 | **Mini-design ✅ 2026-07-17** (grounding `wf_1ec0797b` → adversarial panel `wf_49cd3f40` → probes → owner rulings = track doc §2a round 7 **D27–D29**): `expr-validate` node (plan-as-data; ONE rule `Result S E`, ONE arm), E = `Result S (Map Path Reason)` (nominal closed Reason; collect-all = API), polarity-tabled skip witness (skip-set-empty test = the executable re-trigger), FULL DELEGATE (bridge deleted s3; doors bake the NODE — one engine). Partition p0→s4 + probe list = **§13.8** | D27–D29 | §13.8 per-slice gates |
| F1b.6 **POSTURE FLIP** | D23: escape-boundary HARD ERROR for D19-tagged undischarged metas at the store boundaries (def/defr/annotated/foreign/data) — strictly after F1b.3 (presence changes what "undischarged" means, D24 gradient) | D23 | the P7 def-leak probe flips to error w/ def-srcloc; exploration display unchanged |
| F1b.close | Bench matrix (D21+D22+D24 fast paths, interleaved/worktree-pinned per the §12.7 lesson) · PIR · typed-solution-rows mini-track charter (D25.3 gates) · consolidated DEFERRED triage · roadmap/CIU-master refresh | — | the col-close gate recipe |

### §13.7 Consolidated scaffolding ledger (the cross-cluster watchout, discharged)

F1b adds ZERO propagators/cells (honest per-cluster Network Reality Checks; the F1a §6 posture). The imperative mechanisms + their on-network retirement mappings, in ONE place:

| Mechanism (off-network, imperative) | Born | Retirement mapping |
|---|---|---|
| Width-discharge predicate (check fallback + qtt mirror) | F1b.3 | PPN-era: a typing-network propagator fire-fn; stateless/effect-free by construction → ports trivially |
| Presence-consulting guards (C_Cons arms) | F1b.3 | F-row: presence facts as observation-cell components (§12.5 pin 1 keyspace), hash-union merge |
| Seal residual discharge (elaboration boundary) | F1b.4 | The one-solver era (rows+traits, §3b Toohey): residual = constraint entries; closedness scan stays seal-time (CUE lesson) |
| `:check` panic bridge (preparse desugar) | pre-F1a (repaired F1b.4) | **RETIRED ✅ F1b.5-s3b `11660e92`** (D29 FULL DELEGATE): `wrap-schema-checks` + its sole call site DELETED, replaced by `wrap-seal-validate` (the door delegates :check to validate∘expect-valid); `subst-underscore`/`normalize-check-pred` cashed out to the s2b plan bake (elaborator + test consumers keep the provides) |
| validate runtime tabulation (node + arm + witness + renderer + abort face — the COMPOSED off-network mass) — **✅ ALL FIVE LANDED (stamped F1b.5-s4):** node + arm (s2 `ab468799`), witness (s1b `45337221` / field-witness.rkt), renderer (s1c `fb751643` errors-to-list/render-failures), abort face (s3a `d9986bb1` expect-valid). Selection-validate (s4 `0f95d544`) RIDES this row — zero new mechanism (same node/arm, a 7th plan slot). | F1b.5 | OWN horizon (D27.7): the on-network RUNTIME era — broadcast-over-plan-items into a Map-E request-accumulator cell (the hash-union E join IS the merge); NOT the punify/PPN typing rows. Port claim holds ONLY while collect-all + order-free E survive. NRC verbatim: zero propagators / zero cell-writes / no trace |
| D23 store-boundary sweep (imperative filter) | F1b.2/6 | Obligation/readiness cells when the constraint machinery goes on-network (PPN); mirrors check-unresolved-trait-constraints, which migrates in the same era |
| Sequential per-field discharge (andmap, no broadcast) | F1b.3/4 | The mantra waiver, named: per-field parallel path = punify deployment / PPN goal-lists (punify verified production-OFF — no mantra credit until it deploys) |

### §13.8 V — the validate face (D27–D29 ✅ LOCKED 2026-07-17; the D22.4 pins discharged)

Ruling text = track doc §2a round 7 (D27 node+E+scope · D28 witness/polarity · D29 full delegate). Evidence: grounding `wf_1ec0797b` (8 facets @ `6f80956b`, R-lens-verified) → adversarial options panel `wf_49cd3f40` (5 clusters, propose+critique+synthesis vs DESIGN_PRINCIPLES.org; A1 / row-leaf-E / C1 / C-gen / D1 / D3 / D4 all KILLED on principle) → main-session probes (field-type expressibility: angle-sentinel mangling + bare-head qualification — scratch `f1b5-tier3*/tier2b` probes, 2026-07-17).

**The sub-phase partition (each per-change-gated; s2 = first green):**

| Slice | Content | Gate |
|---|---|---|
| **p0** PINS+PROBES | The probe list below; acceptance-file skeleton (clean-state file per the hang discipline); the M2-boundary doc paragraph (runtime VALUE fill in the arm ≠ the refuted preparse TYPE-conditional fill — different mechanism, no window) | probes answered; attachment + E-key pins resolved |
| **s1** E + WITNESS SUBSTRATE ✅ | `data Reason` module (4 variants, D27.3); the polarity table + three-valued witness (subsumption-aware, whnf-first; extend `value-matches-type?` vs consume static predicates — per the network-free probe) TEST-LOCKED; the **skip-set-empty test**; `errors->list` renderer (golden tests snapshot renderer output ONLY — champ prints are unsorted trie order); **the two field-type repairs** (angle-sentinel processing at `parse-field-properties`; head qualification at `schema-field-type->expr`) | witness table green; the tier-2/tier-3 construction probes FLIP |

**✏ s1 CLOSE (2026-07-17 — four commits; mini-audit `wf_194002cf`; full suite GREEN 8825/462/0):** landed as spec'd + FOUR empirically-forced widenings. **s1a `7450c35f` — the field-type CLASS repair** (the audit widened "two repairs" → a two-list-drift class: `builtin-type-names` LACKED Float32/64; `schema-field-type->expr`'s case table LACKED Quire/List/Map/Option/… AND had no union/arrow conversion arm). macros `normalize-field-type-datum` (`<A|B>`/`<A->B>` reader sentinels → canonical `$union`/`$arrow` at registration; sigma refuses loud) + qualify-guard (`$`/`->`) + Float32/64 into builtins; typing-core `schema-field-type->expr` gains guarded name-resolution (import-map FIRST — the bare-container-head unification fix, the `list-type-fvar` recipe generalized), `$union`→right-nested `expr-union`, `$arrow`→`expr-Pi 'mw`, Quire arms. **s1b `45337221` — the witness substrate**: the type-lattice→reduction cycle forced the split — `field-type->witness-tag` (typing-core, CONSUMES `subtype?` → the acceptance set IS the subtype closure; the D28 err-polarity invariant) emits PLAIN-DATA tags `(prim …)/(ctor …)/any/(union …)` (no pnet surface); `field-witness.rkt` (NEW, below reduction) interprets them via struct predicates + ctor-meta. Polarity table + skip-set discipline TEST-LOCKED (12 cases). **s1c `fb751643` — Reason + renderer + Keyword shim** (owner Q1 STRING payloads; Q2 the shim NOW): `errors->list` renamed **`errors-to-list`** (the original is NOT WS-lexable — `<`/`>` reserved); realized IN-LANGUAGE (reuses `sort-on`+`map-to-entry-list`+`entry-key`, an annotated key-lambda pins the generic inference); `keyword-ops.rkt` IR-unwrap shims (`kw-name`/`kw-lte`) fill the stdlib's missing Keyword Ord/->string; prelude wiring = TWO-file edit (namespace.rkt + PRELUDE manifest, gen-prelude-consistent). **s1d `0b6e9d07` — the hygiene rider**: the p0 by-catch paid as a CLASS — five registry params (schema/selection/session/strategy/process) join the macros snapshot vector (19→24; batch-worker isolation) + test-support. Deferred within s1 (noted): the sub-schema-field qualification asymmetry (mitigated by `lookup-schema-by-name` short-name fallback) → the walker charter; the deprecated run-ns-* capture-tuple surgery. Witness realization NOTE for s2: the tag is plain data, so the s2 node payload carries it with zero pnet registration; the reduction arm requires `field-witness.rkt` (the s2 wiring).
| **s2** THE NODE (**first green**) ✅ | `expr-validate` through the full internal-node pipeline (~8 files; `pnet-serialize` regN! SAME commit — the vector-impostor rule); ONE typing rule `Result S E`; ONE reduction arm (presence → fill → witness → :check → Map-accumulate); explicit face over schemas + runtime champs; `_`-subject per the attachment pin | freestanding `[validate Person champ]` → ok/err on flat schemas — **the DEMO beat**; both retired-loud pointer sets land TRUE; ZERO door changes |

**✏ s2 CLOSE (2026-07-17 — `ab468799` engine + `69cafdd6` surface; mini-audit `wf_5fc4c3eb`; full suite GREEN 8875/465/0; acceptance 20/20 first-run):** FIRST GREEN — the DEMO beat works end-to-end on the first full-pipeline run (`[validate Person m]` → `ok … : Result Person (Map Keyword Reason)`; `[validate Cfg {}]` fills BOTH defaults — the non-literal fill debt DISCHARGED; `:check` as recoverable Result; the HOF beat `[map [validate Person _] rows]`; loud unknown-schema). Landed as audited + the audit's corrections: subject discipline = INFER-DISPATCH (the map-get template — the check-vs-`(Map Keyword ?meta)` proposal was audit-REFUTED: dyn rows fail `record-<:-map?`'s meta-V refusal); ctor minting = PAYLOAD-ONLY FQN heads (the foreign.rkt dual-arity contract; the partial-type-arg silent-stuck trap pinned); the bake at ELABORATION (lazy structurally impossible — typing can't rewrite immutable exprs, reduction can't reach typing-core; the whnf memo cache forbids registry reads at reduce time); ONE infer + ONE inferQ arm only (conversion fallbacks cover check/checkQ); the three zonk arms + `definitely-not-map?` exemption + pnet regN! + the `#f`-rule noise-suppression registration ALL same-commit. Two new require edges (elaborator→typing-core, elaborator→parser) raco-make-proven cycle-free; `subst-underscore`/`normalize-check-pred` re-homed (D29's second consumer LANDED — the `>`-reversal polarity golden-pinned). **Mid-slice fix (instrumentation-diagnosed):** `map-ish?` accepts holes — unannotated defn params zonk domain metas to HOLES, so defn-param subjects passed typing but tripped QTT as a spurious Multiplicity violation. Both retired-loud champ-arm pointer sets now land TRUE (actual coordinates typing-core:3120-3128/:3137-3141 — the doc's cited 3010-3025 was STALE, corrected here). ZERO door changes (seal 29/29 + schema-seal 28 green throughout). **By-catches for the close sweep:** `<`-preds unusable in WS files (`<` opens an angle group — pre-existing reader class; workaround `(> N _)`); match with an INLINE validate scrutinee fails inference (def-bound works — route-sensitivity); test-validate.rkt wall 47s > the ~30s guideline.
| **s3** DOOR FLIP + BRIDGE DELETION ✅ | stdlib abort fn (name/home decided at p0); doors bake node∘abort in the let-skeleton; DELETE `wrap-schema-checks` + call site; re-home `subst-underscore`/`normalize-check-pred` (second consumer lands HERE); gate probe both catch sites; P4 / ;;12–;;15 matrix re-run; strengthened-door census; single-failure message parity | suite green; the §13.7 bridge row lands TRUE on its named date |

**✏ s3 CLOSE (2026-07-18 — s3a `d9986bb1` + s3b `11660e92`; mini-audit `wf_e30376b8`, 6 facets + critic @ `033454bb`; full suite GREEN 8876/465/0):** the panic BRIDGE is DELETED; the door delegates :check to validate. **s3a `d9986bb1` — the abort face** (additive, zero door change): `expect-valid : {A} String [Result A (Map Keyword Reason)] -> A` in `reason.prologos` (ok unwraps; err PANICS with `render-failures` — the panic is the match ARM BODY, so it surfaces top-node at both catch sites per the audit's whnf-delta-unfold + expr-panic?-coupling findings) + `render-failures`/`render-reason`/`render-entry` (single-failure BYTE PARITY with the retired `wrap-schema-checks` message, probe-verified identical; multi-failure = field-sorted, `; `-joined). `expect-valid`+`render-failures` join the prelude unqualified (two-file namespace.rkt + PRELUDE, kept in sync). `test-reason.rkt` +1 case (consolidated — L3 file, ~4s/prelude-load, the ~30s watchdog). **s3b `11660e92` — THE FLIP**: `wrap-seal-validate` replaces `wrap-schema-checks` (SAME :check-presence gate → no-:check doors byte-identical), wrapping `(the S {aug|arg})` as `(let __schema-check-tmp (the S …) (…::expect-valid "S" (validate S __schema-check-tmp)))` on BOTH ctor doors — the LITERAL door swaps bridge→composition (byte-parity), the NON-LITERAL door is the D29 STRENGTHENING ([Checked m] runs :check at commit; census: ZERO live :check non-literal ctors). The let skeleton keeps the arg a schema-ann → `seal-application-body?`'s beta-redex gate fires body-independently (probe-verified at both catch sites; the one inferred link — rewrite-specializations preserves the shape — now REAL-emission-verified). Q_B keep-the-seal: the inner `(the S …)` still statically type-checks (wrong-typed literal fields stay a STATIC error). **The empirically-forced correction (elaborator.rkt): validate resolves S to the ns-QUALIFIED schema name (try-qualified-first).** The flip exposed a LATENT s2 gap — short and qualified schema fvars are DISTINCT types, so validate's short S failed to unify with a `def x : Schema :=` annotation (`Expected test::Strict / Got Strict`, test-schema-properties §6) AND rendered unqualified (the ;;13 display). Qualifying S fixes both; the outer-`(the S …)` display fix was REFUTED (`(the Schema <app>)` is seal-special → refuses a non-map subject → "could not infer type"). Census (D22.3): the two `:no-prelude` :check surfaces (f1b4 seal accept; test-schema-seal CHECKED) gain result+reason requires — a :check ctor door is now prelude-dependent (the f1b5 file already did this). Test-surface flips: ;;13 stays qualified (the resolved-sname fix); test-validate.rkt's two `Result Person` regexps → `Result vt[0-9]+::Person` (validate's direct display now qualified — s2 markers are `=>~` contains on value fragments, unaffected). Acceptances: seal **29/29**; f1b5 **24/24** (+4 s3 markers: both doors + the strengthening). Helpers `subst-underscore`/`normalize-check-pred` STAY (elaborator bake + test consumers; the "re-homed cashes out" obligation met). **NEXT: s4** (selection-validate — the elaborator arm currently emits a "lands at s4" honest error) + the composed §13.7 ledger row + the walker charter finalized in DEFERRED.
| **s4** SELECTIONS + LEDGER ✅ | selection-validate (view-subset residual); the composed §13.7 row finalized; the walker charter → DEFERRED.md; acceptance sections + gate; docs/dailies | acceptance green; DEFERRED entries carry REAL gates |

**✏ s4 CLOSE (2026-07-18 — `0f95d544`; mini-audit `wf_bd911236`, 5 facets + critic @ `5194ad92`; full suite GREEN 8885/465/0):** `[validate SelectionName e]` works (was an honest stub at elaborator.rkt). A selection is a read-side VIEW; `:requires` = READ-CAPABILITY, not completeness (D22.4 amendment 2). **Semantics**: enforce the single-segment `:requires` fields PRESENT (a miss = the read-capability violation → `missing-required`); validate every PRESENT parent field's type + `:check` (B1 — a view carrying a violating `:age` is flagged); accept extra parent fields (view over fuller data); NO parent-completeness; closedness iff the parent is `:closed`; the Result's S = the (qualified) SELECTION name. **Encoding = WIDEN THE PLAN ENTRY** (the audit's third option, beating both binary choices): a 7th per-field slot `required-on-miss?`. A 6th STRUCT field would break four positional match/ctor sites (typing-core:1867 / qtt:1262 / reduction:2341,2346) + pnet-serialize; a plan slot breaks NONE (all bind `plan` opaquely). Schema plans set it #t for every field → schema behavior BYTE-IDENTICAL. **The bake unifies** schema + selection: a selection borrows the PARENT schema's fields (lookup-selection-parent-schema), the plan spans the FULL parent field set (so plan-kws covers the parent for the closedness scan AND present parent fields validate), required? = #t (schema) / (field-kw ∈ single-segment requires-set) (selection). **The arm** (validate-tabulate) splits the missing branch: absent+no-default+required → missing-required; absent+no-default+NOT-required → a NEW partial-view SKIP arm (naively gating the old branch fell to `(nf #f)` = garbage). Q_A defaults: A2 (defaults still fill — schema-preserving, no live consumer of defaults-on-a-view). **Scope**: single-segment requires (length-1 keyword-paths); deep/wildcard (`:address.zip`/`:address.*`) defer to the walker charter (DEFERRED item 4 — the SAME descent mechanism). **pnet**: the 7th boolean slot rides the existing regN! registration (tag-only; `closed?` is already a serialized boolean). **Ledger**: §13.7 validate row stamped ✅ (all five components landed s1–s3a; selection-validate rides it, zero new mass). **Walker charter**: the nested-requires dimension folded into DEFERRED (item 4, one mechanism). **Residue (e)**: opt-in path now REAL; auto still deferred (s4 does not close it). Probe-verified (extra-field-accept / missing-requires-err / present-non-requires-still-validated / qualified-S); test-validate-node +1 (selection skip); f1b5 acceptance 24→**28** (+4 s4 markers). **F1b.5 implementation COMPLETE — next: F1b.6 (D23 posture flip) → F1b.close.**

**The p0 probes (flag-don't-guess, all pre-code):**
1. WS file-level preparse ordering vs a parse-time `validate` keyword → gates A2-e vs preparse-rewrite attachment (and thereby the `_`-mechanics: parse-time section composition vs explicit eta at the guard).
2. Path-as-Map-key spec health → gates the E key shape (fallback = Keyword with the widening cost named in-doc).
3. Let-skeleton gate: node∘abort inside `(let tmp (the S {aug}) …)` — does `seal-application-body?`'s beta-redex arm fire at BOTH catch sites (def-forcing + eval arm) with zero extension? Plus: projection-over-violating-seal still panics (the 4c exemption, expected bridge-independent).
4. `Result A (Map K Reason)` through `map-err`/`and-then` without annotation crutches (5-line probe) — container-E under Result polymorphism is the untested composition.
5. Network-free subsumption: can `subtype?`/`unify` (or the `field-type-satisfies?` machinery) run on closed ground values at reduction time? → decides extend-vs-consume for the witness (the mirror-drift cap wants CONSUME).
6. `.pnet` staleness of baked plans: schema-def change → does invalidation track the cross-module edge to validate-site cached bodies?
7. Two-context registry lifecycle at the bake stage (module-load, batch workers, cached bodies) — and confirm the reduction ARM needs ZERO runtime registry reads (any leak ⇒ the pipeline.md Two-Context Audit is mandatory).
8. P-Real demo schema shapes — list-typed fields would promote element recursion out of the walker charter (re-open D28's deferral with the honest Y).
9. The §3b blame-latch citation — must concretely name a monotone failure-set mechanism before anchoring the (e)-auto DEFERRED gate.

**✏ p0 PROBE OUTCOMES (2026-07-17 — all nine answered; both pins RESOLVED; p0 CLOSED):**
1. **Attachment → A2-e CONFIRMED** (agents at `6584b443`): preparse is WHOLE-FILE with schema pre-registration in Pass 0 (macros.rkt preparse-expand-all: pass 0 registers ALL schemas before pass-2 rewrites/parse/elaboration — order-insensitive by construction, all three contexts). The dual-parser cost collapses to the PRECEDENTED move: ONE datum-parser keyword arm + ONE tree-parser bail-list entry (the solve/defr/rel/facts/session pattern — tree parser bails "expression keyword handled by preparse", the merge takes the datum-parser surf). Bake at ELABORATION via `lookup-schema-by-name` — **cell-first reads ONLY, never the raw parameter** (see 7) — with a LOUD srcloc'd unknown-schema error on miss (strictly better than the incumbent silent no-op class, see 6).
2. **E key → the D27.3 fallback FIRES: `Map Keyword Reason`.** Path failed BOTH routes: inference cannot mint Path-keyed maps (the row carrier's key-domain is keyword/nat — Path-as-Map-key is structurally **F-carrier** key-domain work) AND `Path` is unbound in annotation position. Widening story NAMED: nested identity at walker time = dotted-keyword path encoding (`:address.zip`-class keys — the E TYPE never breaks; key VALUES widen; the dot-access-tokenization ambiguity check rides the walker charter); REAL Path keys re-trigger = F-carrier.
3. **Let-skeleton gate → fires body-independently** (direct read, driver.rkt:217-227: the arm destructures only `app(lam, schema-ann)`); after `nf` a failed check IS the top node at both catch sites. ;;12 = the live in-class proof; s3 re-probes with the real emission.
4. **Container-E Result generics → HEALTHY in the post-freeze shape**: annotated `err` construction, spec'd fns through `map-err` (→ `Result Int Int`), `unwrap-or` (default-FIRST — noted) all work; the sole mismatch was inferred-row-E × inferred-row-fn, which the nominal-Reason freeze avoids.
5. **Network-free subsumption → CLEAN**: `subtype?` runs standalone, tower included (Nat<:Int ✓, Int<:Rat ✓, Int<:Nat ✗). The polarity table CONSUMES `subtype?` (D28's mirror-drift-cap outcome; no hand-rolled parallel table for subsumption).
6. **.pnet staleness → SAME PRE-EXISTING CLASS, plus a sharper hole**: cache validity = own source mtime + driver.zo stamp ONLY (pnet-serialize.rkt source-hash-for-module — its own comment concedes no content/dep hashing); defaults/checks baked from module A into B's cached AST already go stale today. AND: schemas are neither serialized into .pnet nor re-registered on cache-hit → **A-cache-hit + B-cache-miss ⇒ lookup-schema #f ⇒ the existing inject/wrap silently NO-OP** (cross-module schemas are cold-only today). Validate INHERITS the class, adds none, and its loud bake-miss error IMPROVES diagnosability. → the consolidated DEFERRED "cross-module schema channel" entry.
7. **Two-context lifecycle → safe iff cell-first**: `current-schema-registry` (the parameter) is ABSENT from batch-worker restore, test-support parameterize, AND the macros 19-param snapshot — masked because reads are cell-first and the schema CELL-ID rides `save-macros-cell-ids` (index 0). Pin: the bake reads only via `lookup-schema`/`lookup-schema-by-name`; the three missing-list insertions = an **s1 hygiene rider** (the pipeline.md New-Parameter class). Cache-hit bodies deserialize with ZERO registry access (no preparse/parse/elaborate on that branch) — the baked plan needs nothing at load.
8. **P-Real shapes → element recursion DEFERS** (walker charter): every demo schema is flat all-String (Package/DependsOn; packages.json flat); the demo beat maps validate over the row LIST. Bonus: the demo wishlist's "accumulating applicative" ask is satisfied by collect-all Map-E inside plain Result. Stale demo-doc gap-row claims (":check parses sexp-only"; string→typed coercion unscoped) → F1b.close doc-truth sweep + coercion named-deferral.
9. **§3b blame-latch citation → CONCRETE** ("blame as monotone failure-set latch", the F-polarity phasing line; noting the phase is optional) — the (e)-auto gate anchors honestly.
p0 artifacts: acceptance skeleton `examples/2026-07-17-ciu-t6-f1b5-validate.prologos` (3/3 self-verified; s1–s4 target sections commented) + the M2-boundary paragraph below.

**The M2 boundary (p0 doc obligation — why validate's runtime fill does NOT re-open the refutation):** M2 refuted *preparse TYPE-level conditional fill* — a surface `if`-composition whose **branch unification** must type both arms: on dyn rows the join loses the filled field; on closed rows the type claims a field the value lacks (value/type mismatch). The failure lives in the TYPING of generated surface code. Validate's fill is a *runtime VALUE operation inside the reduction arm*: no surface `if` exists, no branch unification happens, and no type is asserted by composition — the node's type is `Result S E` by its ONE rule, and the `ok` payload is S because the arm **tabulates** (fills, witnesses, checks) before constructing `ok`. Different mechanism, no typing window. The residual corollary stands unchanged: on non-literal static routes, missing-defaulted remains type-accepted-no-fill (the 4e rule) — the VALUE materializes only when validate runs.

**Watchouts carried (panel synthesis, binding):** the composed off-network mass gets ONE ledger row w/ the runtime retirement horizon (never free-ride the typing rows) · no propagator vocabulary on the tabulation loop (NRC = zero/zero/no verbatim) · "byte-parity" language demoted (parity-on-single-failure only) · golden tests via the renderer only · the skip-set-empty test is a LANDING requirement (prose ledgers = correctness-by-vigilance) · strengthened doors need the census BEFORE landing (the D22.3 lesson) · "re-homed" must cash out in s3 · `:not-checkable` omission is a NAMED bet · panic-during-`:check` disposition documented ("inherits panic semantics, v1").
