# Dailies — 2026-07-19

Fresh dailies rolled at the open of a new working-day interval. The
`2026-07-05_dailies.md` reached ~519 lines and closed the Num-Series-founding +
CIU-Track-6 arc (F1b COMPLETE + PIR'd); the working topic now shifts to the
**Rel series** (relational surface language — solve typing + NAF correctness).
This file covers the Rel first-track arc. Prior work lives in git history + the
CIU/Num design docs + the F1b PIR; the `2026-07-05` dailies remains the record
through F1b.close.

STATE head seeded (copy-forward) from `2026-07-05_dailies.md`, per
`DAILIES_METHODOLOGY.org`.

---

## 📍 STATE  (overwrite in place — always current; per `DAILIES_METHODOLOGY.org`)

- **HEAD** `61ed9d6d` · **Suite** GREEN **8917 / 465 / 0** · **Branch** `main` (ahead of origin; don't push unless directed) · **Working tree**: pre-existing OWNER WIP only — leave alone, stage only mine, never blind-commit.
- **Where we are**: fresh interval opened into the **Rel series**. CIU T6 F1b (records / `Map`↔schema SEAL / runtime `validate`) is COMPLETE + PIR'd (`2026-07-19_CIU_T6_F1B_PIR.md`). The Rel first track (solve typing + NAF correctness) is a SEED (`2026-07-19_REL_SOLVE_TYPING_NOTE.md`) being opened for Stage-3 design.
- **NEXT**: open the **Rel first-track Stage-3 design** — grounding audit over the logic engine (S1 NAF stratum + `solve` surface + `&>` desugaring + solution-row reduction + the two-context relation registry) → typed-rows design (gates a–e) **co-designed with the NAF fix**. **Scope EXPANDED (owner, this session)**: (1) typed rows as **codata/coinductive observation** — the key's value type = the observed literal; reuse the F1 record mechanisms; (2) **`schema`-as-relational-facts + validation** — gives a-priori *static* type checking to relational facts/data (demo-relevant); (3) **`?logic-var:Type` domain constraints** on logic variables as a further typing source (design notes exist); (4) other Rel corners worth polishing for the demo — to be enumerated in co-design.
- **Open / blockers**: none blocking. Rel sequenced ahead of Path Selection (its highest-value consumer). NTT model + SRE lattice lens required (the track touches the NAF stratum + relation cells).
- **Watching**: the F1b PIR's NEW-but-unpromoted lessons (err-polarity-is-for-witnessing-not-`:check`; plan-as-data-widening; census-before-behavior-change; **mini-audit-refutes-premise**) — captured in the PIR, not yet in principles docs; promote if they recur.

---

## LOG  (append per commit / decision / surprise — newest at the BOTTOM)

*(New entries append below in the STATE-head + append-LOG format — see `DAILIES_METHODOLOGY.org`. First entry lands with the first commit of the Rel track.)*
