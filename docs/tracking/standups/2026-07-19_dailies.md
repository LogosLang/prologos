# Dailies — 2026-07-19

Fresh dailies rolled at the open of a new working-day interval. The
`2026-07-05_dailies.md` reached ~519 lines and closed the Num-Series-founding +
CIU-Track-6 arc (F1b COMPLETE + PIR'd); the working topic now shifts to the
**Rel series** (relational surface language — solve typing + NAF correctness).
This file covers the Rel first-track arc. Prior work lives in git history + the
CIU/Num design docs + the F1b PIR; the `2026-07-05` dailies remains the record
through F1b.close.

STATE head seeded (copy-forward) from `2026-07-05_dailies.md`, per
`DAILIES_METHODOLOGY.org`.

---

## 📍 STATE  (overwrite in place — always current; per `DAILIES_METHODOLOGY.org`)

- **HEAD** `680e0146` (+ finalize commit pending) · **Suite** GREEN **8917 / 465 / 0** · **Branch** `main` (ahead of origin; don't push unless directed) · **Working tree**: pre-existing OWNER WIP only — leave alone, stage only mine, never blind-commit.
- **Where we are**: **Rel Track 1 (Relational Language Usability), Stage-3 — Aspect-A core LOCKED.** Q-A2 (the NAF invalidation fork) is SETTLED = **E-with-B** (belief-layer per-binding narrowing), verified safe across two grounding audits + an adversarial options-panel + dynamic/structural probes; the design doc §5 (A.2 + NTT + SRE + adversarial) is finalized. CIU T6 F1b COMPLETE + PIR'd (prior).
- **NEXT**: **implement Aspect A** (main-session + per-change gate). **P0** ✅ · **A.1** ✅ (top-level echo) · **A.2 core** ✅ (per-binding belief-clear; FACT-generator `{both}` repro fixed; suite 8927/0). **→ NEXT (checkpoint):** **A.2b** = rule/recursive-generator NAF **DFS-defer** (the generator under-tags → per-binding can't enumerate; `safe-twohop`/`safe-reach` still buggy) · **A.3** static floundering gate in `install-conjunction` · **A.4** guard crash residuation + floundering. Then Aspects **B/C/D** design (still open) + polish. **Deferred to UCS**: `?v:Type` CLP resolution.
- **Open / blockers**: none blocking. **The bug, restated**: on-network Tier-2 non-recursive NAF collapses the generator var to ONE binding + clears ONE shared bit ⇒ `{both}` (over-include) or `{neither}` (over-exclude); recursive/all-ground NAF routes to DFS/WF and is correct. Fix = per-binding belief-clear. Two pre-existing scope boundaries E/B can't cover: body-local-generator NAF (→ DFS-defer) + bm=0 gating-only markers. Guard shares the single-bit structure at S0 (per-binding fix deferred, Q-A4). NTT (for/fold value-tier, NOT broadcast) + SRE (belief-vs-existence) locked in the doc.
- **Watching**: the F1b PIR's NEW-but-unpromoted lessons (err-polarity-is-for-witnessing-not-`:check`; plan-as-data-widening; census-before-behavior-change; **mini-audit-refutes-premise**) — captured in the PIR, not yet in principles docs; promote if they recur.

---

## LOG  (append per commit / decision / surprise — newest at the BOTTOM)

*(New entries append below in the STATE-head + append-LOG format — see `DAILIES_METHODOLOGY.org`.)*

### Rel first-track grounding audit — the "`&>` inversion" premise REFUTED; 3 real bugs + the typed-rows pivot — `wf_7ad61165-85d`  (2026-07-19, no commit)

Opened the Rel first-track design; ran the logic-engine grounding audit (6 facets + completeness critic, all HEAD-pinned to `b1e881c7`), R-lens-verified the load-bearing claims.

- **Surprise / correction (load-bearing)**: `&>` is the **rule-clause separator** (Prolog `:-` → `$clause-sep`, parse-reader.rkt:1796/1974 → parser.rkt:5254), carrying **zero negation**. The seed's "Problem 2b — `&>` guard inversion" is a **stale label**. `defr light-vehicle &> (vehicle v) (license v)` is a plain positive conjunction, so `{:lv "automobile"}` is CORRECT; the owner *intended* `¬license` and wrote it positive. The right spelling is `&> (vehicle v) (not (license v))` — but that hits bug (ii) below, so the underlying instinct ("negation in the clause is wrong") was RIGHT, just mislocated.
- **The 3 REAL bugs** (facet-6 *live probes*, on-network path): **(i)** top-level `not`/`guard`/`cut`/conjunction **ECHO** — `run-solve-goal`/`-one`/`-explain` handle only app/rel/unify/is then `[else (expr-solve goal*)]` (reduction.rkt:517/611/636); **shallow** dispatch fix. **(ii)** **NAF-subgoal OPEN-VAR LEAK** — `solve (unlicensed lv)` returns BOTH members (licensed leaks); ground queries correct; root = `install-conjunction` allocates ONE gate-bitmask per conjunction (relations.rkt:2197), so a single naf-bit can't isolate per-enumerated binding; **deep**. **(iii)** guard-fire **FFI CRASH** — an on-network guard over an unresolved var marshals the raw `expr-logic-var` into an FFI primitive (relations.rkt:2118/2140) → crash; DFS path substitutes, on-network path doesn't.
- **Architecture fork (NEW, load-bearing)**: top-level `solve` runs the **on-network ATMS path** (`stratified-solve-goal`), NOT the DFS `solve-single-goal` — DFS handles not/guard *correctly* but isn't the top-level path. Choosing which engine the surface fix targets is a deliberate mantra-relevant decision (on-network vs off-network scaffolding).
- **Typed-rows pivot**: `solve` is a bare `expr-hole` in BOTH checkers (typing-core.rkt:2930, qtt.rkt:2161) — starts from nothing, not `Seq(Map …)`. Solution-row keys = **query-var names, not schema field names** (reduction.rkt:239-250) → naive F1-SEAL reuse hits a KEY MISMATCH. NO single ground/free predicate (walk reproduced 3× + champ-build loop 5×); entry-gate-(b)'s "ONE predicate" must be BUILT. **schema-as-facts CONFIRMED a-priori static** (facts only, not `&>` rules; driver.rkt:814-822 checks before register, reuses F1 `schema-field-type->expr`+`check`) — a positional schema→row bridge exists (the typeable fragment).
- **`?v:Type`**: no type field on `expr-logic-var` (syntax.rkt:996); `?:` collides with dep-session-receive (parse-reader.rkt:926) so a fused token is impossible; prior art (`docs/research/2026-03-16_NEXT_GEN_LOGIC_PROGRAMMING.tex`) frames it as **runtime CLP(X)** — UNRECONCILED with the owner's **static-typing** framing; the defr signature schema is elaborated but DEAD (never types the clause vars). Reconcile before designing.
- **Next**: owner co-design on the re-scoped questions (track shape / engine fork / typed-rows keying / CLP-vs-static). Then Stage-3 doc. Correct the seed's `&>` label + the memory as part of the design write-up.

### Rel T1 scope LOCKED (owner co-design round) — general relational usability; NAF on-network; static-only `?v:Type`  (2026-07-19, no commit)

Owner answered the re-scoped questions decisively.

- **Track framing**: NOT just "typed rows + NAF" — the first Rel track is **general relational-language USABILITY**. Owner: typing on solutions is "not much work" given the F1 foundations. Three delivered pillars + one held research item.
- **(A) NAF/guard correctness — the deep core, ON-NETWORK.** Owner re-probed the *correct* spelling `&> (vehicle v) (not (license v))` → `solve (light-vehicle lv)` returns `[{:lv "automobile"} {:lv "bicycle"}]`, expected `[{:lv "bicycle"}]` — automobile LEAKS. Gave the analogous Prolog (`\+ license(V)` → `V = bicycle`). Ruling: **"fix the on-network path. This is a major correctness issue."** (Not the DFS off-network engine.) Fired a focused isolation grounding (`wf_1891cfd0-197`).
- **(B) Typed rows** — schema-facts a-priori typing already realized (schema→defr); **codata path must be as first-class as the schema'd variant** (Q2). Key strategy nudge (Q3): **rename query-vars → field names**, pass typing already present on the logic-var / schema — and this **ties to the `^` dynamic key-rename in Path Selection** (the next track). Owner may revisit tradeoffs.
- **(C) Polish** (Q5): answer-set dedup (diamonds) · drop `_anon` wildcard keys from projection · **NEW: present keys in predicate/fact declaration order** (not hash order).
- **HELD until asked (Q1/Q5)**: **efficient fact representation + query optimization** — a deeper, possibly *frontier research* agenda (query-opt + data representation for performative fact queries). Prior design conversation may be in standups; relates to the RPF track. **Do NOT dive until owner asks.**
- **DEFERRED to a later UCS track (Q3/Q4)**: domain-constraint *resolution* (`?v:Type` as CLP(X) — FD/interval arc-consistency). **This track uses only the STATIC-typing reading of type info**; design with the CLP vision in mind but don't build it. (Q4 clarified: owner intends static now; CLP later.)
- **Next**: on `wf_1891cfd0-197` return → design the on-network NAF per-binding-isolation fix (NTT model + SRE lens) → Stage-3 design doc for Rel T1 → implement (A first). Name the track.

### On-network NAF fix — mechanism grounded end-to-end; the invalidation fork surfaced — `wf_1891cfd0-197`  (2026-07-19, no commit)

Focused isolation grounding (5 facets + critic, HEAD-pinned + R-lens-verified). The leak is now understood to the bit.

- **Root CONFIRMED (verified, not round-1's inferred)** — TWO independent under-evaluations: (1) ONE naf-bit for the whole conjunction, allocated in the **`[(not)]` arm** via `solver-assume` (relations.rkt:2021 — NOT install-conjunction; round-1 coord imprecise, it only reads the bit back + OR-folds), inherited by every enumerated fact-row; (2) `process-naf-request` reads `v` **once** (scope-cell-merge last-write-wins collapse), evals the negation **once**, clears the **single** bit (relations.rkt:118/146/239). A single Boolean bit over all bindings ⇒ can only ever yield {both leak} or {neither}, never {bicycle only}.
- **The substrate ALREADY isolates per-branch** — facts (per-row `solver-assume` + fact-bit, relations.rkt:2461/2491) + clauses (per-clause bit) = propagator-design.md **Variant C**. `vehicle(v)` already tags bicycle@fact0 / automobile@fact1; the naf-bit is orthogonal + coarser.
- **Entry chain (corrected)** — top-level → `stratified-solve-goal` → **`solve-goal-propagator`** (relations.rkt:2880); `[(null? query-vars)]` → DFS `solve-goal` (CORRECT, why ground queries work); `[else]` → **Tier-2 ATMS** path (LEAKS). Fix targets Tier-2 + its S1 handler.
- **Fix = common core + an invalidation FORK (the design decision).** Common: the S1 handler **enumerates the raw tagged bindings** (`net-cell-read-raw` + `tagged-cell-value-entries` — the SAME primitive dissolution already uses at relations.rkt:2706) and evals the negation **per-binding under that binding's worldview**. Fork: **(B, minimal)** `solver-retract` the failing binding's existing **fact-row aid** → narrows its decisions-state component → worldview-projection (**decisions-state-bitmask ONLY**, propagator.rkt:975) drops the bit → that row's dissolution entry vanishes; needs `ctx` threaded into the pending entry. **(A, general)** allocate per-binding NAF bits via **`solver-amb`** (atms.rkt:397 — the real primitive; "make-choice-point" doesn't exist) over the enumerated bindings. **B reuses existing per-branch bits** (works when the generator tags per-branch — fact relations do; rule/computed generators may not, where A is needed). **Compound-nogood (c) RULED OUT** — nogoods are NOT projected into worldview-cache (only decisions-state is).
- **Safe/floundering — NO check exists** (C3, verified): a never-bound negation var silently "succeeds." Fix adds the **groundness gate** → flounder error, not silent wrong answer.
- **Guard (bug iii)** — FFI crash = guard-fire hands a **raw** `expr-logic-var` to `eval-fn` on bot (relations.rkt:2118/2140 → foreign.rkt:91 marshal). Fix = **residuation** (reuse discrimination's residuate-on-bot, relations.rkt:720; a no-op fire-once is NOT consumed, propagator.rkt:3384, so it re-fires when the var resolves) **paired with a flounder terminal** at quiescence (residuation alone is UNSOUND — the guard silently passes). Guard ALSO shares NAF's per-binding leak but evaluates at **S0** (fire-once), so its per-binding fix shape DIFFERS from NAF's S1 handler — decide fix-now-vs-defer.
- **Next**: co-design the A-vs-B invalidation fork + guard scope + floundering-in-scope; NTT model + SRE narrowing lens (retraction = lattice narrowing) into the Stage-3 doc; implement main-session + gate.

### Stage-3 design doc landed + owner course-correction (cart-before-horse; NTT step-think caught)  (2026-07-19, no commit)

- **Course-correction (owner)**: I'd run two grounding audits + a full fix design into what was effectively Stage-4 audit/implement mode BEFORE persisting a phased/scoped Stage-3 artifact. Owner: "the cart is half-way in front of the horse." Also caught: my NTT sketch had a **step-think smell** — a nested `for` over independent per-binding negation checks = the `for/fold`-over-independent-items anti-pattern. **Fix**: the checks are embarrassingly parallel → **broadcast**, and invalidation should **emerge** from per-binding contradiction, not an imperative check-then-retract.
- **Landed**: `docs/tracking/2026-07-19_REL_T1_RELATIONAL_USABILITY_DESIGN.md` — Stage-3 artifact: summary · progress tracker (S3→X.close) · scope (A/B/C + polish + held + UCS-deferred) · grounding (verified, with the `&>` premise correction) · per-aspect options **marked OPEN** · **on-network (broadcast) NTT model** replacing the step-think sketch · SRE narrowing lens · **first-pass adversarial principles challenge** (two-column catalogue→challenge on the invalidation fork) · reserved `X.close`. Registered in Rel Master + Roadmap (both note the `&>` supersession).
- **Why-forward**: the doc marks DECISIONS OPEN (not decided) — the next step is the *joint* adversarial round, per the owner's ask to "go through these design options carefully and challenged adversarially by our principles." Deepest live challenge surfaced in the doc: is Option B's `solver-retract`-call itself imperative dispatch vs the mantra-deeper "contradiction under each binding's worldview structurally narrows"?
- **Next**: the adversarial principles round on the OPEN forks (main-session + owner; optionally the design-options-panel for the high-stakes fork) → finalize S3 → name the track → P0 acceptance file → implement.

### Q-A2 independent adversarial panel — corrected my framing TWICE + surfaced 2 novel options — `wf_208ab506-5e2`  (2026-07-19, no commit)

Ran the design-options-panel (propose→critique→synthesize) on the invalidation fork, in parallel with my own conversational analysis, to compare. It earned its keep — two load-bearing corrections to the MAIN-SESSION framing (R-lens-reconfirmed here @ HEAD 680e0146):

- **CORRECTION 1 (idiom-flip, load-bearing)**: my "Option B narrows the PRIMARY = principle-native reuse" was built on a FALSE-evenhandedness premise. Verified: the CURRENT solver NAF handler (relations.rkt:239-242) AND the guard-fail path (:2152-2155) BOTH **direct-write the DERIVED worldview-cache** (AND-NOT) and NEVER touch decisions-state; only ONE solver stratum handler (the NAF one, :246); `solver-retract` is called INLINE (atms.rkt:534), no between-round solver retraction stratum exists. ⇒ **direct-write-to-derived IS the established solver invalidation idiom**; **E = idiom-continuation (lowest delta)**; **B/A/D = idiom-DEPARTURES** (first-ever solver-NAF use of retract→decisions-state→projection). The mechanism question is NOT a clean primary/derived gradient — it's a genuine **ship-now (E) vs principle-clean-but-new-unexercised-path (D)** tradeoff.
- **CORRECTION 2 (broadcast category-error)**: my NTT "broadcast, not for-loop" correction (made in response to the step-think flag) is a **category error for THIS context** — the per-binding work is a nested fork+run-to-quiescence inside an **S1 BETWEEN-ROUND stratum handler**, where a **for/fold over the pending set is the CANONICAL BLESSED shape** (process-naf-request/-resolution/-retraction are ALL for/folds); the A/B broadcast benchmark (light items) does NOT transfer. **Reconciliation** (panel's own watchout, honest): for/fold is blessed UNDER THE CURRENT SCHEDULER; the parallelism is a **deferred scheduler-layer concern** (Cell/Propagator/Scheduler orthogonality), not "off the table." The step-think instinct wasn't wrong in spirit — it lands as scheduler-layer deferral, not broadcast-now.
- **2 NOVEL options I missed**: **D** = between-round accumulator narrowing the PRIMARY (E's atomic-accumulator SHAPE but on decisions-state; the principle-clean form — but must BUILD a new solver-side retraction stratum + win an ordering race vs solver-assume's eager wv write). **G (anti-join, the deep one)** = `gen(v), not q(v)` IS a relational **anti-join** `gen ▷ q`; eval q ONCE with v free, collect the kill-set K={v:q(v) provable} (inner fork's tagged entries), semijoin-clear the matching generator fact-rows. **One eval, not N** → moots the per-binding perf/parallelism debate for the **fact-based** case (which is our repro — `license` is a fact table). Orthogonal DETECTION axis; composes atop E or D. Gated on the (absent) floundering/safety check. Panel: the ONLY option reducing detection to a single relational-algebra primitive — "deepest mantra alignment on offer, under-weighted as a mere optimization."
- **The clean 3-axis factorization**: IDENTITY (B reuse fact-aid / A fresh amb) × MECHANISM (E extend direct-write-of-derived / D new primary-narrowing accumulator) × DETECTION (G anti-join / N-fork per-binding). A ship = one point. Both agents AGREE: IDENTITY=B with A as named fallback (A's dual-bit is INTRINSIC not scaffolding — principle-dominated by B); floundering gate + ctx-plumbing = non-differentiating prereqs. They DISAGREE on MECHANISM (propose=D principle-clean; critique=E ship-now + D as named BLOCKING follow-up migrating NAF+guard together).
- **Strongest under-weighted tension**: E's **CONTINGENT CLOBBER** — E leaves β "believed" in the primary; if a LATER conjunct narrows decisions-state after the NAF clear, the projection re-derives worldview-cache from a decisions-state that still believes β → β re-asserted. A state-order bug that PASSES the fact test case and detonates later ("works for the test case" one level up). The strongest argument FOR D over E; if E ships it must be proven UNREACHABLE, not assumed rare.
- **Deciding R-lens (unverified, collapse the fork)**: (a) generator-shape generality — do rule/computed generators tag per-branch? (decides B-vs-A); (b) D's projection re-fire + ordering race vs eager wv write; (c) G's free-var eval safety/finiteness + kill-set enumerability. The idiom-flip + no-solver-retraction-stratum are VERIFIED.
- **Next**: bring the honest comparison to the owner (done this turn); decide MECHANISM (E-vs-D) + the G reframing + run the deciding probes; then fold the settled option space into design-doc §5 A.2 + implement.

### Q-A2 probes — B settled, floundering=static, and D-first hits a belief/existence collision — `wxiplxs59` + main-session runs  (2026-07-19, no commit)

Ran dynamic `.prologos` probes (main-session) + a 3-facet structural grounding audit + critic. Owner picked D-first + G-appeal; the probes resolved the three questions AND surfaced a fourth that bears on D-first. R-lens-verified the load-bearing claims @ 680e0146.

- **DYNAMIC (observed)**: the leak is generator-shape-INVARIANT — fact / rule / two-clause-disjunction generators ALL return both members. Ground/DFS path is CORRECT (bicycle→`[{}]`, automobile→`nil`). **G's anti-join is behaviorally trivial for the fact case**: `solve (license lv)`=`{automobile}` (kill-set, finite), `solve (vehicle lv)`=`{bicycle,automobile}`, difference=`{bicycle}` = correct. The `atms_hypothesis_count:0` counter is a scoping artifact (solver forks uncounted), NOT the assume-path failing.
- **IDENTITY (B) — SETTLED**: the ONLY enumerating goal kind is `app` (facts + rules); `is`/`unify` are deterministic single-value; NO enumerating builtin generator exists. Fact + multi-clause + single-rule-clause-over-inner-fact ALL tag per-branch with distinct RETRACTABLE decisions-state aids (relations.rkt:2461/:2491; head-param↔body-var share the query scope-ref). **⇒ B generalizes; the A-fallback is likely UNNEEDED.** ONE open gap: **body-LOCAL intermediate rule vars** (`p(x):-q(x,y),r(y)`) get NO scope cell on-network (`collect-clause-vars` is DFS-ONLY, relations.rkt:1219/:1381/:1733) — do such rules RUN on-network or DFS-fallback? = the #1 remaining scoping probe.
- **G — REALIZABLE, medium cost**: kill-set harvester **`dissolve-solver-pu` EXISTS** (relations.rkt:2674, raw+tagged→listof hasheq, callable with query-vars=(list inner-var)). Delta = re-target the current `for/or` boolean collapse (must use raw+tagged, else last-write-wins under-populates K — the SAME collapse bug as the leak) + thread generator var/cell into the NAF `info` (currently only inner-goal/env/naf-bit-pos) + generator-carrier tagged-promotion.
- **FLOUNDERING seam = STATIC install-time**: `install-conjunction` (relations.rkt:2190) pre-scans gating kinds — the natural home for a static range-restriction check (a `not`-goal var must appear in a positive body goal); has the positive-bound-var set, fires PRE-fork, clean compile error. The dynamic S1 seam (`inner-vars-final`) CANNOT distinguish safe from floundering. Phase-0 prereq for ALL options; no safety infra exists.
- **D — mechanism FEASIBLE but a NEW belief/existence collision (the reframe)**: ✅ the ordering race (my + panel's top D-blocker) RESOLVES SAFELY — narrow sticks (top-absorbing intersection), worldview-cache-merge is REPLACEMENT (propagator.rkt:955; `:989` ior comment STALE), BSP restarts S0 → projection re-fires → eager bit drops. ⚠ BUT **belief-vs-existence** (atms.rkt:583-596, VERIFIED): decisions-state = which assumptions EXIST; worldview-cache = which are BELIEVED for reads (can be a SUBSET). `solver-state-with-worldview` writes a believed subset DIRECTLY to worldview-cache without touching decisions-state, LIVE via `solver-state-solve-all` (atms.rkt:679) — **the exact multi-answer row-enumeration path Aspect B builds on**. Current NAF/guard AND-NOT = **BELIEF narrowing** (decisions-state untouched) → coexists cleanly with enumeration. **D's EXISTENCE narrowing would re-fire the projection mid-enumeration and CLOBBER solve-all's belief scratchpad** (replacement merge). D is a NEW WIRING (NAF/guard never call solver-retract; sole caller infra-cell.rkt:451). **This INVERTS "D is purer"**: a NAF contradiction is a per-branch BELIEF statement; belief-narrowing is the layer that coexists with row-enumeration; D's existence-narrowing fights the belief/existence separation AND collides with Aspect B.
- **Reframed mechanism read (mine, post-probe)**: E-with-B-identity (per-binding BELIEF narrowing — generalize the single-shared-naf-bit AND-NOT to per-binding using each binding's existing fact-bit) is now the architecturally-FAITHFUL fix (not merely "ship-now"): stays in the belief layer, coexists with solve-all, reuses B's fact-bits, smallest delta. D's contingent-clobber is the SAME projection-replace mechanism — a remaining reachability probe for E's safety. TWO remaining probes: (1) body-local-intermediate rules on-network-vs-DFS (bounds B + scope); (2) contingent-clobber reachability (does anything narrow decisions-state after the NAF clear?).
- **Next**: present the honest synthesis + the D reframe to the owner; run the 2 remaining probes; re-decide mechanism with belief/existence in view; then fold the settled space into design-doc §5 A.2.

### Q-A2 CONVERGED — E-with-B validated safe; scope boundaries surfaced — `wkz1x7e79` + dynamic runs  (2026-07-19, no commit)

Stress-test audit (3 facets + critic) + dynamic `.prologos` runs. **The E-with-B lean HOLDS.**

- **VALIDATED SAFE** (3 green checks): (1) `dissolve-solver-pu` (the production free-var enumerator, relations.rkt:2964) HONORS E's belief-clear — `bitmask-visible?` (relations.rkt:2764) gates every enumerated row against the E-narrowed worldview-cache, so AND-NOT'd bindings vanish; (2) the **contingent clobber is UNREACHABLE** — NAF is a VALUE-tier handler writing ONLY worldview-cache (no propagator-input dependents → no worklist → no S0 restart → dissolve reads post-quiescence, relations.rkt:239-242/2957/2964); (3) `solver-state-solve-all` (the collision path) is **DEAD CODE** (no production caller — compat shim) so the belief-enumeration collision I cited for D is moot; (4) belief-vs-existence re-confirmed (E belief-layer coexists; D existence-narrowing would clobber via projection re-fire).
- **BUT E is a REAL implementation, not a tweak**: the current handler computes ONE `inner-provable?` boolean + clears ONE `naf-bit-pos` (relations.rkt:122/239). E must ADD: per-binding enumeration (read the generator's tagged bindings via `net-cell-read-raw`/`tagged-cell-value-entries`) + per-binding provability + map each FAILING binding → its fact-bit (2^aid, relations.rkt:2491) + AND-NOT those. Net-new machinery in `process-naf-request`. Land at the VALUE-tier site (relations.rkt:239-242), NOT the guard's S0-tier clear (:2151, clobber-exposed; guard's own S0 clobber-safety = separate open item).
- **TWO pre-existing scope boundaries E/B can't paper over** (flag, not introduced by us): (a) **body-local-generator NAF** — `p(x):-q(x,y),not r(y)` with y body-local: NAF FORCES on-network (has-naf-or-guard? ⇒ use-propagator?=#t, no threshold escape, stratified-eval.rkt:216) but body-local y gets NO on-network cell (clause-env = head params only; raw-symbol args silently skipped) → the bindings E would AND-NOT never materialize. **Fix path**: extend the NAF handler's EXISTING all-ground DFS escape-valve (relations.rkt:177-206) to also DFS-defer body-local-generator NAF. (b) **bm=0 gating-only success markers** can't be belief-cleared (fact-generators SAFE — non-zero bits, relations.rkt:2491).
- **Shape-dependence explained** (correcting my "uniform leak" + the earlier "generator-shape-invariant"): NAF forces on-network; the on-network single-bit collapse gives {both} (last-write-wins picks an unblocked binding → keep bit) or {neither} (picks a blocked binding → clear bit) — DYNAMIC-confirmed: fact/body-local non-recursive → {both}/{neither}; recursive (safe-reach) → correct (all-ground DFS sub-case or WF path — minor flagged nuance). Body-local rules DO run + are correct when NAF-free (demo `needs` routes to DFS: no negation, 2 clauses < threshold 256).
- **VERDICT**: E-with-B is the mechanism; Q-A2 CONVERGED. **Next**: finalize Stage-3 §5 A.2 (settled E-with-B + the per-binding machinery spec + the body-local DFS-defer + bm=0 note + static floundering gate) → name the track → P0 acceptance covering BOTH {both}/{neither} modes → implement.

### Q-A2 LOCKED into the Stage-3 design — Aspect-A core finalized  (2026-07-19, owner: "capture this lock back into the design; proceed")

Owner approved the E-with-B lean + told me to capture it. Rewrote design-doc **§5** from OPEN-fork to SETTLED: **A.2** = the E-with-B decision record (why belief-narrow not existence-narrow; why B not A; verified-safe; the net-new per-binding machinery; the two scope boundaries) · **A.3** = the **static** install-time floundering gate · **NTT model** corrected (for/fold-over-pending value-tier — the broadcast was a category error; parallelism = deferred scheduler-layer concern) · **SRE lens** rewritten (belief-vs-existence, not narrow-the-primary) · **adversarial challenge → RESOLVED** (clean red-flag scan) · added **§5.G** (anti-join detection option, deferred). Title/banner/tracker/§11 updated; **track NAMED "Relational Language Usability"**; Rel Master + Roadmap → "A-core LOCKED (E-with-B)". **Next: implement Aspect A** (P0 acceptance [both faces] → A.1 echo → A.2 belief-clear + body-local DFS-defer → A.3 static gate → A.4 guard), main-session + per-change gate; then B/C/D design. **Watching (promote if it recurs)**: the *premise-refutation cascade* this arc — 3 owner/panel/probe corrections to MY confident framing (`&>`-inversion; B-as-reuse; uniform-leak) — the independent adversarial round + dynamic probes each caught a wrong main-session premise; strong evidence for the "always run the independent critique + probe before locking" discipline.

### A.1 DONE — top-level bare `not` echo fixed  (`860248bf` P0 + A.1 commit pending)

Aspect-A Phase A.1 (the shallow echo fix), full per-change gate green.

- **Mini-audit finding**: the reachable top-level `solve` grammar is app/rel/unify/is/**not** (`parse-relational-goal` just flags + parses a datum); **guard/cut/conjunction are clause-body-only** → A.1 = the `not` arm alone (adding guard/cut arms = dead scaffolding, avoided). `solve-single-goal` already handles `'not` correctly (prove inner; empty ⇒ succeed).
- **Landed**: `expr-not-goal?` arm in `run-solve-goal`/`run-solve-one-goal`/`run-explain-goal` (reduction.rkt), each building `(goal-desc 'not …)` and dispatching to `solve-single-goal` — mirrors the existing inline-unify arm. Previously fell to `[else (expr-solve goal*)]` → echoed the goal unevaluated.
- **Probe-first (acceptance)**: `solve (not (license "bicycle"))` → `[{}]` (NAF succeeds); `→ automobile` → `nil` (NAF fails). Was echoed before.
- **Gate GREEN**: check-parens + `raco make driver.rkt` OK; acceptance `--check` 4/4; `tests/test-rel-t1-naf.rkt` (NEW, 3 tests, the dedicated Phase-T file grown during impl); **full suite 8922 / 465 / 0**.
- **Note**: A.1 is the *top-level bare-not* echo (a distinct site from A.2's *clause-body* per-binding leak). Pre-existing "PRELUDE DRIFT" warning is unrelated (I only touched reduction.rkt).
- **NEXT: A.2** — the deep core: per-binding belief-clear in `process-naf-request` (enumerate tagged bindings → per-binding provability → AND-NOT failing fact-bits from worldview-cache) + body-local-generator DFS-defer.

### A.2 core DONE — per-binding belief-clear fixes fact-generator NAF; A.2b split out  (A.2-core commit pending)

The deep core. E-with-B per-binding belief-clear landed for FACT generators; the rule/recursive-generator class split to A.2b (empirically pinned).

- **Landed**: `naf-per-binding-mask` (relations.rkt, before `process-naf-request`) — resolves the negated goal's args, detects the free generator var by its **RAW tagged entries** (≥2 bindings; NOT `logic-var-read`, which collapses), evaluates the negation per binding via DFS `solve-goal` (ground inner goal), and returns the belief-clear mask **(OR failing-bits) & ~(OR passing-bits)** — clears the failing bindings' distinguishing bits, preserves the passing. `process-naf-request` gains a `pb-mask` override branch; the existing single-shared-bit logic moves verbatim into the `else` fallback (surgical, zero change when pb-mask=#f).
- **Probe-first**: `solve (light-vehicle lv)` `'[{:lv "automobile"} {:lv "bicycle"}]` → **`'[{:lv "bicycle"}]`** — the `{both}` over-include (the reported foray repro, a FACT generator) is FIXED. Ground queries + the top-level A.1 cases unchanged.
- **A.2b split (empirical, honest)**: a debug trace showed **rule/recursive generators UNDER-TAG** — `twohop` (rule w/ body-local join) + `reaches` (recursive) resolve the NAF var to a single collapsed value (not ≥2 tagged entries), so `naf-per-binding-mask` returns #f → falls back → `safe-twohop` `{neither}`, `safe-reach` partial-drop UNCHANGED (no regression). So the design's "body-local NAF var" boundary is BROADER: it's "the **generator** doesn't materialize per-branch tags" (rule + recursive). **A.2b = DFS-defer** for this class (route to the DFS solver, which handles NAF correctly — the all-ground precedent), or deeper materialization. Distinct, larger change.
- **Gate GREEN**: check-parens + `raco make` OK; acceptance `--check` **5/5**; `test-rel-t1-naf.rkt` grown to **5** (A.1×3 + A.2×2); **full suite 8927 / 466 / 0**.
- **NEXT (checkpoint)**: A.2b (rule/recursive-generator DFS-defer) — OR A.3/A.4 first, owner's call. The reported repro is fixed; A.2b covers the constructed rule/recursive stress cases.
