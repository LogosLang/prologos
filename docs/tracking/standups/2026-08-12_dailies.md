# Dailies — 2026-08-12

Rolled from `2026-08-11_dailies.md` (**616 lines**, past the ~400–500 cap) at the
opening of the 2026-08-12 working-day interval, paired with
`docs/standups/standup-2026-08-12.org`. Prior file is read-only history; its LOG
(i–viii) carries the whole **P4e-1b** arc: the mini-audit, Q_U38–Q_U44, slices
1b-i and 1b-ii, and **both 1b-iii reverts**. `2026-08-10_dailies.md` carries the
P4e-1a arc (LOG xxvi–xxix). STATE head below is SEEDED (copy-forward per
`DAILIES_METHODOLOGY.org`) and refreshed to current — the prior file's accreted
historical layers are left there rather than carried.

---

## 📍 STATE  (overwrite in place — always current)

- **HEAD**: re-derive with `git rev-parse HEAD` — never trust a hash in prose.
  At this refresh: **`f6b52b81`** (TSG T1 — the tree-sitter grammar repair,
  LOG (xviii)). Last COMPILER-surface commit is still **`cb555002`**
  (slice 1b-v A/B1/B1b/B2a **+ verify closed** — `*_` is LIVE in the three bands
  that spell it; B2b, the mint, remains) — the suite figures below are FRESH at
  `cb555002` and remain valid, since (xv) touched no compiler surface.
  · local `main`, MAIN CHECKOUT · **116 unpushed** — MEASURED 2026-08-14 against
  `origin/main` = `4dbcfbba`; RE-MEASURE rather than trusting this number ·
  **do NOT push unless directed** (origin advances externally) ·
  battery **550** live · ✅ **suite 10217 / 488 / 0** (`--all --force-rerun`,
  `[488/488]`, 197.0s, exit 0 — **FRESH at `cb555002`**) · acceptance **89** markers, 0 errors ·
  ✅ **THE TIMEOUT HEADROOM IS FIXED** (`a4cbe7b8`) — and both "warts" were BUGS:
  `--timeout` never reached the worker (spawned with no `--file-timeout`, so the
  real limit was a hardcoded 120s while the banner printed 600s), and "⛔ DEAD
  WORKERS" was a 30s deadline rather than a liveness probe, which is why
  `--tests tests/test-path-selection.rkt` "aborted" on a clean tree. Now threaded
  (default **300s**, printed AND enforced) and the probe RE-ARMS while
  `subprocess-status` says the workers run. ⭐ The fix landed ONE RUN before it
  was needed: the regression suite for it measured that file at **121.9s**, over
  the old ceiling. ⚠ Headroom is not permanent — it measures 112–122s on the same
  commit; watch it against the limit, not against memory.
  working tree = owner WIP only; stage explicit paths, never stash.
  ⚠⚠ **THE SUITE IS ~16% INTERMITTENT AND PRE-EXISTING — query
  `racket/prologos/data/benchmarks/timings.jsonl` (NOT repo-root `data/`) BEFORE
  any base A/B.** 470 runs: 74 with `all_pass=False`; `test-properties.rkt` is
  RANDOMIZED, so a ±5 swing is NORMAL.
  ⚠ Do NOT compile while a suite is in flight. ⚠ **COUNT `ERROR` AS WELL AS
  `FAILURE`** — a raise inside a `test-case` reports as ERROR and
  `grep -c '^FAILURE'` reads it as green. ⚠ **Run from the REPO ROOT** — a failed
  `cd` in an `&&` chain silently skips the step (bit me twice at 1b-i/1b-ii, and
  once more this session where it failed LOUDLY, which is the good case).
  ⚠ **NEVER `git checkout` a file to undo a mutant over uncommitted work** — it
  left a stale `.zo` that faked a cross-twin failure, then discarded the
  uncommitted twins entirely. Invert the mutant in place; force the rebuild.
  ⭐ **NUMBERING is RULED** [owner 2026-08-08]: monotonic, permanent, from the
  register at the top of `DEFERRED.md`. **Next free 134 · next free Q-label U54.**
- **Where we are**: **CIU T6 — THE DEEP FLATTEN IS LIVE AND TWICE-VERIFIED.**
  1b-iii shipped as C1 (`4f30a171`, the shared join hoisted, returning BARE) →
  C2 (`68014124`, the seat migrates) → verify round 1 (`eee68262`, 3 blocking
  regressions) → verify round 2 (`73c56b2c` + `28b524d7`). Measured live:
  `cfg{db.hosts*}` → `{:db @[1 2]}` · `two{a.{x y}*}` → `{:a @[1 2 3]}` ·
  depth-3 `e3{a.b.c*}` → `{:a {:b @[1 2]}}` · `cfg{db^.hosts*}` / `vh{0.{0}*}`
  keyless · written-order at depth: `w{zz.u* aa.v*}*` → `@[3 4 1 2]` versus
  `w{zz^.u* aa^.v*}*` → `@[1 2 3 4]`.
  ⭐⭐ **Q_U47 IS IMPLEMENTED BY *NOT* IMPLEMENTING IT** — nothing computes the
  landing; the pre-check narrows, a deep star falls through to the head dispatch,
  ONE tail arm (matching EXACTLY `(sₙ ★)`) returns the shared join BARE, and
  whichever caller catches it supplies the wrapper. **Two of the three landings
  needed no code at all.** Earlier history:
  `f0d91853`·`43c91859`·`e38845a9`·`dd5d145c` (**A** — instrument, preconditions,
  ordering substrate, fail-kind trapdoor) · `1c0c5123` (**B1** — both twins' star
  arms, atomic, inert) · `d5934397` (**B2** — the seat migration; the flatten goes
  live) · `5e4a3338` (**verify round 1** — 5 distinct defects fixed/shielded).
  Measured live: `rowsv:tags*` → `@[1 2 3] : [PVec Int]` (the spec's own ω·ω→ω) ·
  Q_U44 canonical `@[3 4 5 6 1 2]` and the recovery spelling `@[1 2 3 4 5 6]` ·
  the ravel `@[1 2 3 4]` · L4 guided in BOTH orders · leaf PERMANENT · nominal
  not-yet. Records: D4 [§5.P4e-1b-iii-A](../2026-07-28_CIU_T6_PATH_SELECTION_D4.md#p4e-1b-iii-a)
  and [§5.P4e-1b-iii](../2026-07-28_CIU_T6_PATH_SELECTION_D4.md#p4e-1b-iii).
- **NEXT — slice 1b-v **B2b**, MINIMAL by [Q_U53](../2026-07-28_CIU_T6_PATH_SELECTION_D4.md#q-u53):
  widen the gate's lexeme conjunct to EXACT table membership + route the emit
  through the same lookup ([DEFERRED 129](../DEFERRED.md)); **130** rides with it.
  Reader-only, so the three live bands cannot regress.
  ⚠ **[DEFERRED 133](../DEFERRED.md) IS A PRECONDITION FOR B2b's OWN TESTS** —
  the battery's `p4e-star-type` / `p4e-last-star-type` are exact-lexeme `"*"` and
  return `#f` on a `*_` source, so they cannot tell "correctly did not mint" from
  "I cannot see this token". **DEFERRED 127's defect a THIRD time, one layer
  down, where the repair never propagated.** Widen them BEFORE writing any row.
  Also owed: the DEFERRED-108 snapshot needs a deliberate re-baseline (a reader
  widening changes which sources abort; it will look like a bug).
  Then **1b-v-C — THE FOLD** [owner: "land the fold before 1b-vi"]:
  `split-star-lexeme` delegates to the family table, so the four bands share ONE
  semantic authority; the two TOOL lists get CONTAINMENT PINS, never derivation
  (`star-spellings` must stay a SUPERSET or it re-blinds itself — that is
  DEFERRED 127's whole repair). Then **1b-vi**, **P4e-2**, PF → P5 → PX → P6 →
  X.close.
  ⭐⭐ **THE FINDING THAT SCOPED IT: B1's "the family lives as ONE table" is FALSE
  at HEAD.** `$postfix-star-synth` has **zero producers**; the three live bands
  go through `split-star-lexeme` and never touch the table. Measured —
  `mn{a*_}` → `{:a-x Int}` ✅ vs `mn{a}*_` → `Unbound variable` ❌. Same
  operator, two authorities, disjoint bands.
  ⚠ **THE HOOKS WERE DEAD AND ARE NOW LIVE** (`96ef73f0`): `core.hooksPath`
  pointed at the pre-move checkout, which OVERRIDES the hooks dir entirely, so
  `check-parens` ran on nothing for weeks. **It failed OPEN.** Re-verify after any
  clone: `./tools/install-git-hooks.sh` (now relative-linking, guarded, and it
  refuses to install into a void).
  ⭐⭐ **THE LESSON TO CARRY: A DIRECT-CALL PIN IS ONLY AS GOOD AS ITS FIDELITY TO
  THE PRODUCTION SHAPE.** B1b's pin built champs with SYMBOL keys; production uses
  `expr-keyword` STRUCTS. It passed on a fiction while the real path took a
  whole-file abort — and an INERT helper's pin is exactly where that goes
  unchecked, because nothing else exercises it.
- **(superseded) slice 1b-v **A** (the instrument, RUN RED first). [Q_U52](../2026-07-28_CIU_T6_PATH_SELECTION_D4.md#q-u52)
  SCOPED IT: the mint **+ `*_`'s semantics**; `*-`/`*-_` are now **1b-vi**.**
  The mint is a TWO-SITE change (the reader gate's exact-lexeme test + the
  parser's closer-adjacent arm, which hardcodes `'flatten` and never calls
  `split-star-lexeme`) — `*_` already works in the other three bands. Two bare
  sentinels; name the new one **`$postfix-star-synth`** so five substring-matching
  gates keep covering it. ⚠ Before pinning anything: **three rulings have diverged
  from HEAD** (Q_U42 both examples · Q_U45's literal output order · the
  pre-written 1b-v pin block's 2 exercisable checks) and **DEFERRED 127** means
  the arrival-matrix instrument goes blind unless its carrier table widens in the
  SAME commit.
  ⭐⭐ **THE LESSON THIS AUDIT ADDED: a stale IN-CODE comment outranked a
  corrected AMBIENT rule in my reading** — I relayed a whole-file-abort footgun
  the facet had "verified" from a comment, while `pipeline.md` already recorded
  that exact correction. Check the rule before repeating the code's own claim
  about itself.
- **(superseded) 1b-v's earlier framing — [Q_U51](#q-u51) rescoped it: the mint
  is not attached to the slice, it IS the slice.** 1b-iv is COMPLETE (A/B1/B2 +
  verify, 11 survivors resolved) and BOTH owed rulings are made. `*_`'s prefix is
  RULED and FORCED (`<content's key in the deleted layer>-<its own key>`); what
  1b-v must build is the band where the operator can actually do its job —
  measured, the only spellable band today has ONE content by construction, so
  `*_` cannot remove a collision there at all ([DEFERRED 124]). Slice = the mint
  + `*_` + `*-` ([Q_U48](#q-u48)) + the `*-_` decision.
  ⭐⭐ **THE LESSON TO CARRY: A VERIFY FINDING IS A HYPOTHESIS TOO.** Three rounds
  have each had at least one finding whose RECOMMENDED FIX was wrong; this round's
  S8 was disproved by writing it and watching it break a working shape.
- **(superseded) 1b-iv — ROUNDS 1-3 CLOSED.**
  ⭐⭐ **ROUND 3's LESSON, and it is the one to carry into 1b-iv: DERIVE A MESSAGE
  FROM THE RULING, NOT FROM THE EXAMPLE IN FRONT OF YOU.** Round 2's rewrite of
  `star-l4-mixed` fixed non-working remedies by writing new ones from its single
  driving example — where the star branch happened to be the keyless one — so
  every remedy was scoped to a *sibling* and was a non-action elsewhere. Q_U47
  already said the landing follows the REMAINDER; writing that would have
  produced the right advice first time. ⭐ And a **VACUOUS PIN LICENSES THE
  DRIFT**: the only pin over the keyed-star population had been dead since
  `d8776b66`, so nothing contradicted the specialisation.
 Round 2
  (`wf_87b2134b-d05`, 4 lenses + adjudicator): 13 lens findings → 7 survivors, 1
  killed, 6 deduped to 3 root causes; round 1's three fixes all HOLD.
  **F1 (BLOCKING) `73c56b2c` · F3+F4 `28b524d7` · F5+F7 `7ed99f39` · the three
  diagnostic majors + DEFERRED 122's un-park `6a64f2b2`.** ✅ **Full suite
  10187/488/0 at `6a64f2b2`** — the round-2 arc is regression-gated.
  ⭐ **Three things from the tail worth carrying**: (1) two SHIPPED remedies were
  non-actions and the round-1 record's proposed replacement was wrong too — all
  three caught by RUNNING them; (2) **major (a) was implemented, measured and
  REVERTED** — `(append path steps)` renders the right branch but `path` stores
  bare NAMES, so `y3{a^.b[0]*}` / `y3{a.b[0]*}` / `y3{a^r.b[0]*}` ALL rendered
  `a.b.0*`, naming a SIBLING as the failing branch ([DEFERRED 123](../DEFERRED.md);
  the lossiness is systemic — `branch-str` does it for every fail kind); (3) the
  MEASUREMENT broke again (bare `raco make driver.rkt` from the repo root, error
  swallowed by `| tail -3`, two runs on stale `.zo`) — gates now record **exit
  code AND output size**.
  Then **1b-iv** (nominal join + Q_U38's full collision rule + `*_`, plus the
  splice-vs-component question Q_U47 left open — Q_U40's own `m{a* b}` is that
  case), **1b-v** the star-SUFFIX slice (`*-` · `*-_` · Q_U39's mint), **P4e-2**,
  then PF → P5 → PX → P6 → X.close.
  ⚠ **A LANDMINE, before it is stepped on**: DEFERRED 117 says widen the L4 gate
  to `/deep`, but `star-l4-mixed`'s diagnostic renders its label with the OLD
  top-level test — widen the gate without the `findf` and it hands `#f` to
  `pp-select-branch` on the error path, the whole-file-abort shape this track has
  already shipped once. **Move both in one edit.**
- **The rulings — do not revisit** (D4 §3, link by anchor). Newest first:
  ⭐⭐ **Q_U47** the join lands as the component **branch-minus-star-minus-`sₙ`**
  would produce — keyed if that remainder names a key, **keyless otherwise,
  including when it is empty**. One sentence, no exceptions, covering depth-1
  (unchanged), deep-keyed, deep-dissolved and ordinal-head. ⚠ It rules the
  landing for **VECTOR** contents only; whether a NOMINAL join splices its fields
  into the level or lands as one component is 1b-iv's — and Q_U40's own
  `m{a* b}` → `{:x 1, :b {:y 2}}` is exactly that unanswered case.
  ⭐ **Q_U48** `*-` is the star family's **TOTAL collapse** — flatten *and*
  collapse all ancestry, landing keyless at the top. Its semantics are FORCED,
  not chosen: the parallel-to-`^-` reading (keep the leaf key) would re-attach
  the very key the star's layer-delete removed, which is Q_U23's identity defect.
  It is to `*` what `^-` is to `^` (total vs selective — MEASURED, they coincide
  at depth 2 and separate at depth 3), it depends on Q_U47's keyless case, and
  its lexical surface is already FREE (`u*-` fuses and reaches
  `split-star-lexeme` today). Slice: **1b-v**, with `*_` and the `*-_` decision.
  ⭐⭐ **Q_U46** deep `*` = the PRECEDING step's layer, join lands BARE
  ("B-splice"); the two gaps are GUIDED ERRORS. ⭐ The deciding argument is
  MECHANICAL: a bare chain re-nests to a single-key map at every level, so the
  branch-root reading's contents are always exactly ONE thing — "join one thing"
  is the identity, i.e. it can never JOIN, only unwrap ancestry, which `^`
  already spells (the defect Q_U23 retired `.*` for). ⚠ It buys exactly TWO
  idioms: group-N-siblings (`two{a.{x y}*}`; `^` provably cannot reach it) and
  cross-element ω join with ancestry.
  ⭐⭐ **Q_U45** L★'s qualifier widens — `x{p₁* … pₙ*} ≡ x{p₁ … pₙ}*` holds with
  no intervening ω **AND** Map contents; it fails for vectors on **ARITY**. The
  surface-observable form is the better test: `m2{a* b}` legal vs `mm{zz* aa}` an
  L4 error — checkable WITHOUT computing the answer.
  **Q_U40** star attaches OUTER; sort follows the CONTENTS · **Q_U41** `*_`
  nominal-only · **Q_U42** same-key vectors CONCAT · **Q_U43** L4 sort check at
  typing · **Q_U44** canonical key order, `x{a* b* c*}*` recovers written order ·
  **Q_U38** collision REFUSED · **Q_U39** closer-adjacent `*_` is its own slice ·
  **Q_U36/Q_U37** the positive-list fuse and territory-scoped refusal.
- **Open / blockers**: DEFERRED **101** (unspellable carriers) · **103**+sibling ·
  **104** · **105** · **106–112** · **113** (`pp-expr`'s `'path` arm hardcodes
  `.`; its owed reproduction LANDED at B2 — `users.:tags*` — ready to schedule) ·
  **114** (`#p(a.b*)` + `get-in`'s first-class `<error>` at 0 errors) · **115**
  (L2's normal form is NOT legality-preserving across a star — P5 owns it; write
  the side condition BEFORE P5 builds the normalizer) · **116** (NEW — the
  bracket ordinal `m[0]` is redundant with `m.0` **only for a LITERAL index**;
  `v[i]` is dynamic indexing and `v.i` is an error, so the owner's redundancy
  premise is half falsified. It also does NOT simplify the star work — `.0`
  remains and the ordinal BRANCH head is a different kind. Three framings, none
  ruled; out of the P4e line).
  Chips: `task_204859b9` · `task_4c00d3f0` · `task_e1c15ee6`.
- **Watching** (carried; the live ones this session re-proved):
  1. **A record written from an implementation inherits its author's reading, and
     the next session executes the reading rather than the code** — 4 instances
     now, one of them mine one day later in the artifact written to prevent it.
     **Read the artifact the record describes before executing the record.**
  2. **The design's own enumeration under-counts** — 12th+ arc.
  3. **A green battery over a surface the battery does not spell is not
     evidence** — the verify found 5 defects under a green 497-case battery.
  4. **The MEASUREMENT can be the broken thing** — 5 data points; this session
     added the stale-`.zo` mutant and a confounded single-key probe.
  5. **Pins that assert KINDS never assert MESSAGES** — a raising format arm hid
     behind a blanket handler. The fix is a RENDER LOOP over the whole axis,
     which then caught two more wording defects. **Pin the class, not the
     instance.**
  6. ⭐ **THE OWNER ASKING TO SEE IT WORKED OUT IN EXAMPLES IS THE INSTRUMENT** —
     5 sessions running it found what no gate did; at Q_U46 it found three errors
     in my own analysis, including a claim I had already put in front of them.
  7. **Verify rounds do not converge** — budget for 2 and 3.
  8. **Premature verdicts from incomplete evidence** — NEW this session: I
     declared a workflow dead from a journal holding only its first phase; it had
     completed. Check the artifact, not the poll.

---

## LOG  (append per commit / decision / surprise — newest at the BOTTOM)

*(The P4e-1b arc's entries — i through viii — are in `2026-08-11_dailies.md`;
the P4e-1a arc's — xxvi through xxix — in `2026-08-10_dailies.md`. This file
opens at the 2026-08-12 working-day boundary, with 1b-iii attempt 3 as the
resume target.)*

### (i) THE 2026-08-12 INTERVAL OPENS — and re-reading the parked patch found the work order pointing at the wrong fix — `83ae4714` · `596ca323`

**The interval**: the 2026-07-28 standup's window closes at **364 commits over
15 days** — CIU T6 D4 from the lexical seams to a working broadcast and a
half-built flatten (P1 → P4e), alongside QTT P1–P7, the LET track, ARROW T1,
SolveCarrier, the two-spine convergence (**29% → 98%** agreement), a DEFERRED
clearing run (51/51c/52, 57–59, 61, 102) and a build-time recovery (warm
precompile **34.7s → 1.2s**). Standup + dailies created as a pair; the dailies
rolled at 616 lines with the STATE head copy-forwarded and the prior file's
accreted historical layers left behind rather than carried.

**⭐⭐ THE FINDING, and it is a regrounding finding rather than a coding one: the
work order I was about to execute mis-framed its own headline defect, in a
direction that named the wrong repair.** D4's attempt-3 list said the
`$postfix-star` parser arm should *"respect `cur` like its three siblings"*.
Reading the parked patch instead of the summary: **two** siblings respect `cur`
(the ω and `$dot-access` **continuation** arms); the third — `star-sym?`, the
fused `database*` band — also calls `(closed-acc)` unconditionally and is
**correct** to, because a bare name always starts a branch (it sits beside
`plain-key?`, which does the same). `closed-acc` is
`(if cur (cons (reverse cur) acc) acc)`, so calling it closes the branch in
progress and opens a new one holding only the star — which *is* the
branch-initial reading, i.e. operate on the subject.

So the defect is not a forgotten transplant. **The bare `$postfix-star` sentinel
is the one arm reachable in BOTH positions** — branch-initial as the outer
`$select-path` carrier, continuation when written in-block — **and it assumes
only the first.** The fix is a position decision; "copy the sibling" would have
been wrong. And it re-specifies the missing pin: it must spell a postfix star
**after a sub-block** (`vh{0.{0}*}`), where `cur` is non-empty — **not**
`vh{0*}`, where the star fuses into the lexeme and takes the `star-sym?` arm
instead. Corrected at the site and in the attempt-3 list, with the reasoning
recorded rather than the sentence quietly swapped.

**Also corrected**: *"parked at the session scratchpad"* no longer resolves —
the patches are in the **`311a1847-…`** session directory and the current
session's scratchpad is empty. Full path now recorded in D4, the STATE head and
the memory file. A future session would have looked in its own scratchpad,
found nothing, and rebuilt 427 lines from scratch.

**The shape worth naming, because it is *Watching 12* one level up**: that
track's own recurring lesson is *"I wrote a precondition into a comment and then
invalidated it a slice later without re-reading it."* This is the same failure
displaced onto a **record**: a summary written from an implementation inherits
its author's reading, and the next session executes the reading rather than the
code. **Read the artifact the record describes, before executing the record.**

**Gate**: docs only, no production code, no test delta. Two commits — the
interval pair, then the correction (kept separate so the correction is legible
as a correction rather than folded into a housekeeping commit).

**Next**: 1b-iii **attempt 3** — open with the mini-audit, then the four fixes
(the position decision + its pin · `symbol<?` on the keyword NAME · the
`star-not-yet` split into mid-branch/leaf/nominal · the `{:a :a!}` key set),
typing and reduction landing **atomically**, with the adversarial verify
delegated to a workflow and round 3 budgeted.

### (ii) THE ATTEMPT-3 MINI-AUDIT — the four-item list was NOT the work, and Q_U45 widens L★'s qualifier — `781b8f75`

**The audit** (`wf_bbf1169e-340`, 6 partitioned read-only facets + the adversarial
completeness critic, HEAD-pinned `7fd25f35`, ~1.55M tokens): **37 critic findings,
17 capture gaps, 16 R-lens targets.** Every load-bearing claim R-lens-verified on
the main thread before use. Three of the four recorded items stand; the list is a
**subset**, and one of the four rested on a sentence that does not exist.

**⭐⭐ [Q_U45](../2026-07-28_CIU_T6_PATH_SELECTION_D4.md#q-u45) [owner] — L★'s
qualifier widens.** The law had been cited constantly and **written down in one
piece nowhere**, which is how the gap survived three sessions; it is now stated
once. It fails for vectors on **ARITY, before order enters**: the distributed form
is an n-tuple of vectors, the result form one flat vector. The Map side is equal
only because a Map-valued branch star SPLICES its keys — the same operation the
result form's keywise join performs — **and every worked example behind the law
used Maps.** The non-equivalence turned out to be **Q_U44's mechanism seen from
the other side**, not a collision with it, which is why the amendment costs one
clause and no shipped behaviour.

**⭐⭐ THE OWNER'S OBSERVATION FOUND A LIVE DEFECT A FACET DID NOT.** Asked to
deliberate, the owner noted that in the vector cases every branch must be starred
while the Map case tolerates a mix. That is the **surface-observable form of
absorption** — a LEGALITY difference, checkable without computing the answer — and
tracing it through the parked patch shows `(list #f)` makes **`m2{a* b}` an L4
error at the parser**, i.e. the patch refuses **Q_U40's own headline expressivity
example**. Fourth session running that the thing which found the defect was the
owner asking to see it worked out (*Watching 15*).

**⭐⭐ THE POSITION RULE IS DERIVABLE, TOTAL, AND NEEDS NO `cur-subbed?` TEST.**
The reader mints only for a `*` byte-adjacent to a preceding **closer**, and a
block's `{` is an **opener** — so a bare `$postfix-star` can never be a payload's
first item. Hence `cur = #f` ⇔ the outer carrier ⇔ the subject; `cur` non-`#f` ⇔
in-block ⇔ cons onto the branch. **Six** arrival positions measured where D4
recorded one, including one after an ORDINAL step that appears in no record — a
fix keyed on `cur-subbed?` would have covered one of six.

**⛔ AND MY OWN CORRECTION FROM YESTERDAY IS FALSIFIED.** I wrote that `vh{0*}`
"fuses into the lexeme". Measured: nothing fuses — the mint needs a preceding
closer and `0` is a number token — so the in-block ordinal band is unimplemented
and its message tells the user to do what they already did. **Kept in place rather
than silently repaired**, because the pattern is the finding: I inherited a
reading instead of measuring it, which is the same failure the correction was
written to fix. *Watching 12 has a third data point, and it is mine, one day
later, in the artifact I wrote to prevent it.*

**Also found**: the third instance of the revert shape sitting untouched
(`select-branch-keyless?` still justifies its `#f` with the precondition the seat
migration deletes, so two classifiers in one file will give opposite sort answers
for the same branch, both pinned) · the mid-branch sentence D4 says "already
exists in reduction.rkt" **does not exist** — it is patch-only text, written from
the patch's perspective as if applied · the patch's headline new instrument is
**vacuous** (`p4e1-has?` ormaps the setup `def` lines, whose type contains the
regex) while its comment calls it the check that caught the real defect · Q_U44
names half of `make-record` · the fail-kind axis has a silent `[else #f]` with
four blanket handlers over it · the twins diverge on an empty layer and on `seen`
· `rrb-concat` already exists · the "SHARED walk" safety comment is false (zero
references in either twin) · `pp-expr` will echo the nonexistent `c{a}.*` · `*_`
has no postfix carrier at all.

**One audit claim DOWNGRADED**, and the reason matters: a facet traced a
whole-file abort whose precondition the position rule proves impossible. That is
**an argued-unreachable precondition living in an audit** — precisely the artifact
class that caused both reverts. It owes a comment at the four raise sites plus a
tripwire, not a note in a document.

**Gate**: docs only, no production code, no test delta. Probe run under
`scratch-run.sh` (one file, reaped); the parked patch `git apply --check`s clean.

**Next**: agree the attempt-3 **re-slice** with the owner — the audit's findings
do not fit one atomic slice, and *Watching 9* says mid-flight widening is where
this arc introduces defects.

### (iii) SLICE 1b-iii-A SHIPPED — the instrument, the preconditions, the ordering substrate, and a trapdoor — `f0d91853` · `43c91859` · `e38845a9` · `dd5d145c`

**The re-slice**: the audit's findings do not fit one atomic slice, and B is
irreducible — the seat migration forces the twins' arms; an arm that refuses in
one slice and joins in the next is the dual-path scaffolding 1b-ii refused to
build; and the join forces the classification, or `mm{zz* aa}` reaches
`make-record` with a `#f` label and raises. So **A absorbs everything star-free
and B gets pre-pinned instead of made smaller.** Thesis: *you cannot verify
semantics with a broken instrument and a lying echo.* Battery **481 → 484**,
every item inert for star behaviour. Full record at D4
[§5.P4e-1b-iii-A](../2026-07-28_CIU_T6_PATH_SELECTION_D4.md#p4e-1b-iii-a).

**⭐⭐ THE INSTRUMENT WAS VACUOUS TWICE OVER, AND MY FIRST PIN FOR IT OVER-CLAIMED.**
`p4e1-has?` ormapped every output including the setup `def` lines. Measured on the
attempt-2 patch's headline pin against the STARLESS control: the setup line
`rowsv : [PVec {:tags [PVec Int]}]` matches `[PVec Int]`, and so does the WRONG
answer `[PVec [PVec Int]]`. Fixed **loudly** — restrict to the final command's
output so vacuous pins redden rather than needing to be guessed at. ⭐ Then my own
pin asserted the fix kills that regex, which it cannot: the containment half is a
weak REGEX, not a weak instrument. **Running it is what caught that**, same shape
as 1b-ii's Q_U43 arm placement. The pin now separates the halves and pins the
containment one as STILL LIVE.
⚠ Stated precisely, because it is tempting to over-read: exactly ONE pin reddened
across 35 call sites and it was mine. That does **not** vindicate the other 34 —
the loud fix only catches a pin depending EXCLUSIVELY on setup output.

**⭐⭐ THE STRUCTURAL FIX FOR THE REVERT SHAPE.** Both reverts came from a
precondition written into a comment and invalidated a slice later. Two such
comments were live in `syntax.rkt` and the seat migration deletes the reason each
gives — so they were rewritten **while still inert**, before the migration, as a
CHECKABLE INVARIANT ("every production consumer pre-checks for a star", all three
named with the grep that audits them) rather than a precondition anyone has to
remember. A **third** instance turned up in the battery itself ("when 1b-iv makes
it reachable…" — the migration is 1b-**iii**) and was corrected too. The gate
comment now also **forbids the parked patch's `(list #f)` in writing**, with the
reason: it refuses `m2{a* b}`.

**Also**: canonical key order gets ONE definition, sited so the twins cannot
drift — and the pin's KEY SET is the finding, not the assertion. `{:a :a!}`
separates `symbol<?` from the struct-display order where attempt 2's `aa/mm/zz`
agreed under both; the pin carries a MUTATION GUARD asserting they disagree, so
it reports if it ever stops being evidence. **Mutation-tested: attempt 2's exact
`(format "~a" …)` ordering turns it red.** · The FAIL-KIND axis was a silent
trapdoor (`[else #f]`, measured UNREACHABLE — all 14 kinds have arms — and 1b-iii-B
adds three); made total, and it **reports rather than raises**, because a raise in
the message formatter is a whole-file abort on the `infer` path.

**⛔ AND ONE AUDIT FINDING DID NOT REPRODUCE, so it was FILED not fixed.**
The `pp-expr` `'path` separator: the code shape is verified (it hardcodes `.`
while `pp-select-branch` honours `first?`, the identical bug fixed one arm over at
P4d-0 slice 5), but the audit's user-visible claim — `c{a}*` echoing as `c{a}.*` —
**did not reproduce**: my probe's diagnostics never route through that arm.
[DEFERRED 113](../DEFERRED.md) records the shape as verified and the rendering as
NOT established, with what is owed before a fix. *Third time this arc that
checking an audit's consequence changed the disposition.*

**Gate**: battery 484 · 0 FAILURE / 0 ERROR by direct require · 2459 lines (the
run HAPPENED) · neighbourhood 326 then 325 tests, `[5/5]` and `[6/6]`, all pass ·
one mutant installed, killed, reverted, tree re-verified green.

**Next**: **1b-iii-B**, the atomic one — parser position rule across four bands,
`star-not-yet-message` retired definition-and-all, both twins' arms at MATCHING
positions, the layer walk, the vector concat in canonical order, the three failure
kinds, and typing's L4 sort check. Failing-test-first over the full pinned set
BEFORE any of it is written, then the delegated adversarial verify with rounds 2
and 3 budgeted.

### (iv) SLICE 1b-iii-B1 SHIPPED — the flatten's semantics land in both twins, inert; and my mutant protocol destroyed the work once before it shipped — `818e07de` · `ae8707a6` · `1c0c5123`

**The star MEANS something now.** Both twins carry the real join at the SAME
position (FIRST in each cond, ahead of `col`/`keyless?` — round 1's abort was
this pair diverging), landed INERT: the parser still refuses every spelling, so
the E2E surface is **byte-identical to HEAD** (verified on the seven-position
probe). 1a-ii's Tier-O pattern, not the refusing-arm scaffolding 1b-ii rejected.
Before it: `818e07de` parked the arrival-position pin set (six positions, the
derived `cur` rule, the tripwire deliberately left UNWRITTEN with what it owes
recorded — shipping a plausible-looking unverified call is the hedged-instrument
failure), and `ae8707a6` corrected my own "B is irreducible" claim.

**EIGHT fail kinds where attempt 2 had one** — mid-branch · leaf (PERMANENT,
says so) · nominal / hetero / ω-tuple (each not-yet for its TRUE reason, keeping
the established substring for B2's pin migration) · open-row (Q_U38 conservative
on the tail + presence — the guards the audit said were missing) ·
synth-positional (Q_U41) · l4-mixed (Q_U44's obligation: `m{name tags*}` AND the
reorder BOTH take a guided error naming the star spelling, at
`select-level-components` — Q_U43's actual migration). Labels render the user's
spelling via `pp-select-branch`; slice A's totality `[else]` backs any miss.
**Twin contract decided rather than defaulted**: `seen` FORWARDED in both;
typing deliberately more conservative in two cells (empty layer, hetero), the
safe direction, documented at the site; reduction's refusals all
`(return (expr-panic …))`, never `error`.

**Gate, failing-test-first end to end**: 10 unit pins written FIRST, all failing
at HEAD as `select-step-kind-unhandled` raises — the right reason; battery
484 → **494**, 0/0; EXACT-type joins; both L4 orders; both tripwires;
neighbourhood 416 `[8/8]`; **mutation-tested per twin**, each twin's pins
reddening exactly their own side.

**⭐⭐ AND THE MEASUREMENT WAS THE BROKEN THING, TWICE, BEFORE THE CLEAN RESULT**
(*Watching 8*, 4th and 5th data points — same session, same protocol):
1. Mutant B's first run reported **typing** raise signatures
   (`select-below-field` — a typing-only walker name). Cause: `git checkout`
   restores SOURCE without recompiling, so mutant A's `.zo` was still live —
   mutant B was effectively A+B. **The raise signature is what exposed it.**
2. Worse: `git checkout` as mutant-restore **DISCARDED THE UNCOMMITTED
   IMPLEMENTATION** — both twins' arms, gone silently. Caught only because the
   post-restore clean rebuild went red where it should have been green.
   Re-applied from the session record and re-verified from scratch.
**Never `git checkout` a file to undo a mutant over uncommitted work — invert
the mutant in place, and force the rebuild.** Slice A's cp-backup protocol was
right; I "simplified" it and paid twice.

**Next**: **1b-iii-B2** — the parser position rule across all four bands (the
derived `cur` rule), `star-not-yet-message` retired definition-and-all,
uncomment the parked pins (⚠ pin A's expectation needs rewriting first — its
subject has Map contents, which is 1b-iv territory), migrate the seat-sensitive
1a pins with reasoning recorded, then the delegated adversarial verify with
rounds 2 and 3 budgeted.

### (v) SLICE 1b-iii-B2 SHIPPED — THE FLATTEN IS LIVE. Three attempts, two reverts, and `rowsv:tags*` means `@[1 2 3]` — `d5934397`

**The seat migrated.** The four parser star bands MINT `(@star cont)`; the
refusal lives at typing, the first seat that can see the layer's sort. Measured
live: the spec's own ω·ω→ω · Q_U44's canonical order AND the owner's recovery
spelling · the keyless ravel · the L4 guided error in BOTH orders naming
`tags*` · the leaf PERMANENT · the nominal honest not-yet · **round 1's
whole-file abort and round 2's silent wrong answer both dead** (`mm{zz*.q^}` →
per-command star-mid-branch; `vh{0.{0}*}` refuses on ITS branch's layer).

**The position rule landed as DERIVED, not transplanted** — `cur = #f` ⇔ the
outer carrier ⇔ the subject; non-`#f` ⇔ cons onto the branch; position H
decided (the star PRESERVES `cur-subbed?` — it does not unseal the sub-terminal
rule). `star-not-yet-message` retired definition-and-all. One wording defect
caught live and fixed mid-slice: nat-record contents took "Map-valued"; the
nominal bucket now admits keywise-joinable contents only.

**Four pins migrated seat, found EMPIRICALLY** — run the battery, see what
reddens — and it found exactly C31's predicted class: the bcast fused-star pin
(the miss now names `:tags`, proving the token SPLIT) plus three not-yet
assertions on LEAF-content subjects. ⚠ My first migrated cut of the bcast pin
was OVER-BROAD: it flagged the message's ECHO rendering `tags*`, which is
capture-fidelity WORKING. Corrected to the real discriminator (a FIELD named
`tags*`). ⭐ And that echo handed **DEFERRED 113 its owed reproduction**:
`users.:tags*` — `pp-expr`'s `'path` arm hardcoding `.` before an ω step, live
on a real diagnostic. Entry updated: reachable, testable, ready to schedule.

**Gate**: battery 494 → **497**, 0/0 · neighbourhood 416 `[8/8]` · acceptance
file 0 errors · E2E probe derived-by-hand before running · adversarial verify
DELEGATED (`wf_30884180-93c`: five attack axes — values, aborts, twin
agreement, message truth, starless regression — then independent reproduction
of every blocking/major claim, with PRE-EXISTING as an explicit verdict) ·
full suite queued behind the verify so the two don't contend.

**Next**: act on the verify (rounds 2 and 3 budgeted — this track has never had
one round suffice), full suite as the regression gate, then close 1b-iii and
open **1b-iv** (nominal semantics + Q_U38's collision refusal + `*_`).

### (vi) VERIFY ROUND 1 — five distinct defects, the battery green over every one; fixed, shielded, and the suite is FRESH-GREEN — `5e4a3338`

**The pattern held a fifth time**: the delegated verify (5 attack axes →
independent reproduction of all 12 serious claims: 9 REAL, 3
misattributed-with-real-observations, 0 not-reproducible) found five distinct
defects under a 497-case green battery.

**F2 (mine, B1)** — star-omega-tuple's format arm RAISED on every render (one
`~a`, two args); the blanket hint handler swallowed it to the bare generic. *A
raising arm is worse than the trapdoor slice A closed*, and it silenced Q_U40's
own ravel. Root cause of the green battery: pins asserted KINDS, never
MESSAGES. Fixed — and the new RENDER LOOP over all ten kinds went red TWICE
more before commit (star-open-row never named `*`; l4's remedy was abstract).
**The instrument that finds a class, not the instance.**

**F1/F7/F8 — the depth-≥2 divergence, a DESIGN-SENTENCE-VS-RULING contradiction
the implementation inherited.** D4's algorithm sentence (the branch-ROOT layer)
and Q_U40's rule (the PRECEDING step's layer) coincide at depth ≤ 1 — every
worked example — and diverge at depth ≥ 2, where B2's mint made it REACHABLE:
an ω behind a key returned the RAW field at 0 errors; `mm3{zz.q*}` took a false
nominal. **Shielded, not fixed** (Watching 9 — no mid-flight widening): both
twins refuse prefix ≥ 2 with a guided message naming the unruled question;
depth ≤ 1 stays live. **WHICH LAYER a deep flatten deletes = the next owner
question** (D4 § the deep-prefix question), for the 1b-iv co-design.

**Also**: union contents branded PERMANENT → routed to hetero · F3 (match
extra-pattern abort, star-road) PRE-EXISTING → noted at DEFERRED 103/108 ·
`#p(a.b*)` + `get-in`'s first-class `<error>` at 0 errors, PRE-EXISTING →
**DEFERRED 114** (register → 115).

**Gate**: battery 497 → **498** 0/0 (red twice in between, each a real wording
defect) · fix-probe live (five guided refusals rendering branch spellings;
`rowsv:tags*` and `mm3.zz.q*` still work) · **full suite 10165 / 488 / 0,
`[488/488]`, fresh** — the regression gate holds.

**Next**: the deep-prefix ruling (owner) · verify round 2 if wanted before
1b-iv (the pattern says round 2 attacks round 1's fixes — the shield and the
render loop are the fixes) · then **1b-iv**.

### (vii) Q_U46 RULED — a deep `*` deletes the PRECEDING step's layer and lands BARE; the shield becomes the implementation target

**The design round the owner asked for**, run as an options panel
(`wf_f8461dda-659`, 4 propose axes + 2 critics + synthesis, ~1.64M tokens) over
grounding I measured first. **Every load-bearing claim was re-measured on the
main thread**, and doing so corrected me three times.

**⭐⭐ Q_U46 [owner]**: for `s₁.s₂ … sₙ*` the star deletes the layer **sₙ** made,
joins its contents, and the result lands as the **value** of the surviving
`sₙ₋₁`. `cfg{db.hosts*}` → `{:db @[1 2]}` · `two{a.{x y}*}` → `{:a @[1 2 3]}` ·
`rows{k:s*}` → `{:k {:s @[1 2 3]}}`. Gaps ruled **guided errors**: an ordinal
prefix has no preceding-step layer (Q_U2), and L2's normal form across a star
(→ [DEFERRED 115](../DEFERRED.md), P5's).

**⭐⭐ THE ARGUMENT THAT DECIDED IT IS MECHANICAL.** The branch-root reading takes
the layer to be the WHOLE re-nest — and a bare chain re-nests to a SINGLE-KEY map
at every level, so its contents are always **exactly one thing**. "Join one
thing" is the identity. **Under branch-root a deep star can never join; it only
unwraps one level of ancestry, which `^` already spells** — the exact defect
Q_U23 retired `.*` for. Under Q_U46 the innermost layer's contents can be many,
so the star genuinely joins.

**⛔ THREE CORRECTIONS TO MY OWN ANALYSIS, all caught by measuring:**
1. **I declared the panel DEAD.** It had completed seconds after I polled a
   journal holding only the propose phase. A premature verdict from incomplete
   evidence — the session's own recurring lesson, and I walked into it.
2. **"At depth 3 it deletes THREE layers"** — wrong, and the truth is worse for
   branch-root: it deletes exactly ONE, always the ROOT one. Mis-aimed, not
   over-eager. My §3.5 "exactly one layer" argument against it was therefore
   void; the duplication argument is the real one.
3. **"`{:db @[1 2]}` is not expressible at HEAD"** — FALSE. `cfg{db.hosts^..}`
   (Q_T8's parent-collapse) already spells it, MEASURED. My expressivity case
   rested on that gap. The real prize is exactly TWO idioms: group-N-siblings
   (`two{a.{x y}*}`, and `^` provably cannot reach it — `^` binds to the segment
   on its left, which is a `}`) and cross-element-ω-join-with-ancestry.
   ⚠ Also corrected: `rows{k:s*}` is `{:k {:s @[1 2 3]}}`, not `{:k @[1 2 3]}` —
   the block band's ω KEEPS the inner key, so the join is keywise.

**Also recorded**: D4's own algorithm sentence is SUPERSEDED at depth ≥ 2 and
marked so at its site — it is the sentence the implementation inherited, correct
at depth ≤ 1 (where every ruled example lives, which is why the divergence was
invisible for three slices).

**Next**: implement Q_U46 — replace the `star-deep-prefix` refusal with the
preceding-step layer walk in BOTH twins (atomically), keep a guided refusal for
the ordinal-prefix gap, pin the two new idioms and the band-agreement
(`cfg{db.hosts*}` as the projection of `cfg.db.hosts*`). Then **1b-iv**.

### (ii) THE 1b-iii-C GROUNDING AUDIT — Q_U47 + Q_U48 RULED, the sequencing set, and the audit refuted my own implementation hypothesis with a measurement

**The audit** (`wf_00c82b4e-219`, 6 partitioned read-only facets + the
completeness critic, HEAD-pinned `0feca574`, ~1.55M tokens, 0 errors) — **34
critic findings**, all in disposable context. Every load-bearing claim
R-lens-verified on the main thread before use, which is what caught the two
places the record was wrong and the one place I was.

**⭐⭐ MY OWN HYPOTHESIS WAS REFUTED, AND BY A MEASUREMENT RATHER THAN AN
ARGUMENT.** Between launching the audit and reading it I had proposed
implementing Q_U46 by reusing the STARLESS branch machinery with a substituted
leaf — five data points fit it. Measured: `cfg{db.{hosts*}}` → `{:db @[@[1 2]]}
: {:db ⟨[PVec Int]⟩}`, **legal today**, and it is the **B-tuple** — the option
Q_U46 explicitly rejected. The mechanism is `select-below-field`'s memq arm
delegating to the branch walk and then calling `select-assemble-row`, whose
keyless-first fork mints `(make-record 'nat …)`. So the outer re-nest is free,
but **the BARE landing is the missing piece and composition cannot supply it**.
The in-tree precedent for what IS needed is the `bcast` arm, whose comment in
BOTH twins says verbatim why: it would *"wrap the result in a spurious level"*.
*A hypothesis that fits five data points can still be refuted by the sixth —
and the near-miss spelling was already in the language, waiting to be run.*

**⭐⭐ AND THE SLICE HAS NO INSTRUMENT.** Deleting the shield produces a **GREEN
WRONG ANSWER**, not a failure: `rows{k:s*}` returns the raw field keyless at 0
errors. Five of the six deep-shape pins assert `#rx"."`. Every star unit pin
uses `'path`, whose arm discards the key — structurally blind to the very flip
Q_U47 rules on. Hence C's ordering: **the depth-≥2 assertions are written before
either shield is touched.**

**Q_U47 [owner]** — the join lands as the component branch-minus-star-minus-`sₙ`
would produce: keyed if that remainder names a key, keyless otherwise, INCLUDING
when empty. I measured the classifier directly: `'(db)` → `(db)` ·
`'((@key db dissolve))` → `(#f)` · `'((@ord 0))` → `(#f)`. ⭐ The two candidate
rules DIVERGE on dissolve — drop-star-only gives `(hosts)`, fabricating a key the
output does not carry — and **`'()` RAISES**, which is the depth-1 case, so a
literal "drop two and ask" implementation crashes on the commonest input. ⚠ The
ruling covers VECTOR contents; Q_U40's own `m{a* b}` is depth-1 NOMINAL, where
the join splices, and that is 1b-iv's to answer.

**Q_U48 [owner]** — `*-` is the total collapse. The owner proposed it from the
`^-` analogy ("pick the key and bring it to the top, flatten"), and measuring the
caret family showed the analogy is exact: `dd{p.u^-}` ≡ `dd{p^.u}` at depth 2,
`e3{a.b.c^-}` ≡ `e3{a^.b^.c}` at depth 3, while `e3{a.b^.c}` → `{:a {:c @[1 2]}}`
proves the selective form is not redundant. ⭐ **`*-`'s semantics are FORCED**:
the literal parallel to `^-` (keep the leaf key) would re-attach the key naming
the layer the star just deleted — Q_U23's identity defect. And the lexical
surface is already free: `u*-` fuses and reaches `split-star-lexeme` today with a
guided star error, exactly where `*_` sat before Q_U39.

**The sequencing** (owner-delegated): **C1** machinery inert → **C2** seat lift +
the two holes C2 opens → **1b-iv** nominal → **1b-v** the star-suffix family.
Reasoning in D4 §5.P4e-1b-sequencing. C repeats the B1/B2 shape because that is
the shape that finally worked after two reverts.

**⚠ TWO HOLES C2 OPENS, neither in any prior record.** The duplicate-output-key
check **has no seat anywhere** — `select-branch-top-keys` returns `'()` for star
branches, typing still asserts the parser owns uniqueness, and nothing checks
downstream, so `cfg{db.hosts* db}` becomes a silent last-win the moment a deep
star's key survives. And `star-l4-mixed` acquires a lying population (a deep
KEYED star told it "contributes a KEYLESS component").

**Also corrected**: band agreement is STRUCTURAL, not algorithmic — Q_U13's NEST
means the path band never builds a multi-step prefix, so `cfg.db.hosts*`
succeeding proves nothing about the reading, and the bands genuinely DISAGREE on
ω. The `seen` twin-contract claim in the record was stale (both forward; B1 fixed
it). And **DEFERRED 116**: the owner's `m[0]` deprecation premise is half
falsified — `v[i]` is dynamic indexing with no dot-band spelling, and the
deprecation would not simplify the star work anyway.

**Gate**: docs only, no production code, no test delta. The working-directory
trap fired once (a stale `cd` from an earlier call) and failed LOUDLY, which is
the good case.

**Next**: slice **1b-iii-C1**.

### (iii) SLICE 1b-iii-C1 — the shared join is HOISTED and returns BARE, and hoisting it is what made the reduction twin pinnable at all — `4f30a171`

**What C1 is**: a pure extraction whose entire content is a contract — the join
returns the joined type/value **BARE**, with no component wrapper.
`star-join-type` (typing-core) and `star-join-value` (reduction) now sit at
module level, both exported; each twin's existing caller wraps keyless, which is
its remainder-empty case and the behaviour B2 shipped.

**Why bareness earns a pin of its own.** [Q_U47](../2026-07-28_CIU_T6_PATH_SELECTION_D4.md#q-u47)'s
landing belongs to the CALLER, because each caller IS the branch remainder's own
arm. A helper that wrapped its own result would force ONE landing on all three,
and the wrapper it would naturally choose is the keyless one — which assembles
into the **B-tuple Q_U46 rejected**. Not hypothetical: `cfg{db.{hosts*}}` is
legal today and gives `{:db @[@[1 2]]} : {:db ⟨[PVec Int]⟩}`. A future re-wrap is
a silent regression to a rejected design.

**⭐⭐ THE SLICE RE-SCOPED ITSELF, and only because I tried to write the pin.**
C1 was to land both star-TAIL arms inert, B1-style. Typing has a seam (I exported
`select-below-field`). **Reduction has none** — both its walks live inside
`select-reduce`'s ~470-line scope and neither is exportable, while
`select-reduce` itself refuses a deep branch at the shield. So that C1 would have
shipped the typing arm directly pinned and the reduction arm **mutation-tested
only**, at the seat whose own comment says landing either alone is *"not a
half-measure but a REGRESSION"* and where a twin-order divergence already cost a
whole-file abort. Verifying one twin to a lower standard than the other is the
wrong trade, so C1 became the EXTRACTION and the arms moved to C2. The hoist also
escapes the `sort` shadow the audit independently flagged. *The plan survived
until it met the test, which is earlier than it usually fails.*

**Doc-truth, and it was the THIRD instance of this arc's signature defect**:
syntax.rkt's *"THE KIND IS INERT AT 1b-ii"* header had been false since B2
(`d5934397`) and is the first thing a reader grounding on the star opens. Its two
siblings were `select-branch-top-keys` and `select-branch-keyless?` — both of
which got 1b-iii reverted. Corrected in place, with a line telling the next
person the comment is a PRECONDITION.

**Gate**: battery 498 → **501**, 0 FAILURE / 0 ERROR by direct require (every
pre-existing pin green IS the regression proof — B1's pins already exercise
depth-1 through both twins) · **mutation-tested PER TWIN**, inverted in place
rather than checked out: re-wrapping typing's return gave 3 FAILURE + 3 ERROR and
reduction's 2 FAILURE + 4 ERROR, the C1 pin among them each time and for its
stated reason · **E2E inertness** — the star surface is byte-identical to the
pre-C1 measurement · neighbourhood **318** `[4/4]` · acceptance **0 errors**.
⚠ **Both mutant runs exited 0.** The exit code is not the signal; counting ERROR
alongside FAILURE is.

⚠ Both new pins use a **two-content** layer deliberately: a one-content layer —
every key-terminal chain, `cfg{db.hosts*}` included — makes the concat an
identity and cannot discriminate a broken join.

**Next**: **C2** — the tail arms (now thin), the pre-check narrowed to
remainder-empty, both shields out atomically, the three gap dispositions, the
`vh{0.{0}*}` flip, the dup seat's first arm, the L4 message, and the E2E battery
written BEFORE either shield moves.

### (iv) SLICE 1b-iii-C2 — THE DEEP FLATTEN GOES LIVE, and Q_U47 turned out to be implemented by NOT implementing it — `d8776b66` · `68014124`

**The feature**: `cfg{db.hosts*}` → `{:db @[1 2]}` · `two{a.{x y}*}` →
`{:a @[1 2 3]}` (the group-N-siblings idiom Q_U46 says the ruling buys) ·
`cfg{db.{hosts ports}*}` → `{:db @[1 2 80]}` · depth 3 works · `cfg{db^.hosts*}`
and `vh{0.{0}*}` land keyless. Both twins, atomically.

**⭐⭐ THE FINDING: NOTHING COMPUTES THE LANDING.** The pre-check narrows, a deep
star falls through to the head dispatch, one tail arm returns C1's shared join
BARE, and whichever caller catches it supplies the wrapper. Q_U47's rule — *"the
component the remainder would produce"* — then holds because **the caller IS that
remainder's arm**. Measured consequence: two of the three landings needed **no
code at all** (the dissolve and ord-branch arms already wrap keyless), and
`select-below-components`, which I had scoped for a twin arm, needed none.

**⭐ AND THE ARGUMENT THAT DECIDED Q_U47 ALMOST SHIPPED UNDEMONSTRATED.** I told
the owner Q_U44's written-order recovery generalizes to depth, then probed it
with `:p`/`:q` written p-first — where canonical and written order **coincide**.
It printed the same vector for both spellings and proved nothing. Re-probed with
`:zz`/`:aa`: `w{zz.u* aa.v*}*` → `@[3 4 1 2]` vs `w{zz^.u* aa^.v*}*` →
`@[1 2 3 4]`. Now pinned. *An ordering claim needs keys whose orders differ, or
the pin is vacuous* — a sibling of slice A's `p4e1-has?` finding, one layer up.

**The two holes, measured open then closed.** `cfg{db.hosts* db}` →
`{:db {:ports @[80], :hosts @[1 2]}}` at **ZERO errors** before the seat existed
— the star's surviving `:db` silently overwritten by a sibling. New kind
`star-dup-key` (added to the render loop in the same commit; it cannot
self-report an unlisted kind). And `star-l4-mixed` stopped asserting the star is
the keyless branch, which is false the moment a deep star lands keyed.

**Two defects the run caught, both mine, both from assuming the twins are
symmetric**: reduction's `@sub` arm matches any sub-headed step list and panics
on anything following it, so the tail arm had to go to the FRONT of its cond
(typing's sub arm has a `(null? (cdr steps))` guard and did not) — the same edit
was wrong in one file. And the ordinal gap's reaching spelling is `a[0]*`, not
`a.0*`, which is a parse error; that is also the spelling DEFERRED 116 proposes
deprecating, so the deprecation would make this ruled gap unreachable.

**Gate**: battery 504 → **511**, 0 FAILURE / 0 ERROR · neighbourhood 348 `[5/5]`
· acceptance 0 errors · the six parked pins uncommented and green ·
`vh{0.{0}*}`'s B2 pin FLIPPED from refusal to keyless success, deliberately, with
its reasoning at the site.

**Next**: the adversarial verify (`wf_bf354f2d-675`) told to attack THE FIXES,
with the full suite queued behind it. Rounds 2 and 3 budgeted.

### (v) C2 VERIFY ROUND 1 — three BLOCKING regressions, and all three came through the ONE route a comment of mine declared safe — `eee68262`

`wf_bf354f2d-675`, 4 adversarial lenses + adjudicator, told to attack THE FIXES.
Three blocking findings, all C2 regressions, all re-measured on the main thread
before I acted. **The battery, the neighbourhood and the acceptance file were
GREEN over every one** — the arc's lesson, paid again and on schedule.

**⭐⭐ ONE ROOT CAUSE.** `below-components` / `select-below-components` is a
SECOND route into the branch machinery, reached through a dissolved head; C2
treated only the `below-value` route. And the sentence that closed the question
is the one that caused it — I wrote *"`select-below-components` needs NO twin of
this arm … **Verified, not assumed**"*, having verified TYPING's twin and never
opened reduction's sibling. **The confidence was the defect**: a hedge would have
invited a second look.

1. **Order-dependent silent wrong answer.** `s7{a^.{b.c*} b}` → `{:b @[9]}`,
   `s7{b a^.{b.c*}}` → `{:b @["x" "y"]}` — same selection, branches swapped,
   different value AND type, zero errors. The dup gate saw only TOP-LEVEL steps,
   so a star inside a sub-block was invisible. *Verbatim the failure that gate
   was added to prevent, one nesting level down.*
2. **Silently answered the question Q_U46 RESERVED.** `y{a[0]*}` refused,
   `y{a^[0]*}` answered — the ordinal check lived at one seat and the dissolved
   spelling reaches the layer through the other. A semantic commitment on a
   reserved question, made by accident, contradicting a refusal printed two lines
   earlier in the same run.
3. **Twin divergence.** `cfg{db^.{hosts ports}*}`: typing accepted, reduction
   panicked with a message asserting the shape impossible — a shape C2 made
   reachable. ⭐ The parser never forbade it; the SHIELD did.

**⭐ The enumeration that hid it was wrong on its face.** `star-join-type`'s
header — the argument justifying the whole C1/C2 design — listed THREE callers
and named `select-below-components` as the third. There are two. *An enumeration
wrong about ITSELF is how a seat gets skipped.*

**Also**: the `select-star-step?` precondition header was stale for the whole of
C2 — the **fourth** instance of that shape, and the **second** time in that same
comment. Rewritten to state the CONTRACT (the landing rule) rather than the STATE
(which shields exist), so it cannot rot the same way again.

**⚠ Two honest gaps, recorded rather than smoothed.** The dup fix is PARKED
rather than pinned — probe-verified, but its pin cannot run under `p4e1`'s string
path (DEFERRED 121), and an unpinned fix is how defect 1 shipped. And my first
cut of that pin interpolated only the source, not the OBSERVED output, so when it
went red it could not say what came back — *Watching 8* costing a cycle inside
the pin written to close a defect.

**Filed**: DEFERRED **117** (the L4 gate shares the dup gate's blindness —
deliberately not widened here per *Watching 9*; widen both together) · **118**
(the block-band ω assembles instead of extracting — and it is why one C2 pin is
green for the wrong reason, so it will FLIP on an unrelated correct fix) ·
**119** · **120** · **121**.

**Gate**: battery 513 live (+1 parked), 0/0 · neighbourhood 261 `[3/3]` ·
acceptance 0 errors · all three reproducers re-run and correct.

**Next**: round 2, the three diagnostic majors, and the full suite.

### (vi) C2 VERIFY ROUND 2 — round 1's fixes generated a fresh defect class, twice, and the BLOCKING one was the same class a third time in a sibling function — `73c56b2c` · `28b524d7`

**The run** (`wf_87b2134b-d05`, 4 lenses + adjudicator, ~1.3M tokens, 0 errors):
13 lens findings → **7 survivors**, 1 KILLED by the adjudicator's own re-run, 6
deduped to 3 root causes. Round 1's three fixes all HOLD — I re-measured those on
the main thread before launching, and every survivor before acting.

**⭐⭐ I ASKED THE ADJUDICATOR OUTRIGHT WHETHER ROUND 1's SHAPE REPEATS. It
answered YES, TWICE, and opened both siblings.** That question was worth its
place in the prompt: both answers became findings.

**F1 (BLOCKING, fixed `73c56b2c`) — the same class a THIRD time, in the sibling
function.** `split-caret-lexeme` has guarded *"the CONT contains a `*`"* since
DEFERRED 90, whose own comment records the class being under-counted once
already. The mirror — *"the NAME contains a `^`"* — was never written in
`split-star-lexeme`. Silent wrong answer, because a caret-suffixed field name is
spellable: `q{db.hosts^*}` → `{:db @[99]}` at zero errors, the contents of a
field the user never wrote. ⚠ Only ONE of three bands was safe and only by ARM
ORDER; **DEFERRED 90 declares the requirement "✅ DISCHARGED" and every worked
example in it is branch-initial** — the one band where the guard is reached. The
dot band is a **C2 regression** (the shield used to catch it); the ω band is
pre-existing. Guard went in the splitter, not the arms: there it covers every
caller by construction, and the arms' ORDER is what failed.

**F3 + F4 (fixed `28b524d7`) — round 1's fix generating round 1's own defect
class.** The hoist existed so the ordinal rule could be asked at EVERY
layer-computing seat; it reached both typing seats and **neither** reduction one
— zero hits in reduction.rkt. The unguarded seat carried the invariant as
**PROSE**, on the blank line C2's removed shield had occupied, six lines under a
header forbidding the excuse, while its sibling twelve lines away already panicked
on the same condition. F4 was the same story one payload-kind over:
`select-branch-has-star?/deep` claimed "ANYWHERE" and saw only `(@sub …)`.

**⚠ HONEST BOUND, and I want it in the log rather than only the commit**: F3(a)
is LATENT and I **could not exercise it**. I disabled typing's guard to make
reduction's fire and `y{a^[0]*}` still refused, via a *different* typing arm. Two
independent typing routes prevent the shape; the new guard is unreached. Correct
on the twins' doctrine, unexercised, and load-bearing only at the next shield
removal — which is precisely how round 1's defect #3 became reachable.

**⭐ AND THE RECORD'S OWN FIX FOR AN OWED MAJOR IS WRONG — caught by measuring
instead of adopting.** The round-1 record proposes the parser's *"group same-head
branches into a sub-block"* remedy for `star-dup-key`. Measured:
`cfg{db.{hosts* ports}}` → an **L4 error**. Following the record would have
replaced a non-working remedy with a different non-working one — the defect class
being fixed, repeated. Verified-working instead: `^k'` rename for the dup, and
**ADDING** a caret for the both-starred L4 case. ⭐ The dup remedy's defect is
STRUCTURAL: under Q_U47 the surviving key comes from the remainder, which the
star never touches, so dropping the star can never resolve a collision.

**Also**: DEFERRED **122** filed — a parse-level state leak drops `def s0|s1|s7`
and **invalidates DEFERRED 121's recorded diagnosis** (it is process-state
pollution, not a Level-2/Level-3 harness gap; the identical string binds fine in
a clean process). A one-character rename un-parks round 1's headline regression
pin today. Mechanism NOT located, and recorded as such.

**Gate**: battery 513 → **517**, 0 FAILURE / 0 ERROR by direct require ·
failing-test-first with the observed output captured in the failure message ·
mutation-tested (F1's guard neutered → exactly the two defect pins redden,
neither control) · E2E all three bands · round 1's three reproducers re-run and
still correct · neighbourhood 318 `[4/4]` · acceptance 0 errors.
⚠ The working-directory trap fired TWICE this session and failed loudly both
times. Frequent enough now that "run from the repo root" wants verifying in the
command, not remembering.

**Next**: F5 · F7 · the three diagnostic majors · DEFERRED 122's rename ·
**then the full suite**.

### (vii) C2 VERIFY ROUND 2's TAIL — two shipped remedies were NON-ACTIONS, my own third fix was reverted by its own measurement, and the measurement itself broke once — `7ed99f39` · `6a64f2b2`

**Closed**: F5 · F7 · the three diagnostic majors · DEFERRED 122's un-park.
Battery 517 → **520**, 0/0. Two of the four went a different way than the work
order said, and both divergences came from **measuring instead of adopting**.

**F5 — the sibling's fix was the WRONG fix, and "no pin can reach it" was FALSE.**
Round 1 repaired `below-components` by going terminal-only-else-delegate;
`below-value` cannot, because its fallthrough is the `memq` arm which LISTS
`sub` — an entry its own P4a comment records as dead *only while this arm guards
on `@sub` alone*. Delegating would revive it and project a RAW STEP LIST as a
nominal key. So the guard stayed and only the wording changed (owner ruled the
cheap honest fix, recorded latent). ⭐ Then sweeping for echoes of the string I
was renaming found the P3a-verify pin **constructing that exact residue as IR**
and green for its whole life across a message asserting a prohibition that never
existed — because it asserted only `expr-panic?`. *Watching 5* verbatim, second
time this slice. I had written "no pin can exercise it" into the code an hour
earlier; the sweep is the only reason it did not ship.

**MAJOR (b) + (c) — the shipped remedies did not work, and neither did the
record's replacement.** `star-dup-key`'s *"drop the `*`"* is wrong BY
CONSTRUCTION under Q_U47 and measured: dropping the star just moves the refusal
to the parser's gate — advice that **relocates** an error reads as progress and
is worse than none. The round-1 record's proposed replacement (sub-block
grouping) measures as an L4 error; adopting it would have repeated the class
being fixed. `star-l4-mixed` told users to do two impossible things when both
branches are starred at different depths, and the real fix is the OPPOSITE of
one of them. ⭐ **The root error in both was talking about STARS when Q_U47 makes
the landing a property of the REMAINDER** — `m2{a* b*}` works while
`m2{a* d.c*}` does not, though both are "starred on each". Naming the RULE is
what makes a message true across the whole population.

**⭐⭐ MAJOR (a) — I SHIPPED THE FIX, MEASURED IT, AND REVERTED IT.** The tail
arm's label echoes `(sₙ ★)`, so `y2{a.b[0]*}` says `0*`. `(append path steps)`
looked free and rendered the right branch — but `path` stores bare NAMES, so
`y3{a^.b[0]*}`, `y3{a.b[0]*}` and `y3{a^r.b[0]*}` **all** rendered `a.b.0*`: in a
multi-branch block that names a SIBLING as the failing branch. A label that
identifies too little beats one that identifies the wrong thing — this round's
own doctrine, turned against my own fix. Filed [DEFERRED 123](../DEFERRED.md),
and the reverted attempt is recorded AT THE ARM so nobody re-attempts it blind.
⭐ The finding that earns the entry is that the lossiness is **systemic**:
`format-select-fail`'s `branch-str` already dot-joins `path` for every OTHER
fail kind, unmeasured.

**⚠⚠ AND THE MEASUREMENT BROKE — the third working-directory hit this session,
and the most expensive.** Two backgrounded runs used a bare
`raco make driver.rkt` from the REPO ROOT, where `driver.rkt` does not exist;
`| tail -3` swallowed the error and both ran against stale `.zo`. So "the mutant
was caught" and "the restored tree still fails" were **the same stale build**,
and for a few minutes I believed the round-1 regression pin was broken. Redone
with the cwd asserted INSIDE the script and a `trap` restoring the mutant on any
exit: MUTANT FAILURE=1 with `{:b @[9]}` observed, RESTORED FAILURE=0, both
full-size at exit 0. *Watching 4*, and the reason the gate now records **exit
code AND output size** — a truncated run reads as green.

**Gate**: battery **520**, 0/0 by direct require, exit 0 + full size ·
mutation-tested per message (each old wording reddens only its own pins) · the
un-parked pin mutation-tested against the defect it exists for · neighbourhood
`[4/4]` · acceptance 89 markers, 0 errors.

**Next**: the FULL SUITE (unrun since `5e4a3338`), then 1b-iv.

### (viii) C2 VERIFY ROUND 3 — my own remedy rewrite repeated the class it was fixing, and a pin that was vacuous from BIRTH is why it could — `428e0c80`

**The run** (`wf_1bb04012-af2`, 4 lenses + adjudicator, ~1.1M tokens, 35 min):
14 raw findings → 7 survivors + 2 charged elsewhere, 5 killed or merged by the
adjudicator's **own re-runs**. Suite **10188/488/0** after the fixes.

**⭐⭐ #1 + #5 + #2 ARE ONE STORY.** Round 2 rewrote `star-l4-mixed` because its
remedies did not work — and derived the replacement from the ONE example in
front of it, where the STAR branch happened to be the KEYLESS one. So both new
remedies were written as things to do to a *sibling*. Measured outside that
shape they are non-actions: `pc{db.hosts* ports*}` returns the error
**byte-identical**, `pc{db.hosts* ports^^}` is a parse error, `d3{a* x^.y.z*}`
is byte-identical at depth 3. The action that works is on the **star branch**
(`pc{db^.hosts* ports^}`) and was never named.
· **Why it read as fine (#5)**: the rule was written *"no step survives AFTER
it"* — but `*` is branch-FINAL, so the condition is **vacuously true** and the
sentence asserts every star lands keyless, while `e3{a.b.c*}` lands KEYED. The
sibling message I wrote in the SAME commit said BEFORE. One rule, two messages,
opposite directions — and the battery pinned the wrong one.
· **Why nothing caught it (#2)**: the only pin covering the keyed-star
population has been **vacuous since `d8776b66`** — the fixture nests `:ports`
under `:db`, so the pinned expression died at field resolution and never reached
the L4 gate; the negative half is `#t` for that error and the `check-false`
matched nothing. ⭐ **A vacuous pin does not merely fail to catch a defect — it
licenses a rewrite to specialise to the wrong population.**
⭐ **The lesson is derive-from-the-RULING-not-the-example.** Q_U47 says the
landing follows the REMAINDER; had the message been written from that, none of
the three non-actions would have been written.

**And the fix for a lying message must not itself overclaim (#8)**: "sub-block
grouping does not survive a star" is false — it survives when EVERY grouped
branch is starred (`cfg{db.{hosts* ports*}}` → `{:db @[@[1 2] @[80]]}`).
Narrowed, both spellings pinned.

**Two corrections to DEFERRED 123, both mine, one day old** (#3, #9): it is
LIVE not latent (`rec3{a.b a^r.b.zzz}` names a **valid sibling** as the failing
branch); it has a second collapse channel (the ω marker); and its repair scope
charged for a **reduction twin that does not exist** (0 `append path`, 0
`select-fail`, no `path` parameter). ⭐ That clause survived a whole verify round
because **pins assert kinds and code, never prose** — the same class F5 fixed.

**Left where they belong**: DEFERRED 117 reproduces at HEAD with understated
exposure (`mx{z a^.{c*}}` is a WHOLE-FILE ABORT; the swapped order silently
drops a key at 0 errors) · a LATENT raw-IR render in `select-step-name`'s
`[else]`, reachable only from constructed IR.

**Gate**: battery 520 → **521**, 0/0, exit 0 + full size · mutation-tested in
place (round-2 wording restored → exactly the two new R3 pins redden) ·
acceptance 89, 0 errors · neighbourhood `[4/4]` · **suite 10188/488/0**.
⚠ Two pins first compared FULL rendered lines, which end with the source
expression — byte-identical messages could never compare equal, so the pin went
red for its own construction. They compare the message body now.

**Next**: 1b-iv.

### (ix) SLICE 1b-iv — Q_U49 RULED and the NOMINAL SPLICE SHIPPED, A→B1→B2; three defects caught by running my own un-parked pins — `561737f9` · `b216f926` · `714607dc` · `0e2f5af7` · `c02efd18`

**Q_U49 answers what Q_U47 left open**: a nominal join SPLICES its fields into
the enclosing level, at every seat; a vector join stays ONE keyless component;
where a key SURVIVES the fields still NEST. Suite **10203/488/0**, battery 521 →
**536**.

**⭐⭐ THE RULING WAS ALMOST DECIDED BY A WRONG COST ESTIMATE.** The grounding
audit reported splice at the ordinal-head seat as impossible without changing the
below-walk's return contract in both twins, which argued for splicing at Seat A
only and REFUSING Seat B. Reading the arm refuted it: the ord-branch caller has
`rest` in scope and `select-steps-star-tail?` was already exported and used two
arms away, so the caller can ask "is this a BARE star join?" exactly and locally.
A predicate test per twin. **The audit's FACTS were sound; its COST ESTIMATE
would have produced the wrong ruling** — which is the whole case for R-lens
verifying the load-bearing claim rather than the whole report.
⭐ Two independent arguments then settled it beyond the owner's call: splice IS
"the sort follows the CONTENTS" (a Map-valued join contributes keyed material,
and keyed material at a level IS components-with-keys), and **Q_U45's L★ holds
for Maps only under splice** — one-component would have FALSIFIED a ruling
already made.

**⚠ AND I ANNOUNCED B1's SCOPE WRONG.** I told the owner B1 was "a signature
change, not an arm addition", carrying the audit's *"1b-iv changes both helpers'
SIGNATURES"*. Checked before implementing: `join-contents` already receives the
CONTENTS; the layer's keys are discarded because that IS the layer delete, and
they are needed for `*_` PROVENANCE only — out of scope by ruling. B1 is 93 lines
of pure insertion. Same class as adopting a record's remedy instead of running
it: **the audit's claim was true of `*_`, and I generalised it without
measuring.** Third instance in two days, same source.

**⚠⚠ THREE DEFECTS, ALL MINE, ALL FROM B2, ALL FOUND BY RUNNING THE UN-PARKED
PINS:** the `'path` carrier regression (splice ignored the sort; a path selector
must yield exactly ONE component) — ⭐ fixed by the RULE that **splicing needs an
enclosing level to splice into**, which is also what makes Q_U40's `m{a b}*` →
`{:x 1, :y 2}` work at last · the EMPTY-layer sort flip, **predicted verbatim by
the audit** (`(andmap … '())` is `#t`, so `@[]` became `{}`) · and `*_` falling
into the nominal join on the VALUE side, caught by A's seat pin, written for
exactly that.
⚠ **TWO OF MY OWN PINS WERE ALSO WRONG**: `coll{inner* inner}` does not collide
(`inner*` lifts `:k` while the plain branch keeps `:inner`), and two pins
asserted a full rendered line and reddened on ORDER while the semantics were
right — `make-record` canonicalizes, the champ carries HASH order. Q_U38's third
fact says to pin the two SEPARATELY, and now they are.

**⭐ WHAT CAME OUT FREE**: on the type side the splice is a **no-op conversion** —
a level component `(key-or-#f . record-field)` and a row field are the same shape
— so Q_U38's splice-vs-sibling and splice-vs-splice collision checks cost NO
code; the level's existing dup fold sees the lifted keys. Verified refusing.

**Gate**: mutation-tested PER TWIN at both B1 and B2 (each neutering reddens only
its own pins) · E2E inertness proven at B1 (star surface byte-identical) · all
seven parked pins un-parked and green at B2 · acceptance 89/0 · neighbourhood
4/4 · suite 10203/488/0.

**Open**: the ω tension — `star-omega-nominal` refuses on Q_U38's
possibility rule while Q_U40's own `rowsm:cfg*` example assumes disjoint keys ·
`*_`'s prefix source is unruled (Q_U41's two examples disagree).
**Next**: the adversarial verify round (`wf_0df64e7f-231`, running).

### (x) THE 1b-iv VERIFY ROUND — 2 BLOCKING, and the sibling question answered YES four times; one BLOCKING was inside my own fix for the previous round — `dbb8a55d` · `729ac3b0` · `2c481d21`

`wf_0df64e7f-231` (4 lenses + adjudicator): 16 raw → **11 survivors**, none
killed outright. Suite **10206/488/0**, battery **539**. All 11 resolved.

**⭐⭐ THE ROUND'S SHAPE: EVERY BLOCKING DEFECT THIS SLICE HAS PRODUCED CAME FROM
A FIX FOR THE ROUND BEFORE.** B2's `'path` regression was self-caught and fixed
by threading the sort into both landing HELPERS — and the verify found that fix
reached **neither twin's FEED**. [Q_U20] says a SUB inner assembles at `'block`
ALWAYS; typing forces it with a literal at two sites, reduction's `bcast-apply`
passed the OUTER sort down. Measured at 0 errors: `vv:{a*}` gave value
`@[@[{:x 1}] @[{:x 2}]]` against type `[PVec {:x Int}]` — and it PROPAGATED,
`r:x` → `none : [PVec Int]`, a well-typed expression evaluating to `none`.

**[DEFERRED 117] DISCHARGED, and the landmine held.** Its own entry called it
"cheap"; measured on the UNTOUCHED vector path it is a WHOLE-FILE ABORT
(`nv{m k^.{a*}}`, no output at all) and, swapped, a silent key drop at 0 errors.
The gate and its diagnostic's `findf` render through the same predicate, so they
moved in ONE edit — written down as a landmine at the START of this session,
before it was stepped on.

**⭐ THE SEMANTIC ONE**: a TRANSPARENT ordinal decided whether the landing
spliced. `vk1{0.m*}` spliced, `vk2{0 .0.m*}` did not — two spellings of one
shape. The caller was using the ARM's exactly-two predicate as a proxy for "did
the arm fire"; ordinals contribute no layer and the walk recurses through them.
Minted `select-steps-yield-bare-join?`; the arm's warning is untouched, because a
KEYED prefix must re-nest and that reason does not apply to a no-layer step.

**FOUR MESSAGES Q_U49 HAD FALSIFIED**, every remedy RUN before shipping — one of
them **named an UNSPELLABLE remedy** (`seg^k*` is a parse error, so a starred
branch cannot be renamed), another's remedy pointed the OPPOSITE way from the fix
that works. This is the third round in a row where the message work was "derive
from the ruling, not the example".

**⚠⚠ AND ONE FILED GAP WAS NOT A GAP — established by writing the fix and
breaking a working shape.** The verify said the two permanent typing refusals
lack sibling arms in `star-join-value`. I added one; it made `vhm{0.{0}*}` PANIC
where typing splices — **reduction refusing where typing ACCEPTS, the divergence
inverted**. Both are TYPE properties invisible at the value layer (opaque Map and
closed record are both champs; ω container and legal Seat B layer are both rrbs).
Reverted, with "the asymmetry is inherent" recorded at the seat.
⭐ **The lesson: a verify finding is a hypothesis too.** Three rounds have now
each had at least one finding whose recommended fix was wrong — round 2's
sub-block remedy, round 3's, and this one.

**Also**: the render loop caught my own L4 rewrite (no `*` in the text; a failing
check inside `check-not-exn` surfaces as "must not raise"). [DEFERRED 124] filed
— `*_` is structurally unspellable at the closer-adjacent seat, so the `*_` slice
must not assume coverage there.

**Next**: the `*_` prefix-source ruling (Q_U41's two examples disagree), the ω
tension (Q_U40's `rowsm:cfg*` vs Q_U38's possibility rule), then 1b-v.

### (xi) TWO RULINGS — the ω tension DISSOLVED, and `*_`'s prefix turned out FORCED while its justification turned out UNSPELLABLE — `436ad295`

**Q_U50 — the ω "tension" was never one, and the measurement is the whole
argument.** A vector literal whose elements carry DIFFERENT row types cannot be a
`PVec`; it types as a TUPLE, so the join's contributors are individually known
and non-collision is PROVABLE — `rowsm:cfg*` → `{:a 1, :b 2}`, exactly Q_U40's
claim. A homogeneous one types as `[PVec …]`, same keys N times with N unknown,
and is refused — exactly Q_U38's possibility rule. **The discriminator is the
subject's own type and the language already computes it**, so
`star-omega-nominal` was firing on precisely the right side all along. ⭐ Read
forward, this is Q_U38 doing real work rather than over-refusing: it declines
exactly where the type cannot rule a collision out. No code change.

**Q_U51 — the prefix source is FORCED**, the same shape as Q_U48. Each lifted key
is `<the key its content sat under in the deleted layer>-<its own key>`. The
rival "prefix = the segment the star attaches to" COINCIDES in the fused band
(the layer is always the re-nest `{:seg {…}}`) and has nothing to name in a
multi-content layer, so it is not total. ⚠ The audit's filed "Q_U41's two
examples disagree" was a FALSE ALARM — they are two EXPRESSIONS with different
layers; one rule covers both, tabulated from measurement.

**⭐⭐ AND THE FINDING THAT DECIDED THE SLICE: `*_`'s ENTIRE JUSTIFICATION IS
UNSPELLABLE TODAY.** Q_U24 adopted `*_` as the collision remover, and a
cross-content collision needs a layer with SEVERAL contents. Measured:
`mn{a b}*` works (multi-content, closer-adjacent) · `mn{a b}*_` is `Unbound
variable` · `mn{a*_}`'s layer is `{:a {:x Int}}` — the only spellable band has
ONE content **by construction**. So the fused band can never exercise the
operator's purpose. **The expressivity call**: `cfg{database*_}` has workarounds
(`cfg{database^.url database^.pool-size}`); `regions:{host port}*_` has NONE —
no spelling flattens a homogeneous Map-of-Map keeping provenance. That capability
is the whole reason `*_` exists and it lives entirely in the band we cannot
write, so **Q_U39's mint is not attached to 1b-v, it IS 1b-v**.
⚠ Rejected (a) ship-the-fused-band: it would field an operator that works where
it is not needed and is unspellable where it is — and would rule the prefix on
evidence drawn only from single-content layers, the derive-from-the-example
failure that cost three message rewrites this session.

**Next**: 1b-v — Q_U39's mint + `*_` + `*-` (Q_U48) + the `*-_` decision.

### (xii) 1b-v's GROUNDING AUDIT + Q_U52 — the slice got smaller in one direction and split in another, and the critic refuted a footgun I had already relayed — `783be940`

**The run** (`wf_cc1a72a0-75b`, 4 facets + critic). ⚠ The critic CRASHED on an
API error; the resume bailed because I omitted `args`, so I re-ran it as a
standalone agent pointed at the journal. **It was worth the re-run — it found
more than the four facets combined**, which is the third time the completeness
pass has justified itself.

**⭐⭐ THE SLICE IS SMALLER THAN Q_U39 FRAMED IT.** `*_` is ALREADY fully parsed
and already reaches a dedicated refusal in THREE of the four bands; only the
closer-adjacent band hardcodes `'flatten` and never calls `split-star-lexeme`.
Two sites, not a new operator.

**⭐⭐ AND THE QUESTION NOBODY ASKED, which the critic did**: is 1b-v a MINT or
the SEMANTICS? The mint alone ships a better error message and leaves Q_U51's
motivating capability non-existent — the shape Q_U51 itself rejected. Owner ruled
**(b)**: mint + `*_` semantics; `*-`/`*-_` split to **1b-vi** ([Q_U52](../2026-07-28_CIU_T6_PATH_SELECTION_D4.md#q-u52)).
The scope ruling settles the sentinel shape as a side effect — one new spelling
means TWO bare sentinels, and naming it `$postfix-star-synth` keeps five
substring-matching gates covering it for free.

**⚠⚠ I RELAYED A FALSE FOOTGUN TO THE OWNER, and the ambient rule had already
corrected it.** The facet marked "verified" that a new bare sentinel missing
`pattern-var?`'s exclusion list is a WHOLE-FILE ABORT, citing the comment above
that function. The comment is STALE — `datum-subst` was fixed 2026-08-01;
measured, the cited shape gives **0 errors**. ⭐ `pipeline.md` already says so,
*including* the sentence that macros.rkt's own comment still carries the stale
claim. So the ambient rule was right, the facet quoted the stale artefact, and I
passed the artefact on. **A stale in-code comment outranked a corrected ambient
rule in my reading** — and it removed the loudest argument against
N-bare-sentinels from the WRONG side of the ledger.

**THE CENSUS ENUMERATED THE SENTINEL NAME; three hazard sites key on the
CHARACTER.** That reframing found a live collision: `_` and `-` are
`ident-continue?`, so the retired-broadcast recognizer **already owns** `.*_` and
`.*-` (measured: *"broadcast `.*_` was retired"*) — putting Q_U26's ravel and the
star's continuation grammar on a collision course. Filed **125**; also **126**
(the ANGLE band is a fourth carrier of DEFERRED 124's degradation) and **127**
(the arrival-matrix instrument cannot distinguish "correctly does not mint" from
"I cannot see this token", and its carrier table hardcodes a bare `*` in all 11
sources — a widened reader would measure 190/190 covered for a question it had
stopped asking).

**DEFERRED 124's MECHANISM CORRECTED** — verified on the main thread, not taken
from the report: `x{a b}*_` lexes as ONE symbol token, not `*` applied to `_`.
Second route: the SPACED form errors differently from the glued one, which it
could not if they parsed alike. Conclusion stands; the fix gets cheaper.

**⭐ THREE RULINGS HAVE DIVERGED FROM HEAD** and 1b-v would inherit all three:
Q_U42 in BOTH examples; **Q_U45's own literal output text** (`{:x 1, :y 2}` vs
HEAD's `{:y 2, :x 1}` — the equivalence it claims holds, the printed order does
not); and the pre-written 1b-v pin block, whose 2 exercisable checks are stale
against kinds 1b-iii/1b-iv minted. **So even a ruling's own example text is not a
safe pin source** — "derive from the ruling, not the example", one layer down.

**Housekeeping**: reaped a 66-minute 166 MB orphan from a DIFFERENT session.

**Next**: slice 1b-v **A** — the instrument, written and RUN RED first.

### (xiii) SLICE 1b-v — `*_` IS LIVE, and the verify answered the standing question YES SIX TIMES — `317ae5af` · `2625b38f` · `d09d4a49` · `161e8bc1` · `b12d02ed` · `cb555002`

**Shipped**: A (instrument, RED-first) → B1 (cont channel, inert) → B1b (the join,
inert) → B2a (shield down, `*_` LIVE in three bands) → verify (`wf_fc98d241-4e5`,
29 raw → 16 survivors, 12 fixed, 4 filed). Battery 539 → **550** · suite
**10217/488/0** · acceptance 89, 0 errors. Record:
[§5.P4e-1b-v shipped](../2026-07-28_CIU_T6_PATH_SELECTION_D4.md#p4e-1b-v-shipped).

**⭐⭐ THE VERIFY'S ANSWER: YES, SIX TIMES — and three had a comment IN THE TREE
asserting the sibling was safe, one of them in B2a's OWN commit body.** I wrote
*"`star-synth-positional`'s message now MEANS what it says"* and a matching test
comment; both were false, because B2a threaded the `keys` channel into the
nominal-contents arm and not into its vector-contents sibling one clause below.
ONE message served TWO producers whose truths are COMPLEMENTARY, so each
falsified a different half of one sentence — and one falsified the REMEDY (a
nat-domain layer told to "use bare `*` to flatten positionally", while bare `*`
joined it KEYWISE in the same run).

**⭐ THE LESSONS THIS ARC ADDED, all earned:**
1. **A DIRECT-CALL PIN IS ONLY AS GOOD AS ITS FIDELITY TO THE PRODUCTION SHAPE.**
   B1b's pin built champs with SYMBOL keys; production keys them with
   `expr-keyword` STRUCTS. The pin passed on a fiction while the real path took a
   WHOLE-FILE ABORT. An inert helper's pin is exactly where that fidelity goes
   unchecked, because nothing else exercises it.
2. **RENDERING A KIND PROVES NOTHING ABOUT WHETHER ANYTHING PRODUCES IT.**
   `star-nominal` had ZERO producers and a message asserting a rule "not yet
   ruled" that Q_U51 had settled — kept green by a render loop that PRODUCES
   every kind by hand.
3. **MUTATION-TEST PER TWIN IS NOT SATISFIED BY MUTATING PER TWIN** if the pins
   share a `test-case` — rackunit aborts at the first failure, so a red pin masks
   a sibling exactly as a green one does. ⚠ And I re-introduced the same
   structure ONE COMMIT LATER in the B2a contrast pins: the fix had been applied
   to the twins, not to the pattern.
4. **THE MEASUREMENT BROKE TWICE MORE** — a mutation whose match string had one
   paren too many SILENTLY DID NOT APPLY and reported a green mutant (the
   script's own assert caught it), and two Racket string-escape bugs (`\$`, then
   `\{`) whose failure mode is a file that will not READ.

**⚠ TWO LANDMINES CLEARED THAT SAT DIRECTLY UNDER B2b**: the PARKED 1b-v pin held
an invalid `\*` escape — unparking it, which is what B2b does, would have made
the battery fail to READ (zero tests, "empty not partial") — and
`p4e1-star-class`'s identity test would have classified a genuine `*_` leak as
`'gone` and PASSED. It is the one gate the `$postfix-star` name-prefix trick
cannot rescue.

**⚠ THE INSTRUMENT'S HEADROOM IS BEING SPENT FAST**: `test-path-selection.rkt` is
now **145.5s** (121.9s two arcs ago; limit 300s since `a4cbe7b8`). At ~24s per
slice that is ~6 slices of headroom, not indefinite.

**Next**: **B2b** — widen the reader mint so the closer-adjacent band can spell
`*_`. [DEFERRED 129](../DEFERRED.md) is its precondition (the EMIT is
spelling-blind — B1's ordering argument covers the parser arm, not the emit), and
**130** rides with it.

---

### (xiv) The path/hook repairs, and **Q_U53 RULED** — the emit's miss-case is ruled out at the GATE, and B1's "ONE table" claim turns out to be false · `83effe09` · `96ef73f0` · (this commit)

**Why (the detour first)**: the owner moved the project to `logos/prologos` and
asked for absolute paths to go relative. Six live sites carried the old checkout.
⭐ **The one that mattered was not a path at all**: `.git/config` had
`core.hooksPath` pointing at the pre-move directory, and that setting OVERRIDES
the hooks directory entirely — so **git has been running NO hooks since the move**
and the `check-parens` pre-commit gate was silently inert on every commit.
⚠ **It fails OPEN**, which is why weeks passed. The installer compounded it by
`ln -s`-ing an ABSOLUTE target while PRINTING the relative form, so its own
transcript asserted the property it lacked. Fixed: relative links, a
`core.hooksPath` guard that refuses to install into a void, post-install
verification that the link RESOLVES, and a `.disabled` marker that is now
respected (a blanket re-install would have silently re-enabled the deliberately
-off mempalace post-commit). **Verified it FIRES** — staged a broken `.rkt`,
attempted a real commit, watched it refused with HEAD unmoved.

**Surprise**: the audit template `.claude/workflows/grounding-audit.js` was
handing every facet agent a repo path that does not exist. Agents fell back to
cwd, so results looked normal — a false anchor that fails open, same shape as the
hook.

**Q_U53, the ruling.** Two parts. **(1) The emit's miss-case is not given a
behaviour — it is made UNREACHABLE at the gate** (exact table membership, so gate
and emit ask one question of one datum). All three alternatives are disqualified
INDEPENDENTLY, by measurement: a bare `#f` passes two permissive `[else]` arms
and lands as `(datum-bool false)` in user-visible data — and is **worse than the
sentinel leaking**, because `$postfix-star` deliberately has no `pp-datum` arm so
a leak prints its internal name while `#f` prints `false`; a fallback is DEFERRED
129's hazard relocated; a raise is UNCATCHABLE (`parse-reader.rkt` has **zero**
`with-handlers`, and the G2 guard runs strictly after the reader).
⭐ **So last turn's open question — "do we also plant the assertion?" — DISSOLVED
rather than being decided**: the only assertion mechanism at the reader IS the
raise, and the raise is out on its own merits. The canary belongs in a test.
**(2) One semantic authority, folded in its own slice, before 1b-vi** [owner:
"C, and land the fold before 1b-vi"].

**⭐⭐ THE SURPRISE THAT CHANGED THE SLICE: `$postfix-star-synth` has ZERO
PRODUCERS, and B1's "the family lives as ONE table" is FALSE at HEAD.** The
downstream is fully wired (ten table-driven consumers) and the upstream is empty,
so B2b's emit would be the *first* producer — the miss-case is the initial
contract of a path that does not exist yet, not a change to one that does. And
the three "live" bands never touch the table at all: they go through
`split-star-lexeme`, which maps the suffix straight to a cont. MEASURED in one
file — `mn{a*_}` → `{:a-x Int}` ✅ versus `mn{a}*_` → `Unbound variable` ❌.
**Same operator, two authorities, disjoint bands, nothing cross-checking them.**

**⚠ FOUR ENUMERATIONS WRONG, THREE OF THEM OURS** — the recurring shape, again:
DEFERRED 129's "one other site" is **19** (and the two TOOL sites would re-blind
the mint verifier, reinstating DEFERRED 127); Q_U52's "all five
`select-star-cont` readers" is **SIX and was wrong when written** at my own
`2625b38f`, inherited from the source comment, so it had **two carriers** and
fixing either alone leaves the other lying; `parse-reader.rkt`'s "sole non-test
consumer" is not sole; and a **fourth** family copy (`tools/form-deps.rkt:48`)
that no facet's grep shape could find.

**Next**: **B2b, MINIMAL** — gate + emit + DEFERRED 130. ⚠ [DEFERRED 133](../DEFERRED.md)
is a precondition for its OWN tests: the battery's `p4e-star-type` /
`p4e-last-star-type` are exact-lexeme `"*"`, so a B2b row written with them has
no power — DEFERRED 127's defect a third time, one layer down, where the repair
never propagated. Then **1b-v-C** (the fold), then 1b-vi.

---

### (xv) EDITOR SUPPORT RESTORED — and the VS Code half was never a path bug: the extension has never been INSTALLED · `9d37d2b0` · merged `586b868d`

**Why**: the owner reported syntax highlighting and interactive editing dead in
BOTH Emacs and VS Code after the move. Three causes, and only ONE of them was a
stale string in this repo — the sweep at `83effe09` could not have caught the
other two because they live outside it.

**Emacs — one dead line, whole feature off.** `~/.spacemacs:1206` `:load-path`
pointed at the pre-move checkout, so `use-package` failed and `.prologos` fell
back to fundamental-mode. Repointed (`~`-relative). ⚠ Note what did NOT break:
the tree-sitter grammar, because `install.sh` **copies** the dylib into
`~/.emacs.d/tree-sitter/`. A copy survives a move; a load-path does not. Verified
in batch at the new path — mode loads, grammar available, 8 faces, no
`treesit-font-lock-rules-mismatch`. Also `~/.config/tree-sitter/config.json`.

**⭐⭐ THE SURPRISE: the VS Code extension has NEVER been installed.** No folder,
no symlink, no `extensions.json` entry — shell history shows it was always
`code --extensionDevelopmentPath="$(pwd)"` from `editors/vscode-prologos`. So
highlighting existed only inside that ONE dev-host window, and it "broke" because
the directory that command was run from stopped existing. Nothing in the
extension was stale. **The reported symptom and the actual defect had no causal
relation** — the move only removed the cwd the workflow depended on.

**⚠ AND INSTALLING IT EXPOSES A LATENT DEFECT THAT FAILS OPEN — the THIRD in two
entries.** `resolveServerPath` and the LSP `cwd` both did `extensionPath/../..`,
true only while the extension sits inside the checkout. Symlinked into
`~/.vscode/extensions`, `../..` is `$HOME`; server.rkt is not found, `activate()`
**returns early**, and every interactive command silently fails to register —
while highlighting keeps working, because the grammar is declarative and needs no
activation. So the broken state LOOKS like the working state. Fixed by RESOLVING
rather than assuming: setting → in-repo → `realpath` → workspace folder, with
`cwd` derived from the *resolved server*, the one path already proven to exist.
Verified against all five real shapes with a throwaway harness.

**Also**: `icons/prologos-{light,dark}.png` have been declared in `package.json`
since March and **never existed** — an activation error on every load, invisible
because nobody read the log. Generated. And the 22 dead `settings.local.json`
permissions flagged at `83effe09` are rewritten to the new root (0 residual).

**Watching — fail-open is now 3-of-N in two entries**: `core.hooksPath` (xiv),
the audit template's false repo anchor (xiv), and `activate()`'s early return
here. All three kept the surface looking normal while the mechanism was off. The
common shape is *absence* of a signal read as *absence of a problem*. One more
data point and this is a `DEVELOPMENT_LESSONS.org` entry, with the actionable
one-liner being: when a guard/anchor/registration goes missing, ask what PROVES
it is present — never what would show if it were absent.

**⚠ ADDENDUM — a FOURTH site, found only because the owner ran the thing.** With
Emacs restarted, `prologos-repl` failed: `collection not found for module path
(lib "prologos/repl")`. `prologos-repl.el:45` runs `racket -l prologos/repl` — a
COLLECTION path — and the `prologos` collection is a **linked Racket package**
registered in TWO places outside this repo, both still naming the old checkout:
`~/Library/Racket/9.0/links.rktd` and `~/Library/Racket/9.0/pkgs/pkgs.rktd`.
Re-linked via the supported route (`raco pkg remove` → `raco pkg install --link
-n prologos-dev <repo>/racket`), both registries verified, `[+ 1 2]` → `3 : Int`.
⚠ **`--no-setup` was deliberate**: a `raco setup` here recompiles the tree
WITHOUT `PLT_CS_COMPILE_LIMIT`, which silently interprets the giant-match modules
— the CLAUDE.local.md trap, reachable straight through a package operation that
looks unrelated to compilation.

**What the addendum says about the method**: my own audit at (xv) grepped editor
CONFIG and found nothing more. It could not have found this, because the
registration is not a path in a config file — it is a package-manager record, and
`racket -l` is the only consumer in the tree (verified: every other `"prologos/X"`
goes through `foreign-module-path->require-spec`, which is path-independent by
construction). **The grep-shaped audit has a blind spot exactly where a path is
held by a REGISTRY rather than a file.** Re-verified after: `test-foreign.rkt`
57/57, and the Emacs ERT suite 33/33 (the 7 "skipped" were a bare-batch artifact
— they pass once `prologos-ts-mode` is loaded; a skip is not a pass).

**Also learned, not a bug**: the REPL needs a **blank line** to terminate a form
(the two-space continuation prompt is the layout reader waiting). Typing at the
comint prompt therefore needs RET twice — but `prologos-repl.el:185` already
sends `expr + "\n\n"`, so every interactive command is correct as shipped.

**Next**: unchanged — **B2b, MINIMAL** per (xiv). The editor work was a detour
and touched no compiler surface (4 files: `extension.ts`, 2 icons,
`settings.local.json`); the suite was not re-run and does not need to be.

---

### (xvi) EMACS `prologos-send-form` + PERSISTENT RESULTS — and the E2E found a cold-start bug that has been answering with the BANNER · `cd0e8532` · merged `51eee18f` · `08d939e4`

**Why**: owner asked for two things — rename `prologos-eval-last-sexp` to
`prologos-send-form` with VS Code `cmd+enter` semantics, and make result
overlays persist like the VS Code decorations do.

**The rename was the smaller half; the SEMANTICS were wrong.** `backward-sexp`
finds one bracket group *before point* — in a layout-sensitive language that is
not a form. Point inside a multi-line `defn` sent whatever bracket preceded the
cursor. `prologos--top-level-form-bounds` is now a faithful port of
`getTopLevelFormRange` (`src/forms.ts`): column-0 opener + indented
continuations, interior blank kept when the next non-blank is still indented,
trailing blanks trimmed. 8 tests, one per branch, so the two editors cannot
silently diverge. **Persistence**: clearing moved off `pre-command-hook` (the
next keystroke erased the answer) onto `after-change-functions`, matching
`onDidChangeTextDocument`; results for different forms now COEXIST, and
re-sending one replaces only its own, matched by form range.

**⭐⭐ THE FIND, AND IT CAME FROM THE E2E, NOT FROM REASONING: the first eval
after a cold start has been returning the STARTUP BANNER.** `make-comint-in-buffer`
returns before the process prints anything, so the banner was still in flight
when the callback was enqueued; the filter matched the BANNER's own trailing
prompt, popped that callback, handed back `Prologos v0.3.0 / :quit to exit …`
as the result — and the real result then arrived to an empty queue and was
**silently dropped**. Pre-existing; invisible because it fires only on the cold
path and the old overlay vanished on the next keystroke. **My persistence change
is what left it on screen.** ⚠ The pin was checked for POWER: with the wait
disabled it goes red. (The project's own "vacuous from birth" lesson, applied.)

**Method note, and it is the same one as (xv)'s addendum.** I would not have
found this by reading. Every unit test passed — 48/48 — over a REPL that was
answering with its own version string, because the units never start a process.
It took driving the actual thing end to end. **(xv) found a defect the greps
could not see; (xvi) found one the unit tests could not see.** Two consecutive
entries where the instrument, not the reasoning, was the binding constraint.

**RETIRED** (owner ruling): `prologos-eval-defun-at-point` + `C-c C-d`, both
keymaps — it duplicated send-form via a `def|defn|spec|…` regexp (blind to a
bare `[+ 1 2]`) plus `forward-sexp` (ends a multi-line form at its first bracket
group). Unbinding is test-pinned. `C-x C-e` kept, `s-<return>` added (= cmd+enter
on macOS), old name kept as an autoloaded obsolete alias.

**Verified**: ERT **49/49** with Racket present (integration pin runs, 4.3s);
`prologos-repl.el` byte-compiles with **zero** warnings; E2E against a live REPL
— point on a continuation line sends the whole `defn`, a bare `[dbl 21]`
evaluates to 42 against it, both results coexist, survive point motion, and
clear on the first edit.

**Next**: still **B2b, MINIMAL** per (xiv) — no compiler surface touched here
either.

---

### (xvii) C-g / ESC DISMISS RESULTS — the advice that implements it would have broken C-g globally on the first reload · `4e9a894f`

**Why**: owner asked for a quit gesture to clear the overlays too, and rebound
`s-<return>` -> `M-<return>` (their Command key is Meta, not Super).

**Advice, not `post-command-hook`, and the choice is FORCED** — measured, not
assumed. C-g runs `keyboard-quit`, which SIGNALS; the signal unwinds past the
command loop's post-command-hook call. After a quit the hook saw
`this-command` = **nil**, not `keyboard-quit`, so the obvious hook-based
implementation would have silently never fired. `:before` advice runs ahead of
the signal. Second measurement that shaped the design: `advice-add` ACCEPTS a
symbol that is not fbound yet and applies when it is defined — so the Evil ESC
commands can be advised without Evil being loaded, and no load-order dance is
needed.

**⭐⭐ THE TRAP, caught by asking "what happens on unload?" before shipping:**
`unload-feature` unbinds the advice FUNCTION but leaves the advice INSTALLED on
`keyboard-quit`. Every subsequent C-g — in ANY buffer, Prologos or not — then
fails with `void-function`. Measured directly. The owner's reload script calls
`unload-feature`, so this was the live path on their very next reload, and the
blast radius is the whole session rather than this package.
`prologos-repl-unload-function` strips the advice; test pins
load → unload → C-g is still a plain quit → reload → advice back exactly once.

**The keymaps had DRIFTED and the suite caught it.** The owner rebound
`M-<return>` in `prologos-ts-mode.el` only; `prologos-mode.el` still said
`s-<return>`, as did both tests. That is the test suite doing its job on a
change that did not come from me — worth noting because (xv)/(xvi) were both
cases where the *instrument* was the binding constraint, and here the instrument
was already in place and paid off immediately.

**Verified**: ERT **53/53**; zero byte-compile warnings; the owner's reload
script runs clean against this code (7 modules, bindings correct, advice
re-installed, C-g sane); E2E — two results coexist, survive point motion, C-g
clears both without touching text, re-send restores, edit clears.

⚠ **Left for the owner, in THEIR file** (`~/scripts/elisp/prologos-reload.el`,
not mine to edit): its report line still prints `s-<return> -> nil`, which now
reads like a failure when the binding simply moved; and
`prologos-clear-results-commands` should join its `stale-vars` list so a future
change to that default actually propagates on reload.

**Next**: still **B2b, MINIMAL** per (xiv) — no compiler surface touched.

---

### (xviii) SURF T1 OPENED then BLOCKED; TSG T1 repaired the grammar — 13.6% → 4.7% unparsed, and the metric was wrong for most of it · `097dc93b`…`f6b52b81` (18 commits)

**Why**: owner asked to re-engineer the disabled `prologos-surfer-mode`, loosely
on gopcaml-mode. Three rulings taken (§2 of the SURF doc): header-line
breadcrumb as the ONLY always-on element · navigation first, transformation to
T2 · `C-M-*` takeover.

**The design landed and the track immediately blocked.** The noise was
architectural, not tuning: the old surfer repaints a depth-tinted background on
every cursor move. The framing that fixed it — *parens are TEXT, an overlay is
CHROME; making chrome behave like text is the mistake.* gopcaml shows structure
ON DEMAND (a `set-transient-map` session) and never otherwise.

**⭐ BLOCKED because P0's fixture — idiomatic WS code, compiler-clean at 0
errors — produced 7 tree-sitter ERRORs.** Corpus scan: 142 files, **18 clean**,
8,336 ERROR nodes. `grammar.js` untouched since 2026-03-11; LET, ARROW and Rel
T1 all landed after; `defr` appeared **zero** times. Owner ruled: fix the
grammar first. → **TSG Track 1**.

**TSG delivered** (`2026-08-14_0030_TREE_SITTER_GRAMMAR_REPAIR.md`): P0 corpus
gate · P1 multi-clause `defn` · P1b bare operators · P2 `let` · P3 `fn` (already
fixed, no work) · P3b `<Type>` return annotations · P3c `require`/`imports` ·
P4 `trait` bodies · P5 `defr`.

| | baseline | now |
|---|---|---|
| clean files | 18 | **25** |
| error-bytes | 13.6% | **4.7%** |
| files SWALLOWED (one error ≥20%) | 9 | **6** |
| worst single error | **100%** of its file | 51% |

**⭐⭐ THE METRIC WAS WRONG FOR MOST OF THE TRACK, and the owner is who fixed
it.** foray is a scratchpad, deliberately holding forms that do not run — *"a
grammar that can still be functional in presence of dirty code is a valuable
thing"*. Right, and decisive: editor support is ALWAYS on half-written code, so
the property is not "parses at 0" but **a broken form damages ITSELF and stops
there**. Implemented as the LOCALITY metric. It re-scored P3c from a −56 B
nothing into the fix that took foray from a 67% swallow to under 18%.

**⚠ FOUR TRAPS, each cost real time:**
1. **Leading-token clustering misdirected FOUR times** (`defn` 779, `(fn` 272,
   `ns` 16.9%, `spec` 191). It counts the first token of the line CONTAINING an
   error — a heat map of where errors LAND, not of what causes them. `ns` had
   **no defect at all**: one 19,280 B swallow from a `require` two lines below.
   Only isolated shape probes establish causation.
2. **`install.sh` overwrote the live dylib in place → SIGKILL'd the owner's
   Emacs** (3 crash reports). macOS invalidates code-signature pages when a
   mapped dylib is rewritten. Fixed with atomic rename. It also **invalidated a
   diagnosis**: I read `EXIT=137` as "the grammar hangs" and reverted a correct
   fix — 137 was that same kill.
3. **ERROR COUNT rises when the grammar improves** — parsing further replaces
   one mega-ERROR with several precise ones. Replaced with error-BYTES, then
   with LOCALITY.
4. **P1 looked like a failure and was one rule from a large win** (blocked
   behind operators). A gap DOWNSTREAM of your fix masks it entirely; never
   revert on a flat corpus number alone.

**Left BLOCKED, deliberately, twice reverted rather than bodged**: same-line
`defn` bodies (255 corpus lines) and inline paren match arms. Four rounds proved
conflict declarations relocate the failure rather than remove it. **RULED by
measurement**: take the `_line_end` scanner token — the greedy-body option is
disqualified because 7 corpus sites would have a defn body swallow the FOLLOWING
top-level form (newlines are `extras`).

**Next**: TSG P6 — `_line_end` in `src/scanner.c`; then SURF T1 unblocks.
