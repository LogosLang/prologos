# Dailies — 2026-08-12

Rolled from `2026-08-11_dailies.md` (**616 lines**, past the ~400–500 cap) at the
opening of the 2026-08-12 working-day interval, paired with
`docs/standups/standup-2026-08-12.org`. Prior file is read-only history; its LOG
(i–viii) carries the whole **P4e-1b** arc: the mini-audit, Q_U38–Q_U44, slices
1b-i and 1b-ii, and **both 1b-iii reverts**. `2026-08-10_dailies.md` carries the
P4e-1a arc (LOG xxvi–xxix). STATE head below is SEEDED (copy-forward per
`DAILIES_METHODOLOGY.org`) and refreshed to current — the prior file's accreted
historical layers are left there rather than carried.

---

## 📍 STATE  (overwrite in place — always current)

- **HEAD**: re-derive with `git rev-parse HEAD` — never trust a hash in prose.
  At this refresh: **`781b8f75`** · local `main`, MAIN CHECKOUT ·
  **25 unpushed — do NOT push unless directed** (MEASURE; origin advances
  externally) · battery **481** (measured — 1b-iii's +5 went out with the
  revert) · acceptance **84** markers · suite **10143 / 488 / 0**
  (`--all --force-rerun`, `[488/488]`) — **last measured at P4e-1a and NOT
  re-run since**, which is honest rather than stale: no production code has
  landed after 1b-ii. Working tree = owner WIP only; stage explicit paths, never
  stash.
  ⚠⚠ **THE SUITE IS INTERMITTENT AND IT IS PRE-EXISTING — check
  `racket/prologos/data/benchmarks/timings.jsonl` (NOT repo-root `data/`) BEFORE
  any base A/B.** 470 runs recorded: **74 with `all_pass=False`** (~16%), and
  `test-properties.rkt` is RANDOMIZED (`gen:integer-in`) — 13 cases in 412 runs,
  8 in 57 — so a ±5 total swing is NORMAL. A 5-run base sample missed both and
  nearly cost slice A an attribution it did not deserve; one query answered in
  seconds what five worktree suite runs could not.
  ⚠ Do NOT compile while a suite is in flight — one run was contaminated that
  way and its failure sent the diagnosis down the wrong path first.
  ⚠ **COUNT `ERROR` AS WELL AS `FAILURE`.** A raise inside a rackunit
  `test-case` reports as **ERROR**, and `grep -c '^FAILURE'` reads it as green.
  ⚠ **Run commands from the REPO ROOT** — a failed `cd` in an `&&` chain
  silently skips the step, and a grep-based green then reads an EMPTY capture as
  a pass. Happened twice at 1b-i/1b-ii.
  ⭐ **NUMBERING is RULED** [owner 2026-08-08]: monotonic, permanent, allocated
  from the **NEXT FREE register** at the top of `DEFERRED.md` — never
  `max(heading)+1`. **Next free 113 · next free Q-label U46** (Q_U45 ruled 2026-08-12).
- **Where we are**: **CIU T6 — P4e-1b. The semantics are FULLY RULED
  (Q_U38–Q_U44); slices 1b-i (`226844f1`) and 1b-ii (`dbb9ec77`) are SHIPPED;
  1b-iii was BUILT AND REVERTED TWICE.** Both attempts passed their own gate
  (battery + neighbourhood green) and were killed by the adversarial verify —
  round 1 found 3 blocking defects, round 2 found 2 MORE **in the fixes for
  round 1**. Patches parked; full records + the attempt-3 list in D4
  [§5.P4e-1b-iii](../2026-07-28_CIU_T6_PATH_SELECTION_D4.md#p4e-1b-iii).
  ⚠ **The parked patches are in the PRIOR session's scratchpad**:
  `/private/tmp/claude-501/-Users-avanti-dev-projects-prologos/311a1847-2220-4539-bd51-0e2270a3625f/scratchpad/slice-1b-iii-attempt{1,2}.patch`
  (attempt 2 = 427 added lines across six files). "The session scratchpad" in
  the older records means *that* one, not the current session's.
- **NEXT**: **slice 1b-iii ATTEMPT 3 — and its SCOPE IS THE OPEN QUESTION.** The
  2026-08-12 mini-audit (`wf_bbf1169e-340`, 6 facets + critic, ~1.55M tokens,
  **37 critic findings / 17 capture gaps**, every load-bearing claim
  R-lens-verified on the main thread) found the recorded four-item list is a
  **SUBSET** of the work — full record at D4
  [§ the attempt-3 audit](../2026-07-28_CIU_T6_PATH_SELECTION_D4.md#p4e-1b-iii-audit3),
  findings A1–A7. Headlines: the position rule is **derivable and total** from the
  reader's mint (a bare `$postfix-star` can never be a payload's first item, so
  `cur = #f` ⇔ subject, `cur` non-`#f` ⇔ cons onto the branch — **no
  `cur-subbed?` test**), and **six** arrival positions are measured where D4
  recorded one · the patch's `(list #f)` **refuses `m2{a* b}`**, Q_U40's own
  headline expressivity example (**found by the owner's uniformity observation,
  not by a facet**) · the **third** instance of the revert shape is untouched
  (`select-branch-keyless?`) · the mid-branch sentence D4 says exists in
  `reduction.rkt` **does not exist** · the patch's headline new instrument is
  **vacuous** while its comment claims it caught the real defect.
  ⚠ Start from `slice-1b-iii-attempt2.patch` (it `git apply --check`s clean at
  `7fd25f35`), not from scratch — its four round-1 fixes all HELD (the
  caret-after-star abort became a per-command error, both branch orders of
  `m{name tags*}` gave the guided L4 error, Q_U44's canonical order and the
  `mm{zz* aa* mm*}*` recovery spelling both worked).
  The four RECORDED items, all still owed:
  · **(a)** the `$postfix-star` parser arm ignores `cur` and unconditionally
    calls `(closed-acc)`, so an in-block postfix star is re-based onto the
    SUBJECT — `vh{0.{0}*}` → `concat(vh)` at ZERO errors, where HEAD gave a
    guided error. **PIN an in-block postfix star; nothing covered it.**
    ⚠ **FRAMING CORRECTED 2026-08-12 by reading the patch**: it is *not* "all
    three siblings respect `cur` and this one does not". **TWO** do — the ω arm
    and the `$dot-access` arm, both genuine CONTINUATION arms. The third
    (`star-sym?`, the fused `database*` band) also calls `(closed-acc)`
    unconditionally and is CORRECT to, because a bare name always starts a
    branch. The real defect: the bare `$postfix-star` sentinel is the one arm
    that can arrive in **either** position — branch-initial as the outer
    `$select-path` carrier, continuation when written in-block — and it assumes
    only the first. **The fix is a position decision, not a transplant**, and
    the missing pin must spell a postfix star AFTER A SUB-BLOCK, where `cur` is
    non-empty.
  · **(b)** sort keyed-layer contents on the keyword **NAME** with `symbol<?`,
    matching `make-record`. Attempt 2 sorted `(format "~a" key)`, i.e. the
    transparent STRUCT's display form, which diverges whenever one key prefixes
    a sibling whose next char is below `)` — `bang{a a!}*` → `@[2 1]` against a
    `symbol<?` type. **The comment AND the pin both asserted `symbol<?`**; the
    code did not keep it.
  · **(c)** split `star-not-yet` into distinct failure kinds — mid-branch, LEAF
    (a **PERMANENT** error per Q_U40, not "not yet"), and nominal each need the
    true sentence. ⚠ **CORRECTED 2026-08-12 (audit A4): the claim that "the
    accurate mid-branch wording already exists in `reduction.rkt` and is
    unreachable" is FALSE at HEAD** — `grep flatten reduction.rkt` returns only
    `flatten-union`/`flatten-app`. That sentence is **patch-only text**; attempt 3
    starts from HEAD and would have hunted for it and found nothing. ⚠ And the
    split walks into a **silent** `[else #f]` catch-all in `format-select-fail`
    (typing-errors.rkt:600) with three `string-append` nesting sites over it and
    four blanket handlers above that — decide it deliberately.
  · **(d)** add a battery key set that SEPARATES `symbol<?` from struct-display
    order (`{:a :a!}` is the shape — `!` sorts below `)`).
  Then **1b-iv** (NOMINAL semantics + Q_U38's typing-seated collision refusal +
  `*_`), the closer-adjacent `*_` mint slice (Q_U39), **P4e-2** (the `.*`
  retirement inventory + Q_U26 ravel), then PF → P5 → PX → P6 → X.close.
- **The rulings — do not revisit** (all in D4 §3, link by anchor):
  ⭐⭐ **[Q_U45](../2026-07-28_CIU_T6_PATH_SELECTION_D4.md#q-u45)** (NEW
  2026-08-12): **L★'s qualifier WIDENS.** The law — written down in one piece for
  the first time — is **`x{p₁* … pₙ*} ≡ x{p₁ … pₙ}*`**, and it holds with no
  intervening ω **AND where the branches' contents are MAPS**. It fails for
  vectors on **ARITY, before order ever enters**: `mm{zz* aa*}` is an n-TUPLE of
  vectors (each branch contributes ONE keyless component — Q_U44's own text)
  while `mm{zz aa}*` is one FLAT vector. The Map side is equal only because a
  Map-valued branch star **SPLICES** its keys, the same operation the result
  form's keywise join performs; every worked example behind L★ used Maps.
  ⭐ The non-equivalence is **Q_U44's mechanism seen from the other side**, not a
  collision with it. ⚠ Precision: the Map join is order-insensitive because the
  RESULT IS A MAP, **not** because of champ hash ordering — hash order was the
  defect Q_U44 killed. Three regimes: **Map→Map** order never arises ·
  **Map→vector** order is manufactured ⇒ CANONICAL · **keyless→vector** order is
  already there ⇒ WRITTEN.
  ⭐⭐ **The surface-observable form is the better test** [owner]: absorption is a
  **LEGALITY** difference, not only an equality one — `m2{a* b}` is legal (splice
  `a`, keep `b`'s key) while `mm{zz* aa}` is an L4 mixed-sorts error. Checkable
  **without computing the answer**, so it is what the battery should assert.
  ⭐⭐ **[Q_U40](../2026-07-28_CIU_T6_PATH_SELECTION_D4.md#q-u40)**: `*` attaches
  **OUTER** — it deletes the container layer the PRECEDING STEP contributed,
  joining its contents, and the join's SORT follows the **CONTENTS** (vectors
  concat · Maps join keywise · keyless components concat · leaves error). ONE
  rule over both axes. Law **`x{p₁* … pₙ*} ≡ x{p₁ … pₙ}*`** ⚠ **only with NO
  intervening ω** (`m:{a* b*}` ≢ `m:{a b}*` — different layers, one apart; **a
  test must pin the NON-equivalence**). `vv:{0 1}*` → `⟨1 2 3 4⟩` is a matrix
  ravel with no special case.
  ⭐ **Q_U42**: the keywise join CONCATENATES same-key VECTOR values — so the
  join is ONE recursive rule at every depth and nothing is inherited by
  elimination. ⭐ **Q_U44**: a keyed layer's positional join takes **canonical
  key order**, and `mm{zz* aa* mm*}*` recovers WRITTEN order — the user opts
  into KEYLESSNESS, already the language's carrier of positional meaning.
  ⚠ Its obligation: `mm{zz* name}` mixes keyless with keyed and must give a
  **guided** L4 error (users following the documented recovery spelling hit it
  whenever they mistype).
  **Q_U38**: the nominal-join collision is **REFUSED**, conservative on the
  `'dyn` tail; the gate MOVES to typing, because `select-branch-top-keys` takes
  exactly ONE parameter and no parse-gate arm can ever see a subject-derived
  key. **Q_U41**: `*_` is NOMINAL-ONLY. **Q_U43**: the L4 SORT check joins
  Q_U38 at typing. **Q_U39**: the closer-adjacent `*_` mint is a follow-on
  slice. Standing from 1a: **Q_U36** (positive-list fuse over the PREDECESSOR)
  and **Q_U37** (the refusal is decided IN TERRITORY).
  ⚠ DEFERRED 105's REPRESENTATION blocker is discharged by Q_U40 (the wrapper);
  its COUNT half is NOT. Do not reopen R4 on this ruling alone.
- **Implementation facts that outlast the slice**:
  · **TYPING AND REDUCTION ARE ATOMIC TWINS** — `reduction.rkt` says landing
    either alone is *"not a half-measure but a REGRESSION"*. They disagreed by
    exactly one container level once (right value, wrong type). Typing uses
    `select-below-field`, reduction `below-value`.
  · **A raw `error` on these paths is a WHOLE-FILE ABORT.** Channels are
    `select-fail` (typing) and `(return (expr-panic …))` via reduction's single
    `let/ec`. The abort signature is **EMPTY** output, not partial — so every
    probe file needs a successful top-level `data` decl plus `def before` /
    `def after`.
  · ⚠ **`sort` is SHADOWED inside `select-reduce`** by the selection sort
    (`'path`/`'block`); calling Racket's `sort` there applies `'path` as a
    procedure. Hoist helpers to module level.
  · The star is a **BRANCH-LEVEL transform, not a step arm** — it belongs with
    `branch-entries`' pre-classifications (`col`, `select-branch-keyless?`),
    because it removes the label the preceding step would have contributed. A
    `case` arm for `'star` fires too late to unmake it.
- **Open / blockers**:
  · **DEFERRED 101** — the unspellable non-identifier carriers + the recorded
    analysis of why both mint attempts failed. The design gap itself.
  · **DEFERRED 103** — ✅ RULED by Q_U32 as a language ruling. ⚠ Its SIBLING is
    NOT discharged: `compile-match-tree` aborts on a multi-form LITERAL arm
    (`| 2 3 ->` → `list-ref: contract violation`, `macros.rkt` **`:11333`** —
    coordinate re-measured), and it reproduces in **sexp**, so it is an IR
    defect.
  · **DEFERRED 104** — a bare operator bound by `def` yields a STUCK TERM at
    ZERO errors while `let` binds it correctly. Reproduces with `+`, so it is
    the first-class-operator surface, not a star defect.
  · **DEFERRED 105** — R4 is NOT independently landable (count half live).
  · **DEFERRED 106 + 107** — both PRE-EXISTING and differential-proven. 106:
    the `let` nested-shorthand binding value leaks a raw sentinel, reached by
    none of the four `rewrite-dot-access` seats; it BOUNDS Q_U35's "dissolves
    that whole blast radius". 107: carrier-plus-star in PATTERN position mints
    and is unruled — `c.a` in the same seat gives `h defined (arities: 1, 3)` at
    ZERO errors.
  · **DEFERRED 108** — a trailing `*` in PATTERN or MATCH-SCRUTINEE position is
    a WHOLE-FILE ABORT: **19 cells** (was 21; the 1a-iii fuse retired two).
    PINNED as a measured snapshot, not skipped.
  · **DEFERRED 109–112** — the `spec` type-region mis-fusion + raw syntax
    objects at the user · the pipe-terminal fused star (`expand-pipe-block`'s
    unwrap ambiguity, pinned AS an accident) · the raw-datum-echoing error-site
    CLASS (two fixed, census unclaimed) · `$facts-sep` leaking as an unbound
    variable.
  · **A CLAIMED live defect that does NOT reproduce**, recorded so it is not
    re-raised: `$bcast-step` is genuinely ABSENT from `pattern-var?`'s exclusion
    list, and a critic predicted a whole-file abort for a broadcast inside a
    `defmacro` TEMPLATE. **Three shapes probed at `19560a7c`: all clean at 0
    errors.** The absence is real; the predicted symptom is not. The exclusion
    list is **20** entries; **23** is the count of reader-emittable `$`-symbols
    that REMAIN misclassified (of 33) — two different quantities that had merged
    into one figure. ⭐ The census states it best: `pattern-var?`'s residual is
    now **SILENT rather than LOUD**, so a miss no longer announces itself.
  · Live DEFERRED: 31, 33, 34, 35, 39, 40-residual, 41, 44, 54, 56, 57, 58, 62,
    63, 65, 75/76, 77–84, 88, 89, 91, 92, 93–101, 104–112.
  · Chips out: `task_204859b9` · `task_4c00d3f0` · `task_e1c15ee6`.
- **Watching**:
  1. **The mini-audit / probe keeps refuting the premise — including MY OWN
     RECOMMENDATIONS, the design's own text, and an adversarial critic's.**
     **PROMOTION OVERDUE** — fold into the rules tier at the next close.
  2. **The design's own enumeration under-counts** — 11th+ arc: 1b-i measured
     the "COMPLETE 13-site recipe" at **59 live sites / 8 genuinely omitted / 2
     silent trapdoors**, and *four of the eight are functions the sixth kind
     itself introduced*. **An enumeration cannot be validated by the member it
     was written for.**
  3. **A failing test is only evidence if it fails for the reason you claim.**
  4. **A claim is only evidence at the layer you measured.**
  5. **An A/B over a dirty tree measures the tree, not the change.**
  6. **A green suite proves nothing for a class no file spells** — ≥5 this arc.
     **Promotion candidate alongside item 1.**
  7. **HEAD moves; handoff coordinates go stale fast.**
  8. **The MEASUREMENT can be the broken thing, not the code** — 3rd data point
     at 1b-i: an attribution pass matching only column-0 `define` reported **41**
     sites outside the recipe against a true **8**, a ~3× over-count that would
     have gone into a commit message. ⭐ **A measurement whose output contains an
     impossibility is telling you about itself** (it claimed six recipe entries
     had no live sites, which cannot be true of a shipped recipe).
  9. **WIDENING a slice mid-flight is where I introduce defects** — 3 data
     points. **Budget for the second verify, not just the first.**
 10. **A FACT HAS A TIMESTAMP, and a partial revert is how one rots quietly.**
 11. **AN AMBIENT RULE CAN GO STALE, AND NOTHING RE-READS IT** — `pipeline.md`'s
     `defmacro`-template abort claim, corrected at `79e34380`. ✅ Discharged, but
     the class stands: `.claude/rules/*.md` is the ONLY ambient tier.
 12. **NEW (2 data points, same day) — I WRITE A PRECONDITION INTO A COMMENT AND
     THEN INVALIDATE IT A SLICE LATER WITHOUT RE-READING MY OWN WARNING.** Both
     1b-iii reverts have this shape: `select-branch-top-keys`' *"safe ONLY
     because the star cannot reach this walk at 1b-ii (the parser refuses
     first)"* — and the seat migration is precisely what makes the star reach a
     block; and the `$postfix-star` arm's *"the star arrives alone"* — true only
     for the OUTER carrier. **Before changing a seat, grep your own comments at
     that seat for their preconditions.** Sibling of item 10 (a fact has a
     timestamp) applied to comments rather than measurements.
 13. **NEW (2 data points) — A CHAINED COMMAND THAT SHORT-CIRCUITS LOOKS EXACTLY
     LIKE ONE THAT PASSED.** A failed `cd` in an `&&` chain silently skipped a
     compile step twice, and a grep-based gate read the EMPTY capture as green.
     **A grep-based green must prove the run HAPPENED**, not merely that the
     output lacks the word. Run from the repo root.
 14. **NEW — the ADVERSARIAL VERIFY is worth DELEGATING to a workflow.** It
     costs no main context and it caught what would have shipped, twice. Feed it
     the diff, the rulings, the already-fixed defects, and tell it to attack
     **THE FIXES** — round 2 finding defects in round 1's fixes is this track's
     single most reliable pattern.
 15. **NEW — the WORKED EXAMPLES are the instrument.** Three sessions running,
     the thing that found the defect was the owner asking to see it worked out
     in examples — not a gate, not a verify round, not the audit. Q_U40's law
     was missing its no-intervening-ω qualifier and Q_U41's third argument was
     struck, both found that way.

---

## LOG  (append per commit / decision / surprise — newest at the BOTTOM)

*(The P4e-1b arc's entries — i through viii — are in `2026-08-11_dailies.md`;
the P4e-1a arc's — xxvi through xxix — in `2026-08-10_dailies.md`. This file
opens at the 2026-08-12 working-day boundary, with 1b-iii attempt 3 as the
resume target.)*

### (i) THE 2026-08-12 INTERVAL OPENS — and re-reading the parked patch found the work order pointing at the wrong fix — `83ae4714` · `596ca323`

**The interval**: the 2026-07-28 standup's window closes at **364 commits over
15 days** — CIU T6 D4 from the lexical seams to a working broadcast and a
half-built flatten (P1 → P4e), alongside QTT P1–P7, the LET track, ARROW T1,
SolveCarrier, the two-spine convergence (**29% → 98%** agreement), a DEFERRED
clearing run (51/51c/52, 57–59, 61, 102) and a build-time recovery (warm
precompile **34.7s → 1.2s**). Standup + dailies created as a pair; the dailies
rolled at 616 lines with the STATE head copy-forwarded and the prior file's
accreted historical layers left behind rather than carried.

**⭐⭐ THE FINDING, and it is a regrounding finding rather than a coding one: the
work order I was about to execute mis-framed its own headline defect, in a
direction that named the wrong repair.** D4's attempt-3 list said the
`$postfix-star` parser arm should *"respect `cur` like its three siblings"*.
Reading the parked patch instead of the summary: **two** siblings respect `cur`
(the ω and `$dot-access` **continuation** arms); the third — `star-sym?`, the
fused `database*` band — also calls `(closed-acc)` unconditionally and is
**correct** to, because a bare name always starts a branch (it sits beside
`plain-key?`, which does the same). `closed-acc` is
`(if cur (cons (reverse cur) acc) acc)`, so calling it closes the branch in
progress and opens a new one holding only the star — which *is* the
branch-initial reading, i.e. operate on the subject.

So the defect is not a forgotten transplant. **The bare `$postfix-star` sentinel
is the one arm reachable in BOTH positions** — branch-initial as the outer
`$select-path` carrier, continuation when written in-block — **and it assumes
only the first.** The fix is a position decision; "copy the sibling" would have
been wrong. And it re-specifies the missing pin: it must spell a postfix star
**after a sub-block** (`vh{0.{0}*}`), where `cur` is non-empty — **not**
`vh{0*}`, where the star fuses into the lexeme and takes the `star-sym?` arm
instead. Corrected at the site and in the attempt-3 list, with the reasoning
recorded rather than the sentence quietly swapped.

**Also corrected**: *"parked at the session scratchpad"* no longer resolves —
the patches are in the **`311a1847-…`** session directory and the current
session's scratchpad is empty. Full path now recorded in D4, the STATE head and
the memory file. A future session would have looked in its own scratchpad,
found nothing, and rebuilt 427 lines from scratch.

**The shape worth naming, because it is *Watching 12* one level up**: that
track's own recurring lesson is *"I wrote a precondition into a comment and then
invalidated it a slice later without re-reading it."* This is the same failure
displaced onto a **record**: a summary written from an implementation inherits
its author's reading, and the next session executes the reading rather than the
code. **Read the artifact the record describes, before executing the record.**

**Gate**: docs only, no production code, no test delta. Two commits — the
interval pair, then the correction (kept separate so the correction is legible
as a correction rather than folded into a housekeeping commit).

**Next**: 1b-iii **attempt 3** — open with the mini-audit, then the four fixes
(the position decision + its pin · `symbol<?` on the keyword NAME · the
`star-not-yet` split into mid-branch/leaf/nominal · the `{:a :a!}` key set),
typing and reduction landing **atomically**, with the adversarial verify
delegated to a workflow and round 3 budgeted.

### (ii) THE ATTEMPT-3 MINI-AUDIT — the four-item list was NOT the work, and Q_U45 widens L★'s qualifier — `781b8f75`

**The audit** (`wf_bbf1169e-340`, 6 partitioned read-only facets + the adversarial
completeness critic, HEAD-pinned `7fd25f35`, ~1.55M tokens): **37 critic findings,
17 capture gaps, 16 R-lens targets.** Every load-bearing claim R-lens-verified on
the main thread before use. Three of the four recorded items stand; the list is a
**subset**, and one of the four rested on a sentence that does not exist.

**⭐⭐ [Q_U45](../2026-07-28_CIU_T6_PATH_SELECTION_D4.md#q-u45) [owner] — L★'s
qualifier widens.** The law had been cited constantly and **written down in one
piece nowhere**, which is how the gap survived three sessions; it is now stated
once. It fails for vectors on **ARITY, before order enters**: the distributed form
is an n-tuple of vectors, the result form one flat vector. The Map side is equal
only because a Map-valued branch star SPLICES its keys — the same operation the
result form's keywise join performs — **and every worked example behind the law
used Maps.** The non-equivalence turned out to be **Q_U44's mechanism seen from
the other side**, not a collision with it, which is why the amendment costs one
clause and no shipped behaviour.

**⭐⭐ THE OWNER'S OBSERVATION FOUND A LIVE DEFECT A FACET DID NOT.** Asked to
deliberate, the owner noted that in the vector cases every branch must be starred
while the Map case tolerates a mix. That is the **surface-observable form of
absorption** — a LEGALITY difference, checkable without computing the answer — and
tracing it through the parked patch shows `(list #f)` makes **`m2{a* b}` an L4
error at the parser**, i.e. the patch refuses **Q_U40's own headline expressivity
example**. Fourth session running that the thing which found the defect was the
owner asking to see it worked out (*Watching 15*).

**⭐⭐ THE POSITION RULE IS DERIVABLE, TOTAL, AND NEEDS NO `cur-subbed?` TEST.**
The reader mints only for a `*` byte-adjacent to a preceding **closer**, and a
block's `{` is an **opener** — so a bare `$postfix-star` can never be a payload's
first item. Hence `cur = #f` ⇔ the outer carrier ⇔ the subject; `cur` non-`#f` ⇔
in-block ⇔ cons onto the branch. **Six** arrival positions measured where D4
recorded one, including one after an ORDINAL step that appears in no record — a
fix keyed on `cur-subbed?` would have covered one of six.

**⛔ AND MY OWN CORRECTION FROM YESTERDAY IS FALSIFIED.** I wrote that `vh{0*}`
"fuses into the lexeme". Measured: nothing fuses — the mint needs a preceding
closer and `0` is a number token — so the in-block ordinal band is unimplemented
and its message tells the user to do what they already did. **Kept in place rather
than silently repaired**, because the pattern is the finding: I inherited a
reading instead of measuring it, which is the same failure the correction was
written to fix. *Watching 12 has a third data point, and it is mine, one day
later, in the artifact I wrote to prevent it.*

**Also found**: the third instance of the revert shape sitting untouched
(`select-branch-keyless?` still justifies its `#f` with the precondition the seat
migration deletes, so two classifiers in one file will give opposite sort answers
for the same branch, both pinned) · the mid-branch sentence D4 says "already
exists in reduction.rkt" **does not exist** — it is patch-only text, written from
the patch's perspective as if applied · the patch's headline new instrument is
**vacuous** (`p4e1-has?` ormaps the setup `def` lines, whose type contains the
regex) while its comment calls it the check that caught the real defect · Q_U44
names half of `make-record` · the fail-kind axis has a silent `[else #f]` with
four blanket handlers over it · the twins diverge on an empty layer and on `seen`
· `rrb-concat` already exists · the "SHARED walk" safety comment is false (zero
references in either twin) · `pp-expr` will echo the nonexistent `c{a}.*` · `*_`
has no postfix carrier at all.

**One audit claim DOWNGRADED**, and the reason matters: a facet traced a
whole-file abort whose precondition the position rule proves impossible. That is
**an argued-unreachable precondition living in an audit** — precisely the artifact
class that caused both reverts. It owes a comment at the four raise sites plus a
tripwire, not a note in a document.

**Gate**: docs only, no production code, no test delta. Probe run under
`scratch-run.sh` (one file, reaped); the parked patch `git apply --check`s clean.

**Next**: agree the attempt-3 **re-slice** with the owner — the audit's findings
do not fit one atomic slice, and *Watching 9* says mid-flight widening is where
this arc introduces defects.
