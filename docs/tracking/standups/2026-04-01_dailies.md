# Dailies — 2026-04-01

## Carried Forward from Previous Interval

### PPN Track 2B ✅ COMPLETE
- Merge deployed (source-line-keyed). Mixfix Pocket Universe. use-tree-parser? deleted.
- 382/382 GREEN, 7454 tests. PIR written.

### SRE Track 2G ✅ COMPLETE
- Algebraic domain awareness: property cells, domain registry, meet ops, ring action, inference with witnesses, implications, diagnostic reporting, property-gated behavior.
- FINDING: type lattice NOT distributive → SRE Track 2H (type lattice redesign).
- 383/383 GREEN, 7491 tests. PIR written (full 16-question methodology). A/B: zero regression.

### Priority Ordering (from 2026-03-30)
1. **PPN Track 3** — parser as propagators (next)
2. Grammar Form research (speculative design, parallel with PPN 3/4)
3. SRE Track 2H — type lattice redesign (prerequisite for PPN 4)
4. PPN Track 4 — elaboration on network

### Process Improvements Codified (March 30 session)
- Propagator Design Mindspace (DESIGN_METHODOLOGY.org Stage 3)
- Implementation Protocol (DESIGN_METHODOLOGY.org Stage 4)
- NTT Model REQUIRED (DESIGN_METHODOLOGY.org Stage 3)
- Validated≠Deployed + "Pragmatic" Ban (DEVELOPMENT_LESSONS.org)
- 7 Topology Design Patterns (PTF Master)

### Known Gaps / Blockers
- test-structural-decomp: meta-solving via imperative state under BSP
- WS-mode solver syntax: `=` unification, `is` bridging
- Type lattice not distributive (SRE Track 2H scope)
- Module-load-time infrastructure not on network (PM Master Track 8 audit)

### Deferred Work
- Phase 5 (preparse parameterization): shelved — soon replaced by Track 3
- Heyting pseudo-complement: deferred to SRE Track 2H (type lattice redesign)
- CDCL for Boolean domains: UCS Track 3
- Quantale resource tracking: UCS Track 2

### Watching (medium-term patterns)
- **Implementation Protocol correlation**: Track 2G used it, 0 reverts. Track 2B didn't, 3+ reverts. N=2, watching.
- **Mathematical assumptions as new wrong-assumption frontier**: Track 2G's distributivity assumption was mathematical, not code. First instance. Watching.
- **NTT modeling catches impurities**: 2/2 instances → codified as mandatory.

---

## Lessons Distillation Check

### Scanned: Track 2B PIR + Track 2G PIR + March 27-31 dailies

**Ready for codification (flagged READY in prior dailies, still pending):**

1. **"Merge/hybrid over wholesale replacement"** — 5 instances (PM T10, PM T8, PPN T2, PAR T1, PM T10B). Flagged READY since March 30. **→ DEVELOPMENT_LESSONS.org**
2. **"Integration phases find all the bugs"** — 8 of 11 PIRs. **→ DEVELOPMENT_LESSONS.org or DESIGN_METHODOLOGY.org**
3. **"Wrong assumptions at boundaries"** — 9 of 11 PIRs. Most persistent. **→ DESIGN_METHODOLOGY.org** (explicit audit step)
4. **"Vision contaminates audit"** — 6 instances. Flagged READY since March 26. **→ DESIGN_METHODOLOGY.org** (audit discipline)
5. **"Resist algorithmic thinking: look for the lattice"** — 3+ instances (Track 2B: queue→identity join, Pratt→claim lattice; Track 2G: scatter→Pocket Universe). Track 2B PIR explicitly recommends codification. **→ DESIGN_PRINCIPLES.org** (reinforces Propagator-First)

**New candidate from Track 2B PIR:**
- **"Validated on test path ≠ validated on production path"** — extends Validated≠Deployed. Track 2B §12 documents this. 1 strong instance. **WATCHING** for 2nd.

**Already codified:**
- CALM requires fixed topology (DEVELOPMENT_LESSONS.org)
- Diagnostic protocol (workflow.md)
- Pre-0 benchmarks shape design (DESIGN_METHODOLOGY.org)
- Two-lens self-critique (DESIGN_METHODOLOGY.org)
- Validated≠Deployed + Pragmatic ban (DEVELOPMENT_LESSONS.org, workflow.md)
- NTT model required (DESIGN_METHODOLOGY.org)
- Implementation Protocol (DESIGN_METHODOLOGY.org)
- Propagator Design Mindspace (DESIGN_METHODOLOGY.org)

**NOTE**: 5 patterns have been flagged READY across 2+ dailies without being codified. This backlog risks the lessons going stale. Consider a dedicated codification session.

---

## Today's Work

### Completed

1. **PPN Track 3 Stage 2 audit** (commit `9bb0804c`)
   - Grep-backed inventory of parser.rkt (6,605 lines, 73 parse-* functions, 236 keyword heads)
   - Audited macros.rkt sexp expanders (21 expand-* functions, ~500-600 deletable lines)
   - Tree-parser coverage gap: 8 error stubs (data/trait/impl/session/defproc/defr/quote/solver)
   - **Key finding**: parser.rkt is a PURE function — 0 registry reads, 1 boolean parameter
   - **Key finding**: parse phase = 0ms wall time; reduction (64%) and type-check (18%) dominate
   - **Key finding**: The hard problem is `preparse-expand-all` registration, not parsing
   - **Design implication**: Track 3 value is architecture/incrementality/extensibility, NOT performance
   - Phasing suggestion: close tree-parser gaps → move registration to tree level → delete sexp expanders → formalize as propagators → shared cells → retire parser.rkt

2. **PPN Track 3 Stage 3 design D.1** (commit `1da543c6`)
   - 10-phase design: close tree-parser gaps (Phases 1-2) → registration migration (Phase 3) → delete sexp expanders (Phase 4) → production registry as Pocket Universe (Phase 5) → per-form Pocket Universe architecture (Phase 6) → shared cells replace merge (Phase 7) → retire parser.rkt (Phase 8)
   - **Propagator Design Mindspace**: Four Questions answered. Per-form parse lattice (⊥→raw→tagged→grouped→rewritten→parsed). Production registry as monotone set. Identity IS the cell (structural, not computed).
   - **NTT model**: FormCell Pocket Universe, ProductionRegistry, dispatch-production. Registration explicitly scaffolding (PM series migrates to cells). No impurities detected.
   - **Key design decision**: parser.rkt DEMOTED to sexp compatibility shim, not fully deleted. Full deletion when sexp path retires.
   - **Key design decision**: Registration stays on parameters (scaffolding). Structured for PM migration. Two-pass ordering (specs first).
   - **Lessons from discussion**: Module-loading as PM scope (aware, not solving). Pocket Universe for cell allocation control. Per-form stratified Pocket Universes for different rewriting complexity.
   - Next: D.3 self-critique cycle

3. **PPN Track 3 Pre-0 benchmarks** (commit `dc87fb34`)
   - 19 tests: M1-M7 micro, A1-A5 adversarial, E1-E4 E2E, V1-V3 algebraic validation
   - Parse = 0-4% of pipeline. WS overhead <2%. Set-valued registry: zero overhead at scale.
   - **ALL algebraic properties PASS**: commutativity, associativity, idempotence, distributivity, Heyting pseudo-complement
   - Results incorporated into design §8-§9 (commit `c2028afd`). No design changes needed.

4. **PPN Track 3 D.3 self-critique** (commit `140babd8`)
   - Lens 1: 6 principles findings. Registration is imperative scaffolding. Two-pass spec ordering is algorithmic workaround. merge-form has impure spec-store dependency.
   - Lens 2: CRITICAL — registration functions are 3,017 lines of interleaved parse+register. Phase 3 scope significantly underestimated.
   - Propagator Mindspace: 3 red flags detected (for/fold, sequential ordering, parameter read in merge).

5. **PPN Track 3 D.4 revision** (commit `8f254daa`)
   - Dependency-set replaces stage chain: transforms as powerset (Boolean). Critical pair analysis: T(0)||G(0), all rewrites independent.
   - Spec cells: per-function cells replace two-pass ordering. Defn residuates until spec written. Propagator-native.
   - Phase 3 → 3a-3e: spec cells, data/trait-impl/spec registration extraction, generated-def injection.
   - Registration as data descriptors (not side effects). FormCell product lattice upgraded Heyting → Boolean.
   - parser.rkt honestly labeled incomplete. Full retirement at PM module-loading-on-network.

6. **PPN Track 3 D.5 external critique** (commit `a0de1346`)
   - 10 findings, all accepted and incorporated.
   - F2 (HIGH): Four-pass dependencies + trait→spec critical pair — modeled as cell residuation.
   - F4 (HIGH): Spec cell merge commutativity violation — fixed: collision = ⊤.
   - F5 (MEDIUM): Simplified dependency-set — sub-expression transforms internal to productions.
   - F7 (HIGH): 7 missing form types added to Phase 1b scope.
   - F8 (HIGH): Dual-mode registration interface (Two-Context Audit).
   - F9 (MEDIUM): FormCell product corrected Heyting (not Boolean).
   - F10: Phase dependency DAG — 5 parallel workstreams.

7. **PPN Track 3 acceptance file** (commit `a3034a75`)
   - 7 sections: core forms, data/trait/impl, preparse-consumed forms, complex expressions, cross-form dependencies, generated defs, integration.
   - 0 errors baseline. All computed values correct.
   - Two pre-existing issues documented (single-field data ctor typing, eval before pipe syntax).

8. **PPN Track 3 Phase 1b: subtype + selection** (commit `27d22906`)
   - Mini-audit finding: of 8 Phase 1b forms, only subtype + selection have surf-* structs. The other 6 (deftype, bundle, defmacro, property, functor, schema) are preparse-consumed — same pattern as data/trait/impl. These defer to after Phase 6.
   - Also found Phase 1a (data/trait/impl) is same pattern — no surf-data/trait/impl structs exist. Phases 1a and 3b-3e merge into combined work after Phase 6.
   - Implemented: parse-subtype-tree, parse-selection-tree + helpers. Added tag-subtype to surface-rewrite.rkt.
   - All targeted tests pass. Acceptance: 0 errors.

9. **PPN Track 3 Phase 5: dependency-set Pocket Universe** (commit `9f3c63dc`)
   - form-pipeline-value.transforms replaces .stage: seteq powerset (Boolean) instead of symbol chain (Heyting)
   - transform-deps hash declares dependencies per transform. advance-pipeline fires first ready transform.
   - Merge = set-union (commutative, associative, idempotent). External API unchanged.
   - Inline tests updated: merge commutativity, transform advancement, run-form-pipeline reaches done.
   - Acceptance: 0 errors. surface-rewrite: 31 pass. WS regression: all pass.

10. **PPN Track 3 Phase 6: per-form cells** (commit `7a2a4bd0`)
    - New module form-cells.rkt: create-form-cells-from-tree, dispatch-form-productions, form-cell-ref.
    - 1 infra-cell per top-level form on elab-network. Merge = Phase 5 set-union.
    - Verified: 3-form → 3 cells. 8-form → 8 cells, all correct tags, all done.
    - Mini-audit finding: parse-tree-node srcloc is `(line col start end)` list, not srcloc struct. First element is line.
    - Gate phase unlocked: 3a (spec cells), 1a+3b-3e (consumed forms), 7 (merge retirement).

11. **PPN Track 3 Phase 3a: spec cells** (commit `bd13dfbb`)
    - spec-cell-value: name, type-surf, metadata, top? Merge: bot+x=x, x+x=x, x+y=top.
    - D.5 F4 fix verified: collision = top, commutativity confirmed.
    - extract-specs-from-form-cells: scans form cells for spec tag, creates per-function spec cells.
    - Tested: 2 specs from 6-form program. Collision detection works.
    - Consumption (defn reads spec cell) is Phase 7 scope.

12. **PPN Track 3 Phase 7: form cells wired into driver** (commit `40d07caa`)
    - driver.rkt imports form-cells.rkt. merge-preparse-and-tree-parser now creates form cells + dispatches productions + extracts spec cells alongside existing merge.
    - extract-surfs-from-form-cells: produces identical output to direct tree-parser pipeline (verified).
    - Cell maps stored in current-form-cell-map / current-spec-cell-map parameters.
    - Merge still drives process-command. Full switch after Phase 4 (preparse deletion).

13. **Full test suite regression gate**: 383/383 GREEN, 7491 tests, 133.5s. No regressions from Phases 1b/5/6/3a/7.

---

## Considerations

- 5 patterns flagged READY for codification — backlog growing.
- PPN Track 3 D.1 complete. Design cycle continues with D.2 (self-critique + Pre-0 benchmarks).
- Registration migration (Phase 3) is the architectural crux — two-pass ordering for specs, generated def production, load-module path.
- parser.rkt demoted to sexp shim rather than deleted — cleaner separation, sexp path retained for REPL/test.
- The audit reveals that Track 3's early phases (closing tree-parser gaps, moving registration) are prerequisite infrastructure before the core propagator migration. Consider splitting into Track 3A (prerequisite: tree-parser coverage + registration migration) and Track 3B (core: grammar productions as propagators).
- SRE Track 2H (type lattice redesign) is prerequisite for PPN 4 but not for PPN 3.
- `preparse-expand-all` registration side effects are the architectural crux — registration as cell writes (SRE 2G domain registry pattern) is the propagator-native solution.

