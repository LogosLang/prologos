# 2026-03-15 Dailies

**Session start**: 2026-03-15
**Previous dailies**: 2026-03-14 (covered WFLE full implementation, D4 provenance, WS syntax fixes, generic operators, relational gap fixes, formal modeling research)

---

## Completed

- **Track 3 Phase 0**: Performance baseline + acceptance file (commit `a5408f0`, `4ba09f4`)
  - Acceptance file: 13 sections, ~712 lines, 0 errors at L3 (process-file)
  - Baseline: 194.3s / 7096 tests / 370 files
  - Fixed `double` definition for L3 (brackets needed for multi-arg: `[add x x]`)
  - Discovered HKT Foldable resolution fails at L3 — commented out with BUG annotation
- **Track 3 Phase 1**: Core type registries → cell-primary (commit `a7f61ca`)
  - 8 readers + elaboration guard, 14 computation reads converted
  - Critical design discovery: `current-macros-in-elaboration?` guard pattern
  - See "Lessons Learned" for direct parameter mutation leak issue
- **Track 3 Phase 2**: Trait + instance registries → cell-primary (commit `0880c4a`)
  - 7 readers, 12 computation reads converted across macros.rkt, elaborator.rkt, trait-resolution.rkt, driver.rkt
  - Purely mechanical — Phase 1 pattern applied directly, no surprises
  - 189.5s / 7096 tests (no regression)

## In Progress

- **Track 3 (Cell-Primary Registries)**: Phases 0-2 complete, continuing through Phases 3-7

## Considerations

- Prior interval was exceptionally productive: two complete subsystem implementations (WFLE 7 phases + D4 Provenance 7 phases), ~40 commits, ~125 new tests
- Track 2 Phase 7 (Error Cell) has an approved plan — infrastructure for error descriptor cells, resolution callback writes, post-fixpoint sweep reads cell
- Track 3 (Cell-Primary Registries) has a completed Stage 2/3 design doc (`30f02fb`) — 6-phase plan for converting 24 registry parameter reads to cell-primary
- Several .prologos example files have uncommitted changes from prior interval
- Test count: 7096 (370 files, 2 skipped), performance ~200s

## Lessons Learned

- **Direct parameter mutation leaks outside elaboration scope**: `register-macros-cells!` sets `current-macros-prop-net-box` and cell-id parameters via direct `(current-X val)` mutation (not `parameterize`). These persist beyond the elaboration scope, causing test assertion failures where cell reads see stale/cumulative data. Solution: `current-macros-in-elaboration?` boolean set via `parameterize` in `process-command` provides proper scoping.
- **Elaboration-context guard is essential for all phases**: The `macros-cell-read-safe` + `current-macros-in-elaboration?` pattern is the structural boundary between elaboration reads (cell-primary) and non-elaboration reads (parameter fallback). Must be reused for all subsequent phases.
- **Phase 2 was purely mechanical**: Once the guard pattern was established in Phase 1, Phase 2 applied identically to 7 more registries with zero surprises. This validates the design doc's "mechanical pattern" characterization for Phases 1-4.

## Metrics

- Test count: 7096 (370 files, 2 skipped)
- Performance baseline: 194.3s (Track 3 Phase 0)
- Latest run: 189.5s (Phase 2 complete, no regression)

---

## Notes

New working-day interval begins. Prior interval completed two major subsystems (WFLE + D4 Provenance). Multiple implementation paths available: Track 2 Phase 7 (error cell, plan ready), Track 3 (cell-primary registries, design doc ready), or other priorities.
