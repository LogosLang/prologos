# 2026-03-12 Dailies

**Session start**: 2026-03-12
**Previous dailies**: 2026-03-11 (created with standup at ~09:00)

---

## Completed

### LSP Tier 4: Interactive Evaluation (REPL)
Full implementation of Calva/CIDER-style inline evaluation:

**Server side** (`16cbc46`):
- `repl-session` struct with persistent global-env, all registries
- Prelude caching (test-support.rkt pattern): load once, share across sessions
- `eval-in-session!`: parameterize → process-string-ws → snapshot state back
- 4 custom LSP methods: `$/prologos/eval`, `loadFile`, `typeOf`, `resetSession`

**Client side** (`a151413`):
- `forms.ts`: text-based top-level form detection (col-0 start, indentation walk)
- `decorations.ts`: inline result display (grey/red italic after-text)
- `repl.ts`: ReplManager orchestrating form detection → LSP request → decoration
- `extension.ts`: 5 commands wired, "Prologos REPL" output channel
- `package.json`: commands + keybindings (Ctrl+Enter, Ctrl+Shift+Enter, etc.)

### Propagator Observatory — Bugfixes & UX Improvements

**Contract violation fix** (`ff87fef`):
- `driver.rkt` passed `elab-network` (wrapper) where `prop-network` expected → extract via `elab-network-prop-net`
- Added `surf-defproc?` and `surf-session?` label extraction for descriptive capture names (`elab:defproc-greeter`, `elab:session-Greeting`)

**Observatory fallback** (`e5778cd`):
- "Propagator Network" panel used old `$/prologos/propagatorSnapshot` endpoint requiring BSP rounds; most files have 0 rounds
- Made `show()` fall back to `showObservatoryForUri(uri)` when no old-style trace exists

**Observatory UX overhaul** (`d276cce`):
- Force-directed layout for networks >100 nodes (replaces flat topological row layout that showed 450 cells in a horizontal line)
- Full cell labels in tooltips (`fullLabel` field); graph labels truncated at 24 chars
- Propagator tooltips show cell labels alongside IDs (e.g., `#147 (prologos::core::add)`)
- Captures sorted by propagator count descending with `(290c/196p)` suffix
- Tooltip max-width 300→500px, word-break for long content
- Error panel word-wrapping improvements

### Visualization Refinements — Phase A: Auto-refresh on file save (`c36b1ac`)
- `PropagatorViewManager.refreshIfOpen()` — debounced at 300ms, re-requests observatory for last URI
- `extension.ts`: `onDidSaveTextDocument` listener triggers refresh when `.prologos` file saved
- Tracks `lastUri` and `lastSelectedIndex` for preserving state across refreshes

### Visualization Refinements — Phase B: BSP round replay timeline (`ee0e6a1`)
- Range slider for stepping through BSP propagation rounds (0=initial, K=after round K)
- Reconstructs cell state by applying cellDiffs from trace.initialNetwork
- Changed cells highlighted with yellow ring + outer glow
- Round info bar: round number/total, diff count, propagators fired, contradiction indicator
- Cell-diff table shows old→new values with source propagator for current round
- Covers design phases 5a (slider UI), 5b (step-through), 5c (diff highlighting)

### Visualization Refinements — Phase C: Source-location linking (`16cb7ce`)
- Cell click in graph sends `revealSourceLocation` message to extension host
- Extension opens file, sets cursor at source line/col, reveals in center
- Pointer cursor + blue "Click to jump to line N" hint in tooltip for cells with source-loc

### Visualization Refinements — Phase D: Subsystem filter checkboxes (`ca7662a`)
- Checkbox row with color swatches for each subsystem in header
- Unchecking hides that subsystem's captures from dropdown
- Preserves current selection when possible, auto-selects first visible otherwise
- Addresses the 778-capture overwhelm problem from prelude loading

### Observatory — Module Capture Cache (`eb294bd`)
Capability inference captures (290c/196p networks) were invisible in the Observatory because prelude loading ran without observatory active, and cached module loads skipped inference entirely.

Fix: enable observatory during `load-prelude-cache!` in LSP server, cache per-module captures, replay them into per-file observatories on cached module loads. Three-file change: `prop-observatory.rkt` (cache infrastructure), `driver.rkt` (cache/replay calls in `load-module`), `lsp/server.rkt` (enable observatory during prelude, store cache in `lsp-state`, parameterize during elaboration).

### Observatory — Module Capture Cache Perf Fix (`8d9e919`)
Initial approach replayed all 775 prelude captures (33MB JSON, 6.3s serialize) into every per-file observatory, causing VS Code hangs. Fixed: merge only capability captures (10 of 775) at snapshot serialization time, not during elaboration. 1.4MB JSON, 0.9s serialize.

### Track 1: Constraint Cell-Primary — Design Document (`a82b05f`)
Stage 2 (Design) document for migrating constraint tracking from dual-write to cell-primary reads. 7 phases (0-6) with 25 subphases in the progress tracker. Incorporates outside critique: hand-rolled propagator framing, capability dual-write gate, hash-set! grep verification, focused speculation test list, propagator-edge mental model for dirty-flag removal, concurrency readiness note.

Prior commits: `020d1ce` (Track 0 draft), `f72ddc8` (Track 1 initial), `70d454b` (cross-domain section)

### Track 0: Dissolved (`020d1ce`)
Track 0 (Cell-Primary Reads Infrastructure) was created then dissolved after self-critique against core principles — violated Completeness Over Deferral and "ship complete solutions, not scaffolding." Benchmarking protocol folded into Track 1 Phase 0.

### Propagator Observatory — Visualization Phases 5d, 6a-6d Deferred
Marked as ⏸️ in the visualization design doc, added to DEFERRED.md.

### Track 1: Cell-Primary Constraint Tracking — COMPLETE
All phases (0-7b) implemented across multiple sessions. Final phases this session:
- Phase 7a: hasmethod wakeup cell (commit in prior session)
- Phase 7b: trait-cell-map dual-write fix (commit in prior session)

### Track 2: Reactive Constraint Resolution — Design
- D.1: Design analysis and formal grounding (commit `061455e`)
- D.2: Design iteration from review notes (commit `4edfa80`)
- D.3: Data Orientation principle added to DESIGN_PRINCIPLES.org (commit `5a9bf3f`)
- D.4: Phase 1 dependency mapping (commit `eb66ea0`)
- D.5: Performance baseline: 6888 tests, 188.8s
- D.6: External review integration — 12 critique points, 6 accepted/partial, 6 rejected with rationale (commit `91cf99d`)

### Track 2 Phase 1: Instance Registry Cell Reader (commit `d8045b1`)
- Added `read-impl-registry` cell reader with parameter fallback
- Added `current-macros-prop-cell-read` callback parameter
- Converted 4 elaboration-time read sites (trait-resolution, driver introspection, REPL)
- Key finding: two distinct read paths — registration-time (parameter) vs elaboration-time (cell)
- Parameter write stays as cross-command accumulator; full dual-write elimination deferred
- 6888 tests pass

### Track 2 Phase 2: Constraint Status Cells (commit `b4cdb1e`)
- Added `cid` (gensym) field to constraint struct for cell-map keying
- Single constraint-status-map cell (enet9) with `merge-constraint-status-map` (monotone: resolved wins)
- `read-constraint-status-map` reader + `write-constraint-status-cell!` writer
- Dual-write: `add-constraint!` writes `'pending`, unify.rkt writes `'resolved` on solved/failed
- Transient `retrying`/`postponed` transitions NOT written to cell (re-entrancy guards, not monotone)
- Closes the speculation gap: constraint status now captured by network snapshot
- 6888 tests pass

### Track 2 Phase 3: Stratified Quiescence Architecture (commit `4c0c927`)
- Split `solve-meta!` into `solve-meta-core!` (write only) + `run-stratified-resolution!` (flat loop)
- S0→S1+S2 loop with fuel counter (100 iterations), progress tracked via mutable box
- `current-in-stratified-resolution?` prevents nested loops — recursive solve-meta! only writes
- `retrying` guard now dead code (re-entrancy eliminated structurally)
- 6888 tests pass, 211.5s (slightly faster than pre-refactor 220.3s)

### Track 2 Phase 4: Action Descriptors (commit `e6aeafc`)
- 3 descriptor structs: `action-retry-constraint`, `action-resolve-trait`, `action-resolve-hasmethod`
- 5 pure S1 scan functions (collect ready actions as data)
- S2 interpreter with `match` dispatch + re-check guards (stale action detection)
- Key fix: actions in same batch can solve shared metas — re-check prevents "already solved" errors
- 6888 tests pass, 247.9s (overhead from collect+append+re-check; Phase 8 will compare)

### Track 2 Phase 5-6: Trait/HasMethod Resolution Propagators (commit `4a91b24`)
- Phase 5 (trait): Already covered by Phase 4's `collect-ready-traits-via-cells` — trait-cell-map from Track 1 provides cell-state readiness
- Phase 6 (hasmethod): Symmetric hasmethod-cell-map cell (enet10) with `merge-hasheq-union`
  - Cell-id tracking in `register-hasmethod-constraint!` (mirrors trait-cell-map pattern)
  - `read-hasmethod-cell-map` reader, `collect-ready-hasmethods-via-cells` scan function
  - Wired into S1 of stratified loop alongside targeted wakeup
  - New exports: `current-hasmethod-cell-map-cell-id`, `read-hasmethod-cell-map`, `collect-ready-hasmethods-via-cells`
- 6888 tests pass

### Track 2 Phase 7: Error Cell + Grouped Error Reporting (commit `510b77f`)
- Phase 7a: Error descriptor cell infrastructure
  - `merge-error-descriptor-map` (last-write-wins per meta-id) in `infra-cell.rkt`
  - `current-error-descriptor-cell-id` parameter, enet11 cell, reader/writer
- Phase 7b: Resolution callbacks write error descriptors
  - Extracted `build-trait-error` helper from `check-unresolved-trait-constraints`
  - Trait resolution callback writes `no-instance-error` on failure (ground args, no instance)
- Phase 7c: Post-fixpoint error sweep reads error cell first
  - Cell errors (pre-computed) take priority; fallback sweep for uncovered constraints
  - Stale error filter: skips cell errors for subsequently-solved metas
- HKT-7 ambiguity detection deferred to Phase 9 (per external review disposition)
- 6888 tests pass, 217.4s

### Track 2 Phase 8: Post-Pass Elimination (commit `8ff9cbe`)
- Removed all 9 `resolve-hasmethod-constraints!` call sites (5 in driver.rkt, 4 in expander.rkt)
- HasMethod resolution is now fully reactive via propagator callbacks
- Updated elaborator.rkt comment to reflect reactive resolution
- Performance: 200.1s (vs 247.9s post-Phase 4, 217.4s post-Phase 7, 188.8s D.5 baseline)
- ~20% improvement over Phase 4 from eliminating redundant batch hasmethod scanning
- 6888 tests pass

## In Progress

- Track 2 Phase 9: Confluence verification

## Considerations

- Prelude loading is lazy (first eval, not server start) — avoids startup delay
- Session state snapshots 11 parameters back after each eval — matches test-support.rkt exactly
- Form detection is text-based (col-0 convention) — no tree-sitter WASM needed
- Decorations clear on any document edit — prevents stale results

## Lessons Learned

- The `test-support.rkt` prelude caching pattern (lines 71-96) is the exact blueprint for REPL sessions — same parameters, same snapshot technique
- All registry parameters live in `macros.rkt`, transitively available via `driver.rkt`
- `process-string-ws` already formats results via `pp-expr` — no custom formatting needed
- VSCode `DecorationRenderOptions.after.contentText` for inline result display

## Metrics

- Test count: 6888 (358 files, 2 skipped)
- Session commits: `f4177b3`, `16cbc46`, `a151413`, `ff87fef`, `e5778cd`, `d276cce`, `c36b1ac`, `ee0e6a1`, `16cb7ce`, `ca7662a`, `eb294bd`, `9ddd4b1`, `8d9e919`, `020d1ce`, `f72ddc8`, `70d454b`, `a82b05f`, `061455e`, `4edfa80`, `5a9bf3f`, `eb66ea0`, `91cf99d`, `d8045b1`, `b4cdb1e`, `4c0c927`, `e6aeafc`, `4a91b24`, `510b77f`, `8ff9cbe`

---

## Notes

Tier 4 went fast because the Racket infrastructure was already well-designed for this use case — the parameterize/snapshot pattern from test-support.rkt maps directly to REPL session management. The main design decisions were: (1) text-based form detection instead of tree-sitter WASM, (2) lazy prelude loading, (3) separate REPL output channel.
