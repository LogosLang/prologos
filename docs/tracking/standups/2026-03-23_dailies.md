# Dailies — 2026-03-23

## Carried Forward from Previous Interval

### PM Track 8 (A/B/C/D): COMPLETE
- All parts delivered. Pure α/γ bridges fire in S0. Quiescence writes reduced.
- 3 remaining symptomatic callbacks tied to solve-meta! cold path (PM Track 8E scope).
- PIR: `docs/tracking/2026-03-22_TRACK8_PIR.md` (`fe2673e`) + principles audit.

### SRE Track 0: COMPLETE
- `sre-core.rkt`: 6 domain-parameterized functions. 23 type-domain ctor-descs. Term-value validated with zero changes.
- PIR: `docs/tracking/2026-03-22_SRE_TRACK0_PIR.md` (`089d53b`)
- **Key insight**: `sre-domain ≡ NTT :lattice :structural` — isomorphic data models.

### NTT Research Program: ~90% clarity
- 4 rounds of syntax design. 6-system case study program complete.
- 7 NTT forms finalized: `trait Lattice`, `propagator`, `interface`, `network`, `bridge`, `stratification`, `exchange`
- SRE expanded to Structural Relation Engine (equality, duality, subtyping, coercion, isomorphism)
- Remaining unknowns: TracedCell provenance details, speculation dynamics, trigger formalization
- Toplevel Forms Reference: 25+ forms, 50+ keywords, 7 domains

### Unified Infrastructure Roadmap
- 5 PM tracks: 8D ✅ → 8E (17 registries) → 8F (meta-info as cells) → 9 (reduction) → 10 (box elimination)
- SRE Series: Track 0 ✅ → Track 1 (subtyping) → Track 2 (elaborator on SRE) → Track 3 (traits) → Track 4 (patterns)
- Track 10 = convergence: no box, no dual-writes, no zonk, module loading on-network

### CIU/BSP-LE: UNBLOCKED
- CIU Track 3 (trait-dispatched access): unblocked by Track 8 B3-B4 (HKT) + Track 8D (pure bridges)
- BSP-LE Track 1 (UnionFind): independent, can proceed anytime

### Known Bugs / Gaps (carried)
- 5 punify parity bugs with toggle ON: `head`, `map`, `Pair`, `match`, `Vec`
- 5 L3 parser bugs from acceptance file (preparse/parser level)
- `bench-zonk.rkt` stale
- PUnify toggle flip blocked by Option module loading hang
- B2e: macros dual-writes (module-load-time fallback needed)

### Pending Distillations
- 5 lessons from SRE Track 0 PIR → DEVELOPMENT_LESSONS.org, DESIGN_METHODOLOGY.org
- 3 items from Track 8 PIR → DESIGN_METHODOLOGY.org, pipeline.md
- "Minimal syntax" → DESIGN_PRINCIPLES.org

### Watching (medium-term patterns, carried + updated)
- **Design critique as collaborative design**: 8+ instances. **Ready for codification.**
- **Principles alignment as checklist vs application**: Track 8 D.1-D.3 + SRE Track 0 D.2 (which was done right). 2 instances (1 failure, 1 success). Pattern clarifying: external critique + principles challenge = success.
- **Design:implementation ratio predicts smoothness**: 22 PIR data points. SRE Track 0 (2:1 ratio, zero bugs) is the strongest confirmation. **Ready for codification** in DESIGN_METHODOLOGY.org.
- **Second domain as abstraction test**: SRE Track 0 + PUnify parity. 2 instances. Emerging pattern.
- **NTT-Racket isomorphism**: `sre-domain ≡ :lattice :structural`. NEW (1 instance). If SRE Track 1 confirms that `#:relation` maps to NTT structural relations, this becomes a confirmed pattern. Watch.
- **Latent abstraction extraction**: PUnify → SRE was mechanical because PUnify was data-oriented. Track 8 bridges → pure α/γ required architectural cleanup first (Track 8D). 2 instances. Pattern: data-oriented code extracts cleanly, imperative doesn't.
- **Suite time trajectory**: ...→ 228.2s → 244.3s → 239.3s → 236.7s. Stabilizing around 237-240s.

---

## Lessons Distillation Check

### Scanned: SRE Track 0 PIR + 2026-03-22 dailies + Track 8 PIR

**Candidates for principles documents**:

1. **"Design:implementation ratio predicts smoothness" — 22 PIR data points, ready for codification.** SRE Track 0 (2:1, zero bugs), Track 8 Part A (0.5:1, ghost-meta bugs), PUnify (1:1, 5 parity bugs). The pattern is consistent across all tracks: more design time → fewer implementation surprises. **Action**: Add to DESIGN_METHODOLOGY.org § "Design Investment."

2. **"Second domain validates abstraction boundary" — 2 instances, emerging.** SRE Track 0's term-value domain is the clearest instance. PUnify's punify-toggle parity testing is another. **Action**: Note in DESIGN_METHODOLOGY.org as emerging practice, not yet codified as principle.

3. **"External critique + principles challenge = clean API" — 2 instances.** SRE Track 0 D.2 caught 3 API issues before coding. Track 8 D.4 caught the enet-box rationalization. **Action**: Part of the critique rounds codification (already pending).

4. **"NTT speculative syntax as design companion" — 1 instance, codified in workflow.** Already a workflow rule (`bcdf1c4`). Monitor for effectiveness across future tracks.

5. **"Data-oriented code extracts cleanly, imperative doesn't" — 2 instances.** PUnify→SRE (data-oriented, mechanical extraction) vs Track 8 bridges→pure α/γ (imperative, required 8D). **Action**: Add to DEVELOPMENT_LESSONS.org as "Extractability correlates with data orientation."

**Actions**: Items 1, 5 are ready for immediate codification. Items 2, 3 need one more data point. Item 4 is already captured.

---

## Today's Work

### Completed

- **Working day interval opened** (`46e61be`)

- **Principles-First Design Gate expanded** (`c7cda29` area)
  - 10 principles in two groups: Group A (infrastructure) + Group B (design quality)
  - Completeness, Composition, Progressive Disclosure, Ergonomics, Most General Interface added
  - Gate applied at ALL decision points: design drafting, implementation mid-flight, critique rounds

- **Track 8D: Pure bridge fire functions** (`eb9857a`)
  - Bridges read cells directly via `net-cell-read`; no zonk, no box, no `with-enet-reads`

- **NTT research program** (multiple commits)
  - Categorical Foundations doc (`68d7302`): polynomial functors, fibrations, Kan extensions, 5 open questions
  - NTT Syntax Design through 4 rounds (`c7816a1`, `c60ce81`, `2b27ea8`, `12dd587`): ~90% clarity
  - Architecture Survey (`e2940a8`): 7 systems modeled, 5 gaps found
  - Type Checker deep case study (`29bda15`): full NTT spec, impedance mismatch analysis
  - NF-Narrowing case study (`4e7b584`): most network-native system, DT-driven topology
  - Sessions/QTT/WF-LE/NAF-LE case studies (`7d9c577`): completes 6-system program
  - Level 0 split: value lattices (`impl Lattice`) vs structural lattices (`:lattice :structural`)

- **SRE Master Tracking Document** created (`a059a3d` area)
  - 5 tracks scoped: T0 Form Registry ✅ → T1 Relations → T2 Elaborator-on-SRE → T3 Trait Resolution → T4 Pattern Compilation
  - Coercion phasing note: runtime concern, deferred to Track 2

- **SRE Track 1 Design: 4 critique rounds** (`df29231`, `23f717c`, `81f997f`, `b958e3d`)
  - D.1: Combined Stage 2/3, 6 phases, sre-relation struct, variance on ctor-desc
  - D.2 (Completeness revision): User-defined structural subtyping via polarity inference, dependent session duality, transitivity falls out of propagation. 4 deferrals were Completeness violations — incorporated.
  - D.3 (self-critique): Removed `component-sub-relations` (derive from component lattice types). Added vision/goal, expectations, completion criteria, meta-interaction boundary, query pattern code sketch.
  - D.4 (external critique): Subtype semantics explicit (checker not propagator). Frequency counter for query pattern. Duality derivation assumption documented. Polarity edge cases (recursive fixpoint, mutual recursion, GADT out-of-scope). Decomp cache interaction test. Binder generality test.

- **SRE Track 1 Phase 1** (`6c24277`, `45a816d`)
  - `sre-relation` struct: first-class relation with `sub-relation-fn`
  - 5 built-in relations: equality, subtype, subtype-reverse, duality, phantom
  - `component-variances` on ctor-desc (9th field): 12 built-in types annotated
  - `binder-open-fn` on ctor-desc (10th field): scaffolded for Phase 3
  - `dual-pairs` on sre-domain (8th field): scaffolded for Phase 3
  - Polarity inference utilities: `variance-join`, `variance-flip`
  - 7358 tests, 244.1s, all pass

- **SRE Track 1 Phase 2** (`200bec6`)
  - Relation-parameterized `sre-make-structural-relate-propagator` with `#:relation` kwarg
  - Subtype propagator: structural checker with variance-driven decomposition
  - `sre-maybe-decompose` + `sre-decompose-generic` thread relation through
  - Decomp cache key includes relation name (equality ≠ subtype decompositions)
  - 22 new tests in `test-sre-subtype.rkt`
  - 7384 tests, 259.7s, all pass
  - **Key discovery**: type-lattice merge is equality-ordered (flat: Nat ≠ Int → top). Subtype propagator can't use `merge(a,b) = b` — needed domain-specific subtype check.

- **SRE Track 1 Phase 2b: Principles fixes** (`260903f`)
  - **Propagator-First violation fixed**: `flat-subtype?` (off-network predicate) → `subtype-lattice-merge` (proper lattice merge encoding subtype ordering). `subtype-merge(Nat, Int) = Int` because Nat <: Int. Subtyping now fully on-network.
  - **Circular dependency broken**: `subtype?` + `type-key` + `subtype-lattice-merge` extracted to `subtype-predicate.rkt`. Both `unify.rkt` and `typing-core.rkt` import without cycles.
  - **Duality placeholder fixed**: errors instead of silently falling back to equality (equality is stricter → wrong behavior).
  - **Lesson**: The user caught the `flat-subtype?` violation by asking "is this moving the propagator off-network?" — the Principles-First Design Gate in action. Scanning for principles violations during implementation (not just during design) caught this before it compounded.
  - **Design insight**: Two lattice orderings on the same carrier — equality (flat) and subtype (partial order) — require separate merge functions. The relation selects which merge to use. This is the "correct lattice for the correct relation" principle.
  - 7384 tests, 254.4s, all pass

- **SRE Track 1 Phase 3: Duality-aware structural-relate** (`20a3e84`, `48634d8`)
  - 5 session constructors registered in ctor-registry (Send, Recv, AsyncSend, AsyncRecv, Mu)
  - `session-lattice-spec` as `'session` symbol sentinel (circular dep: session-lattice → type-lattice → reduction → ctor-registry)
  - `sre-make-duality-propagator`: constructor pairing via dual-pairs, bidirectional with involution
  - `sre-duality-decompose-dual-pair`: asymmetric decomposition — each side uses its OWN descriptor
  - `sre-domain` +`top-value` field (10th): for relation violations where lattice-merge can't produce contradiction
  - **Key lesson**: Duality decomposition is fundamentally asymmetric (Send extracts ≠ Recv extracts). Can't reuse `sre-decompose-generic` (assumes same tag both sides). Needed dedicated `sre-duality-decompose-dual-pair`.
  - **Key lesson**: Don't pre-write dual values with copied sub-components — they need to be dualized by sub-cell propagators. Skeleton approach failed (cross-domain bot values). Solution: decompose directly, let sub-cell propagators + reconstructors build correct values.
  - 9 tests in test-sre-duality.rkt. ctor-registry test updated (21 → 26 descs).

- **SRE Track 1 Phase 4: subtype? delegation** (`9045059`, `7c8ef21`)
  - `subtype?` now: equal → flat fast path → SRE structural check
  - Query pattern: mini-network, two cells, subtype-relate propagator, quiesce, check contradiction
  - `current-subtype-check-count` frequency counter for monitoring
  - Behavioral change: `subtype?` is now reflexive (standard preorder semantics). Test updated.
  - `type-sre-domain-for-subtype`: standalone domain for structural queries, no meta-recognizer

- **SRE Track 1 Phase 5: Session duality via SRE** (`58dd714`)
  - Old: two unidirectional propagators calling `(dual v)` — off-network Racket function
  - New: single SRE duality propagator via `sre-make-structural-relate-propagator #:relation sre-duality`
  - `session-sre-domain`: session domain spec with dual-pairs, sess-meta? recognizer
  - **Drop-in replacement**: all 21 session test files pass with zero test changes

- **SRE Track 1 COMPLETE** (`155d0ba`)
  - 7392 tests, 243.9s, all pass. +34 tests, +3.0% wall time.
  - PIR: `docs/tracking/2026-03-23_SRE_TRACK1_PIR.md`

- **SRE Track 1B: Post-Implementation Fixes** (in progress)
  - Phase 1 (benchmarks, `312ca3a`): Zero compound checks in suite. Success 8-54μs, failure 333μs. Failure cost is cold-start (mini-network creation), not computation (4 firings before exit).
  - Phase 2a (merge-registry, `d276922`): `sre-domain` 10→9 fields. `case` dispatch for zero overhead. Generalizes to any number of lattice orderings.
  - Phase 2b: Early-exit quiescence already implemented — confirmed only 4 firings before contradiction exit.
  - Phase 2c (compound guard, `3e00244`): Flat NOT path 2.0μs→0.47μs (4.3× faster). `compound-type?` struct predicate check eliminates `sre-constructor-tag` overhead on atoms.
  - Phase 2d (direct recursive, `a1b347d`): **110× failure speedup** (333μs→3μs). Success 3-7× faster. Zero-allocation recursive walk using SRE data (ctor-desc, variance, merge-registry) without propagator machinery. Ground-type subtyping is a pure function — propagation adds no information.
  - **Key design insight**: Propagation is for incremental information flow. Ground-type checking is a total function. Using a propagator network for a total function is correct but unnecessarily expensive. The SRE's data structures (ctor-desc, variance, merge-registry) carry the structural knowledge; the recursive walk reads this data without creating cells or propagators.
  - **Key lesson**: Benchmarking before changing (Phase 1 reorder) revealed that early-exit quiescence was already implemented, and the real bottleneck was cold-start allocation, not computation. This shifted the optimization target from "smarter quiescence" to "eliminate the network entirely for ground types."

- **SRE Track 1B Phase 3: Relation-parameterized decomposition guard** (`faeb915`)
  - `requires-binder-opening?` field on `sre-relation` (3rd field)
  - Replaces `(eq? relation-name 'equality)` name-check — correct-by-construction
  - equality=#t (needs fresh metas), subtype/duality/phantom=#f (ground types)
  - Zero remaining name-checks on 'equality in sre-core.rkt

- **SRE Track 1B Phase 4: Dependent duality** (`87ab254`)
  - DSend/DRecv registered with binder-depth=1, binder-open-fn (substS-based)
  - Ground duality with bvar(0) preserved — binder opening not needed for duality on ground types
  - **Key insight**: for ground-type duality, both sides share the same binder structure. The bound variable (bvar 0) is preserved through extraction/reconstruction. binder-open-fn is for Track 2 (equality with metas).

- **SRE Track 1B Phase 5: Edge case tests + mu fix** (`19fa95d`)
  - 7 new tests: 5-level nested, mu recursive, mixed constructors, both-concrete duals/non-duals, payload mismatch
  - **Bug fix**: `sre-decompose-generic` used unified value for b-side components when b-side was bot. For duality, this copies un-dualized sub-components (mu body gets `Send` instead of `Recv`). Fix: non-equality relations use BOT sub-components when b-side doesn't match recognizer. Sub-cell propagators then push correct values.
  - **Pattern**: This is the THIRD instance of "don't pre-populate sub-cells with values from the wrong side" (first: dual-pair pre-write, second: skeleton approach, third: decompose-generic unified fallback). The pattern is now confirmed: for non-equality relations, always let propagators build values from sub-cells — never copy from the source side.

---

- **SRE Track 1+1B lessons distilled** (`eca77b4`, `4df4ce5`)
  - DESIGN_METHODOLOGY.org: "Benchmark as design input" — draft → benchmark → iterate → implement
  - DEVELOPMENT_LESSONS.org: "Non-equality relations break equality assumptions" (3 classes)
  - SRE Master updated: gaps tracked on Tracks 2-4

- **SRE Track 1+1B PIR** (`fe2673e`, `4df4ce5`)
  - 16-question review. 3 wrong assumptions, 7 bugs (3 from equality assumptions).
  - Key lesson: structural relations are a FAMILY of operations with different algebraic properties.
  - 4-axis analysis for new relations: symmetry, merge semantics, constructor mapping, binder requirements.

- **GADT research discussion** (captured in SRE Master `999d71d`)
  - GADTs map naturally to TMS-based speculation (each branch is a speculation scope)
  - NF-Narrowing already solves the core exhaustiveness problem (term + type level via unified narrowing)
  - TMS + QTT interaction already solved by session type infrastructure
  - Connection to typed graph rewriting: GADTs, e-graphs, interaction nets, GoI are instances of same pattern
  - Tropical semirings for optimization: dual enrichment (correctness lattice + cost lattice)

- **SRE Track 2 Design: 4 iterations** (`8e013dd`, `260e8a0`, `20ca084`, `333a3ac`)
  - D.1: Combined Stage 2/3. Key audit finding: elaborator is store-agnostic; migration target is unify.rkt classifier (37 cases), not typing-core.rkt (158 cases).
  - D.2: Pre-0 micro-benchmark. Critical finding: `ctor-tag-for-value` linear scan is 4-200× slower than struct predicates. O(1) dispatch via `prop:ctor-desc-tag` as Phase 0.
  - D.3: External critique (10 points). 6 accepted (e2e benchmark, multi-domain tags, polarity decoupled to 2B, flex-app ordering, rollback toggle, jump table rejected). 2 partially accepted (.zo verification, nat-val reclassified). 2 pushed back (PUnify toggle, equal? optimization).
  - D.4: E2E benchmark. Extraction closures settled as non-issue (3ns overhead). 3 cases already FASTER from reordering alone (identical 0.57×, meta 0.28×, flex-app 0.25×). With O(1) dispatch, ALL cases predicted faster.
  - **Architectural insight**: Static dispatch (routing) vs dynamic reasoning (on-network). Dispatch is like NF-Narrowing's definitional tree — pre-computed, doesn't change during elaboration. Reasoning (decomposition, propagation) is on-network. Principled boundary.

- **SRE Track 2 Phase 0: O(1) dispatch** (`f97c9a9`, `01ca400`)
  - `prop:ctor-desc-tag` struct-type property defined in syntax.rkt
  - 19 AST structs annotated: 12 type-domain, 7 session-domain
  - `ctor-tag-for-value` updated: property-first → fallback for data domain
  - **Measured results**: Pi 357→34ns (10.5×), suc 329→35ns (9.4×), atom 825→48ns (17×). All structural types uniform 34-35ns.
  - **Bug found + fixed**: `struct?` guard too aggressive — broke extra-domain lookups (test-pair, narrowing vars). Removed guard; non-property structs correctly fall through to data + extra domain scan.
  - **Lesson**: The `struct?` optimization assumed all non-property structs are type-domain atoms. But extra-domain test structs also need the fallback scan. Premature optimization on the fallback path.
  - 7401 tests, 244.1s, all pass

---

## Considerations

- **SRE Track 1B COMPLETE** (`5c5fc89`). 7401 tests, 244.8s, all pass. +9 tests, +0.4% from Track 1 baseline.
- **NTT-Racket isomorphism CONFIRMED**: Promote from "watching" to "confirmed pattern."
- **Recurring pattern codification candidate**: "Don't pre-populate sub-cells from wrong side for non-equality relations" — 3 instances in Track 1/1B. Ready for DEVELOPMENT_LESSONS.org.
- **SRE Track 2 COMPLETE** (`70cb5f7`). 7401 tests, ~238s (2.5% faster). PIR written (19 sections).
  - All 10 structural cases via SRE dispatch. 85 lines across 4 files.
  - O(1) prop:ctor-desc-tag: Pi 357→34ns (10.5×), atom 825→72ns (11.5×)
  - 1 bug (struct? guard), 1.7:1 design:impl ratio
  - **Key surprise**: elaborator is store-agnostic; migration target was unify.rkt classifier (37 cases), not typing-core (158 cases)
  - **Key learning**: benchmark-before-building confirmed (3rd instance); data-oriented code migrates cleanly (3rd instance)
- **Polarity inference decoupled to Track 2B**: new feature, not migration. Separate design cycle.

---

## PM 8F: Metas as Cells — Implementation Progress

### Completed Phases

- **Pre-0**: Micro-benchmark + adversarial testing (`683eb43`)
  - Key finding: id-map is the real bottleneck (82ns of 150ns total meta-solution cost)
  - meta-solution ALREADY reads from cells — D.1 Phase 0 was a no-op
  - bvar risk CONFIRMED by adversarial probe (but frequency = 0 across all tests)
  - Design fundamentally revised: new Phase 0 = bvar detection, new Phase 1 = embed cell-id

- **Phase 0**: bvar closure detection (`14705f6`)
  - bvar-free? assertion in solve-meta-core! — detection only, no behavior change
  - 0 bvar solutions across 7401 tests. PUnify always opens binders before solving.
  - close-expr (Phase 3) confirmed as PREVENTIVE, not corrective

- **Phase 1**: Embed cell-id in expr-meta (`8bbdee4`)
  - expr-meta gains cell-id field + custom gen:equal+hash (id-only equality)
  - fresh-meta sets cell-id at creation time
  - meta-solution/cell-id: fast path skips 82ns id-map lookup
  - 36 files changed (11 source, 8 test/bench). Custom equal? is 2.3× faster for hash ops.
  - **Lesson**: Agent bulk edits can confuse field accessors (expr-meta-id) with constructors (expr-meta). Verify accessor calls aren't mistakenly wrapped.

- **Phase 2**: Zonk call site audit (`2edada1`)
  - D.1 estimated ~225 zonk sites. **Actual: 31 external.** typing-core.rkt makes ZERO zonk calls.
  - 0 defensive calls found. All 13 non-boundary calls are necessary.
  - Phase 2 has zero elimination scope. Design revised with new elimination path.

- **Phase 3**: zonk/zonk-at-depth cell-id fast paths (`22b4a4a`, `6e773ef`)
  - Both zonk and zonk-at-depth now use meta-solution/cell-id
  - **5.4% suite improvement** (244.3s → 231.2s) from cell-id paths
  - **Critical discovery**: replacing `zonk-at-depth 0` with `zonk` in unify.rkt causes timeouts — NOT from value mismatch (zero mismatches verified across 7401 tests) but from recursion pattern difference (perf counter + different self-recursion target). Fix: update zonk-at-depth's OWN meta-solution call instead of changing unify.rkt's call pattern.
  - **Lesson**: value-equivalence ≠ identity-equivalence. The unify hot loop relies on specific recursion patterns. Changing which function recurses (even to a value-equivalent one) can cause combinatorial blowup.

### Process Improvements (from Phase 3 debugging)

- **Workflow rules added** to MEMORY.md:
  - Separate compile from test, always
  - 3-timeout = revert immediately (systemic regression)
  - "Pre-existing issue" is a process smell — file it, estimate cost, escalate if >30min/session
  - Capture full error output on first run

- **Test runner improved** (`4211a78`): bail-out message now uses box-drawn banner with ⛔ SYSTEMIC REGRESSION DETECTED, explicit ACTION: REVERT, common causes listed.

- **REPL/process-string hang FIXED** (`29a1fad`):
  - Root cause: process-string without with-fresh-meta-env had current-prop-net-box = #f
  - Module loading hangs during elaboration when network absent
  - Fix: auto-create minimal network (make-elaboration-network + reset-meta-store!) when missing
  - Does NOT reset prelude-env — preserves existing context
  - Was "pre-existing" for multiple sessions, cost 2+ hours in workarounds. Fixed by applying the Completeness principle to our own process.
  - **Lesson**: CPU saturation from parallel suite can cause false timeout signals in concurrent REPL tests. Don't run secondary tests while suite is running.

### Design Findings That Changed the Plan

1. **D.1 estimate of 225 zonk sites was 7× too high** (actual: 31). typing-core doesn't call zonk.
2. **Phase 0 as designed was a no-op** — meta-solution already reads from cells.
3. **Phase 2 has zero scope** — no defensive zonk calls exist.
4. **The real performance win is cell-id** (Phase 1+3), not zonk elimination (Phase 2).
5. **5.4% suite improvement from cell-id paths alone** — validates the design direction.

### Completed Phases (continued)

- **Phase 4**: Eliminate redundant zonk in resolution (`1ab5f99`)
  - 8 `(normalize-for-resolution (zonk e))` → `(normalize-for-resolution e)` — outer zonk was redundant
  - normalize-for-resolution already chases metas at positions it needs for key extraction
  - normalize-for-resolution updated with cell-id fast path
  - **Finding**: Phase 4a (freeze replacing zonk-final) has zero value — zonk-final already benefits from Phase 3's cell-id optimization. The ~200 line freeze.rkt replacement is unnecessary while zonk.rkt still has necessary callers.

- **Phase 5**: Defaults at solve-time — **DEFERRED**
  - default-metas is a cheap single-pass walk, runs once per boundary. Architectural cleanup, not performance.
  - Moving to solve-time requires changing level-meta/mult-meta solve paths + resolution timing. Deferred to cleanup track.

- **Phase 6**: ground-expr? cell-id update (`dbfce20`)
  - trait-resolution.rkt ground-expr? updated with meta-solution/cell-id
  - Simplified from 33 match cases to 11
  - global-constraints.rkt ground-expr? NOT unified — serves different purpose (logic engine "no logic variables" vs type checking "no unsolved metas"). These are distinct concepts, not duplicate code.
  - **Design finding**: the "two incompatible definitions" from D.1 are actually two DIFFERENT concepts with the same name.

- **Phase 7**: CHAMP fallback removal — **DEFERRED to PM Track 10**
  - Needs cells available in ALL contexts (module loading, test isolation)
  - Track 10 (module loading on-network) is the prerequisite

- **Phase 8**: A/B benchmarks + final verification (`d204a6e`)
  - meta-solution/cell-id: 103ns (1.5× faster than id-map 150ns)
  - Suite: 235.7s (3.5% improvement from 244.3s baseline)
  - bvar-free? assertion gated behind debug flag (was running on every solve-meta! — struct->vector overhead)
  - solve-meta! shows 3.5× vs Pre-0 baseline — benchmark context drift from Track 8C bridges, NOT PM 8F regression
  - **Lesson**: always gate debug assertions behind flags. The bvar-free? struct->vector walk on every solve-meta! was costing ~1500ns per call.
  - **Lesson**: benchmark baselines drift as infrastructure changes. The Pre-0 baseline was measured before Track 8C bridges were active. Cross-track A/B comparisons need identical infrastructure state.

### PM 8F Summary

**Delivered**: cell-id in expr-meta (1.5× faster meta reads), cell-id in zonk paths (5.4% → 3.5% suite improvement), 8 redundant resolution zonks eliminated, ground-expr? cell-id, bvar detection, REPL hang fix, test runner improvement, 4 workflow rules.

**Deferred**: defaults at solve-time (architectural), CHAMP fallback removal (Track 10 prerequisite).

**Key meta-lesson**: D.1 estimates were systematically wrong — 225 zonk sites (actual: 31), Phase 0 was a no-op, Phase 2 had zero scope, Phase 4a had zero value. Pre-implementation benchmarks corrected the plan before wasted effort. The "benchmark as design input" methodology (4th confirmed instance) prevented building the wrong thing.

### Instrumentation Cleanup + Methodology Codification

- **Dead instrumentation found** (`3339fec`): `subtype-check-count` from SRE Track 1B was allocating a cons pair on EVERY subtype? call. Nobody reads the counter. Gated behind `#f` default — 1.2% suite improvement (235.7→232.9s).
  - **Lesson**: Always gate debug instrumentation behind flags. Review active instrumentation periodically.
  - **Cumulative PM 8F improvement**: 244.3s → 232.9s = **4.7%**

- **DESIGN_METHODOLOGY.org updated** (`cf3a856`):
  - Stage 2: concrete code measurements (grep-backed), not vision estimates
  - Stage 3: Pre-0 benchmark as explicit design phase (after D.1, feeds D.2); codebase reality check in self-critique
  - Stage 4: post-implementation A/B benchmark + instrumentation cleanup as explicit final phase; write PIR per methodology (explicit rule)

- **PM 8F PIR** (`ba55f52`): 19 sections, 5 wrong assumptions, longitudinal across 6 PIRs. 7 patterns tracked, 2 codified, 2 watching.

- **Master Roadmap updated** (`179662e`): PM 8F complete with design doc + PIR links.
