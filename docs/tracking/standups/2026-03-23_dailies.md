# Dailies — 2026-03-23

## Carried Forward from Previous Interval

### PM Track 8 (A/B/C/D): COMPLETE
- All parts delivered. Pure α/γ bridges fire in S0. Quiescence writes reduced.
- 3 remaining symptomatic callbacks tied to solve-meta! cold path (PM Track 8E scope).
- PIR: `docs/tracking/2026-03-22_TRACK8_PIR.md` (`fe2673e`) + principles audit.

### SRE Track 0: COMPLETE
- `sre-core.rkt`: 6 domain-parameterized functions. 23 type-domain ctor-descs. Term-value validated with zero changes.
- PIR: `docs/tracking/2026-03-22_SRE_TRACK0_PIR.md` (`089d53b`)
- **Key insight**: `sre-domain ≡ NTT :lattice :structural` — isomorphic data models.

### NTT Research Program: ~90% clarity
- 4 rounds of syntax design. 6-system case study program complete.
- 7 NTT forms finalized: `trait Lattice`, `propagator`, `interface`, `network`, `bridge`, `stratification`, `exchange`
- SRE expanded to Structural Relation Engine (equality, duality, subtyping, coercion, isomorphism)
- Remaining unknowns: TracedCell provenance details, speculation dynamics, trigger formalization
- Toplevel Forms Reference: 25+ forms, 50+ keywords, 7 domains

### Unified Infrastructure Roadmap
- 5 PM tracks: 8D ✅ → 8E (17 registries) → 8F (meta-info as cells) → 9 (reduction) → 10 (box elimination)
- SRE Series: Track 0 ✅ → Track 1 (subtyping) → Track 2 (elaborator on SRE) → Track 3 (traits) → Track 4 (patterns)
- Track 10 = convergence: no box, no dual-writes, no zonk, module loading on-network

### CIU/BSP-LE: UNBLOCKED
- CIU Track 3 (trait-dispatched access): unblocked by Track 8 B3-B4 (HKT) + Track 8D (pure bridges)
- BSP-LE Track 1 (UnionFind): independent, can proceed anytime

### Known Bugs / Gaps (carried)
- 5 punify parity bugs with toggle ON: `head`, `map`, `Pair`, `match`, `Vec`
- 5 L3 parser bugs from acceptance file (preparse/parser level)
- `bench-zonk.rkt` stale
- PUnify toggle flip blocked by Option module loading hang
- B2e: macros dual-writes (module-load-time fallback needed)

### Pending Distillations
- 5 lessons from SRE Track 0 PIR → DEVELOPMENT_LESSONS.org, DESIGN_METHODOLOGY.org
- 3 items from Track 8 PIR → DESIGN_METHODOLOGY.org, pipeline.md
- "Minimal syntax" → DESIGN_PRINCIPLES.org

### Watching (medium-term patterns, carried + updated)
- **Design critique as collaborative design**: 8+ instances. **Ready for codification.**
- **Principles alignment as checklist vs application**: Track 8 D.1-D.3 + SRE Track 0 D.2 (which was done right). 2 instances (1 failure, 1 success). Pattern clarifying: external critique + principles challenge = success.
- **Design:implementation ratio predicts smoothness**: 22 PIR data points. SRE Track 0 (2:1 ratio, zero bugs) is the strongest confirmation. **Ready for codification** in DESIGN_METHODOLOGY.org.
- **Second domain as abstraction test**: SRE Track 0 + PUnify parity. 2 instances. Emerging pattern.
- **NTT-Racket isomorphism**: `sre-domain ≡ :lattice :structural`. NEW (1 instance). If SRE Track 1 confirms that `#:relation` maps to NTT structural relations, this becomes a confirmed pattern. Watch.
- **Latent abstraction extraction**: PUnify → SRE was mechanical because PUnify was data-oriented. Track 8 bridges → pure α/γ required architectural cleanup first (Track 8D). 2 instances. Pattern: data-oriented code extracts cleanly, imperative doesn't.
- **Suite time trajectory**: ...→ 228.2s → 244.3s → 239.3s → 236.7s. Stabilizing around 237-240s.

---

## Lessons Distillation Check

### Scanned: SRE Track 0 PIR + 2026-03-22 dailies + Track 8 PIR

**Candidates for principles documents**:

1. **"Design:implementation ratio predicts smoothness" — 22 PIR data points, ready for codification.** SRE Track 0 (2:1, zero bugs), Track 8 Part A (0.5:1, ghost-meta bugs), PUnify (1:1, 5 parity bugs). The pattern is consistent across all tracks: more design time → fewer implementation surprises. **Action**: Add to DESIGN_METHODOLOGY.org § "Design Investment."

2. **"Second domain validates abstraction boundary" — 2 instances, emerging.** SRE Track 0's term-value domain is the clearest instance. PUnify's punify-toggle parity testing is another. **Action**: Note in DESIGN_METHODOLOGY.org as emerging practice, not yet codified as principle.

3. **"External critique + principles challenge = clean API" — 2 instances.** SRE Track 0 D.2 caught 3 API issues before coding. Track 8 D.4 caught the enet-box rationalization. **Action**: Part of the critique rounds codification (already pending).

4. **"NTT speculative syntax as design companion" — 1 instance, codified in workflow.** Already a workflow rule (`bcdf1c4`). Monitor for effectiveness across future tracks.

5. **"Data-oriented code extracts cleanly, imperative doesn't" — 2 instances.** PUnify→SRE (data-oriented, mechanical extraction) vs Track 8 bridges→pure α/γ (imperative, required 8D). **Action**: Add to DEVELOPMENT_LESSONS.org as "Extractability correlates with data orientation."

**Actions**: Items 1, 5 are ready for immediate codification. Items 2, 3 need one more data point. Item 4 is already captured.

---

## Today's Work

### Completed

- **Working day interval opened** (`46e61be`)

- **Principles-First Design Gate expanded** (`c7cda29` area)
  - 10 principles in two groups: Group A (infrastructure) + Group B (design quality)
  - Completeness, Composition, Progressive Disclosure, Ergonomics, Most General Interface added
  - Gate applied at ALL decision points: design drafting, implementation mid-flight, critique rounds

- **Track 8D: Pure bridge fire functions** (`eb9857a`)
  - Bridges read cells directly via `net-cell-read`; no zonk, no box, no `with-enet-reads`

- **NTT research program** (multiple commits)
  - Categorical Foundations doc (`68d7302`): polynomial functors, fibrations, Kan extensions, 5 open questions
  - NTT Syntax Design through 4 rounds (`c7816a1`, `c60ce81`, `2b27ea8`, `12dd587`): ~90% clarity
  - Architecture Survey (`e2940a8`): 7 systems modeled, 5 gaps found
  - Type Checker deep case study (`29bda15`): full NTT spec, impedance mismatch analysis
  - NF-Narrowing case study (`4e7b584`): most network-native system, DT-driven topology
  - Sessions/QTT/WF-LE/NAF-LE case studies (`7d9c577`): completes 6-system program
  - Level 0 split: value lattices (`impl Lattice`) vs structural lattices (`:lattice :structural`)

- **SRE Master Tracking Document** created (`a059a3d` area)
  - 5 tracks scoped: T0 Form Registry ✅ → T1 Relations → T2 Elaborator-on-SRE → T3 Trait Resolution → T4 Pattern Compilation
  - Coercion phasing note: runtime concern, deferred to Track 2

- **SRE Track 1 Design: 4 critique rounds** (`df29231`, `23f717c`, `81f997f`, `b958e3d`)
  - D.1: Combined Stage 2/3, 6 phases, sre-relation struct, variance on ctor-desc
  - D.2 (Completeness revision): User-defined structural subtyping via polarity inference, dependent session duality, transitivity falls out of propagation. 4 deferrals were Completeness violations — incorporated.
  - D.3 (self-critique): Removed `component-sub-relations` (derive from component lattice types). Added vision/goal, expectations, completion criteria, meta-interaction boundary, query pattern code sketch.
  - D.4 (external critique): Subtype semantics explicit (checker not propagator). Frequency counter for query pattern. Duality derivation assumption documented. Polarity edge cases (recursive fixpoint, mutual recursion, GADT out-of-scope). Decomp cache interaction test. Binder generality test.

- **SRE Track 1 Phase 1** (`6c24277`, `45a816d`)
  - `sre-relation` struct: first-class relation with `sub-relation-fn`
  - 5 built-in relations: equality, subtype, subtype-reverse, duality, phantom
  - `component-variances` on ctor-desc (9th field): 12 built-in types annotated
  - `binder-open-fn` on ctor-desc (10th field): scaffolded for Phase 3
  - `dual-pairs` on sre-domain (8th field): scaffolded for Phase 3
  - Polarity inference utilities: `variance-join`, `variance-flip`
  - 7358 tests, 244.1s, all pass

- **SRE Track 1 Phase 2** (`200bec6`)
  - Relation-parameterized `sre-make-structural-relate-propagator` with `#:relation` kwarg
  - Subtype propagator: structural checker with variance-driven decomposition
  - `sre-maybe-decompose` + `sre-decompose-generic` thread relation through
  - Decomp cache key includes relation name (equality ≠ subtype decompositions)
  - 22 new tests in `test-sre-subtype.rkt`
  - 7384 tests, 259.7s, all pass
  - **Key discovery**: type-lattice merge is equality-ordered (flat: Nat ≠ Int → top). Subtype propagator can't use `merge(a,b) = b` — needed domain-specific subtype check.

- **SRE Track 1 Phase 2b: Principles fixes** (`260903f`)
  - **Propagator-First violation fixed**: `flat-subtype?` (off-network predicate) → `subtype-lattice-merge` (proper lattice merge encoding subtype ordering). `subtype-merge(Nat, Int) = Int` because Nat <: Int. Subtyping now fully on-network.
  - **Circular dependency broken**: `subtype?` + `type-key` + `subtype-lattice-merge` extracted to `subtype-predicate.rkt`. Both `unify.rkt` and `typing-core.rkt` import without cycles.
  - **Duality placeholder fixed**: errors instead of silently falling back to equality (equality is stricter → wrong behavior).
  - **Lesson**: The user caught the `flat-subtype?` violation by asking "is this moving the propagator off-network?" — the Principles-First Design Gate in action. Scanning for principles violations during implementation (not just during design) caught this before it compounded.
  - **Design insight**: Two lattice orderings on the same carrier — equality (flat) and subtype (partial order) — require separate merge functions. The relation selects which merge to use. This is the "correct lattice for the correct relation" principle.
  - 7384 tests, 254.4s, all pass

- **SRE Track 1 Phase 3: Duality-aware structural-relate** (`20a3e84`, `48634d8`)
  - 5 session constructors registered in ctor-registry (Send, Recv, AsyncSend, AsyncRecv, Mu)
  - `session-lattice-spec` as `'session` symbol sentinel (circular dep: session-lattice → type-lattice → reduction → ctor-registry)
  - `sre-make-duality-propagator`: constructor pairing via dual-pairs, bidirectional with involution
  - `sre-duality-decompose-dual-pair`: asymmetric decomposition — each side uses its OWN descriptor
  - `sre-domain` +`top-value` field (10th): for relation violations where lattice-merge can't produce contradiction
  - **Key lesson**: Duality decomposition is fundamentally asymmetric (Send extracts ≠ Recv extracts). Can't reuse `sre-decompose-generic` (assumes same tag both sides). Needed dedicated `sre-duality-decompose-dual-pair`.
  - **Key lesson**: Don't pre-write dual values with copied sub-components — they need to be dualized by sub-cell propagators. Skeleton approach failed (cross-domain bot values). Solution: decompose directly, let sub-cell propagators + reconstructors build correct values.
  - 9 tests in test-sre-duality.rkt. ctor-registry test updated (21 → 26 descs).

- **SRE Track 1 Phase 4: subtype? delegation** (`9045059`, `7c8ef21`)
  - `subtype?` now: equal → flat fast path → SRE structural check
  - Query pattern: mini-network, two cells, subtype-relate propagator, quiesce, check contradiction
  - `current-subtype-check-count` frequency counter for monitoring
  - Behavioral change: `subtype?` is now reflexive (standard preorder semantics). Test updated.
  - `type-sre-domain-for-subtype`: standalone domain for structural queries, no meta-recognizer

- **SRE Track 1 Phase 5: Session duality via SRE** (`58dd714`)
  - Old: two unidirectional propagators calling `(dual v)` — off-network Racket function
  - New: single SRE duality propagator via `sre-make-structural-relate-propagator #:relation sre-duality`
  - `session-sre-domain`: session domain spec with dual-pairs, sess-meta? recognizer
  - **Drop-in replacement**: all 21 session test files pass with zero test changes

---

## Considerations

- **Phase 6 (verification + PIR)** in progress. Full suite running.
- **NTT-Racket isomorphism CONFIRMED**: `#:relation` parameter maps directly to NTT's relation-parameterized structural decomposition. `subtype-merge` IS the NTT's subtype lattice. Session duality IS the NTT's `:dual-pairs`. The isomorphism holds at Track 1 level. Promote from "watching" to "confirmed pattern."
- **Dependent session duality (DSend/DRecv)**: Not yet tested via SRE (binder-open-fn not implemented). Current SRE handles binder-depth=0 constructors only. DSend/DRecv registered but binder decomposition falls through. Phase 3 deliverable partially complete — basic duality works, dependent duality deferred to follow-up.
- **Suite time**: awaiting Phase 6 results.
