# Dailies — 2026-03-21

## Carried Forward from Previous Interval

### First-Class Path Values: Phases 0-7c COMPLETE
- Full 14-file pipeline for `expr-path`, `expr-get-in`, `expr-update-in`
- Broadcast `.*field` via dedicated `expr-broadcast-get` AST node (13-file pipeline)
- Key renaming `^` syntax for branching paths
- 33 acceptance results, 0 errors
- Deferred: Phase 7d (dot-brace `.{...}` expansion) — depends on reader work, independent of dispatch redesign

### Collection Interface: Audit + Design Complete, Awaiting Review
- Audit: `docs/tracking/2026-03-20_COLLECTION_INTERFACE_AUDIT.md` (commit `e632dce`)
- Design: `docs/tracking/2026-03-20_COLLECTION_INTERFACE_UNIFICATION_DESIGN.md` (overhauled `e7227f2`)
  - Propagator-resolved trait dispatch (post-Track 8)
  - Seq with efficient dispatch, LSeq as specific lazy type
  - Pre-Track 8 phases: ground-expr? fix, dot-brace syntax
  - Post-Track 8 phases: T1-T4 (trait-dispatched indexing/broadcast/union/Seq)

### Categorical Re-Examination: Five Systems Analyzed (`5cb97ad`)
- `docs/research/2026-03-21_CATEGORICAL_STRUCTURE_FIVE_SYSTEMS.org`
- Only type checker genuinely inhabits opfibration structure
- Bifibration/quantale claim from taxonomy is overgeneralization

### PUnify Parts 1-2: COMPLETE (47 commits)
- PIR: `docs/tracking/2026-03-19_PUNIFY_PARTS1_2_PIR.md` (commit `c72c133`)
- 7308 tests, 377 files, ~183s

### PUnify Part 3 Design: Overhaul Complete, Awaiting Review
- `docs/tracking/2026-03-19_PUNIFY_PART3_ATMS_SOLVER_ARCHITECTURE.md`

### Known Bugs / Gaps
- 5 punify parity bugs with toggle ON: `head`, `map`, `Pair`, `match`, `Vec`
- 5 L3 parser bugs from acceptance file (preparse/parser level)
- `bench-zonk.rkt` stale (uses `solve-meta!` without propagator infra)
- `current-punify-enabled?` no integration test at ON

### Deferred Items (from PUnify + Track 7)
- HIGH: Propagator/cell allocation efficiency track (audit `f7bd03d`, 6 optimizations identified)
- `restore-meta-state!` retirement (Track 8 P1: meta-info/id-map TMS-awareness)
- Mult bridge wiring in `decompose-pi` (Track 8 P1: id-map accessibility)
- 24 macros parameter writes (low priority)
- Layered scheduler (`run-to-layered-quiescence`) — Track 8+ scope
- Callback dependency hierarchy: root callbacks (`cell-read`/`write`, 63 sites) need coordinated module restructuring

### Watching (medium-term patterns, need 2-3 more data points)
- **Design critique as collaborative design**: Every design improves substantially through critique. Collection interface design (D.1 rejected as patch → D.2 principled overhaul) is the latest instance, joining Track 7 D.2/D.3, PUnify D.2, Part 3 overhaul. Now 5 instances. Approaching codification threshold — consider DESIGN_METHODOLOGY § "Critique Rounds" formalization.
- **Imperative bridge coupling**: Phase -1 bug (punify dispatch bypassing `solve-meta!`) is the third instance of elaboration/module-loading boundary. Track 8 module restructuring keeps getting scoped out. Watching for 4th, 5th instance.
- **PIR → tests → bug → fix chain**: Track 7 PIR found S(-1) retraction bug; PUnify PIR found 5 parity bugs. Two consecutive PIRs surfacing real bugs.
- **Suite time trajectory**: 235.2s → 181.2s → 175.9s → 183.2s → 247.5s. The 247.5s is a full-suite run; may include variance. Monitor whether it stabilizes.
- **Categorical precision over generalization**: Taxonomy's bifibration claim analyzed and found to overgeneralize. Pattern: research documents tend toward more general characterizations than implementations warrant. The 2026-03-13 analysis was more careful but still stretched for Systems I/II/V. Watching whether this "aspiration as characterization" pattern recurs.

---

## Lessons Distillation Check

### Scanned: PUnify PIR + 4 recent dailies (03-17 through 03-20)

**Candidates for principles documents**:

1. **"Design critique as collaborative design" — approaching codification threshold**. Now 5 instances (Track 7 D.2/D.3, PUnify D.2, Part 3 overhaul, Collection Interface D.1→D.2). The collection interface case is the most dramatic: D.1 was a complete architectural mismatch (patching dispatch sites) that D.2 replaced with a principled propagator-resolved approach. **Action**: Promote to Watching with intent to codify in DESIGN_METHODOLOGY after one more clear instance.

2. **"Categorical precision: attribute only load-bearing structure"**. The Five Systems analysis identified a methodological principle: attribute categorical structure only when it is genuinely load-bearing (the system would break without it), not merely compatible. Quantale enrichment present in fibers but not used by inter-fiber transitions. **Status**: New observation from this interval. Add to Watching — one data point so far.

3. **"Phase separation dissolves with propagator infrastructure"**. The collection interface design discovered that the compile-time/runtime gap is the same gap Tracks 1-7 have been dissolving. Sugar generating trait constraints that resolve on the propagator network is not a new mechanism — it's the existing mechanism applied to a new site. **Status**: Specific to collection dispatch; generalizes to "propagator-first design eliminates phase gaps." Already captured in DESIGN_PRINCIPLES as "Propagators as Universal Computational Substrate."

4. **Item 4 from 03-20 dailies (pipeline.md known coupling for solve-meta!)**: Was flagged for action. Verify it was added.

**Action taken**: Items 1-2 in Watching. Item 3 already captured. Item 4 should be verified.

---

## Today's Work

### Completed

#### Work Structure Principles Document (`ab5279d`)

- **Document**: `docs/tracking/principles/WORK_STRUCTURE.org`
- Codifies the hierarchy of work units: Series → Track → Phase → Sub-phase (+ Parts)
- Formalizes Audits as Stage 2 artifacts that bridge observation to action
- Documents the Audit → Track pipeline (every significant Track preceded by an Audit)
- Cross-Track Requirements as Series coherence mechanism (Collection Interface §6 as prototype)
- Acceptance files as executable specifications (beyond their testing origin)
- Design critique as collaborative design — 5 instances documented, approaching principle status
- Checklists for starting/completing Series and Tracks
- Filing conventions and mapping to five Design Methodology stages

#### BSP-LE Series Master Roadmap (`3080f8e`)

- **Document**: `docs/tracking/2026-03-21_BSP_LE_MASTER.md`
- Elevated former PUnify Part 3 scope into a full Series with 6 Tracks
- **Thesis**: "Every piece of the logic engine operates on propagator cells — choice points are ATMS assumptions, conjunction is worklist scheduling, backtracking is nogood accumulation, tabling is cell quiescence, parallel exploration is BSP"
- **Tracks**: (0) Allocation Efficiency, (1) UnionFind, (2) ATMS Solver / Multiverse Multiplexer, (3) Tabling, (4) BSP Pipeline with Kan extension architecture, (5) Solver Language
- Captures external categorical recommendations: left Kan (pipelined strata), right Kan (demand-driven forwarding), polynomial functors (network topology), operads (propagator typing)
- Dependency graph: Tracks 1-2 parallel, Track 3 depends on Track 2, Track 4 extends BSP, Track 5 depends on all
- **Design decision**: Part 3 design doc becomes Series origin document; each Track gets its own Stage 3 design doc
- First application of the WORK_STRUCTURE.org methodology codified earlier this session

#### CIU Series Master Roadmap (`53caf8a`)

- **Document**: `docs/tracking/2026-03-21_CIU_MASTER.md`
- Elevated collection interface unification design into a full Series with 6 Tracks
- **Thesis**: All collection access dispatches through traits resolved on propagator network — syntactic sugar generates trait constraints, not constructor-specific AST nodes
- **Tracks**: (0) Trait Hierarchy Design (deep Stage 2), (1) Seq Protocol, (2) Syntactic Sugar Normalization, (3) Trait-Dispatched Access [post-T8], (4) Trait-Dispatched Iteration [post-T8], (5) Union-Aware Dispatch [post-T8]
- Addresses 5 principle violations from Collection Interface Audit
- Pre/post Track 8 split: Tracks 0-2 now, Tracks 3-5 after propagator migration
- Absorbs DEFERRED.md items: Seq-as-trait migration, transducer runners for non-List
- Flagged: DESIGN_PRINCIPLES.org line 205-206 "LSeq as hub type" needs correction to "Seq with efficient dispatch"

#### DESIGN_PRINCIPLES.org Update: Seq with Efficient Dispatch (`1363d42`)

- Corrected §Most Generalizable Interface: "LSeq as hub type" → "Seq as universal iteration protocol"
- Every collection implements `Seq` natively; `LSeq` is a specific lazy type, not mandatory intermediary
- Syntactic sugar generates trait constraints resolved on propagator network — not constructor-specific dispatch
- Principles documents are cornerstones — accuracy is load-bearing for all downstream designs

#### MASTER_ROADMAP.org: Top-Level Work Index (`66da68a`, updated `732cfb6`, `7a7d41f`, `eff3790`)

- **Document**: `docs/tracking/MASTER_ROADMAP.org`
- Single source of truth for all planned, active, and completed work
- Indexes 3 active Series, 8 completed standalone Tracks, 5 standalone designs, 8 audits, research, principles
- Cross-Series dependency diagram (Track 8 as critical path for CIU Tracks 3-5)
- Verified: all open work is folded into one of the three Series or tracked in DEFERRED.md
- **Current Implementation Order** (`eff3790`): Prioritized sequencing across Series:
  1. PUnify Cleanup (pre-Series)
  2. BSP-LE Track 0 (allocation efficiency)
  3. CIU Track 0 (trait hierarchy audit — pre-Track 8, informs Track 8)
  4. PM Track 8 (informed by CIU Track 0 + allocation improvements)
  5. CIU Track 0 revisit (post-Track 8 design calibration)
  6-7. CIU Tracks 1-5 and BSP-LE Tracks 1-5 (on Track 8 infrastructure)
- **Design insight**: CIU Track 0 audit → Track 8 → CIU Track 0 revisit is the audit→build→re-audit pattern — honest about infrastructure changing the design space

#### CIU Track 0: Trait Hierarchy Audit (`f62fc06`)

- **Document**: `docs/tracking/2026-03-21_CIU_TRACK0_TRAIT_HIERARCHY_AUDIT.md`
- Deep Stage 2 audit: 8 traits, 5 collection types, 4 sugar dispatch sites, against 3 principles
- **8 findings**: F-1 Seqable/Buildable LSeq-coupled, F-2 Seq is deftype not trait, F-3 PVec/Set/Map lack Seq, F-4 Indexed/Keyed exist but sugar ignores them, F-5 Map is an island, F-6 HKT `impl` doesn't work (CRITICAL), F-7 broadcast cons/nil only, F-8 Collection bundle LSeq-centric
- **Critical path**: F-6 (HKT `impl` support) is the linchpin — without it, Seq-as-trait can't resolve, sugar can't generate resolvable constraints, entire CIU vision blocked
- **Recommended direction**: Seq as primary trait (replacing Seqable), Functor for structure-preserving map, Buildable redesigned around Seq not LSeq
- **Track 8 priorities informed**: HKT impl/resolution, callback elimination for `surf-get`, module restructuring for elaborator→trait-resolution
- **4 post-Track 8 revisit questions** captured for CIU Track 0 revisit phase
- **Design insight**: Zero HKT traits use `impl` syntax anywhere in the codebase — all use manual `def C--Trait--dict`. This is the infrastructure gap, not a design choice.

#### PM Track 8 Stage 3 Design D.1 (`d8367c7`)

- **Document**: `docs/tracking/2026-03-21_TRACK8_DESIGN.md`
- Two-part design grounded in Track 8 audit + CIU Track 0 audit:
- **Part A (Infrastructure Migration)**: 5 phases — meta-info TMS-awareness, id-map accessibility, mult/level/session on elab-network, restore-meta-state! retirement, resolution state simplification
- **Part B (HKT Resolution + Module Restructuring)**: 6 phases — module graph analysis, cell-ops extraction, root callback elimination (63 sites), HKT `impl` registration, HKT trait resolution on propagator network, sugar constraint generation
- **Key design decisions**: Option C architecture confirmed, per-entry assumption tagging for meta-info TMS, cell-ops extraction to break circular deps, `(trait-name, type-constructor)` as HKT impl key
- **CIU Track 0 F-6 elevated**: HKT `impl` as Part B centerpiece — the linchpin finding from the trait hierarchy audit drove the entire Part B design
- **Cross-track requirements table**: documents what Track 8 provides to CIU (HKT resolution, sugar constraints) and BSP-LE (TMS-clean speculation, callback elimination)
- **Status**: D.1 awaiting critique

#### BSP-LE Track 0 Stage 3 Design D.1 (`8476ac6`)

- **Document**: `docs/tracking/2026-03-21_BSP_LE_TRACK0_ALLOCATION_EFFICIENCY_DESIGN.md`
- Seven-phase design from allocation audit:
  - Phase 0: micro-benchmarks + A/B baselines
  - Phase 1-2: eq?-first fast path + merge identity audit (enables eq?-only fixpoint)
  - Phase 3: mutable worklist/fuel in quiescence loop (highest per-iteration win)
  - Phase 4: field-group struct split hot(2)/warm(2)/cold(9) (largest total impact)
  - Phase 5-6: batch cell registration + input batching via transient CHAMP
  - Phase 7: verification + A/B comparison
- **Target**: zero struct allocation per worklist iteration (Phases 3+4 compound)
- **Preserves**: pure data-in/data-out contract throughout
- **Status**: D.1 — next item in implementation order after PUnify cleanup

#### BSP-LE Track 0 Phase 0: Baselines (`8c4da4a`)

- **Micro-benchmarks**: `benchmarks/micro/bench-alloc.rkt` — direct timing with 15 samples
- **Key baselines**: net-new-cell 0.42μs/op, net-cell-write 0.27μs (change) / 0.09μs (no-change), net-add-propagator 1.08μs/op, struct-copy raw 0.03μs/op
- **Finding**: Raw struct-copy (0.03μs) is NOT the dominant cost — CHAMP operations are. net-add-propagator at 1.08μs (36× raw struct-copy) is the most expensive due to per-input dependency registration
- **Memory**: 51.3 KB retention over 100 quiescence runs on 500-cell chain
- **Change ratio**: 100% no-change on re-propagation (validates audit assumption)

#### BSP-LE Track 0 Phase 1: eq?-first fast path (`19274b5`)

- Single-line change to `propagator.rkt`: `(or (eq? merged old-val) (equal? merged old-val))`
- Applied to both `net-cell-write` and `net-cell-write-widen`
- **7308 tests pass, 232.3s** — no behavioral change
- Real impact comes after Phase 2 (merge identity audit makes eq? catch 100% of no-change cases)

#### BSP-LE Track 0 Phase 2: Merge identity audit (`7e8875b`)

- Audited 17 merge functions across 7 files; 13/17 did not preserve identity
- **Fixed 6** in `infra-cell.rkt`: empty-new guard on hasheq-union, hasheq-list-append, list-append, set-union, constraint-status-map, error-descriptor-map
- **4 already OK**: mult-lattice-merge, merge-replace, merge-mod-status, term-merge
- **7 structural** (unavoidable reconstruction): type-lattice-merge, session-lattice-merge, generic-merge (ctor-registry), merge-tms-cell, make-tms-merge, interval-merge, constraint-merge (partial)
- **7308 tests pass, 231.0s** — no behavioral change
- Combined with Phase 1: empty-new writes short-circuit before net-cell-write's merge call

#### BSP-LE Track 0 Phase 3a: Struct-copy site classification (`a5de346`)

- **Document**: `docs/tracking/2026-03-21_PHASE3A_STRUCT_COPY_CLASSIFICATION.md`
- All 25 `struct-copy prop-network` sites classified: 6 hot-only, 4 warm-only, 6 cold-only, 4 warm+hot, 2 warm+cold, 1 all-three
- **Worklist audit confirmed**: no propagator fire function reads `worklist` or `fuel` — mutable worklist approach is safe
- `net-add-propagator` (#11) is the only site touching all three groups

#### BSP-LE Track 0 Phase 3b: Struct split (`bfe8e4f`)

- hot(2)/warm(2)/cold(9) split of prop-network. 29 sites in propagator.rkt + 4 external files
- Zero-cost accessor macros preserve old API across 17 consumer files
- 7308 tests 240.5s
- **Lesson**: External files (bilattice.rkt, elaborator-network.rkt, test-propagator-bsp.rkt, bench-alloc.rkt) also had struct-copy sites — batch worker crashed silently until these were migrated

#### BSP-LE Track 0 Phase 3c: Mutable worklist drain (`7a94274`)

- Box-based drain pattern eliminates per-iteration struct-copy for worklist/fuel
- eq? identity preserved for no-work fast path (test-propagator-persistence.rkt caught this)
- Applied to both serial and traced quiescence loops
- 7308 tests 238.2s

#### BSP-LE Track 0 Phase 3d-3f: Group isolation + stable API (`cb7eb38`)

- Phases 3d-3e already achieved by Phase 3b migration (warm+hot on cell-write, warm+cold on allocation)
- Phase 3f: accessor macros retained as stable API — decouples 18 consumer files from inner struct layout at zero cost

#### BSP-LE Track 0 Phase 4: Batch cell registration (`6cf9a70`)

- `net-new-cells-batch` using transient CHAMP + contiguous ID range pre-allocation
- 5 tests. Site conversion awaits Track 8 callback elimination

#### BSP-LE Track 0 Phase 5: Input batching REJECTED (`8af8695`)

- Transient CHAMP for `net-add-propagator` dependency registration regressed 345s (from 239s)
- `champ-transient`/`tchamp-freeze` cycle too expensive for typical 2-3 input propagators
- **Lesson**: transient CHAMP suited for large batches (N>>10), counterproductive for small-arity

#### BSP-LE Track 0 Phase 6: Verification + GC analysis (`210d80d`)

- **Wall-time neutral** (+2%, within noise): CHAMP operations dominate, not struct-copy
- **GC during quiescence: 0 ms** — mutable worklist eliminates all intermediate struct allocation
- struct-copy 3× cheaper (0.03→0.01μs), cell-write -7%, propagator allocation -4%
- Memory retention: -12.8 KB (no leak)
- Wall-time target (≤180s) not met; allocation/GC improvement is the real win
- **Scaling argument**: zero GC per quiescence compounds as Track 8/BSP-LE add more propagation

#### BSP-LE Track 0 PIR (`00a873f`)

- 16-question PIR. 3 wrong assumptions, 6 lessons, 3 cross-PIR patterns
- Key finding: CHAMP operations are the actual bottleneck (10× struct-copy)
- Pipeline.md updated: struct changes require codebase-wide grep for struct-copy sites

#### CHAMP Performance Audit + Design D.1 (`12035d0`)

- **Audit**: `docs/tracking/2026-03-21_CHAMP_PERFORMANCE_AUDIT.md` — 8 findings in champ.rkt
  - F-1: `equal?` key comparison where `eq?` suffices; F-3: manual copy loops; F-4: value-only updates copy full vector; F-5: no owner-ID transients; F-6: sequential keys concentrate modifications
- **Design**: `docs/tracking/2026-03-21_CHAMP_PERFORMANCE_DESIGN.md` — 7 phases
  - Phases 1-3: low-risk (vector-copy!, eq?-first, value-only fast path)
  - Phases 4-6: owner-ID transient infrastructure (rehabilitates Track 0 Phase 5)
  - Phase 7: verification + BSP-LE Track 0 Phase 5 revisit + accumulate-during-quiescence prototype

#### CHAMP Performance Phase 0: Baselines + acceptance (`c3e1b37`)

- `test-champ-owner-id.rkt`: 8 §A persistent baseline tests
- CHAMP benchmarks: insert 0.16μs, value-update 0.10μs, lookup 0.04μs
- **Key finding**: transient cycle at N=2 costs 95.5μs — 600× a persistent insert. Quantifies why Track 0 Phase 5 regressed.

#### CHAMP Performance Phases 1-3: Low-risk optimizations (`57e052d`)

- Phase 1: `vector-copy!` in `vec-insert`/`vec-remove` (memcpy vs manual loop)
- Phase 2: `eq?`-first in all 6 key comparison sites (3-5× faster for network keys)
- Phase 3: Value-only update fast path — same-value insert returns eq? node with zero allocation. Propagates through ALL trie levels (leaf→parent→root chain). Compounds with Track 0 merge identity audit: entire merge→champ-insert→net-cell-write chain short-circuits on no-change.
- §B acceptance tests (3) uncommented and passing
- 7324 tests, 234.7s, all pass

#### CHAMP Performance Phase 4: Owner-ID node structure (`1fd5eff`)

- Added `edit` field to `champ-node` (4th field, `#f` for persistent). 12 constructor sites updated.
- Pipeline checklist: codebase-wide grep confirmed no external struct-copy/match on champ-node
- No regression: 232.5s (vs 234.7s pre-Phase-4)

#### CHAMP Performance Phases 5-6: Owner-ID transient operations + freeze (`ce5a4df`)

- Phase 5: `tchamp-insert-owned!`, `tchamp-delete-owned!`, `tchamp-insert-join-owned!` with ownership discipline. `ensure-owned` helper path-copies shared nodes, stamps with edit token. eq? value check in owned-node path.
- Phase 6: `freeze-node` walks trie clearing edit on owned nodes — O(modified paths × depth). `champ-all-persistent?` invariant checker.
- §C-F acceptance tests (6) uncommented and passing. 7330 tests 234.7s.
- **Key milestone**: Owner-ID transient cycle is now O(modified paths), not O(N log N). Ready for Phase 7 verification + BSP-LE Track 0 Phase 5 revisit.

#### CHAMP Performance Phase 7: Verification + Track 0 Phase 5 rehabilitated (`cfcead4`)

- **Owner-ID transient benchmarks** (the payoff):
  - N=2: 95.5μs → 6.0μs (**16× faster**)
  - N=10: 19.4μs → 1.1μs (18× faster)
  - N=100: 2.57μs → 0.28μs (9× faster)
  - Value-only updates: 0.07μs (30% faster than persistent 0.10μs)
- **BSP-LE Track 0 Phase 5 rehabilitated**: `net-add-propagator` now uses owner-ID transient for dependency registration. Old hash-table transient regressed to 345s; owner-ID: 232.9s (neutral). The 44% regression is eliminated.
- Suite: 7330 tests, 232.9s, all pass

#### CHAMP Performance PIR (`b748e5d`)

- 16-question PIR. 4 cross-PIR patterns (including 2 new: negative results rehabilitation, cross-Track compounding)
- 2 wrong assumptions (N=2 target, value-only recursion propagation)

#### PUnify Cleanup: Toggle Blocked (`3a2f59c`)

- Attempted to flip `current-punify-enabled?` to `#t`. Option module loading hangs — cell-tree unification issue with sum types during module loading. Toggle reverted. Blocker captured in BSP-LE prerequisites.

#### PM Track 8 Design: D.1 through D.3 (multiple commits through `a9b2c7c`)

- **D.1**: Two-part design (A: infrastructure migration, B: HKT resolution + module restructuring)
- **D.1.1**: Part C added (all constraint resolution as propagators) + B2e (macros cleanup) + deferral scan
- **D.2**: External critique — 13 items incorporated (tag lifecycle, id-map lifecycle, bridge mid-quiescence safety, speculation-failures, dual cell-ops API, transient-awareness, constructor extraction boundary, fallback selection, feedback loop acknowledgment, S1 as verification pass, memo cache Option E, performance budget)
- **Post-D.2**: Kan extension annotations (left Kan = C1 resolution in S0, right Kan = demand-driven quiescence); accumulate-during-quiescence promoted from deferred to B2f; prior work pattern study (session-type-bridge as C1 template); Part C rewritten grounding in cross-domain bridge pattern
- **D.3**: Self-critique + principles alignment — 9 findings including CRITICAL solve-meta! re-entrancy issue, Gödel Completeness termination arguments, network size growth, principles alignment table (10 aligned + 2 tensions)
- **Track 9** (Reduction as Propagators): Stage 1 research note capturing memo-cache-as-cells vision
- PM Master Track renumbered: Track 9 = Reduction as Propagators, Track 10 = GDE, Track 11 = LSP

### In Progress

- PM Track 8 implementation: starting Phase A0

---

## Considerations

- Phase 7a revealed that the `Path` parser atom conflict with `prologos::data::path` requires a per-context solution — foreign type annotations use `$foreign-Path` sentinel; library specs infer Path type from foreign primitives
- Foreign type token processing has an inherent ambiguity between multi-token uncurried segments and bracketed type applications — `multi-token-list` wrapper resolves this for now
- QTT `expr-union` gap was discovered via user's heterogeneous map — any type former that can appear as an embedded type annotation in an AST node needs an `inferQ` case
- Dash-list (`-`) items in implicit map syntax don't flatten multi-line key-value continuations — this is a reader/preparse issue, not an elaboration issue
- Collection interface design: the user explicitly said "design discussion mode" — engage with design critique, not implementation

---

## Metrics

- Start: 7308 tests, 377 files, ~183s baseline
- Current: 7330 tests, 378 files, 232.9s
- Last commit: `a9b2c7c` (Track 8 D.3: self-critique + principles alignment)
- Memory: MEMORY.md updated with Document Index section pointing to MASTER_ROADMAP.org
