# Dailies — 2026-03-21

## Carried Forward from Previous Interval

### First-Class Path Values: Phases 0-7c COMPLETE
- Full 14-file pipeline for `expr-path`, `expr-get-in`, `expr-update-in`
- Broadcast `.*field` via dedicated `expr-broadcast-get` AST node (13-file pipeline)
- Key renaming `^` syntax for branching paths
- 33 acceptance results, 0 errors
- Deferred: Phase 7d (dot-brace `.{...}` expansion) — depends on reader work, independent of dispatch redesign

### Collection Interface: Audit + Design Complete, Awaiting Review
- Audit: `docs/tracking/2026-03-20_COLLECTION_INTERFACE_AUDIT.md` (commit `e632dce`)
- Design: `docs/tracking/2026-03-20_COLLECTION_INTERFACE_UNIFICATION_DESIGN.md` (overhauled `e7227f2`)
  - Propagator-resolved trait dispatch (post-Track 8)
  - Seq with efficient dispatch, LSeq as specific lazy type
  - Pre-Track 8 phases: ground-expr? fix, dot-brace syntax
  - Post-Track 8 phases: T1-T4 (trait-dispatched indexing/broadcast/union/Seq)

### Categorical Re-Examination: Five Systems Analyzed (`5cb97ad`)
- `docs/research/2026-03-21_CATEGORICAL_STRUCTURE_FIVE_SYSTEMS.org`
- Only type checker genuinely inhabits opfibration structure
- Bifibration/quantale claim from taxonomy is overgeneralization

### PUnify Parts 1-2: COMPLETE (47 commits)
- PIR: `docs/tracking/2026-03-19_PUNIFY_PARTS1_2_PIR.md` (commit `c72c133`)
- 7308 tests, 377 files, ~183s

### PUnify Part 3 Design: Overhaul Complete, Awaiting Review
- `docs/tracking/2026-03-19_PUNIFY_PART3_ATMS_SOLVER_ARCHITECTURE.md`

### Known Bugs / Gaps
- 5 punify parity bugs with toggle ON: `head`, `map`, `Pair`, `match`, `Vec`
- 5 L3 parser bugs from acceptance file (preparse/parser level)
- `bench-zonk.rkt` stale (uses `solve-meta!` without propagator infra)
- `current-punify-enabled?` no integration test at ON

### Deferred Items (from PUnify + Track 7)
- HIGH: Propagator/cell allocation efficiency track (audit `f7bd03d`, 6 optimizations identified)
- `restore-meta-state!` retirement (Track 8 P1: meta-info/id-map TMS-awareness)
- Mult bridge wiring in `decompose-pi` (Track 8 P1: id-map accessibility)
- 24 macros parameter writes (low priority)
- Layered scheduler (`run-to-layered-quiescence`) — Track 8+ scope
- Callback dependency hierarchy: root callbacks (`cell-read`/`write`, 63 sites) need coordinated module restructuring

### Watching (medium-term patterns, need 2-3 more data points)
- **Design critique as collaborative design**: Every design improves substantially through critique. Collection interface design (D.1 rejected as patch → D.2 principled overhaul) is the latest instance, joining Track 7 D.2/D.3, PUnify D.2, Part 3 overhaul. Now 5 instances. Approaching codification threshold — consider DESIGN_METHODOLOGY § "Critique Rounds" formalization.
- **Imperative bridge coupling**: Phase -1 bug (punify dispatch bypassing `solve-meta!`) is the third instance of elaboration/module-loading boundary. Track 8 module restructuring keeps getting scoped out. Watching for 4th, 5th instance.
- **PIR → tests → bug → fix chain**: Track 7 PIR found S(-1) retraction bug; PUnify PIR found 5 parity bugs. Two consecutive PIRs surfacing real bugs.
- **Suite time trajectory**: 235.2s → 181.2s → 175.9s → 183.2s → 247.5s. The 247.5s is a full-suite run; may include variance. Monitor whether it stabilizes.
- **Categorical precision over generalization**: Taxonomy's bifibration claim analyzed and found to overgeneralize. Pattern: research documents tend toward more general characterizations than implementations warrant. The 2026-03-13 analysis was more careful but still stretched for Systems I/II/V. Watching whether this "aspiration as characterization" pattern recurs.

---

## Lessons Distillation Check

### Scanned: PUnify PIR + 4 recent dailies (03-17 through 03-20)

**Candidates for principles documents**:

1. **"Design critique as collaborative design" — approaching codification threshold**. Now 5 instances (Track 7 D.2/D.3, PUnify D.2, Part 3 overhaul, Collection Interface D.1→D.2). The collection interface case is the most dramatic: D.1 was a complete architectural mismatch (patching dispatch sites) that D.2 replaced with a principled propagator-resolved approach. **Action**: Promote to Watching with intent to codify in DESIGN_METHODOLOGY after one more clear instance.

2. **"Categorical precision: attribute only load-bearing structure"**. The Five Systems analysis identified a methodological principle: attribute categorical structure only when it is genuinely load-bearing (the system would break without it), not merely compatible. Quantale enrichment present in fibers but not used by inter-fiber transitions. **Status**: New observation from this interval. Add to Watching — one data point so far.

3. **"Phase separation dissolves with propagator infrastructure"**. The collection interface design discovered that the compile-time/runtime gap is the same gap Tracks 1-7 have been dissolving. Sugar generating trait constraints that resolve on the propagator network is not a new mechanism — it's the existing mechanism applied to a new site. **Status**: Specific to collection dispatch; generalizes to "propagator-first design eliminates phase gaps." Already captured in DESIGN_PRINCIPLES as "Propagators as Universal Computational Substrate."

4. **Item 4 from 03-20 dailies (pipeline.md known coupling for solve-meta!)**: Was flagged for action. Verify it was added.

**Action taken**: Items 1-2 in Watching. Item 3 already captured. Item 4 should be verified.

---

## Today's Work

### Completed

(Session just started — no completed work yet)

### In Progress

(Awaiting direction)

---

## Considerations

- Phase 7a revealed that the `Path` parser atom conflict with `prologos::data::path` requires a per-context solution — foreign type annotations use `$foreign-Path` sentinel; library specs infer Path type from foreign primitives
- Foreign type token processing has an inherent ambiguity between multi-token uncurried segments and bracketed type applications — `multi-token-list` wrapper resolves this for now
- QTT `expr-union` gap was discovered via user's heterogeneous map — any type former that can appear as an embedded type annotation in an AST node needs an `inferQ` case
- Dash-list (`-`) items in implicit map syntax don't flatten multi-line key-value continuations — this is a reader/preparse issue, not an elaboration issue
- Collection interface design: the user explicitly said "design discussion mode" — engage with design critique, not implementation

---

## Metrics

- Start: 7308 tests, 377 files, ~183s baseline
- Current: 7308 tests, 377 files
- Commits this session: (none yet)
- Last commit: `5cb97ad` (Categorical re-examination of five propagator systems)
