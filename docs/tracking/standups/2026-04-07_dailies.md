# Dailies — 2026-04-07

## Carried Forward from Previous Interval

### PPN Track 4B ✅ COMPLETE
- Full attribute evaluation: 5 domains, S0/S1/S2 stratification, global attribute store
- 90% on-network (up from 46%), 391/391 files, 7609 tests, 134.1s
- PIR written (21 sections, 11-PIR longitudinal survey)
- 6 imperative bridges documented for Track 4C dissolution

### PPN Track 4C — SCOPED (not started)
- Design note captured: 6 bridges (solve-meta!, resolve-trait-constraints!, freeze, checkQ, emit-coercion-warning!, unsolved-dict)
- Each bridge has root cause + resolution path
- Depends on BSP-LE 1.5 for union type bridge

### BSP-LE Track 1.5 — DESIGN NOTE CAPTURED (not started)
- Cell-based TMS: worldview as cell value (on-network)
- Blocks PPN 4B Phase 8 + Track 2

### Known Gaps / Outstanding
- Batch-worker isolation: 12 tests still failing in batch mode (DEFERRED, medium severity)
- 6 lessons pending distillation to principles documents
- Grammar Form R&D deferred
- Usage tracking has minimal dedicated test coverage (Phase 4)

### Watching (medium-term patterns)
- **Conversational cadence effectiveness**: Track 4B validated the pattern (90% on-arch vs Track 4's divergence). Now codified in DESIGN_METHODOLOGY + workflow.md. Monitor across next 2-3 tracks to confirm it generalizes.
- **Per-phase test gate compliance**: Rule codified but Track 4B's own Phase T was still end-of-track. First real test: next implementation track.
- **Lattice assumption errors**: 5/11 PIRs had wrong lattice/algebraic assumptions. This is the #1 risk category. Any new lattice work should include explicit property verification.
- **Test delta = 0 pattern**: 3/11 PIRs had +0 test delta, all required follow-up. The new phase-completion gate should prevent this.

---

## Lessons Distillation Check

### From Track 4B PIR (2026-04-07)
- ⊥ must be distinguishable from every legitimate value → **pending** distillation to DEVELOPMENT_LESSONS.org
- `#f` registration = invisible deferral → **pending** distillation
- Comprehensive vocabulary audit before coverage expansion → **pending** distillation
- P1/P2/P3 propagator patterns → **pending** distillation to PATTERNS_AND_CONVENTIONS.org

### From recent dailies (2026-04-04, 2026-04-05)
- Side-effect coupling as systemic pattern (3 tracks hitting same boundary) → **addressed** by Track 4B (all 5 domains on-network)
- Divergence detection via Network Reality Check → **validated** (1 successful application in Track 4B)
- Conversational cadence preventing divergence → **codified** (commit `b3614018`)

### Status: 4 lessons pending distillation. Schedule for next quiet interval.

---

## Session Log

### Process retrospective: PPN Track 4 vs Track 4B

Discussed the process differences between Track 4 (catastrophic divergence during long autonomous implementation) and Track 4B (stayed on-architecture via phase-by-phase dialogue). Key findings codified:

1. **Conversational implementation cadence** — max ~1h autonomous stretch, dialogue at every phase boundary. Prevents implementer defaults from overriding design intent. Added to DESIGN_METHODOLOGY.org Stage 4 + workflow.md (commit `b3614018`).

2. **Per-phase test gate** — test coverage verification as step (a) of phase completion checklist. "Will add tests later" is not valid. Added to both files.

### Pipeline collapse assessment

Audited the 14-file compilation pipeline post-Track 4B. Result: **8 files still mandatory** for new expression kinds, **2 collapsed** (typing-core.rkt via fallback, qtt.rkt via install-usage-from-rule). The structural pipeline (syntax, parser, elaborator, reduction, substitution, zonk, pretty-print) is untouched. The grammar-rule vision (PPN 6/7) requires SRE Track 6 (Reduction-on-SRE) to collapse reduction + substitution.

### Priority discussion: next track sequencing

Evaluated three candidates:
- **BSP-LE Track 2** (ATMS Solver + Cell-Based TMS) — foundation everything builds on
- **SRE Track 3** (Trait Resolution) — dissolves resolve-trait-constraints! bridge before PPN 4C
- **SRE Track 5** (Pattern Compilation) — unifies narrowing + reduction dispatch

Sequencing decision: BSP-LE 2 first (TMS architecture), then SRE 3 (trait resolution), then SRE 5 (pattern compilation), then PPN 4C (dissolve remaining bridges).

### BSP-LE Track 2 Stage 1/2 document (commit `04a97d4c`)

Combined research synthesis + codebase audit. Synthesized: Logic Engine Design (Phases 4-7), PUnify Part 3 (Multiverse Multiplexer), Cell-Based TMS note, PAR Track 1, categorical foundations, OR-parallel Prolog. Audited: TMS implementation (only 2 integration points need migration), 5 speculation users, ATMS (397 lines, functional), DFS solver (300 lines to replace). Estimated ~1700 lines across 10 phases. Ready for Stage 3 design.

### BSP-LE Track 2 D.1 → D.2: self-critique cycle

**Self-critique (P/R/M three-lens, 17 findings)**:
- R lens: migration scope corrections (6 files not 5), prop-net-warm placement, solver entry point in reduction.rkt
- M lens: clause-match-bulk → parallel-map propagator pattern (new infrastructure). Branch-pruner via topology request protocol. Answer accumulator cell (HIGH — was missing). NAF = BSP barrier.
- P lens: Two-tier confirmed as correct (not pragmatic — complete). Decision cells primary, worldview derived. ATMS struct = second source of truth → needs dissolution.

**P4 design conversation — ATMS struct dissolution**:
All 7 ATMS fields → cells. No off-network state remains. Key findings:
- `believed` ELIMINATED: worldview emerges from decision cells, no separate cell needed. Consistency = AND-fan-in of per-decision viability.
- Retraction = decision cell narrowing = SRE structural operation. The word "retract" disappears.
- `atms-minimal-diagnoses` → tropical semiring CSP. Self-referential: solver diagnoses itself using own infrastructure.

**SRE Lattice Lens** — new mandatory practice codified in CRITIQUE_METHODOLOGY.org + workflow.md. Every lattice design decision gets 5-question SRE analysis: classification, properties, bridges, composition diagram, primary vs derived. Origin: Track 2 finding that worldview was misidentified as primary when decision cells are primary.

**Hyperlattice Conjecture** — captured in DESIGN_PRINCIPLES.org. Every computable function → fixpoint on lattices. Design mandate: never accept "this has to be an algorithm."

**Critique Methodology** — new dedicated document (CRITIQUE_METHODOLOGY.org) with P/R/M three lenses, external critic briefing, anti-patterns, SRE lattice lens section.

### System diagnostics (non-project)

Diagnosed macOS UI soft-lock: WiFi subsystem spin loop (WiFiAgent/airportd/locationd/ControlCenter consuming ~100% CPU). Root cause: corrupted WiFi driver state after 3.5 days uptime, possibly triggered by numa DNS resolver upstream mismatch with Mullvad VPN. Investigated 1.8 GB WireGuard upload spike — likely queued traffic flush after WiFi restore, not exfiltration. VPS port 8317 forwarding was receiving SYN scans from T-Mobile IP. Discussed VPS hardening (SYN cookies, per-IP rate limiting, fail2ban).
