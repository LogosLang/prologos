# 2026-03-14 Dailies

**Session start**: 2026-03-14
**Previous dailies**: 2026-03-13 (covered master roadmap, Track 3 design, categorical analysis, principles docs)

---

## Completed

### Revised Categorical Analysis of Layered Recovery Principle
Major revision of `2026-03-13_LAYERED_RECOVERY_CATEGORICAL_ANALYSIS.org`. Key changes:
- **Thesis shift**: from "stratified endofunctors on quantale-enriched lattices" to "Grothendieck opfibrations over well-founded bases with optional enrichment"
- **System I (Logic Engine)**: quantale structure identified as incidental, not load-bearing. Fiber lattice is ℘(H) (Herbrand powerset), no sequential composition needed for stratification or ATMS
- **ATMS**: new sheaf-theoretic characterization — presheaf of supported values on a consistency site (Boolean algebra of assumptions filtered by nogoods). Nogood sharing identified as topology refinement (cross-level: fiber computation modifies base)
- **System II (Effect Order)**: quantale enrichment retained as genuinely load-bearing (session-to-effect bridge preserves sequential composition via quantale morphism)
- **System III (Type Checker)**: reframed as mixed enrichment (T: quantale, R: semilattice, A: free monoid)
- **Common structure**: opfibration in Sup over well-founded base, with optional quantale enrichment. Galois connections reframed as local adjunctions in fibered categories
- New §4 on ATMS sheaf theory and cross-level interactions
- 6 updated open questions (opcartesian vs lax, nesting order, Herbrand internalization, evolving sites, 2-categorical structure, graded monads)
- New references: Grothendieck fibrations, Turkenburg (CALCO 2023), Fujii (graded monads), coalgebraic semantics
- (commit `c518971`)

### Resolved Three Open Questions in Categorical Analysis
Cross-referenced implementation against fibration theory to resolve:
- **Q1 (opcartesian?)**: Systems I & III have canonical (unique) lifts; System II has chosen lifts (multiple valid linearizations). Framework is opfibration with cleavage, not strict Grothendieck opfibration.
- **Q2 (nesting order)**: Strata and worldview axes are orthogonal — product base B_strata × B_worldviews, not nested ∫_W ∫_k. Stratification is the default eval engine; ATMS is optional/value-level. Nogood discovery is a one-directional cross-axis side-channel.
- **Q3 (Herbrand base)**: Properly external under NAF — pure proof-search failure (`null?` check on result list), not complement against explicit universe. Internalization would require Boolean hyperdoctrine structure, needed only for explicit negation.
- Document made standalone (removed self-referential revision history language)
- (commit `1c8f9ea`)

### Stage-0 Research: Formal Modeling on Propagator Networks
Comprehensive research document covering the full landscape of temporal logic, fixpoint theory, and formal verification, mapped to propagator network computation.
- 1337 lines, 10 major sections, ~130 references
- Covers: modal μ-calculus (Kozen, alternation hierarchy, parity games, automata-logic-games triangle), temporal logics (LTL/CTL/CTL*, STL robustness semantics, MTL decidability, HyperLTL), probabilistic/quantitative logics (PCTL, CSL, discounted LTL, energy games), fixpoint theory (Knaster-Tarski, AFT/bilattices, mixed lfp/gfp, domain theory, coinduction, fixpoint games), runtime verification (LTL3, Lola/RTLola/Copilot/R2U2, distributed RV, predictive RV, shield synthesis), session types and linear logic (Caires-Pfenning, μ vs ν recursion, multiparty, Actris), verification frontiers (Iris/RustBelt, liquid types, algebraic effects, TLA+, reactive synthesis, coalgebraic semantics, capability security)
- Key thesis: all formalisms compute fixpoints over complete lattices; propagator networks are the universal engine
- Identifies 13-item research program from immediate (descending cells) through long-term (shield synthesis from session types)
- (commit `6d1b05c`)

### Stage 2 Design: Well-Founded Logic Engine (WFLE)
Design document for an alternate logic engine backend using bilattice cell pairs (AFT) to compute well-founded semantics.
- **Core idea**: Each logic variable gets two propagator cells — `lower` (ascending/μ) and `upper` (descending/ν). The gap `[lower, upper]` narrows to produce three-valued output: true/false/unknown.
- Evaluated 3 approaches: (A) bilattice pairs, (B) polarity-tagged cells with alternating scheduler, (C) hybrid. Recommended A with evolution path to C.
- Infrastructure gap analysis: descending cells, meet-fn registry, bilattice-var struct, well-founded propagator patterns, three-valued tabling
- Establishes the design pattern for coinductive session types, runtime monitoring, model checking, shield synthesis
- 7 implementation phases, 5 risks with mitigations, 5 open questions
- (commit `55b21ad`)

### Stage 3 Implementation Guide: Well-Founded Logic Engine (WFLE)
Detailed 7-phase implementation plan with concrete Racket code, data structures, propagator patterns, and test strategies.
- 1532 lines, 7 phases, ~130 tests planned, 3 appendices
- Phase 1: Descending cells in propagator.rkt (`cell-dirs` registry, `net-new-cell-desc`)
- Phase 2: Bilattice module (`bilattice-var` struct, `lattice-desc`, Boolean/set lattices, three-valued reading)
- Phase 3: WF propagator patterns (positive/negative/unfounded/aggregate, clause/program compilation)
- Phase 4: WF engine orchestration (`wf-solve-goal`, solver dispatch via `current-solver-mode`, `wf-answer` with certainty)
- Phase 5: Three-valued tabling extension (`wf-table-entry` with certainty, `wf-all-mode-merge`)
- Phase 6: Test suite — 30 tests from literature (Van Gelder 1991), comparison, error handling
- Phase 7: Benchmark comparison — wall time, cell count, propagator firings on same programs
- Appendix A: Full end-to-end walkthrough of `p :- not q. q :- not p.` through 8 cells, 8 propagators, 1 BSP round → both `'unknown`
- Appendix B: Interaction analysis (metavars, traits, ATMS, QTT — all orthogonal)
- Appendix C: Evolution path to Approach C (hybrid with alternating scheduler)
- (commit `aca9cb8`)

### Self-Critique and Revision of WFLE Stage 3 Design
Comprehensive self-critique against principles docs (11 findings), then incorporated resolutions:
- Rewrote Phase 4 as hybrid DFS+bilattice architecture (preserves Prolog-style unification, wraps existing solver)
- Split Phase 4 into 4a (core engine) / 4b (tabling integration after Phase 5)
- Added `semantics` key to solver-config for backend dispatch (`'stratified` | `'well-founded`)
- Added `wf-explain-goal` with undeterminacy explanations for unknown atoms
- Fixed O(n²) aggregate upper bound, generalized `bilattice-read`
- Added acceptance `.prologos` file section (Level 3 WS validation gate)
- Appendix B: solver config table, explain path for unknown
- Appendix C: first-class bilattice-vars as future evolution
- (commit `063ca8c`)

### External Critique Refinement of WFLE Stage 3 Design
Addressed 8 areas from external independent review:
- `'defer` semantics: clarified as clause-level skip (NAF failure for this iteration, retried next); noted `current-naf-oracle` parameter must be introduced in Phase 4a
- Bilattice granularity: explicitly stated predicate-level (4a) → per-tabled-variant (4b)
- Fixed undefined `head-clauses`/`clause-effective-bvars`: `wf-compile-clause` now returns body-bvars triple
- Fuel/iteration interaction: documented; `max-iterations` = 2n instead of hardcoded 100
- Consistency propagator: made optional via `#:consistency-check?` keyword
- Tabling merge: `wf-all-mode-merge` rewritten to dedup by answer value, upgrade certainty monotonically
- Phase 7: added iterations-to-convergence metric; fixed stale `current-solver-mode` refs
- Appendix C: added §C.3 incremental re-query as future evolution
- (commit `c0cf366`)

### WFLE Acceptance Test File (Phase 0)
Comprehensive `.prologos` acceptance file covering the full relational language capability envelope:
- 9 sections (A-I): baseline, solver config, WF semantics, explain/provenance, tabling, advanced relational features (is/guard/cut/multi-arity/rel/solve-one/schema), functional interaction, type system, edge cases
- Baseline (Section A) validated: process-file runs with 0 errors on all uncommented expressions
- Discovered QTT multiplicity interaction when binding solve results to `def` — noted for investigation
- Commented sections are acceptance criteria: uncomment as phases complete
- (commit `742e075`)

### WS Syntax Gaps: Solver Config Pipeline (commits `c74357e`, `8f3a336`)
Fixed end-to-end WS pipeline for solver configuration and related relational forms:
- **Solver form WS pipeline**: `solver name :key val` now works from `.prologos` files. Added `$solver-config` sentinel to route around `parse-map-literal` (which treated values as variable references). Added `flatten-ws-kv-pairs` helper in `macros.rkt` to handle WS reader's sub-list wrapping of indented `:key val` pairs.
- **Zonker crash fix**: Guarded `expr-solver-config` handlers in all three zonk functions against `solver-config` structs (which aren't expr nodes).
- **`solve-with` overrides**: Fixed reduction to actually merge override config into base solver config (was ignoring overrides). Fixed elaborator to handle `#f` solver field.
- **`brace-params?` vs `brace-params-stx?`**: Fixed `parse-solve-with` and `parse-explain-with` to use syntax-aware predicate.
- **`solve-one` disambiguation**: Added `surf-solve-one` struct — parser was producing `surf-solve` for both `solve` and `solve-one`.
- **Schema WS indentation**: Applied `flatten-ws-kv-pairs` to both Pass 0 and Pass 2 schema handlers.
- Files modified: `macros.rkt`, `parser.rkt`, `elaborator.rkt`, `reduction.rkt`, `zonk.rkt`, `surface-syntax.rkt`
- Acceptance file sections passing: A (baseline), B1/B3/B4/B5 (solver config), F5 (solve-one), Z (validation)
- (commits `c74357e`, `8f3a336`)

### Documented Pre-Existing Relational Runtime Gaps
Added 5 pre-existing gaps to `DEFERRED.md` under "FL Narrowing — WS Surface Gaps":
- `is`-goals stub (doesn't evaluate functional exprs)
- `guard`/`cut` not in parser keyword list (runtime handlers exist but unreachable from WS)
- Multi-arity `|` solver registration incomplete
- Anonymous `rel` + `solve` integration incomplete
- Source: acceptance file §F STATUS annotations

### Fixed Pre-Existing Relational Runtime Gaps (4 of 5)
Resolved 4 of the 5 documented gaps before starting WFLE implementation:

1. **`is`-goals evaluate functional expressions** (commit `df65974`): Added `current-is-eval-fn`
   callback parameter in `relations.rkt`, set to `nf` in `reduction.rkt` (avoids circular dep).
   Parser now disables relational context for `is` expression arg so `(add ?x ?y)` produces
   `surf-app` not `surf-goal-app`. Added `subst-logic-vars-in-expr`, `rename-logic-vars-in-expr`,
   `collect-logic-vars-in-expr` helpers. Files: relations.rkt, reduction.rkt, parser.rkt, elaborator.rkt.

2. **`guard` wired through parser/elaborator** (commit `a863f20`): Added `surf-guard` struct,
   parser keyword dispatch with 1-arg (condition only) and 2-arg (condition + goal) forms.
   Guard condition parsed with relational fallback disabled. Updated zonk/substitution/reduction/
   pretty-print to handle `#f` goal for 1-arg form.

3. **`cut` wired through parser/elaborator** (commit `a863f20`): Added `surf-cut` struct, 0-arg
   parser keyword dispatch. Runtime handler succeeds but doesn't prune (needs solver-level
   cooperation — true cut semantics deferred).

4. **Anonymous `rel` + `solve` integration** (commit `14c3d2b`): Added `expr-rel->relation-info`
   to convert anonymous rel AST to temporary `relation-info` with gensym name. `run-solve-goal`
   and `run-solve-one-goal` now dispatch on `expr-rel?`, register temp relation, and solve with
   all params as query vars.

Remaining gap: multi-arity `|` zero-arg solve path (low priority, workaround exists).
Acceptance file sections F1, F2, F4 uncommented and passing; F6 (cut) annotated as partial.

### Fixed Stratified-Eval Crash on 1-Arg Guard (commit `ea19b3a`)
`collect-goal-deps` in `stratified-eval.rkt` crashed with `cadr: contract violation` on
1-arg guard (condition only, no inner goal). Fixed by checking `(pair? (cdr gargs))`
before accessing `cadr`.

### Enabled Int Arithmetic in Relational is-Goals and Guard Conditions (commit `78f978e`)
Two fixes to support Int (and other trait-dispatched types) in relational code:
1. **Generic struct walking** in `relations.rkt`: `collect-logic-vars-in-expr`,
   `rename-logic-vars-in-expr`, and `subst-logic-vars-in-expr` now walk into any
   transparent struct via `struct->vector`/`struct-type-make-constructor`. Fixes parser
   keyword nodes (e.g., `expr-int-add`) whose `expr-logic-var` children were invisible
   to the substitution pass, causing `int+` in is-goals to return `unknown`.
2. **Trait-dispatched names work via algebra module**: `plus` (Add), `minus` (Sub),
   `times` (Mul), `ord-gt`/`ord-lt` (Ord), `eq-check` (Eq) from the prelude work in
   is-goals and guard conditions because their `spec` constraints trigger trait dict
   insertion at elaboration time.
- Updated acceptance file F1/F2 with Int variants (F1b, F1c, F2b) alongside Nat (F1a, F2a)
- Level 3 validation passes; all 6907 tests pass (181.8s)
- Marked trait dispatch deferred item as RESOLVED in DEFERRED.md

### Simplified Acceptance File F1/F2 to Use Generic Operators (commit `ca39fd6`)
Updated acceptance file to use generic `+`, `*`, `gt` operators instead of trait-dispatched `add`/`plus`/`ord-gt`. F1a/F1b use `+` in is-goals (Int and Nat), F2a/F2b use `gt` in guards (Int and Nat — exercises different reduction branches: iota for Nat vs concrete pattern-match for Int).

### Added Generic gt, ge, mod Operators (commit `0b30c9d`)
New parser keywords `gt`, `ge`, `mod` added across 10-file AST pipeline (syntax → surface-syntax → parser → elaborator → typing-core → qtt → reduction → substitution → zonk → pretty-print). These dispatch to type-specific implementations at reduction time for Int, Nat, Rat, and Posit types.
- In WS mode, `<` and `>` are angle-bracket delimiters (Pi/Sigma types), so comparison operators use keyword names: `lt`, `le`, `gt`, `ge`, `eq`.
- `mod` supported for Int and Nat; Rat mod skipped (Racket's `remainder` requires integers); Posit skipped (modulo not meaningful for approximate types).
- 27 new tests in `test-generic-arith-03.rkt` covering sexp mode, WS-mode, and relational contexts (is-goals with generic `+`, guards with `gt`).
- Acceptance file updated: F1d/F1e (generic `+` in is-goals), F2c/F2d (generic `gt` in guards). Level 3 validation passes.
- Full suite: 6934 tests, all pass (197.2s).

### WFLE Phase 1: Descending Cells (commit `4a492c7`)
Extended `propagator.rkt` to support descending cells — cells that start at top and refine downward via meet (greatest fixpoint computation). Infrastructure prerequisite for bilattice pairs.
- Added `cell-dirs` field to `prop-network` struct (champ-root registry: cell-id -> 'ascending|'descending)
- `net-new-cell-desc`: creates descending cell using existing merge-fn machinery (meet-fn stored as merge-fn; `net-cell-write` is already generic)
- `net-cell-direction`: queries cell direction, defaults to `'ascending` for backward compatibility
- Key design insight: no changes to `net-cell-write` needed — it calls `merge-fn(old, new)` and checks `merged = old`. For descending cells, merge-fn = meet, so writes that try to raise the value above current are no-ops.
- 15 tests covering creation, direction queries, meet semantics, mixed ascending/descending networks, propagator cross-direction wiring, both schedulers (Gauss-Seidel + BSP), widening coexistence, set-intersection lattice
- Full suite: 6949 tests, 361 files, all pass (199.5s)

### WFLE Phase 2: Bilattice Module (commit `edb3fad`)
New `bilattice.rkt` module — the abstraction layer that pairs ascending and descending cells into bilattice variables with three-valued reading.
- `lattice-desc` struct: bot, top, join, meet, leq — parameterizes the base lattice
- `bool-lattice`: Boolean lattice {false < true} for standard logic programming
- `make-set-lattice`: powerset lattice over a universe (for non-ground extensions)
- `bilattice-var`: pairs lower (ascending, starts bot) + upper (descending, starts top) cells
- `bilattice-new-var`: creates both cells, optional `#:consistency-check?` propagator
- `bilattice-read`: returns `(list 'exact val)` | `(list 'approx lo hi)` | `'contradiction`
- `bilattice-read-bool`: convenience — returns `'true` | `'false` | `'unknown` | `'contradiction`
- `bilattice-lower-write` / `bilattice-upper-write`: monotone writes via underlying cell merge-fns
- `bilattice-add-consistency-propagator`: enforces lower <= upper, sets contradiction if violated
- Design choice: consistency propagator is optional (`#f` by default for Boolean lattice where the invariant is maintained by construction; `#t` for non-Boolean or testing)
- 20 tests covering lattice descriptors, variable construction, three-valued reading, monotonicity, consistency propagator, multi-var coexistence, propagator wiring between vars, both schedulers
- Full suite: 6969 tests, 362 files, all pass (195.2s)

### WFLE Phase 3: Well-Founded Propagator Patterns (commit `fc83451`)
New `wf-propagators.rkt` — the "wiring diagrams" that translate logic program clauses into bilattice cell updates for well-founded semantics computation.
- `wf-wire-fact`: sets lower to true immediately (ground facts are unconditionally true)
- `wf-wire-negation`: creates derived bilattice-var for `not q` — lower-neg-q = not(upper-q), upper-neg-q = not(lower-q). The cross-wire flip is the key insight that makes negation monotone in precision ordering.
- `wf-wire-positive-clause`: lower-head := lower-head OR (AND of body lowers). Only fires lower bound; upper handled by aggregate.
- `wf-wire-aggregate-upper`: upper-head := OR over all clauses of (clause body feasibility). "Head is possibly true iff at least one clause could prove it."
- `wf-compile-clause`: handles mixed positive/negative bodies, wires negation for neg literals, returns body-upper-fn + effective body bvars for precise dependency tracking
- `wf-compile-program`: end-to-end compilation — creates bilattice-vars for all atoms, compiles all clauses, wires aggregate upper bounds, detects unfounded atoms (appear only in bodies, never as heads) and forces their upper to false
- 25 tests covering: individual patterns, clause compilation, classic WF models (odd cycle → unknown, stratifiable chain, win/lose game, even/odd, Nixon diamond → both unknown, self-reference, long negation chain, BSP convergence)
- Full suite: 6995 tests, 363 files, all pass (193.7s)

### WFLE Phase 4a: Well-Founded Engine Core (commit `333130a`)
New `wf-engine.rkt` (~350 lines) — the orchestration layer that wraps the existing DFS solver with bilattice cells to compute three-valued well-founded semantics.

**Architecture: Hybrid DFS + Bilattice**
- Reuses the existing DFS solver (`solve-goal`) as-is — no fork or rewrite
- Adds a bilattice layer on top: one bilattice-var per predicate name (Phase 4a uses predicate-level granularity)
- NAF oracle (`current-naf-oracle` parameter in relations.rkt) intercepts negation-as-failure: returns `'succeed` (definitely false), `'fail` (definitely true), or `'defer` (unknown → skip clause conservatively)
- Iterative fixpoint: probe predicates → update bilattice bounds → check stability → repeat until convergence. Max iterations = max(10, 2n) for n negation-participating predicates.

**Key design decisions:**
- `current-naf-oracle` is a parameter, not a field: clean separation — oracle is optional, standard NAF path completely unchanged when oracle is `#f`
- Defer tracking via `current-naf-deferred?` parameter: distinguishes conclusive "no proofs" (upper := #f) from inconclusive "NAF deferred" (leave as unknown). Without this, stratifiable negation like `a :- not b. b :- not c. c.` would incorrectly mark `a` as definitely false in early iterations when NAF for `b` defers.
- `wf-relation-info->dep-info` inlined to break circular dependency (wf-engine.rkt ↔ stratified-eval.rkt). Alternative was restructuring module hierarchy, but inlining is simpler for a small function.
- Solver dispatch via `solver-config-semantics` key: `'stratified` (default) or `'well-founded`. Added to `solver.rkt` valid keys/defaults/accessor.

**Components:**
- `wf-solve-goal`: top-level entry — transitive pred closure, negation identification, bilattice creation, iterative fixpoint, answer annotation
- `wf-iterate`: fixpoint loop with stability check
- `make-wf-naf-oracle`: creates oracle from bilattice state, tracks defer via side-effect parameter
- `probe-single-predicate`: facts → lower := #t, no clauses → upper := #f, clauses → DFS with oracle
- `annotate-answers`: tags DFS answers with 'definite or 'unknown based on final bilattice state
- `wf-answers->standard`: converts to standard hasheq format ('strict filters unknowns, 'all adds certainty tag)
- `wf-explain-goal`: explanation with undeterminacy cycle identification via Tarjan SCC

**Solver dispatch wired in `stratified-eval.rkt`:**
- `stratified-solve-goal` checks `solver-config-semantics` — 'well-founded dispatches to `wf-solve-goal` + `wf-answers->standard`, else original stratified path
- `stratified-explain-goal` similarly dispatches

**Tests (24):** NAF oracle (4), bilattice stability (2), transitive closure (2), preds-with-negation (1), answer conversion (2), solver config (2), WF solve basic queries (3), WF-specific (odd cycle, self-reference, mixed definite+unknown) (3), solver dispatch (3), edge cases (2)
- E2E tests through `:with solver-config` syntax deferred to Phase 6 (WS syntax not wired yet)
- Full suite: 7019 tests, 364 files, 204.4s, all pass

**Lessons:**
- Circular dependency is the #1 gotcha when adding a new module that both provides to and requires from existing modules. Plan the dependency direction early.
- The `replace_all` edit mode can double-prefix names: renaming `relation-info->dep-info` to `wf-relation-info->dep-info` also renamed already-prefixed occurrences to `wf-wf-relation-info->dep-info`. Use targeted edits instead.
- Upper bound conservatism matters: without defer tracking, the fixpoint converges to the wrong answer for programs where negation is stratifiable but the iteration hasn't stabilized yet.

### WFLE Phase 5: Three-Valued Tabling Extension (commit `9bc99fc`)
Extended `tabling.rkt` with three-valued table entries for the well-founded engine.

**What was added (~80 lines new code):**
- `wf-table-entry`: extends `table-entry` with a `certainty` field (`'definite` | `'unknown`)
- `wf-all-mode-merge`: merge function that deduplicates by answer value, preferring `'definite` over `'unknown` when the same answer appears with different certainties — monotonic in the certainty ordering
- `wf-table-register`: creates a table with `wf-all-mode-merge` and initial certainty `'unknown`
- `wf-table-add`: adds `(answer . certainty)` pair to a three-valued table
- `wf-table-answers`: reads `(listof (cons answer certainty))`
- `wf-table-complete`: marks table as complete with a final certainty
- `wf-table-certainty`: reads the table-level certainty tag

**Design choices:**
- Subclassing via `struct ... table-entry`: `wf-table-entry` extends `table-entry`, so all existing `table-entry` accessors (name, cell-id, answer-mode, status) work on WF entries. Existing code that uses `table-entry?` also matches WF entries — no breakage.
- Cell stores `(cons answer certainty)` pairs, not bare answers: the merge function keys on `car` (answer) for dedup, and resolves certainty via subsumption (`'definite` > `'unknown`). This is cleaner than a separate certainty registry.
- WF tables coexist with regular tables in the same `table-store` — no parallel infrastructure needed.

**Tests (16):** register (2), add/answers (3), merge (4), complete/certainty (4), integration with propagator network (3)
- Full suite: 7035 tests, 365 files, 190.5s, all pass

### WFLE Phase 4b: Engine + Tabling Integration (commit `7281c1a`)
Connected the WF engine's bilattice fixpoint with three-valued tabling — after convergence, answers are stored in WF tables with per-predicate certainty.

**What was added:**
- `wf-solve-goal-tabled`: wraps `wf-solve-goal` to populate WF tables after fixpoint. For each negation-participating predicate, registers a WF table, stores answers with certainty derived from the bilattice (`'definite` if bilattice reads true/false, `'unknown` if bilattice reads unknown), then marks the table complete.
- `current-wf-table-store` parameter: holds the WF table store during tabled evaluation
- `stratified-solve-goal` now parameterizes `current-wf-table-store` and uses `wf-solve-goal-tabled` for the `'well-founded` path

**Design choices:**
- Per-predicate certainty (not per-answer): the bilattice operates at predicate-level granularity, so all answers for a predicate inherit the same certainty. Per-variant NAF refinement would require changing the oracle interface to accept ground arguments, which is a future enhancement.
- No-negation fast path also stores in WF tables: even when the fast path is taken (no negation-participating predicates), answers are stored with `'definite` certainty for API consistency.
- Table store is a parameter, not a return value: allows nested calls and composability without changing the `wf-solve-goal` return signature.

**Tests (5):** fact→definite (1), odd cycle→unknown (1), stratifiable negation→definite (1), dispatch via stratified-solve-goal (1), no-negation fast path (1)
- Full suite: 7040 tests, 365 files, 188.4s, all pass

### WFLE Phase 6: Test Suite — Known Well-Founded Models (commit `9a03bb9`)
Comprehensive test suite validating WFLE against published well-founded semantics examples and comparing with the stratified engine.

**Three test files (30 tests total):**
- `test-wf-literature-01.rkt` (13 tests): canonical odd cycles (2-atom, 3-atom), stratifiable chains (a:- not b. b:- not c. c.), self-reference (p:- not p.), unfounded atoms (mutual positive recursion), Clark completion (multiple clauses), disjunctive-like patterns, mixed definite/unknown coexistence, even positive cycle, long negation chain (4 strata), default reasoning (bird/penguin), multiple support paths, engine agreement on stratifiable programs
- `test-wf-comparison-01.rkt` (10 tests): stratified vs WF engine comparison — pure positive programs, rule chains, multi-strata, undefined negation targets (WF handles via closed world, stratified errors), empty stores, answer format consistency, default config
- `test-wf-errors-01.rkt` (7 tests): empty store, missing relation, negation of missing (closed world assumption), config creation, 10-predicate chain convergence

**Key finding from comparison tests:**
The WF engine is strictly more robust than the stratified engine for edge cases — it handles undefined relations and empty stores gracefully (via closed-world assumption and `with-handlers`), while the stratified engine raises `solve: Unknown relation` errors. This is a practical advantage beyond the theoretical gain of handling negation cycles.

- Full suite: 7069 tests, 368 files, 188.9s, all pass

## In Progress

- WFLE Phase 7: Benchmark Comparison

## Considerations

- Track 3 (Cell-Primary Registries) has a completed design doc (`30f02fb`) — ready to implement
- Several .prologos example files have uncommitted changes — review and decide whether to commit
- Remaining propagator work:
  - Track 3: Cell-Primary Registries (designed, ready)
  - Phase 3e: Reduction cache cells (deferred)
  - Phase 5a-5b: Driver simplification (not started)
  - P5: QTT Multiplicity Cells (architectural)
  - P1-G: Pure Unification as Propagators (high risk, 123 call sites)
- WFLE implementation: Phases 1-4a complete; Phase 5 (three-valued tabling) next
- Acceptance file sections C-E remain commented until WFLE phases land; sections G-H untested
- Generic operators now available: `+`, `-`, `*`, `/`, `lt`, `le`, `gt`, `ge`, `eq`, `mod`, `negate`, `abs`. These are the recommended interface for numeric operations in relational code. Bare `add`/`gt?` still resolve to Nat-specific primitives; `plus`/`ord-gt` work via trait dispatch.

## Lessons Learned

- **WS reader sub-list wrapping**: The WS reader wraps each indented continuation line as a sub-list. For keyword-value forms like `solver name :key val`, this produces `(solver name (:key val))` instead of flat `(solver name :key val)`. Need `flatten-ws-kv-pairs` to selectively flatten only `:keyword`-headed sub-lists (initial version was too aggressive and broke compound types like `(List Nat)` in schema tests).
- **`$brace-params` is wrong for solver config**: `{:key val}` in WS → `$brace-params` → `parse-map-literal` → treats values as expressions (variable references). Solver config values are symbols, not expressions. Need a dedicated sentinel (`$solver-config`) that routes to elaborator code that builds `expr-solver-config` with symbol values directly.
- **Syntax-aware predicates**: Parser args are syntax objects, not raw datums. `brace-params?` checks `(pair? d)` which is `#f` for syntax objects — must use `brace-params-stx?` which unwraps first.
- **Circular dependency via callback**: `relations.rkt` can't import `whnf`/`nf` from `reduction.rkt` (circular). Solution: `current-is-eval-fn` parameter set via `parameterize` in reduction.rkt.
- **Parser context sensitivity for `is` expression**: `parse-datum` in relational context produces `surf-goal-app` for `(add ?x ?y)` — must `parameterize ([current-parsing-relational-goal? #f])` when parsing `is` expression arg.
- **`#f` goal propagation**: When `expr-guard` has `#f` goal (1-arg form), ALL pipeline stages that traverse it (zonk×3, substitution×2, reduction, pretty-print) must guard against it. Easy to miss one.
- **Descending cells need no net-cell-write changes**: The existing `merge-fn(old, new)` + `merged = old` check is direction-agnostic. For ascending cells merge-fn = join (raises); for descending cells merge-fn = meet (lowers). Only the constructor and direction registry are new.
- **Stale .zo cache on struct field addition**: Adding `cell-dirs` to `prop-network` requires recompiling ALL transitive dependents, not just `driver.rkt`. Modules like `session-propagators.rkt` and `trace-serialize.rkt` that import `prop-network` by struct linklet fail with "expected 12 fields" if not recompiled.
- **`andmap` + `member` returns truthy, not `#t`**: Racket's `andmap` returns the last truthy value, and `member` returns the tail of the list. Use `(and (andmap ...) #t)` to coerce to proper boolean for `check-true`.

## Metrics

- Test count: 6995 (363 files, 2 skipped)
- Performance baseline: 193.7s (post-WFLE Phase 3)

---

## Notes

New working-day interval begins. Prior interval was documentation and planning focused — master roadmap, Track 3 design, research papers, principle documents. Ready to return to implementation.
