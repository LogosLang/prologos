# Dailies — 2026-03-30

## Carried Forward from Previous Interval

### PPN Track 2: Surface Normalization ✅ COMPLETE
- Phase 10 delivered: reader.rkt DELETED (1898 lines). Switchover live.
- ~2700 lines new code (surface-rewrite.rkt + tree-parser.rkt + integration)
- ~2350 lines deleted (reader.rkt + test-reader.rkt). Net: ~+350 lines.
- 57 module tests. 7454 tests, 382 files, ALL PASS (125.3s).
- Phase 11 (macros.rkt expander retirement, ~1000 lines) deferred to Track 3 prerequisite.
- PIR written + addendum for Phase 8-10.

### Priority Ordering (from 2026-03-28)
1. ~~**PPN Track 2**~~ ✅ COMPLETE
2. SRE Track 2B (optional) — polarity inference
3. PPN Track 3 — parser on network (includes Phase 11 prerequisite)
4. SRE Track 2G — algebraic domain awareness
5. PPN Track 4 / SRE Track 2C — elaboration fusion + cell refs

### Known Gaps / Blockers
- test-structural-decomp: meta-solving via imperative state under BSP
- WS-mode solver syntax: `=` unification, `is` bridging, arithmetic
- SRE Track 2C: cell refs (zonk elimination prerequisite)
- Phase 11: macros.rkt sexp expanders (~1000 lines) — blocked on tree parser for all form types

### Deferred Work
- PAR Track 2 R3-R5: closed (answered by implementation)
- PM Track 10C Phases 2-3: per-test splitting (Phase 1 captured core value)
- PPN Track 5: bilattice (elimination ordering)
- Batch worker simplification (needs registries as cells)

### Watching (medium-term patterns)
- **"Architecture ahead of implementation"** — 2 instances (PTF Track 1, SRE Track 2F). Watching for 3rd.
- **"Propagator density determines parallel benefit"** — 1 instance (PAR Track 2 R1: 18 BSP rounds). PPN migration will increase density.
- **"Pocket Universe principle"** — 1 confirmed application (PPN Track 2 parse pipeline). Needs 2nd application to generalize.
- **"Parsing is not special"** — 2 instances (type inference + parsing share lattice+propagator+bridge). Needs 3rd system.
- **"Pipeline-as-cell"** — 1 instance (PPN Track 2). Promising pattern for staged transformation pipelines.

---

## Lessons Distillation Check

### Scanned: PPN Track 2 PIR (incl. addendum) + March 26-29 dailies

**Ready for codification (5 patterns with 3+ instances):**

1. **"Correctness infrastructure before parallelism"** — 3 instances confirmed (PAR T1: 28:1 ratio, PPN T1: 9 design iterations → 0 bugs, SRE T0: 2:1 → 0 bugs). Flagged READY since Mar 28. **→ DESIGN_PRINCIPLES.org**
2. **"Merge/hybrid over wholesale replacement"** — 5 instances (PM T10, PM T8, PPN T2, PAR T1, PM T10B). PIR calls approaching codification. **→ DEVELOPMENT_LESSONS.org**
3. **"Integration phases find all the bugs"** — 8 of 11 PIRs. Infrastructure fast+clean, wiring finds bugs. **→ DEVELOPMENT_LESSONS.org or DESIGN_METHODOLOGY.org** (planning heuristic)
4. **"Wrong assumptions at boundaries"** — 9 of 11 PIRs. Most persistent. Stage 2 audits partially address. **→ DESIGN_METHODOLOGY.org** (explicit audit step)
5. **"Vision contaminates audit"** — 6 instances. Flagged READY since Mar 26. **→ DESIGN_METHODOLOGY.org** (audit discipline: concrete code measurements, not estimates)

**Already codified:**
- CALM requires fixed topology (DEVELOPMENT_LESSONS.org)
- Diagnostic protocol (workflow.md)
- Pre-0 benchmarks shape design (DESIGN_METHODOLOGY.org)
- Two-lens self-critique (DESIGN_METHODOLOGY.org)
- Design:implementation ratio ≥1.7:1 (MEMORY.md — partially, needs formal codification)

---

## Today's Work

### Completed

- **Process adjustment** (`c4224b88`) — Two new meta-lessons codified from PPN Track 2 PIR analysis:
  1. "Validated Is Not Deployed" — DEVELOPMENT_LESSONS.org.
  2. "'Pragmatic' Is a Rationalization for Incomplete" — DEVELOPMENT_LESSONS.org.
  - workflow.md: Extended red-flag phrases + Validated≠Deployed gate + pragmatic-ban rules.
- **PPN Track 2B Design** — Full Stage 3 design cycle:
  - D.1 (`7f3390d6`): 6-phase design for production deployment of tree parser
  - D.1→D.1b (`588ec09a`): Phase 11 incorporated (decouple registration from expansion). Gap traceability across PPN Master, SRE Master, DEFERRED.md. V(1)/V(2) placement documented.
  - D.2 (`95885973`): Self-critique — 5 findings. P5 HIGH: cannot delete sexp expanders (sexp path needs them). P2 MEDIUM: parameterize, not duplicate.
  - D.3 (`c006ed96`, `91bb33b8`): External critique — 10 findings, 3 HIGH. F1: tree parser error stubs needed. F2/F7: 4-way form classification for Phase 5. F4: impl-generated ordering must match Pass 5b. F10: merge is throwaway bridge — propagator cells replace it in Track 3-4.
- **Research note** (`4f019ddd`): "S-Expression IR to Propagator Compiler" — 3 sexp roles, self-hosting trajectory (Racket → propagator compiler → LLVM → Logos). LANGUAGE_VISION.org updated. SH Series placeholder in Master Roadmap.

### In Progress

- **PPN Track 2B Implementation** — Phases 0, 0.5, 1 complete. Phase 2+3 revealed AST parity gap.
  - Phase 0 (`3457c6ca`): acceptance file, 0 errors baseline
  - Phase 0.5 (`9a8f9158`): 15 error stubs for preparse-consumed forms (D.3 F1)
  - Phase 1 (`67df701f`): merge function extracted, zero behavioral change
  - Phase 2+3 (`8a263d89`, `b7809237`): **CRITICAL FINDING** — tree parser output NOT elaboration-equivalent to preparse. 3 merge policies tested, each producing failures. Root: `parse-expr-tree` and `parse-defn-tree` produce different surf-* shapes than preparse for complex forms (inline types, match, list exprs). load-module can't use merge (recursive). Deployed: validation-only mode (tree parser runs, preparse provides ALL output).
  - Phases 4-5: BLOCKED until AST parity achieved
  - tag-check/tag-infer missing from T(0) — fixed
  - §12 designed: propagator-only mixfix (Pocket Universe claim lattice), pipe/compose rewrites, expression keywords, defn variants. 3 critique rounds (D.2c, D.3b, revision).
  - Phase A+B (`3c1bb200`): pipe + compose rewrite rules in surface-rewrite.rkt. Disambiguator merges `>>` for tree pipeline. 45/45 test-pipe-compose, 108/108 test-parse-reader.
  - Phase A+B (`3c1bb200`): pipe + compose rewrite rules. `>>` disambiguator merge for tree pipeline. Tag rules for raw lexemes.
  - Phase E partial (`8ff16dca`): defn Pi chain fix (inline type → full Pi). Per-form merge attempted → 11 failures including regressions.
  - Merge reverted to validation-only (`a49c4eef`). Root cause: AST parity gap wider than anticipated. Tree parser produces non-error surf-* for forms it handles INCORRECTLY (false positives). Known gaps: `>>` inside brackets consumed by G(0) as rangle, dot-access in expressions, implicit-map syntax, higher-rank types, relational keywords, multi-arity pattern defn.
  - Domain audit complete: 3 root cause categories identified (expression sentinels, expression keywords, complex defn variants). Queue-based merge was fundamental flaw — replaced with source-line-keyed identity matching.
  - Phase F (`ebb3b290`): **MERGE DEPLOYED** with source-line-keyed matching. Per-form lattice join (merge-form). 7459 tests, ALL PASS.
  - Phase G (`6f599054`): **use-tree-parser? DELETED**. process-string-ws-inner collapsed to merge-only path.
  - §12.6 merge architecture analysis: lattice join (permanent), identity matching (scaffolding → shared cells Track 3), scheduling (scaffolding → propagator firing Track 3).
  - Phase C prereq (`38383e3b`): G(0) mixfix context fix — `<` `>` preserved as operators inside `.{...}`.
  - Phase C (`8574079a`): **Mixfix Pocket Universe IMPLEMENTED**. DAG-stratified claim lattice. Position-aware merge (commutative/associative/idempotent). Dataflow fold from tightest to loosest. Binary operators, swap? flag, right-associative chains, multi-token operands.
  - Phase C.1+C.2 (`eba3aad7`): comparison chaining pre-pass + unary minus detection. Chain expansion as separate concern (Decomplection). Unary handles both spaced (`- x`) and merged (`-x`).
  - **Track 2B COMPLETE.** Phase 5 shelved (optimization of soon-to-be-replaced infrastructure).
  - 382/382 GREEN, 7454 tests, 132.1s

### Lessons

- **"Validated on test path" ≠ "validated on production path"** — Track 2 Phase 6g validated merge on process-string-ws. Track 2B found it fails on process-file (more complex forms). The Validated≠Deployed lesson applies recursively.
- **Diagnostic protocol prevented thrashing** — 3 merge policies tested systematically. Each narrowed the problem. Validation-only is the principally correct stopping point.
- **Resist algorithmic thinking: look for the lattice** — Three failed merge policies used algorithms (queues, positions). Source-line-keyed identity matching (information-flow) succeeded immediately.
- **Propagator Design Mindspace codified** — DESIGN_METHODOLOGY.org §Stage 3: four questions (information, lattice, identity, emergence), red flags table, Pocket Universe as design tool, universal substrate perspective.
- **7 topology design patterns captured in PTF Master** — set-latch/functorial map, pocket universe, threshold/gather, queue/work-set, structural merge/PUnify, claim lattice, Kan extensions.

### Track 2B Final Accounting
- New code: ~600 lines (surface-rewrite.rkt +450, tree-parser.rkt +40, driver.rkt +60, parse-reader.rkt +50)
- Deleted: ~100 lines (driver.rkt old paths)
- 18 commits. 10 design iterations. D:I ratio ~1:1.
- PIR written: `2026-03-30_PPN_TRACK2B_PIR.md`

### Starting: SRE Track 2G (Algebraic Domain Awareness)
- Research context refreshed: Algebraic Embeddings, Module Theory, SRE Track 2F foundations
- Design direction discussed: property-based (not class hierarchy) for decomplection and composability
- Property cells on sre-domain with implication propagators between properties
- Declaration as fast path, structural inference as fallback
- PPN Track 3 consumer needs identified: token lattice (Boolean), type lattice (Heyting), surface lattice (Distributive)
- Stage 2 audit complete (`6c32d723`): 2 domains, 5 relations, 28 ctors, no meet, no domain properties, hardcoded variance-map.
- D.1 design (`e81185a8`): property cells, domain registry, ring action meet, inference Pocket Universe, implication scatter, Heyting consumer. 8 phases.
- D.2 self-critique (`6682436d`): P1→cells on network, P2→ring-action function, M3→Pocket Universe inference.
- D.3 external critique (`ec61c7ef`): 11 findings. 4-valued lattice, evidence with witnesses, context-dependent equality, meta meet=⊥, domain registry scoped in.
- NTT model (`fad8da5e`): 7 sections, everything on-network. Revealed architectural impurity: Phase 6 scatter → Pocket Universe internal stratification (`5f08e321`).
- **Implementation started**:
  - Phase 1 (`baa0fde6`): property-cell-ids field on sre-domain, 4-valued property lattice, has-property? API. 9 construction sites updated.
  - Phase 1.5 (`191d0933`): domain registry. register-domain!, lookup-domain. type + session domains registered.
  - Phase 2 (`9737625d`): type-lattice-meet. Ring action: contra→join, co→meet, inv→eq-meet. Pi + Sigma + base types. Meta→⊥.
  - Phase 3: session-lattice-meet in session-lattice.rkt. Ground sessions (same shape → id, different → ⊥).
  - Phase 4: declared-properties field on sre-domain (11th). Type domain: 6 atomic properties (commutative-join, associative-join, idempotent-join, has-meet, distributive, has-pseudo-complement). Session domain: 4 properties. has-property? reads declarations with cell fallback.
  - Phases 5-8 remaining.

---

## Considerations

(To be updated as work progresses.)
