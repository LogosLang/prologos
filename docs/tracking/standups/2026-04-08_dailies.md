# Dailies — 2026-04-08

## Carried Forward from Previous Interval

### BSP-LE Track 2 — DESIGN COMPLETE (D.3)
- Full design cycle: Stage 1/2 audit → D.1 → self-critique (17) → D.2 → external critique (16) → D.3
- 1157-line design document. 33 total critique findings incorporated.
- Architecture: decisions-primary, ATMS dissolved, per-nogood propagators, parallel-map pattern, propagator-native search, tropical hitting set
- Pre-0 benchmarks captured (21 micro + 4 adversarial baselines)
- Ready for Phase 0d (acceptance file) then implementation

### Process Improvements Codified
- CRITIQUE_METHODOLOGY.org: P/R/M lenses + SRE lattice lens + grounded pushback
- DESIGN_PRINCIPLES.org: Hyperlattice Conjecture
- DESIGN_METHODOLOGY.org: conversational cadence, three-lens critique
- workflow.md: SRE lattice lens mandatory, per-phase test gate

### PPN Track 4B ✅ COMPLETE
- 90% on-network, 391/391 files, 7609 tests, 134.1s
- PIR written (21 sections)

### Known Gaps / Outstanding
- Batch-worker isolation (DEFERRED, medium severity)
- 4 lessons pending distillation to principles documents
- Usage tracking test coverage (Phase 4)

### Watching
- **Conversational cadence**: Track 4B validated (90% on-arch). Now codified. Monitor next track.
- **Per-phase test gate**: Codified but Track 4B's Phase T was still end-of-track. First real test: BSP-LE Track 2.
- **SRE lattice lens**: New practice. BSP-LE Track 2 was the origin case. Monitor for gaps in future designs.
- **Per-nogood propagator scaling**: Theoretical O(|ng|) per nogood. Need implementation data on propagator count at 100+ nogoods.

---

## Lessons Distillation Check

### From BSP-LE Track 2 design cycle
- SRE lattice lens catches compositional errors per-lattice analysis misses → **codified** in CRITIQUE_METHODOLOGY.org
- Decisions-primary reframes worldview as emergent → **captured** in design, pending codification as pattern
- Hyperlattice Conjecture: every computation → fixpoint on lattices → **codified** in DESIGN_PRINCIPLES.org
- Per-nogood propagators more principled than per-decision watchers → **captured** in design (4.1)
- Grounded pushback on external critique → **codified** in CRITIQUE_METHODOLOGY.org §5
- Separate critique files (not inline in design) → **codified** in CRITIQUE_METHODOLOGY.org

### Status: 4 lessons from Track 4B still pending distillation. 6 new patterns from Track 2 design captured.

---

## Session Log

### Phase 1: Decision cell infrastructure + parallel-map propagator (commit `4df2a4d8`)

New module `decision-cell.rkt` — pure leaf, no project dependencies. Follows `constraint-cell.rkt` convention (Critical 1.1): bot = all alternatives viable, top = empty = contradiction, merge = set-intersection (narrowing). Decision domain, nogood lattice, assumptions accumulator, counter — all as pure lattice operations.

`net-add-parallel-map-propagator` in `propagator.rkt` — installs N independent propagators that fire in one BSP superstep. Results accumulate to shared output cell. Polynomial functor pattern.

30 tests in `test-decision-cell.rkt` covering all lattice ops + multi-write BSP verification (3.1 gate). 392/392 files, 7639 tests, 142.3s.

SRE domain registration deferred to Phase 5 — needs solver context for merge-registry (per-relation merge functions).

**Observation**: `check-true` in rackunit requires exactly `#t`, not just truthy. `member` returns list tail (truthy but not `#t`). Use `check-not-false` for membership tests.

### Phase 1Bi: A/B benchmark — N-propagator vs broadcast (commit `a50fc138`)

Broadcast is 2.3× faster at N=3, scaling to 75.6× at N=100. N-propagator: linear overhead. Broadcast: constant. Data captured in design §Phase 1.

### Phase 1Bii: Broadcast propagator + scheduler profile (commit `fb0650a3`)

`propagator` struct extended with `broadcast-profile` field (#f default). `broadcast-profile` struct: items, item-fn, merge-fn — metadata for scheduler decomposition. `net-add-broadcast-propagator` installs ONE propagator processing N items. 5 new tests (35 total). 4 stale .zo failures from struct change resolved by recompile.

### Phase 2: Assumption-tagged dependents + branch PU (commit `0a78069a`)

`dependent-entry` struct: paths + assumption-id. `filter-dependents-by-paths` checks viability via `current-assumption-viable?`. Inert dependents skipped — branches die by information flow. `make-branch-pu` = fork-prop-network + assumption identity. `perf-inc-inert-dependent-skip!` counter for Phase 11 checkpoint. 7 tests in `test-branch-pu.rkt`. 393/393, 7651 tests.

perf-counters struct gained field → stale .zo in 6 test files. 4 resolved by recompile, 2 needed constructor arity update (test-perf-counters, test-properties).

### Phase 3: Per-nogood infrastructure (commit `a38baefb`)

Commitment cell as structural unification of nogood pattern against decision state. Cell value IS the provenance (no gathering). Broadcast commit-tracker writes assumption-ids to component positions. Narrower fires at N-1 threshold. Contradiction detector writes provenance to nogoods cell. Topology handler for `nogood-install-request` (data-driven, not callback). 9 tests. 394/394, 7660 tests.

Key insight during implementation: commitment cell must NOT use the cell-level `contradicts?` parameter — network-level contradiction halts all propagation. The contradiction detector PROPAGATOR handles the logic instead.

Mini-design conversation resolved: SRE lattice lens on commitment cell (structural, not value). No committed-count field — narrower reads positions directly. Cell value carries assumption-ids not Bools (provenance is structural). Topology trigger via data descriptors with set-union dedup (no last-seen cell, no callbacks, no mutable closure).

### Design triage: broadcast pattern + component-path invariant

D.4 → D.4d across 4 commits: broadcast propagator replaces N-propagator throughout design. Full deployment audit (§6b): 5 Track 2 sites + 4 future tracks. NTT invariant §3.1a: compound cell access requires component-paths (type error without). PU component-path audit: BranchPU interface + all Phase descriptions verified. per-nogood-watcher superseded by commitment-cell decomposition (avoids thrashing).
