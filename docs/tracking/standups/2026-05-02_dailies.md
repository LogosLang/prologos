# Dailies — 2026-05-02

## Carried forward (from 2026-04-30 dailies — interval ran 2026-04-30 → 2026-05-02 morning, full)

The previous dailies ran ~301 lines covering 3 calendar days and absorbed
the 2026-05-02 morning concurrency-primitives research arc as a trailing
section. Opening a fresh interval as of mid-day 2026-05-02 because session
work is shifting from research to roadmap maintenance + queued
implementation.

### SRE Track 2I — Phase 4 ready, Phase 3 close gated

- **Status**: Phases 1, 2, 2a, 3c ✅ committed. Phase 3 sweep
  infrastructure landed (`bd1179b1`) but surfaced a sweep-semantic bug:
  `test-distributive` and `test-sd-{vee,wedge}/detailed` in `sre-core.rkt`
  hardcode `'equality` for the join, mixing lattices when meet-fn comes
  from a non-equality relation. **Phase 4 mini-design persisted**
  (`cd93a7b9`): refactor 5 functions in `sre-core.rkt` + ~25 callsites
  across 4 files to take explicit `join-fn` parameter. Mirrors Phase 3c's
  `meet-fn` pattern. Atomic per pipeline.md co-migration discipline.
- **After Phase 4**: re-run sweep; expected all 6 findings confirmed; if
  type×subtype STILL refutes → escalate.
- **Phase 3 close immediately follows Phase 4 close**.
- **Pattern naming**: "Scaffolding-Hides-Truth" — chosen 2026-04-30
  dialogue. 3 data points after Phase 4 lands (Phase 2a, 3c, 4) →
  graduate to `DEVELOPMENT_LESSONS.org`.
- **Phase 3.5** (deferred): pseudo-complement empirical check.
- **Phase 3b** (deferred): generator extension for session×equality and
  form×equality sweeps.
- **Phase T** (after 3, 3.5, 3b land): dedicated `test-sre-sd-properties.rkt`.

### PReduce Series — Track 0 sub-deliverables outlined

- **Status**: Master + roadmap update committed (`2103b11f`). Track 0 🔄
  series founding research synthesis. Three sub-deliverables outlined
  (0.1 architectural sketch, 0.2 rule-property taxonomy, 0.3 `.pnet`
  extension + LLVM lowering interface). Implementation tracks 1-9 ⬜
  placeholder rows.
- **Architecture lock-ins** (per master, refined via adversarial round):
  per-AST-PU compound regions (NOT per-node PUs); shared e-class cells
  with refinement-poset structure; unified rule registry generalizing
  SRE form-registry pattern; two strata only (S0 rewriting + S(-1)
  retraction); rule properties as scheduler dispatch tags.
- **Gating**: implementation Tracks 1+ gate on PPN 4C Phase 1B
  (tropical-fuel substrate) + Track 0.1 closure. Track 0 design work
  proceeds in parallel with PPN 4C Phase 1B.
- **Track 0.3 framing**: our canonical format; collaborator's LLVM
  prototype is one consumer voice, rebases to whatever lands on `main`.
- **User-facility forward-compat (Track 9)**: design-only, not shipped.
  Architecture must not preclude eventual user-defined rewrite rules.

### Master Roadmap staleness sweep — in flight today

- **Status**: Audit complete; 3-commit fix plan agreed.
- **Commit 1 — series-header staleness**: PM/BSP-LE/SRE/PPN status lines;
  add missing tracks (BSP-LE 2 + 2B, SRE 2D/2G/2H/2I + 2F note, PPN 4D);
  PPN table header "(Proposed)" → "Tracks"; refresh priority ordering.
- **Commit 2 — missing top-level series**: add SH (Self-Hosting) as
  top-level section; promote UCS from research subsection to top-level;
  update title-page metadata.
- **Commit 3 — naming + research-table sweep**: rename "PReductions" →
  "PReduce" globally; add 8 newer research docs to Active Research;
  mark NTT Doc 1 ✅; update Cross-Series Dependency Map.

### PPN 4C Addendum Phase 1C — ready

D.2 hybrid pivot architecture committed; D.2 self-critique drafted with
18 findings (3 BLOCKING resolved). Pre-0 phase 100% complete. Phase 1C
implementation can open when SRE Track 2I closes.

### Concurrency primitives research arc — landed (2026-05-02 morning)

`docs/research/2026-05-02_CONCURRENCY_PRIMITIVES_LLVM_SUBSTRATE.md` filed
(`9dc3e7eb`). Sprint D recommendation captured. Three calibration
measurements on the current sequential prototype (~2 hours work) named
as the next move that anchors primitive choices empirically. Sprint D
design doc on the prototype branch is collaborator's call; note is
informational input.

### Capability-as-Datalog correction — landed (2026-05-02 morning)

Vocabulary conflation between Spera's "capability" sense and OCap sense
corrected in §1, §3.1, §8 of the 2026-04-23 research note. Math
unchanged.

### Issues filed (still open)

- **Issue #40**: codebase-wide `with-handlers` audit (72 remaining
  instances post-Phase-2a). A/B/C/D categorization framework. SRE-adjacent
  findings preserved as starting point.
- **Issue #41**: `.prologos` acceptance-file CI gap. Two solver-output
  regressions (2026-04-30) caught by user acceptance-file inspection;
  Layer-1 CI would have caught both.

### Cross-track sister concerns filed

- **PM Master Track 12**: `current-lattice-meta-solution-fn` retirement
  (sister callback to the subtype one Phase 3c retired). Phase 3c
  retirement template documented for Track 12 to adopt.
- **DEFERRED.md**: cross-reference for visibility.

---

## Watching (medium-term patterns; promote to principles when threshold met)

Carried forward from 2026-04-30 — promotion gates unchanged unless noted:

| Pattern | Data points | Promotion gate |
|---|---|---|
| Scaffolding-Hides-Truth pattern (formerly "Move B+") | 2 (Phase 2a malformed-compounds; Phase 3c distributivity-flip) | Phase 4 lands → 3rd instance → graduate to `DEVELOPMENT_LESSONS.org` |
| Mempalace cross-check catching draft-staleness (inverse mode) | 2 (2026-04-30 lattice batch + SH batch) | One more occurrence → codify in `mempalace.md` as dual staleness mode |
| Three-artifact batch shape for series founding (master + path-note + research-note) | 2 (SH 2026-04-30; PReduce 2026-05-02 — master + roadmap update; PReduce hasn't yet emitted the path-note + deep-research-note triad but has 5 prior research notes feeding the master) | One more matched pattern → codify in `DESIGN_METHODOLOGY.org` Stage 1 |
| Two-axis retirement framing for migration problems | 1 (SH runtime-vs-compile-time) | Look for this shape elsewhere; promote at 2-3 instances |
| Algebraic posture findings can flip across track boundaries | 1 (Track 2G "not distributive" → Phase 3c "is distributive") | One more instance → codify discipline |
| Per-relation registration generalizes (`merge-registry`, `meet-registry`, future) | 2 (Track 1B; Phase 3c) | One more → graduate to `structural-thinking.md` |
| Solver-output regressions silent because output is "form-shaped" | 2 (`unknown` rendering; `(solve [?path ...])`) | Issue #41 lands → codify in `testing.md` |
| PReduce ↔ concurrency interaction (tropical-quantale merges change per-cell merge cost) | 1 (concurrency primitives note §10 forward-references PReduce) | Re-measure post-PReduce; promote when calibration data confirms |
| Three-layer decomposition pattern (PReduce: per-AST-PU + shared e-class + unified registry) | 1 (PReduce founding) | Look for analogous decompositions in PPN 4D (attribute-grammar substrate); promote if same shape |

---

## Completed today

### PReduce series founding (`2103b11f`)

Stage 0/1 series master + roadmap update. ~261-line master with three-layer
architecture (per-AST-PU compound regions + shared e-class cells with
refinement-poset + unified rule registry), two orthogonal axes (rule-property,
persistence-regime), Track 0 sub-deliverables (0.1 architectural sketch, 0.2
rule-property taxonomy, 0.3 `.pnet` extension + LLVM lowering interface),
implementation Tracks 1-9 with PPN 4C Phase 1B as gating substrate. Pre-conversation
included 5-pillar literature survey + adversarial round on three-layer
decomposition (refining/validating, not tearing down — six refinements emerged).

### Master Roadmap staleness sweep — 3-commit batch

- **Commit 1** (`6b423e7c`) — series-header status lines + tracks tables refreshed
  for PM, BSP-LE, SRE, PPN. Added BSP-LE Track 2 + 2B ✅; SRE Tracks 2D/2F/2G/2H ✅
  + 2I 🔄 + 2B/2C/2E/PUnify rows; PPN Tracks 2/2B/3/4A/4B ✅ + 4C 🔄 + 3.5/4D rows.
  PPN table header dropped `(Proposed)`. BSP-LE Track 6-future row added.
- **Commit 2** (`2429aa0e`) — SH Series promoted from "Future" subsection to
  top-level series section (master + 11-track tracker + Two-Axis Retirement
  Picture). UCS Series promoted from research subsection to top-level. Added
  `#+OPTIONS: toc:nil` + `Last updated` line. Future + Lower Priority subsections
  refreshed (BSP-LE Tracks 1-2 → just Track 1; SH/PReduce no longer placeholders).
- **Commit 3** (`a12282c5`) — global "PReductions" → "PReduce" rename (6 places).
  Active Research table refreshed (+13 newer research notes from 2026-03-30 →
  2026-05-02; NTT Doc 1 marked ✅; OE Track 2 row updated to reflect PReduce
  ownership). Current Implementation Order section overhauled with Active /
  Recently Completed / Queued / Future buckets reflecting actual current state.
  Cross-Series Dependencies diagram updated: PM Track 9 reference replaced with
  PReduce + SH convergence.

### Series masters staleness sweep (`ccedebba`)

5 masters touched:
- **PAR Master**: Tracks 0/1/2 R1-R2 ✅ now reflected (were all ⬜); Track 2 R3-R5
  ⬜ split to separate row; PReductions → PReduce (2×).
- **CIU Master**: status line updated (was "Series inception"); Track 0 ✅
  (Trait Hierarchy Audit landed 2026-03-21).
- **PRN Master**: PReductions → PReduce (15 occurrences across thesis, source
  list, findings/conjectures, universal-primitives, contribution ledger,
  emerging patterns).
- **SH Master**: PReductions → PReduce (2×); Track 9 cross-ref text updated to
  reflect PReduce series existence.
- **PM Master**: Track 9 description reframed as "Promoted to PReduce Series"
  with subsumption note + PM-side residual.

### Standup/dailies opening (`680fd282`)

Standup-2026-05-02 + dailies-2026-05-02 created. Standup yesterday-section
covered the 2026-04-30 → 2026-05-02 morning interval. Dailies' carried-forward
captured all in-flight threads.

### Interleaved by user (parallel work)

- `0538582f` — D.3 first revision: 3 BLOCKING findings from D.2.SC accepted +
  applied (Cell Staleness Contract via typed dual-API, plus 2 others)
- `934b2ba3` — D.3 P1 acceptance + Issue #55 opened for hybrid pivot
  scaffolding retirement

These landed cleanly between my commits without conflicts.

---

## Lessons surfaced this session (worth codifying)

### 1. Master-roadmap-vs-series-masters drift discipline

**Observation**: The series masters were ALL more current than the master
roadmap. PPN Master, SRE Master, BSP-LE Master correctly tracked completed
tracks; the roadmap's per-series status lines and tracks tables were stale.

**Hypothesis**: Roadmap drift happens because PIRs trigger updates to the
series master (which is the canonical view) but the corresponding roadmap row
is sometimes missed. The series master IS the source of truth; the roadmap is
a navigation index that drifts unless explicitly synced.

**Discipline candidate** (worth codifying in `workflow.md` if pattern recurs):
when a track closes (PIR), update the series master AND the corresponding
roadmap row in the SAME commit. Or add to the post-close checklist: "verify
roadmap row matches master-tracker row."

**Promotion gate**: One more occurrence of roadmap drift on a track close →
codify as workflow.md discipline.

### 2. Adversarial framing as refinement, not destruction

**Observation**: User explicitly framed the adversarial round on PReduce's
three-layer decomposition as "Yes, and" — refining/validating rather than
tearing down. Result: six refinements emerged (per-AST-PU compound regions
NOT per-node PUs; one rewriting + one retraction stratum NOT six strata;
unified rule registry NOT two registries; e-class poset NOT just semilattice;
opaque-evaluation boundary; retraction-bit consultation regime). Framework
held; got tighter.

**Hypothesis**: Adversarial rounds work best when applied to a model the
designer trusts well enough to defend at every challenge. "Yes, and" is the
right disposition for tightening; "tear down" only fits when the model is
suspect. The methodology rule (`workflow.md`) says adversarial framing must
surface drift — it doesn't say the model must break.

**Promotion gate**: One more "Yes, and" adversarial round that tightens
without breaking → codify as a recognized adversarial-round disposition in
`workflow.md` / `CRITIQUE_METHODOLOGY.org`.

### 3. Series-promotion artifact pattern (master + roadmap-update commits)

**Observation**: PReduce founding pattern (today) was: master doc creation
+ roadmap section addition + same-commit register. SH founding pattern
(2026-04-30) was: master + path note + super-optimization research note,
followed by roadmap update later. The master-and-roadmap-update pattern
gives faster discoverability; the master+companion-notes pattern gives
more depth for series with substantial foundational research.

**Hypothesis**: For series with extensive prior research (PReduce had 5+
research notes already), the master + roadmap update is sufficient at
founding. For series founded from a single research arc (SH had ongoing
research conversation), the three-artifact pattern is appropriate.

**Promotion gate**: Look for the pattern in next 1-2 series foundings →
codify in `DESIGN_METHODOLOGY.org` Stage 1 as recognized series-opening
shapes.

---

## Phase 11 mini-design opening bookmark (2026-05-08)

Track 2I reopened post-Phase-10 (commit `6aab87cd`) for Phases 11-15 gap-closure work for Nation report. Phase 11 = `has-complement` empirical check (Boolean placement; Tier 1). Persisted at commit `658c2511`.

**User-flagged refinements** (2026-05-08):

1. **YAGNI reversal on /detailed variant**: original scope said "no /detailed per Phase 5 YAGNI." User: *"YAGNI may prematurely preclude considerations vacuously, as an excuse to defer."* Reversal honors Completeness Over Deferral. Add `complement-evidence` struct + `test-has-complement/detailed`. Non-vacuity ratio (fired/total) IS informative for has-complement (distinguishes strong-vs-weak empirical Boolean from concrete refutation). Phase 4's SD-vee 3.5% vs SD-wedge 91.4% is precedent. Going-forward (Phases 12-15): each property check gets /detailed unless decomposition is genuinely uninformative.

2. **In-phase sweep + report integration**: original scope said validation only; sweep deferred. User: front-loading code and back-loading data is unprincipled. Revised: each phase = code + sweep findings + design doc + Nation report update, single commit.

3. **Concurrent sweep parallelization**: extend harness with `--properties` flag; invoke 4 racket processes (one per domain) for ~10-15 min wall via 4-concurrent-process model.

**Drift risks (6 named)**: sample-set sensitivity, bot/top per-element handling, form domain expected refutation (potential Scaffolding-Hides-Truth #6 if confirms instead), type×subtype potential Boolean reach (NEW finding parallel to Phase 5a Heyting reach), report integration consistency, implication-rule ordering.

## Phase 11 close — has-complement (Boolean placement) + in-phase sweep + report integration (2026-05-08)

**Phase 11 substantively complete.** Targeted suite GREEN (164 tests / 4.4s). Single commit ties code + 4-concurrent sweep + design doc + Nation report update — per user-flagged in-phase pattern (vs. batched-at-end).

**New code**:
- `complement-evidence` struct (5-field; YAGNI reversed per user pushback + Completeness Over Deferral)
- `test-has-complement/detailed` + wrapper (per-element search; bot/top per-element handling skips vacuous)
- Wired into `infer-domain-properties` props-14, `resolve-and-report-properties`, `all-sweep-properties` (12 now), `sweep-one-property`, `extract-detailed-fields` (both sre-property-sweep AND run-phase9-sweep harness), Boolean column derivation in both summary tables
- Harness extension: `--properties` flag for parallel-by-domain sweeps with subset of properties

**Sweep results** (4 concurrent processes, ~30s wall via has-complement-only filter):
- type × {eq, sub} × {ground, wider}: **Boolean REFUTED, witness `Int`** (4 rows; same atom Int has no x with Int∧x=⊥ ∧ Int∨x=⊤ under either equality or subtype merge). Net-new architectural finding: type×{eq,sub} reaches Heyting on ground (Phase 5a) but does NOT extend to Boolean.
- spec-cell × equality × {ground, wider}: Boolean REFUTED.
- session × equality × {ground, wider}: untested (no top in samples).
- form-cell × equality × {ground, wider}: untested (no bot/top per Phase 8 lesson).

**Surfaced bugs caught + fixed**:
- Witness shape: my wrapper passed bare atom but `format-property-profile` expected list. Fixed: wrap in list at wrapper layer.
- Two stale tests reflecting pre-Phase-11 state (boolean prop-unknown → prop-refuted; report contains prop-unknown → prop-refuted) — updated to honor Phase 11 finding.
- `extract-detailed-fields` clause needed in BOTH `sre-property-sweep.rkt` AND `tools/run-phase9-sweep.rkt` (harness has its own copy). Phase 9b lesson re-surfaced; both updated.
- `apply` with keyword args doesn't work as I expected; replaced with explicit `cond` branch.

**Adversarial VAG passed**: code complete + sweep complete + report integration complete + finding captured + bugs caught and fixed in-flight without dialogue gate. No drift requiring corrective.

**Phase 11 NOT a Scaffolding-Hides-Truth instance**: positive finding designed to be surfaced (mini-design said "may surface type×subtype Boolean reach"). Validates Track 2H's `'has-complement prop-refuted` declaration for form-cell empirically (where testable). New empirical claim for type×{eq,sub} (Boolean refuted with concrete witness Int).

## Phase 12 mini-design opening bookmark (2026-05-08)

Phase 12 = M3 / N5 sublattice detection (Birkhoff's forbidden-sublattice theorem; Tier 1).

**Implementation strategy reversal**: original "enumerate 5-tuples" approach was 105 min per tuple at N=58 — unacceptable. Revised: **direct construction** via antichain-of-3 (M3) and comparable-pair-plus-incomparable (N5). O(N³) ≈ 30k iterations × ~700µs ≈ 20-30 sec per tuple. 4-concurrent sweep wall ~40s.

**Birkhoff 9.2 connection**: distributive ⇔ no-M3 ∧ no-N5. Our empirical sweep gives forward implication. The classical converse predicts: distributive-refuted lattices (type wider) MUST contain M3 or N5 — Phase 12 will find them if Phase 4's distributivity refutation reflects real Birkhoff witnesses.

**For Nation**: M3/N5 witnesses are CONCRETE LATTICE STRUCTURES he immediately recognizes — stronger presentation value than Phase 4's Pi-typed-triple distributivity refutation.

**Adversarial pass + 7 drift risks**: direct-construction soundness (closure check); cost; witness verbosity; sample-set sensitivity; harness `extract-detailed-fields` in both copies (Phase 11 lesson); architectural curiosity (Pi-typed Phase 4 witness — does it complete M3 or N5?); 1h size budget.

**/detailed variants for both** per Phase 11 Completeness-Over-Deferral discipline.

## Phase 12 close — M3 / N5 sublattice detection (Birkhoff 9.2) + sweep + Nation report (2026-05-08)

**Phase 12 substantively complete.** Targeted suite GREEN (171 tests / 4.8s). Single commit ties code + 4-concurrent sweep + design doc + Nation report update — Phase 11 in-phase pattern continued.

**New code**:
- `m3-evidence` + `n5-evidence` 5-field structs (per-Phase-11 Completeness-Over-Deferral)
- `lattice-incomparable?` + `sublattice-closed?` helpers (exported)
- `test-no-m3-sublattice/detailed` + wrapper (antichain-of-3 enumeration; canonical hash-code ordering to avoid permutation duplicates; closure verification)
- `test-no-n5-sublattice/detailed` + wrapper (comparable-pair-plus-incomparable-c enumeration)
- Wired into all 4 sites (sre-core.rkt provides + props-15/16, evidence map; sre-property-sweep.rkt + run-phase9-sweep.rkt extract-detailed-fields and case dispatch — both copies per Phase 11 lesson)
- `all-sweep-properties` now 14 properties

**Key design decision**: **Birkhoff 9.2 forward-implication-rule deliberately NOT added** despite seeming natural. Reason: `no-M3 ∧ no-N5 ⇒ distributive` is sound on the FULL lattice (theorem) but UNSOUND on sample-restricted checks. Phase 12's empirical findings demonstrate this concretely: type × wider confirms both no-M3 + no-N5 on sample but distributive refuted via Phase 4 triple. Sample limitation, not contradiction.

**Sweep results (4-concurrent, ~9.5s wall total)**:
- type × {eq, sub} × {ground, wider}: both no-M3 + no-N5 confirmed on sample (sample limitation per Birkhoff 9.2 — full lattice must have witness somewhere; depth-1 generator doesn't construct closed forbidden sublattices)
- session × eq × wider: **M3 REFUTED** with concrete witness `(sess-bot, sess-top, sess-end, sess-svar 0, sess-send(Bool, sess-svar 0))` — three pairwise-incomparable session types forming closed M3 sublattice. Concrete Birkhoff witness for session non-distributivity.
- spec-cell × eq × {ground, wider}: **M3 REFUTED** with witness `(spec-cell-bot, collision-top, spec-cell-value(bar, Int), spec-cell-value(foo, Int), spec-cell-value(foo, Bool))`.
- form-cell × eq × {ground, wider}: both confirmed.

**Bug surfaced + fixed in-flight**: first session sweep produced bogus M3 witness with `#f` element. Diagnosed: session sweep config had `#:include-bot-top #t` injecting `#f` into samples (since session is bounded-below per Phase 11 finding — bot=top=`#f`). Fix: match form-cell's `#f` config in run-phase9-sweep.rkt sweep-config. Re-sweep produced legitimate witnesses. Sister concern of Phase 11 bounded-vs-bounded-below clarification.

**For Nation**: M3 witnesses for session and spec-cell are CONCRETE LATTICE STRUCTURES he immediately recognizes — stronger presentation value than Phase 4's Pi-typed-triple distributivity refutation. Type domain sample-limitation surfaced as honest scope acknowledgment.

**Adversarial VAG passed**. 7 drift risks named, all materialized as expected or didn't fire (#1 closure check soundness — all witnesses verified closed; #2 cost — 9.5s wall, way under estimate; #5 both harness copies updated atomically per Phase 11 lesson; #6 architectural curiosity — type domain Phase 4 triple does NOT correspond to a sample-captured M3/N5; #7 size — 1h budget honored).

**NOT a Scaffolding-Hides-Truth instance** — both findings (concrete witnesses on session/spec-cell + sample-limitation on type) are designed-to-surface, not hidden. Clean phase close.

## Phase 13 mini-design opening bookmark (2026-05-08)

**Frame**:
- Design reference: design doc § Phase 13 (high-level scoping); + Decisions subsection persisted
- Obligations carried: Phase 11+12 in-phase pattern (code + sweep + report single commit); Phase 11 `/detailed` Completeness-Over-Deferral; Phase 11 atomic both-extract-detailed-fields-copy update
- Principles in play: **Correct-by-Construction** (explicit meet-fn + join-fn from same relation); **Decomplection** (J(L) + closure + axiom 3 orthogonal helpers); **Data Orientation** (anti-exchange-evidence carries non-vacuity decomposition)
- Mantra check: off-network sample-iteration scaffolding (Track 2G lineage); inherited; not new debt

**Q-decisions**:
- Q1 max-subset-size `k=2` (parameterizable; ~50 min sweep wall via 4-concurrent)
- Q2 AGT 2003 iff implication rule **deliberately NOT encoded** — independent empirical measurements give Nation more information than implication-derived results (matches Phase 12 Birkhoff-9.2-forward precedent)
- Q3 sweep scope: ALL 10 (domain, relation, depth) tuples
- Q4 single phase with sub-phase fallback if >1h
- Q5 `/detailed` decomposition: 5-field anti-exchange-evidence (total-checked / hypothesis-fired / conclusion-held / witness)

**Adversarial pass applied** (P/R/M/S two-column): no drift requiring corrective. Honest acknowledgments — Hasse-traversal optimization not in scope; AGT iff via empirical comparison not implication rule.

**Why this matters for Nation**: AGT 2003 is recommended in his lattice notes companion (ch11). Bidirectional empirical evidence of the iff — across all 10 tuples — is direct relevance to his recommended literature.

**Drift risks**: 7 named; |J(L)| explosion (k=2 bound), AGT iff rule deliberately deferred (Phase 12 precedent), closure-cache memory, sample-set sensitivity, both extract-detailed-fields copies (Phase 11 lesson), Phase 11 in-phase pattern, 1h phase-size boundary.

## Phase 13 close — anti-exchange-on-J (AGT 2003) + Phase 14 close — targeted congruences + Nation report integration (2026-05-08)

**Single combined commit per Phase 11+ in-phase pattern.** Phase 13 + 14 + Nation report update + tracker + design-doc § Phase 9 Findings #15-19 all in one atomic commit.

### Phase 13 substantively complete

**Code**: `sample-join-irreducibles` + `sample-closure-on-J` helpers + `anti-exchange-evidence` 5-field struct + `test-anti-exchange/detailed` + wrapper. Wired into all 7 sites (sre-core.rkt provides + props-17 + evidence map; sre-property-sweep.rkt + run-phase9-sweep.rkt extract-detailed-fields + dispatch — both copies per Phase 11 lesson). 6 new test-cases.

**Sweep — 8 of 10 tuples captured** via 4-concurrent processes:
- session × eq × ground: ✓ (vacuous |J|=2)
- session × eq × wider: **✗ REFUTED** (757 triples, 1 fired, 0 held — concrete iff disagreement vs SD-confirmed; sample-bounded, NOT theorem refutation)
- form-cell × eq × {ground, wider}: ✓ (1.1% non-vacuity each)
- spec-cell × eq × {ground, wider}: **✗ REFUTED** (concrete witness 30.8% non-vacuity; coheres with Phase 12 M3 + Phase 9 non-distributive)
- type × {eq, sub} × ground (from earlier sanity check): ✓ (26.9%, 32.2% non-vacuity)
- **type × {eq, sub} × wider: SWEEP TIMEOUT** at 57+ min CPU (k=2 enumeration on |J(L)|>>50 estimate). Killed for time-budget. Honest scope-bound documented.

**No forward implication rule** (matches Phase 12 Birkhoff-9.2 precedent — sample-unsound).

**For Nation**: AGT 2003 iff disagreement at session × eq × wider is the empirical headline — **bidirectional iff cannot be soundly claimed by bounded-subset checks**. Same lesson Phase 12 demonstrated for Birkhoff. Strong empirical-comparison data point.

### Phase 14 substantively complete

**Code**: `congruence-evidence` 5-field struct + `test-congruence-axiom/detailed` + 4 candidate wrappers (trivial, total, mult-forgetful, erasure-equivalence) + 2 normalization helpers (`forget-mult-norm`, `m0-erasure-norm`) + 4 equiv predicates. Wired into all 7 sites. 11 new test-cases.

**Sweep — ground-only** per Q5 time-budget pragma (4 concurrent processes, ~seconds wall):
- All 6 ground tuples confirm trivial + total (vacuous-by-construction; non-vacuity asymmetry 2-25% vs 100% reflects firing-rate of antecedent)
- type × {eq, sub} × ground confirm mult-forgetful + erasure-equiv (vacuous — atomic samples have no binders to strip; degenerates to trivial)
- Non-type tuples for mult-forgetful + erasure-equiv: untested with reason `'congruence-not-applicable-to-domain` (gating works correctly)

**Honest scope-bound**: empirical content for type-domain candidates on wider sample (with binder compounds) deferred to post-meeting follow-up. Framework-wiring + sanity confirmation succeeded.

**No forward implication rule** to subdirectly-irreducible characterization (sample-unsound). The classical Nation territory (Con(L) → SI) requires global Con(L); targeted candidates cannot make this claim.

### Nation report integrated

`docs/research/2026-05-08_PROLOGOS_LATTICE_VARIETY_CATEGORIZATION.md` updated with:
- Anti-exchange column block (Phase 13) with iff-disagreement note for session×eq×wider
- Targeted congruences column block (Phase 14) with vacuity disclosure
- **NEW: "Merge-as-canonical-form structural-optimality claim" subsection** addressing prior-meeting follow-up — frames our merge as emergent canonical-form-witness, mapping to Whitman/Freese-Nation 1.17 + Reading-Speyer-Thomas FTFSDL. Connection to Phase 6 Whitman's W ✓ across all 10 as the structural foundation. Open question for Nation: empirical equivalence between our merge and FL/RST canonical form.

### Tracker updates

- Phase 13 ⬜ → ✅ (substantively closed, type-wider scope-bound flagged)
- Phase 14 ⬜ → ✅ (ground-only, empirical content deferred for type-domain candidates)
- Phase 15 (Day's doubling / Adaricheva-Nation 2017) ⬜ remaining — user fork point

### Adversarial VAG passed (combined for Phase 13 + 14)

Two-column P/R/M/S applied. Honest acknowledgments:
- Phase 13 type-wider sweep timeout = real operational data point (k=2 on |J|>>50 is intractable); sister-concern k=1 rerun for post-meeting
- Phase 14 ground-only vacuity for type-domain candidates is honest scope-bound, not silent gap
- AGT iff + SI characterization both deliberately NOT encoded as forward rules (sample-unsound) — matches Phase 12 discipline, preserves empirical-comparison data points

### Time spend

Phase 13 + 14 combined: ~3 hours (mostly Phase 13 type-wider waiting). Empirical type-wider anti-exchange sweep is the main deferred item; framework + 8/10 tuples + Phase 14 ground complete.

**User noted**: planning to fork from earlier and continue with Phase 15 (Day's doubling).

## Today's planned work (status)

**Primary**: Master Roadmap staleness sweep — ✅ COMPLETED (3 commits as
agreed) + masters sweep extended ✅ (1 additional commit, 5 masters).

**Secondary** (if time / context permits): SRE Track 2I Phase 4 — sweep
semantic correction (atomic 5-function refactor + ~25 callsites). Or
PPN 4C Phase 1C implementation. *Status*: not yet attempted; open for
remainder of session if context budget allows.

**Out of scope** (deferred): Phase 3b (SRE generator extension),
Phase T (test consolidation), Discussion phase (declaration update
decisions), PM Track 12 sister callback retirement, PReduce Track 0.1
(architectural sketch document).

---

## Lessons distillation check (per workflow.md)

Scanned: most recent PIR (BSP-LE Track 2B, 2026-04-16), past 4 dailies
(2026-04-30, 2026-04-26, 2026-04-23, 2026-04-22).

No new graduations needed at this interval-open beyond what the Watching
table tracks. Lessons distilled in the prior 2 weeks have already landed
in `workflow.md`, `DESIGN_METHODOLOGY.org`, `DEVELOPMENT_LESSONS.org`,
`pipeline.md`, `propagator-design.md`.

Three patterns surfaced today (see § "Lessons surfaced this session" above):
- Master-roadmap-vs-series-masters drift discipline (promotion gate: 1 more
  occurrence on a track close)
- Adversarial framing as refinement, not destruction (promotion gate: 1 more
  "Yes, and" round)
- Series-promotion artifact pattern variants (promotion gate: 1-2 more
  series foundings)

These are NEW patterns from this session that warrant tracking. The
"Watching" table at the top of this dailies covers the prior medium-term
patterns; today's three are tracked in their own section above with full
structure (Observation / Hypothesis / Discipline candidate / Promotion gate).
Consolidate into Watching at next interval-open if they remain relevant.
