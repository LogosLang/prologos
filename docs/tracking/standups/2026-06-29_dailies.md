# Dailies — 2026-06-29

**New initiative opened and substantially advanced: the DEMO Series.** A
demo-driven dogfooding effort — co-developed with the owner — that exercises the
Process + Relational + Functional paradigms end-to-end through a single `schema`
contract, using the demo as a *forcing function* to surface and close real
language gaps. This session took it from "opening conversation" → Stage-3 design
(D.2, critique-complete) → runnable P0 acceptance core → **two language fixes
landed** (schema-typed `defr`, `_` wildcard) → editor/tooling fixes (LSP, test).

Design doc: `docs/tracking/2026-06-28_DEPENDENCY_RESOLVER_DEMO_DESIGN.md`.
Memory: `demo-dependency-resolver-track`, `prologos-look-and-feel-conventions`.

---

## ⏸ CHECKPOINT — Resume State (read this first) — refreshed for P1 (2026-06-30)

**Where we are**: DEMO Track 1 — Stage-3 design (D.2) + P0 DONE; **all post-rewind work closed,
suite fully green (8394)**. This stretch: gap #3 (`data` ADT constructors) FIXED so the JSON parser
can use a **real ADT**; batch state-isolation flakes KILLED; `nat.pnet` churn killed; GADTs grounded
+ parked as a full SRE 5 track. Demo runs **0 errors at L3 via BOTH `run-file.rkt` AND the LSP REPL**.
**Next: P1 — the pure-Prologos JSON parser, now on a real `data Json` ADT (at last).**

**Warm up by reading (in order)**:
1. This checkpoint + dailies "Post-resume continuation 2" (gap #3, nat.pnet, GADT note, flake fix).
2. Design doc `docs/tracking/2026-06-28_DEPENDENCY_RESOLVER_DEMO_DESIGN.md` — §3 progress tracker,
   §5 D1 (JSON value type) + D2 (validation), §12 gaps (#3 now FIXED).
3. Memory: `demo-dependency-resolver-track`, `prologos-look-and-feel-conventions`.
4. SRE 5 note (only if GADTs resurface): `docs/tracking/2026-06-29_SRE_TRACK5_DEPENDENT_MATCH_GADT_NOTE.md`.
5. `git log --oneline d7f9daf..HEAD` for the session's commits (below).

**HEAD**: `fa7de89c` (branch `main`, base `d7f9daf`). Key session commits:
- `282b138c` P0 acceptance core + Stage-3 D.2 design
- `fac8902e`/`bb16e535`/`2f8f699c`/`99fd7cd8`/`81097db9`/`aeda051d` gaps #1/#6 + LSP/REPL fixes (pre-rewind)
- `0deee838` + `73d3a13a` **gap #3 — `data` ADT separators FIXED** (both pipelines; `tests/test-data-adt-forms.rkt`, 6)
- `526abee6` **`nat.pnet` untracked + gitignored** (machine-specific cache)
- `8e370bfc` **SRE 5 dependent-match/GADT grounding note** (linked from SRE Master)
- `ebabbc14` **batch state-isolation flakes KILLED** (SRE merge-fn registry snapshot/restore)
- `fa7de89c` dailies (post-resume continuation 2)

**Suite**: **8394 tests, ALL PASS** (flakes gone — deterministic fix, verified by forced bad orderings).

**Runnable now (0 errors)**: `racket/prologos/examples/dependency-demo/dependency-resolver.prologos`
— `racket racket/prologos/tools/run-file.rkt <file>` (any cwd) AND the LSP REPL. Demonstrates: file IO
+ pure-Prologos CSV parse + schema-typed `package` relation + recursive closure + audit join (`_`
wildcard) + `explain` provenance + `session`/`spawn`.

**Working tree (do NOT blind-commit)**: `lib/examples/foray.prologos` (owner WIP + union-forms hang
workaround) + other pre-session owner changes (standups, settings). `nat.pnet` is now gitignored.
Leave owner files alone.

### → NEXT: P1 — pure-Prologos JSON parser

- **Deliverable**: `prologos::core::json` with `parse-json : String -> Result Json`.
- **P1 can now use a REAL `data Json` ADT** — gap #3 FIXED (commit `0deee838`, post-rewind):
  the bug was `:=`/`|` separator parsing in `data` ctors (`process-data` never stripped the
  separators; field-bearing ctors bound nullary + phantom field-type ctors). Fixed via
  `normalize-data-ctor-clauses` (macros.rkt) + literal-`|`→`$pipe` canonicalization for the cell
  pipeline; validated at L3 via BOTH process-file AND process-string-ws (`tests/test-data-adt-forms.rkt`,
  6 tests; suite 8394). Numeric: faithful `JNum`-as-`Rat`. Pure-Prologos (max dogfooding).
  *(Newly surfaced, filed: polymorphic colon-field ctors `data Lst {A} … cns : A -> Lst A` fail in the
  cell pipeline only — separate pre-existing bug, orthogonal to the separator fix.)*
- Then **P2a**: `Validation` accumulating applicative (`Result` short-circuits — design D2/§11).
- **Validate at L3 via BOTH `process-file` AND `process-string-ws`** (the REPL path) — the recurring
  two-pipeline trap (memory + §2). Repro pattern: compare both paths × cases.

**Settled — do NOT re-litigate**: schema-as-connective-tissue; seam-first; dependency-graph
dataset; P3 "split the diff" (demo slice + owned RPF track); validation = parsing-not-typechecking
(needs `Validation`); relation fact store off-network (= SQLite prereq); **GADTs deferred to a full
SRE 5 track** (not a demo blocker; grounding note `8e370bfc`).

### → P1 quick-start (the concrete next move)

- **Deliverable**: `prologos::core::json`, pure-Prologos, `parse-json : String -> Result Json`.
- **Model JSON as a real `data Json` ADT** (gap #3 fixed): e.g. `data Json := JNull | JBool Bool |
  JNum Rat | JStr String | JArr [List Json] | JObj [Map String Json]`. **First real recursive ADT
  use — verify it parses + works at L3 via BOTH `process-file` AND `process-string-ws`** before
  building the parser on it. Numeric: faithful `JNum`-as-`Rat` (lex sign/digits/decimal/exponent →
  Rat; int/float identity erased — coerce to schema types later at P2).
- The parser exercises `string-ops` hard (split / index-of / trim / char-level) — expect ergonomics
  findings (that's the dogfooding point). Then **P2a**: `Validation` accumulating applicative.

**Other open** (not next): union-type hang (DEFERRED.md); owned tracks §11 (RPF, core::json,
data::validation, session named-composition, channel delegation); nested sub-schema defs in cell
path; form-cells silent-drop diagnostic; `_anon` result-key cosmetic; field-named result projection
+ schema-typed RULES.

**Settled — do NOT re-litigate**: schema-as-connective-tissue framing; seam-first;
dependency-graph dataset; P3 "split the diff" (demo slice + owned RPF track);
validation = parsing-not-typechecking (needs `Validation` applicative); the relation
fact store is off-network (= the SQLite prerequisite the owner already named).

---

## The arc (chronological, with the *why*)

### 1. Opening + grounding (4 parallel read-only agents)

Owner wants demos that exercise Process + Relational together: read CSV + a JSON
array of records (Process/IO), schema-validate them (`schema`), and query them
relationally with reusable subqueries over a graph; plus showcase *session-type
composition* (a session type in another's continuation). SQLite deferred until
sub-process infra + a deeper relational fact representation exist.

Rather than design from memory, grounded the *actual* state via 4 HEAD-pinned
agents (Process/IO, `schema`, Relational, session types). Headline:
- **The 3-paradigm table** (`RELATIONAL_LANGUAGE_VISION.org:13`) is the organizing
  insight: `schema` is the **relational "spec"** (the signature that types a
  relation's columns), and `session`/`defproc`/`proc` are the Process column. So
  the demo is "exercise all three columns at once," with `schema` as the
  *connective tissue* (it types both the records and the relations).
- CSV module exists (FFI to `io-ffi.rkt`); **no user-facing JSON parser**; IO is
  thin; the relational engine (`defr`/`solve`/recursion/joins) is solid; session
  basics run at L3, composition is partial.

### 2. Stage-3 design — D.1 → D.2

Wrote D.1 (the dependency-resolver demo concept), captured owner decisions
(D1–D7), then ran an **independent P/R/M/S critique cycle** (4 adversarial
reviewers). The critique earned its keep — it caught **two blocking defects
before any implementation**:
- **`Result` short-circuits** (S-lens): stdlib `Result.map2` stops at the first
  error; validation that should accumulate ALL field errors needs a `Validation`
  applicative (Semigroup on the error side). Filed as P2a.
- **The relation fact store is off-network** (M-lens + S-lens, independently):
  facts are a `make-parameter` copied per-query into a fresh fork behind a
  *name-grained replace* merge. So real `:from` (dynamic fact loading) =
  migrating to a persistent fact-grained cell — which **is the same "deeper
  relational fact representation" the owner named as the SQLite prerequisite.**

The two P/R reviewers *conflicted* on D2's premise (is schema metadata
runtime-readable?); resolved by direct read (`macros.rkt:534/768/776`):
dual-written param+cell, Racket-runtime-readable, not Prologos-exposed → D2
premise corrected. And (S-lens) validation of runtime-parsed data is
**parsing, not type-checking** — so "validate via the type-checker" is a
category error.

**P3 "split the diff"** (owner): the demo builds a *scoped first slice* of real
on-network `:from`; the full competitive fact-representation work spun out as the
owned **RPF track** (Relational/PM, multi-track). Governance fix (P-lens): the
enabling fixes get owned-series tracks + deployment gates, not "filed somewhere."

### 3. P0 — runnable acceptance core (commit `282b138c`)

Opened P0 with a **6-facet syntax-grounding workflow** whose facets *ran live
`process-file` probes* (ground truth, not inference). Wrote the acceptance demo
`examples/dependency-demo/dependency-resolver.prologos` + synthetic dataset.
**Runs 0 errors / ~30s at L3**, results hand-verified: real file IO, pure-Prologos
CSV parse, relational graph queries + recursive closure + audit join (subquery
reuse), `explain` provenance, schema-as-maps, session+spawn.

P0's payoff was **5 filed gaps** — the demo's runnable core is much smaller than
the design assumed:
1. schema-typed `defr R : Schema` unwired at L3;
2. `require [core::csv]` E2001 at module load;
3. user `data` ADT constructors don't bind at WS L3 (→ P1 models JSON as maps);
4. top-level file IO works only as a bare expression (`def x := [read-file p]` → E2001);
5. `solve` returns per-derivation-path bindings (diamond → dups).

### 4. Schema-typed `defr` — gap #1 FIXED (commit `fac8902e`)

Grounding workflow → root cause: `parse-defr` never recognized `: Schema`; it fed
the bare `:` token to `parse-rel-params`, which errored, and the **file merge
silently dropped the error** → the relation never registered. The `surf-defr`
schema field + elaborator→relation-info threading were already wired.
- **parser**: `parse-defr-schema-typed` recognizes `: Schema`, derives arity +
  field-named params from the registered schema (arity drives both fact-row
  splitting and query matching).
- **driver**: `check-relation-schema-rows` type-checks each fact row positionally
  against the schema field types (reusing `schema-field-type->expr` + `check`).
- Tests `tests/test-defr-schema.rkt`; demo now uses `defr package : Package` and
  joins it in the audit query.
- Scope = register + type-check **facts**; field-named result projection +
  schema-typed **rules** deferred.

### 5. Review fixes (commits `bb16e535`, `2f8f699c`)

- **Look-and-feel conventions** (owner): implicit `eval`, single `require` with
  block-notation module specs, and **no outer delimiters on top-level calls**
  (`println "hi"`, not `[println "hi"]`). Applied to the demo; saved as memory.
- **`_` wildcard in goals (gap #6) FIXED**: `_` parses to `surf-hole` → was
  elaborating to `expr-hole` (doesn't unify as a free var). Now a `surf-hole`
  elaborates to a *fresh* `gensym '_anon` logic var under
  `current-relational-fallback?`, `expr-hole` in functional context.
- **LSP stdout pollution FIXED**: stdout IS the JSON-RPC channel; `lsp/server.rkt`
  redirected only `current-error-port` during `process-file`, so a doc with
  top-level IO (`println …`) corrupted the message framing ("Content-Length
  missing"). Now also redirects `current-output-port` (diagnostics + REPL paths).
- **`test-parse-reader` lock-file skip**: the full-suite failure ("2 unbalanced
  library files") was the owner's **Emacs lock files** (`.#core.prologos`,
  `.#book/ordering.prologos`) — `file->string` threw, miscounted as unbalanced.
  The examples scan already skipped `.#`/`~`; applied the same filter to the
  lib scans.

---

## Design decisions (the why)

- **`schema` as connective tissue** — the unifying narrative that makes the demo
  cohere (one `schema` typing both records and relations), not three disconnected
  pieces.
- **Seam-first** (owner) — deliberately drive into the gaps to *collect* them,
  per the stated goal ("identify where more work is needed").
- **Software dependency graph dataset** — on-theme with the owner's
  capability/supply-chain work; graph-shaped so subquery-reuse is natural.
- **P3 split-the-diff** — a scoped on-network slice now; the foundational
  competitive fact-representation as the owned RPF track (= SQLite prerequisite).
- **Validation = parsing, not type-checking** (D2 corrected) — runtime-parsed
  data isn't a compile-time type; needs a `Validation` accumulating applicative.
- **Schema-typed `defr` scope** — register + type-check facts (the demo's need);
  field-named projection + rules are honest follow-ons, not silently skipped.

## Surprises

- The critique caught **two blocking defects before implementation** — exactly
  what an adversarial cycle is for.
- The relation store being off-network *converged* with the owner's own SQLite
  instinct (the demo found the prerequisite they'd already named).
- **P0's runnable core was much smaller than the design assumed** — the whole
  ingestion→relation bridge is unbuilt at L3. Good: that's the dogfooding payoff.
- The suite failure was **Emacs lock files**, not the obvious suspect (foray).
- The LSP corruption was **stdout pollution from a top-level `println`**, not the
  perf-counter noise (those already go to stderr).
- `_` was a **`surf-hole`** (the partial-application placeholder), not a logic var.

## Lessons

- **The demo-as-forcing-function works.** In one session it surfaced and *fixed*
  real gaps: schema-typed `defr`, the `_` wildcard, the LSP framing bug, a test
  robustness bug. The runnable artifact is the diagnostic instrument.
- **Grounding workflows with live probes beat inference.** Facets that actually
  ran `process-file` gave ground truth; the acceptance file ran first-try because
  every form was probed.
- **Diagnose, don't assume** (recurring): the test failure wasn't foray; the LSP
  bug wasn't the perf-counters; `_` wasn't a `surf-var`. Each obvious suspect was
  wrong; the fix came from reading the actual path.
- **Adversarial critique before code pays for itself** — the `Validation` and
  off-network-store findings would have been expensive mid-implementation pivots.

---

## Carried forward

- **foray type-check hang — HUNTED + ISOLATED + worked around** (commit pending).
  Bisected to a **union-type non-convergence bug**: `<Int | String>` in a specific
  ORDER-DEPENDENT sequence (`def x : <Int|String> := 42` → `x` → `the <Int|String>
  "0"`) makes the typing propagator network never quiesce (infinite BSP firing in
  `attribute-map-merge-fn`, the `:type`-facet union join not reaching a fixpoint).
  Reordering avoids it; plain types are fine; each form alone is fine. NOT foray's
  fault — foray just triggers it. Commented foray's union forms (foray now loads:
  `--- 4 errors ---`, its own experimental bugs, no hang). Filed in DEFERRED.md
  ("BUG: Union-type checking hangs the type-checker") — needs a dedicated
  type-lattice-convergence fix + likely a fuel bound on the typing network so
  non-convergence is a bounded diagnostic, not a hang.
- **Next demo phases**: P1 (pure-Prologos JSON parser → maps/lists/Result, per
  gap #3) + P2a (`Validation` applicative); then iterate demo look-and-feel.
- **Owned follow-on tracks** (§11): RPF (fact representation = SQLite prereq);
  `core::json`; `data::validation`; session named-composition; channel delegation
  (PL series). Field-named result projection + schema-typed rules are smaller
  follow-ons on the schema-typed-defr feature.
- **Watching**: a flaky `test-module-network-01` (passes in isolation; batch
  state-isolation) — surfaced during this session's full-suite runs.

### Cosmetic / minor filed

- `_anon…` gensym names leak into `solve` result-map keys (result projection
  could drop anon vars).
- `check-parens.sh` false-positives on `.{…}` infix syntax (uses Racket's reader).

---

## Post-resume continuation (2026-06-29) — relative-path fix + REPL divergence

Session rewound from a further point; files/commits were ahead of the conversation. Caught
up via this dailies + design doc + memory (one correction: the demo lives under
`racket/prologos/examples/dependency-demo/`, not repo-root `examples/`). Then: fixed an
owner-reported relative-path bug, and investigated the REPL-vs-`process-file` divergence it
exposed.

### 1. Source-file-relative `read-file` paths (commit `81097db9`)

**Bug**: loading the demo in the editor REPL, a relative `read-file` path *doubled*
(`<repo>/…/dependency-demo/` + `racket/…/deps.csv`). Worked via `run-file.rkt`, not the LSP/REPL.

**Root cause**: `read-file` → `file->string` resolves against Racket's `current-directory`, and
**nothing in the compiler ever sets it** — purely ambient process CWD. `run-file.rkt` runs from
repo root (works); the editor launches the LSP/REPL with CWD = the file's dir (doubles). Modules,
by contrast, resolve location-independently against an absolute lib root (`driver.rkt:143`) —
resource files were the odd one out.

**Decision (owner)**: source-file-relative semantics. `process-file` absolutizes its path then
parameterizes `current-directory` to the source dir; `#:source-dir` override for callers
processing a copy (LSP diagnostics temp file); the REPL `loadFile` path parameterizes from the
doc URI. Demo uses sibling `"deps.csv"`. Regression test `tests/test-relative-path-resolution.rkt`.

**Surprise**: the doubling is NOT our code anchoring at the file dir — our code never touches
`current-directory` at all. It's the *editor* launching the process with CWD = file dir.

**Lesson (recurring, 2× this session)**: changing an exported signature (`process-file` +
`#:source-dir`) invalidates the `.zo` of EVERY dependent — must `raco make` ALL entry points
(`run-file`, **`repl.rkt`**, `lsp/server`), not just the ones you test. Missing `repl.rkt` →
Emacs REPL crashed with a linklet mismatch on relaunch. After an export-surface change,
recompile the entry-point graph, not just the module.

### 2. REPL ≠ `process-file`: schema-typed `defr` (FIXED — commit `aeda051d`)

**Symptom**: the demo runs 0 errors via `process-file`/`run-file.rkt`, but the LSP REPL
(`loadFile` → `eval-in-session` → `process-string-ws`) reports 1 error and the schema-typed
`package` relation never registers (`Unknown relation: package` at `solve`).

**Root cause (definitively pinned, via a repro harness)**: `process-file` uses the **merge**
pipeline; the REPL uses the **cell** pipeline (`extract-surfs-from-form-cells`). There,
**`schema` is mis-categorized as side-effect-only** (`form-cells.rkt:289`) — but `schema`
actually emits a type def `(def Name : (Type 0) (Type 0))` via the **whole-program** preparse
expander (`macros.rkt:2742`). The cell path discards it → the type `Package` is unbound → the
schema-typed `defr`'s `surf-var Package` can't elaborate → the `defr` command is **silently
dropped** (no residuation in the string path to recover). General gap: *any* `schema` use in the
REPL is affected, not just schema-typed `defr`.

**Design choice (owner)**: fix it properly in the cell path (**option A** — teach the cell
pipeline to emit schema's type def), over routing `loadFile` through `process-file` (option B).
Scope grew from "wire `defr`" to "fix schema def-emission in the cell pipeline" — broader but
more valuable (fixes schema-in-REPL generally).

**Fix (commit `aeda051d`)**: `form-cells` now emits the schema's `(def Name : (Type 0) (Type 0))`
in the cell pipeline (it was mis-categorized as side-effect-only). Simpler than factoring the
whole-program expander: emit the main type def directly from the datum's `cadr` — no re-parse, no
re-register, no datum-shape risk (the fields are already registered by preparse-expand-all).
Verified: `process-string-ws` output now identical to `process-file` (untyped + typed); the full
demo runs 0 errors via the REPL path; new regression test in `test-defr-schema.rkt` exercises the
`process-string-ws` path. Full suite **8388**, all pass. Nested sub-schema defs in the cell path
remain a follow-on.

**Surprises**:
- The **parse was fine** — `parse-datum` produced a correct schema-typed `surf-defr` (arity 3,
  field-named params); the failure was DOWNSTREAM at elaboration (unbound type), not in parsing.
  The cell path *does* route `defr` through `parse-datum` (via `parse-eval-tree-for-cell`),
  despite `parse-defr-tree` being a stub.
- The failure was **silently dropped** — `form-cells.rkt:302-304` drops error surfs with no
  diagnostic; the only visible error was downstream at `solve`. Silent drops mask bugs.

**Lessons**:
- **Diagnose, don't assume** (hard-won here): three fix attempts failed because I hypothesized
  before confirming (init-parity; routing schema to the datum branch). Only the **instrumented
  repro** (printing `process-command`'s `defr` arm) gave ground truth — the arm is reached for
  untyped but NEVER for schema-typed in the string path.
- **A reproduction harness beats reading code**: `scratchpad/repro-repl-divergence.rkt`
  (process-file vs process-string-ws × untyped vs typed) was the decisive instrument.
- **Two-context boundary, again**: features wired in ONE parser path don't auto-work in the
  other. The schema-typed `defr` feature (`fac8902e`) only taught the **preparse** parser; the
  cell pipeline never got it. The `process-file` (merge) vs `process-string-ws` (cell) split is a
  standing divergence source — **validate new user-facing features at L3 via BOTH `process-file`
  AND the REPL/`process-string-ws` path**, not just `process-file`. (Watching: candidate for a
  testing-rules addition once it has 2-3 data points.)

---

## Post-resume continuation 2 (2026-06-29/30) — gap #3, housekeeping, GADT grounding

After re-syncing post-rewind, closed four items (all committed; suite **8394 all-green**):

1. **DEMO gap #3 — `data` ADT constructors — FIXED** (`0deee838`). Corrected diagnosis (live repro + adversarial workflow): the bug was the `:=`/`|` **separator forms** — `process-data` mapped `parse-data-ctor` over raw `(:= c1 $pipe c2 …)`/`(| c1 …)` tokens without stripping separators (field-bearing ctors bound nullary + phantom field-type ctors). `normalize-data-ctor-clauses` strips `:=`, canonicalizes literal `|`→`$pipe` (**the cell reader keeps `|` literal — the two-context piece the diagnosis missed, caught only by validating BOTH pipelines**), splits, unwraps single-list segments. `tests/test-data-adt-forms.rkt` (6). **P1 can now use a real `data Json` ADT.** Convention: colon ctor form = fields-only, return implicit; full GADT explicit returns deferred. Filed orthogonal bug: polymorphic colon-field (`data Lst {A} … cns : A -> Lst A`) fails in the cell pipeline only.

2. **`nat.pnet` cache churn — FIXED** (`526abee6`): the `.pnet` embeds an absolute source path + mtime, so it regenerates with the local path on every machine (committed copy had a `/home/user/…` Linux path). Untracked + gitignored `racket/prologos/data/cache/pnet/`.

3. **SRE Track 5 (dependent match / GADTs) grounding note** (`8e370bfc`): [`2026-06-29_SRE_TRACK5_DEPENDENT_MATCH_GADT_NOTE.md`](../2026-06-29_SRE_TRACK5_DEPENDENT_MATCH_GADT_NOTE.md), linked from SRE Master Track 5. Verified: we already have Σ (= existentials) + the `Eq`/`refl`/`J` kit; the defining gap is **index-refining dependent `match`** (`check-reduce` is non-dependent — we hand-write `vhead`/`vtail`/`vindex` *because* of it). Decision: defer to a **full SRE 5 track** (Phase 1 formation cheap, Phase 2 dependent-match the real work) — NOT a partial formation-only step (half-feature trap). Not a demo blocker.

4. **Batch state-isolation flakes — KILLED** (`ebabbc14`): `test-facet-sre-registration` + `test-module-network-01` flaked because `test-merge-fn-registry` + `test-cell-domain-inheritance` `hash-clear!` the process-global Tier-2 merge-fn registry per case and never restored the ~46 production entries → poisoned siblings in the same batch worker. Classification flows cell→merge-fn→Tier-2→domain→Tier-1, so the Tier-2 clear ALSO silently disabled `test-module-network-01`'s structural-`#:component-paths` guard (**the cascade — confirmed by live forced-order repro**). Fix: `save/restore-merge-fn-registry-snapshot` + polluters snapshot-at-load/restore-at-end (deterministic); `test-module-network-01` requires the registration modules to own its precondition. Forced bad orderings that reproduced the failures 100% now pass 100%.

**Lesson (flake fix)**: the diagnosis called `test-module-network-01` "isolation-only / deterministic"; the live forced-order repro showed it ALSO cascades in batch (Tier-2 clear → classification-lookup `#f` → guard no-op). **Reproduce-then-fix beat the inference** — the owner's "it's a flake, sometimes green sometimes red" framing was the tell that pointed at the cascade.

---

## Post-resume continuation 3 (2026-06-30) — Numerics track spawned; P1 re-sequenced (commit `25fc1256`)

Owner re-prioritized P1's number handling: **`Rat` is insufficient for realistic JSON numbers → we need `Float`**, and the **numeric tower should be reviewed generally** — reconceived as **refinement-via-`trait`** (compositionally safe) rather than subtyping (subtyping ≅ logical-implication, fragile under composition — same lesson as "no trait hierarchies, use bundle"). Posit display is inscrutable; numerics ergonomics need cleanup. Scope expansion is the point (demo as forcing-function for language design).

**Grounded first, not designed from memory** — ran the `numerics-grounding` workflow (5 HEAD-pinned facets + completeness critic + design-options synthesis; the synth did its own R-lens pass and corrected several facet claims). Key grounded findings (HEAD `f44133e9`):
- It's **NOT greenfield**: [`2026-02-19_NUMERICS_TOWER_ROADMAP.md`](../2026-02-19_NUMERICS_TOWER_ROADMAP.md) Phases 1–3f ✅ (posits, Int/Rat, traits, within-family subtyping = the 9 edges, cross-family conversions); **Float was always Phase 4 — as FFI-only**. Plus a ready [`ergonomics audit`](../2026-02-22_NUMERICS_ERGONOMICS_AUDIT.org).
- Bare `3.14` = **Posit32** at HEAD (ergonomics-audit Decision 4; roadmap's "3.14=Rat" is stale) → a Float literal needs a NEW form; `(the Float64 3.14)` does NOT re-width (verified).
- Tower = 9 hardcoded subtype edges; cross-family = `numeric-join` LUB; ONE subsumption point (`typing-core.rkt:2419-2428`) carries ALL subtyping (the high-risk consumer for any reconception). Refinement = lossy-through-arithmetic library wrappers (`base-numeric-type` erases). Posit display = dead decoder (`posit-display` exists, `pp-expr` never calls it). Exponent `1e10` doesn't tokenize.
- **Premise refined**: `Rat` IS precision-exact for JSON (`0.1`=`1/10`); JSON's real gaps are exponent-lexing + int/decimal identity; NaN/Inf illegal in JSON → Float's true motivation is C-FFI/outside-world, not JSON precision.

**Owner decisions (panel)**: dedicated Numerics track; **JSON blocks on Float**; **Float = full compute primitive** (revises the FFI-only vision).

**Landed (commit `25fc1256`)**: charter [`2026-06-30_NUMERICS_TRACK_CHARTER.md`](../2026-06-30_NUMERICS_TRACK_CHARTER.md) (resume+revise framing, grounded state, N0–N6 phases, §5 open forks, §6 risks); DEMO doc re-sequenced (P1 ⛔ BLOCKED on Numerics; D1 `JNum Rat`→`JInt|JFloat Float`; §11 + status); MASTER_ROADMAP row; demo `solve`-unwrap. Memory: [[numerics-track]] created, [[demo-dependency-resolver-track]] + MEMORY.md index updated.

**Next**: the Numerics track's Stage-2/3 design cycle — open with the §5 forks (tower disposition; refinement substrate: extend-bundle / Galois-domains / liquid; refinement-preserving arithmetic; Float literal syntax) + a full read of the `base-numeric-type` pivot (`typing-core.rkt:166-178`). DEMO P1 unblocks after N1 (exponent-lex) + N3 (Float).

---

## Numerics design cycle (2026-06-30) — Stage-3 design COMPLETE (commits `de5d0b14`, `5a28dfa0`)

Ran the Numerics track's design cycle to a complete Stage-3 design ([`2026-06-30_NUMERICS_TRACK_STAGE3_DESIGN.md`](../2026-06-30_NUMERICS_TRACK_STAGE3_DESIGN.md)) via two grounding/research workflows + careful owner co-design (Clusters 1–4).

- **Foundation**: Galois/abstract-domain substrate × **disposition B** (surgical — keep primitive widening, trait-refine only the user layer), commit-now. Grounded by `numerics-refinement-design` (the Sign/Interval domains already model PosInt/Zero/NegInt; `net-add-cross-domain-propagator` already bridges a type-level pair = the existence proof).
- **Cluster decisions**: Q1 refinement=attribute; Q2 Sign built-in/extensible; Q3 propagate-and-check (full inference deferred); Q4 8-element Sign lattice (=Q₃ cube) + preserve-or-⊤ transfer; Q5 subsumption base+⊑, F2-clean; Q6 erased representation (coercion=identity); Q7 surface sliced → UCS track 5 + note; Q8 **context-typed literal (D)**, unconstrained→Rat + terminating-decimal display; Q9 exponents exact-at-source; Q10 round-tripping marked-literal display; Q11 Float new rank family.
- **Spawned**: UCS track 5 ("traits ARE refinement typing" — general `Type@property` surface + trait-as-type-producer; note `2026-06-30_TRAITS_AS_REFINEMENT_TYPING_NOTE.md`); a future full-refinement-inference track.
- **Key insights / surprises**: the **sibling-meta-domain framing de-risks the bridge** (refinement never enters the type lattice → the F7/F2 composition worry dissolves; `type↔mult` is the proof). The **decimal-literal research changed the call** — surfaced option D (context-typed) which dominates both fixed-Rat (Raku's denominator-blowup footgun) and fixed-Posit (doesn't deliver decimal exactness; posits are binary). The owner's **"traits ARE refinement typing"** = the algebra is 100% traits, the type-system integration is thin trait-backed wiring.
- **Lesson (process)**: research-before-deciding paid off twice — the adversarial substrate workflow + the cited decimal-literal research each moved the decision off its initial lean.

**Next**: implement — N0 verification (bridge-soundness + 2 audits) → **N1 exponent-lex + N2 Posit-display** (cheap independent wins; N1 on the DEMO-P1 path) → N3 Float → N4 literals → N5 refinement substrate → N6.

### Numerics Stage-3 critique + adjudication (2026-06-30) — design D.2 (commits `de5d0b14`, `5a28dfa0`, `6f843f58`, + this batch)

Ran the methodology's adversarial critique round (`numerics-stage3-critique`: P/R/M/S + completeness, HEAD-pinned, verified-against-source) → collaborative owner adjudication. **The cheap spine (N1/N3) verified sound; the critique caught real problems in the doc** (it earned its keep):
- **N5 reframed to FUNCTION-LEVEL** (accepted): numeric typing is plain imperative fns (`typing-core.rkt:186,789-833,2419-2428`), so the meta-domain + Galois-bridge framing was over-engineered. Refinement = an erased inference-time attribute, function-level; subsumption decomposed so refined types never reach `subtype-lattice-merge`. NO meta-domain/bridge/universe-migration/N0-bridge-gate. **Labeled scaffolding** (owner: "anything not fully on-network is scaffolding") → future-PPN retirement note `2026-06-30_NUMERICS_ON_NETWORK_PPN_NOTE.md`.
- **Sign was overstated as "already built"** → it's a 5-element flat lattice; the 8-element powerset + transfer + Int↔Sign are net-new; **decision: REPLACE the 5-element Sign** (migrate the Interval-analysis consumers/tests).
- **Census broadened** (grep the 5 names, not just edges; fix the `NonZero⊃Zero` classifier bug; break the `abstract-domains`→`refined-int` circular dep). **N4 = parse-time**. **D8 bare-integer polymorphism DEFERRED** (integers absorb via numeric-join; decimals are trapped in Posit → only decimals need D). **N2 split** N2a/N2b. Minors folded; push-backs rejected (⊤-widening is sound; scope-creep owner-locked).
- All captured in the design doc **§15 (D.2 adjudication, supersedes the body where noted)** + memory [[numerics-track]].
- Spawned: **UCS track 5** (traits-as-refinement / `Type@property` surface) + the **future-PPN** on-network note.
- **Process lesson**: the critique's meta-finding — the design under-counted by enumerating from its own framing, not the code (the *capture-gap* failure mode, 4 lenses). Correction adopted: re-ground every "reuse/already-built/cheap/only-consumer" claim against grep before implementing.

---

## Numerics N1 — exponent lexing ✅ (commit `34973d5c`)

First production code on the Numerics track. Stage-4 methodology end-to-end: grounding-audit (`grounding-audit` workflow) → R-lens-verify → co-design 2 scope forks with owner → implement (main-session) → tests → full-suite gate.

- **The grounding-audit earned its keep** (the capture-gap discipline from the §15 meta-rule): the design's "cheap; localized to recognize-decimal-literal/recognize-number (260-303)" framing was REFUTED. A naive lexer edit yields **Posit32**, because (a) the parser's `$decimal-literal`→`surf-approx-literal` chokepoint (`parser.rkt:645-652`) discards the exact value for ANY dotted lexeme, and (b) the `number` value path lacked `#e` (→ inexact→Posit32 fallback). The critic enumerated 6 sites across 2 files.
- **Cleaner approach found** (owner picked it): ONE new `recognize-exp-literal` (priority 97, classified `number`) matching `[-]?digit+(.digit+)?[eE][+-]?digit+` — fires only when an exponent is present, converts via `#e` to a PLAIN exact datum → existing exact dispatcher → `1e10`→Int, `1.5e-3`→Rat, `-1.5e-3`→Rat. Bare `3.14`→Posit32 + the parser chokepoint UNTOUCHED (N4). No new token type, no parser change; the "only fires with an exponent" guard protects `-> -0>` arrows.
- **Owner scope decisions (2 forks)**: (1) dedicated recognizer over extend-3-recognizers; (2) **sexp deferred to N4** — grounding revealed the sexp reader has NO number tokenizer (delegates to Racket, which collapses `1e10`/`3.14` to inexact), so a clean sexp fix needs a new custom number reader that front-runs N4; not worth it for the internal IR. WS-only; divergence asserted in the test.
- Tests: `tests/test-exp-literal.rkt` (21 — token / WS round-trip / WS-string eval / L3 `process-file` + arrow non-regression). **Full suite green 8415** (was 8394; +21). *Lesson surfaced*: `process-file` is not test-fixture-safe (pollutes global context-cell state) — the L3 test must run LAST in its module.
- **DEMO P1 now needs only N3 (Float).** N1 + N3 = the unblock.

---

## Numerics N3a — Float core (type + value) ✅ (commit `b963eff1`)

Opened N3 (Float primitive) Stage-4: grounding-audit (`grounding-audit` workflow, 5 facets + completeness critic, 715K tok) → R-lens-verify → co-design 3 forks with owner → implement N3a → tests → full-suite gate.

- **Grounding-audit (HEAD `90d3d94c`)**: Float is native IEEE → `float-impl` delegates to Racket flonums (<100 lines vs posit-impl's **525**, not orientation's miscounted 135). Heavy cost = AST pipeline (17 ops × full chain) ×2 widths + **conversions** (`From`/`TryFrom` each backed by a ~5-file prim-op; Q11's explicit Posit↔Float increases it). Design clause-counts under-counted ~30% (capture-gap). Clean fits: **Q11 no-Posit↔Float-join = ABSENCE of a numeric-join arm**; `Float32<:Float64` = +1 subtype edge.
- **Owner decisions (3 forks)**: Float-complete-first; exact+Float PRESERVES the Float operand's width (Int+Float32→Float32, not the posit clamp-to-≥P32); bare `3.14` STAYS Posit32 (additive; N4 reconciles).
- **N3a shipped**: `expr-Float32/64` types + `expr-float32/64` values (val = native flonum; +nan.0/+inf.0 valid, no range check) across 12 always-touch files. `Float32 : [Type 0]` at L3; values type-check + pretty-print `[float32 …]`. `tests/test-float-core.rkt` (5). Suite green **8420** (+5). NO float-impl yet (raw flonum; arithmetic + single-precision rounding = N3b).
- **Next = N3b** (arithmetic ops + `float-impl.rkt`).

---

## Numerics N3b — Float arithmetic + comparison ops ✅ (commit `db816e65`)

Stage-4 per the methodology: grounding-audit (`grounding-audit` workflow, 5 facets + completeness critic, HEAD `b0d21b05`, ~737K tok) → R-lens-verify (surgical greps) → co-design 2 forks with owner → implement (main-session) → tests → full-suite gate.

- **Grounding-audit headline capture-gap**: the "<100-line float-impl" framing was the SMALL part. N3a added type+value structs only (`syntax.rkt:526` "Ops … added in later N3 sub-phases"); posit ops are **per-op-per-width AST structs** (15/width: 10 N3b ops + 4 conversions + `if-nar`). So N3b = **20 new op AST nodes** (10 × f32/f64), each across ~13 always-touch files. The **typing-propagators per-op registration** (`register-binary-ops!`/`register-unary-ops!`, :2198) was the easily-missed site (it uses accessors, not match arms — my first grep missed it). All `racket/flonum` delegations verified empirically (`fl=` → NaN≠NaN; `flsingle` → single-round).
- **Owner forks (2)**: op surface naming = **per-width `f32+`/`f64+`** (mirror posit); sequencing = **both widths in one pass**.
- **Shipped**: NEW `float-impl.rkt` (flonum delegation, ~60 lines; f64 plain, f32 rounds each result via `flsingle`; neg via `(fl* -1.0 a)`); 20 op structs + provides + `expr?` predicates; substitution/zonk(×3 fns)/pretty-print(×2)/reduction(`reduce-float-binary`/`-unary` helpers + iota fast-path + stuck + nf)/typing-core/qtt/pnet-serialize/typing-propagators + surface keywords/parse-rules/surf-structs/elaborator/macros builtin-names. Conversions (from-int/from-rat/to-rat) deferred to N3e; no `if-nar` analog (NaN handled in compare/eq). `tests/test-float-ops.rkt` (7 cases: f64/f32 arithmetic, `flsingle` rounding, IEEE specials NaN≠NaN/-0.0/±inf, comparisons, typing incl. mixed-width error, WS-L3 over Float-ascribed lambda params). Suite green **8427** (+7).
- **Lesson reaffirmed**: the grounding-audit + capture-gap discipline earned its keep again — the per-op AST-node footprint (~20 nodes × 13 files) and the typing-propagators registration site would both have been under-counted by the design's "small float-impl" framing.
- **Next = N3c** (`f`/`f32`/`f64` literals — mirror N1 exp-literal recognizer; gives Float values a surface form → enables full L3 compute tests, currently struct-level only).

---

## ⏸ RESUME-STATE CHECKPOINT (Numerics track) — read first after rewind

**Where we are**: Numerics **Stage-3 D.2** complete (critiqued + adjudicated). **N1 (exponent lexing) ✅ DONE** — first production code (commit `34973d5c`; full suite green **8415**, was 8394 +21). The grounding-audit caught a capture-gap (the "cheap 2-recognizer" framing under-counted; a dedicated `recognize-exp-literal` was the clean answer). WS-only; sexp→N4. **N3 Float = IN PROGRESS, complete-first: N3a ✅** (`b963eff1`) **+ N3b (arithmetic/compare ops + float-impl.rkt) ✅** (`db816e65`; 15 files; suite green **8427**). Decisions: Float-complete-first; exact+Float preserves Float width; bare 3.14 stays Posit32. Next = N3c (`f` literals).

**Warm up by reading (in order)**:
1. This checkpoint + the **"Numerics N3a — Float core" entry** above (and the N1 entry).
2. **Design doc** `docs/tracking/2026-06-30_NUMERICS_TRACK_STAGE3_DESIGN.md` — **§2 tracker** (N1 ✅, N3 🔄 N3a ✅) + **§9 N3** (grounding-audit findings + the N3a–g sub-phase plan + the 3 owner decisions) + **§15 (D.2 adjudication)** which supersedes the D.1 body.
3. Charter `2026-06-30_NUMERICS_TRACK_CHARTER.md`; spawned notes `2026-06-30_TRAITS_AS_REFINEMENT_TYPING_NOTE.md` (UCS), `2026-06-30_NUMERICS_ON_NETWORK_PPN_NOTE.md` (PPN).
4. Memory: [[numerics-track]] (full decision record + the N3a STATUS block), [[demo-dependency-resolver-track]].
5. `git log --oneline d7f9daf..HEAD` — incl. the real production commits `feat(Numerics N1)` `34973d5c` + `feat(Numerics N3a)` `b963eff1`.
6. Prior art the design builds on: `2026-02-19_NUMERICS_TOWER_ROADMAP.md`, `2026-02-22_NUMERICS_ERGONOMICS_AUDIT.org`.

**SETTLED — do NOT re-litigate** (the whole design): foundation = Galois abstract-domain × disposition B; Q1–Q11 (Clusters 1–4); the D.2 adjudication (N5 function-level + scaffolding-labeled; Sign→8-element replace; D8 defer; N2a/N2b; the push-backs). The substrate decision + the literal/display scheme + the Float-as-rank-family are all closed.

**NEXT — IMPLEMENT**: **N1 ✅** + **N3a ✅** (`b963eff1`) + **N3b ✅** (Float arithmetic/compare ops + `float-impl.rkt`, commit `db816e65`; suite 8427). **N3 in progress, complete-first** (owner): → **N3c** `f`/`f32`/`f64` literals (mirror N1 exp-literal) → **N3d** numeric-tower (3rd numeric-join family + Float32<:Float64) → **N3e** conversions + prim-ops (**Rat→Float = DEMO-P1-critical**) → **N3f** FFI flonum↔Float (NaN/Inf round-trip) → **N3g** tests (mirror test-posit32). **DEMO P1 unblocks after Rat→Float (N3e).** Cheap parallel wins still available: **N2a** posit-display. **N0** (2 broadened audits, **NO bridge-gate**) gates **N5** (function-level refinement). Then **N4** context-typed literals, **N6** ergonomics. Per the §15 meta-rule, grep-verify every "reuse/cheap" claim before each sub-phase — N1 + N3a both validated it (the audits caught real under-counts: the Posit32 chokepoint, the ~30% clause under-count + conversions).

**N3b implementation guide** (key locations to minimize re-grep; mirror the **Posit32 op template ×2 widths f32/f64**, delegating compute to native Racket flonums — verify line numbers, they drift):
- **NEW `float-impl.rkt`** (the audit-verified flonum delegation; <100 lines vs posit-impl's 525): `f64-add = fl+`, `-sub/-mul/-div`, `-neg = fl-`, `-abs = flabs`, `-sqrt = flsqrt`, `-lt/-le/-eq = fl</fl<=/fl=` (native `fl=` already gives **NaN≠NaN**). **Float32 ops round via `(flsingle …)`** (`require racket/flonum`) so f32 results stay single-precision; f64 = plain. NaN/±Inf = native `+nan.0/+inf.0/-inf.0`.
- **N3b ops = the 10 arithmetic/compare** (f+/f-/f*/f// + neg/abs/sqrt + lt/le/eq). NOT N3b: from-nat/to-rat/from-rat/from-int (conversions → N3e); posit `if-nar` has no Float analog (Float NaN handled in compare/eq directly).
- **Per-op chain to mirror (×2 widths)**: surf struct `surface-syntax.rkt:698-722` (+ provides `:155-156`); parser case w/ `check-arity` `parser.rkt:1460-1557`; elaborator `elaborator.rkt:1607-1682`; expr struct `syntax.rkt:481-495` (+ provides `:123-135` + the `expr?` list `:1069`); typing-core `typing-core.rkt:990-1146` (binary `F→F→F`, compare `→Bool`); qtt `qtt.rkt:815+` (with usage); **reduction TWO layers** — iota fast-path `reduction.rkt:1737` (`[(expr-f32-add (expr-float32 a) (expr-float32 b)) (expr-float32 (f32-add a b))]`) + stuck-term `:1787` (a `reduce-*-binary` helper reduces args first); on-network typing-rule register `typing-propagators.rkt:2198-2212`.
- **Op surface-name fork (decide at N3b mini-design w/ owner)**: explicit per-width `f32+`/`f64+` (mirror `p32+`) vs a width-inferred `f+`. Generic `+ - * /` on floats is N3d (numeric-join) territory, not N3b.
- **Discipline**: open N3b with a per-sub-phase **grounding-audit / R-lens** of the above (line numbers drift; the design's per-file counts under-count ~30% → capture-gap-grep before claiming "cheap"); implement main-session; `check-parens` + `raco make driver.rkt` + targeted `test-float32/64.rkt` (mirror `test-posit32.rkt`, swap IEEE constants) + **full-suite gate** (was **8420**); 5-step completion checklist. `process-file` is NOT test-fixture-safe → its L3 test runs LAST in a module.

**Working tree**: owner WIP untouched (`lib/examples/foray.prologos`, standups, settings); the demo `solve`-unwrap is committed. Don't blind-commit owner files.
