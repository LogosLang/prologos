# Dailies — 2026-03-26

## Carried Forward from Previous Interval

### PM Track 10B: COMPLETE (WS-A 7/9, WS-B B1)
- Network-always, freeze rename, id-map elimination, scoping audit
- PUnify toggle: systemic regression → dedicated SRE track
- Session meta cell-id delivered. Zonk elimination → SRE 2C.
- **134s suite, 376/376**

### PPN Track 0: COMPLETE
- 4 lattice domains, 6 bridge/exchange specs, 57 tests
- NTT extensions (`:set-once` kind, parse stratification)
- **150.3s suite, 379/379 (7421 tests)**
- PIR: 0 wrong assumptions. Research investment → smoothest track.

### Research Program Established
- PRN Master (theory) + PPN Master (application, 10 tracks)
- 3 research documents: Lattice Foundations, Kan/ATMS/GFP, Tropical Optimization
- Key insights: bridge/exchange distinction, bilattice parsing (novel), residuation = propagator waiting, 4-level search (ATMS→LKan→RKan→tropical), "parsing is not special"

### Known Gaps / Blockers
- SRE Track 2C: cell refs in expressions (zonk elimination prerequisite)
- SRE Track PUnify: systemic parity bugs
- PM Track 10C: per-test scheduling via Places
- 5 syntax gaps in acceptance file (> in identifiers, PVec literal, trait method format, zonk on trait dict, bundle crash)
- "Vision contaminates audit" at 6 instances — approaching codification

### Deferred Work
- PPN Track 5: bilattice (elimination ordering)
- Batch worker simplification (needs registries as cells)
- Zonk.rkt deletion (~1300 lines, after SRE 2C)

### Watching (medium-term patterns)
- **Research investment → implementation smoothness**: PPN T0 = 3 research docs, 0 wrong assumptions, 2 trivial bugs. Prior tracks: 0-1 research docs, 2-5 wrong assumptions, 3-12 bugs. Two data points. Watch for 3rd.
- **Bridge/exchange distinction recurs**: emerged in PPN T0. Will it apply in SRE 2C, PReductions? The distinction between domain-crossing and strata-crossing is architectural, not local.
- **"Parsing is not special"**: Same mechanism (lattice + propagator + bridge) for type inference and parsing. Will it hold for reduction? Session checking? If 4+ systems use the same mechanism, it's a PRN universal.

---

## Lessons Distillation Check

### Scanned: PPN Track 0 PIR + PM Track 10B PIR + March 24-26 dailies

**Candidates for principles documents**:

1. **"Research investment → implementation smoothness"** — 2 instances (PPN T0: 3 docs → 0 bugs; PM T10: 0 docs → 12+ bugs). Pattern is suggestive but not yet confirmed. Need 3rd instance. **Status**: Watching.

2. **"Infrastructure tracks deliver substrate; system migration tracks deliver behavior change"** — from PM T10B PIR. 2 instances (Track 8 B5 → CIU T3; Track 10B B2 → SRE 2C). **Status**: Watching, 1 more instance confirms.

3. **"Vision contaminates audit"** — 6 instances. **READY FOR CODIFICATION** in DESIGN_METHODOLOGY.org. The pattern: combined Stage 2/3 format lets the design vision contaminate the audit section. Fix: Stage 2 audit must have concrete code measurements (grep counts, call sites), not estimates from architectural vision. **Action**: Codify now.

**Actions for this interval**: Codify pattern #3 if time permits.

---

## Today's Work

### Completed

- **PPN Track 0 COMPLETE** — all 9 phases. 57 tests, 4 lattices, 6 bridges. PIR written. 0 wrong assumptions.
- **Grammar toplevel form research** (`d9ffe51`) — Stage 0 vision: multi-view spec (parse/type/reduce/SRE/display/serialize/tooling). DPO structural preservation by theorem. PPN Track 3.5 added to roadmap.
- **PPN Track 1 Stage 2 audit** (`0f6fbc1`) — reader.rkt: 1898 lines, 54 functions, 10 context-sensitive decisions, 50 consumers. Two tightly coupled subsystems.
- **PPN Track 1 D.1** (`edec500`) — initial design: 3 subsystems, 11 phases, API wrappers
- **PPN Track 1 D.2** (`5d73aac`) — Completeness revision: constraint-based tree construction. Indent resolver as propagator chain. O(1) incremental editing from day one. Content-addressed cells.
- **PPN Track 1 D.2b** (`82a3ff1`) — primary tree API + compatibility datum API. Two-level golden tests (topology + datums).
- **PRN/PPN Master documents** + master roadmap updated with all series
- **Lattice Foundations addendum** (§11): GFP bilattice, 4-level search, demand lattice, NF-Narrowing as strategy
- **Kan/ATMS/GFP research note**: Right Kan = NF-Narrowing demands, residuation = propagator waiting
- **Pre-0 benchmarks** (`b076359`) — tokenizer, structure, constraint chain, golden, e2e, hashing
- **PPN Track 1 D.2-D.9** — 9 design iterations: constraint-based → Propagator Only → 5-cell architecture → RRB embedded lattices → tree M-type → set-narrowing tokens → prefix-scan monoid → termination proof
- **Tree rewriting = structural unification research** (`88d54e2`) — 4th SRE relation type
- **Parallel propagator scheduling research** (`33bf91e`) — topological-sort rounds, prefix scan, array programming
- **PPN Track 1 Phase 0** (`f677847`) — golden baseline: 110/110 files, 4 levels, 0 failures
- **PPN Track 1 Phase 1a** (`8b7757c`) — character + indent RRB embedded cells. 21 tests.
- **PPN Track 1 Phase 1b** (`5819465`) — tokenizer: 18 patterns, set-of-types. 15 tests (36 total).
- **PPN Track 1 Phase 1c+1d** (`9af5b2d`) — bracket-depth RRB + tree-builder. parse-tree-node with RRB children. Parent from indent constraint + bracket suppression. 8 tests (44 total). **ALL 5 PARSE DOMAINS OPERATIONAL.**
- **PPN Track 1 Phase 1e** (`006ffec`) — context disambiguator + full parse pipeline. `disambiguate-tokens`: set-narrowing for ambiguous tokens via bracket-depth context. Fixed bd-before bug (post-processing depth means closing delimiters see depth 0; must check i-1). `parse-string-to-cells`: complete 5-domain pipeline with ≤2-round disambiguation cycle. 7 new tests (51 total).
- **PPN Track 1 Phase 1f** (`6df01a9`) — integration gate PASSED. Topology comparison across all 110 .prologos files. Zero mismatches. 7 new tests (58 total).
- **PPN Track 1 Phase 2** (`a046558`) — reader macro token patterns. 17 new recognizers. Priority fix: quote-lbracket > char-lit > quote. 17 new tests (75 total).
- **PPN Track 1 Phase 3a+3b** (`33bfcb1`) — Read API + Write API. 9 functions total. 12 new tests (87 total).
- **PPN Track 1 Phase 3c** (`65fa3dd`) — Compatibility wrappers. compat-token struct, compat-tokenize-string. 7 new tests (94 total).
- **PPN Track 1 Phase 4** (`801e9d1`) — Golden comparison: topology 110/110, brackets 110/110. Datum/srcloc deferred to parser integration. 3 new tests (97 total).
- **Full suite: 7518 tests, 380 files, 146.8s — zero failures.** No regressions from PPN Track 1 implementation.
- **PPN Track 1 Phase 5a** (`a2dd08a`) — datum extraction: tree → syntax objects. Bracket grouping, sentinels, string escapes, :: paths. 41/72 files (57%). 11 new tests (108 total).
- **PPN Track 1 Phase 5b-i** (`f4368d7`) — tokenizer gaps: nat-literal, :0/:w, -N>, +>, ?/!, negative numbers. 47/72 files.
- **PPN Track 1 Phase 5b** (`e6e4d25`) — **72/72 library files match (100%).** Three bugs found via diagnostic protocol: indent-close truthiness (25→69), colon-annotation lookahead (69→70), >> compose ambiguity (71→72).
- **PPN Track 1 Phase 5c** (`bac32cd`) — new reader wired into driver.rkt with parallel validation mode. 7529 tests, zero mismatches in validation. Switchover attempt: 25 failures → diagnostic protocol → 6 tokenizer fixes (`5fb49e4`) → 18 failures → sentinel fixes (`7952943`). Remaining: session compound operators (`&>`, `!:`, `?:`), char literal WS divergence (correct), postfix/mixfix deep patterns. Library files: 72/72 correct. Approach: continue adding tokenizer patterns until 380/380 green.

---

## Considerations

- PPN Track 1 (lexer as propagators) is the natural next step
- Priority question: PPN 1-4 in sequence, or interleave with 10C?
- The acceptance file syntax gaps are PPN Track 7 motivation — but some (> in identifiers) could be quick reader fixes that improve development velocity now
