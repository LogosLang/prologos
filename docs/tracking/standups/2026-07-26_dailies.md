# Dailies — 2026-07-26

Rolled from `2026-07-24_dailies.md` (219 lines — under the size cap, but a clean
**track shift**: Rel Track 1 closed ✅ with its PIR + Batches A–D, and the work is
now **CIU Track 6 — Path Selection**, a DESIGN-first track). Prior file is
read-only history; its LOG carries the SUB close, the POL roster, X.close and the
four post-PIR batches. STATE head below is SEEDED (copy-forward per
`DAILIES_METHODOLOGY.org`) and refreshed to current.

---

## 📍 STATE  (overwrite in place — always current)

- **HEAD** `dc5d4d85` — **MERGED TO LOCAL MAIN** (fast-forward; `main` == this branch) · **Suite** GREEN **473 files / 9190 / 0 measured IN THE MAIN CHECKOUT** (worktree agreed at 9185) · acceptance 21/21 + 29/29 · **`.pnet` is v4** — every cache regenerated on first run, which is the deliberate sweep of the pre-fix POISONED caches · **Working tree**: owner WIP only (41 entries, untouched by the merge — zero overlap with the 20 files it updated) — stage ONLY my files, **verify with `git diff --cached --name-status`**, NO Co-Authored-By, **NEVER `git stash`**.
- **Where we are**: **CIU T6 Path Selection — Stage-3 D.2 + the D.3 critique cycle COMPLETE** (`docs/tracking/2026-07-26_CIU_T6_PATH_SELECTION_DESIGN.md`). Grounding audit (6 facets + completeness critic @ `fe03b493`) + a 33-system prior-art survey are DONE and folded in; every load-bearing claim was R-lens-verified in the main session before it entered the doc. **The surface co-design ran five rounds and SETTLED (D.2)** — the consolidated spec is §5.9 PS1–PS15; §5.4–§5.8 carry the conversation record and rationale.
- **NEXT**: **implement P2.b** (ruled (c) — the two-tier principle, spec §5.10 round 7; failing-test-first; the 2 test-map pins flip; strictness-mark mechanism gets its own mini-audit). Fresh session seeds from `handoffs/2026-07-26_CIU_T6_PATH_SELECTION_IMPL_handoff.md`. Then P3 → P4.a-d → PX → P5 → P6 → X.close. P0 ✅ · P1 ✅ · P2.a ✅ · ⚠ concurrent chip session on parse-reader.rkt (task_43ece3f8). **Surface = §5.9 PS1–PS15 + the §5.10 D.3 amendment deltas (deltas WIN)**. D.3 critique ran and is FULLY ADJUDICATED (`9aff6b82` + `7a701587`): 2 BLOCKING accepted (B1 miss-by-negative-information incl. Option-shaped Map miss; B2 dot-sections WITHDRAWN → **keyword-projection `map :name users`** [owner], zero reader work); splat STAYS w/ contributions-keying + `^_`-derive-all remedy; `v.i` kept as cuttable P4.d; `m[:a]` retires outright; **new phase PX** = the lambda-adoption substrate bug (`[the [List String] [map [fn [x] x] ints]]` accepts silently — owner: in-track, not to roll unnoticed).
- **#78 (PM T10 `.pnet` registry restore) is DONE and merged**: P0–P4 + bench check + DEFERRED triage (PIR skipped, owner). `.pnet` v4. Remaining: reply on the issue + close. Design `2026-07-27_PM_T10_PNET_REGISTRY_RESTORE_DEFECT_DESIGN.md`; Option C routed to `2026-07-27_PM_TRACK12_REGISTRY_READ_PATH_NOTE.md`.
- **Open / blockers**: none blocking. Owner ruled **Q_P5** (`.{` retires fully, clean switchover, NO deprecation/diagnostic path). Q_P1/P2/P3 fold into the surface conversation; Q_P4/P6 (scope, series supersession) are proposed in doc §9 awaiting ratification.
- **Watching**:
  1. **Suite test-count is NONDETERMINISTIC** — the gate is **0 failures across 471 files**, never the count.
  2. **Probing before ruling changes the RULING** — now at a **4th data point and the strongest yet**: the design doc's own recorded broadcast PREREQUISITE was REFUTED by probe (both named shapes work, incl. over typed solve rows). Threshold reached → promote at the next close per the two-tier rule.
  3. **The design's own enumeration under-counts** — the completeness critic found the V4 crux that *no facet* reported (shipped branched `get-in` is width-erased AND test-pinned). Confirms the grounding-audit-as-default-opener rule; a 5th confirming arc.
  4. **A recorded repro can be a red herring** (Batch C shape, recurring): the broadcast prerequisite's *stated mechanism* ("the element type doesn't push through `map`") was wrong — it does push through; the real boundary is bidirectional checking.
  5. **"The pattern already existed"** — `^`-glued symbols need splitting, and POL.6 already built `split-fused-symbol`/`fused-type-annot?` for `x:Int`. Reach for it; don't write a second splitter (the `recognize-keyword` drift class).

---

## LOG  (append per commit / decision / surprise — newest at the BOTTOM)

### New dailies rolled on the track shift  (2026-07-26, docs)

- **Why**: Rel T1 is ✅ COMPLETE (PIR + X.close + Batches A–D); the work is now CIU T6 Path Selection. Track shift is an explicit roll trigger even under the size cap.
- **Carried forward**: the staging/stash disciplines, the nondeterministic-count gate, and the probe-before-ruling entry (which reached promotion threshold this session).

### Path Selection design OPENED — grounding + prior art + Stage-3 D.1 (`7e70151f`)  (2026-07-26)

Two workflows in parallel (grounding audit `wf_a62d4b88-47b`, 6 facets + critic; prior-art survey `wf_c1acb1c7-ce1`, 33 systems / 5 families), then main-session R-lens verification of every load-bearing claim before anything entered the doc.

- **⭐ The headline: V4 is NOT a blank slate.** Branched multi-select `(get-in n :a.{x y})` **already works in sexp mode** (dead only in WS) and its result types as `[Map Keyword V]` — width-erased, not a row — and **the suite PINS the degraded type** (`test-path-expressions.rkt:193` asserts the string "Map"). The fixture's own preamble concedes why (`:101` "homogeneous maps (avoid Peano union type issues)"), and heterogeneous selections fail outright. **Root cause is ONE local seed choice**: `elaborator.rkt:2301` seeds `expr-map-empty` with two fresh metas, missing the row arm at `typing-core.rkt:1806` whose own comment says the literal seed is its only source. So the design supersedes an existing semantics, one seed from structural rows — a far better starting position than "invent a result shape."
- **The predecessor's own V4 lever is refuted by shipped code**: `{a.{b}}` was to preserve one level; probed, it flattens (`:loc` erased) because `expand-brace-branches` flattens before `(last path)` keys the result.
- **⭐ The recorded broadcast PREREQUISITE is refuted** (found on the main thread before the workflows returned; the facet assigned to it later died on a connection error, so this was the load-bearing one). `[map [fn [r] r.field] rows]` and `[map [validate Person _] rows]` **both work**, over closed rows, dyn rows, schema-typed lists, inline literals, AND Rel T1 typed solve output. The *stated mechanism* was also wrong — the element type DOES push through `map`. The real boundary is **bidirectional checking**: a lambda in ARGUMENT position gets its domain from `map`'s spec; `def f := [fn [p] p.name]` standalone and `defn f [p] p.x` do not. **Broadcast is unblocked** — it is now a design choice, not a gated one. Provenance (fixed by Rel T1, or mis-recorded originally) is unresolved and would need a worktree bisect; the actionable fact is that it works.
- **The reader already does most of §5.3's work**: `v[1.b]` reads as a structured `($postfix-index (1 ($dot-access b)))`. The failure is 100% downstream — `macros.rkt:2488` re-entrantly preparses the payload so the generic dot-fold turns it into `(map-get 1 :b)`. **The lift is one fold arm + stopping the re-entrancy.** But **every `.N` mis-lexes**: `x.1` → a bare `.` symbol, and `x.1.2` → the **rational 6/5**. The reader supports exactly ONE direction of mixed keyword/positional paths (nat→field yes, field→nat no).
- **Prerequisite defects found**: `record-project`'s dynamic leg is Nat-only (`typing-core.rkt:563`) while the literal leg 6 lines above is Nat-or-Int *with a comment saying it mirrors `expr-get`'s gate* — so `def i := 1` (Int by our own convention) fails as a tuple index. PVec OOB returns `<error> : Int` **silently, 0 errors**. Miss semantics are four-way inconsistent.
- **Two Exhaustive-Walker gaps under `expr-broadcast-get`**: it occurs exactly twice in `reduction.rkt`, both in the `nf` family (no whnf arm), and is **absent from `definitely-not-map?`** where its two path siblings were explicitly added by the 2026-07-16 value-loss fix. Plus `ground-expr?` in `global-constraints.rkt` is 14 arms + `[_ #t]` with no `expr-meta` arm, while its same-named twin in `trait-resolution.rkt` IS meta-aware.
- **Separable soundness defect filed**: `[map-size 5] : Nat` and `[map-size "hello"] : Nat`, both 0 errors — a constant-return typing rule (`typing-propagators.rkt:2372`) that never checks its subject, winning via on-network-first dispatch (`driver.rkt:1786`). This also empirically settles DEFERRED's open selection question: `[map-size v]` leaks full parent width (`2N`) while `v.age` errors — selection is **inconsistent, not "strict."**
- **Owner ruling Q_P5**: `.{` repaired and retired FULLY — clean switchover, no deprecation or diagnostic path. It is a **live regression today**: four audit examples use it as leading mixfix and `audit-09-numerics` **aborts fatally** (raw Racket `error`, zero output).
- **Prior art's sharpest rulings**: result shape must dispatch on the selector's KIND, never its ARITY (λ⟨⟩ TyDe'23; R and JSONPath are the canonical disasters) · the mirror principle ships but has never sufficed alone (GraphQL/Datomic both bolted on rename, a static collision rule, a key-order rule, synthetic nesting) · **EQL's spec concedes the hole our row types close** · broadcast must be explicit *because* unifying Array and Map deletes the discriminator · **`coll.[…]` is contested territory — Julia #19169 open since 2016** · and **typed deep multi-select with a result shape does not exist anywhere** (five typed/config teams declined; Dhall #771 open) — so this lands in the core elaborator with no principal-typing result to lean on.
- **Next**: the surface conversation (doc §5.2's six challenges), then D.2. P1 can land in parallel.

### Surface round 1 RULED — Q_S1–Q_S6 recorded (`403866d0`)  (2026-07-26, docs)

- **Rulings**: bare names as selector keys (dynamic-index spelling DEFERRED; `get`/`get-in` stay the escape hatch) · `x[a]` is ALWAYS `{:a …}`, redundant `x[a^]` ≡ `x.a` accepted · selectors are FIRST-CLASS (`my-selector m` + `map my-selector m`; `[…]`/`.[…]` = the anonymous selector, akin to `fn`/`rel`) · key collision = static error with a rename-pointing diagnostic · miss = loud/type error with rich diagnostics.
- **Grounded during the round**: `*`/`**` history found (2026-03-02 SCHEMA_SELECTION §4.4 — `*` level-wildcard, `**` globstar, `:address` ≡ `:address.*`); first-class Path probed at HEAD — `#p(a.x) : Path` constructs (incl. `*`/`**` literals) and `get-in` evaluates it, but a Path is NOT applicable, its result types as a leaked meta, and wildcard get-in yields a silent `<error>`.
- **Surprise worth naming**: working the algebra uniformly surfaced that the settled Q_S3 rule (`x[a]` always keeps the key) collides with the SHIPPED F1a-col degenerate case — by uniformity `v[0]` would be `⟨elem⟩`, not the element, moving element access to `v.0` (which today MIS-LEXES). Q_S7, owner to rule; re-opens a locked F1a-col composition (`v[1].b`).
- **Next**: round 2 (Q_S7/Q_S8/Q_S9), then fold to D.2.

### Surface round 2 RULED — Q_S7a/Q_S8/Q_S9 + the positions-renumber breakthrough (`4bf3b585`)  (2026-07-26, docs)

- **Q_S7 RULED (a)**: the uniform law wins — `v[…]` always selects, `v.0` extracts (new `.N` lexing, census owed); re-targets the shipped F1a-col degenerate case, clean switchover. **Q_S8**: `^_` was placeholder notation, but the misread is ADOPTED — `^_` = rename-to-derived-key, the collision remedy. **Q_S9 RULED**: selector-as-lambda is v1 first-classness.
- **The remaining owner unease ("`admins.0^[…]` is unsatisfying; `v[1 5]` should be a two-tuple") dissolved structurally**: the carrier's dense-prefix invariant FORBIDS sparse nat rows, so nat-keyed selections MUST renumber — `v[1 5]` → `⟨v.1 v.5⟩` with no `^`, because in the nat domain elision is structural (positions are not identities; pvec slice/concat already renumber). And the satisfying spelling for the 0-key case is dot-descend-then-select: `app-config.admins.0[name^ role^]`. Proposed as PR-1..PR-4 (§5.6) incl. the names-vs-numbers asymmetry that makes `v.i` dynamic indexing sound (a name segment on a nat-keyed subject can only be a variable).
- **Next**: owner rules on PR-1..PR-4, then D.2 folds the surface; P1 (`.{` retirement) remains landable in parallel.

### Surface round 3 — the `*`-segment recast proposed (`caa406ec`)  (2026-07-26, docs)

- **Owner named the impurity**: `.[…]` broadcast makes `.` mean a third thing (iterate) when the algebra rests on dot = extract, bracket = keep. Asked for a different broadcast operator and a first-principles needs pass.
- **The recast (PR-5..PR-8, design §5.7)**: `*` as a PATH SEGMENT is the iteration operator — the owner's own 2026-03-02 vocabulary (`depts.*.projects`), and the shipped `.*name` broadcast generalized (`admins.*name` ≡ `admins.*.name`). All four result quadrants get minimal spellings with zero `^`; keying refines to "last segment before the first structural operator"; `^`-forms become selector-final; `.[…]` is freed to be the anonymous-selector literal (`.name` = the simple section, healing ERGONOMICS `.:name`); stacked broadcast dissolves into plain segments. Cost: the `*`-splat proposal conflicts and is recommended deferred.
- **Open for owner**: PR-5/6/7 adoption · PR-8 mixed-`^` (coerce-to-tuple, owner lean, vs static error) · plus round-3's Q_S10 (brackets fully literal) / Q_S11 (`m[:a]` flip) / `**` reserve.

### Surface round 4 — segment-attached `^` adopted · the `:`-variant · solve modeling (`4a686a03`)  (2026-07-26, docs)

- **Owner's `x[admins^.*[name] features^]` placement ADOPTED**: `^`-forms attach to the KEY-GENERATING segment (supersedes the selector-final corollary; pure paths coincide). Lexing verified (`admins^` glues, chains).
- **PR-9**: the owner's `:`-for-iteration instinct decomposed the overload — `*` = "all fields HERE" (splat final + `[*]` identity, the March wildcard meaning), `:` = "along EACH entry" (NumPy/q). Grounded: `.:.`/`.:[` tokens available; ⚠ `m.:name` dot-key is LIVE (probed) — the enabling move is dot-key retirement (census owed), else the one-dot hazard m.:name/m.:.name.
- **Solve modeling**: `rows.:.f` columns · `rows.:[f]` narrowing · `rows.:[f^ c^]` zip · **empty-prefix selectors = named unzip** `rows[:.f^fruits :.c^colors]` — falls out of the keying rule, no new machinery.
- **Next**: owner rules PR-9 (`:` vs `*`) + PR-8 (coerce vs error) + the carried set; then D.2.

### Surface round 5 — PR-8/PR-9 RULED; **D.2: THE SURFACE IS SETTLED** (`0ba5a5d6`)  (2026-07-26, docs)

- **PR-9 ADOPTED**: `:` = iteration ("along each entry" — NumPy/q precedent, owner: "a good indicator we're circling toward a better syntax-design stance"), `*` = wildcard/splat (all-fields-here, both March meanings restored), **dot-key retired** (`m.:name` → `m.name`, census owed). **PR-8 = STATIC ERROR** over coercion — the R rationale recorded in PS5: R's bite was shape-by-runtime-arity; coercion's would be shape-by-non-local-static-scan; the error keeps every selector's contribution locally derivable. Owner accepted with the ergonomics cost named (PR-1 makes nat-enders positional anyway; the all-tuple spelling is one `^` per selector).
- **D.2 landed**: §5.9 consolidates five rounds as **PS1–PS15** (operator core · uniform law · keying · identities-vs-positions · assembly · the `^` family · collision · miss · `*` · dot-only dynamism · selector-as-lambda · retirements · reserved slots · sexp form · subjects). Tracker rebuilt as **P0 (acceptance file) → P1 (`.{`) → P2 (prerequisite defects) → P3 (reader/lexer) → P4 (the node + typing) → P5 (iteration + first-class) → P6 (migrations) → X.close (PIR gates ✅)**. §7 NTT posture resolved: v1 = zero propagators, Network Reality Check stated in advance; the NTT model becomes mandatory at the future broadcast-propagator node. Predecessor doc's OPEN block closed with a pointer.
- **Delegated-adoption items** (owner: "incorporate with your recommendations"), marked ⊳ in the doc: brackets fully literal (Q_S10) · `m[:a]` flip w/ census (Q_S11) · `**` reserved · the corner-case defaults table.
- **Next**: P0 acceptance file, then P1 — implementation begins.

### D.3 critique RAN + FULLY ADJUDICATED — round 6 (`9aff6b82` critique · `7a701587` adjudication)  (2026-07-26)

- **The workflow** (13 agents: P/R/M + SRE + surface red-team + completeness, each with a refute-by-default verifier): 40 raw findings → **2 BLOCKING, 11 SIGNIFICANT, 8 MINOR surviving; 12 refuted with reasons retained**. Six load-bearing findings re-verified on the main thread before adjudication. My §7 zero-propagators suspicion was REFUTED (the Check asks what the track ADDS; expr-map-get is already #f-routed by design) — do-not-churn recorded.
- **D3-B1 (accepted)**: five silent wrong values in one probe file, 0 errors — Map miss `<error> : Int`, a CLOSED row minted over a fabricated value, `map-get`-on-nat-row `none`, PVec + List OOB. PS8 restated: miss semantics = f(the subject's negative information); Map misses become Option-shaped; sealability requires SOURCED presence.
- **D3-B2 (accept-problem, owner replaced the solution)**: `map .name users` never lexed — the fold absorbs the preceding token (`((map-get map :name) users)`); the "probe-verified" annotation was carried across the round-3 re-spelling unre-probed — MY error, and the exact probe-before-ruling failure the Watching entry tracks (5th data point, in the asserting direction). Owner's replacement is BETTER: **keyword-as-projection, `map :name users`** (Clojure idiom) — a check-mode coercion arm, no reader work at all; `[map-get m :name]` keeps the keyword reading (verified).
- **Owner rulings**: `.{` split per D3-S5 (WS repair + TOTAL deletion incl. diagnostics; sexp leg re-points 20 tests at P4) · `v.i` kept as bounded/cuttable P4.d (neutral segment + soft resolution) · splat STAYS (contributions keying restores `*` to the structural-op list; `^_`-on-splat = derive-all = the collision remedy) · Q_S3 RE-RULED on the collection basis (selection returns a sub-tree/forest; `x.a` is the single-value spelling) · **D3-S10 gets phase PX in-track** (node-kind-independent checker unsoundness: concrete-codomain lambda adoption).
- **Also folded**: PS15 + PVec/List; support-boundedness replaces "closed tails" for tail-less subjects; `rows[:f]` ambiguity dissolved by retiring `m[:a]`; P3 grounds on the token-registry inventory (the walker discipline applied to the tokenizer); §9 rewritten against the CIU master (T2 partially re-homed — the live `expr-union` `ground-expr?` gap goes to P2; T3 re-chartered); write-phase headline = source-overlap disjointness; P0 must carry function-typed forms.
- **Next**: P0 (with the function-typed obligation) → P1 (WS leg). The §6 Q4 bridge-diagram re-run lands with D.4.

### P0 LANDED — the acceptance file, 21/21 `--check` green (`f47851a5`)  (2026-07-26)

- **`examples/2026-07-26-ciu-t6-path-selection.prologos`** — ideal syntax throughout [owner charter]: §A the guild-hall `app-config` (three levels, fully-precise row at 0 errors) · §B/§C selection + splat as commented P4 targets incl. the four mandated static errors · §D the party PVec + iteration targets + the PS2 flip pinned LOUD (`party[0].name` marked as changing) · §E functions-over-rows — working HOF pins (`map [fn [m] …] party` → PVec-preserving!), `map [+ 5 _]`, the annotated `add5` hatch, named-schema `veteran?`, the standalone-def family commented [PX] · §F the quest board (defr facts → typed solve rows → HOF composition + paren-goal query) · §G typing seams (def-stored sub-rows, het tuple exactness). At X.close it promotes to a suite-gated test [owner].
- **Round 6b capture (owner data point)**: `def add5 := [+ 5 _]` fails — and probing showed the boundary is the **def-RHS seam itself**: even `[int+ 5 _]` (the #70 hint's OWN recommended form) fails standalone, as does `[fn [r] r.name]`; all work in argument position + annotated. Second diagnostic-recommends-impossible-fix instance. **PX widened** to the binder-seam phase: adoption hole + body-directed standalone-def inference; trait-op case honestly flagged (may need defaulting/residuation → Num T2/F-row).
- **Authoring finds**: `>` is spelled `gt` (angle-bracket conflict — good diagnostic); `map` over a PVec preserves PVec; def-bound solve echo uses hash order (POL.3's named fallback) — all pinned as markers, none a defect.
- **Next**: P1.

### P1 LANDED — `.{` retired completely; audit instrument restored (`d18648f0`)  (2026-07-26)

- **Deleted in one commit** (no dual path; grep = 1 history-note hit): the recognizer, its registration, BOTH grouping arms (the legacy `$mixfix` route had survived in group-tokens alongside the `$mixfix-retired` route — two routes for one dead surface), the error path in macros, and every dead `mixfix-rbrace` closer leg. Post-retirement `.{…}` = generic per-command error, file CONTINUES (was: raw Racket abort, zero output).
- **Surprise (probe-first paid again)**: `<` inside `.( )` SILENTLY SWALLOWS following lines — the form-extent scanner counts `<` as an opener; `mixfix-close?`'s own comment claims both mixfix forms suppress angle grouping but only the group-items leg is wired. Comment-vs-code drift class. **Spawned as its own task** (out of P1 scope); the audit `<`/`<=` lines converted to `lt`/`le` applications with the capability loss NAMED in the commit.
- **Near-miss recorded**: the registration deletion left an arity-broken `(register-token-pattern!)` with BALANCED parens — check-parens structurally cannot catch this class; `raco make` did. Compile-before-test is the real gate.
- **Gates**: targeted battery pass · both acceptance files `--check` green (21/21, 89/89) · **full suite 9150 / 471 / 0**.
- **Next**: P2 (the five B1 miss-legs + walker twins, failing-test-first, censuses from D3-M7).

### P2.a LANDED — Int gate, walker fallbacks, broadcast whnf; P2.b = owner checkpoint (`ad75e57a`)  (2026-07-26)

- **The mini-audit (5 facets) re-scoped the phase before code — again**: (1) the Int-gate fix had a suite-invisible CENSUS HAZARD (pvec-nth's Record leg relied on record-project's Nat-only check; no Int-variable test existed anywhere) — closed in the same commit with both directions pinned. (2) The recorded leg-3 repro was WRONG-SUBJECT: `[map-get {:a 1} :zzz]` on a record is ALREADY loud; the silent class needs a `(Map K V)`-typed subject. (3) PVec OOB is TWO divergent silent legs (expr-get→`<error>` vs pvec-nth→stuck) + two unenumerated siblings (pvec-update, pvec-pop). (4) The `(expr-fvar 'none)` degradation is ONE shared arm serving mixed-type unions by design — scoping matters. (5) A panic-shaped LOUD realization is bounded TOP-NODE-ONLY at the driver seam (nested panics print, count 0) — probe-pinned.
- **Landed failing-test-first (8 failing → 14/14)**: record-project dynamic Nat-or-Int (the PS10 prerequisite — `def i := 1` works as a tuple index) · pvec-nth's own Nat-only dynamic gate · BOTH ground-expr? permissive tails → the generic transparent-struct fallback (`expr-substructs-all?`, syntax.rkt; the D3-S9 union-of-metas defect closed; champ/rrb element descent free via #:transparent; mult/level-metas ruled ground + pinned; struct?-not-expr? gating, 35× trap audit-measured) · normalize-for-resolution union descent (the upstream capture gap) · broadcast-get whnf arm + dnm exemption (minimal scope).
- **Near-miss**: champ tries store entries in raw VECTORS — the helper's first draft missed them and the failing champ pin caught it. Failing-test-first earning its keep literally.
- **Gates**: 12-file neighborhood battery + both acceptance files green; **full suite 9164 / 472 / 0** (+1 file, +14 tests).
- **Next**: **P2.b owner checkpoint** — the loud-miss family realization (the census data is in: Option-wrap ≈ 35-40 tests + 6 stdlib ×2 book twins + ~24 corpus sites with ergonomics ~5:1 against wrapping; keep-V-runtime-LOUD ≈ 2 test pins + the type-fork need + the top-node counting bound; nil-safe-get/nth/kv-get already form the honest-absence tier).

### P2.b RULED (c) — the TWO-TIER PRINCIPLE; handoff written for the fresh session  (2026-07-26, docs)

- **Owner ruled realization (c)**, recorded as design §5.10 round 7 (`eac74bd6`): the ASSERTIVE tier (`map-get`/`.field`/`v[k]`) = a failed runtime lookup is a LOUD, counted, panic-shaped error (elaboration-time strictness mark on Map-typed subjects; dyn-rows keep D19; top-node-bounded, named — the D22 precedent); the HONEST tier (`nil-safe-get`/`nth`/`kv-get`) unchanged. **Option-wrap rejected-with-reason** (census: ~35-40 tests + 6 stdlib ×2 + ~24 corpus sites, ergonomics ~5:1 against, kv-get double-wraps) — the census revised the D3-B1 adjudication while honoring its actual claim ("never a bare V silently"). Doc-truth bonus: the map tutorial already TEACHES error-on-miss 3× — (c) makes the teaching true.
- **Handoff written**: `docs/tracking/handoffs/2026-07-26_CIU_T6_PATH_SELECTION_IMPL_handoff.md` (§1–§6 per protocol) — next session opens at P2.b implementation, failing-test-first. Memory (`ciu-t6-records` + MEMORY.md index) refreshed to the full ladder status. ⚠ noted in the handoff: a CONCURRENT session is running the `<`-in-`.( )` mixfix fix (task_43ece3f8) and may touch parse-reader.rkt.

### Partiality/absence design-space survey — the research note lands (`72168e92`)  (2026-07-27, research conversation)

- **Owner opened the partiality question** (Maybe/Option dissatisfaction · tracked-nil sympathy · Verse's `<decides>` · "a corner not quite expressed"). Six-facet primary-source survey + synthesis → `docs/research/2026-07-27_PARTIALITY_ABSENCE_DESIGN_SURVEY.md` (Stage-0, never gating).
- **Headline findings**: (1) Prologos already occupies ALL SIX design-space positions' substrate; the gap is discharge SURFACE only. (2) **Verse's hardest unfinished problem (transactional failure contexts, Appendix F "preliminary") IS our existing substrate** — worldview speculation + nogood retraction, parallel where theirs is sequential; but `one{}` ≈ `solve-one` is INEXACT (deterministic-first vs bag-pick — do not import ordering without a semideterminism story). (3) Hickey's anti-Option arguments, verified, land ON machinery we built (`<V|Nil>` + contextual requiredness). (4) The P2.b silent-`<error>` episode = the universal untracked-absence failure mode, lived in-house — loud-assertive is settled by EVIDENCE. (5) **The ranked corners**: #1 why-not provenance as first-class values (cheap ONLY on an ATMS — no language has it); #2 context-polymorphic absence (goal/loud/typed-nil chosen by CONTEXT — nobody has the three-way; POL.9's meaning-from-context is the enabling move, already ruled); #3 cardinality inference as a monotone network stratum (Mercury's lattice + Dialyzer's optimism, zero annotations).
- **Forced open rulings recorded in the note**: the `<V|Nil>` nesting collapse (generic containers over nilable elements) · absent-key vs stored-nil in open maps · the foreign-interop nullability boundary (Kotlin's platform-types lesson: do NOT defer).
- **Disposition**: research note only — a future track charter is the owner's call. No language commitment made.

### Mixfix `.( 3N < 5N )` swallow FIXED — extent scanner now agrees with grouping (`31d27c83`)  (2026-07-26)

- **The P1-spawned defect, closed.** Root cause as captured: `make-bracket-depth-rrb` counted EVERY langle as an opener, so operator-`<` inside `.( )` left depth 1 after `)` → every following top-level line silently joined the form. **Fix = layer agreement, not a patch**: the extent scanner now carries a frame stack mirroring group-items' decisions — `<`/`>` inside `.( )` are operators by fiat (nested `( )` = nested mixfix; `[ ]`/`{ }` restore normal context), and elsewhere `<` opens only when `langle-matched?` (the lookahead twin of `has-matching-rangle?`) finds its `>` in scope. Matched angle groups, incl. multi-line, keep continuation behavior.
- **Surprise (scope widened 1 step)**: `[< 3N 5N]` had the SAME silent swallow — the class is "unmatched `<` counted as opener", not mixfix-specific. The matched-lookahead fixes both; `[< …]` now errors LOUDLY (unbound `<`) with following commands intact.
- **Surprise 2 — the 6 suite fails were NOT the fix**: schema/seal/validate/reason family failed with `keyword-name: expected a Keyword value, got #(struct:expr-keyword age)`. Baseline (fix reverted, rebuilt) fails IDENTICALLY. Mechanism verified: `reason.prologos:22` `foreign racket "prologos/keyword-ops"` → `handle-foreign-decl`'s non-`.rkt` branch → `(string->symbol …)` collection path → **dynamic-require of the MAIN checkout's copy** → second compiler instance → its `expr-keyword?` rejects our structs. **3rd data point for the two-instance class, on a NEW surface** (lib `foreign` decl; testing.md's rule covers test files only). Cache ruled out first (`--no-pnet-cache` still fails). Spawned as its own task.
- **Also**: dead legacy `flatten-tokens`/`group-tokens` pair deleted (self-recursive only, superseded by group-items — the drifted comment on `mixfix-close?` was the defect report's "legacy legs"); comment fixed. Audit-06/-09/-12 mixfix comparison lines restored from the `lt`/`le` workaround (06/09 run 0 errors; audit-12's 3 errors are the pre-existing documented quote section, baseline-verified).
- **Tests**: +8 extent-scanner unit pins (116/116) · +7 Level-3 WS pins incl. three no-swallow assertions (36/36). Targeted battery + probes green.
- **Watch note**: a BACKGROUNDED full-suite run stalled (25 files then 600s silence, all 10 workers) while the same suite ran fine foreground — background process-group throttling suspected; prefer foreground for timing-sensitive suite runs.
- **Next**: P2 (unchanged).

### Foreign two-instance defect FIXED — one canonical resolver, both sites (`9303a077`)  (2026-07-27)

- **The spawned task from the mixfix session, closed.** `foreign racket "prologos/X"` resolved via `(string->symbol …)` → installed collection → the MAIN checkout's copy → a second compiler instance whose `expr-keyword?` rejects the running instance's structs. Fix: **`foreign-module-path->require-spec`** (pnet-serialize.rkt) — `"prologos/X"` rewrites to the RUNNING compiler's own `X.rkt`; external collections stay on the collection route; main-checkout behavior unchanged (rewrite resolves to the identical file).
- **Surprise 1 — TWO resolution sites, and fixing one changed NOTHING**: `handle-foreign-decl` (fresh elaboration) AND pnet-serialize's `expr-foreign-fn` re-link (every cached module load) carried the same collection fallback. The no-change-after-fix result is what exposed the second site. Both now call the ONE resolver — the drift-proof shape.
- **Surprise 2 — `--no-pnet-cache` was a NO-OP** (a lying diagnostic switch): batch-worker only acted on env `"1"` while `current-use-pnet-cache?` defaults `#t`, so `"0"` silently kept the cache on — it "ruled out" the cache in the prior session's diagnosis while the cache was in play the whole time. Fixed (explicit `"0"` branch). Meta-lesson recorded: when a flag is load-bearing for a conclusion, verify it actually flips the behavior.
- **Census**: only two lib surfaces used `"prologos/…"` (reason.prologos keyword-ops ×2; core/path path-ops ×6 — latent, same fix). Tests/examples: zero.
- **Gates**: +4 resolver pins in test-foreign.rkt incl. an instance-identity E2E check (57/57) · the six formerly-failing files PASS in the worktree · **full suite 471 / 9165 / 0 in the worktree**.
- **Lesson promoted (3rd sighting, new surface)**: testing.md relative-path entry extended (lib-foreign surface + resolver pointer + the lying-flag trap) + `DEVELOPMENT_LESSONS.org` § "Two Compiler Instances: the Collection-Path Trap".

### GitHub #78 — the `.pnet` registry-restore defect: verified, designed, critiqued, FIXED (P0–P4)  (2026-07-27)

- **Contributor issue #78 (kumavis) is REAL and was still live**, against a 4-day-old main. Independently verified before trusting: restore writes ~19 params with ZERO cell writes while all 24 readers are cell-primary, and because an **empty hasheq is truthy** the param fallback is *unreachable* post-init — not a fallback losing a race, a fallback that can never fire. The sibling 300 lines away (spec-store) dual-writes correctly, so it is an oversight, not an asymmetry.
- **Two corrections to the issue, one retraction of my own.** (a) Its "nothing is lost in serialization" is FALSE for subtype/coercion/specialization — both serializer and deserializer funnel every hash through `for/hasheq`, so cons-keyed `equal?` registries lose lookup semantics (REPL-confirmed). That makes **fold-not-assign a correctness constraint** on the fix. (b) I deleted its severity 3 as unsupported — **wrong, retracted**: I refuted one mechanism (index/partiality; those guards ARE dead) then declared the symptom unsupported *without testing the symptom*. It reproduces with the issue's exact string via the never-serialized **schema** registry. Our own Watching entry, firing in the **asserting-absence** direction.
- **NEW finding the issue lacks — durable poisoning**: a dependent mis-elaborated under the defect is itself serialized, so the wrong answer survives into fresh processes. On-disk corruption, not a warm-process hazard.
- **Landed**: P0 `62a1a34c` + `28f73adf` (failing-test-first, 4 cases) · P1 `e74719b0` (table-driven dual-write; severities 1+2) · P2 `54358a5f` (serialize the 7 never-serialized registries, `.pnet` v3→v4; severity 3) · P3 `d3660aee` (ONE table + a coverage gate driven off it) · P4 `dd8b6c69` (comment truth). Suite **472 / 9171-9175 / 0** throughout; acceptance 21/21 + 29/29.
- **Surprise 1 — three masking mechanisms, the third found by validating the second.** Resetting the cells does nothing, because `init-macros-cells!` re-seeds from the *parameters* the buggy restore just polluted. And then the test masked ITSELF: the severity-3 case passed at a commit where it was verifiably broken, because my clean-snapshot list omitted `current-schema-registry`. The file now carries that warning.
- **Surprise 2 — a near-false "fixed" claim.** The first post-P1 severity-3 check came back correct; it was a cache **MISS** masquerading as a fix (recompiling made `driver_rkt.zo` newer ⇒ `infrastructure-stale?`), and that run then rewrote the cache so the next one failed. Every cache measurement now carries an explicit `pnet-stale?` HIT assertion.
- **Surprise 3 — `pipeline.md` earned its keep twice in one phase.** The 7 value structs were unregistered in the tag table; registering `schema-entry` but not its NESTED `schema-field` produced the documented misleading symptom verbatim — `given: '#(struct:schema-field name String #f #f)`, a raw vector that PRINTS like the struct.
- **Method**: grounding-audit (5 facets + completeness critic) → design → **adversarial critique** (5 lenses × refute-by-default verifiers: 40 findings, **12 survived, 28 refuted**) → per-phase implementation. The critic caught what all five facets missed; one verifier did an independent A/B repro rather than taking its finding on faith. Tooling defect found + fixed (`aa78fa31`): the audit template silently sent the literal string `undefined` to every facet, so "N facets agreed" was false corroboration — **our own silent-wrong-answer class, in the audit tooling**.
- **Deliberately NOT swept in**: the eager cell-id capture in the resolution bridges (verified, separate), and Option C (parameter-fallback retirement) → new **PM Track 12 note** `2026-07-27_PM_TRACK12_REGISTRY_READ_PATH_NOTE.md`, linked from the PM master. Its load-bearing finding: retiring the fallback TODAY would *widen* #78, since the un-guarded re-seed is currently the accidental healing.
- **Next**: X.close — bench check, DEFERRED triage, PIR-lite, then **merge to main** (owner wants the work on main *before* #78 is closed).

### #78 MERGED TO LOCAL MAIN — fast-forward, verified in the main checkout (`dc5d4d85`)  (2026-07-27)

- **Sequence, deliberately in this order**: merged `main` INTO the branch first, resolved, and gated the COMBINED tree — *then* moved `main`. Neither stream had ever been compiled against the other's code, so doing it the other way would have put an untested combination on `main` and discovered any breakage there.
- **The two streams were disjoint at file level** (checked before merging, not after): `main` touched typing-core / reduction / global-constraints / syntax / unify / trait-resolution; #78 touched driver / pnet-serialize / macros / parse-reader / batch-worker. **One** conflict — the shared dailies — resolved by KEEPING BOTH (STATE head merged rather than picked; all 8 LOG entries from both streams retained).
- **Owner WIP was never at risk, and that was verified rather than assumed**: `comm -12` of the 41 WIP paths against the 20 files the fast-forward would update returned EMPTY, so the FF could not clobber anything. Post-merge the WIP count is still 41.
- **Landing gate run in the MAIN CHECKOUT, not just the worktree** — this mattered: the foreign-resolver fix (`9303a077`) changes how `foreign racket "prologos/X"` resolves *there* too, and the main checkout is the one place the old collection path resolved to itself. **473 / 9190 / 0**, acceptance 21/21 + 29/29, and `algebra.pnet` confirmed regenerated at `version=4 slots=31`.
- **Rollback, if ever needed**: `git -C /Users/avanti/dev/projects/prologos reset --hard 3bbad429` (nothing was rewritten; the FF only advanced a ref).
- **Next**: reply on #78 and close it; then CIU T6 **P2.b** per the handoff.
