# Dailies — 2026-07-28

Rolled from `2026-07-26_dailies.md` (**604 lines**, well past the ~400–500 cap)
on the owner's word, paired with `docs/standups/standup-2026-07-28.org`. Prior
file is read-only history; its LOG carries the whole Path Selection design arc
(five surface rounds → D.2 → the D.3 critique → P0/P1/P2 → the P3 audit → **the
REDESIGN** → D4 → D4.P0), plus the #78 and #58 arcs, the test-suite perf
recovery, and the mixfix/foreign fixes. STATE head below is SEEDED
(copy-forward per `DAILIES_METHODOLOGY.org`) and refreshed to current.

---

## 📍 STATE  (overwrite in place — always current)

- **⭐ MERGED 2026-07-31**: local `main` and worktree branch `relaxed-mcnulty-bfefb0` are UNIFIED (the merge commit's first parent is main; prepared + fully gated in the worktree, then fast-forwarded — the owner's 41-entry WIP verified disjoint from every merged path first). Contents: the CIU T6 D4 P3b/P3c arc (main side) + the worktree arc (branch-result diagnostic · QTT P1–P7 · 4 PM soundness fixes · LET track P0–X.close). Suite at the merge: **9599 / 476 / 0** · all 6 acceptance files `--check` clean. Conflicts were 2 (a require-block union in parser.rkt; the dailies log tail — both arcs kept in order below). STILL NOT PUSHED. The per-arc STATE bullets below predate the merge — coordinates in them are historical.

- **⭐ SIDE-TRACK, 3 pieces, LANDED 2026-07-31** (branch `focused-morse-3454bf`, NOT merged/pushed): the **Solve Carrier** flip, **top-level `let`** (`53251a80` + `aea73811`, doc `2026-07-31_LET_TOPLEVEL.md`), and the **`.pnet` container-sentinel sweep** (`c99bfa89` — four unprotected wrappers, `PNET_VERSION` 9→10). Suite **9646/478/0**, 9 acceptance files **317/317**.

- **⭐ SIDE-TRACK LANDED 2026-07-31 (separate worktree/branch — NOT merged, NOT pushed)**: the **Solve Carrier** spin-out on branch `focused-morse-3454bf` (worktree `.claude/worktrees/focused-morse-3454bf`, based on `cab30b9a`). `solve`/`solve-with`/`explain`/`explain-with` now return **PVec**, discharging **Q_U9's recorded counter-argument upstream** — so P4d's List refusal now costs solve rows nothing. Also closes the `let x := (goal …)` implicit-solve gap the LET track left, and adds the inverse of POL.9's guiding diagnostic. Gate: suite **9548/477/0**, 8 acceptance files **296/296**. Doc: `2026-07-31_SOLVE_CARRIER_SPINOUT.md`; roadmap row Rel **T1.5**. ⚠ The seam was **SEVEN** sites, not the two D4 recorded — D4's Q_U9 block now carries a correction block naming both wrong claims. **The main-checkout STATE below is unaffected by this branch.**

- **HEAD**: re-derive with `git rev-parse HEAD` — never trust a hash in prose. At this refresh: **`5520e321`** · suite **9835 / 482 files / 0** (carries — `git diff e71ef6b8..HEAD` is docs-only, ZERO `.rkt`) · battery **344** (MEASURED, not derived) · acceptance 52/52 + 89/89 + 6/6 + 29/29 + 28/28 · **14 unpushed — do NOT push unless directed** · working tree = owner WIP only. ⚠ origin has advanced EXTERNALLY twice this arc. ⚠ The out-of-band `loc->line` arc LANDED and MERGED (`1d23600e`, **verified an ancestor of HEAD**): the dual-spine merge's TREE LEG is GONE.
- ⚠ **We are on `main` in the main checkout again.** The prior session ran in a git worktree (`.claude/worktrees/crazy-merkle-cbee28`, branch `ciu-track-6-path-selection-a66040`) — an **owner-flagged oversight**. Removed 2026-07-28 after verifying 0 unique commits + clean tree; branch deleted; the 41 owner-WIP entries live here and were never at risk. Handoff §1's "branch `main`" was aspirational, not descriptive — it is now true.
- **Where we are**: **CIU T6 Path Selection — the surface was REDESIGNED 2026-07-28.** The live law is **dot DESCENDS · brace SELECTS (`x{…}`) · `:` BROADCASTS · `^` RE-KEYS · `*` FLATTENS · `<` DISCLOSES**. (Anything phrased "dot extracts · bracket selects · `:` iterates · `*` splats", or PS1–PS15, is the SUPERSEDED surface.) Normative surface = the spec `docs/research/2026-07-28_path-selection-spec.md` — an OUTSIDE, idealized document, **ruled a GUIDE not a prescription**; every adaptation is recorded in D4 §3, and an *unrecorded* divergence is a bug in D4. Implementation design = `docs/tracking/2026-07-28_CIU_T6_PATH_SELECTION_D4.md` (per-phase §5 sections; §1.2 supersession table; **§2.3 the CARRIER TABLE**; §3 rulings ledger; §8 risks; §9 two-column gate). **D4 is SETTLED end-to-end**; **D4.P0 ✅ `e2674208`** (28/28). Everything LANDED stands as substrate: P1's `.{`-as-MIXFIX retirement + **P2 COMPLETE** (the two-tier principle, 5 slices `ad75e57a`→`ac89341f`).
- **NEXT**: **P4c-4c + G2 as ONE slice** — ⭐ **G2 RULED ALONGSIDE, not after** [owner, 2026-08-04; folded `5520e321`]. (A) the PVec ω VALUE semantics: 3 typing arms (`walk-to-leaf` / `select-branch-entries` / `select-below-field`, returning a TYPE into the `(values x fail)` slot) + 3 reduction arms (returning a VALUE, **no failure slot**, escape via `let/ec return`) + **a FOURTH site the partition never named** (an `rrb-of` container guard — `champ-of`/`index-into` are the precedent; **verified: there is no `rrb` twin**) + the L1/extent LAWS pinned in `def` position. (B) **G2** — retire `broadcast-enabled-contexts`, making preservation UNCONDITIONAL. ⚠⚠ **This is the FIRST slice in the arc where production behaviour MOVES** — every prior P4c slice leaned on "default `'()` ⇒ byte-identical" and that argument is gone. Three obligations follow: the **corpus A/B is load-bearing** (re-derive the mintable set, don't inherit P4c-4b's 12) · the **battery INVERTS** (P4c-4a's pins were mutation-verified AGAINST the grant seam; enumerate RED and **VACUOUS separately** — the second class is the dangerous one) · the accepted PRESERVE residual (macro pattern vars) **goes live**, so verify the error CHANNEL not the message. **Q_U18 ✅ RULED** (PRESERVE + **G4**, discharged BY this slice); flip LANDED `e71ef6b8`. Mini-audit running: `wf_a24f3e0f-d84`. Still open: **Q_U19** (`^`-on-broadcast, due P4d) · **DEFERRED 42** (the `:{` mint, a P4d prerequisite, **lands alone**) · **PF** (Q_U17 B2, before P5). Sequencing: D4 `#p4c-sequencing`.
  ⚠ The handoff `docs/tracking/handoffs/2026-08-02_CIU_T6_P4c4c_handoff.md` predates the G2 ruling and its §1/§5 are now stale on that point — **D4 is the authority**; not rewritten because handoffs are owner-requested only.

- **Watching** ⚠ *(section header + items 1–2 were LOST to a bad edit — item 2's tail had been welded onto the NEXT line above, which is how it was found. Item 2 is reconstructed from its surviving tail; **item 1 is unrecoverable and is NOT invented here**. Restored 2026-08-04.)*
  1. *(lost)*
  2. **The mini-audit / probe keeps refuting the premise — including, repeatedly, MY OWN RECOMMENDATION TO THE OWNER**: I advised against multi-digit `:N` on the premise that `:`+digit was QTT-owned space; measuring to the edges showed the recognizer accepts 12 lexemes while `mult-annot?` accepts 3, so the overlap is **two lexemes**, not a space, and the owner's instinct was right (Q_M8). The tell was available and missed — the recognizer's own comment (`:0, :1, :w, :m`) already disagreed with its code (`char-numeric?`). **8th, against a REPORT rather than a design**: the P1a skeptic's negative-index mechanism ("falls through to `(get v -3/2)`") was wrong — probing showed `-1.5` arrives WRAPPED as `($decimal-literal -3/2)`, so the fix it implied would not have worked. Verify the mechanism, not just the symptom. This arc: three of round 7's premises probe-refuted; round 8's "constructor arity mismatch is a compile-time error" was FALSE (cross-module mismatch **compiles clean, exit 0** — only `match` patterns are loud); the D5 critique's "1-tuple unimplementable" was itself wrong one layer down. **PROMOTION IS OVERDUE** — fold into the rules tier at the next close, per the two-form rule.
  3. **The design's own enumeration under-counts** — **8th arc, and now THREE TIMES on the SAME enumeration**: the `.`-band cite went three → five → six, and at the P2 audit **§Q8.1 (owner-reviewed, NORMATIVE) was found contradicting ITSELF** — a six-row table above a five-member Totality prose. Worse, §Q8.5 invariant 3's token layer was corrected AGAIN for the identical reason it was corrected at P1b-iii: an enumeration written from ONE member's shape (an opener) under-specifies for the next differently-shaped member (a self-contained token), by exactly the amount that lets it through. The `grounding-audit`-as-default-opener rule keeps paying.
  4. **A failing test is only evidence if it fails for the reason you claim** — 4 instances this arc under the `:no-prelude` fixture (one regex matched digits in a **temp-file path** inside a printed error struct). The track test file now carries a prelude-backed second fixture + a fixture-sanity guard. Guard the premise, not just the conclusion.
  5. **A CLAIM IS ONLY EVIDENCE AT THE LAYER YOU MEASURED** — **NEW, and it had already propagated into a session summary.** §Q8.1/§5.P2 said the rational mis-lex sits "at 0 errors" and demanded failing-test-first on it. That was READER-LAYER: end to end `m.0`/`m.1.2`/`m.10.20` are `ERROR: Unbound variable` (4 errors). I repeated the claim to the owner after probing only the datum layer. Four of six facets caught it. This is hazard 4's sibling — not "the test fails for the wrong reason" but "the CLAIM is true at a layer nobody named". State the layer with the measurement.
  6. **An A/B OVER A DIRTY TREE MEASURES THE TREE, NOT THE CHANGE** — **NEW, 3rd sighting of the dirty-tree class**. P1b-ii's first corpus A/B reported 5 diffs; they were *exactly* the 5 owner-modified `.prologos` files, because the two legs read different CONTENT. Re-run with both legs on identical HEAD content and only the reader differing → 1 diff. Same trap made two facets of the P1 audit "refute" a correct design count. **Pin BOTH legs' inputs, not just the code.**
  7. **A green suite proves nothing for this class** — three live defects sat under one this arc: permissive `expr-broadcast-get` (`ladmins.*nope` → 0 errors), the `def X :=` layout defect (**issue #80**), the loose-`.{` shatter.
  8. **HEAD moves, and handoff coordinates go stale FAST.** `8d2eb340` → `6a444cba` was 36 commits mid-audit. This session: the relay note said `4028e727`, the handoff's own §1 said `c284182a`, the dailies STATE said `5949f7b5` — **actual HEAD `5c171caa`**, i.e. a handoff's §1 went stale within 2 commits of being written. Always `git rev-parse HEAD` first and re-pin.
  9. **When folding rulings, the PHASE SECTIONS are the drift surface, not the tracker** (`c284182a`, 2 BLOCKING + 7 SIGNIFICANT). The per-phase structure is both what makes this drift possible and what makes it findable.

---

## LOG  (append per commit / decision / surprise — newest at the BOTTOM)

### New working-day interval opened; back on `main`; D4.P1 audit launched  (2026-07-28, docs)

- **Why**: session handoff into a fresh context at a phase opening, per `HANDOFF_PROTOCOL.org`. Hot-load ran over the handoff §1–§6, the spec (676 lines), D4 (636), the 07-26 dailies, the acceptance file, the track test file, and the predecessor's §3 + §5.10 — then understanding was summarized back and **owner-validated before any work started** (protocol step 5/6).
- **Three state corrections found at hot-load**, all from `git rev-parse` rather than trust: HEAD was `5c171caa`, not any of the three hashes the relay note / handoff §1 / dailies STATE carried; the session was running **in a worktree** on a side branch, not on `main`; and the "41 owner-WIP entries" the handoff said to protect were in the *main checkout*, i.e. not even reachable from where the session sat. **Owner: the worktree was an oversight** — removed (0 unique commits, clean tree, branch deleted, WIP verified intact at 41 before and after).
- **The suite figure was re-derived, not inherited**: `git diff --name-only ac89341f..HEAD` returns **zero `.rkt` files**, so 9238/474/0 genuinely still holds across the 9 docs commits since.
- **Owner rulings this checkpoint**: (1) work on `main` in the main checkout; (2) **accept the `.*name` P1→P4 gap** rather than migrate the 4 sites — migrating risks never migrating back, and the breakage is useful noise; (3) open the new working-day interval and roll the dailies; (4) workflows for major work, not minor doc cleanup.
- **D4.P1 mini-audit launched**: `wf_789e4f0f-f02`, 7 read-only HEAD-pinned facets + the completeness critic — the dot band and `.{` as a new opener · brace adjacency + the three-bucket census · the colon seam (annotations, `:N`, `:diags*`, `:{`, `:<`) · the dot-key retirement family · `.*name` + `expr-broadcast-get`'s full pipeline footprint · the reject batch + the diagnostic seat · the test/pin surface. 19 design claims sent for confirm-or-refute; 9 questions for the synthesis, headed by Q8 itself.
- **Next**: R-lens-verify the audit's `rlens_targets` surgically on the main thread, resolve its capture-gaps, then co-design Q8 with the owner before any code.

### D4.P1 mini-audit ADJUDICATED — P1 splits into P1a/P1b; six rulings folded (docs)  (2026-07-28)

- **The audit** (`wf_789e4f0f-f02`, 7 facets + completeness critic @ `5c171caa`, 8 agents / ~1.7M tokens): 153 facet findings + 15 critic findings; every load-bearing claim **re-verified on the main thread** before adjudication (the narrowing-var sites, surface-rewrite's arms, the parser keyword, `definitely-not-map?`, the HEAD-vs-dirty-tree censuses, and a live probe of the tilde template). **The mini-audit-refutes-premise streak is now 8 consecutive.**
- **⭐ The headline: ruling 3d (colon-seam position-disjointness) is REFUTED.** Narrowing-query typed logic vars (`[add ?x:Nat ?y:Nat] = 5N`, 6 live sites, `narrow-var-constraints` parser.rkt:6871) are `ident:Ident` annotations in EXPRESSION position — a subsystem the ruling's "POL.6 territory" census scope missed entirely. Rider: the surface is ALREADY silently broken in WS (the splitter needs the sexp reader's glued symbol; constraint dropped, arity inflated) and its sole WS test is VACUOUS (passes on substring "x"). **Owner Q_L1: adopt the `?`-prefix discriminator + scope the WS repair into P1b.**
- **The `.{` glyph is NOT free**: `surface-rewrite.rkt:516-521` is a SECOND grouping implementation still routing `dot-lbrace` → retired mixfix semantics, production-reachable via driver.rkt:2432 — `d18648f0`'s "grep = 1 hit" VAG was scoped to one file. Registering the token at P1b without cleanup silently wakes dead semantics. Also: "three layers" is really SIX sites (the two langle skip-set TWINS were collapsed into one — and their disagreement IS the 31d27c83 defect).
- **`broadcast-get` is a 4-site chain, not a reader deletion** — a live parser keyword + surf struct + elaborator arm, user-writable independently of the token. Census: ZERO live users. **Owner Q_L3: retire the full chain at P1a** (ban-dual-paths; the audit itself proved partial retirements rot).
- **The named diagnostic template is a WHOLE-FILE ABORT** — probe: a mid-file `~3` prints NOTHING, not even earlier commands (the reader tokenizes the whole file before any command runs). This contradicts both the accepted-gap ruling's premise (noise ≠ silence) and spec §3.6/Q8. **Owner Q_L4: build the marker-form seat at P1a** — `$mixfix-retired` (deleted at `d18648f0`) proves the marker mechanism; its flaw was raising instead of returning a `parse-error` value (the POL.4 conversion discipline). The tilde seat's own whole-file swallow + the bare-`[]` hard-abort → **2 new DEFERRED defect entries**.
- **The dirty-tree census trap, caught by the critic**: two facets "refuted" the design's `.*name` = 4 count — both had censused the working tree, where owner WIP had commented 3 of 4 sites (prior `.[x]` sketching — **Q_L5: disregard**). At HEAD the design's counts are RIGHT (`.*name` 4 · dot-key 2 · `racket{…}` 10). New census discipline: `git show HEAD:<file>`, never plain grep. The `m[:kw]` count is rule-dependent (22 loose / **16 adjacency-verified = the actionable rule** / 30 occurrences).
- **My own D4 paragraph from the same morning was stale on arrival** (critic): it claimed the retirement unwinds a `definitely-not-map?` exemption that P2.b slice 1 had already dissolved (positive-list inversion). Corrected in place; the surviving pin's comment has the same drift and its replacement (a direct conservative-default pin) is in the P1a test delta.
- **Also ruled**: **Q_L2** — the WS-vs-sexp `.{` divergence is INSTITUTIONALIZED (sexp fan-out stays, 15 test sites; convergence = the future sexp phase) · **Q_L6** — the P1 split. The accepted gap VERIFIED genuine noise (`first-class-paths.prologos`: zero markers, zero non-doc references).
- **Fold**: D4 tracker split + §3 six rulings + §5.P1 restructured into audit record + §5.P1a/§5.P1b work lists; DEFERRED +2.
- **Next**: P1a failing-test-first, opening with the surface-rewrite probe.

### D4.P1a LANDED — the retirement batch + the marker seat; the seat's own abort caught pre-commit (`859b529d`)  (2026-07-28)

- **Why**: P1a is the retirement half of the split phase — clear the dead/colliding old surface and build the diagnostic substrate P1b's re-mint needs. Failing-test-first: 10 RED guided-diagnostic pins → green.
- **THE SEAT (Q_L4), mechanism refined by grounding**: retirement TOKENS stay as marker emitters; the preparse REWRITE arms (the semantics) die; retired shapes normalize to ONE `($retired-selection kind detail)` head; **the PARSER** converts it to a per-command guided `parse-error` VALUE. Conversion moved preparse→parser because error values are already legal there and flow per-command, while preparse Pass 2 has no per-form handler. This is `$mixfix-retired`'s mechanism (deleted at `d18648f0`) minus its fatal flaw — the raise. Payoff nobody predicted: because the tokens survive, **every reader token-layer pin stayed green** and the test churn shrank to rewrite-units + E2E.
- **⭐ The headline is self-inflicted, and it is the good kind of catch.** The adversarial verify's seat-soundness skeptic **refuted**: my marker arm called `(car args)` unguarded, so a user-written zero-arg `[$retired-selection]` RAISED and killed the whole file — **the exact failure mode the seat exists to eliminate, in the seat itself**. Its three raw-sentinel siblings carried `(pair? args)`; I wrote that guard three times and omitted it on the fourth. **Lesson shape: when a new arm joins a family, diff it against its SIBLINGS, not against its own intent.** Fixed + pinned (B5) before the commit.
- **Second real defect, pre-existing**: `pattern-var?` excluded the LIVE sentinels but not the retired `$dot-key`/`$nil-dot-key`, so a retired shape in a `defmacro` TEMPLATE read as an unbound pattern variable and raised out of preparse — a retired-surface path that **bypasses the seat entirely**. The mini-audit had flagged the asymmetry; closed here since the phase owns that list (B6).
- **Third, and a probe-corrects-the-report moment**: negative NON-integer indices escaped the guard. The skeptic's stated mechanism was wrong — probing showed `-1` arrives BARE but `-1.5`/`-2/3` arrive **WRAPPED** as `($decimal-literal -3/2)`/`($rat-literal -2/3)`, so the implied one-word fix would have silently not worked. Hence `negative-index-payload?` (B7).
- **Retired, each with a guided migration message**: dot-key `.:name` (BOTH shapes — the prefix form's migration is an argument reorder, not a dot rewrite) · the `#:name`/`#.:name` twins (**already silently broken in WS**, mechanism now known: the stx arm kept the RAW LEXEME as the key) · broadcast `.*name` → names `:name` + P4 · keyword-literal `m[:a]` (**hint corrected** — the design row named `m[a]`, the *superseded* surface's own spelling) · `v[-1]` (was a SILENT WRONG ANSWER: typed `Int`, stuck term, 0 errors) · `v[]` · `_[k]` · and the **full `broadcast-get` chain** end-to-end per pipeline.md, killing both permissive defects (`'[<error> <error>]` and the chained `'[none none]`, each at 0 errors under a green suite). **No PNET bump** — symbol-keyed table + a zero-hit cache census.
- **Substrate**: surface-rewrite's `'mixfix-group` subsystem DELETED (**445 lines**, count-asserted) — it served only the dot-lbrace leg, dead since `d18648f0`, and was a **WAKE hazard** for P1b's re-mint. Skeptics confirmed `.( )` mixfix fully intact **including comparison chains**: the live pratt path implements them natively, so the deleted `expand-comparison-chain` was a redundant dead twin. audit-06/-09 differential-identical to HEAD.
- **Filed, not fixed** (2 pre-existing whole-file aborts, both identical at HEAD): live `.( )` mixfix errors still raise through preparse — **the same class Q_L4 documents, and the seat now exists for them**; and a union-typed def + implicit-binder spec + call hangs in typing (triage against the existing union-hang entry).
- **Gates**: suite **9253 / 474 / 0** · neighborhood 477/16 files · acceptance 28/28 + 89/89 · the accepted `.*name` gap emits guided per-command noise **with the file continuing** — noise, not silence, exactly as the owner ruled.
- **Next**: D4.P1b — the seams + Q8, opening with its mini-audit.

### D4.P1b mini-audit ADJUDICATED — the hazard was INVERTED; P1b splits three ways; the top-level `<` swallow gets FIXED (docs)  (2026-07-28)

- **The audit** (`wf_d0862784-5e5`, 6 facets + completeness critic @ `bc0c7578`; 27 critic findings, 22 capture gaps): scoped as a DESIGN audit, not a re-census — facets were told to re-verify recorded facts post-P1a and spend their effort on the decisions. **9th consecutive phase whose premise it refuted.**
- **⭐ THE HAZARD IS INVERTED — the phase's headline.** D4 said "five parse-reader sites + the P1a-cleaned surface-rewrite", which reads as DISCHARGED. `surface-rewrite.rkt:516` is the **general brace-opener arm**, alive, and needs `dot-lbrace` as a POSITIVE ADDITION. Probe-verified on the main thread: the spec flagship **groups CORRECTLY TODAY**, precisely because `.{` lexes as two tokens and `lbrace` is handled there. Minting the token without that arm stops it firing and expels `version` from the block. The whole risk framing was "registering the token WAKES retired semantics" (the P1a hazard, closed); the surviving hazard has the **opposite shape** — a currently-correct grouping BREAKS. Rider: "match your siblings" (P1a's own lesson) would here **inherit a live bug** — `dot-lparen` is absent from surface-rewrite entirely, and `(a .( b ) c)` mis-groups with `c` expelled.
- **⭐ `:N` is NOT unclaimed lexical space.** `:0`/`:1`/`:w`/`:m` are the **QTT multiplicity vocabulary** (18 consumption sites). Verified: `users:0` → `(users :0)` and `[fn [x :0 Int] x]` → `(fn (x :0 Int) x)` are **the same lexeme**. Ruling 3c framed this as a multi-digit question settled by a `{:10 v}` probe — **the specified probe cannot see the collision**. Owner ruled **Q_M1(b)** position-disambiguation, and I carried the obligation rather than the assumption: position-disjointness was refuted ONCE already on this seam, so P1b-i's FIRST probe hunts expression-position `:0`, and a counterexample re-opens the ruling.
- **⭐ The `<` swallow is MIS-ATTRIBUTED, and the owner ruled it FIXED.** Verified: `users<{a}` (no colon) swallows identically to `:<`, and so does `def p := 1 < 2` / `def q := 3 > 4` — **no colon, no brace, ordinary user code, ONE form, ZERO errors**. Mechanism pinned: `langle-matched?`'s terminating arm needs `close-type`, which is `#f` at top level, so the scan runs to the end of the whole token stream. Fix is plumbing — `content-line-indices` is computed at Domain 2, before Domain 4's scanner. Owner: *"This has been an issue to fix for a while, and there is a stronger need now than ever before."* Scoped into P1b-i with a STOP-and-checkpoint clause, since `31d27c83` deliberately kept multi-line angle groups working and its 15 pins are the gate.
- **Two rulings CORRECTED because they were not executable as written**: ruling 3a's precedent becomes **`hash-lbrace`** (6/6 sites, plain `'rbrace`) — citing `dot-lparen` silently imported a sentinel closer that exists to carry semantic MODE and would reproduce the `31d27c83` swallow class; and the standing **`^` ruling names the WRONG primitive** — `split-fused-symbol` splits on `":"`, and the tree's only `^` splitter lives in the sexp-only surface Q_L2 freezes, so **P3 has no reusable WS `^` primitive**. A self-contradiction between two rulings in the same ledger, caught before P3 could build on it.
- **A distinct select-block sentinel is FORCED, not preferable**: adjacency is DESTROYED at the datum layer (`x{a b}` ≡ `x {a b}` byte-identical), and `$brace-params` is **≥7-purposed** (design said triple), every fork position-disambiguated. Constraint the design missed: `combine-foreign-blocks` has NO adjacency test, so spaced `racket {code}` is back-compat-PINNED — head-adjacent must keep `$brace-params` unchanged.
- **Also**: the registry has ONE hard-coded site (not two — driver.rkt:3337 is an import declaration); a leaf module is the only option with in-tree precedent (no cross-module registration idiom into parse-reader exists); `(pair? result)` protects 13 `.prologos` lines **plus 16 more in gated tests**; the sexp `.{` surface is ~2× the recorded size (isolation still confirmed); the `.` duty count is **six**, not four; **priorities are decorative where they tie** — prefix-disjointness is the safety property; and a THIRD already-silently-broken-in-WS surface surfaced (the `selection` form).
- **Owner rulings Q_M1–Q_M4** folded into D4 §3; §5.P1b restructured into the audit record + **P1b-i / P1b-ii / P1b-iii** with the ordering constraint (`.{` before-or-with adjacency). Acceptance delta for all of P1b: **ZERO markers uncommented** — no honest `;;N=>` result exists for "this now tokenizes."
- **Next**: P1b-i — the Q_M1 gating probe first, then failing-test-first on the `<` swallow + the narrow-var joiner.

### D4.P1b-i LANDED — three repairs; the datum-layer fusion was UNSOUND and got rebuilt in the tokenizer (`fc65ca54`)  (2026-07-28)

- **Why**: the repairs-and-probes slice, so Q8 is written FROM measured facts. Failing-test-first. Suite **9263 / 474 / 0**; acceptance 28/28 + 89/89; **reader corpus A/B against a worktree-pinned HEAD baseline = 160 files, 2 diffs, both INTENDED** (the two files carrying `?x:Nat`, which now return solution sets instead of `nil`, at 0 errors).
- **The Q_M1 gate PASSED and sharpened the ruling**: of 291 live multiplicity tokens, **289 are spaced** and the other **2 are preceded by `{`** — an opener, not a focus. So the discriminator is adjacency to a *focus-bearing* token, not position alone; same shape as `is-postfix?`'s `(pair? result)` conjunct.
- **The `<` swallow is fixed (Q_M4)** — and it took THREE attempts, each refuted by measurement: (1) a hand-rolled THIRD definition of "indent-0 content line" that drifted from BOTH `content-line?` and `measure-indent` (so a CRLF blank line DESTROYED a working angle group, and tab-indented files got no bound at all) — **the F1b.7g class, committed by me in the very phase whose rules cite it**; (2) delegating correctly but testing indent 0 ABSOLUTELY, which misses every file whose forms start indented (`"  def a\n  def b"` is TWO forms); (3) the landed version — **relative** indent, per **TOKEN**, which makes multi-line STRINGS safe for free (a string is one token, so column-0 text inside it cannot register as a form start). Both twins needed the bound: **the bracket-depth pin went green while the datum layer still collapsed**, which is the only reason I found the second twin — the `31d27c83` "layers must agree" lesson landing a third time.
- **⭐ The headline lesson, and it is mine.** The WS narrowing repair's FIRST implementation fused `?x` + `:Nat` at the **datum layer — where adjacency is already destroyed**, the exact fact this phase's own audit had just established for braces. It therefore absorbed ANY following colon-symbol: `[add ?x :foo ?y] = 5N` silently became a different 2-argument goal **returning six solutions where HEAD returns nil**, `[map-get ?m :name]` became an arity error, `{:name ?n :age 30}` swallowed a map key. **Two independent skeptics found it.** Worse, my own comment claimed it was *"provably a no-op in sexp mode"* — asserted, never tested, and FALSE (Racket reads `:name` as a symbol, so sexp has the same adjacency). **Writing "provably" is not a proof; it is a place a probe should have been.**
- **The sound fix moved a layer down**: `recognize-narrow-var-annot` lexes a CONTIGUOUS `?x:Nat` (chains included) as ONE token. Contiguity is inherent to a token, so spaced `?m :name` cannot match — no datum-layer guessing, no second splitter. WS now converges with sexp: `nil` → the six-solution set.
- **`def ?x` reserved (Q_M3)** with ONE message across all three def arms (not three copies — the inline-enumeration drift class). Known and NAMED under-application: `def ?1` / `defn ?f` are still accepted, since the gate is `narrow-var-symbol?` wired only into `parse-def`. Recorded rather than silently widened.
- **Q8 WRITTEN** (D4 §Q8) from the probes: the `.` band is **SIX duties** (the design said four — the single-char fallback and the decimal point were unlisted); the `{` "order" is **not a lexical order at all** (two lexical rows + a WS-only grouping table); the `:` seam table; and four standing invariants, headed by **prefix-disjointness, not priority, is the safety property** (priorities TIE in two places and the registry is a plain hash — ties break by unspecified hash order). Recommendation recorded: **do NOT add a digits-only `:N` recognizer** — it would collide with the QTT vocabulary for a surface with zero live uses. **⚠ Q8 awaits owner review before P1b-ii/iii.**
- **Next**: owner reviews Q8; then P1b-ii (the `.{` opener — remembering the hazard is INVERTED: omitting `surface-rewrite.rkt:516` REGRESSES a grouping that is correct today).

### Q8 OWNER-REVIEWED and NORMATIVE — amended by Q_M8: ordinals are MULTI-DIGIT in both bands  (2026-07-28, docs)

- **Why**: Q8 was P1b's named deliverable and gated P1b-ii/iii. Presented for review; adopted as written with **one amendment**, plus two doc-truth drifts fixed in the same pass.
- **⭐ The owner ruled AGAINST my recommendation, and the refutation is measured.** I had recommended NOT adding multi-digit `:N`, on the premise that `:0/:1/:w/:m` were "the QTT multiplicity vocabulary" owning `:`+digit space. **Probing to the edges destroyed the premise**: `recognize-colon-annotation` accepts **12** lexemes (`:0`–`:9`, `:w`, `:m`) while `mult-annot?` (parser.rkt:3904) accepts **3** (`:0 :1 :w`). So **nine of the twelve already lex as one token and already are not multiplicities** — probe-verified, a binder-position `:7` gives a clean loud *"Expected binder [x <T>] or (x : T)"*. **Ordinal ∩ multiplicity = `:0` and `:1` ONLY** — two lexemes, not a space — and those two are already discriminated by the focus-adjacency rule (289 live multiplicity tokens: 287 spaced, 2 opener-preceded, **zero focus-adjacent**).
- **So widening the digit run from 1 to N is not widening a collision** — it finishes a job the recognizer already half-did. `mult-annot?`'s `memq` rejects `:10` by the *identical arm* that already rejects `:7`: no new failure mode, no new error surface on the multiplicity side, and zero live `:2`–`:9`/`:m` uses to disturb.
- **It also repairs a latent defect with nothing to do with Path Selection**: `{:0 v}`, `{:1 v}`, `{:9 v}` are legal map keys today and **`{:10 v}` SHATTERS** into `: 10`. Arbitrary and user-surprising.
- **On the dot side the ruling FIXES A LIVE SILENT WRONG ANSWER.** `x.1.2` reads as the rational **6/5** and `x.10.20` as **51/5**, both at 0 errors. A dot-anchored `digit+` recognizer kills it **structurally** — the tokenizer consumes `.1` before `decimal-literal` (which anchors at a *digit*) can anchor. Multi-digit is the *same one-line* `digit+` that fixes the rational, not an extra. P2's section now carries a failing-test-first obligation on those two, not just on `x.10`.
- **Homes, deliberately unchanged**: `:N` widening rides **P1b-iii** (until broadcast exists `users:10` has no meaning); `.N` `digit+` lands at **P2**. **Neither moves into P1b-ii.** Both owe a corpus A/B, being tokenizer changes.
- **Two doc-truth drifts fixed in the same commit** (the Watching-7 class): §5.P1b-i's heading still read `⬜ ← NEXT` while its own Status line and the tracker said ✅ `fc65ca54` — a fresh session would have REDONE a landed slice; and **§3 contradicted itself on `^` eleven lines apart** — Q_M7 corrects the standing ruling as *not executable*, while the "Carried from the P3 mini-audit" block still asserted it verbatim. Struck rather than left standing, because that is precisely how P3 would inherit the false premise Q_M7 exists to prevent. Also corrected: §5.P2's **three-member** band cite (the band is FIVE — the same under-count Q8.1 fixed), and Q8.3's census 291/289/2 → the re-measured **289/287/2**.
- **Watching-2 gains its 9th data point, and the first against MY OWN recommendation** — a premise I stated confidently to the owner and had not measured to its edges. The tell was available and I missed it: the recognizer's own comment says "`:0, :1, :w, :m`" while its code tests `char-numeric?`, i.e. the comment and the code already disagreed about the accepted set.
- **Next**: P1b-ii — the `.{` opener, opening with its mini-audit.

### D4.P1b-ii LANDED — the `.{` opener; adversarial verify caught a BLOCKING regression I had introduced (`1a1091d4`)  (2026-07-29)

- **Why**: `x.{…}` (the mid-path sub-block `server^.{ssl port}`) must lex and group as ONE construct before brace adjacency lands (Q_M2's ordering constraint). No semantics — those are P3's; until then the form owes a GUIDED per-command error. Failing-test-first; suite **9279/474/0**, acceptance 28/28 + 89/89, battery 443/443, corpus A/B **158 files / 1 intended diff**.
- **The mini-audit refuted the design on three points — 10th phase running.** (1) Coordinates had drifted **+136 to +148** on all five parse-reader cites. (2) **The hazard model was INVERTED**: the design said a missing tree-parser arm hits the LOUD "Unhandled form" fallthrough — but that arm is unreachable from a group tag; the reachable one calls `parse-expr-tree` **silently**, and `driver.rkt` admits tree output when non-error ∧ same-form-type ∧ same-line, so a garbage surf can **beat preparse's correct one**. (3) **My own question's premise was wrong** — I asked whether `dot-lbrace` belongs in the "langle skip-set" at all; those lists are **opener depth-balancing** sets, not suppression sets, so adding it is REQUIRED and omitting it causes a wrong-frame pop. Angle suppression is keyed on frame kind `'mixfix`, which a `'brace` frame never sets — so type-level angle groups keep working inside a select block, and that falls out of Q_M5 rather than needing a rule.
- **It was EIGHT edit regions, not the six the design priced**: the recognizer is separate from the registration, and `surface-rewrite.rkt` needed `:516` **and** `:519` unconditionally (a two-way `if` cannot express three token types).
- **⭐ The headline, and it is mine. Adversarial verify caught a BLOCKING regression under a green suite.** `$dot-brace` was missing from `pattern-var?` (macros.rkt), so the new sentinel read as a macro **pattern variable**: `.{ }` inside a defmacro **template** made `datum-subst` raise — a **whole-file abort with zero results**, where the identical source gave a per-command error *before* the token existed. It is the exact P1a headline defect class, at the exact site P1a fixed for `$dot-key`/`$nil-dot-key` — **and Q8.5 invariant 3 names this obligation explicitly.** I wrote that invariant one commit earlier and then did not follow it. Writing a rule down is not the same as executing it; the checklist has to be *run*, not just authored.
- **The pin shape mattered too**: P1a's sibling pin only exercises macro REGISTRATION, which is harmless — the abort needs macro **USE**. A registration-shaped pin would have stayed green through the entire defect. Mine uses the macro.
- **The guided error was also wrong at first.** `.{ }` reported **"Unbound variable"** until it was routed through P1a's marker seat — telling a user their valid new syntax had a missing variable. Now it names the construct and the file continues.
- **⚠ The corpus A/B was WRONG the first time** — 5 diffs, which were exactly the 5 owner-modified `.prologos` files. Both legs read different CONTENT, so it compared the *tree*, not the reader. Re-run with both legs on identical HEAD content: 1 diff, intended, form count unchanged 52→52. (Became Watching entry 6 after a later insert renumbered the list.)
- **Three SIGNIFICANT findings, all verified NOT regressions** → DEFERRED 3+4: the guided error is unreachable in arity-checking contexts (because `$dot-brace` stays a *sibling* until P1b-iii fuses it — ordering-dependent, named not fixed); `.{ }` inside `.( )` aborts; inside a parenless `defr` goal it is silent. The first shares a root with a **pre-existing family**: `$set-literal` and `$mixfix` are ALSO still pattern-vars and abort identically in a macro template. DEFERRED records the structural reading — `pattern-var?` is a hand-maintained NEGATIVE list defaulting to "pattern variable", the same inverted polarity `definitely-not-map?` had before P2.b slice 1. **More exclusions is not the fix.**
- **What the Q_N3 guard bought immediately**: it caught the THIRD opener enumeration — the test oracle's verbatim copy of the production list — which reported a real example file as unbalanced until `dot-lbrace` was added there too. The guard also pins `dot-lparen`'s known divergence as a *known-bad* case, so repairing that bug turns the test red and forces its promotion into the guard list.
- **Next**: P1b-iii (brace adjacency + the head registry) — opens with its mini-audit.

### D4.P1b-iii LANDED — brace adjacency, the head registry, Q_M8; adversarial verify caught 3 BLOCKING, all mine (`a6af2761`)  (2026-07-29)

- **Why**: the last P1b slice — adjacent `x{…}` becomes a select block, spaced `f {…}` must not move, reader-form heads win first. Carried Q_M8. Suite **9304/474/0**, acceptance 28/28 + 89/89, battery 501/501, corpus A/B **158 files / ZERO diffs on both halves** (the audit predicted an empty expected-diff set and it held).
- **The mini-audit found a BLOCKING problem with an OWNER RULING — 11th phase running.** Q_M8's safety premise ("`:10` is rejected by the same arm that rejects `:7`") was FALSE: probed, `defn g [x :7] x` was **silently accepted** with `:7` consumed as a **TYPE NAME** (`g : [Pi [x :0 <[Type 0]>] x -> x]`, 0 errors) while `:0`/`:1`/`:10` were all loud. So the VALID multiplicities were loud and `:7` was the anomaly, and widening would have moved `:10` **LOUD → SILENT WRONG**. Root cause: `fused-type-annot?`, a SECOND colon enumeration excluding 4 lexemes against a recognizer minting 12 — with a comment eight lines above warning that "a second copy is how the two paths would drift". Owner widened the scope (Q_N4); the structural fix (no type name starts with a digit) **repairs the pre-existing `:7` defect** as a side effect. A third site was needed too — a stray colon-symbol in a defn param list became a *parameter named `:7`*.
- **⭐ THE HEADLINE, AND IT IS MINE: adversarial verify caught THREE BLOCKING defects, one root cause, under a green suite AND a clean corpus A/B.** My fusion arm was **not idempotent** — it kept the sentinel as head, so `preparse-expand-subforms` re-entry swallowed one more left sibling per pass. Consequences: a **multi-arity `defn` clause lost its `$pipe` head and was SILENTLY DROPPED** (the function evaluated with the wrong arms at 0 errors); `defn g [x base{a}] x` silently defined a **4-parameter** function; the application head itself was eaten. Every other access-sentinel arm rewrites the sentinel AWAY and is a fixpoint **by construction** — their predicates pin a fixed arity; mine deliberately accepted `>= 1`, removing exactly that protection. Fixed by emitting the NOT-YET marker (non-sentinel head ⇒ fixpoint, and zero new registrations). **The irony is the lesson: this phase's own Q_N4 hunk exists to repair a silent wrong answer, and the same commit introduced one.**
- **Three more findings that were defects in my own work**: (1) `reader-forms.rkt` — the registry I created for single-source-of-truth — was **required by macros.rkt zero times**, which still tested an inline `(eq? v 'racket)`; its docstring asserted the opposite. Two lists; the module unified nothing. (2) A **VACUOUS PIN**: a test titled "reachable inside fn params too" asserted on a select block in the fn **body**, with the param list untouched — it passed for a reason unrelated to its name. (3) My new diagnostic recommended `[x : Int]`, which the defn form-shape gate **rejects in the very shape that raises the error**.
- **A justification of mine was disproved by construction.** The Q_N7 comment claimed that without the surface-rewrite fork the guided error "would be SILENTLY SWALLOWED". A skeptic built a third checkout with that file reverted and got **byte-identical output across 20 files**: `same-form-type?` only pairs surf-infer/def/defn/defn-multi, so a preparse ERROR surf can never pair with a non-error tree surf — **preparse always wins when it errors**. The fork is kept for the reason that IS true (the groupers should agree, and the guard now pins it), but the false story is corrected in the comment, the design, and DEFERRED — because that reasoning will be reached for again.
- **Filed (DEFERRED 5–8)**: the two groupers still diverge on `<`-adjacent braces, **live at `foray.prologos:674` — the disclose surface P4 owns**; binder-position select blocks get a raw-syntax diagnostic instead of the guided error; and `pattern-var?`'s residual is **23 of 33**, not the 2 I recorded — `$list-literal` included, so `'[1 2]` in a defmacro template is a whole-file abort *today*.
- **The Q_N3 guard is now shape-aware.** v1 was blind twice over: every row was SPACED (exactly the population adjacency must not change) and it compared item COUNTS while this phase's defect class is shape-divergence at equal count.
- **P1b IS COMPLETE** (i `fc65ca54` · ii `1a1091d4` · iii `a6af2761`). **Next**: P2 grade-1, which carries Q_M8's dot half — `.N` takes `digit+`, and that same one-line change kills the live `x.1.2` → `6/5` rational bug.

### D4.P2 mini-audit ADJUDICATED — the phase's stated headline was WRONG, and so was my summary of it (docs)  (2026-07-29)

- **The audit** (`wf_22020418-a5f`, 6 facets + completeness critic @ `c5153685`; 7 agents / ~1.76M tokens): 142 facet findings + 43 critic findings + 24 capture gaps; every load-bearing claim **re-verified on the main thread** before adjudication (the end-to-end probe, the adjacency datums, both gates, the fold arm's terminal `else`, `$dot-access`'s unconditional `symbol->string`). **12th consecutive phase whose premise it refuted or rescoped.**
- **⭐ THE HEADLINE, AND IT IS MINE: "P2 fixes a LIVE SILENT WRONG ANSWER … both at 0 errors" IS A LAYER ERROR.** The `6/5` / `51/5` rational readings are real but **reader-layer only**. End to end the stranded bare `|.|` is unbound, so `m.0` / `m.1.2` / `m.10.20` → `ERROR: Unbound variable` and `m.-1` → `Could not infer type` — **4 errors, not 0**. I had probed the datum layer, then repeated "0 errors" to the owner as an end-to-end fact. Four of six facets caught it independently. **Consequence, not pedantry**: a pin written as "was silently 6/5" *cannot fail for the reason it claims*, so the honest pin is TWO-LAYER (datum + end-to-end "was a misleading error, now computes the right value"). New Watching entry 5: **a claim is only evidence at the layer you measured** — hazard 4's sibling, where the CLAIM is true at a layer nobody named.
- **And the REAL deliverable was unclaimed by §5.P2**: the fold repairs a **family of lying diagnostics**. Because `x.0` is three datum items today, every arity-checking context blames the wrong thing — `{:a m.0}` → *"Bare symbol '.' not allowed as map key"*, `[fn [v] v.0]` → *"all parameters must be bare symbols"*, bare `m.0` → *"Unbound variable"*. Same shape as P1b-ii's carried residual, and it tells you what to pin.
- **⭐ The sentinel choice was NEVER RULED and it sets the whole phase cost** → **Q_R1: reuse `$postfix-index`.** Both naive `$dot-access` reuses are BROKEN — a numeric payload **hard-raises** at macros.rkt:5556 (`symbol->string` on `10` = whole-file abort), a symbolised `|10|` silently mints `(map-get x :10)`, the wrong NODE (no PVec leg) with the wrong KEY DOMAIN. Reuse verified near-free: `$postfix-index` is already in `pattern-var?` (:1152) AND `access-sentinel?` (:5496), and its fold arm already terminates `[else `(get ,target ,key)]` (:5625). So `v[0]` ≡ `v.0` **byte-identical**, making the owner's standing "two surfaces, one mechanism" ruling literally true at the datum layer — and the nine-tier surface, which produced the BLOCKING defect in *both* prior slices, is avoided entirely.
- **The fixpoint question is ANSWERED rather than open**: the existing fold is a fixpoint *by construction* because no arm's output is sentinel-headed and re-entry is gated on the datum having CHANGED. `(get target N)` inherits it — a second independent reason reuse is the safe option.
- **`token-entry->stx`'s `case type` is the site that mattered, and a miss there is SILENT** (falls through both `[else]`s to the bare symbol `|.1|`). §Q8.5 invariant 3 had never named it, because its token layer was written from an OPENER's shape — three of its five sites are N/A for a self-contained token. Rewritten as a table with an applicability column. ⚠ Payload must use `string->number`, not the siblings' `string->symbol` — that is what makes Q_R1's byte-identity actually hold.
- **Two work items are FREE or already done**: chains (`admins.0.name`) arrive as flat siblings and the existing single fold-left handles them; bare-path extraction already works at top level, `def` RHS, HOF lambda and spec'd `defn` — the only gap is an UNANNOTATED `defn` param, which is **§5.PX's** inference family. §5.P2's Intent silently promised that.
- **Also ruled**: **Q_R2** copy the `:N` `ident-continue?` trailing guard (`xs.0N` a NAMED non-goal, not an oversight) · **Q_R3** the dot band stays adjacency-free, ruled not inherited (it has no gate at all — `x .0` and `x. 0` read identically, and `.k` never enforced it either) · **Q_R4** `m.0` moves out of the §10.6 "PERMANENTLY commented" v2 block it self-contradicted, and trailing markers land first because `run-file.rkt` keys `;;N=>` to RESULT INDEX (line 118 costs **23** renumbers, a trailing add costs zero) · **Q_R5** the `.N` error surface IS in scope (het-tuple OOB is a bare *"Could not infer type"* because `closed-row-miss-hint` is **keyword-gated** — and het tuples are exactly what the corpus pins) · **Q_R6** doc-truth separately (`0e5a56a3`).
- **A FALSE PREMISE in the predecessor struck**: its "one fold arm plus **stopping that re-entrancy**" — the re-entrancy is **LOAD-BEARING** (`v[idx.i]` → `20 : Int` works only because the payload is re-preparsed). Also found: `rewrite-dot-access` has **THREE** production callers, so a `.N` arm inside it inherits map-literal values and the `.( … )` token stream free.
- **The A/B is predicted EMPTY** (zero non-comment `ident.digit` across all 160 tracked `.prologos` at HEAD, via the real tokenizer over a `git archive` snapshot) **and the dirty-tree trap is quantified at exactly ONE site** — `foray.prologos:787`, owner WIP, absent at HEAD. If the A/B shows one diff in `foray`, that is the tree. Also: **there is no committed A/B harness** — re-derived every phase, which is how P1b-ii measured the tree; P2 commits one, with a tripwire for the mutable-registry footgun that gave one facet a FALSE ZERO.
- **New-instance guards P2 must not introduce**: `namespace.rkt:888`'s ns-dot guard raises only for `$dot-access`, so `ns foo.2` would **silently drop** the segment — reintroducing the bug `b0db8f3e` fixed.
- **Next**: P2 implementation, failing-test-first, then adversarial verify before the commit (it has caught a BLOCKING defect in every behavioural slice this arc: 1 · 1 · 1 · 3).

### D4.P2 LANDED — ordinal `.N`; adversarial verify caught a diagnostic REGRESSION of mine (`3005170b`)  (2026-07-29)

- **Why**: grade-1 core — `.` DESCENDS uniformly, so `.0`/`.10` is ordinal descent, the positional twin of `.name`. Carries owner ruling Q_M8's dot half. Failing-test-first: **19 pins written RED**, then implemented. Suite **9370/475/0** (from 9304/474), acceptance **35/35** (from 28) + 89/89, battery 558/16, corpus A/B **158 files / ZERO diffs**.
- **Q_R1 paid off twice.** Reusing `$postfix-index` rather than minting a sentinel was verified near-free — already in `pattern-var?` and the `access-sentinel?` fusion gate, fold arm already emitting `(get target key)` — so `v[0]` and `v.0` are **byte-identical datums**, making "two surfaces over ONE mechanism" true at the datum layer rather than merely architectural. The second payoff nobody priced: the existing fold is a **fixpoint by construction** (no arm's output is sentinel-headed, and re-entry is gated on the datum having changed), so `.N` inherits it — and P1b-iii's BLOCKING defect was exactly a non-fixpoint fold. The nine-tier surface that produced the blocking defect in *both* prior slices is avoided entirely.
- **⭐ THE HEADLINE, AND IT IS MINE: the adversarial verify caught a diagnostic REGRESSION — 5th behavioural slice running.** `ordinal-key-index` accepted ANY `expr-int` including NEGATIVE, dropping the `exact-nonnegative-integer?` half of the guard it mirrors (`record-project`'s literal-nat leg) — the **infer/inferQ-twin drift shape**. Because the ordinal branch sits FIRST in `closed-row-miss-hint`'s `or`, `(get het -1)` got a hint claiming it was out of range for an expression that **type-checks fine**, and that false hint **SUPPRESSED the correct keyword miss-hint** on the same expression. Strictly worse than the message it replaced. **The adjudicator worktree-pinned a baseline and A/B'd** — that is what turned it from "a gap" into "a REGRESSION" and made it commit-blocking. Unreachable from `.N` (cannot lex a sign) and from `het[-1]` (postfix-neg intercepts): the reachable surface is `(get het -1)`, which is **exactly why no `.N` test caught it**. Fixed + 3 pins.
- **⚠ A LAYER ERROR IN MY OWN COMMENT — the THIRD instance of that class in this phase** (after `0e5a56a3` and `f6f30eaa`). It called the `char-numeric?` counterfactual a "SILENT WRONG DATUM … worse than" the audit's predicted `exact?: contract violation`. Both halves layer-confused: the `#f` payload is silent only at the DATUM layer (end-to-end it is a LOUD per-command error, i.e. LESS severe), and the audit's prediction describes the SHIPPED path correctly — right, not superseded. **The same comment block gets the distinction right 30 lines earlier.** Three strikes in one phase says the discipline is not yet reflexive: state the layer WITH the measurement.
- **Three of my own TEST defects, all caught before the commit**: a **VACUOUS pin that PASSED** — bare `#rx"9"`/`#rx"3"` matched **digits in the temp-file path** inside a printed error struct, the identical trap this arc already hit; a **MIS-PREMISED pin** — `.( v.0 )` is a single-operand mixfix that errors identically on the baseline with a plain `.name`; and a **ruling I stated wrong from the audit's own text** — a space BEFORE the dot is irrelevant but a space AFTER it means there is no `.N` lexeme at all, contiguity being inherent to a token. The audit conflated the two spaces.
- **Two more verify fixes**: the `token-entry->compat` sibling arm — **four lenses found it independently**, the token falling through to `|.10|` against both its siblings and Q_R1's numeric payload; and `closed-nat-row?` used instead of hand-inlining its conjunction.
- **The audit corrected the design in four places**: two work items were FREE or already done (chains fall out of the existing fold-left; bare-path extraction already works in 3 of 4 positions, the 4th being §5.PX's family — which §5.P2's Intent silently promised) · the predecessor's "stop that re-entrancy" is a **FALSE PREMISE**, the re-entrancy is LOAD-BEARING (`v[idx.i]` works only because the payload is re-preparsed) · `rewrite-dot-access` has **THREE** production callers, so the arm inherits map-literal values and the `.( … )` stream free · and the "0 errors" framing was the layer error already corrected at `f6f30eaa`.
- **Two pieces of infrastructure now COMMITTED instead of re-derived every phase** — which is how P1b-ii's first A/B measured the tree: **`tools/reader-corpus-ab.rkt`**, which refuses to guess a corpus so both legs must get the same snapshot and carries a tripwire for the mutable-registry footgun that gave one facet a confident FALSE ZERO; and **`tests/test-path-selection-acceptance.rkt`**, because this file's siblings were all gated and it was not — fine while phases only APPENDED, but P2 uncomments mid-file and shifts ~20 markers, the exact situation that produced the Rel T1 misnumbering defect. It also pins that no two markers claim the same index, which P2's own first attempt did.
- **The A/B's quantified predictions BOTH held**: an empty diff set, and exactly one dirty-tree site (`foray.prologos:787`, owner WIP, absent at HEAD) that does read differently under `.N`.
- **DEFERRED 9–13 filed**. The one P3 must know: **item 11 — the Q_R2 trailing guard blocks `^`**, so `x.0^` declines while `x[0]^` lexes. The moment P3 makes `^` meaningful, the two spellings Q_R1 unified diverge on exactly the character P3 introduces.
- **Next**: P3 blocks, opening with its mini-audit.

### D4.P3 co-design rounds 1–3 — the `^` family RULED (Q_T3/4a/4b/6/7/8); my ancestry misreading traced to an old-surface import (`86f546b1`)  (2026-07-29)

- **Why**: the P3 mini-audit (`wf_27a84061-c7e`, 7 facets + critic @ `76214095` — **13th consecutive premise refuted**) fed a deliberative co-design, one question per turn at the owner's direction. Audit headline: the KEYED half needs ZERO new nodes (an all-keyword map literal already mints the corpus rows, probe-verified to the flagship); the KEYLESS half cannot be desugared (`@[…]` collapses to PVec at EVERY homogeneous n — ruling 2a's justification covered half its own corpus); strict merge cannot be inherited (`make-record` silently last-wins — the exact outcome spec §3.6 rejects); the P1a gate has a HOLE at `spec`.
- **⭐ The mutual-clarity round, and the misreading was MINE.** I read leaf `^k'` as collapsing ancestry; owner: **rename is IN PLACE**, output key = the **surviving head key**, only dissolve removes a level. Traced WHY: **no §10 example renames a leaf under a KEPT ancestor**, so the corpus cannot discriminate the readings — and I filled the silence from the acceptance §B `^_` line, which P0 had imported from the SUPERSEDED surface's "derive-ALL, flat" ruling without the R5 classification. Corpus corrected; mechanism recorded in D4 §3.
- **Rulings folded**: **Q_T3** level-local = OUTPUT-level-local (the syntactic reading ACCEPTS what Ruling B rejects — the one monotonicity break) · **Q_T4a** `^` never attaches to an ordinal (guided error; `admins^first.0` is the spelling; DEFERRED 11 dissolves to message quality) · **Q_T4b** the base rules + **Reading N** for `^_` · **Q_T7** the `^-` collapse family in scope (`^-`/`^-k'`/`^-_`; leading `-` reserved, eyes-open) · **Q_T8** parent-key collapse spelled **`^..`** — probes confirmed the owner's `^.` hazard TWICE (`a.b^.c` and `a.b^.0` silently identical to legitimate dissolve forms; under `^..` both are LOUD; `^...` absorbs into `$rest`, the malformed seat rejects it).
- **Q_T6**: the `spec` hole FILED (DEFERRED 14), C30 ANSWERED as the worse reading — the dropped spec REGISTERS its garbage datum (the follow-up defn error quotes the raw marker).
- **Pending**: Q_T1 (parse representation — the completed-architecture question), Q_T2 (presence), Q_T5 (slicing). The audit's doc-truth batch (§8 R6's false PNET-bump obligation headlining) still queued as its own commit.

### D4.P3 co-design COMPLETE — Q_T1/Q_T2/Q_T5 ruled; §5.P3 rewritten as P3a/P3b/P3c; session closes to a handoff  (2026-07-29)

- **Q_T1 (`77451f62`) — ROUTE A: mint `expr-select` NOW, grades-1-scoped.** Decided from the owner's completed-architecture question, on four legs: P5's spine identity is a comparison over a structured step representation (a desugared block leaves nothing to compare); the error-surface argument REVERSES at the completed horizon (factored spellings + merge remedies need block context — and the audit's "the node loses the miss hints" concern is answered by wiring `format-closed-row-miss` into the node's arm WITH branch context); option 2 is a strict PREFIX of option 3, so the desugaring route builds one-phase scaffolding the dual-path ban then tears out; ruling 4b had already fixed the destination.
- **Q_T2 (`57c3b947`) — presence: HORN D, LENIENT.** A block selects a field iff the subject's type SOURCES its presence as `'present`; `'unknown`, unlisted-on-dyn, and `(Map K V)` subjects refuse loudly with the 4d remedy list. **A probe reshaped the case table mid-deliberation**: `map-dissoc` on a CLOSED row simply REMOVES the field (no `'unknown` mark — that arises only from dissoc-on-DYN), so closed rows cannot carry unknown presence and the corpus costs ZERO. The `.field`-vs-block asymmetry ruled deliberate: exploration vs assertive construction. (Also: the full probe with a schema half HUNG at 2min — the reduced probe answered; the union-state BSP hang class is the suspect, not chased.)
- **Doc-truth (`3cf60868`)** — §8 R6's false PNET-bump obligation corrected BEFORE its scheduled promotion into the ambient rules tier; the `〈String〉`→`⟨String⟩` R5 classification recorded in §2.3; §2.4's wrong sexp figure fixed (the bulk is `test-selection-paths.rkt`, 56 cases); Q_M7 coords re-pinned (+103/+35) with the second unliftability reason; pipeline.md item 4 fixed twice.
- **Q_T5 — P3 SPLITS P3a → P3b → P3c**, ordering FORCED (the splitter extends the payload parser; both keyless corpus lines need bare-`^`). **Zero tokenizer changes in the whole phase** (every ruled spelling already lexes, probe-verified) ⇒ NO corpus A/B owed. §5.P3 rewritten into per-slice sections; the tracker row split three ways.
- **Process note worth keeping**: the owner's one-question-per-turn deliberative walk EARNED its cost twice — Q_T1's third option (and the winning framing) only appeared under the forced full presentation, and the Q_T4b mutual-clarity round caught a misreading of mine that would have shipped wrong acceptance markers. "Taking the time to explain what is at play … will often reveal greater clarity and better options" [owner].
- **Session closes to a handoff** for a fresh-context P3a implementation run (owner ruling).

### D4.P3a OPENED — light re-grounding clean; Q_U1: the §5.P3a corpus list was written against no file (docs)  (2026-07-30)

- **Why**: the fresh P3a session, per the handoff. Hot-load ran (handoff §1–§6 · D4 in full incl. the Q_T ledger + §Q8 + all §5 slices · the spec · these dailies · the memory tail · DEFERRED 9–14 · the acceptance §B/§C/§I regions), then understanding was summarized back and **owner-validated before work** (protocol 5/6).
- **Re-grounding VERIFIED, not inherited**: HEAD `cbafb3f1` matches the relay note; **main checkout, no worktree this time** (the hazard that fired twice this arc); 41 owner-WIP entries intact; suite 9370/475/0 legitimately inherited (`git diff 3005170b..HEAD` → zero `.rkt`); acceptance re-run LIVE 35/35. Key coordinates spot-checked at HEAD: `pattern-var?` :1145 · `access-sentinel?` :5493 · the fold arm + its fixpoint comment :5576-5594 · parser :847/:774 · `format-closed-row-miss` typing-errors.rkt:128.
- **Both P3a hazards re-probed at HEAD**: `{:a 1 :a 2}` → `{:a 2}` at **0 errors** (make-record last-wins is live — the strict check MUST precede minting); and `cfg{}` hits the P1a NOT-YET marker today, so the empty-block arm **REPLACES the marker seat** for this shape — "ahead of L4" orders P3a's own new arms, not the existing seat.
- **⭐ Q_U1 [owner]: P3a owns EVERY no-`^` block line — and §5.P3a's own corpus list was wrong** (Watching-3, the enumeration-under-count class, now on the slice's own work list). `{name}` does not exist in §B (a mis-transcription, dropped); `{server.{ssl.{enabled}}}` and `regions{eu us}` were UNASSIGNED (the §9 "assigned to NO PHASE" class); §G's `[D4.P3]`-tagged seal/validate pair is `^`-bearing (`server^.{host port}`, mid-path dissolve) and is REASSIGNED to P3b, whose delta now names it. §5.P3a + §5.P3b corrected.
- **Next**: the RED pin battery — E2E keyed corpus + error battery + twins pins, each failing for the reason its name claims (today's failure = the NOT-YET marker text, not "Unbound variable").

### D4.P3a LANDED — `x{…}` has semantics; the adversarial verify caught a SILENT WRONG VALUE in a pipe (`290f77f9`)  (2026-07-30)

- **Why**: P3a per Q_T1/Q_T2/Q_T5 + Q_U1. Failing-test-first: **34 pins RED at open** (each failing on the NOT-YET marker), then the node across the full pipeline.md § New AST Node surface (13 production files), the preparse `$select` fusion, parser segmentation + the malformed seat, Horn-D LENIENT typing, subject-once reduction. Suite **9418/475/0** (+48), acceptance **41/41** (+6 markers, renumber gate-checked), battery 136/136, neighborhood 623/21.
- **⭐ THE HEADLINE — the verify's BLOCKING catch, 6th consecutive slice, and this one was a SILENT WRONG VALUE**: block-form `|>` with a select init. Head-macro dispatch runs BEFORE the access-sentinel fold, and the pipe's step builder appends its accumulator INTO whatever a step's datum is — appending into a raw `$select-brace` payload corrupted it, and the later fold fused the corrupted select onto the FUNCTION. `|> cfg{server} f2` selected `{server cfg}` off **f2** at 0 errors. Pre-P3a the same input was a guided error — the slice had turned a refusal into corruption. Fixed with a pipe-local pre-fold (sound because the fold is a fixpoint); the loud-but-lying if/cond/let siblings are dot-identical pre-existing (DEFERRED 17).
- **Two skeptics independently found the schema gap**: `sealed{name}` refused as 'subject-other while the refusal text named "seal the subject" as remedy #1 — a circular dead-end. `select-project` now projects THROUGH schema fvars via `schema->row` (all-'present by construction). And the third remedy as ruled — "annotate its row type" — turned out to have **no working spelling at HEAD** (zero in-tree uses; refuses everywhere): dropped from the messages as the advice-that-does-not-work class, **recorded as a Q_T2 adaptation awaiting owner ratification** (DEFERRED 19).
- **My own falsified premise**: the fold-arm comment claimed the subject was "already expanded when fused" — FALSE (the fold runs before subform recursion), freezing raw sentinels inside compound subjects into lying errors. `$select` is now PARTIALLY opaque: subject expands, payload protected. Also caught: `^`-bearing items fabricated field misses on exactly the spellings the duplicate message recommends (now a P3b-pointer arm); two dead negative pins of mine (an open row prints `{… | _}` — my `} |` regexp could never fire); a ground non-map subject silently STUCK where the nested descent panics (tier symmetry restored).
- **Scope finds**: multi-arity defn clauses with HETEROGENEOUS result types fail PRE-EXISTING with the lying unannotated-param message — pinned select-free (`| 0 -> {:a 1} | n -> 5`) and spawned as its own task; the def-RHS block-pipe is pre-existing broken (baseline worktree-pinned at clean `6d919142`); mid-branch ordinals (`{admins.0}`) refused loud — their semantics are genuinely UNRULED (the Q_U2 candidate).
- **Filed**: DEFERRED 15–20 (def-RHS pipe · do-expander abort family · head-macro family · dyn-assoc desync · row annotations · selection-subject capability).
- **Next**: the three owner checkpoints (annotate ratification · Q_U2 ordinals · the P6 demand decision), then P3b.

### P3a-close checkpoints RULED — annotate RATIFIED; Q_U2 = Reading A (`c1e185ce` + this commit)  (2026-07-30)

- **Q_T2 annotate adaptation RATIFIED** [owner: "the adaptation stands — annotate comes back when it's real"] — DEFERRED 19 carries the re-entry trigger.
- **Q_U2 RULED — Reading A** [owner: "Reading A stands — ordinal blocks own the re-derivation"]: a grade-1 `.N` step in a keyed branch DESCENDS, no output level (contingent keys have no identity to preserve; nominal ancestry survives); re-derivation is the ordinal BLOCKS' (`{N M}`, `.{0}` 1-tuple). Keeps `.0` ≠ `.{0}` (step vs block), matches the bare path, and composes with Q_T4a's `admins^first.0`. **Lands at P3c**; P3b untouched.
- **Remaining before P3b: the P6 demand decision** (presented — recommendation: defer to a named post-v1 phase; the lift is a runtime rep change touching `.pnet` + the effect gate, and Stratum 3 is comment-only).

### Q_U3 RULED — demand DEFERRED post-v1, option (c); the P3a checkpoint queue is EMPTY  (2026-07-30)

- **Q_U3** [owner: "defer it — (c) stands"]: demand semantics → a named post-v1 phase (charter stub in D4 §5.P6: thunked-champ vs demand-mark · `.pnet` thunk serialization · the effect gate · the whnf memo · sequencing against the effect stratum). The 4a X.close gate stays armed — the PIR must re-state the staged status. Spec §1.3 tag sharpened. Eyes-open: `[now]`/`[env …]` leaves keep forcing at def-commit through v1.
- **P3a is fully CLOSED** (code `290f77f9` · docs `5e6d9f41` · rulings `c1e185ce`/`3647fdd8`/this commit). **NEXT = P3b**, light re-grounding only.

### D4.P3b LANDED — the `^` family; the verify caught a BLOCKING whole-file abort of mine (`36ce601c`)  (2026-07-30)

- **Why**: P3b per Q_T3/T4a/T4b/T4b′/T7/T8 + Q_U1's reassigned §G pair. Light re-grounding only (per the relay): coordinates verified at `de9caeec`, the hazard-4 lex probes run (all `^` continuations GLUE — zero tokenizer changes held, so Q_T5's no-corpus-A/B exemption stood), the Q_T3 naive-lowering baseline re-probed. Failing-test-first: **29 pins RED at open**, each failing on the P3a pointer / stray-`.` text it was named for. Suite **9469/475/0** (+51), acceptance **50/50** (from 41), track battery 178, neighborhood 433/10 files.
- **Shipped**: THE ONE SPLITTER (`split-caret-lexeme`, continuation grammar `-`?·{ε|label|`_`}) · the `(@key name cont)` step vocabulary + the **SHARED branch walk** in syntax.rkt — parser dup-check, typing and reduction consume the SAME `select-branch-top-keys`/`select-synth-name`, so Q_T3's check and the assembly meaning cannot drift · dissolve/splice · in-place rename · `^_` Reading N · the `^-` family · `^..` desugared at segmentation to the ruled `[P^ L^P]` · Q_T4a's ONE message across all three datum shapes · the malformed battery · keyless leaf → P3c pointer. P3a's `re-key-sym?` gate demolished as planned; its pins flipped deliberately, not discovered.
- **⭐ THE HEADLINE — the verify's BLOCKING catch, 7th consecutive slice, and it was MINE**: the first ordinal-rekey seat replaced the WHOLE datum with the guided marker, so a `match` arm containing `v[0]^` lost its `->` and raw-crashed the reader — **whole-file abort where HEAD recovered per-command** (same root cause: swallowed defn clause, map-literal arity lie, shredded pipe inits). Fixed ELEMENT-WISE (the P1a dot-key precedent: marker replaces base+ordinal+caret, siblings survive) + 11 regression pins. The crash SITE (arrowless match arms) is pre-existing → DEFERRED 22.
- **Eight SIGNIFICANT, all fixed pre-commit**: fused `^..enabled`/`^..{…}` continuations missed the two-bare-dot lookahead (INVERTED stray-dot advice on a spaceless input — the lookahead now takes fused second dots; spaced ≡ fused pinned) · `k^...label` leaked `($rest-param …)` verbatim · **the dup message's `^_` remedy REPRODUCES the collision in both canonical classes** (dissolved-ancestry synth = the colliding leaf name, correct per Q_T4b′ — `^_` dropped from that message, the advice-that-does-not-work class) · the P3c ordinal-step pointer now names its phase (closes the Q_T4a advice loop — the ruled example `admins^first.0` lands on it until P3c) · a VACUOUS `#rx"-"` pin of mine matched the temp-file path (3rd sighting of the arc's own trap) · digit-leading rename targets minted a dot-unreachable `:0` field (refused — the promoted minor) · `{0^first}` escaped the ONE message · `server^{x}` lost its segment name.
- **First-battery catch worth keeping**: the initial `^_` implementation synthesized from the TRUNCATED tail branch (`server.host^_` → `:host`, not `:server-host`) — the recursion lost consumed steps; fixed by threading `seen` steps so the SHARED synth walk sees the full branch. The RED battery caught it before the verify even ran.
- **Q_U4 candidate flagged (not silently decided)**: `^_`/`^-_` synth scope implemented as **branch-of-its-block** (`server.{host^_}` → `{:server {:host …}}` vs `server.host^_` → `{:server {:server-host …}}`). Every ruled example agrees under both readings; the divergence is observable only at P5's L2 factoring. If factoring-consistency should win, the scope flips at P5.
- **Filed**: DEFERRED 21 (`k^:x` — the splitter's `#\:` arm is WS-dead, degraded-not-lying) · DEFERRED 22 (arrowless match arms raw-crash, pre-existing, Q_L4 marker-seat class).
- **Next**: P3c (keyless + L4 + honest nesting + Q_U2's ordinal-step lift) — light re-grounding; its demolition sites are the keyless-leaf and ordinal-step refusals landed here.

### Q_U4 RULED — subject-root synth scope PREFERRED, flip deferred; P3c OPENS  (2026-07-30)

- **Q_U4** [owner: "subject-root scope is preferred, but it's not a high priority feature per se, so we can switch to the later when it matters next"]: the shipped branch-of-its-block `^_`/`^-_` synth scope stands for now; subject-root is the preferred end-state (a sub-branch is less likely to share a common leaf key). DEFERRED 23 carries the trigger (P5's L2 factoring, which wants `x{server.host^_}` ≡ `x{server.{host^_}}` as a theorem) + the one-line flip site (thread `seen` through the `@sub` boundary in typing + reduction). Folded into D4 §3 + the §5.P3b close notes.
- **The P3b checkpoint queue is EMPTY. NEXT = P3c** (keyless + L4 + honest nesting + Q_U2's ordinal-step lift), opening with the light re-grounding.

### D4.P3c LANDED — keyless + L4 + ordinals; P3 (BLOCKS) COMPLETE; the verify's first no-BLOCKING slice in eight (`1b021d57`)  (2026-07-30)

- **Why**: the last P3 slice per Q_T5's forced ordering + Q_U2 + ruling 2a. Light re-grounding probed the three demolition sites, the `pvec-slice` ⟨⟩ mint (the honest-nesting machinery pre-exists), and surfaced the **G11 conflict** (the recorded `{a^0}` rename-to-`:0` was overtaken by P3b's digit-target refusal — pinned AS AMENDED, both halves loud, owner-validated at the opening). Failing-test-first: **16 pins RED** on the three P3c pointers. Suite **9497/475/0**, acceptance **52/52**, track battery **204**, neighborhood 410/11.
- **Shipped**: keyless `^`-terminated branches (leaf VALUES, written order, B5 no-dedup — `{version^ version^}` is a legal ⟨String String⟩) · ordinal branches `{N M}` (fresh indices) · **Q_U2 Reading A** ordinal STEPS (descend, no output level; `{admins.0}` ≠ `{admins.{0}}` pinned) · the `(@ord N)`-head vs bare-number-step vocabulary (dissolve-splice continuations keep their keys) · **L4 at the OUTPUT level** over the shared walk's components · the nat-row mint at EVERY n (`⟨String⟩` 1-tuples honest) · `select-index-of` + components `(key-or-#f . field)` in typing; `rrb-from-list` + LOUD bounds-checked `index-into` in reduction · **the Q_T4a advice loop closes end-to-end** (`{admins^first.0}` executes).
- **Two of my own twin-drifts caught at the RED battery, pre-verify**: reduction's `below-value` dropped the `seen` synth threading; the whnf arm admitted only champ subjects (ordinal blocks over vectors would have panicked as non-maps). The shared-walk discipline is doing its job — both were divergences from typing that the battery surfaced same-session.
- **⭐ THE VERIFY: NO BLOCKING — first slice in EIGHT.** 2 SIGNIFICANT + 3 MINOR, all fixed pre-commit. The big one (mine, the Exhaustive-Walkers class): `walk-to-leaf`'s dispatch missed the `(@ord N)` pair, so `admins{0.name^}` got a lying "not a record" and records got a BLANK generic (the hint's swallow-all ate a format throw). Fixed in BOTH walks atomically — the adjudicator flagged that a typing-only fix would have CREATED a blocking (runtime champ-of panic). Also: the `{N.M}` decimal-fusion sentinel leak (guided arm; leak pre-existing, advice-staleness diff-introduced — adjudicator-split); `.-1` invisibility; in-block `v[0]` ≡ `.0` ratified-by-pin (Q_R1 extended); two stale pin titles.
- **P3 IS COMPLETE** (P3a `36ce601c` · P3b `36ce601c`-family · P3c this commit): the full block surface — keyed projection, the `^` family, keyless tuples, ordinals, L4 — is live end-to-end with zero tokenizer changes across the phase.
- **Next**: P4 broadcast ω (opens with its mini-audit — NOT light: P4 is a fresh design surface with the 2b heterogeneity split, map-generic `:`, disclose, and the step-list node question). The components model is broadcast's natural assembly substrate.

### The multi-clause `defn` lying diagnostic — CLOSED; the verify broke my own gate premise (`04085007`)  (2026-07-30)

- **⚠ WHERE THIS LANDED**: worktree `.claude/worktrees/relaxed-mcnulty-bfefb0`, **branch `relaxed-mcnulty-bfefb0`**, base `5e6d9f41` → commit **`04085007`**. NOT on main, NOT pushed. The STATE head above still describes the main-checkout P3a→P3b arc and is deliberately left alone — this was the standalone task the P3a entry spawned ("multi-clause defn clauses with HETEROGENEOUS result types fail PRE-EXISTING with the lying unannotated-param message"), not a D4 phase. Owner merges at will.
- **Why**: the P3a scope find, taken on its own. Grounded first (HEAD-pinned facet fan-out + completeness critic), then implemented, then adversarially verified — the verify paid for itself again, **7th consecutive slice**.
- **The defect was worse than reported**: not "under-determined" but **VACUOUS**. `infer` on a hole-domain lambda returns `(expr-error)` WITHOUT inspecting the body (typing-core.rkt:1129), and `compile-pattern-group` hard-codes `(surf-hole loc)` as every generated clause binder's type (macros.rkt:10282/:10294) **even when a `spec` is present** — so the guard reduced to "e is a generated clause lambda" and fired for ANY failure cause. Its own advice ("add a `spec`") is unable to help for exactly the same reason. Confirmed empirically: adding a spec does not change the message. The real cause is a FIRST-ARM-WINS join (one motive meta, solved by branch 1, then `nf`/`whnf` resolve it so branch 2 checks against branch 1's type).
- **⭐ THE VERIFY'S HEADLINE — my "generated dispatch" gate was not a discriminator at all.** I keyed on an `expr-int-eq` boolrec target as "this is generated clause dispatch". But `int-eq` is a **user-callable primitive**, so `(if [int-eq x 0] 1 "s")` — a ONE-clause function — was reported as "every clause of a multi-clause definition"; and user `match` compiles through the SAME `compile-match-tree`, so a single-clause defn with a `match` body got it too. **And the test I wrote to assert the exclusion was VACUOUS** (`if true 1 "x"`: Bool-literal target, no lambda to peel — it exercised neither guard). Post-elaboration there IS no generated-vs-user discriminator. The fix was not a better gate but a TRUE statement: the message says **"branches"**, which holds for defn clauses, `match` arms and `if` alike — and dropping the impossible gate also picked up the guard-fallthrough boolrec it had been silently excluding.
- **Three more from the verify, one BLOCKING**: (a) a **WRONG type with raw de Bruijn junk** — `[Pi [x <?bvar0>] ?bvar1]` for a body whose real type is `Int -> Int`, because `names` is never extended alongside the ctx the walk builds; (b) **order-dependent firing** — `conv` treats `expr-hole` as a WILDCARD so the dedup fold was NON-TRANSITIVE, and two defns differing only in ctor declaration order got different messages; (c) a **silent correctness cliff at exactly 64 clauses** (bisected 63 fine / 64 lying, identical wall time) from a depth guard that removing `whnf` had already made unnecessary. All three fall to ONE filter: refuse to report a type mentioning a hole or a bvar.
- **Surprise — a hole hides behind TWO non-expr layers.** That filter took **three rounds**. `expr-subfields` does `(filter expr? f)` on list fields, so a Record's list-of-(label . type) **pairs** yields nothing; and gating the struct descent on `expr?` stops one layer short of the type, because a field's value is a **`record-field` wrapper struct** (syntax.rkt:690). `{:v _}` printed as a user type through both partial versions. The scan must descend ANY transparent struct.
- **Earlier, self-caught**: `whnf`-beta-reducing the clause let-redex **over-reduced a record literal's map-assoc chain** into a runtime value whose type no longer inferred, so every record-bodied clause silently lost its type — while Int/String cases passed the whole time. A battery without a record-bodied case ships that green. Descending under the binder replaced it.
- **Union route TRIAGED and REJECTED** (the task asked for the triage explicitly): a union codomain forks at EVERY call site (`maybe-emit-fork-on-union-request`), and that machinery holds two open unbounded-hang defects. A hang is strictly worse than a bad message; also the same join serves user `if`, so a semantic change is project-wide. Recorded with the evidence, not as a hand-wave.
- **Two PRE-EXISTING defects found and independently re-verified**, both filed: a guarded clause group with no `[params]` header **crashes with a raw Racket `list-ref` and aborts the whole file** (the same whole-file-abort silence class as the `.( )` and tilde-reader entries); and the join **silently ACCEPTS a float branch against a String branch** (`| 0 -> 1.5 | n -> "x"` → `Int -> String`, 0 errors, with the Int control erroring in the same run) — the join is not merely strict, it is unsound there.
- **Gate**: suite **9429 / 475 / 0**; all **5** CIU T6 acceptance files pass `--check` (exit 0); +12 test cases (43 in test-error-messages.rkt), every verify defect regression-pinned. Diagnostic-only — zero typing behaviour changed, A/B-confirmed by the verify (identical error counts and positions, only the hint text differs).
- **Watching (feeds the promotion queue)**: *"a gate is only a discriminator if the thing it keys on is unreachable by user code"* — NEW, and the same shape as Watching-2 (probe before ruling): I reasoned about `int-eq` as an internal marker without checking that it is a documented user-facing primitive. Also a 9th data point for Watching-3 (**my own enumeration under-counted** — the reportability scan, three times).

### QTT now checks pattern matching — the missing alternation operator, and a linear API that was decorative (`966226cf` + `9fbbc90f`)  (2026-07-30)

- **⚠ Same worktree/branch as the entry above** (`relaxed-mcnulty-bfefb0`, base `5e6d9f41`), NOT main, NOT pushed. Came in as a Suggested-task chip from another session ("Add expr-reduce arms to inferQ/checkQ"), verified against its cited HEAD `02dd27d7` — `driver.rkt` and `qtt.rkt` are BYTE-IDENTICAL between that SHA and this branch, so the verification transfers exactly.
- **Triage of the chip's four claims**: three CONFIRMED (the guard's `expr-reduce` entry + both driver gates; `grep -c expr-reduce qtt.rkt` = 0; and the one it flagged as second-hand — fio's three `-1>` specs are all `match`-implemented, verified first-hand). The fourth (a false "Multiplicity violation" from the guard's `[_ #f]` on a match nested in a map) **did NOT reproduce**: nested pattern-match does not elaborate at all, and an `expr-foreign-fn` in a map/list was clean too. Reported unreproduced rather than impossible — I did not exhaust 9 node kinds × all containers.
- **⭐ THE GAP, demonstrated minimally**: identical violation one line apart — `defn dup [b] (pair b b)` correctly ERRORS, `defn dup-match [b] match b (mk-box n -> (pair b b))` was **accepted with 0 errors**, and its printed type was `[Pi [x :1 <Box>] …]` — the compiler *recorded* the parameter as linear and handed back a function duplicating it.
- **⭐ WHAT THE GROUNDING CHANGED ABOUT THE FIX — the chip's own shape was wrong in two places.** (1) "joins branch usages" is right, but **the only in-tree precedent does the opposite**: `boolrec` SUMS them, and an implementer reaching for the nearest model would have injected false violations across 1395 multi-clause lines in 64 files. (2) "delegate the TYPE to `infer`" is **structurally inapplicable** — `checkQ` returns `bu`, which has no type field, and `infer` has no `expr-reduce` arm either. Both only surfaced because the grounding was adversarially critiqued.
- **P1 — the missing operator.** `mult-add` is semiring addition (m1+m1 = mw) and PPN 4C Phase 2 deliberately analysed it, accepting `:usage` as a commutative MONOID and recording "REFUTE idempotence". **That finding stands** — it was about the tensor. ALTERNATION is a second operation the tree simply had no operator for, so `:usage` carries both; I amended the D2 note in place rather than overturning it, and kept the join arm-local (a typing-rule combinator, NOT a second SRE merge relation). `mult-join` is the lub of `mult-leq` and **differs from `mult-add` in exactly ONE cell** (m1⊔m1 = m1), which makes the swap monotone-permissive: it can only accept more, never reject more. 5 sites (boolrec + four posit `if-nar`s); natrec deliberately left on `add` — its step runs 0..n times so joining would UNDER-count.
- **P2 — the arm.** `reduce-arm-ctx`/`reduce-scrutinee-decompose` factored OUT of `check-reduce-structural` and exported, so the QTT arm uses the type checker's OWN derivation (one derivation, two consumers). Plus a checkQ **beta-redex** arm: `let` desugars to `(app (lam …) arg)` and checkQ had none, so a let-bound `match` would have reached inferQ (no reduce arm) and reported a spurious violation — harmless while reduce was skipped wholesale, load-bearing the moment it wasn't.
- **⭐ THE CRITIC'S CATCH THAT NOTHING ELSE WOULD HAVE FOUND — the `.pnet` bump.** On a cache hit the driver deserializes and NEVER elaborates, so the QTT gate does not run; every `.pnet` on disk was written while match bodies skipped `checkQ`. Without `PNET_VERSION` 6→7 in the same commit, a module that should now fail keeps loading from cache and **the suite goes green on a warm tree while a cold clone fails**. The file documents this exact precedent ("POISON INVALIDATION"), and no facet mentioned it.
- **FALLOUT: none.** Suite **9445/475/0**, all 5 acceptance files `--check` exit 0. fio loads clean with its linearity genuinely checked for the first time — the declarations were never verified and they hold. The reason the blast radius was ~0: almost all code is `mw`, where any usage is compatible.
- **⭐ FILED AS AN OWNER DECISION, not defaulted**: `m0 ⊔ m1`. The critic demonstrated on the REAL fio API that HEAD's behaviour was **inverted** — `always-close` (closes on every path, the CORRECT linear program) was REJECTED while `maybe-close` (closes on one branch, silently DROPS the handle = fd leak) was ACCEPTED. P1 fixes the first; the leak stays accepted under the lenient cell. Three options recorded incl. the one the first analysis never enumerated (lenient join + per-position m1 agreement guard = strict semantics with a PRECISE failure). Do not read the shipped default as an endorsement.
- **Redex checked, no action needed** (verified, not skipped): the model has NO boolrec/reduce/if-nar rule; its only branch-combining rule is natrec, which I did not touch — so spec and implementation still agree.
- **Twice this session a test of mine failed for the WRONG reason** (`add` unbound; sexp `Sigma` needs a binder form) — Watching-4's exact shape, caught only because I read the failure text instead of the pass/fail. Moved the branch-usage tests down to the AST level (`checkQ-top` + `expr-boolrec`), where there is no surface syntax to fight.
- **Also filed**: natrec's step counted once though it runs n times (recorded, not defaulted); retire `contains-unsupported-qtt?` by arming the remaining 8 nodes (12 recurse / 8 flag / `[_ #f]` over ~344 structs — it both over-skips and under-detects).

### QTT P3 — linear types are linear PER PATH; the VAG changed the implementation (`3a4d521a`)  (2026-07-30)

- **Owner ruled option 3** on the `m0 ⊔ m1` question filed at P2: *"linear types should always be linear... there's a correctness concern otherwise."* Ran the Stage-4 Per-Phase Protocol properly for the first time in this arc (the earlier P1/P2 were chip-driven and skipped it).
- **Design doc created RETROACTIVELY** (`2026-07-30_QTT_PATTERN_MATCHING_DESIGN.md`) because this work arrived as a Suggested-task chip with no Stage-3 predecessor — Progress Tracker P1–P4 + X.close, the ruling and its evidence, the mini-design, the mini-audit, and the two-column VAG. QTT is now a Series in `MASTER_ROADMAP.org`. The methodology assumes a design doc exists; when it doesn't, write it rather than skip the step.
- **Mini-design named 5 drift risks BEFORE any code** — position misalignment, over-reach beyond linear positions, natrec-must-not-get-the-guard, the permissive fallback, scope creep into the diagnostic. Two of the five paid off directly: risk 2 became a two-directional test pin, risk 3 was verified by grep rather than memory. Risk 4 MATERIALIZED (below).
- **Mantra check recorded as NEUTRAL, not passing** — `qtt.rkt` is imperative off-network typing in its entirety; this phase adds a pure predicate, so it neither advances nor retreats. Claiming a "pass" would have been cataloguing.
- **⭐ THE ADVERSARIAL VAG CHANGED THE CODE, twice.** (a)'s challenge column: the guard and the join were two separate calls at all 6 alternation sites — a discipline a 7th eliminator could forget, silently restoring affine-per-path for that construct alone **with a green suite**. Bundled into `join-branches`, so joining branch usages without the linear check is now impossible: correct-by-construction rather than maintained-by-care. Same column also produced the structural framing worth keeping for when typing goes on-network — the guard *is* "the join must be EXACT at linear positions", a side condition on the lattice op, not an ad-hoc comparison. (c)'s challenge: P2's permissive fallback is no longer merely "may miss a violation" — a skipped arm is never checked for AGREEMENT either, so the exact leak P3 rejects can hide in the one path that doesn't look. Comment sharpened, hole filed. **This is the third arc where Column 2 was where the work actually happened.**
- **Named the benefit that did NOT land**, up front in §5.P3 rather than discovering it at close: option 3 was argued partly on being *able* to name the dropped resource; `bu`/`tu-error` carry no payload, so the rejection still reads generic "Multiplicity violation". Tracker row P4. Shape-without-benefit is only invisible deferral if you let it be.
- **Gate**: suite **9455/475/0**, acceptance 5/5, +5 tests. Behavioural change is intended and narrow — a linear var consumed on only some branches now errors; the only in-tree instance was a scratchpad probe. On the real fio API the pair is finally correct in both directions (`always-close` ✓, `maybe-close` ✗) where before P1 it was **inverted**.
- **Watching**: *"a discipline maintained at N call sites is a defect waiting for the N+1th"* — 2nd data point this session (the first: the branch-result hint's gate that keyed on a user-callable primitive). Both were caught by forcing Column 2, neither by testing.

### QTT P4 + X.close — the multiplicity message finally says something; QTT track COMPLETE (`e7fbd2ba`)  (2026-07-30)

- **⚠ Still the worktree branch** `relaxed-mcnulty-bfefb0`, not main, not pushed. **No PIR** (owner ruling): the track arrived as a chip, not a Stage-3 design, so `workflow.md`'s objective PIR gate does not apply — the per-phase VAGs + close notes serve instead. X.close = roadmap row + DEFERRED sweep + these notes.
- **⭐ THE MINI-AUDIT REFUTED THE PLAN'S OWN PREMISE.** §5.P3 (written by me at P3) said `bu`/`tu-error` carry no payload, so P4 would need a protocol refactor or a post-hoc walk. **Wrong.** `multiplicity-error` has ALWAYS had `variable`/`declared`/`actual` fields that ALWAYS rendered as three lines — they were filled with the string literals `"declared"` and `"actual"`, and `Variable:` got the whole pretty-printed body. P4 turned out to be *computing values for fields that already existed*. Nth data point for Watching-2 (probing before ruling changes the ruling) — and this time the refuted premise was **my own, one phase old**.
- **What the message does now**, all four classes: linear-used-twice / linear-unused / erased-used / **branch disagreement** (the P3 class that had no diagnostic at all). e.g. *"a linear value must be used exactly once on EVERY path, but the branches disagree: it is used once in one branch and not used in another. Dropping it on a path does not release it (there is no implicit destructor)."*
- **`explain-qtt-failure` lives in `qtt.rkt`, not typing-errors.rkt** — explaining the failure means reproducing the tests that produced it (the lambda arm's `compatible`/`uhead`, each eliminator's per-branch expected types, per-arm binder stripping). Re-deriving those in the error module is the twin drift `pipeline.md` names. Same rationale as typing-core exporting `select-project` for the hints.
- **⭐ THE VAG CAUGHT A SHAPE-WITHOUT-BENEFIT AT THE GATE, not after it.** First draft filled the struct fields correctly and left the message as bare "Multiplicity violation" — and `tools/run-file.rkt`, the ~11 test files copying its `result->string`, and the `;;N=>` acceptance harness all print `prologos-error-message` **ALONE**. The whole improvement was invisible in exactly the surface users see. Caught by running the probe instead of trusting the struct. **Generalizable, and worth promoting: a diagnostic is not done until it is read back through the renderer the user actually gets.** (This hazard was in the P2 grounding and I failed to carry it into P4's named risk list — the risk list is only as good as what you transcribe into it.)
- **Honest limit**: the message says "the parameter of type T", not a variable NAME, because `recover-name-map` is best-effort and `driver.rkt:1913` passes no names at all. Naming the type is what can be PROVEN; inventing a name is the failure mode this project keeps re-learning.
- **Gate**: suite **9460/475/0**, acceptance 5/5, +5 tests incl. an **E1003 LSP pin** (the code is regexp-derived from message text with `type.?mismatch` tested FIRST, so a reword could silently retag the class — now test-guarded rather than comment-guarded; second time this session that coupling needed pinning).
- **Track summary** — `match`/multi-clause `defn` went from **no multiplicity checking at all** to checked; fio's `-1>` handles from declared-but-never-verified to verified (they hold); a linear var used once per branch from **rejected** to accepted; a linear var dropped on some branches from accepted (**fd leak**) to rejected; and the message from three placeholder lines to an explanation. 9418 → 9460 tests, 0 failures throughout, zero stdlib fallout.
- **Residuals carried in DEFERRED** (not silently dropped): natrec's step counted once though it runs 0..n times; the Church-fold arm never checks branch agreement so a leak can still hide there; retire `contains-unsupported-qtt?` by arming its remaining 8 nodes.
- **Watching**: *"a discipline maintained at N call sites is a defect waiting for the N+1th"* now at **2 data points** (P3's split guard/join; the branch-result hint's gate). And a new one at 2: *"the struct field is not the user surface"* — detail placed in a rendered-but-unprinted field is invisible; check the renderer, not the struct.

### QTT P5 — the belt-and-suspenders guard is DELETED; multiplicity checking is unconditional at the def seam (`9f0ddede` + `7b14fffe`)  (2026-07-30)

- **⚠ Still the worktree branch**, not main, not pushed. Reopened the closed QTT track for the first DEFERRED residual (the guard retirement).
- **What the guard was**: 47 lines that flagged 8 node kinds `#t`, recursed 12, and ended at `[_ #f]` — over ~344 expr structs. It **over-skipped** (one Vec anywhere disabled QTT for the entire def) AND **under-detected** (anything outside the 12 recursed kinds stopped the walk and was reported "supported" without being looked inside). `workflow.md` names this shape: the guard WAS the dual path masking qtt.rkt's gaps.
- **⭐ THE LOAD-BEARING FINDING — the four constructors had to be checkQ arms, not inferQ arms.** typing-core has no `infer` case for vnil/vcons/fzero/fsuc either (check-only there too), so an inferQ-only arming would have left the conversion fallback → inferQ → `[_ (tu-error)]`, and **every annotated Vec/Fin def would have died as a generic "Multiplicity violation"** the moment the guard went — the lying-diagnostic class, on the very surface this phase makes checkable. Two-commit split (arms, then deletion) meant this was verified against the twins before any behaviour flipped.
- **The usage rules came from `whnf`, not from reading field names.** Its own computation rules `[(expr-vhead _ _ (expr-vcons _ _ hd _)) …]` / vtail DISCARD the type and length fields and consume only head/tail — that IS the erased/runtime split, proven rather than inferred. vcons ADDS head and tail (both stored: sequential, not alternation), so Vec is linear-safe; the eliminators pass the subject's usage through, which is exactly the fst/snd projection stance already in the file, not new laxity.
- **⭐ THE CRITIC'S CATCH, again the cache**: PNET_VERSION 7→8 had to ride the deletion commit — 24 of 40 `.pnet` files carry serialized `expr-foreign-fn` structs, all written while those bodies were QTT-skipped, and on a cache hit the driver never elaborates so the gate never runs. Warm tree green, cold clone red. Second time this session that the cache was the thing no facet volunteered.
- **Corrections the critic made to the facets** (worth recording, all probe-backed): only **3** capture-bearing defs become newly checked, not 15 — zero-capture `racket{}` blocks eager-evaluate at elaboration and never build a foreign-fn node; and Vec/Fin **ARE** WS-reachable (the tree-parser delegates parenthesised datums to the sexp parser), so a Level-3 file test was warranted, not just sexp pins. Both facets had asserted the opposite.
- **Scope stated precisely rather than over-claimed**: unconditional **at the def seam**. Bare top-level exprs, `check` commands, foreign decls and cache-hit loads stay outside the gate. Also verified NOT a hole: the champ/hset/rrb opaque zero-usage arms are SOUND, because the SUB ruling makes runtime containers closed w.r.t. de Bruijn vars by construction — a linear bound var structurally cannot occur inside one.
- **Gate**: suite **9473/475/0**, acceptance 5/5, Level-3 WS Vec/Fin file clean, +13 tests. Before P5 there was **no driver-path Vec/Fin test in the tree at all** — the existing ones drive typing-core directly and never touch the gate. New pins include a linear-consed-twice negative (with its consed-once positive control) and a **linear-capture-into-`racket{}`** negative that nothing covered before.
- **Filed**: 4 residuals (vindex checks but does not compute — no whnf rule; Vec/Fin lack pnet-serialize registration; Redex has no Vec/Fin QTT rules so these ship spec-unbacked; foreign-fn's type is arity-wrong once `args` is non-empty, equally in both twins) **plus a LATENT tripwire**: `shift`/`subst` call `expr-foreign-fn` an *"opaque leaf — no Prologos sub-expressions"* while `reduction` accumulates exprs into its `args`. Probed both shapes — not reproducible, because substitution happens on the enclosing app before whnf ever builds the partial. Live tripwire, not live bug; exactly the comment-asserts-an-unenforced-invariant shape `pipeline.md` documents.
- **Watching**: *"a diagnostic is not done until it is read back through the renderer the user actually gets"* stays at 1 (P4). New at 1: *"the two-commit split (capability, then behaviour flip) is what makes a risky retirement verifiable"* — arming and deleting in one commit would have made the wrong-arm-kind failure indistinguishable from a fallout.

### QTT P6 + P7 — the Church-fold hole was LIVE (leak + double-free), and "natrec" was really 9 nodes (`e5810cfe` + `63dea0b6`)  (2026-07-31)

- **⚠ Still the worktree branch**, not main, not pushed. Reopened the closed QTT track for the last two residuals, owner order: church-fold first, then natrec.
- **⭐ P6 WAS NOT THEORETICAL.** A/B at the parent commit: `leakB` (a linear `Handle` dropped), `dupB` (**DOUBLE-FREE**) and `leakF` all **DEFINED with 0 errors** — and `leakF` was then *called*, running the leaking path — while the byte-identical **Bool-scrutinee control was correctly rejected**. After P6 all three error. The mechanism is worse than "an arm is skipped": the skip **swallows NESTED violations** — an arm whose own body contains a linear-per-path disagreement fails its checkQ, gets skipped because the Church path has no derivable arm ctx, and the disagreement vanishes.
- **My own 3 probes failed to reproduce it** before I stopped re-deriving and ran the critic's actual artifact off disk. The difference was subtle: I returned the linear directly (arms check fine individually → `join-branches` catches it) instead of *consuming* it via a call inside a nested match. **The diagnostic protocol's "stop iterating after 2-3 attempts" earned its keep** — and the lesson is narrower than "probe first": *reproduce the reporter's artifact before building your own*.
- **P6 fix**: refuse only when the skip is load-bearing — an arm was skipped AND a linear is at stake in the surviving arms' accumulated usage. Checked at the END of the arm loop (an arm skipped *before* the consuming arm would otherwise slip through). Nothing linear in play → permissive path untouched, pinned.
- **⭐ THE EXPLAINER HAD TO MOVE WITH IT, and was already out of sync**: it bailed on `(and tc …)`, abandoning the Church path entirely, so it could not describe the case the checker was deciding. Fixing that exposed a SECOND mismatch — an arm that fails but *is* analysable is not a skip, and my first version mislabelled an ordinary Bool `match` as "cannot be analysed". Now recurses to find the nested cause. Three false clauses in the arm's own comment corrected in place (it claimed the fallback "cannot invent a violation", that outer variables stayed tracked, and that "the baseline is no checking whatsoever" — P5 had deleted the guard).
- **⭐ P7 WAS FILED WRONG, TWICE OVER.** (1) **Scope**: natrec has ZERO shipped uses; the identical defect sat in **8 HOF primitives with 121 shipped uses** (`pvec-fold`/`-map`/`-filter`, `set-fold`/`-filter`, `map-fold-entries`/`-filter-entries`/`-map-vals`). Fixing only natrec would have fixed the one node nobody uses. Owner ruled all nine. (2) **Framing**: it is not a tightening but the **app rule finally applied** — `(add-usage u1 (scale-usage m u2))` already scales an argument by its binder multiplicity, and these arms *synthesize an mw Pi* for their function argument then fail to scale by it. A user-written HOF capturing a linear was already rejected; only the built-in twins accepted it. I verified that app-arm mechanism myself before trusting the reframing.
- **J folded in per owner, and was worse than both**: its arm verified the base with typing-core's *boolean* `check`, which computes no usage, so a linear consumed in J's base was counted **zero** times. Switched to `checkQ`; ADDED not scaled (J's base runs exactly once).
- **Gate**: 28 sites across both twins + natrec + J. Suite **9483/475/0**, acceptance green, and inert on all 121 shipped HOF uses exactly as predicted (dict params are mw; `mult-mul mw mw = mw`, so only an m1 capture moves). Erasure safe (`mw × m0 = m0`), closed functions unaffected — which is what makes the escape hatch (thread the resource through the accumulator) real. All test-pinned at both AST and surface level.
- **Refuted a scary claim before acting on it**: the critic reported `map`/`filter` over `List` failing at HEAD on unrestricted code and called it a possible P5 regression outranking both items. It does not reproduce — `map`/`filter`/`foldr` all work with correct values; their probes were hitting unrelated syntax errors (mine did too, the same way). **Suite green + a direct probe settled it in two minutes; acting on it would have derailed the session.**
- **Filed, NOT folded in** (each deserves its own test): an unknown constructor in a match pattern silently becomes an irrefutable VARIABLE pattern, making later arms dead code with zero diagnostics; a constructor from an UNRELATED data type is accepted in an arm; and linear destructuring via multi-clause `defn` is rejected though the fio spelling of the same thing works (A/B-verified PRE-EXISTING, not ours).
- **Watching**: *"reproduce the reporter's artifact before building your own"* — NEW, 1 data point, but it cost 3 probes today. And a 3rd for *"the struct field is not the user surface"*'s sibling: **a fix without its explainer is a lying diagnostic** — P6's rejection was a bare "Multiplicity violation" until the explainer arm landed, which would have re-opened P4's own objective.

### Three pattern-matching soundness fixes — and the third one's root cause was `A -1> B` being read as `A -> B` (`55aac8c4` · `09e68e60` · `6d4e8c73`)  (2026-07-31)

- **⚠ Still the worktree branch**, not main, not pushed. These are the three finds from the P6 grounding, taken in owner order (dead-arm first).
- **⭐ THE DEAD-ARM DEFECT WAS BROADER THAN REPORTED.** Filed as "an unknown ctor becomes a catch-all"; that is one *instance*. The general defect: an arm after ANY irrefutable arm is **dropped by the pattern compiler and therefore never TYPE-CHECKED** — `match v (n -> 1N) (zero -> "dead")` defined clean with a String body where a Nat was expected. So I fixed **reachability**, not constructor spelling: a pattern variable is a binding occurrence, so a lowercase name is genuinely ambiguous between "variable" and "typo'd constructor" in isolation, and any spelling-based check needs a heuristic. Reachability needs none and catches both shapes plus any future producer.
- **Guard and final-position clauses are load-bearing and pinned**: `| n when p ->` is refutable so later arms stay reachable; an irrefutable arm LAST is the idiomatic default. Wired at BOTH entry points (`compile-match-expression` and `compile-pattern-group` — they do not share a path).
- **A second fix was needed to make the message usable**: `expand-expression` is a structural rebuild with **no error propagation**, so the error value was carried into the elaborator and printed as `Cannot elaborate: #(struct:prologos-error …)` — the struct itself. Propagation added at the `surf-def` command boundary (the one place every def's body passes through) plus `surf-lam`. Deeper nestings still leak; filed rather than arming every arm, which is the exhaustive-walker hazard.
- **Cross-type ctor** (`09e68e60`) turned out NARROWER than the dead-arm one — probing showed the wrong-type arm's body IS checked, and a genuinely unknown name was already caught; only a ctor that *exists elsewhere* slipped through. `ctor-belongs-to-type?` consults the same registry `reduce-scrutinee-decompose` uses (agree by construction) and DECLINES when membership is undecidable, so it rejects only what it can show is foreign.
- **⭐ THE THIRD ONE'S ROOT CAUSE WAS NOWHERE NEAR ITS SYMPTOM.** Filed as "linear destructuring via multi-clause `defn` is rejected". Actual bug: `extract-pi-binders` matched a `surf-arrow`'s multiplicity field as `_` and substituted `'mw` — so **every binder derived from an arrow type came back unrestricted, and `A -1> B` was indistinguishable from `A -> B` to every consumer**. The `defn`-form symptom was just where it surfaced (generated lambda got 'mw, Pi said :1, checkQ's lam-vs-Pi arm requires equality). Two spellings of the same function now agree, pinned.
- **⭐ THE CONTROL I ALMOST DIDN'T WRITE**: after that fix all four probe cases passed — which is *also* what "I disabled the check" looks like. Wrote enforcement controls: a linear param left unused and an erased param used at runtime are both still rejected (with P4's precise messages) while a correct linear use is accepted. **A fix that makes everything pass needs a test that something still fails.**
- **Gate**: suite **9490/475/0**, acceptance 5/5, fio's linear pair unchanged. +10 tests across the three.
- **Also probed and dismissed**: my `d7` guard case triggered the pre-existing whole-file-abort crash (guarded clause group with no `[params]` header) — already filed, not mine. And `map`/`filter` "broken at HEAD" was refuted earlier the same way: with an explicit import they work identically before and after.
- **Watching**: *"a fix that makes everything pass needs a test that something still fails"* — NEW, 1 data point, but it is the sharpest form of the shape-without-benefit lesson. And a 2nd for *"the filed symptom is not the root cause"* (P7's natrec→9-nodes reframe was the 1st): both times the filed title named the place it was NOTICED, not the place it lived.

### The guarded-clause crash was a missing PARSE — the bare-`|` guard form now works (`f51bda2b`)  (2026-07-31)

- **⚠ Still the worktree branch**, not main, not pushed.
- **The symptom was a whole-file abort**: `def before := 1` / a guarded clause group / `def after := 2` printed NOTHING — not even `before` — just a raw Racket `list-ref: index too large for list, in: '(__arg0 __arg1)`. Same silence class as the `.( )` mixfix and tilde-reader entries: a raise on the expansion path takes the file down by construction.
- **⭐ CAUSE WAS A MISSING PARSE, NOT A COMPILER BUG.** `parse-defn-clause` took everything before `->` as PATTERNS with **no `when` handling** — while the bracketed-header clause parser has always had exactly that split, 200 lines away in the same file. So `n when [int-lt n 0]` became THREE patterns (`n`, `when`, `[…]`), clauses ended up with mismatched arity, and the pattern compiler indexed off the end of its parameter list. The crash was three layers downstream of the actual mistake.
- **So the fix is not "stop crashing" but "make it work"**: mirroring the header path means the bare-`|` guarded form now compiles AND dispatches — and that form is the documented primary dispatch syntax. `classify -3` → 7 / `classify 3` → 5; a three-way `sign` with successive guards falls through in order.
- **Pinned the SEMANTICS, not the absence of a crash** — dispatch, successive-guard fallthrough, the header form unchanged, and a containment pin that a command BEFORE the guarded defn still reports (the crash's most user-hostile property was eating unrelated output).
- **Interaction checked**: a guard makes its clause refutable, so `55aac8c4`'s unreachable-arm check correctly leaves later clauses reachable — already pinned there, still holds.
- **Gate**: suite **9493/475/0**, acceptance 5/5, +4 tests.
- **Watching**: 3rd data point for *"the filed symptom is not the root cause"* (P7's natrec→9 nodes; `A -1> B`'s discarded multiplicity; now this). All three were filed under the place the failure was NOTICED. Worth promoting — the pattern is that a crash/rejection site is downstream of a *parse or derivation* that quietly produced something malformed, and the fix belongs at the producer.

### LET track opens — P0 acceptance + P1: bad `let`s no longer kill the file (`0effba9c` + `49f51c14`)  (2026-07-31)

- **Owner request**: multi-binding `let` blocks (aligned bindings · fused `var:Type` · sibling chains one scope · optional `:=`). Rulings: STRICT body column discipline · mixed spellings allowed · sexp glued `x:Int` splits. Track doc `2026-07-31_LET_BLOCKS_DESIGN.md`; ladder P0–P4 + X.close (X.close also discharges issue #21's doc obligation).
- **Grounding headline (6-agent workflow + critic)**: ONE root cause across all four broken forms — binding normalization is INCONSISTENT across three consumers (`split-last-let` FABRICATES `:=`, `extract-let-binding-tokens` stays verbatim, `parse-assign-bindings` demands `:=` everywhere). Columns are PROVEN GONE at `expand-let` (`syntax->datum` ×5 before dispatch), so the aligned form regroups at the READER layer (P3), not in the macro.
- **⭐ The critic's flagged gap was a LIVE BUG, worse than suspected**: the `:=` sibling chain — issue #21's "working" variant — is **broken in UNSPECCED defns** (tree spine wins at `merge-form`; its independent let-chain's output fails typing). Every prior probe was spec-annotated. Filed; owned by P2. The acceptance file's defns all carry specs, with the header saying why.
- **P0** (`0effba9c`): acceptance file pins the 5 working variants + match-arm + def-:=-RHS contexts, 14 exact markers, `--check` exit 0; P2/P3/P4 targets ride commented.
- **P1** (`49f51c14`): the 13 raw `(error 'let …)` raises — each a WHOLE-FILE abort — now surface as per-command parse errors via ONE `exn:let-syntax` boundary → `($let-error "msg")` marker → the parser's expression-dispatch arm (the retired-selection seat; `(pair? args)` guard included). Messages preserved verbatim incl. the guided top-level error. 5 Level-3 containment pins in the track's new test file.
- **The gate earned its keep twice**: (1) two pre-existing `check-exn` pins flipped WITH the behavior — they pinned the abort class itself; (2) a **batch-worker spec collision**: `spec c` from this branch's own `6d4e8c73` leaked across test files in a worker and broke `test-new-lattice-cell`'s `def c` — passes alone, fails in batch, surfaced only because P1's +1 file reshuffled worker assignment. Renamed to `hconsume`; the LEAK (cell-backed spec registry rides the shared persistent net-box unforked; the parameter snapshot can't cover it) filed with mechanism half-diagnosed. **Watching: any new test file can surface the next collision pair — single-letter spec names in test strings are bait.**
- **Bycatch, spawned as its own task chip**: a `defn` named after a prelude CONSTRUCTOR (`ok`, from Result) type-checks as the function but EVALUATES as the constructor — `[ok 1]` prints unreduced with the defn's type at 0 errors. Found because a P1 probe accidentally named a function `ok`. Issue #72's family.
- **Gate**: suite **9497 / 476 files / 0** · all 6 acceptance files `--check` exit 0. NEXT = **P2** (sibling no-`:=` + the unspecced-spine defect).

### LET P2 — no-:= sibling chains land; my own P0 filing WITHDRAWN as i70-in-a-costume (`e8a41a9a`)  (2026-07-31)

- **The form works**: `let x 4 / let y 5 / let z [+ x y] / body` — one scope, mixed `:=`/no-`:=` allowed (ruling 2). Root cause was the grounding's normalize-vs-verbatim asymmetry: `split-last-let` always fabricated `:=`, `extract-let-binding-tokens` never did; now both synthesize and every `:=` form passes VERBATIM (byte-transparency pinned at the merge layer + test-defmacro's exact-output pins untouched).
- **The tree spine now DEFERS to preparse on let heads** — enacting the driver's own architecture comment (preparse is the stated fallback for "cond, let, multi-arity defn patterns"). The arm it replaces was `:=`-only, annotation-dropping, and one merge arm from letting a junk application surf win silently for no-`:=` bracket heads. Named scaffolding; retires at form-cell let.
- **⭐ THE PHASE'S A/B REFUTED MY OWN FILING AND MY OWN MINI-DESIGN RATIONALE.** The P0-era DEFERRED entry "the `:=` chain is BROKEN in unspecced defns" — which I presented to the owner as "the critic's flagged gap was real, worse than suspected" — was **issue #70 wearing a let costume**: the repro's `[+ a …]` is the documented generic-op-over-unannotated-param limitation, and the no-let control fails identically. A pre-P2 worktree at `974e5cc5` shows the unspecced `:=` chain WORKED with concrete ops. **4th data point for Watching-4 this session — and this one was filed by the process that codified the pattern.** Entry WITHDRAWN in DEFERRED with the full correction (kept, not deleted — a withdrawn filing teaches more than a silent one). The tree-defer ships on the single-implementation principle alone, claimed as exactly that.
- **Test discipline note**: unspecced let tests use CONCRETE ops (`int+`) with an in-file comment naming i70 — the i70 trap is now structurally avoided in this track's tests and acceptance file (§C3's comment points at the design-doc correction).
- **Gate**: suite **9507/476/0** · acceptance 6/6, §C live (20 markers) · the P1 four-broken-classes pin dropped to three (the flip-with-the-feature pattern, 3rd instance this track). NEXT = **P3** (aligned blocks, strict columns — the reader-layer `$let-block` regrouping).

### LET P3 — aligned blocks land; the gate caught a properties-drop that was silently breaking implicit solve (`940c1c16`)  (2026-07-31)

- **The owner's original example works verbatim**: aligned bindings sharing a column, body strictly between the `let` and binding columns, all spellings mixed (`x 4` · `y := 5` · `x : Int := 4` · bare-token body). STRICT per ruling 1: forgot-the-body and mis-indents are guided per-command errors NAMING THE COLUMNS — P1's `$let-error` seat means the reader layer emits them with zero new error machinery.
- **Where**: a recursive eq?-preserving stx walk after `group-items` — the one layer with columns. Activation deliberately narrow (≥2 continuation lines + body signature or all-at-binding-col; `$pipe`-headed continuations excluded STRUCTURALLY — `|` is reserved, so match-bodied lets can never be captured). The forgot-body shape that used to VALUE-SWALLOW silently is now the guided no-body error.
- **⭐ THE GATE'S CATCH — a properties-drop, found by the acceptance suite pre-commit**: the first draft rebuilt every stx via `(datum->syntax #f kids stx)` — srcloc copied, SYNTAX PROPERTIES DROPPED, including POL.9's `'prologos-paren-origin`. Implicit-solve paren goals silently became applications; path-selection acceptance [27]/[28] + two Rel T1 suites went red. **My drift-risk enumeration named LOC hygiene but not PROPERTY hygiene** — the under-counted-enumeration shape again, this time inside my own mini-design. Fix is eq?-preserving by construction (untouched forms keep their stx identity — the pipeline.md reflective-walker idiom), strictly stronger than patching the template.
- **The P0-flagged top-level bypass closed at the FUNNEL**: `:=` is reserved → a body whose second element is `:=` can only be a binding mistaken for a body → refused with a guided message at `let-bindings->nested-fn`, the single point all five formats converge on. No columns needed, no second guard.
- **`split-last-let` gained the `$let-block` arm** sharing ONE normalizer with expand-let's new branch — a bodyless sibling `:=` let followed by an aligned block merges correctly. P2's symbol check in `let-bodyless?` excluded `$let-block` for free (the check earning its keep).
- **Gate**: suite **9513/476/0** · acceptance 6/6, §D live (26 markers) · P1 exemplars flipped forward to fused (3rd flip-with-the-feature). Residual documented: the 2-line forgot-body shape gives a mediocre pre-existing "Unbound variable". NEXT = **P4** (fused `x:Int`, WS + sexp split per ruling 3) → X.close.

### LET P4 — fused binders + multi-line values; an infinite loop caught at the preparse seam (`feb79740`)  (2026-07-31)

- **Fused `x:Int` works in every block form** (aligned/sibling/nested, with and without `:=`), the sexp glued form SPLITS per ruling 3 (it used to bind a variable literally named `|x:Int|` — a silent WS/sexp divergence), module paths never split, chained rejects, and the annotation is REAL (`let x:String 4` = type error). The primitives MOVED to reader-forms.rkt (parser re-imports — the parser→macros cycle made it the only home).
- **⭐ THE MOVE'S NEW CONSUMER EXPOSED A LATENT PREDICATE HOLE AS AN INFINITE LOOP**: `fused-type-annot?` accepted the bare `:` and `:=` (colon-symbols, neither multiplicity-shaped, no length guard), so `parse-assign-bindings`' rewrite arm re-recognized its own emitted `:` forever. The verification battery HUNG; bisected reader → preparse with the stack naming the loop pair; hardened structurally (a fused annotation must CARRY a type). The defn consumer never fed it a bare `:` — **a predicate inherited by a new consumer meets inputs its old consumers never produced** (the session's recurring under-enumeration shape, in predicate form).
- **Multi-line values (owner add-on)**: TWO reader mechanisms — the FOLD (deeper-than-binding-column = continuation of the previous binding's value; pipes deeper than binding-col fold as value arms, pipes at binding/body columns stay arm syntax) and the ABSORB (a multi-line bracket ENDS the let's reader extent, dropping continuation lines as siblings — previously mangled by the P2 bodiless-merge into "apply y to 5"; bodiless-gated, `:=`-chain-safe, eq?-preserving).
- **A/B before attribution, this time**: all five broken multi-line shapes re-probed at pre-P3 — ALL pre-existing, nothing regressed, and P3 had already fixed the multi-line-body case. (The P2 lesson, applied.)
- **Boundary stated**: unannotated match VALUES still die on the QTT infer-position debt (typing-side — the layout datum is now correct; annotate and it works, pinned both ways). Acceptance §E2 carries the annotated form with the boundary named.
- **Gate**: suite **9520/476/0** · acceptance 6/6, §E live (34 markers) · the flip-with-the-feature series CLOSED (P1 exemplars now on the two PERMANENT errors). NEXT = **X.close** (prologos-syntax.md § let — discharges issue #21 — + DEFERRED sweep + close notes).

### LET X.close — track COMPLETE; § let documented (issue #21's obligation discharged) (`docs`)  (2026-07-31)

- **prologos-syntax.md gains § let**: all spellings (aligned STRICT blocks · sibling chains · brackets · nested · fused · `:=`-optional · multi-line values), the sequential-scoping guarantee, the top-level rule, the sexp story, and the ONE user-facing boundary (unannotated match values → annotate; the QTT debt named). This was issue #21's in-repo obligation — **closing the GitHub issue itself is the owner's call** (outward-facing).
- **DEFERRED swept**: 3 residuals filed, each verified at HEAD first — fn-bracket-body lets (per-command since P1 but misleading message; fn-side layout work), the 2-line forgot-body mediocre error (below the ≥2 activation; collides with nested-form byte-transparency), and the match-value boundary pointer (QTT option-(c) debt, let is just where users meet it).
- **LET added to MASTER_ROADMAP** (it was invisible) — status row carries the arc incl. the withdrawn filing.
- **PIR-lite in the design doc, flagged not defaulted**: one-session five-phase track with per-phase VAGs/close notes already in-doc; the owner can upgrade to the full 16-question PIR. The lite version records: delivered-vs-asked, the three honest went-wrongs (the withdrawn i70 filing · the properties drop · the predicate loop), the ×4 flip-with-the-feature pattern, and what outlives the track (`$let-error` seat · hardened fused primitives · the eq?-preserving walk lesson).
- Suite stands **9520/476/0** · acceptance 6/6 (34 markers) · tree clean.

### D4.P4 OPENED — audit + options panel; Q_U5/Q_U6 RULED: ONE reified selector carrier, wholesale path migration (docs)  (2026-07-31)

- **Why**: P4 is a fresh design surface — full mini-audit (`wf_8458c23b-312`, 5 facets + critic @ `02dd27d7`, **14th consecutive premise refuted**) + the adversarial options panel (`wf_82e56156-b28`, 3 clusters), per Stage-4. Every load-bearing claim re-verified on the main thread.
- **⭐ The frame reversed TWICE, and the second reversal was the owner's.** The panel's proposers leaned "charter the reification" on the premise B needs row polymorphism; the panel's own critic refuted the CONCLUSION by probe (monomorphic source-annotated projection types cleanly at HEAD). Then the owner refuted the PREMISE'S FRAMING: the records types were BUILT from row polymorphism + codata — verified: `ρ` row-meta is STAGED at F-row in the carrier comment itself (syntax.rkt:672), the records design names open rows as THE row-polymorphic case, and every P4 carrier's element type is knowable. **P4's surface needs zero row-polymorphism work**; bound-selector reuse rides F-row's existing gate.
- **⭐ The audit's decisive find**: the FIRST-CLASS PATHS track (2026-03-20, Phases 0–7c SHIPPED) is the unnamed predecessor — its problem statement is P4's verbatim; its Phase 7b IS the retired `expr-broadcast-get`; its `#p(…)`/`get-in`/**`update-in` (the write direction!)** are LIVE at HEAD; its Phase 8 charter sat idle four months. The panel's E-option result is recorded for the SRE notes: **a selector typed as its own application ERASES THE SPINE** — rules out the whole selector-as-function family, the positive argument for the data carrier.
- **Rulings (owner)**: **Q_U5** — ONE reified selector carrier (`expr-path`+`expr-select` unify; `#p`, braces, and path position = spellings of one representation; precision-now via known source rows, reuse-across-shapes → F-row). Rejected-with-reason: brace-only (spine loss), charter (a SECOND charter over FCP Phase 8's residue — belt-and-suspenders), Datum (order ≠ selection order), function (spine erasure). **Q_U6** — WHOLESALE path migration (probe: all FOUR fold callers head-agnostic, arity-collapse only — the 4th being P3a's own pipe pre-fold, 6th enumeration under-count; the real cost = the behavior-preservation checklist, since Q_T2's `.field`-vs-block asymmetry is LIVE: one carrier, TWO typing postures by sort), sequenced P4a (totality dispatcher + strategy repairs) → P4b (carrier + migration) → P4c+ (ω).
- **⚠ The LET merge (`5e16ead4`) landed MID-WALK** — +738 lines in macros/parse-reader/parser/typing-core; `02dd27d7` stays an ancestor, no select-surface collision (LET REUSES the P1a marker seat), but audit coordinates need re-pinning, and **the fused-binder hazard population GREW** (`feb79740` ships fused `var:Type` let binders — the exact class Q_U8's `:` gate must not capture; census re-run owed).
- **Pending**: **Q_U7** (the ω-step representation — 4b is self-contradictory: wording says flat marker, rationale says run-carrier, and fusion is FREE under all four) · **Q_U8** (the `:` gate + corpus A/B). Then the P4c+ partition finalizes.

### Q_U7 + Q_U8 RULED — the (@bcast step) wrapper + the uniform positional gate; P4a–P4e LOCKED; pre-implementation PAUSE (docs `cae212c8`/`a090b46a`)  (2026-07-31)

- **Q_U7** [owner: "(b) stands"]: the ω step is the ONE-STEP WRAPPER `(@bcast step)`. Ruling 4b RESTATED: extent structural (broadcast-of-nothing UNCONSTRUCTIBLE; the §3.2.1 extent pair = two visibly different datums), **L1 fusion a THEOREM the battery pins** (free under any candidate — each ω step consumes one layer and re-wraps one), the layer-count clause retired as descriptive. Rejected: flat marker (extent-by-adjacency ⇒ representable malformed states + a backward scan), run-carrier (the parser would MERGE adjacent wrappers — the normalization 4b's own rationale forbids), per-step grade field (taxes the landed vocabulary).
- **Q_U8** [owner: "uniform mint"]: `($bcast-step payload)` minted at GROUPING for byte-adjacent keyword/colon-annotation tokens after a non-empty result (positional, both groupers, a REAL access sentinel — the $postfix-index pattern, nine-site surface); the PARSER dispatches by position (expression → ω step; binder → the annotation consumers UNWRAP). Zero tokenizer changes; corpus A/B mandatory with a NAMED diff set. **The LET merge improved the safety net**: the fused-annotation hazard went 0 → 3+12+17 live suite-watched sites.
- **The partition LOCKED**: P4a (totality dispatcher + strategy repairs) → P4b (the ONE carrier + wholesale migration + behavior checklist) → P4c (the gate + wrapper + PVec broadcast + the `.*name` migration/residue disposal + A/B) → P4d (map-generic + the 2b split + row-meet + the discriminating fixture) → P4e (flatten/splat/disclose + DEFERRED 5 re-census + keyword-projection disposition).
- **PAUSE (owner hold-point) — pre-implementation items named in §5.P4**: ruling-shaped: **Q_U9** List broadcast disposition (changes `quests:t`'s corpus fate) · the update-in ω FENCE (grade-1 only; broadcast writes are §7.7, not v1) · the whole-node abort ratification (a miss inside a broadcast aborts the node — no partial results). Slice-round: the carrier shape (R6 hazard; slice ends single-carrier) · the 'path-sort semantic table (D19/miss-hints/nil-safe/ns-guard/reconstitute) · splat-vs-duplicate-check typing extension · the :diags* splitter grammar · A/B re-derivation at implementation HEAD.

### CORRECTION — the track battery is 204 test-cases, not 206 (caught by the handoff verification)  (2026-07-31)

- The P3c close notes, its commit message, and this file's P3c entry all said **206**. That figure was ARITHMETIC (subtracting the acceptance file's count from a 2-file targeted run), never a measurement. `grep -c '(test-case'` = **204**, the runner reports **204**, and `timings.jsonl` recorded 204/pass at `5e16ead4`. Corrected in D4 §5.P3c + this file; the commit message is immutable and stands as-is with this note as its correction. **Lesson (the layer-error family): a count DERIVED from two other counts is not a measurement — measure the thing you are going to cite.** A fresh session anchoring on 206 would have read −2 as lost tests and gone regression-hunting.

### Session closes to the P4 HANDOFF — design/dailies/memory refreshed; the pause is the fresh session's opening  (2026-07-31)

- **Why**: owner ruling — the P4 implementation opens in a fresh session (the P3 pattern). D4 is current through Q_U8 + the locked partition + the pause items (`cae212c8` · `5b67046a` · `a090b46a` · `f1537a7b`); the memory tail carries the P4 paragraph; the handoff doc is `docs/tracking/handoffs/2026-07-31_CIU_T6_PATH_SELECTION_P4_handoff.md` (verification-audited before landing, per the P3 precedent).
- **State re-derived for the handoff**: suite **9599/476/0** came FREE from the merge's own gate @ `5e16ead4` (LET added a file + ~100 tests); docs-only commits since; unpushed = 4 (origin at the merge); 41 owner-WIP entries intact.
- **The fresh session opens on the PAUSE**: Q_U9 (List disposition) + the update-in ω fence + the whole-node abort ratification, then P4a failing-test-first.

### The PAUSE CLEARED — Q_U9 refuses `List`; and the door it names was chartered four months ago (docs)  (2026-07-31)

- **Why**: the fresh session opened on the owner's hold-point, per the handoff. Re-grounding first: HEAD `711a9bde` re-derived, main checkout, `git diff 5e16ead4..HEAD -- '*.rkt'` EMPTY (so 9599/476/0 is legitimately inherited), acceptance re-measured **52/52**, battery re-counted **204** (the handoff's correction confirmed — the 205th `test-case` hit is a comment at :1156). Coordinates re-pinned: `syntax.rkt`/`parse-reader.rkt` unmoved, `access-sentinel?` drifted **+197** (:5493→:5690), `parser.rkt` `:name` promise :793→:800, the union arm :2232→~:2236.
- **Q_U9 RULED — `:` refuses over `List`** [owner: lean against, argued both ways]. The decisive grounding was a carrier census, not an appeal to taste: **`List` is the only candidate with no native carrier** (`Map`→`expr-champ`, `PVec`→`expr-rrb`, `Set`→`expr-hset`, tuple→`expr-Record`; `List` is `data List {A} | nil | cons` in the STDLIB, no struct in `syntax.rkt`), so broadcasting it would make the compiler-native `select-reduce` pattern-match constructor applications of a library type — a dependency direction no other carrier has. Probe-verified remedy: `[pvec-from-list quests]` → `[PVec {:g String :r Int :t String}]`, **the row survives**, and `qv[0].t` works, 0 errors.
- **⭐ The counter-argument was the strongest thing in the round, and it MOVED the work rather than dying**: `solve` returns List and solve rows are exactly what selection exists for. Owner's reframe — fix it UPSTREAM. Measured the seam: **two lines** (`solve-row-type`'s `'list` arm, which is ALREADY a `wrapper` parameter, typing-core.rkt:4338; and `racket-list->prologos-list` reduction.rkt:905). Consumer census: **2** live `.prologos` sites (both in our own acceptance file, the very `map [fn [q] q.r] quests` lines `quests:t` would replace), ~31 mechanical test type-string pins, and `nil`→`@[]`. Spun out as a **task chip** for its own worktree session.
- **A second bug found while grounding that chip**: implicit solve fires on a `def` RHS but **not** on a `let` binding RHS — `let x := (goal …)` gives a bare `ERROR: Unbound variable` (both nested and aligned), while `let x := solve (goal …)` works. The LET track landed a new binding form today (`5e16ead4`) and the POL.9/POL.10 command-position scope never grew to cover it. Folded into the chip.
- **Both fences RATIFIED**: `update-in` takes grade-1 selectors only (**must land in P4b, with the carrier** — Q_U5 absorbs First-Class Paths' working write direction, so a later fence leaves the domain silently widened between slices); whole-node abort on a runtime miss inside a broadcast (**pinned at P4a** by the LOWER-vs-WALK fixture).
- **⚠ THIRD prior-art miss of the arc — mine, and I stated it to the owner as fact.** I called trait-dispatched broadcast "a real feature, and a much larger one than P4d." Wrong in both halves: `Functor` is **already declared with laws** (`core/collection-traits.prologos:97`, one of EIGHT HK traits), and the dispatch work is **already chartered as CIU Track 4** — whose scope text says, verbatim, "Broadcast via Seq […] any collection, not just cons/nil lists" and "PVec broadcast → PVec; List broadcast → List". Written 2026-03-21, four months before Q_U9 asked the question. Seed note written instead of a track proposal: `2026-07-31_CIU_BROADCAST_TRAIT_DISPATCH_NOTE.md`, linked from the master's T1 + T4 rows.
- **Two staleness findings the seed records**: T4's stated MECHANISM hangs off `surf-broadcast-get`, which **P1a retired** (Q_L3) — it must re-point at `:` / `(@bcast step)` / the shared walk. And T1's own premise ("*extend* `Functor` instances beyond List") is false: censused tree-wide, the eight HK traits have **ZERO live instances**, List included (`map` is monomorphic, `data/list.prologos:46`; the only `impl` mentions are commented aspirations in the Track 8 acceptance file). The first instance is net-new.
- **A 7th enumeration under-count, folded into P4b**: `rewrite-nil-dot-access` (macros.rkt:5906) has zero production callers but a **6-case battery at `tests/test-nil-type.rkt:126-155` asserting the fold's exact output datum** — 4 of 6 go RED under wholesale `$select` minting. Same class as P4c's `broadcast-access` token pin, but it lands in P4b and no enumeration named it.
- **Next**: P4a, failing-test-first.

### D4.P4a LANDED — step-kind totality; the design said 4 sites, I found 8, the verify found 13  (2026-07-31)

- **Why**: the Exhaustive Walkers rule applied BEFORE the new step kind lands, for once, rather than after a silent miss. Q_U7's `(@bcast step)` arrives at P4c and would have met every one of these.
- **The count went 4 → 8 → 13, and the METHOD was the defect each time.** The design's audit said four silent catch-alls. My census found eight. The adversarial verify found **thirteen, across five files**. My method was syntax-directed — grep the exported helper names, look for `cond` arms, in three files — which structurally cannot see dispatchers that OPEN-CODE the shape tests (`pretty-print.rkt`'s `step->string`: no exported identifier to grep) or that are shaped as `and`/`if` rather than `cond` (the leaf classifiers; `parser.rkt`, a fourth file I never opened). Owner ruling: extend the routing, deliver the totality.
- **⭐ The two worst sites were UPSTREAM of the guards, so they DEFEAT them.** `select-branch-collapse` / `select-branch-keyless?` classify a branch's LAST step and run *before* the guard in all three branch walks. A silent `#f` there mis-SORTS the branch (keyed vs keyless) with no raise anywhere downstream, and `select-branch-top-keys`' `key` arm returns without touching `rest` — so the wrong key set flows straight into the parser's L4 and duplicate-output-key checks. Traced: `[server (@bcast (@key name dissolve))]` → `'(server)` where `'(#f)` is right.
- **One site deliberately does NOT raise.** `pp-expr` is on the error-message path, so a raise in the printer converts a real diagnostic into an internal crash — and `typing-errors.rkt`'s catch-all could swallow it, achieving *less* than a visible marker. It renders `«unrendered-step-kind:…»` via `select-step-kind/display`, a non-raising variant defined by DELEGATION (never a second copy of the kind list — that is the drift this phase exists to kill). Written scope decision, not an omission.
- **Self-review caught a defect before the verify did** (the twin-drift class): the `memq` guards must list EXACTLY what the old `else` caught, and the four guards do NOT have the same right answer. Sites 5 and 8 — the "below a kept head" twins — sit under arms taking TERMINAL `sub` and `ord-step`, so their else also caught `ord-branch`; I omitted it at both, turning a delegation into a raise. Validated the fix by re-introducing the defect: fails with `select-below-value: no arm for select step kind 'ord-branch`.
- **⭐ THE VERIFY EARNED ITS KEEP — it refuted my own reasoning, twice.** (1) I cited `reduce_steps` flatness as proving "pure per-step cost reduction". `perf-inc-reduce!` fires at reduction.rkt:2054, **before** the `whnf-trivial?` branch at :2062 — so `reduce_steps` *cannot* move for that leg. A tautology dressed as a control. (2) n=3 sequential wall is not an A/B by our own rules; measured spread on an unchanged tree is ~20%, so my 6 ms delta was ~1.2× the noise band. What actually carries the claim is a control I never thought of: `expr-tchamp`/`expr-trrb`/`expr-thset` are structurally identical transients that are NOT whnf-trivial and have no match arm — interleaved, **1822 ns/call, 9.5×**. Mechanism confirmed, my evidence replaced. And the honest magnitude is ≈**−0.1% of wall**, not "−25%": `reduce_ms` is 0.3% of end-to-end.
- **Two FALSE claims in the section I wrote**, both caught: "commit the corpus A/B harness, still uncommitted" — it landed at `3005170b` and **§5.P2's own close notes record it**, four hundred lines above where I asserted it was open; and the `[_ e]` catch-all cited at :3936 when it is :3957. Plus an off-by-one I repeated ten times: the classifier has FIVE kinds, so `(@bcast …)` is the **sixth**, not the fifth — in the phase whose subject is counting correctly.
- **The re-whnf hoist moved NOTHING** and lands as a CONTRACT fix, not a perf win: the header comment has claimed since P3a that the subject is "evaluated ONCE … reused across every branch" while `(whnf subj-expr)` sat INSIDE the `append-map` lambda. On a `def`-bound subject the repeat calls are cache hits. Say so rather than bank an unearned number.
- **Fuel is a real semantic delta the safety proof did not cover** (found independently by two skeptics): the fast path returns before the decrement, so containers now consume zero reduction fuel. One-way, cannot produce a wrong answer, matches the ~70 pre-existing trivial kinds — but "identical semantics" was too strong and is now qualified in place.
- **Test delta +20** (204 → 224). The pins were also weaker than claimed: `check-exn exn:fail?` demonstrably passes on an ARITY error or a malformed fixture, so a change to `make-record`/`champ-insert` could have turned all eight green with the arms fully reverted — narrowed to a message-matching predicate and the fixtures hoisted out of the guarded lambdas. `select-step-kind-unhandled` had **zero coverage** (all 8 pins raise from the CLASSIFIER, never a consumer's else); it has a direct pin now.
- **Known limit, stated not papered over**: the whole-node fixtures discriminate at BRANCH granularity inside one `select-reduce` call. If P4c implements ω by mapping a sub-walk over N elements, each gets its own `let/ec` and burial returns with all three still green. P4c owes the element-level fixture.
- **Next**: P4b — the ONE selector carrier + wholesale path migration, ending single-carrier, with the `update-in` ω fence landing in the SAME slice.

### D4.P4b opens — the audit found a BLOCKING objection to a ruling; b-i's first slice retires a live silent-wrong-answer  (2026-07-31)

- **The P4b mini-audit** (`wf_1cb9d606-89c`, 5 HEAD-pinned facets + completeness critic @ `2cef148b`, 6 agents / 1.17M tokens) — the grounding-audit workflow as the default Stage-4 opener. ⚠ First launch failed instantly: the skill handed its args as a PROSE STRING but the script does `JSON.parse(args)` and wants a structured object with labelled facets. Read the script's arg contract instead of guessing; zero agents had run.
- **⭐ FOUR of seven design claims REFUTED, and the critic found a BLOCKING objection no facet connected**: `.field` on a `(Map K V)` subject WORKS while `{field}` on the SAME subject REFUSES STATICALLY. So wholesale `$select` minting would **silently delete a working surface** — one DOCUMENTED in our own ambient rules (`prologos-syntax.md:117`), TAUGHT as the headline ergonomic in `map-tutorial-demo.prologos:219-225`, and PINNED E2E at `test-dot-access-02.rkt:112-119` — a pin BOTH fold-census facets missed because it asserts a VALUE not a datum, so it fails by TYPE ERROR and no "update the datum assertions" plan catches it.
- **Q_U10 RULED** [owner]: wholesale STANDS, the `'path` sort gains a **Map posture**. C2 ("zero new typing work") formally withdrawn — sort dispatch has to exist anyway for Q_T2's dyn-row asymmetry, so Map is a THIRD posture in a table we were already building. Design-round item 5's semantic table is now **two-dimensional: (subject kind × sort)**.
- **The real carrier-shape decision was one none of the three candidate shapes named**: `expr-path` branches hold `expr-keyword`/`expr-symbol` STRUCTS, `expr-select` branches hold symbols + `@key`/`@sub`/`@ord` S-EXPRESSIONS. Framed as arity; actually an encoding convergence plus a rewrite of every step consumer. **P4b SPLIT** [owner] into b-i (encoding + carrier, ends single-carrier) → b-ii (the wholesale fold migration + the 2-D table) → b-iii (the ω-fence repair at its 6 sites).
- **⭐ b-i's opening probe refuted the AUDIT's own premise — 15th consecutive.** F3 confirmed "`#p(a.b.c)` round-trips" and "`get-in` works": TRUE, for the subset anyone probed. Running the FULL vocabulary end-to-end: `#p(a.*)`, `#p(a.**)` and `#p(a.{b c})` all define as `Path` and then produce an **`<error>` VALUE at ZERO ERRORS** — the P2.b fabrication class, live, unconstrainable because the vacuous ground `Path` type discards the branches. **A facet confirming a claim confirmed it AS PROBED, not as stated.**
- **Q_U11 RULED** [owner]: retire them with a guided error, rather than carry a silent-wrong-answer into the new carrier (belt-and-suspenders, blocking) or fix them into new semantics inside a behaviour-preserving slice. Refusal at ELABORATION via the `parse-error` seat. 5 pins RED → GREEN; the live subset (`#p(a.a1)` → `get-in` → the value) verified untouched.
- **TWO further findings, both from READING THE DATUM after two wrong guesses** (the diagnostic protocol earning its place — a branch-COUNT guard and a keyword-SHAPE guard both missed): (1) **P1b-ii silently broke `expand-brace-branches`** — it keys on `$brace-params`, P1b-ii re-minted `.{` to `$dot-brace`, the test was never re-pointed, so brace expansion has been DEAD since, invisible because its only consumer was already fabricating `<error>` values. One dead thing masking another. (2) **A brace spelling never arrives as a group**: the WS reader collapses `#p(a.{a1 a2})` into ONE symbol `|:a.{a1 a2}|`, so segments come out as ordinary keywords `#:a` and `#:{a1 a2}` — the malformed thing is the segment's CONTENT, so the guard is a shattered-NAME test. The old `[else (expr-keyword seg)]` coerced that (and rename pairs) into a keyword wholesale — the same silent-catch-all class P4a spent its phase eliminating, one arm away and outside the census because it is not a step dispatch.
- Suite **9624/476/0** (full, complete). Battery **229** (measured, +5). Suite delta +10 = my +5 plus `test-properties.rkt` 8→13, the property-test nondeterminism swinging back to its standalone value — which also confirms the earlier P4a diagnosis rather than leaving it a hand-wave. Acceptance 52/52 · 89/89 · 6/6 · first-class-paths clean.
- **Next**: b-i's carrier repurpose proper. The encoding question is now much smaller — the surviving `#p` vocabulary is a keyword chain, a strict subset of the step vocabulary.

### D4.P4b-i slice 2 — the encoding CONVERGES; one representation, not yet one carrier  (2026-07-31)

- **`expr-path`'s branches now hold bare SYMBOLS** — the same step vocabulary `expr-select` uses. `#p(a.b)` and `x{a.b}` carry ONE representation of the selector. Six consumer sites re-pointed (every one had used a segment DIRECTLY as an `expr-map-get` key: get-in/update-in whnf arms + their nf twins + two elaborator static inlines), plus the pretty-printer — whose `[else "?"]` was another silent catch-all of the P4a family.
- **The FFI became the marshalling boundary**, which is where marshalling belongs. `path-head` marshals a symbol out to a Prologos keyword value; `path-from-segments` marshals in and **refuses** a non-keyword rather than coercing — coercing an unrecognized segment into a key is exactly what made `#p(a.*)` fabricate `<error>` values.
- **`whnf-trivial?` gained the selector** — a DECISION, not an inheritance. `whnf-impl/match` has NO `expr-path` arm at any indent (it already fell to `[_ e]`), `nf` is identity, so a literal with no head rule meets the predicate's own criterion. `expr-select`, the APPLICATION, is reducible and stays out. Same type-former-without-its-value-carrier gap P4a closed for champ/rrb/hset — one line away, missed by that census too.
- **Method that mattered**: for a behaviour-preserving refactor the pins go in BEFORE the change and must stay GREEN — the pins ARE the claim. 6 FFI/E2E characterization pins + the whnf identity pin; all green through the change. +7 (229 → 236).
- **A 4th layer of dead-masking-dead, found while writing those pins**: `p::segments` **whole-file ABORTS** — it builds a Prologos cons-chain while the marshaller wants a Racket list, so `Path -> [List Keyword]` never marshalled, taking `from-segments` and `path-append` with it. 5 of 6 primitives work. PRE-EXISTING, filed, deliberately unpinned (an abort would take the test file down).
- **⚠ Said plainly: b-i does NOT yet end single-carrier.** The slot nesting remains, and it is held back on purpose — it makes `expr-select`'s branches hold an EXPR, which activates the audit's C3 finding (`uses-bvar0?` recurses into subject only) and needs `select-map-exprs` to map into the selector. Walker-shaped, owed its own failing-test-first pass. "One encoding" ≠ "one carrier"; letting the headline outrun the ship is the Validated≠Deployed shape.
- Suite **9631/476/0** (full, complete). Battery **236** measured. Delta accounting exact this time — only the track file moved, 229→236, suite +7, no nondeterminism.
- **Process note**: the targeted runner's 30 s default now aborts this file (FFI pins add process-file cycles); `raco test` said 235 passing while the runner said ABORTED. Use `--timeout 180` — an abort there is not a failure.
- **Next**: the slot nesting + the walker work, then b-ii (the wholesale fold migration + the 2-D semantic table).

### Rel/CIU seam spin-out — the SOLVE CARRIER: `solve`/`explain` return **PVec**; the `let` implicit-solve gap closed  (2026-07-31, `7bc8631d` · `b2c4366a` · `b5b641b9` · `353c465e`, worktree `focused-morse-3454bf`)

- **Why**: CIU T6 P4's Q_U9 ruled `:` REFUSES over `List` and named the counter — `solve` returns `List`, and typed rows exist so relational output composes with records. The resolution was routed UPSTREAM as its own mini-track: change what `solve` PRODUCES rather than widen selection's carrier semantics. Q_U9's refusal now costs solve rows **nothing** — `quests:t` will work at P4d with no `pvec-from-list` hop.
- **Shipped**: `solve`/`solve-with`/`explain`/`explain-with` → `[PVec row]`. `solve-one` untouched (bare champ). Narrowing stays `List` (different feature, typed `expr-hole`) — pinned by test, not prose. Plus the `let x := (goal …)` implicit-solve gap and the INVERSE of POL.9's guiding diagnostic.
- **Surprise 1 — the "two-line seam" was SEVEN.** The opening grounding named `solve-row-type` + `answers->prologos-expr`. The census found three DISPLAY walkers it missed (`display-row-type-parts`, `display-result-rows`, `pp-solve-echo-ordered`), all of which fail **silently** — B3.2 echo refinement and the POL.3 declaration-order echo just quietly stop firing. Adversarial verify then found a *seventh*: `pnet-serialize`.
- **Surprise 2 — a LIVE defect the census could not have found, because it asked the wrong question.** It asked "what READS a solve result"; it should also have asked "what PERSISTS one". POL.10 binds whnf-reduced values, so the carrier reaches module caches. `rrb-root`'s `tail` is a **raw Racket vector** and `deep-s->v` has no `vector?` arm — so champ rows leaked through `[else v]` VERBATIM, writing `equal-hash-code` values to disk. Process-stable only; exactly what the `champ-sentinel` arm exists to prevent. Fixed reconstructively + `PNET_VERSION` 8→9. **A green suite would never have shown this** — I found it by probing the serializer directly, because the champ-sentinel comment said POL.10 had already forced this once.
- **Surprise 3 — my own census claim was WRONG, and the acceptance run corrected me.** I read `spec map … [List A] -> List B` and wrote in the doc that the two live `.prologos` consumers would need rewriting to `pvec-map`. That spec is ONE INSTANCE under container-generic dispatch: `map`/`filter`/`length`/`first` all carry PVec through. **Zero `.prologos` source changes were needed.** *A `spec` line is evidence about an instance, not about dispatch.* Corrected in-place in the doc rather than quietly dropped.
- **Surprise 4 — a DOUBLE SOLVE, caught by the acceptance file's own pin.** P2's first position rule wrapped every non-body paren child of a `let`, which wrapped the ARGUMENT of the explicit `let ls := solve (goal …)` → `solve (solve …)`, echoing as a stuck `(solve @[…]) : _`. Fixed by mirroring POL.9b's own rule (only a SINGLE-element value after `:=` qualifies). The acceptance file pinned the explicit spelling beside the implicit one *because* that is where a scope rule can over-reach.
- **Surprise 5 — the guided diagnostic mis-fired ACROSS NAMESPACES.** First cut consulted the relation STORE and matched any key ending `::m`; the store is not namespace-scoped, so a relation `m` in one namespace mis-diagnosed four unrelated Batch-C tests whose `def m := {…}` has nothing to do with relations. Fixed by testing the GLOBAL ENV value for `expr-defr?` — the same test `raise-unknown-relation-error` uses — which makes namespace- AND rebinding-correctness **structural rather than checked**.
- **Surprise 6 — only the FULL SUITE caught the last one.** P2's reader walk descended into `racket{…}`, where the foreign body is still ordinary syntax at that stage, and mangled an embedded **Racket** `let`. 10 targeted files + all 8 acceptance files were green. `test-path-selection`'s multi-line `racket{…}` pin carries a comment saying it exists because "a single-line pin misses this shape" — it was right about a defect that did not exist yet. Guarded at the RECURSION so `classify-let-block` (same latent exposure) and any future let-shaped rewrite are covered by construction.
- **Realization note worth keeping**: goal-ness rides a SYNTAX PROPERTY, but `let` desugars in a preparse macro that receives fully STRIPPED datums (verified by instrumenting `expand-let`'s entry). The property's information content there is ONE BIT — "written in parens" — so it is preserved in the DATUM as a `$goal-rhs` sentinel. **Reader owns position+parens; parser owns the keyword table.** Neither needs the other's table, so nothing can drift (the F1b.7g concern).
- **Gate**: full suite **9548 / 477 / 0** · 8 acceptance files **296/296** · +26 tests in `tests/test-solve-carrier.rkt` and +5 PVec twins beside the List originals in `test-rel-t1-typed-rows` (a differential pair, so a future edit to one display arm cannot diverge from the other). Wall time NOT reported — I compiled concurrently, so it is not a valid measurement.
- **Spun out, not folded in**: `expr-hset` has the same `.pnet` hash-persistence gap (pre-existing, orthogonal).
- **Next**: branch `focused-morse-3454bf` is **NOT pushed** and not merged. CIU T6 P4a can now open with Q_U9's counter fully discharged.

### Top-level `let` (owner ruling) + the `.pnet` container-sentinel sweep  (2026-07-31, `53251a80` · `aea73811` · `c99bfa89`, worktree `focused-morse-3454bf`)

- **Ruling (owner)**: "The scope of a top-level `let` ceases at the beginning of the next toplevel form. It's really only intended for interactive use. A program with a toplevel let would be a bit odd, and wouldn't 'do' anything, but it shouldn't be an error or invalid, either, nor even a warning per se. … The real use-case is interactively building up some computation, then being able to wrap it in a `defn` afterwards as a form of iterative design."
- **MY OPENING FRAMING WAS WRONG, twice, and the grounding refuted both.** (a) "Top-level `let` is refused" — no: a top-level let WITH a body already worked in all four spellings; only the BODYLESS one was refused. (b) I told the owner "no merging scheme can help the REPL, because it reads one submission at a time" — false, because `read-repl-input-ws` reads a **blank-line-terminated MULTI-LINE** submission, so a sibling chain typed in one go IS the interactive workflow. I had the fact and drew the wrong conclusion from it.
- **Three defects, none of which was the thing I set out to fix.** D1: the refusal guard never tested top-level-ness (it tested "one token after the first `:=`"), so it fired on a NESTED bodyless let, naming a location the form isn't in and prescribing `def`, which is illegal there. D2: `let x:Int 5` and `let y <Int> 5` were **silent wrong answers** — printed `5 : Int`, zero errors, annotation silently discarded. D3: a SECOND whole-file abort (`normalize-let-binding-group` raising outside `expand-let`'s handler via `merge-sibling-lets`).
- **Surprise — a comment that asserted its own verification.** The marker-channel note said "all 13 are beneath expand-let's dynamic extent (verified — merge-sibling-lets and its helpers raise nowhere)". **Both halves false**: 20 sites, two of them in that very helper. D3 shipped behind the word "(verified)". Worth promoting: a parenthetical `(verified)` in a comment is an ASSERTION, not evidence.
- **Why chains never merged**: `merge-sibling-lets` already handled the shape — its own comment shows it — but its sole call is from `preparse-expand-subforms`, i.e. over the CHILDREN of an enclosing form. Top-level forms are children of nothing. Nested chains worked; top-level ones couldn't.
- **Realization**: `let-bodyless?` (+2 arms) becomes THE bodyless test; a `$let-noop-body` placeholder fills the body slot so each branch slices unchanged; the funnel resolves it to the last bound name. **Branches 3 and 3f built `((fn …) …)` BY HAND**, bypassing the funnel — which is why the placeholder leaked to users as an unbound `$let-noop-body`, and why the design's "one funnel" claim wasn't quite true.
- **The merge deliberately does NOT absorb a following form as the body** (the ruling makes the next top-level form end the scope, and it may be a `def`) and is **eq?-preserving when nothing merges** (else POL.9b's implicit solve breaks — the SolveCarrier P2 sentinel depends on those syntax properties). Both test-pinned.
- **Then the `expr-hset` chip — and the audit found FOUR, not one.** `expr-hset` plus the three transient builders had neither a sentinel nor a reg entry. `expr-hset`'s round-trip returned a **raw vector that PRINTS as `#(struct:expr-hset …)`** — pipeline.md's unregistered-node failure symptom for symptom. Transients are reachable too (`def ts := (transient s)` → `ts : [TSet Int]`). Fixed all four; `PNET_VERSION` 9→10.
- **A detail worth keeping**: the new test fixture uses long-hashing STRINGS, not keywords. A small keyword's `equal-hash-code` is short enough that a persisted-hash leak slips past a digit scan — which is exactly how my first probe nearly cleared `expr-hset`.
- **Gate**: full suite **9646 / 478 / 0** · 9 acceptance files **317/317** · +6 LET pins, +9 sentinel pins, 4 superseded LET pins updated in place with what replaced them.
- **Flagged, not decided**: a bodyless `let` ECHOES ITS BOUND VALUE (`let x := 5` ⇒ `5 : Int`) rather than being a silent Unit no-op. A choice, not a consequence — the one thing here worth revisiting if it reads wrong in practice.
||||||| cab30b9a

### D4.P4b-i COMPLETE — the slot nesting ends single-carrier; the merge lands solve→PVec and corrects two of my claims  (2026-07-31)

- **Re-grounded first**: the SolveCarrier branch MERGED (HEAD `57c86606`, 23 unpushed, owner WIP intact at 41). My b-i work survived; verified before building on it. `solve` now returns `[PVec {…}]` — probed.
- **⭐ Q_U9's CONSEQUENCE inverts (the ruling does not).** `quests` is a PVec now, so `quests:t` / `quests:{t r}` are no longer refusal cases deferred to P4d — they are plain PVec broadcasts that uncomment at **P4c**. P4d loses two corpus lines, P4c gains them. Exactly what the upstream fix was for.
- **The merge corrected TWO of my claims** (its own census, refuting mine from the other direction): the seam was **SEVEN** sites not two (three DISPLAY walkers fail silently + a live `.pnet` hash-persistence defect — `rrb-root`'s tail is a raw Racket vector and `deep-s->v` had no `vector?` arm), and the two `.prologos` sites needed **NO** rewrite.
- **⭐ Chasing that second correction found my SEED NOTE WRONG, and it arms a track.** I claimed the eight HK traits have ZERO instances and `map` is monomorphic. Both false: instances exist as **dictionary VALUES** (`List--Seqable--dict` etc.) not `impl` declarations — Seqable 4 · Buildable 4 · Indexed 2 · Keyed 1 · Setlike 1 — and generic `map` ALREADY routes through Seqable+Buildable (`core/collections.prologos:33`, probed container-preserving over PVec and List). Corrected in place (`2df47490`) rather than quietly edited. It makes CIU Track 4 MORE tractable: the dicts it needs exist and `map` is the worked precedent; only Functor/Foldable/Reducible are empty.
- **THIRD "doesn't exist / is new" miss of the session**, all mine, all the same shape: a grep for ONE spelling is not a census. (1) trait-dispatched broadcast was already chartered as CIU Track 4; (2) the instance census; (3) `map`'s monomorphism. ⚠ `MEMORY.md`'s prior-art reminder says codify at 3+ data points **across sessions**; mine are within ONE session, so whether that counts is the owner's call — **flagged, NOT promoted unilaterally.**
- **Slice 3 — the slot nesting**: `expr-select`'s branches slot holds an `expr-path` (the reified selector), not a raw list. One mapper (now mapping BOTH slots) · 4 positional matches unwrapping · 2 constructors wrapping · 5 test constructions. `qtt.rkt` binds `_`, no change.
- **The audit's C3 finding RESOLVED, not inherited.** It becomes technically live here (the slot holds an expr) but is **INERT at P4**: all six `expr-path` walker arms are `[(expr-path _) e]` (identity), and the monomorphic ruling means selectors hold bare symbols, never exprs. It goes genuinely live at F-row with bound selectors. `uses-bvar0?` corrected anyway — "inert today" is how the silent-walker class starts — and exported for the pin (a bvar in a selector is unconstructible from the surface; same rationale as P4a's `select-reduce` export).
- **On the NAME**: the struct IS the one selector carrier; only its name is legacy. Not an alias, not a dual path — one struct. The rename is ~30 arms of pure churn with no semantic content and would make a behaviour-preserving diff unreviewable; named as a cosmetic follow-up rather than left implicit.
- **b-i COMPLETE**: Q_U11 retirement ✅ · encoding ✅ · single-carrier ✅. Suite **9678/478/0** (full). Battery **240** measured. Delta exact: +4 mine, −5 `test-properties` nondeterminism, against the merge's own 9679/478 gate. Acceptance 52/52 · 89/89 · 6/6.
- **Next**: b-ii — the wholesale fold migration + the 2-D `(subject kind × sort)` semantic table with the Q_U10 Map posture; the 25 RED tests across 7 files come due there.

### D4.P4b-ii OPENED — the audit found "wholesale" itself ambiguous; Q_U12 scopes it to one leg  (2026-07-31, `b5e92d39`)

- **Mini-audit** `wf_f8568392-b44` (5 HEAD-pinned facets + completeness critic @ `2e3fc14e`, 6 agents / **1.22M tokens**), re-pinned from scratch because the SolveCarrier merge invalidated every P4b coordinate.
- **⭐ THE SORT AXIS IS THREE-VALUED, not two** — and that made the ruling's own word ambiguous. `rewrite-dot-access` has THREE live access legs minting THREE different nodes: `$dot-access`→`expr-map-get`, `$nil-dot-access`→`expr-nil-safe-get` (**no block counterpart** — `#{…}` does not exist), `$postfix-index`→`expr-get`. And the two PATH nodes have **divergent subject tables**: map-get has a union leg get lacks; get has PVec **and List** legs map-get lacks — so **List is a subject row the 2-D table's own enumeration omitted**.
- **Q_U12 RULED** [owner]: migrate the **`$dot-access` leg ONLY**, split b-ii like b-i. Not the complection Q_U6 rejected — that was one SORT on two representations; nil-safe access and ordinal indexing are genuinely DIFFERENT SORTS. → **b-ii-1** Map posture + semantic table · **b-ii-2** fold migration · **b-ii-3** `_.field` rescue.
- **ASYMMETRY #3 (NEW)**: selection-typed subjects — `.f` projects through the view, capability-gated; `{f}` refuses via a catch-all whose message **LIES** ("the subject is not a record") and names no remedy. Three asymmetries now (dyn row · Map · selection), which is why the table must be indexed by (subject kind × sort).
- **⭐ `_.field` SECTIONS WORK** — probe-verified: `[_.a {:a 7}]` → `7 : Int`, `map _.a recs` → `@[1 2]`, while `map _{a} recs` errors. Wholesale minting deletes a live, working, **zero-test** surface. ⚠ And **I had the polarity backwards**: I'd implied `_[k]` was a working symmetric case; it is pinned as a guided **REFUSAL**, because the postfix leg has a `_` guard the dot leg deliberately lacks.
- **⭐ THE RED CENSUS WAS STRUCTURALLY WRONG, not undercounted.** "25 across 7 files" is a **datum-shape** census; the failure mode that matters — a VALUE assertion going red by TYPE ERROR — is invisible to it. Missing: **`test-first-class-paths.rkt`** (WS-mode, value-asserting dot-access, in the ONE file testing `#p`/`get-in`/`update-in` — the highest-coupling RED file in the tree, in NO census), `test-selection-paths.rkt` (~8 cases, a **sixth** `$dot-access` consumer), `test-route-soundness-01`, `test-implicit-map-02`; and `test-dot-access-02` has **FIVE** Map value-assertions, not 2. Plus a **FIFTH fold entry point** (the `rewrite-nil-dot-access` alias, 3 direct test callers).
- **The four callers are NOT uniform**: `preparse-expand-subforms` is the dangerous one — its `$select` arm is partially opaque and skips sibling-level passes `map-get` gets, so a naive mint makes every `.field` lose infix/implicit-map/sibling-let processing.
- **Adjacent, verified live**: the `ns` silent-segment-drop guard has **THREE unclosed new instances** — `ns foo{bar}` and `ns foo#.bar` accept at 0 errors and drop the segment, while `ns foo.bar` correctly aborts. It enumerates **2 of 8** sentinels. The `b0db8f3e` class again.
- **Doc-truth**: FOUR places in D4 disagreed on which phase owns `quests:t` — **resolved: P4c PVec broadcasts**, via solve→PVec. Undrawn consequence now drawn: **P4d's List refusal has NO corpus instance** and needs a purpose-built fixture.
- **Next**: b-ii-1, typing-side only.

### D4.P4b-ii-1 LANDED — the (subject × sort) table; the verify found a LIVE capability bypass and three vacuous pins of mine  (2026-08-01)

- **Re-grounded first**: HEAD `07b79b52` re-derived, main checkout, `git diff 2e3fc14e..HEAD -- '*.rkt'` EMPTY (so 9678/478/0 covers current code), battery re-**measured** 240, acceptance 52/52. Coordinates: `select-row-of` :659 and its Map arm :663 unmoved by the SolveCarrier merge.
- **The three asymmetries re-probed live before designing anything** — Map (`m.a` works / `m{a}` refuses), dyn row (`d1.host` → `?meta` / `d1{host}` loud), selection (`u.name` projects / `u{name}` **lied**: "the subject is not a record"). Plus a 4th observation: `u.age` out-of-view is a bare "Could not infer type" with no explanation at all.
- **Two corrections to what b-ii-1 inherited, both from reading rather than trusting**: the design's "**ALL THREE** per-level dispatches need the Map arm" is structurally wrong — `select-row-of` is ONE shared dispatch with TWO callers, and `select-below-field` reaches it by DELEGATION, so the posture is one arm, not three. And the "clean bench vs the P2 baseline" §5.P4a promised **does not exist** as a recorded artifact; P4a's table IS that baseline.
- **Owner rulings**: the sort is a **FIELD** on the carrier (it is a property of the whole selector, and after b-ii-2 `x.a` and `x{a}` are the same node shape, so it cannot be derived) + **delete the `auto-cache!` line in the same slice** (its body swallows exceptions — this arity change would have voided the registration silently); the Map posture takes **option (b)**, a marker, over fabricating a closed row or folding Map into the dyn machinery; then **cover ALL asymmetries including the diagnostics**; then **fold all three verify-found surfaces in too**.
- **Self-review caught the sort axis was a silent `if`/else** — P4a's exact class on a fresh axis one slice later, and load-bearing because Q_U12 already NAMES the next two sorts. Now `select-sorts` + `select-sort-unhandled`; pin validated by re-introducing the defect.
- **⭐ THE VERIFY (4 skeptics) FOUND A LIVE CAPABILITY BYPASS.** The selection arm sat BELOW the schema arm; the reference orders them the other way, and both registries accept the same name — so with `schema Person` + `selection Person from Person`, **`u{age}` returned `{:age 9}` at 0 errors** for an out-of-view field. The `'block` half was pre-existing, but this slice added the arm and put it where a colliding name could never reach it, AND shipped a test asserting "a block over a view still refuses" that was false for exactly that case. Read-capability cell. Fixed and probe-confirmed closed.
- **It also found the Map arm dropped the KEY-TYPE check** (`(check ctx k kt)` — the reference has TWO obligations, I implemented one; `mi : (Map Int String)` / `mi.a` refuses at HEAD and I would have admitted it), and **my `TBL-VIEW` fixture was MALFORMED** — `requires-paths` is a list of PATHS, I passed a flat one, so the gate refused EVERY label, both selection pins passed vacuously, and the admit branch had ZERO coverage. **The fixture-makes-the-pin-vacuous class, on the slice where I had just quoted it to the owner.**
- **Three more pins were weaker than claimed**: the sort threading through ~15 recursive sites was pinned only at LEAVES (hardcoding `'block` everywhere still passed 260); `whnf`'s sort-preserving reconstruction had a production comment warning of the exact silent re-sort and no pin; and my sort-totality pin's NAME overstated the mechanism (the guard fires at dispatch POINTS, not across the walk).
- **⭐ ASYMMETRY #4 — UNION — was missed by the mini-audit's enumeration of "three".** `u : <(Map Keyword Int) | (Map Keyword String)>` gives `u.a` → `1 : Int | String` at 0 errors while `u{a}` refuses; `select-row-of` had no union arm at all. Wholesale minting would have silently deleted a working surface — **Q_U10's own failure mode, one subject kind over.** Ninth enumeration under-count of the arc.
- **Two more twin-drifts folded**: `select-index-of` — the file's own comment calls it "the nat twin of `select-row-of`" — never got the sort axis, so the ordinal column of a 2-D table silently carried block semantics; and `pretty-print.rkt` hard-coded the BLOCK spelling, so after b-ii-2 every `x.a` would print as `x{a}` in error messages. **Third consecutive slice whose census missed a `pretty-print.rkt` site.**
- **Shipped**: the sort field (+18 arity sites), `regN!`/`reg0!` registration, the full 2-D table across FOUR asymmetries, both repaired diagnostics (the block-over-view one is LIVE at HEAD), sort totality on both dispatchers, and pp-by-sort. Battery **240 → 268**.
- **Owed to b-ii-2, named not rediscovered**: the carrier has **no strictness slot** (a dyn-row miss would panic where it is permissive today — a carrier-shape question re-opened); `select-reduce`'s "invariant violation" header goes false; and `select-project` has TWO callers, the second being the diagnostic re-walk, where `selection-field-type` has registry side effects.

### D4.P4b-ii OPENED — the audit refuted the design's named danger, found the real one, and refuted a claim b-ii-1 shipped (`56f12bab`)  (2026-08-01)

- **Mini-audit** `wf_db6ff64a-b94` (5 HEAD-pinned facets + completeness critic @ `7ba7bdad`, 6 agents / **1.36M tokens**). Every load-bearing finding R-lens-verified on the main thread before folding.
- **⭐ Q_U13 RULED [owner]: the mint uses the NEST encoding** — `x.a.b` → `($select ($select x a) b)`, one carrier per LEVEL. **And that dissolved the hardest problem.** The strictness facet established the tier is decided PER DESCENT LEVEL and concluded a scalar tier slot was "structurally insufficient" — which looked like it re-opened the carrier shape one slice after b-i settled it. The critic showed that holds ONLY for the GATHER encoding: under NEST each level is its own node with its own slot, exactly as `expr-map-get` has today. **The encoding question and the tier question were the SAME question and no facet connected them.** Nesting is also what the fold already does (probe: `x.a.b` → `(map-get (map-get x :a) :b)`), so it is the minimal diff. Accepted cost, recorded: `x.a.b` and `#p(a.b)` then encode the same path text with different nesting arity.
- **⚠ THE DESIGN'S NAMED DANGER DOES NOT REPRODUCE.** §5.P4b-ii said `preparse-expand-subforms`'s partially-opaque `$select` arm would make every `.field` lose infix/implicit-map/sibling-let processing. Five probes refuted it: every sibling pass that arm bypasses is keyed on a HEAD a folded access node never has. The arm DOES expand the subject; only the payload is opaque, which is intended.
- **⭐ THE REAL LOUD→SILENT SITE IS THE PIPE CALLER — and the in-tree prose FORGETS it.** `apply-pipe-step` appends the accumulator into any hole-free step, so `|> m foo.bar` is `(map-get foo :bar m)` → LOUD arity error today (map-get's parser arm is EXACT arity 2) but `($select foo bar m)` → **SILENT two-branch `surf-select`, `branches=((bar) (m))`** after the mint ($select's arm has only an emptiness check). R-lens-verified both halves. `tests/test-path-selection.rkt:1020` still asserts "THREE production callers" and names three — the fourth, `expand-pipe-block`, landed at P3a AFTER that test and is the dangerous one. **Zero dot-access-in-pipe coverage in the tree.**
- **⚠ TWO PREREQUISITES NOBODY NAMED**: `select-reduce` does not RECEIVE the sort (2-param signature, call site discards it) — step zero for any tier work; and **neither walk has a `'path` ASSEMBLY** (both build a ROW unconditionally, while `x.a` must yield the VALUE). The second is not green-field: **`cfg{server}.server.host` is PINNED GREEN** and becomes a `'path` selector over a `'block` one.
- **⚠ I OWED A CORRECTION.** I told the owner the dyn-row regression was "a real regression the D19 pins would catch". **They would not** — all three use the BRACKET spelling, which parses through the `map-get` keyword, not the `$dot-access` sentinel Q_U12 scopes us to. They stay green through every regression, and **nothing in the tree pins the dot spelling's permissive miss.** The set is also wider than b-ii-1 recorded: THREE of four asymmetries regress, plus a second arm nobody framed — a non-map SUBJECT degrades to `none` at zero errors today and PANICS under the carrier, with no typing counterpart, so no typing-axis enumeration could have surfaced it.
- **⚠ SILENT-ACCEPT FLIPS, invisible to any red-set census**: migrating LOOSENS QTT (map-get's `inferQ` is subject-type-gated; `expr-select`'s delegates unconditionally) and silently FIXES a live lying-"Multiplicity violation" on union subjects. Accept-direction flips produce no failing test — they must be pinned BEFORE the fold or they land unrecorded.
- **⭐ AND IT REFUTED A CLAIM b-ii-1 SHIPPED — fixed first, `56f12bab`.** `record-project`'s 'unknown HIT is TAIL-SENSITIVE (dyn → meta, closed → error); my `'path` arm was TAIL-BLIND, minting a meta unconditionally, under a comment claiming it mirrored record-project "EXACTLY". On a closed row that made `map-get` REFUSE where the carrier ADMITS — the silent-accept direction. Unreachable today only because the sole 'unknown producer forces tail='dyn in the same constructor: **latent by luck, an unenforced invariant with a comment asserting it**, the pipeline.md red flag. Pin RED as a meta where a refusal was owed, then green. Suite **9712/478/0**, battery **269**.
- **Also adjudicated**: Q_U12's taxonomy is looser than its phrasing — ordinals ALREADY ride the carrier, so "`[k]` is a different sort" is not the real reason; the structural reason is that `[k]` admits a COMPUTED key (an expr) while the payload is declared static-no-exprs. Stating it structurally says exactly when the follow-up becomes possible. And C5 is wider than recorded: `select-block-hint` SEARCHES every subfield of every failing expr, so after b-ii-2 the `'path` column opens FOUR side-effect classes inside an error formatter.
- **Next**: the b-ii-2 slice split — prerequisites (sort plumbing + `'path` assembly) → the mint → the before-the-fold pins. Owner ruling due on the split.

### D4.P4b-ii-2a LANDED — the two prerequisites nobody had named, the tier field, and the tripwires (`dae5bd75`)  (2026-08-01)

- **Q_U14 [owner]**: b-ii-2 splits **2a → 2b+2c TOGETHER**. 2c is not separable — without it a `.zz` miss gets a block-worded message whose remedy tells the user to spell it `.zz`, and `select-block-hint` runs BEFORE `closed-row-miss-hint` so the bad message WINS. Actively misleading advice, not a lost hint; "fix the message next slice" is how those become permanent.
- **⭐ The audit's two unnamed prerequisites, both landed.** `select-reduce` did not RECEIVE the sort (2-param signature; the whnf call site discarded the sort it had just bound) — so every runtime outcome for a migrated `.field` would have been decided by a sort-blind function. And **neither walk had a `'path` ASSEMBLY**: both built a ROW unconditionally under both sorts, so flipping the fold would have typed `x.a` as `{:a T}`. A path now EXTRACTS, a block PROJECTS, and a malformed multi-component path carrier REFUSES rather than silently taking the first. ⚠ Not green-field — **`cfg{server}.server.host` is PINNED GREEN** and becomes a `'path` selector over a `'block` one.
- **The tier field** on `expr-select` (arity 2→3, `#f` = no claim), INERT at 2a. On the APPLICATION not the selector (a bare `#p(…)` has no subject, so no tier), and a SCALAR only because of Q_U13 — under NEST each level is its own node, exactly as each `.field` is its own `expr-map-get` today.
- **⭐ `select-map-exprs` MAPS the tier, it does not merely carry it.** It is the ONE reconstruction point for six walkers; a carried-but-unmapped tier would never ZONK, `expr-true?` would never hold, and **every Map miss would go silently PERMISSIVE** — a reverse regression with no signal, and precisely the hidden cost the audit found in the rejected put-it-on-the-selector option. Pinned with a MUTATING mapper so the descent is load-bearing, not incidental.
- **The three before-the-fold tripwires** — pins for behaviour 2b will CHANGE, so each change is visible instead of silent. They exist because the audit found the class **no red-set census can see**: accept-direction flips produce no failing test. The union-subject lying "Multiplicity violation" (live, probe-confirmed) · the DOT spelling's permissive dyn-row miss (**nothing pinned it** — all three D19 pins are BRACKET-spelled and never reach the `$dot-access` sentinel) · `_.field`'s three shapes.
- **⚠ The `_.field` tripwire failed on its FIRST RUN** for the `:no-prelude` false-green reason this file was hardened against — `map` does not exist there, so an Unbound-variable cascade masks the deletion the pin exists to catch. The Watching-4 lesson caught my pin rather than the reverse. Moved to the prelude fixture.
- **Two b-ii-1 pins went RED on the assembly change — correctly**: they asserted a ROW where `'path` now extracts. Updated, annotated as having caught their own slice's change.
- Suite **9719/478/0**. Battery **269 → 276**.
- **Next**: 2b+2c — the mint (nested `$select`), the **pipe-caller guard** first (`|> m foo.bar` goes from a loud arity error to a SILENT extra selection branch — the one failure mode here with no signal at all), the tier wiring, the RED set, the "THREE production callers" prose fix, and the sort-aware diagnostics.

### D4.P4b-ii-2b/2c LANDED — THE FLIP; the verify found a BLOCKING permissive→panic and proved my tier pins hollow (`fe7dd4d5` · `54d940fe`)  (2026-08-01)

- **Q_U15 [owner]: a DISTINCT sentinel `$select-path`.** Forced by the light re-grounding — `surf-select` had no sort field and the elaborator HARD-CODED `'block`, so a reused `$select` left the elaborator unable to distinguish a dot access from a brace block. **⭐ And the arity gate pays for the sentinel**: `map-get`'s parser arm is exact-arity-2, `$select`'s has no upper bound, and `apply-pipe-step` appends the accumulator into any hole-free step — so a reused head turns `|> m foo.bar` from a loud arity-error into a SILENT two-branch select. The same append lives in the `>>` twin, so a blacklist needed both; the gate fixes every caller at once.
- **Registration cost MEASURED at FIVE, not the nine I quoted** — §Q8.5's surface is for a GROUPING-minted sentinel; this one is fold-minted in preparse. Mirroring the sibling beat applying the checklist. (The verify then found a sixth: `pp-datum`.)
- **The RED set was 12 datum assertions across 4 files and NOTHING else** — no value-assertion failures, no type errors. The audit's warning that a datum census is blind to type-error failures was sound; it did not bite because b-ii-1 and 2a did the semantic work first.
- **⭐⭐ THE VERIFY'S BLOCKING FIND, by TWO skeptics independently**, A/B against a baseline tree: `.field` on a union whose runtime value is a NON-MAP component **PANICKED** where `[map-get u :a]` degrades to `none` at zero errors. **I tier-gated the keyed MISS and never looked at the SUBJECT-kind arm one level above it.** The exact permissive→panic conversion the slice exists to prevent, with a message that lied twice ("the block" for a dot access; "invariant violation" when typing legitimately admitted the union).
- **⭐ MY TIER PINS WERE HOLLOW — proved by mutation.** Disabling reduction's assertive arm, and separately disabling typing's `solve-strict-assert!`, EACH left 283/283 green. 2a's tripwire covered the PERMISSIVE half; the LOUD half — whose failure STORES a wrong answer — had nothing. Three pins added (P2.b A1/A1b/A2 analogues), both mutations re-run to confirm they now fail. **New lesson: a tripwire on one side of a fork is not coverage of the fork.**
- **A silent accept-flip nobody predicted**: the field rides as a bare SYMBOL now, so the `^` splits into a re-key continuation and the `'path` assembly DROPPED it — five spellings returned the plain field at 0 errors, while `pp-expr` rendered the `^` faithfully. **Honest display over dishonest semantics.** Now a guided refusal.
- **The FOURTH consecutive missed `pretty-print.rkt` site**: `pp-datum` had no arm, so `expand r.a` emitted a raw sentinel. My b-ii-1 census fixed `pp-expr` and stopped 1000 lines short of its DATUM-layer twin — on a datum-layer migration.
- **Three binary sort dispatches, violating the rule I wrote one slice earlier.** `select-sort-unhandled` exists to forbid exactly `(if (eq? sort 'path) …)`, and I wrote its rationale. A future `'nil-safe` node would get tier `#f`, which reduction reads as BLOCK — a nil-safe miss whose contract is `none` would panic.
- **My own mistakes this unit, caught late by my own checks**: `select-reduce`'s parameter `sort` SHADOWED Racket's `sort` (the message applied `'path` as a procedure); and I collapsed THREE miss-outcomes into two, treating a block miss and an assertive Map miss as one because both panic — a P4a pin caught it.
- Suite **9732/478/0** · battery **280 → 289** · acceptance 52/52 + 89/89 + 29/29.
- **Next**: the b-ii close (D4 tracker + what b-ii-3 still owes), then P4c.

### D4.P4b-ii CLOSED — b-ii-3 DISSOLVED; residue filed; the transferable lesson is about FORKS  (2026-08-01)

- **b-ii is COMPLETE**: b-ii-1 (the (subject × sort) table, FOUR asymmetries) → b-ii-2a (the two unnamed prerequisites + the tripwires) → b-ii-2b/2c (the flip + diagnostics) → **b-ii-3 DISSOLVED**, its scope discharged at 2b. Re-verified at the close: all three `_.field` shapes work at 0 errors, including `|> r _.a`. The design's assumed repair (add `$select` to `sectionable-op-keywords`) is **INERT** — that clause is dispatched later in the same cond.
- **Residue filed as DEFERRED 24–30.** The one worth the owner's eye is **24**: `select-block-hint` SEARCHES every subfield of EVERY failing expr, from `infer/err`, under a swallow-everything handler — and the flip made the `'path` column reachable from it, opening FOUR side-effect classes (registry mutation via `register-selection!`/`global-env-add-type-only`, `fresh-meta` ×2, meta-solving `check`, a speculative network FORK) **inside an error formatter**. Mechanism confirmed by reading; **observable harm NOT demonstrated** in two close-time probes. Filed as a reachability RISK and stated that way on purpose — "I could not trigger it twice" is not evidence of safety for a walker that runs on every inference failure.
- **Also filed**: `format-select-fail`'s remaining unconditional arms (`subject-tuple` IS reachable under `'path`; three of five conditionalised arms are unpinned on both sides — mutation-proved) · `typing-propagators.rkt` has no `expr-select` registration, so the highest-traffic access surface in the language now sits on the unhandled fallback · stale rewrite comments in `surface-rewrite`/`sre-rewrite` · `_.a.b` broken **identically at HEAD** (recorded so it is not re-diagnosed as a regression) · the `defr` parenless-clause delta (strictly better, unpinned) · Q_U12's follow-ups, with the reason SHARPENED from taxonomic to structural (`[k]` admits a COMPUTED KEY; the payload is declared static-no-exprs).
- **⭐ THE TRANSFERABLE LESSON: a tripwire on ONE SIDE of a fork is not coverage of the fork.** 2a's tripwire pinned the PERMISSIVE half of the two-tier miss and I treated that as the fork being covered. The LOUD half — whose failure STORES a wrong answer — had nothing, and two independent mutations left the battery fully green. When a change introduces a FORK, both arms need pins, and MUTATION is what proves it, not a green suite.
- **What b-ii cost/bought**: four owner rulings (Q_U12 scoping · Q_U13 NEST · Q_U14 the split · Q_U15 the distinct sentinel), one mini-audit (1.36M tokens / 6 agents) and one adversarial verify (4 skeptics) that found a BLOCKING permissive→panic **two skeptics located independently** plus the hollow-pin proof. Battery 240 → **289**.
- **Next**: **P4c** — the `:` gate + the `(@bcast step)` wrapper + PVec broadcast + the `.*name`→`:name` migration with FULL residue disposal in one commit + the corpus A/B. `quests:t` uncomments there as a PVec broadcast (the solve→PVec spin-out inverted Q_U9's consequence).

### D4.P4c CO-DESIGN — Q_U8 was NOT implementable; Q_U16 keeps BOTH surfaces by moving the unwrap one layer earlier  (2026-08-01, docs)

- **Why**: P4c is where broadcast arrives and it is a fresh surface, so the owner authorized FULL grounding. Two workflows: the `grounding-audit` (`wf_d7c035da-cee`, 6 facets + completeness critic @ `532474c0`, 7 agents / **1.53M tokens**) then the adversarial `design-options-panel` (`wf_6f15c6ae-6a7`, 3 clusters × propose/critique + synthesis, 7 agents / **1.17M tokens**). Every load-bearing claim R-lens-verified on the main thread; **two of the panel's own numbers were wrong** and are corrected in D4.
- **⭐ THE HEADLINE: Q_U8 AS RULED COULD NOT BE BUILT.** §Q8.5 site 8 (join `access-sentinel?` + take a fold arm) and Q_U8's parser position-dispatch are MUTUALLY EXCLUSIVE, because **the preparse fold runs over BINDER positions**. Probed with the existing analogue: `defn f [x.a] x` → **`f : _ _ _ -> _ defined.`, a THREE-arity function at ZERO errors**. The sentinel never reaches the seat the parser would dispatch at.
- **⭐ MY OWN LEAN DIED, and the fact that killed it was one I had already measured.** I proposed dropping `access-sentinel?` membership and fusing parser-side, on the `$brace-params` precedent. The critic found `rewrite-dot-access` has **FOUR call seats** and **TWO run inside PREPARSE** (the `|>` and `$mixfix` expanders) — I had counted those four during re-grounding and never drew the consequence. So `users:name` would work in plain application and break inside pipes, `.( )` operands and map literals. And the precedent was the wrong structural class: `$brace-params` is self-contained and never fuses, while **every base-consuming marker in the tree is an `access-sentinel?` member**. Measured it myself: `[f x{a}]` → `(f x ($select-brace a))` and the FOLD makes it 2 items; unfused, `f` gets TWO args.
- **⭐ THE OWNER SQUARED THE CIRCLE** — he wanted the fused `x:Int` surface (explicitly "more important than broadcast") AND `:` as the glyph, and proposed disambiguating on whether the LHS is a collection. The premise was RIGHT and measured (287 of 289 multiplicity tokens are SPACED; the whole collision is SEVEN sites, all fused TYPE annotations in BINDER position), but "is it a collection" is a TYPE question and the mint fires three layers before typing — **the same discriminator Q_U10 already rejected as its option (c)**, and not total even at typing since `x` in `x:Int` is being BOUND. **Q_U16**: same instinct, one axis over — the early-visible distinction is **binder vs expression POSITION**, and `transform-let-blocks-stx` (parse-reader.rkt:2716) is a general reader-time walk already doing head-keyed classification that **provably precedes all four fold seats**. Mint uniformly · JOIN `access-sentinel?` · unwrap at the reader post-pass. Both surfaces kept, nothing un-shipped.
- **BONUS — it is a REPAIR, not scaffolding**: the fold over binder positions is a **live latent defect today**; P4c surfaces it rather than causing it.
- **COST booked plainly, not softened**: the binding-form head list is a PERMANENT hand-maintained enumeration with a SILENT failure mode and no retirement plan. Three conditions ride with it (eq?-preserving unwrap · a per-binding-form RED pin · TOTAL binder entries).
- **Q_U16b — `users:0` IS a legal ω step** [owner]. So the gate serves both bands, and `colon-annotation` classifies to `'symbol` — the discrimination question went live. **Settled by MEASUREMENT, not by a ruling**: the panel costed the rival provenance-field at three incompatible numbers (25/10/~14–16) with OPPOSITE verdicts; I enumerated exhaustively — **25 constructor sites / 7 files**, and `sre-rewrite.rkt` holds 4, borrowing `token-entry` as a general-purpose term carrier. That makes it a struct-OWNERSHIP question. Classifier-promotion wins by an order of magnitude.
- **The A/B diff set is now NAMED and group-aware**: 161 files / 1532 keyword / 289 colon-annotation → **SEVEN would-mint**, all enumerated in §5.P4c. Colon-annotation would-mint is ZERO.
- **Watching (new, and it is the arc's pattern in one line)**: *a precedent proves what it is, not what it resembles.* `$brace-params` and `$bcast-step` are both grouping-minted sentinels at the SAME fork — and the precedent still did not transfer, because one is self-contained and the other base-consuming. Check the STRUCTURAL CLASS, not the surface similarity.
- **Next**: P4c-1 — the two prerequisites + the classifier promotion, corpus A/B ZERO diffs.

### D4.P4c-1 LANDED — two LIVE defects fixed, and the panel's own recommendation was wrong  (2026-08-01)

- **Why**: P4c-1 carries no new surface. It lands the three things the P4c grounding audit found owed under EVERY option — and two of them turned out to be live defects at HEAD, not prospective ones.
- **⭐ The panel's hoist target was WRONG, and checking the dependency gave a simpler answer.** Three critics said hoist `adjacent-to-base?` to `reader-forms.rkt` "the cycle-free home, which requires nothing project-local". But the predicate is over the `token-entry` STRUCT (parse-reader.rkt:183) — moving it would drag the struct along and break the very invariant that made it the suggested home. And `surface-rewrite.rkt` **already requires parse-reader.rkt** (:26). So the answer is one `provide` line, and the hand-inlined four-conjunct duplicate is deleted. *Verify the dependency before inheriting a recommendation, even a thrice-corroborated one.*
- **⭐ `ns foo:bar` SILENTLY DROPPED `:bar` at ZERO errors — live at HEAD.** The guard was a NEGATIVE list of two sentinel heads, and a bare keyword is not a pair, so it matched nothing and fell through as if it were an option. The fix is a **POLARITY INVERSION**: `ns` accepts exactly ONE option (`:no-prelude`), so the total form is a positive allow-list — the same inversion `definitely-not-map?` took at P2.b slice 1. That CLOSES the `b0db8f3e` class (three unclosed instances recorded) instead of adding a fourth entry. ⚠ Named consequence: the guard RAISES, so this converts a silent drop into a WHOLE-FILE ABORT — strictly better and monotone, but harsher than POL.4 calls for. **DEFERRED 31**, owner-requested at the checkpoint.
- **`colon-annotation` promoted** (Q_U16b) with **EXPLICIT arms at BOTH `case type` sites**, never the `[else]` — whose safety would have been a coincidence of two identity-returning else-arms at the exact site the file documents as its silent-miss hazard.
- **⚠ THREE OF MY OWN PINS FAILED FOR THE WRONG REASON, caught before implementation.** One read `(car rs)` on a lone `ns` line that yields NO results; one asserted a **guessed** datum baseline for `{:0 v}` (it is `($brace-params :0 v)`) — a baseline pin asserting a guessed baseline is worse than none; two were mutually incoherent about the error channel, and fixing that is what surfaced the raise-vs-value finding. **And the FALSE-ZERO footgun bit me**: a direct `tokenize-char-rrb` without `register-default-token-patterns!` returned every character as its own `symbol` token — the exact hazard `tools/reader-corpus-ab.rkt:74` carries a tripwire for. My first probe was invalid and I nearly wrote pins on it.
- **CORPUS A/B: 161 files, ZERO DIFFS**, both legs pinned to one immutable `git archive` snapshot with only the code differing. That zero is the deliverable — it is the clean attribution baseline that makes P4c-2's seven predicted diffs attributable to the mint alone. (The owner's tree has 6 dirty `.prologos` files; a working-tree read would have measured the tree — 3rd sighting.)
- **Test delta +9**; suite green.
- **Next**: P4c-2 — the mint + the reader-post-pass binder unwrap, where the A/B lands against the named seven-site diff set.

### D4.P4c-1 + P4c-2 LANDED — Q_U16 built; the A/B answer is ZERO where the design predicted SEVEN  (2026-08-01, `182f1678` · `b1399016`)

- **Why**: P4c is where broadcast arrives. Full grounding first (`grounding-audit` `wf_d7c035da-cee`, 7 agents/1.53M, then the adversarial `design-options-panel` `wf_6f15c6ae-6a7`, 7 agents/1.17M) — and the audit **refuted the phase's central ruling**: Q_U8 could not be built, because §Q8.5 site 8 and parser position-dispatch are MUTUALLY EXCLUSIVE (the preparse fold RUNS OVER BINDER POSITIONS — `defn f [x.a] x` → a 3-arity function at 0 errors).
- **⭐ Q_U16 (owner) squared the circle.** The owner ruled the fused `x:Int` surface MORE important than broadcast AND kept `:` as the glyph, then proposed disambiguating on whether the LHS is a collection. Premise right and measured (287 of 289 multiplicity tokens are SPACED; the whole collision is 7 sites, all fused TYPE annotations in BINDER position) — but that is a TYPE question three layers before typing, the same discriminator **Q_U10 already rejected as its option (c)**. Same instinct, one axis over: **binder vs expression POSITION**, visible at the reader post-pass, which provably precedes all four fold seats. Mint uniformly · JOIN `access-sentinel?` · unwrap at the post-pass. Nothing un-shipped.
- **⭐ MY OWN LEAN DIED on a fact I had already measured.** I proposed dropping membership and fusing parser-side, on the `$brace-params` precedent. `rewrite-dot-access` has FOUR seats and TWO run inside PREPARSE — I had counted those four during re-grounding and never drew the consequence. And the precedent was the wrong structural class: `$brace-params` is self-contained and never fuses, while every base-consuming marker in the tree IS a member.
- **⭐ THE PANEL'S RECOMMENDATION WAS WRONG, thrice-corroborated.** Three critics said hoist `adjacent-to-base?` to `reader-forms.rkt`; the predicate is over the `token-entry` STRUCT, so that would break the very no-project-requires invariant that made it the suggested home. `surface-rewrite.rkt` already required parse-reader. **R-lens-verify load-bearing claims even when several agents agree.**
- **⭐ THE BINDER TABLE IS MEASURED, and it is NEAR-UNIVERSAL** — not the five entries Q_U16 implied when its cost was booked. Owner caught `$pipe` arms (defn AND match); patterns NEST (`| [cons h:Int t] ->`); `defr`/`rel` are members via the BARE-NAME spelling though `?a:Nat` is immune — an under-count I committed and corrected inside one session by measuring instead of listing. Recorded as a correction to the booked cost, not absorbed.
- **⭐ THE A/B IS ZERO, WHERE §5.P4c PREDICTED SEVEN** — and zero is correct: that prediction was computed against the mint WITHOUT the unwrap; with it those sites ROUND-TRIP. Verified it discriminates rather than banking the number (`users:Int` mints, `let x:Int 5` round-trips, same lexeme), because zero-where-seven-was-predicted is what a silently-broken gate looks like.
- **A REGRESSION I INTRODUCED, caught by an EARLIER PHASE'S pin**: `apply-binder-unwrap` did `(car kids)` unguarded, but `classify-let-block`'s FAIL path returns a syntax object wrapping `($let-error …)` — a contained layout error became a WHOLE-FILE ABORT. parse-reader.rkt documents that exact hazard and I reintroduced it one screen below the note.
- **P4c-1 also fixed a LIVE defect**: `ns foo:bar` silently dropped `:bar` at 0 errors. Fixed by POLARITY INVERSION (positive allow-list of the one option), closing the `b0db8f3e` class rather than adding a fourth entry. Raise-channel residue = **DEFERRED 31** (owner-requested).
- **Watching (new)**: *a precedent proves what it is, not what it resembles* · *measure the baseline, never assert a remembered one* — I guessed twice this session (`{:0 v}`, the quote datum) and both pins failed for the wrong reason.
- Suite **9758/478/0** · battery 294 → **312** · acceptance 52/52 · A/B ZERO.
- **Next**: Q_U16 condition (c) — the loud-refusal hardening at the 17 consumers. P4c-2 stays 🔄 until it lands. ⚠ P4c-2's commit did NOT get an adversarial verify (context exhaustion).

### D4.P4c-2 COMPLETE — re-grounding found a LIVE regression, and MUTATION found my own guard was dead code  (2026-08-01, `cbd8d1a7` · `8c4faee2`)

- **Why**: the session opened to finish P4c-2 with condition (c). The re-grounding (per HANDOFF_PROTOCOL step 5, summarize-before-work) turned into the **retroactive adversarial verify P4c-2's own commit never got** — and it found something, which is now 10 of the last 11 slices.
- **⭐ A LIVE END-TO-END REGRESSION, invisible to every gate the phase ran.** `defn- priv [x:Int] x` works on the pre-mint baseline and FAILS at `b1399016`. `private-form-base` normalized the `-` suffix at PREPARSE, strictly AFTER the reader post-pass, so the head is literally `defn-` and the table's memq misses — **eleven suffixed heads**. Isolated by an A/B against a worktree pinned at `182f1678`. The **corpus A/B stayed ZERO** (no tracked file pairs a private form with a fused annotation) and **all 18 P4c-2 pins are reader-level**, so nothing could see it. Same blindness that lets `def x:Int := 5` hold a green datum pin while aborting the whole file.
- **⭐ OWNER RULED SCOPE (ii)** — full unwrap-coverage repair, not consumer-hardening alone, because **measuring the table produced FOUR misses inside one session**. That record disqualifies the enumeration as the safety property and **inverts the design's framing**: condition (c) is the mechanism, the table is the optimization over it.
- **The four**: private-suffix heads · a leading `{A}` group ATE the arming so the real param group leaked · `defmacro` in no list · the flat single-line `$pipe` spelling. **Plus a direction nobody booked**: a terminator SEARCH ran to the end of the list when no terminator existed (and `:=` is OPTIONAL in WS `let`), silently **stripping BODY broadcasts**. Condition (c) structurally cannot catch that — no sentinel survives to refuse. Q_U16's three conditions do not cover it.
- **ROOT CAUSE of three of five, and it explains an accident**: one STICKY `armed?` flag that survived every SYMBOL and was discharged by the next LIST of any kind. It caused BOTH directions, and it is why the FIRST `$pipe` arm unwrapped **by accident** — the `($bcast-step …)` itself was being mistaken for the param group. Replaced with explicit region consumption over the real grammar.
- **⭐ MUTATION CAUGHT MY OWN DEAD GUARD.** The first `bcast-step-datum?` compared `(car d)` to a symbol, but `stx->datum` peels ONE layer — the head arrives still wrapped, so it never returned #t at ANY site. Suite green, probes green, guards inert. Only emptying the binder tables found it. **A green suite is not evidence for a tripwire; only mutation is.**
- **Condition (c)'s SHAPE changed once measured**: the 17 predicate-keyed sites are NOT where a leak lands (`parse-defn`'s DISPATCHER decides by content — its bare-params arm needs every element `symbol?` — so a leaked LIST never reaches the named consumer; **5th instance of the `parse-rel-params` blindness class**), and the failure mode was LOUD-generic, not silent. So (c) shipped as a **diagnostic upgrade**, named as one. It also replaced a **LYING "Unbound variable"** for expression broadcasts with an honest not-yet error.
- **Filed**: a fix chip for the pre-existing `def x:Int := 5` whole-file abort (`expand-def-assign` wants `(: Int)`, gets `(:Int)`), cross-linked to gh #80/#68/#24 and to DEFERRED 31, to be worked in a separate worktree.
- Gate: suite **0 failures / 478 files** (count nondeterministic — the standing rule) · battery 312 → **321** · acceptance 52/52 + 89/89 + 6/6 + 29/29 + 28/28 · **corpus A/B ZERO diffs / 163 files vs the PRE-MINT baseline**, both legs on ONE `git archive` snapshot · row matrix identical to baseline in every column.
- **Next**: adjudicate the adversarial verify (`wf_cb055ff6-16a`, 6 skeptics + adjudicator), then P4c-3.

### The adversarial verify REFUTED my own close — P4c-2 reopens  (2026-08-01, `wf_cb055ff6-16a`)

- **Why**: the owner asked for the verify after the close. It found **three BLOCKING regressions**, all re-verified on the main thread by A/B against a real `182f1678` build. P4c-2 goes back to 🔄; the ✅ I wrote an hour earlier was wrong.
- **The three**: (1) **comma false-mint** — `{:a 1,:b 2}` mints, because `adjacent-to-base?` reads the raw COMMA token that `group-items` SKIPS and never emits; 7 of 7 collection contexts; regression vs **`b1399016`**, i.e. inherited from the MINT, and the only one on ordinary EXPRESSION syntax where the unwrap cannot help. (2) **five more missing binder heads** — `capability` `Pi` `Sigma` `DSend` `DRecv`, all live surfaces. (3) **`let` bracket form + any SPLICED `let`** — `binder-region-heads` is consulted ONLY by the HEAD test, so a sibling `let` is invisible: **the identical blindness the same commit fixed for `rel`**, applied to param-heads but not region-heads.
- **⭐ THE REAL FINDING, and it is fair**: the commit drew the lesson that *the enumeration cannot be the safety property* — **and then shipped one more round of enumeration**. All three BLOCKING items are a missing row or arm in a hand-maintained table, and all three were invisible to the corpus A/B for the SAME reason the commit itself documented for the private-suffix miss: **no tracked file writes the fused spelling** (verified: zero `,:`, zero fused bracket-lets, zero fused `capability`/`Pi`). **A ZERO corpus A/B is not evidence of safety for a surface the corpus does not exercise** — state it as a limit, do not leave it as an inference.
- **Also**: `unwrap-bcast-step`'s unguarded `cadr` is a SECOND distinct whole-file abort (zero-payload `($bcast-step)`); the over-reach direction survives in property clauses, bare-`fn` and type unions; `parse-param-names-for` is an unhardened consumer; one guard I shipped is dead code by construction.
- **Blind spot nobody covered, me included**: the `.pnet` module-cache round-trip and the REPL/LSP reader entries. Every probe in the whole arc used exactly two doors.
- **Next**: owner ruling on fix strategy — another enumeration round vs inverting the default vs making binder-ness lexical.

### THE INVERTED DEFAULT — P4c-2 closes; all SEVEN regressions erased  (2026-08-01, `68cdaae7` · `36dbc063`)

- **Why**: the verify's 3 BLOCKING findings expanded, on measurement, to **SEVEN** regressions (comma false-mint · `Pi` · `Sigma` · `capability` · bracket-`let` · spliced-`let` after `->` · `let` on a def RHS), each A/B'd against a real `182f1678` build. Owner ruled **option (2), invert the default**, plus the comma fix "and any other enumerations we can identify".
- **⭐ THE ENABLE-SET IS EXPLICIT AND EMPTY.** `broadcast-enabled-contexts` = `'()`. Deliberate staging, not omission: the sentinel has no consumer until P4c-3, so preserving it anywhere buys nothing and costs a **spelling-dependent surface**. I built the MODERATE form first and measured it — it left the sentinel alive in a FLAT `def y := users:name` but not in an indented body, an accident of recursion order rather than a decision. Made uniform.
- **⭐ THREE CHECKABLE CONSEQUENCES, not promises**: the 15-probe matrix is **byte-identical** to a real `182f1678` build · corpus A/B **ZERO/163** vs PRE-MINT · **the eight `test-parse-reader` "deliberate flips" FLIPPED BACK** to their pre-mint datums, mechanically demonstrating **mint ∘ unwrap = identity** on every one of those shapes.
- **⚠ THE ENUMERATION DOES NOT VANISH — IT MOVES** (to "where broadcast SURVIVES"). Only the failure DIRECTION changed: a miss now means "broadcast doesn't fire here yet" instead of breaking working code. Recorded so the close does not overclaim.
- **Closed the COMMA as a CLASS, not a third special case**: `adjacent-to-base?` asks about the physically preceding token while the base it means is the last thing in `result` — and those disagree for every token grouping consumes WITHOUT EMITTING. The quote decline was written for exactly this and its own comment already named the shape.
- Also: region heads are not always form heads (the blindness already fixed for param heads) · the bracket bindings group · `unwrap-bcast-step`'s unguarded `cadr` (a whole-file abort — **third** instance of that shape in this track) · **deleted** the guard the verify proved dead by construction, because a dead tripwire reads as coverage.
- **Deliberately NOT added**: rows for `capability`/`Pi`/`Sigma`/`DSend`/`DRecv` — the inverted default covers them and every future form, which is the point.
- **Honest limit**: the guided `bcast-step`/`bcast-step-binder` messages are now UNREACHABLE, live behind the empty set, reachable at P4c-3. Pins restaged to assert today's inert behaviour so nothing claims coverage it lacks.
- **Watching (new)**: **a ZERO corpus A/B is not evidence of safety for a surface the corpus does not exercise** — all seven regressions were corpus-invisible for exactly that reason, and the commit that first hit it had *documented* the reason. 2nd data point; a 3rd should promote it to the rules tier.
- **NEXT**: P4c-3 — enable contexts one at a time; the over-reach survivors (property clauses, bare-`fn`, type unions) go live then; `parse-param-names-for` unhardened; the `*`/`^` swallow hazard; and **the blind spot nobody has tested: the `.pnet` module-cache round-trip and the REPL/LSP reader entries** — every probe in this arc used exactly two doors.

### P4c-3's mechanics LANDED; the architecture question went to PPN, and four doc-truths fell out  (2026-08-02, `3b998aa8` · `f6310c27` · `19ab78a9` · `e06b5048`)

- **Why**: opened P4c-3 with its prerequisites (owner ruling), landed the thirteen-site kind, then the design panel + audit redirected the remaining question away from an imperative answer entirely.
- **The kind ✅ `3b998aa8`** — `(@bcast step)` across all thirteen sites. **⭐ THE `ADDING A KIND` RECIPE HELD** with no correction: the first enumeration in this arc to survive a new member intact. The arms encode a real split — NAME/KEY walks DELEGATE (ω changes container arity, not key behaviour), VALUE walks emit a guided NOT-YET, because delegating there would project off the CONTAINER instead of broadcasting over it.
- **⭐ THE PANEL FOUND A DEFECT I INTRODUCED** (`f6310c27`). `apply-binder-unwrap`'s `[else]` read `(scan-for-param-heads (map unwrap-binders-deep kids))` — strips deeply FIRST, so the scan was DEAD WORK, and since `binder-param-head?` lives at exactly one site inside it, the **eight most important binder forms would have been blanket-stripped at the first grant**. The enable-set was a switch wired to a broken path. Fixed by asking the SCANNER whether it recognized anything rather than re-testing its arms (a second recognizer is the F1b.7g class).
- **⭐ THE OWNER REJECTED BOTH IMPERATIVE OPTIONS ON ARCHITECTURAL GROUNDS** — "parsing on propagators would not admit an ordering requirement or phase passes". A probe had already shown per-position is NOT computable at descent (aligned/sibling `let` are built AFTER the child recursion), so the imperative answer collapsed to a second PASS — exactly what the mantra forbids.
- **⭐ AND THE ON-NETWORK ANSWER IS CHARTERED, NOT NEW.** The audit refuted THREE of my own stated facts: the reader is NOT propagator-based (**zero** installs across all seven parse-layer files — a cell ledger); "unknown head stops being a special case" is refuted by my own in-code comment (*the enumeration does not vanish, it MOVES*); and broadcast-as-propagator-broadcast is refuted as an IDENTITY (items fixed at install · one output cell · flags 0 so not fire-once · LEFT fold ⇒ O(N²) merge). Homes: umbrella → **PPN 4D** (blocked on 4C), per-node cells → **Track 8**, realization → **NTT `:embedded` PU + `:diff`**, ambiguity → **Track 5**, ω node → the **broadcast-propagator node track** (chartered WITH a trigger, and invisible in every tracker until now).
- **Four doc-truths fixed** (`19ab78a9` · `e06b5048`): the AMBIENT claim that broadcast gives thread parallelism (inert metadata, zero production readers, and adopting it is ANTI-parallel since executors chunk pids) · all four `rewrite-dot-access` seat coordinates, stale in three places · PPN master rows 1 and 3 marked ✅ as propagator work when the Network Reality Check fails · the "Phase 1f" IOU, stale since 2026-03-26 and pointing at a phase that shipped an integration gate instead.
- **PPN 4D §12 seeded** (owner-requested) + linked from the master: the reader layer, which 4D's core proposition names but no phase A–G scopes.
- **Watching (new)**: *a green suite is not evidence for a TRIPWIRE — only mutation is* (2 catches this arc: a dead guard, and the `[else]` defect). *A ZERO corpus A/B is not evidence of safety for a surface the corpus does not exercise* (2nd data point). *Prior-art misses are now FOUR in this arc* — search by CONCEPT.
- Gate: suite **0 failures / 479** · battery 322 → **325** · acceptance 52/52 + 89/89 + 6/6 + 29/29 + 28/28 · corpus A/B ZERO/163 vs PRE-MINT · **41 unpushed, do NOT push**.

### P4c-3 CLOSES on the kind + arms — the enable-set moves to P4c-4, because there is nothing behind the switch  (`d477772c`)

**Why**: the sub-phase opened on the owner's reserved question — per-FORM vs
per-POSITION granting, and is preserve TRANSITIVE. **The question turned out not
to be answerable as posed**, and three MEASURED facts say why: (1) the
"enable-set" is a BOOLEAN wearing a list's clothes — three production
references, and the only consultation is `(pair? …)`, so nothing tests
membership and any non-empty value flips one global switch; (2) mutation A/B
showed granting anything STRIPS application-position broadcast
(`defn body-app [q] [one users:name]` measured UNCHANGED empty vs non-empty)
while `def` RHS and arm bodies preserve; (3) flipping the unknown-head default
to PRESERVE is refuted from the corpus by `[add ?x:Nat ?y:Nat] = 5N`, a live
BINDER position under an unknown head. Both spellings are `[SYMBOL item item]`,
so the head-keyed walk cannot decide it in EITHER direction. Owner agreed with
the recommendation to close on the kind + arms and move the set to P4c-4.

⚠ **CORRECTED 2026-08-02 — THAT REFUTATION WAS ITSELF WRONG.** `?x:Nat` is glued
into ONE TOKEN by `recognize-narrow-var-annot`; `[add ?x:Nat ?y:Nat] = 5N` mints
NOTHING under any grant. I inferred it from "this line runs 0 errors today" without
checking whether it MINTS — and the tree's own comment said "Immune by construction".
**PRESERVE has ZERO measured corpus regressions** (795-file census). See D4 `#q-u18`.

**Surprise**: there is **nothing behind the switch at all.** `make-select-bcast`
has ZERO production callers, and `$bcast-step` is **absent from
`access-sentinel?`** — a P4c-2 partition deliverable that never landed *under a
✅*, hidden because `broadcast-access?` in that list is the RETIRED
`$broadcast-access`. So no grant could have made broadcast work in any position;
it could only have changed which not-yet error you got.

**Fix**: BOTH accessors D4 names "outside the recipe" were ω-blind — the claim
that `select-step-cont` "was covered" was false. `select-step-name` was an
ASYMMETRY (the classifier saw through the wrapper; the LABEL came from the raw
leaf), live NOW in a pure static key computation feeding the parser's duplicate
and L4 checks, where a non-symbol can never match under `eq?` — silent.
`select-step-cont` let `parser.rkt`'s `^`-in-path-access refusal silently not
fire, a hazard **D4 §4310 had already booked**.

**⭐ The adversarial verify changed the fix, and that is the entry.** My first
cut kept `select-step-cont` blind and hand-copied the unwrap into the parser,
justified as "its callers ask several questions (kind AND cont)" — **false at
exactly the site that got the copy**, which asks one. My enumeration said FIVE
call sites; there are NINE. Transparency is a measured NO-OP at the other eight.
So both accessors are transparent, the copy is gone, and the trap is REMOVED
rather than pinned — my first cut had pinned the blindness as *intended*, which
would have made it permanent. It also caught that my fifteenth-site pin defined
a local COPY of the predicate (green after reverting the production fix — a dead
tripwire, the exact thing P4c-2's close deleted one instance of), and that every
`syntax.rkt` self-coordinate I wrote was stale by the length of the comment that
made it stale — **the class `19ab78a9` fixed ONE COMMIT EARLIER**.

**Booked, not fixed**: DEFERRED 39's sweep — which that item asked for and I had
not run — found TWO more ω-blind sites (`parser.rkt:1170`, `:1212`), both silent
and diagnostic-degrading. Left unfixed *on purpose*: no probe I wrote reaches
either, and fixing a diagnostic that cannot be gated is the failure mode.
DEFERRED 40 books `select-step-name`'s remaining non-totality.

**Watching (new)**: *the recipe held for its thirteen; the BOUNDARY around it did
not* — an enumeration cannot see the shapes it does not enumerate, and "the
recipe held" was true of the thirteen and never a claim about the two helpers
outside it. *A self-referential coordinate map invalidates itself* — cite NAMES.

**Next**: P4c-4 — PVec broadcast + the law pins, and it now also owns the
enable-set plus its five prerequisites (`access-sentinel?` membership · the
producer bridge · a TEST SEAM · the per-context dispatch mechanism · the
unknown-head ruling).

Gate: suite **9799 / 479 files / 0** · battery 325 → **328** · acceptance 52/52 +
89/89 + 6/6 + 29/29 + 28/28 · the five `^`-refusal spellings re-probed E2E · the
two unreachable diagnostic sites A/B byte-identical · **1 unpushed**.

### P4c-4a — the test seam lands, and the verify refuted TWO of my own claims  (`f31237fd`)

**Why**: the enable-set could not be exercised by any shippable test —
`broadcast-enabled-contexts` was an unexported `define` and the predicate was a
GLOBAL `(pair? …)` that never read the list. So the whole preservation walk was
validated by SOURCE MUTATION only, which is the route that let three enumeration
gaps through this arc. Now a guarded parameter with membership keyed on the
node's own head; default empty, so production is byte-identical (proven, not
just tested).

**Surprise**: **the verify refuted two claims I had written into the code.**
(1) My scoping story was HALF TOLD — I documented that an outer grant cannot
reach INWARD, and missed that an ungranted ANCESTOR destroys what a granted
descendant preserved (the not-granted arm deep-recurses through already-visited
sub-groups). The real rule is a CHAIN, and it is what decides whether P4c-4b's
first real grant works. (2) "Mechanism only" was too strong — the `[else]` arm's
CODE is untouched but its REACHABILITY is not. Both pre-existing, production
unaffected; both now documented and one of them pinned.

**Fix**: it also found a REAL defect — the parameter had NO GUARD, so the natural
typo (dropping the list) raised `memq: not a proper list` at READ time outside
any per-command handler. **Fourth instance of the whole-file-abort shape in this
track**, with the first three documented one screen away. And my totality comment
argued from `(memq #f …)` being #f for every list, which is false.

**A pin caught me encoding a wish**: I asserted granting the INNER head `g` would
preserve inside `[g users:name]`. It does not — `g` is unknown, so the
unknown-head arm strips regardless. The failure was correct behaviour, and it
re-confirms P4c-3's finding from a second direction, now by a shippable test.

**Perf** (the predicate runs once per node): +45 ns/call; ~681 calls on a
1164-line file vs 139 ms reader wall ⇒ ~31 µs, 0.02%. A 40× `memq` inflation is
within noise — so suite wall movement is ambient, not this.

**Watching (new)**: *a pin that turns only ONE red is thin* — at assertion
granularity my six test-cases were 2-of-12, i.e. four are regression ANCHORS, not
discriminators. Say which is which rather than reporting a count.

**Next**: P4c-4b — `access-sentinel?` membership + the fold arm (whose emitted
datum must NOT be sentinel-headed, or preparse re-entry swallows a left sibling
per pass) + the producer bridge + the `segment-select-items` arm, carrying the
dual-spine merge assumption.

Gate: suite **9807 / 479 / 0** · battery 328 → **336** · acceptance 52/52 + 89/89
+ 6/6 + 29/29 + 28/28.

### Q_U17 RULED B2 — a Path segment is a first-class `Step` value; new slice PF  (`a82b0ac8` open → `b77b3e2e` ruled)

**Why**: the owner's concern — "bring Path Selection closest to
FIRST-CLASSNESS" — turned out to land on a real seam. Path first-classness is
**DECLARED, NOT DELIVERED**: `segments : Path -> [List Keyword]` is true of ONE
of six step kinds, and `path-segments` has **never marshalled** (it hand-builds
a Prologos cons-chain where the marshaller wants a Racket list — pre-existing,
`f072c115`), so `from-segments`/`path-append` are dead with it. Eleven sites read
segments as bare symbols. ω did not create the gap; it made it unignorable.

**Ruling**: reify `data Step` on the `Datum` pattern; `Path` stays ground.
Placed as its own slice **PF**, before P5, because P5's B3 same-spine merge
depends on it.

**Surprise — the panel corrected my framing twice, and the second one is mine
alone.** (1) B1 (defer) is disqualified by **Spec as Living Interface**, NOT by
First-Class-by-Default as I had argued: the foreclosure clause doesn't bite
because Q_U5 already made the first-class decision and the pending composition is
NAMED. What kills B1 is that the declaration is a live lie about a function that
also aborts — and there is **no zero-cost B1**, since the honest minimum deletes
three dead declarations. (2) **My own seed option B3 (parameterize) was
UNNECESSARY, not merely costly** — `step-sub` recurses through `List`, not
`Path`, exactly as `datum-cons` recurses through `Datum`. I proposed a solution
to a problem that does not exist.

**The sharp one**: an emergent **B6 eliminator-first** (a total fold, no new
data type) is *expressively equal* to B2. It fails on a structural ground —
**a `data Step` is a lattice element** (mergeable, cell-storable, unifiable) and
**a continuation is not**, the same argument Q_U5 used against
selector-as-function. Disqualified because P5's B3 same-spine merge is in scope
(verified from the tracker, not taken on the panel's word).

**Decision-forcing answer**: **P4c-4b proceeds unchanged.** `(@bcast step)` is a
unary constructor of a closed union with ONE producer and ONE classifier, so
converting later is cheap *because that is already the ADT shape*. The encoding
is not where first-classness is won or lost. Two riders verified on the main
thread: the `.pnet` gate item **drops** (`expr-path` IS registered), and the four
"a wrapper never heads a branch" comments need correcting **with polarity
inverted** — three justify an arm by a claim the same file refutes.

**Watching (new)**: *"declared, not delivered" is a distinct failure mode from
"validated, not deployed"* — a typed FFI declaration that never marshalled, with
its consumers dead behind it, and nothing red anywhere. Second sighting of a spec
lying while the suite stays green.

**Next**: P4c-4b, with both riders folded in.

Gate: docs-only; suite/battery/acceptance unchanged from the `0ca22343` baseline
(**9822 / 482 / 0** · battery **336** · 52/52 + 89/89 + 6/6 + 29/29 + 28/28).

### P4c-4b — a broadcast goes END TO END, and the not-yet stops being a whole-file abort  (`6b22515d`)

**Why**: the six named parts would have shipped a **whole-file abort**.
`select-bcast-not-yet` raises a raw `error`, `process-command/solve-guard`
catches only `exn:prologos-solve` (deliberately), and the producer bridge is
exactly what makes that raise reachable. So the slice's first act had to be the
channel, not the bridge.

**Fix**: the root cause was **two propositions sharing one channel** — *the
compiler is broken* (`select-step-kind-unhandled`, where a raise is RIGHT) and
*the user wrote something unbuilt* (where it is WRONG). Typing now refuses via
`(values #f (select-fail 'bcast-not-yet …))`; reduction KEEPS its raise, because
reaching the value layer after typing refused IS an invariant violation. Same
root-cause shape as the `loc->line` arc — second sighting.

**⚠ This corrected a claim I made to the owner.** I said an error VALUE would be
invasive and need threading. It wasn't — the walks already thread a failure slot
(`(if cf (values #f cf) …)`) with an established `select-fail` constructor. The
principled fix and the cheap fix were the same fix, and I had argued otherwise.

**Surprise — a probe caught my parser arm being wrong.** `$select-path` consumes
the SUBJECT itself and passes only `(cdr args)`, so for `users:name` the ω step
arrives FIRST with no `cur`. I had treated that as branch-initial, and it refused
the headline spelling with "needs a preceding subject". `cur = #f` now STARTS a
branch, as `plain-key?` does.

**Both silent payload cases are now loud**: `users:0` becomes an ORDINAL (Q_U16b)
rather than a nominal key named `0`; `users:tags*` gets a guided refusal rather
than becoming a field literally named `tags*` (`ident-continue?` admits `*`, so
it arrives as ONE token and nothing keyed on token TYPE can see the operator).

**⚠ TWO FALSE GREENS OF MINE, and they share one cause**: I grepped for a
keyword instead of counting the outcome. A module-load `unbound identifier` is
not a `FAILURE`, and rackunit reports an arity mismatch as `ERROR`. Both were
caught by the batch runner's own count, not by me. **Watching (new)**: *use the
runner's count; a grep for one keyword is not a gate.*

**Next**: P4c-4c — the ω VALUE semantics (PVec broadcast + the L1/extent law
pins), which is exactly what the new not-yet message names.

Gate: suite **9831 / 482 / 0** · battery 336 → **340** · acceptance 52/52 + 89/89
+ 6/6 + 29/29 + 28/28 · **corpus A/B ZERO semantic diffs**.

**On the A/B, because the scoping is the point**: a full 139-file run was started
and ABANDONED (~40 s/file × 2 legs ⇒ hours), then re-scoped to the **12 files
that can possibly MINT**. That is targeting, not narrowing — for every other file
the new `access-sentinel?` member changes an `or`-chain's cost, not its result.
Raw: 4 of 12 differed, all internal-counter drift with a **constant 386 offset**
— the tell for a startup difference rather than a per-file one. **Discriminated
rather than assumed**: a control file that CANNOT mint shows the same drift.
Normalized ⇒ ZERO semantic diffs, control IDENTICAL, one residual that is a
FILESYSTEM PATH in a stack trace (worktree vs main checkout — an artifact of the
method). Both legs read ONE `git archive` snapshot; baseline worktree-pinned;
diffed on FULL OUTPUT, never error counts.

### P4c-4c RE-SCOPED before writing any code — three reader facts, and two carrier facts of mine that were wrong  (`34123b07`)

**Why**: the mini-audit's critic found that **the slice as scoped could not land
its own deliverables**. I re-ran its probes rather than accept them, and all
three hold: a **bare top-level ω is stripped under EVERY grant** (so every
acceptance-file ω line is unreachable); the **brace-payload spelling does not
mint** (killing `quests:{t r}` and both §3.2.1 extent members); and
`broadcast-enabled-contexts` has **zero production setters**, so `process-file`
never sees a grant.

**⚠ Two carrier facts I had asserted were wrong**, and each would have produced
bad code: `expr-hset` has **no arm in any dispatcher** — Set is not a selection
carrier today, which is true of the struct and false of the machinery, so I'd
have written an arm that can never fire — and **`expr-Record` is TYPE-only**, a
het tuple's runtime value being an `expr-rrb`, so I'd have mis-keyed the value
arm.

**Fix**: re-scoped to PVec value semantics (which is what the scope line always
said), plus a **fourth site the partition never named** — an `rrb-of` container
guard, needed because reduction's walks carry no failure slot.

**⭐ Reading the design beat re-homing by instinct.** Most of the deferred work
was ALREADY scheduled at P4d/P4e — and doing so corrected me: I had proposed the
Q_U9 List refusal for P4c-4c, and Q_U9's own ruling says **"Implementation:
P4d"**, twice. Four items were genuinely unhomed and now have homes.

**Two new open questions**, both persisted with their measurements:
**Q_U18** — the unknown-head policy and the first production grant are ONE
decision, gating every corpus uncomment. It is *not* "pick an arm": P4c-3
measured that the head-keyed walk cannot decide it in either direction.
**Q_U19** — `^`-on-broadcast, already constructible, with my P4c-4b pin freezing
an accident rather than a ruling.

**Next**: the co-design on Q_U18. Handoff written for a fresh session.

**Watching (new)**: *a phase's stated scope can be unlandable for reasons that
live a layer below it* — three READER facts blocked a VALUE-semantics slice, and
none was visible from the phase's own partition line.

Gate: docs-only. Suite/battery/acceptance unchanged from `17086a09`
(**9831 / 482 / 0** · battery **340** · 52/52 + 89/89 + 6/6 + 29/29 + 28/28).

### Q_U18 RULED, and the PRESERVE flip landed — but G2 turns out to be the operative half  (`6af65a66` → `3b4a02cf` → `e71ef6b8`)

**Why**: broadcast was fully built reader→fold→parser→typing yet reachable only
from tests. Q_U18 named that as ONE decision — the unknown-head policy *and* the
first production grant.

**⭐ The ruling turned on a refutation of my own record.** I had propagated, into
D4, DEFERRED 32, the dailies, memory and a handoff, that PRESERVE was "refuted
from the corpus" by `[add ?x:Nat ?y:Nat] = 5N`. **That line mints nothing** —
`?x:Nat` is glued into ONE TOKEN by `recognize-narrow-var-annot`, and
`parse-reader.rkt`'s own comment already said "Immune by construction". I had
inferred it from "it runs 0 errors today" without checking whether it MINTS.
Corrected across every artifact at `62adff80` *before* ruling, so the ruling was
made against an accurate record.

**What that exposed**: the discriminator the question was hunting for was
**already in the tree**, one layer below the walk — the tokenizer. PRESERVE has
ZERO measured corpus regressions (795-file census), and the five "measured
casualties" that motivated the inverted default were **synthetic probes**, not
corpus sites.

**Ruled**: (i) unknown-head → **PRESERVE** (owner: "worth the trade"); (ii) grant
= **G4**, hold test-only until P4c-4c. **G2 captured as a LEAN, not a ruling**,
re-evaluated at P4c-4c's close.

**Surprise, and it reframes G2**: the flip **works** — with the inner head
granted, `[one users:name]` → `(one users ($bcast-step :name))`, nested too — but
the enable-set's FIRST arm strips any node whose own head is ungranted, and
granting every function name is absurd. **So the flip is inert in practice until
G2.** G2 is therefore not "retire expired scaffolding" but *the thing that
delivers the feature*. Pinned in the battery so it cannot be forgotten.

**Second correction in the same slice**: the "one real defect" I'd recorded as
owed with the ruling — the digit-headed hole — **is not a defect**. `?x:0` and
`?x:w` are BOTH already refused by an existing guided diagnostic naming them.
Nothing owed.

**Watching (new)**: *reading the DATUM and inferring the MEANING* — three times
this session a recorded mechanism claim fell to an end-to-end probe, same shape
each time. The reader datum is not the semantics.

**Next**: P4c-4c — the PVec value semantics (3 typing arms + 3 reduction arms +
the `rrb-of` guard + the L1/extent laws in `def` position).

Gate: suite **9835 / 482 / 0** · battery 340 → **344** · acceptance 52/52 + 89/89
+ 6/6 + 29/29 + 28/28 · default behaviour pinned UNCHANGED.

### G2 RULED ALONGSIDE P4c-4c — the two halves become one slice, and production behaviour moves for the first time  (2026-08-04, `5520e321`)

**Why**: session opened on a context regain, and the summary-back surfaced the
decision the arc had left dangling. Q_U18 scheduled a G2 re-evaluation for
P4c-4c's *close*; the owner held it at the *opening* and ruled **alongside**.

**The argument, in one line**: the PRESERVE flip is inert at the default because
parse-reader's FIRST arm (`:3117`, re-verified this session) deep-strips any node
whose own head is ungranted. So value semantics alone ship behaviour for a
surface only the battery can reach — *Validated ≠ Deployed*, a shape this arc has
already named once — and G2 alone ships reachability with nothing to evaluate to.

**⚠ The consequence is the operative part, and it is genuinely new.** G2 is the
FIRST change in this arc where **production behaviour actually moves**. Every
prior P4c slice could lean on "default `'()` ⇒ byte-identical" and did. Three
obligations follow, none of which prior slices carried: the corpus A/B becomes
load-bearing (**re-derive** the mintable set, don't inherit P4c-4b's 12 files);
the battery **inverts**, because P4c-4a's pins were mutation-verified AGAINST the
grant seam and one is named *"the flip is INERT AT DEFAULT"* — so RED pins and
**VACUOUS** pins must be enumerated *separately*, the second being the dangerous
class; and the accepted PRESERVE residual (macro pattern vars) becomes reachable
in production, so the error **channel** needs verifying, not the message — four
whole-file aborts have shipped in this track through exactly that gap.

**Verified on the main thread, not inherited** (the arc's recurring failure is
reading a datum and inferring the meaning): the three typing arms at
typing-core `:1043`/`:1142`/`:1191`; the three reduction raises at reduction
`:1762`/`:1823`/`:1875`; `champ-of` `:1649` and `index-into` `:1713` exist and
**there is no `rrb-of` twin**, confirming the unnamed fourth site; and the
first-arm strip at parse-reader `:3117`, which is the whole G2 argument.

**Doc-truth repair, found while updating the STATE head**: the **Watching**
section had lost its header and items 1–2, and item 2's tail was **welded onto
the NEXT line** — which is how it was spotted. Structure restored; item 2
reconstructed from its surviving tail; **item 1 is unrecoverable and was NOT
invented**. Worth naming because the STATE head is the most-read block in the
file and the corruption was silent.

**Also folded**: the critical path revised (P4c-4c + G2 as ONE slice, G4
discharged BY it rather than fired after); the digit-hole fix struck from the
sequencing table (never a defect); the P4c tracker Notes recompacted 680 → 478
chars, back inside the 160–800 band, plus a doubled markdown-link artifact fixed.

**Next**: adjudicate the mini-audit `wf_a24f3e0f-d84` (7 facets + completeness
critic @ `327e278c`, scoped to BOTH halves), R-lens-verify its `rlens_targets`
surgically, then co-design with the owner before any code.
