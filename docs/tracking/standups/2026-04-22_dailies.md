# Dailies — 2026-04-22

## Carried forward from 2026-04-19 interval

### Completed (PPN Track 4C Phase 9+10+11 Addendum — Stage 4 partial)

Single arc across 3 sessions (2026-04-19, 2026-04-21, 2026-04-22):

- **Phase 0** closed — acceptance file `examples/2026-04-17-ppn-track4c.prologos` (commit `d28e6029`)
- **Phase 1 structural enforcement infrastructure** — 1a (lint + baseline), 1b (merge-fn-registry scaffolding), 1c (`#:domain` + cell-domains CHAMP), 1d-A/B/C/D/E (registration sweep), 1.5 (srcloc infrastructure) — commits `d28e6029` through `99d9acad`. Mini-design audit codified as Stage 4 Step 1 in DESIGN_METHODOLOGY.org.
- **Phase 9+10+11 Addendum Stage 1/2/3 design arc** — research doc (1000 lines, `de357aa1`), audit doc (800 lines, `62ce9f83`+`28208613`), D.1→D.2→D.3 design doc (scope revised per Phase 1A mini-audit finding). Lens S + Progress Tracker Placement codified.
- **Phase 1A-i** (dead code retirement) — 29 lines deleted across 2 files (commit `5cf9a262`); 85 tests pass; acceptance file clean
- **Phase 1A-ii-a** (mult/level/session cell migration to tagged-cell-value) — 25 insertions at elaborator-network.rkt:921/995/1011 (commit `7052f590`); 111 targeted tests pass
- **`'mult` SRE registration follow-up** — extended `register/minimal` with `#:contradicts?` kwarg + registered 'mult domain (commits `8b85b28a`, `dc2d2fca`); enables Phase 13 progressive classification ratchet
- **1A-iii mini-design** — Z-wide scope + Framing C (PU refactor + hasse-registry) resolved (commit `21b52027`); pre-0 behavioral probe baseline captured (commit `329d4f30`, 28 expressions, 0 errors)
- **Path T-3 mini-design** — set-union merge + Role A/B decomplection + `type-unify-or-top` helper design captured in D.3 §7.6 via conversational dialogue (no separate Stage 1-3 artifact per user direction). Commits `9c3172e0` (design) + `97897c4c` (dailies)
- **T-3 Stage 2 audit + Stage 3 design** — 4 Role B sites + 8 Role A sites + 2 SRE dispatch tables classified; subtype-lattice-merge identified as template; Role B migration pattern + two-commit ordering specified (commit `6fddc5f7`)
- **T-3 Commit A DELIVERED** — `type-unify-or-top` helper + 4 Role B site migrations in elaborator-network.rkt (commit `37aaba2b`). Zero semantic change: probe diff = 0, 129 targeted tests pass

### Suite state

- Full suite NOT run this interval (would be post-Commit-B regression check)
- Probe baseline (`data/probes/2026-04-22-1A-iii-baseline.txt`): 28 expressions, 0 errors — reference for all T-3 work
- Targeted tests green at post-Commit-A state (129 across multiple runs; 89 in separate runs during Commit B diagnostic)

### In progress

- **T-3 Commit B PAUSED** — set-union fallthrough regressed `test-union-types.rkt:234`; reverted; third accidentally-load-bearing mechanism identified at typing-propagators.rkt:1907/1919 (expr-union writes component types instead of `[Type lv]`, relies on merge→type-top→sexp-infer fallback for correct `[Type 0]` result)
- **T-3 T3-C3 systematic re-audit** — NEXT TASK per D.3 §7.6.13 enhanced audit criteria

### Pending next-steps

1. **T-3 T3-C3 re-audit** (immediate):
   - Audit contradiction-detection-as-fallback sites downstream of cells using `type-lattice-merge`
   - Audit `type-lattice-contradicts?` / `net-contradiction?` consumers
   - Fix `typing-propagators.rkt:1878-1920` expr-union writes (architectural correctness: write `[Type lv]` via `infer-level`)
   - Audit other expr-foo typing patterns for similar issues
   - Migrate newly-found Role B sites (likely Commit A extension / Commit A.2)
2. **T-3 Commit B retry** after audit closure:
   - Set-union fallthrough: `type-lattice-merge` final `[else type-top]` → `(or (try-unify-pure a b) (build-union-type-with-absorption (list a b)))`
   - Update 5 assertions in `tests/test-type-lattice.rkt` (lines 39, 72, 85, 188, 255)
   - Validate: probe diff = 0, test-union-types:234 passes, full suite regression check
3. **Post-T-3**:
   - **T-1** speculation mechanism consolidation (with-speculative-rollback simplification; many sites become unnecessary post-T-3)
   - **T-2** Map open-world realignment at typing-core.rkt:1196-1217 (build-union-type becomes redundant OR migrate to `_` open-world)
   - **1A-iii-a-wide Step 2** PU refactor (4 per-domain universes + shared hasse-registry + elab-meta-read/write API)
   - **1A-iii-b** Tier 2 atms cleanup (independent of Path T)
   - **1A-iii-c** Tier 3 surface ATMS AST retirement (14-file pipeline, independent of Path T)
4. **Phase 1B** (tropical fuel primitive), **1C** (canonical BSP fuel instance), **1V** — after Path T resolves
5. **Phase 2A/2B/2V** (orchestration unification — stratum handler registrations)
6. **Phase 3A/3B/3C/3V** (union types via ATMS + hypercube)
7. **Phase V** capstone + PIR

### Watching

- **Three accidentally-load-bearing findings as a confirmed pattern** (attempt 1 / Sub-A / Commit B, all in this addendum track). **Promotion candidate** after T3-C3 completes: principle for `DEVELOPMENT_LESSONS.org` — "Contradiction-detection-as-fallback is a hidden Role B pattern. Stage 2 audits must check downstream dependencies, not just inline predicates." Methodology update candidate for CRITIQUE_METHODOLOGY.org Lens R (Reality-check): enumerate downstream consumers, not just direct call sites.
- **PU heuristic codification candidate** — 5+ instances of N-separate cells converging to N→1 compound (decisions-state, commitments-state, scope-cell, attribute-map, worldview-cache, now per-meta universe). Lens S sub-question or design-methodology rule worth codifying: "when creating large amounts of cells, ask if this is a PU target." Waiting for 1A-iii-a-wide Step 2 to land as 6th data point before promoting.
- **"Retired" claim post-audit verification pattern** — BSP-LE Track 2 PIR's `current-speculation-stack` overclaim was 3rd instance (PPN Track 2B, BSP-LE Track 2, BSP-LE Track 2B). 3+ instances confirm pattern; promotion candidate: "Verify retirement claims by checking dispatch paths, not just parameterize usage."
- **Mini-design in current design doc (not separate artifact)** — user direction 2026-04-22 for T-3. Watching if productive vs under-documented. One data point so far; need 2-3 more before evaluating the pattern.
- **Per-commit dailies discipline** — started 2026-04-22 session. 10+ commits this session arc, each with corresponding dailies entry. Watching if it sticks across multi-session tracks (sustains into Apr 22 follow-ups); if sustained, promote to Stage 4 per-phase completion protocol.
- **Multi-hypothesis parallel diagnostic** (user teaching 2026-04-22) — applied successfully in Commit B regression (reverted only fallthrough to isolate source, 1-2 cycles vs 3+). 1 data point; watching for confirmation across tracks before codifying.

### Lessons distillation check

Scanned 2026-04-16 BSP-LE 2B PIR + past 4 dailies (2026-04-10, 2026-04-12, 2026-04-17, 2026-04-19 which spans 2026-04-19/21/22) for codification candidates:

- **Merge ≠ Unify** (T-3 finding) — lattice join (accumulate via set-union) and equality check (unify, produce contradiction on mismatch) are semantically opposite; API conflation is bug source. Candidate for DESIGN_PRINCIPLES.org or DEVELOPMENT_LESSONS.org. **Hold** — codify after T-3 lands (Commit B) as the track's central lesson.
- **Substrate conflation worse than under-reuse** (T-1 finding) — applying BSP-LE 2B branch-exploration machinery to try-rollback semantics added complexity without benefit, hid the real mechanism, later broke in confusing ways. Candidate for DEVELOPMENT_LESSONS.org. **Hold** — codify after T-1 lands (validates the finding).
- **Implementation ordering as load-bearing principle** (T-3 two-commit ordering) — ordering can be architectural, not just procedural. Commit A before Commit B prevents silent bugs across Role B sites. Candidate for DESIGN_METHODOLOGY.org Stage 4. **Hold** — codify after T-3 lands.
- **Three accidentally-load-bearing findings pattern** (see Watching). **Hold** — T3-C3 is the response; codify after.

No new entries graduated to principles this interval. All four candidates are tied to T-3 completion; codify after Commit B lands.

### Longitudinal pattern tracking

Tracking patterns with ≥3 data points toward codification:

| Pattern | Data points | Next check |
|---|---|---|
| Accidentally-load-bearing mechanism | 3 (attempt-1, Sub-A, Commit B) | Post-T3-C3 — codify |
| PU heuristic (N→1 compound) | 5+ (decisions, commitments, scope, attribute-map, worldview-cache, per-meta) | Post-1A-iii-a-wide Step 2 — codify |
| "Retired" claim overclaim | 3+ (PPN 2B, BSP-LE 2, BSP-LE 2B) | Post-T-3 — codify |
| Mini-design in existing doc | 1 (T-3) | Observation period |
| Per-commit dailies discipline | 1 session arc | Observation period |
| Multi-hypothesis parallel diagnostic | 1 application | Observation period |

## Session log

### 2026-04-22 (continuation) session — T3-C3 re-audit context restoration

**Session start**: continuation from 2026-04-22 pre-handoff session. Context restoration via HANDOFF_PROTOCOL.org Hot-Load Protocol.

**Hot-loaded** (full):
- §2.0 `HANDOFF_PROTOCOL.org`
- §2.1 Always-load: `CLAUDE.md` + `CLAUDE.local.md`, `MEMORY.md` (via system context), `DESIGN_METHODOLOGY.org`, `DESIGN_PRINCIPLES.org`, `CRITIQUE_METHODOLOGY.org`
- §2.2 Rules (via system context): on-network, structural-thinking, propagator-design, workflow, testing, pipeline, stratification, prologos-syntax
- §2.3 D.3 design doc (`docs/tracking/2026-04-21_PPN_4C_PHASE_9_DESIGN.md`) — including critical new §7.6.11/§7.6.12/§7.6.13
- §2.4 Prior art: `subtype-predicate.rkt` (subtype-lattice-merge template), `union-types.rkt` (build-union-type)
- §2.5 Type lattice + related: `type-lattice.rkt` (incl. `type-unify-or-top` helper), `elaborator-network.rkt` (4 Role B sites + cell factories 917/995/1011), `typing-propagators.rkt:1850-1950` (expr-union typing including architecturally-wrong site 1907/1919), `typing-core.rkt:440-462` (expr-union sexp fallback) + 1196-1217 (map-assoc with with-speculative-rollback)
- §2.5 Tests: `test-type-lattice.rkt` (full, 5 assertions to update at lines 39, 72, 85, 188, 255), `test-union-types.rkt:220-240` (the canary at line 234)
- §2.6 Probe: `examples/2026-04-22-1A-iii-probe.prologos` + `data/probes/2026-04-22-1A-iii-baseline.txt`
- §2.7 Dailies: `2026-04-19_dailies.md` (including 2026-04-21 and 2026-04-22 session arcs); prior handoff (`2026-04-21_PPN_4C_PHASE_1A_HANDOFF.md`)
- Propagator.rkt fallback paths: `net-cell-read:991` (TMS fallback) + `net-cell-write:1248` (TMS fallback — THE 1st accidentally-load-bearing site)

**Git state verified**: `main @ f42434ea`, working tree clean (only benchmark data + `.lsp/` artifacts modified, not touching production). Matches handoff exactly.

**Understanding summarized to user**: Session state, 3-accidentally-load-bearing pattern, settled decisions (Path T, T-3 Role A/B decomplection, Commit A delivered, Commit B paused), typing-propagators.rkt:1907/1919 architectural error identified, T3-C3 plan per §7.6.13 enhanced criteria.

**Ready for T3-C3 re-audit execution**: awaiting user go-ahead on re-audit execution plan.

### Open next working day interval (this transition)

User requested fresh working day interval at session continuation boundary. Prior dailies (`2026-04-19_dailies.md`) closed with T-3 arc captured. New standup `standup-2026-04-22.org` and dailies `2026-04-22_dailies.md` created concurrently per workflow rule.

### PPN Track 4D proposed + vision research (commit `4812892c`)

Draft research note at `docs/research/2026-04-22_ATTRIBUTE_GRAMMAR_UNIFICATION_VISION.md` (~300 lines) proposing new PPN track 4D: Attribute Grammar Substrate Unification. Motivated by the T-3 three-accidentally-load-bearing pattern as structural fingerprint of sources-of-truth fragmentation across typing/elaboration/reduction subsystems (7+ locations: syntax.rkt + typing-core.rkt + typing-propagators.rkt + elaborator-network.rkt + zonk.rkt + unify.rkt + reduction.rkt). Thesis: collapse into single declarative attribute-grammar substrate where each typing rule is a grammar production with attribute-equations compiled to propagator installations. The propagator network IS the attribute grammar runtime.

Preliminary phase structure (A grammar rule representation, B compiler, C migration, D sexp-infer retirement, E unification consolidation, F zonking as readiness stratum, G reduction as :whnf facet). Scope comparable to PPN 4C (multi-month). Prerequisites: 4C complete + T-3 landed + PM Track 12 scoping decided. Prerequisite to Track 7 (CAPSTONE user-defined grammar extensions).

Updated PPN Master (`2026-03-26_PPN_MASTER.md`) Progress Tracker + Track Details section with 4D entry; MASTER_ROADMAP.org linked.

Principle-to-codify candidate (post-T-3): "Fragmentation of sources of truth is architectural debt; accidentally-load-bearing patterns are its fingerprint. Structural fix is unification under one substrate, not more-careful-migration."

### T3-C3 full re-audit executed + classified (2026-04-22)

Per D.3 §7.6.13 enhanced audit criteria. Findings persisted in D.3 §7.6.14.

**Q3 C3 full grep classification** (every `(type-top? ...)` consumer):
- **Category A** (already migrated in Commit A, verified): 4 Role B sites
- **Category B** (NEW Role B sites, §7.6.9 missed these): 5 sites — B1/B2 write-then-check pattern (typing-propagators.rkt app fire + expr-ann); B3/B4/B5 cell merge-fn pattern (classify-inhabit, cap-type-bridge, session-type-bridge)
- **Category C** (architectural error): 1 site — typing-propagators.rkt:1878-1920 expr-union install writing component types with misplaced INFER-time branching
- **Category L** (legitimate, no change): reconstructor propagators, readiness checks, lattice internals, defense code, root fallback gate, tensor result check

**Q2 install-caller audit**: install-typing-network has ONE production caller (line 2220, top-level infer). No check-time invocation. Phase 8 Option D branching at INFER time is misplaced. Confirmed Option A2 (remove branching).

**Q1 M2 centralized helper** — `type-map-write-unified` to be added in A.2-b.
**Q5** confirmed Role B for classify-inhabit classifier×classifier (equality enforcement).
**Q6** ordering at discretion.

### T-3 Commit A.2-a DELIVERED (commit `a5a33a71`)

Architectural fix for Category C expr-union:
- Added `make-union-fire-fn` in typing-propagators.rkt (parallels make-pi-fire-fn; reads left/right :type, writes `(expr-Type (lmax ...))`)
- Replaced expr-union install case (1878-1920): install sub-expressions, install fire function with component-paths; removed worldview-bitmask branching, promote-cell-to-tagged, assumption counter, type-map-write of component types
- Removed dead `union-assumption-counter` parameter
- Removed dead atms.rkt imports (`assumption-id`, `assumption-id-n`, `solver-state-assume`, `solver-state?`)

Validation:
- Probe diff = 0 (baseline matches exactly; p3-my-map still `(Map Keyword Int | String)`, `(infer)` paths unchanged)
- 147 targeted tests pass (test-union-types, test-type-lattice, test-ppn-track4, test-sre-coverage, test-mixed-map, test-elaborator-network)
- Correct under BOTH current merge semantics AND post-Commit-B set-union (standalone-safe; independent of T-3 ordering)

Design rationale captured in commit message + D.3 §7.6.14.

Lessons this step:
- **Architectural fix was transparent** under current merge semantics — zero behavior change. This validates the fix as correct-by-architecture (it was already wrong; fixing it doesn't break anything because the wrong path was being routed around by contradiction-fallback).
- **make-pi-fire-fn was the template** — the parallelism was there all along, expr-union just didn't use it. Reveals the attribute-grammar vision: each typing rule SHOULD be this structurally-similar declarative shape.
- **Dead scaffolding removal** is clean: union-assumption-counter + atms.rkt imports all drop out cleanly once the branching code is removed. Single architectural move with multiple cleanup benefits.

### T-3 Commit A.2-b DELIVERED (commit `f85dd50a`)

Centralized Role B equality-enforcement write helper + B1/B2 migrations:
- `type-map-write-unified net tm-cid position expected` — Role B primitive paralleling `type-map-write` (Role A). Uses `type-unify-or-top` internally.
- Exported from typing-propagators.rkt.
- B1 migration (typing-propagators.rkt:1179-1182): app fire's arg-pos write (expected domain) — preserves type-top contradiction signal under post-T-3 merge.
- B2 migration (typing-propagators.rkt:1962): expr-ann's term-position write — preserves type-top for contradiction-detection propagator.

Validation: 154 targeted tests pass; probe semantic output identical to baseline.

### T-3 Commit A.2-c DELIVERED (commit `105bcdae`)

Role B cell merge-fn swaps — 3 cells whose Role-A-in-disguise merge-fn would under set-union silently union incompatible writes instead of signaling contradictions:
- B3 `classify-inhabit.rkt:163` classifier × classifier merge (inside merge-classify-inhabit)
- B4 `cap-type-bridge.rkt:191` function-type cell
- B5 `session-type-bridge.rkt:115/124` Send/Recv message-type cells

All swap `type-lattice-merge` → `type-unify-or-top`. Q5 confirmed classify-inhabit as Role B.

Validation: 242 targeted tests across 11 files pass (union-types, type-lattice, mixed-map, elaborator-network, sre-coverage, facet-tag-dispatch, session-type-bridge-01, session-runtime-01, capability-01/04/05).

### T-3 Commit B DELIVERED (commit `e07b809f`) — THE MERGE SEMANTICS CHANGE

**The goal of T-3.** `type-lattice-merge` gains set-union Role A semantics: structurally-incompatible concrete types produce the union (via `build-union-type`), not `type-top`. Mirrors SRE Track 2H's `subtype-lattice-merge` applied to the equality relation. `type-top` now reserved for REAL contradictions (explicit annotation violations, Role B callers via `type-unify-or-top`).

Three additional findings surfaced during Commit B integration:

**B6 — Type meta cells (NEW finding)**: `elab-fresh-meta` at elaborator-network.rkt:117 and `identify-sub-cell` at :347/350/353 (3 sub-cell variants) use `type-lattice-merge` as merge-fn. Under set-union, incompatible meta writes would silently union instead of contradicting — but type metas represent ONE type (equality semantics). Migrated to `type-unify-or-top`. This was audit gap — first detected when test-elaborator-network.rkt's contradictory-values test failed during Commit B. Added to D.3 §7.6.14 as audit-complete post-hoc finding.

**Meta resolution in fallthrough**: when one side is a solved meta (via Phase E1 callback), the fallthrough branch preserved raw meta reference instead of the resolution. Fix: resolve top-level solved metas before building union. Test at line 279 now correctly expects `(Bool | Nat)` (resolved) instead of `(Nat | ?a)` (raw).

**Distributivity F7 DISPROVEN**: test-sre-track2h.rkt's distributivity conjecture for meet over subtype-join for Pi types no longer holds under set-union merge. Counterexample: `a=Pi mw Nat Bool`, `b=a`, `c=Pi mw Int Bool`. Under pre-T-3, meet(a,c)=bot (dom merge→top); under post-T-3, meet(a,c)=Pi mw (Int|Nat) Bool. The rhs then hits the pre-existing atom-vs-union subtype? limitation, producing a union. Test updated to document this mathematical finding as a regression guard. Commutativity still holds.

**Validation (acceptance criteria met)**:
- Probe (`examples/2026-04-22-1A-iii-probe.prologos`) output identical to baseline (28 expressions match exactly)
- `test-union-types.rkt:234` canary `(infer <Nat | Bool>) = "[Type 0]"` PASSES
- Full suite: 7942 tests, 1 failure (test-facet-sre-registration — passes individually; batch contamination pre-existing, verified unrelated to T-3 via stash test)
- Target tests across T-3 scope (247 tests, 10 files): all GREEN

### T-3 COMPLETE — summary

4 commits, staged A→B ordering (Role B migration BEFORE merge semantics change):

| Commit | Focus | Hash |
|---|---|---|
| A.2-a | Architectural fix (expr-union): make-union-fire-fn + install rewrite + dead scaffolding | `a5a33a71` |
| A.2-b | Centralized helper (`type-map-write-unified`) + B1 (app fire) + B2 (expr-ann) Role B migrations | `f85dd50a` |
| A.2-c | Cell merge-fn swaps: B3 classify-inhabit, B4 cap-type-bridge, B5 session-type-bridge | `105bcdae` |
| B | `type-lattice-merge` set-union fallthrough + B6 (elab-fresh-meta, identify-sub-cell) + meta resolution + 5 test assertion updates + F7 finding | `e07b809f` |

**Net effect**: Role A/B decomplection complete. `type-lattice-merge` = Role A accumulate (set-union); `type-unify-or-top` = Role B equality-enforce (contradiction signal). All code paths that previously relied on merge-produces-top-on-incompat now use explicit Role B semantics.

### Unblocked downstream tracks

- **Path T-1** (speculation mechanism consolidation): now ready. Audit 4 `with-speculative-rollback` callers — set-union merge likely simplifies map-assoc + some Church folds.
- **Path T-2** (Map open-world realignment): `build-union-type` in typing-core.rkt:1196-1217 likely redundant; decide open-world `_` migration per ergonomics.
- **1A-iii-a-wide Step 2** (PU refactor): type cells already Role B (B6); ready for 4 per-domain universes + shared hasse-registry + elab-meta-read/write API.
- **1A-iii-b** (Tier 2 atms cleanup): independent of Path T.
- **1A-iii-c** (Tier 3 surface ATMS AST): independent of Path T.

### Lessons promoted to DEVELOPMENT_LESSONS.org candidacy (post-T-3 codification)

Three watching-list patterns now have sufficient data points for promotion:

1. **Fragmentation of sources of truth is architectural debt; accidentally-load-bearing patterns are its fingerprint** — 4 confirmed data points in T-3 alone (attempt-1 TMS dispatch, Sub-A with-speculative-rollback, Commit B expr-union typing, post-integration B6 for type meta cells). The integration-time B6 finding is the strongest data point: even with systematic re-audit, a site was missed until the merge semantics change actually activated. Structural fix is substrate unification (PPN Track 4D), not more-careful-migration.

2. **Role A vs Role B API decomplection** — the explicit `type-lattice-merge` (accumulate) vs `type-unify-or-top` (equality-enforce) distinction is a recurring pattern: merge functions serve two semantically-opposite roles; conflating them creates the "accidentally-load-bearing" pattern. T-3's decomplection is the template.

3. **Implementation ordering as load-bearing principle** — T-3's strict A→B ordering was architecturally required (not just procedural). Landing B before A would have silently broken Role B sites across the codebase. Ordering matters structurally, not stylistically.

### Watching — new patterns surfaced

- **Mathematical property regressions as lattice redesign outcomes**: F7 distributivity conjecture was disproved by T-3 Commit B's set-union semantics. This is a data point that lattice redesigns can disprove previously-conjectured algebraic properties — not a bug but a finding. Future lattice work should re-verify algebraic conjectures against the new semantics. 1 data point; watching for confirmation.
- **Integration-time audit findings**: B6 was discovered DURING Commit B integration (via test failure), not by upfront grep audit. Suggests Stage 2 audit must include integration-test runs in addition to static grep analysis. 2 data points (B6, B5 was also partially integration-surfaced — the cell merge-fn behavior). Watching.

### Next session / track

T-3 DONE. Re-sequencing (below) dependency-orders remaining Phase 1A work.

### T-1 mini-design → charter re-framing + re-sequencing (2026-04-22)

Opened T-1 (speculation mechanism consolidation) mini-design audit. Initial dialogue surfaced a deeper question: given BSP-LE 2/2B's cell-based TMS substrate for branch-exploration, what need is there for rollback at all?

Traced to the end-state per PPN 4C D.3: **under the fully-on-network target, `with-speculative-rollback` doesn't exist.** Replaced by ATMS-tagged writes + nogood-recording via BSP-LE 2B's tagged-cell-value substrate. Snapshot exists TODAY only to cover off-network state (meta-info CHAMP + constraint store + id-map). Phase 4 of main track + PM Track 12 retire those.

**Charter-alignment observation** (user direction 2026-04-22): T-1 as "clean up with-speculative-rollback" in isolation would have built new `with-transactional-rollback` API — MORE infrastructure preserving the current mechanism indefinitely. Framed against end-state, T-1 becomes "name vestigial bitmask scaffolding + schedule snapshot retirement to Phase 4/PM-12." Much smaller scope.

**Re-sequencing decided**:
- Previous queue: T-1 ‖ T-2 ‖ 1A-iii-a-wide Step 2 (all parallel-unblocked)
- Pivoted queue: **1A-iii-a-wide Step 1 → T-1 → T-2 → Step 2** (dependency-ordered)

Rationale: 1A-iii-a-wide Step 1 IS the addendum's Phase 1 substrate migration charter continuation (per §7.5.3). T-3 resolved its prerequisites. After Step 1, T-1 becomes tractable cleanup (bitmask scaffolding is sound on new substrate; snapshot gets explicit retirement plan). T-2 becomes mechanical verification.

D.3 updated: tracker rows re-sequenced; new §7.5.10 captures the charter-alignment reasoning.

Q-Pivot-3 (user direction): Phase 4 stays as immediate follow-on to addendum, NOT absorbed into addendum scope. Addendum stays focused on substrate + orchestration + features (Phase 1/2/3).

### Codification candidate — tactical-framing-against-end-state

**Proposed principle** (candidate for DEVELOPMENT_LESSONS.org):

> **Tactical cleanup tracks should be framed against end-state architecture, not as local optimizations.** When a tactical task (simplify X, decouple Y) surfaces, first check whether X or Y is a LOCAL view of a LARGER architectural change already planned. If yes, frame the tactical work as a way-station toward the end-state — scaffolding with explicit retirement plans tied to specific follow-on tracks. Designing in isolation produces MORE infrastructure that preserves the current mechanism indefinitely; framing against end-state produces SCAFFOLDING with retirement plans.

**Origin**: T-1 scope dialogue 2026-04-22 during PPN 4C addendum. T-1 as conceived ("simplify with-speculative-rollback") in isolation would have produced a new `with-transactional-rollback` API + new forward-compatible `(speculate label thunk)` form + 6 caller migrations. Framed against the end-state (PPN 4C Phase 4 + Phase 9 + PM 12 retire snapshot entirely), T-1 becomes a scaffolding-labeling exercise tied to named follow-on tracks. Significantly less work, more architecturally honest.

**Connection to existing methodology**: extends DESIGN_METHODOLOGY.org Stage 4 Step 5 Vision Alignment Gate ("vision-advancing?") to Stage 3 design time — ask the VAG questions when DESIGNING scope, not only when CLOSING a phase. Same question; earlier application.

**Status**: 1 data point (T-1 reframing). Watching for 2-3 more before promoting to DEVELOPMENT_LESSONS.org. Candidate locations if promoted: DEVELOPMENT_LESSONS.org (as new lesson) OR DESIGN_METHODOLOGY.org Stage 3 (as new sub-section under Stage 0 design-time VAG).

### Post-re-sequencing queue

1. **1A-iii-a-wide Step 1** (NEXT): type cell migration + TMS retirement + current-speculation-stack retirement per D.3 §7.5.3. Addendum Phase 1 substrate charter completion. Union-inference adaptation ALREADY delivered via T-3 Commit A.2-a.
2. **T-1** (post-Step-1): scaffolding-retirement-plan labeling + verify map-assoc redundancy.
3. **T-2** (post-T-1): Map open-world verification.
4. **1A-iii-a-wide Step 2** (vision-advancing capstone): PU refactor (4 per-domain universes + shared hasse-registry + elab-meta-read/write API) per D.3 §7.5.4.

Then the addendum's remaining phases: 1B (tropical fuel primitive), 1C (canonical BSP instance), 1V, 2, 3, V. Immediate post-addendum: main-track Phase 4 (CHAMP retirement).

### 1A-iii-a-wide Step 1 DELIVERED (2026-04-22)

Phase 1 substrate migration charter complete. 5 atomic sub-phase commits:

| Sub-phase | Commit | Delivery |
|---|---|---|
| S1.a | `3b6aefdb` | elab-fresh-meta → tagged-cell-value + 4th accidentally-load-bearing finding FIX (visibility scope in with-speculative-rollback parameterize — localized, preserves BSP-LE 2/2B clause-propagator isolation) |
| S1.b | `2c8871ec` | Retired 3 TMS fallback branches in propagator.rkt (dead post-S1.a) |
| S1.c | `d220ca51` | Retired TMS API wholesale (~258 lines from propagator.rkt: struct, read/write/commit, commit/retract, merge-fn factory, net-new-tms-cell factory) + pnet-serialize cleanup |
| S1.d | `9f47ffe9` | Retired current-speculation-stack parameter |
| S1.e | `b1468220` | Peripheral cleanup (deleted test-tms-cell.rkt; updated cell-ops.rkt stale comments) |

**Architecturally**: BSP-LE 2/2B's tagged-cell-value is now the sole speculation mechanism for on-network state. All remaining scaffolding (current-worldview-bitmask parameter, elab-net snapshot) has explicit retirement plans tied to PM Track 12 and Phase 4.

**Validation (VAG all 4 questions pass)**: see D.3 §7.5.11. Full suite 7908 tests / 126.7s / 1 pre-existing batch contamination (unrelated). Test count delta -34 matches test-tms-cell.rkt deletion. Probe diff = 0 across all 28 expressions.

**The 4th accidentally-load-bearing finding (significance)**:

S1.a surfaced a latent issue in `with-speculative-rollback`. Pre-S1.a, TMS's empty-stack bypass trivially exposed prior commits regardless of bitmask parameterize. Post-S1.a, the bitmask layer activated and `current-worldview-bitmask` parameterize to just `hyp-bit` OVERRODE worldview-cache entirely (net-cell-read uses per-propagator-takes-priority semantic for BSP-LE 2/2B clause isolation) — losing visibility of prior commits. Back-to-back map-assoc speculations broke.

**Fix**: parameterize `current-worldview-bitmask` to the FULL worldview (outer-active | worldview-cache | hyp-bit) instead of just hyp-bit. Localized to `with-speculative-rollback`; preserves clause-propagator isolation for global net-cell-read.

**Pattern is now at 4 data points** (attempt-1 → Sub-A → Commit B → S1.a). Critical: S1.a and the earlier B6 (T-3 Commit B integration) were both surfaced by INTEGRATION TEST failures, not static audit. This confirms a stronger codification candidate:

> *Accidentally-load-bearing mechanisms are often surfaced by integration-test behavior, not static audit.* Stage 2 audits that grep for inline predicates (e.g., `(type-top? ...)`, `(tms-cell-value? ...)`) catch SOME sites. They miss sites where a mechanism's BEHAVIOR — not its obvious API — is load-bearing downstream. Implication: Stage 2 audits for API migrations must include integration-test runs of realistic workloads, not just static site enumeration.

### Codification updates to watching list

**Promoted pattern** (4 data points, integration-test-surfaced):
- **Accidentally-load-bearing via behavioral dispatch** — promoted from 3 data points to 4. Two of those (B6, S1.a) were integration-test-surfaced; the other two (attempt-1, Sub-A, Commit B) were mid-migration or static. The integration-test aspect is now the dominant codification recommendation.

**Watching list updates** (from earlier):
- Integration-time audit findings (was 2 data points): **now 3** (B6, S1.a, also visible in attempt-1's original regression). Converging on promotion.
- Role A/B decomplection pattern: 1 more data point (visibility scope in with-speculative-rollback is another Role A/B-adjacent distinction — "all current bits" vs "this hypothesis only"). Still watching.

### Next (per queue)

**T-1**: scaffolding-retirement-plan labeling. Much reduced scope post-Step-1:
- Verify map-assoc's `with-speculative-rollback` becomes redundant under T-3 set-union merge (set-union handles type-incompatibility without speculation needed)
- Name `elab-net` snapshot as scaffolding with retirement tied to Phase 4 (meta-info CHAMP migration) + PM Track 12 (constraint store + id-map)
- Any residual mechanism simplification

Expected: mostly audit + documentation, minimal code changes.

### T-1 DELIVERED (2026-04-22)

Documentation-only pass per charter alignment. Single commit covering 3 files:

1. **`elab-speculation-bridge.rkt`**: completely restructured module docstring. Added "ARCHITECTURAL STATE (post-Step-1)" section explicitly distinguishing ON-NETWORK substrate (worldview tagging via tagged-cell-value) from SCAFFOLDING (elab-net snapshot for off-network residue). Inline comments in `with-speculative-rollback` body restructured into 3 labeled stages (Stage 1 scaffolding snapshot, Stage 2 on-network worldview-cache write, Stage 3 thunk with full-worldview bitmask). Cleaned stale Phase 11 retirement-journey comments (were describing completed state as in-progress). Added cross-references to D.3 §7.5.10 + §7.5.11 + PM Track 12 master. Comment at `current-speculation-stack` re-export removed (parameter retired in S1.d).

2. **PM Master `2026-03-13_PROPAGATOR_MIGRATION_MASTER.md`**: added "PPN 4C Phase 1A-iii-a-wide Step 1 + T-1 (2026-04-22) — `with-speculative-rollback` retirement handoff" section. Specifies:
   - Off-network stores to migrate (meta-info CHAMP → Phase 4; constraint store + id-map → PM 12)
   - PM 12 light cleanup sub-phase: 6 caller migrations to new `speculate` form + mechanism retirement
   - Expected scope: ~20-30 min mechanical sub-phase
   - Caller sites pre-identified: typing-core.rkt:1205/1291/1325/2439, typing-errors.rkt:78, qtt.rkt:2425
   - Replacement API shape: `(speculate label thunk [#:success? success?])` — observationally equivalent once off-network residue gone
   - Updates "Track 12 now has THREE complementary unlocks" summary (was TWO)

3. **`DEFERRED.md`**: added "PM Track 12 design input from PPN 4C Phase 1A-iii-a-wide Step 1 + T-1" entry under "Off-Network Registry Scaffolding" section. Links PM Master detail + summarizes the light cleanup mechanics.

**No code changes**: post-Step-1, the 5 non-C1 callers are architecturally clean (Step 1's visibility fix made the bitmask layer sound). C1 (map-assoc) redundancy is T-2 scope per §7.5.10. No residual simplifications surfaced.

**Validation**:
- elab-speculation-bridge.rkt compiles clean
- 103 targeted tests GREEN across 4 files (speculation-bridge, mixed-map, union-types, elaborator-network)

**Architectural outcome**:
- `with-speculative-rollback`'s internal composition is now explicitly labeled: on-network substrate (permanent) vs off-network scaffolding (Phase 4 + PM 12 retirement)
- PM Track 12's scope formally includes the retirement sub-phase
- Developer reading the code can immediately see what retires when
- Future PM 12 implementer has a pre-scoped sub-phase plan (not a "figure out how to retire with-speculative-rollback" task)

### Next (per queue)

**T-2**: Map type inference open-world realignment. Verify T-3 set-union merge makes typing-core.rkt:1196-1217's explicit `build-union-type` redundant; decide on `_` open-world per ergonomics. Mostly mechanical post-Step-1.

### Process detour — mempalace Phase 1 evaluation + Phase 2 adoption (2026-04-22, commit `06d4ab6c`)

User proposed evaluating [mempalace](https://github.com/MemPalace/mempalace) as a potential mitigation for the growing handoff-protocol context tax. Ran a structured Phase 1 evaluation before any integration commitments:

**Setup**: `uv tool install mempalace`, `mempalace init /Users/avanti/dev/projects/prologos --yes --lang en`, `mempalace mine ./docs --wing prologos`. Mine ran ~42 min, indexed 23,497 drawers from 417 files (91% of 25,873 before interruption; more than enough for evaluation). Palace: 185MB ChromaDB + HNSW index.

**Phase 1 scorecard** (12 topical queries + 3 recency stress tests + 2 extras, 17 total):
- 12/17 scored ≥4/5 (top-3 canonical hits). 5/17 scored 5/5.
- Standouts: Q10 "map open world _ value type ergonomics" → D.3 §7.6.7 "Implications for T-2 (Map open-world)" as top-1 (the EXACT section needed for the upcoming T-2 mini-design). Q7 Hyperlattice Conjecture, Q5 charter-alignment, Q9 Module Theory Realization B, Q12 Track 4D — all 5/5.
- Exact-text retrieval works: query on commit hash `3b6aefdb` returned the dailies row listing that hash. Useful for "what commit was X?" questions.
- Specific technical terms retrieve cleanly; overloaded terms cause drift (Q2 "accidentally load bearing" polluted by "load-bearing *principles*" mentions).

**Real failure mode — F7 canary**: query "F7 distributivity" returns the pre-T-3 conjecture-holds claims (SRE Track 2H D.1 + Track 2H PIR §7 "we got lucky") ahead of the 2026-04-22 disproof in dailies. Semantic search has no recency weighting — verbose stale discussion outranks terse recent update. This is a real hazard for "current state of X" queries.

**Temporal graph investigated and rejected as mitigation**: mempalace ships a bitemporal triple store (`knowledge_graph.py` with `valid_from`/`valid_to`, API `add_triple` + `invalidate` + `query_entity(as_of=...)`). BUT `miner.py` has zero `add_triple` calls — `~/.mempalace/knowledge_graph.sqlite3` does not exist after a full mine. The KG is an empty shell unless facts are asserted manually. That duplicates what dailies + PIR discipline already does; don't bother unless a compelling use emerges.

**Phase 2 decision** (user direction): proceed with MCP-only integration + explicit guardrails. NO Stop/PreCompact hooks (prompt-injection path). NO JSONL mining (would pollute with discarded conversation content). Cross-check every result against current dailies/handoff/design-doc before acting.

**Committed (commit `06d4ab6c`)**:
- `.mcp.json` — registers mempalace MCP server via `uv tool run --from mempalace`
- `.claude/rules/mempalace.md` (133 lines) — full guardrails + F7 canary + cross-check discipline + success criteria + uninstall path
- `CLAUDE.md` — rules line extended
- `.gitignore` — `mempalace.yaml` + `entities.json` (auto-added by `mempalace init`)

Activates on next Claude Code session start after hot-load + restart.

### Process update — HANDOFF_PROTOCOL.org Always-Load extended (2026-04-22)

User surfaced a meta-pain: "context of our project has grown quite a bit, and each session loses the context of everything it has done before — constantly needing to point to prior art, repeat design principles and restate overarching visions. The roadmap + series masters are index attempts but I don't think we're doing everything we could with these pieces."

Specific ask: should MASTER_ROADMAP.org be in the handoff protocol hot-load? **Yes.** Added to `HANDOFF_PROTOCOL.org` §2.1 Always-Load (position 5b), plus a new position 5c "*current series master*" directing per-handoff to name which series master applies to the active track. Also added `CRITIQUE_METHODOLOGY.org` to Always-Load (was implicit via Stage 3 references; now explicit).

Websearch ([AI coding agent persistent memory MCP markdown](https://github.com/zilliztech/memsearch), [basic-memory](https://github.com/basicmachines-co/basic-memory), [Claude Code memory docs](https://code.claude.com/docs/en/memory), [claude-memory-bank](https://github.com/russbeye/claude-memory-bank), [builder.io CLAUDE.md guide](https://www.builder.io/blog/claude-md-guide)) confirmed our existing practices ARE the dominant pattern: markdown-first persistent memory, curated MEMORY.md, daily session logs, layered CLAUDE.md + per-dir rules, `@path/to/file` imports. We're not reinventing — we're evolving.

Missing piece identified but NOT built in this session: **a concept-map document** (`docs/tracking/principles/CONCEPT_MAP.org` as a potential home) tracing "principle → tracks that embodied it → PIR lessons → refinements in later tracks." PIRs' §16 longitudinal surveys have fragments; DEVELOPMENT_LESSONS.org accumulates; no single doc stitches the arc. Evaluate producing one after Phase 2 mempalace data arrives — it might eliminate some of the repetition the user feels.

Alternative tool noted (NOT adopted): `basic-memory` (basicmachines-co) is markdown-first + MCP + local knowledge graph from wiki-links. Philosophy "files are source of truth" aligns with ours better than mempalace's verbatim vector approach. Complementary, not replacement; don't add a second memory system before Phase 2 mempalace is evaluated in real use.

**User's firm constraint**: no prompt injection. All explored alternatives respect this; MCP-only is the architectural gate.

### Next (per queue, post-process-detour)

**T-2**: unchanged. Map type inference open-world realignment. On next session, try `mcp__mempalace__search "Map open world value type ergonomics"` before re-reading D.3 §7.6 — Phase 1 eval indicated it returns §7.6.7 as top-1.

---

## Session continuation — 2026-04-23 (mempalace Phase 2 live; T-2 execution)

Continuation session after compaction. Hot-loaded all §2 documents per
HANDOFF_PROTOCOL.org. Summarized understanding to user.

### mempalace Phase 2 first-use (positive data point)

First real use of `mcp__mempalace__mempalace_search` during T-2 audit.
Two searches:
- `"Map open world value type ergonomics narrow unions"` → returned
  D.3 §7.6.7 T-2 implications as top-1 (matches Phase 1 eval prediction)
- `"heterogeneous map value type Any top wildcard"` → returned
  `2026-03-20_COLLECTION_INTERFACE_AUDIT.md` §7 and
  `2026-03-20_COLLECTION_INTERFACE_UNIFICATION_DESIGN.md` §8 as top
  hits

**The second search was the load-bearing one** — it surfaced prior CIU §8
D7 which EXPLICITLY considered three options (narrow union / dynamic `_` /
schema-first) and recommended Option C (schema-first). This prior art
would have taken substantially longer to locate via manual grep — mempalace's
semantic retrieval compressed it to one query. This is the "semantic grep
over our docs" use case Phase 2 is testing. Success.

Cross-checked both hits against current D.3 + dailies per guardrail. No
recency conflict (both hits align with current T-2 framing). User
consciously overrode CIU D7 with eyes open (D1 decision).

### T-2 execution — 3 staged commits

Architectural decisions (dialogue 2026-04-23):
- **D1**: Override 2026-03-20 CIU §8 D7. CIU was a draft note, not a
  design commitment; the vision has always been Clojure-ergonomic Maps.
  Schema still available for structured validation when needed.
- **D2**: α-semantic for Open (Open = universal type, compatible in
  both directions, never fails, never solves). Not β `unknown`-style
  (would kill ergonomics), not γ per-access-freshening (surprising
  semantics users won't intuit).
- **Display decision**: name is `Open` (Framing 3). User reasoning: `Any`
  carries the weight of a user-writable TypeScript-style type; `Open`
  is distinct enough to position as "Open by Design" in docs and
  signals both ease of use and careful scope. **Not user-writable.**

Motto coined: **"The Pragmatic Prover"** — schema/spec for verification;
Open for ergonomics. Clojure ergonomics within a dependently-typed language.

3 commits:
- `4bfbd141` — Commit 1/3: `expr-Open` AST + pipeline integration (7 files:
  syntax/substitution/zonk/reduction/pretty-print/pnet-serialize/unify)
- `246d4c2e` — Commit 2/3: typing semantics + map-op Open cases +
  map-assoc speculation retirement (typing-core + qtt)
- `07fda438` — Commit 3/3: elaborator `surf-map-literal` emits Open +
  test-mixed-map rewrite (21→25) + test-path-expressions update +
  probe baseline refresh
- `bb058491` — D.3 §7.6.15 + tracker update (T-2 DONE)

### Validation (pre-commit)

- Full suite: **7912 tests / 118.4s / 0 failures** (pre-existing
  test-facet-sre-registration batch contamination cleared as side-effect)
- Probe clean; new baseline captured
- Acceptance file (`examples/2026-04-17-ppn-track4c.prologos`): 0 errors

### Perf deltas (probe)

| Metric | Pre-T-2 | Post-T-2 | Delta |
|---|---|---|---|
| `speculation_count` | 12 | 0 | **-100%** (complete retirement for map ops) |
| `atms_hypothesis_count` | 17 | 5 | -70% |
| `infer_steps` | 73 | 55 | -24% |
| `meta_created` | 19 | 16 | -16% |
| `reduce_steps` | 339 | 315 | -7% |
| Suite time | 126.7s | 118.4s | -6.5% |

### Scope boundaries honored

- **Other 5 `with-speculative-rollback` sites untouched** (union-read
  disambiguation, error enrichment). Scheduled for PM 12 light-cleanup
  sub-phase per T-1 PM Master handoff.
- **Narrow-union annotation preserved** (`(Map K <A | B>)` continues to
  check literal values against union components — Concern B).
- **Annotated concrete narrow types preserved** (`(Map K Int)` strict —
  no silent widening).
- **Schema system untouched** — field-level narrowing works as before.
- **No on-network changes** — typing-propagators.rkt not modified; SRE
  ctor-desc decomposition handles `expr-Open` transparently.

### Pipeline reality vs pipeline.md staleness

Real files touched for `expr-Open` addition:
- syntax.rkt, substitution.rkt, zonk.rkt, reduction.rkt, pretty-print.rkt,
  pnet-serialize.rkt, unify.rkt, typing-core.rkt, qtt.rkt, elaborator.rkt = **10**

Files `pipeline.md` "New AST Node" checklist says to update:
- 14 (or 11 explicit + 3 "possibly")

NOT needed for expr-Open (internal-only, no user surface syntax):
- surface-syntax.rkt, parser.rkt (Open has no reader/parser entry)
- macros.rkt, foreign.rkt (no macro or FFI handling)
- typing-propagators.rkt (SRE ctor-desc handles Open component generically)

MISSING from pipeline.md checklist (realities of post-PM-10 pipeline):
- pnet-serialize.rkt (needed for every new AST node to participate in
  module caching)

Updating pipeline.md as follow-up.

### Codification candidates (T-2 surfaces one new, confirms another)

1. **"Retire narrow-union default maps" as ergonomic-correct replacement
   for the accidentally-load-bearing-mechanism fingerprint** (NEW,
   T-2 is application of the pattern): when a feature produces repeated
   accidentally-load-bearing findings, the ergonomic-correct replacement
   is often simpler than more-careful-migration. T-2 retired the
   narrow-union-widening pathway structurally; the pattern stopped
   producing findings there. Watching for 1-2 more instances.
2. **mempalace semantic search for prior-art retrieval** (Phase 2 first
   success data point): the "what did we decide before about X?" query
   is where mempalace earns its keep. CIU §8 D7 would have been hard
   to find via grep. Watching for more data points; don't over-rely on
   a single successful retrieval.
3. **Narrow-type-expression construction ≠ cell merge** (from T-2
   audit): T-3's set-union merge at the cell level did NOT make
   map-assoc's explicit `build-union-type` redundant because that
   operation constructs a TYPE EXPRESSION (not a cell value). Similar
   distinctions may exist elsewhere. When auditing whether a change
   "subsumes" existing code, verify the operation's level (cell-merge
   vs type-expression construction vs elaboration vs user surface).

### Updates to watching list

**Promoted** (watching → codification candidate):
- 2nd instance of integration-test-surfaced-findings (was 3 at end of
  2026-04-22; now T-2 surfaced no new integration-test-only findings
  but confirmed the pattern by its absence — when the substrate is
  clean, integration tests don't surface hidden dependencies).

**Remaining watching**:
- Per-commit dailies discipline: 2 session arcs now (2026-04-22 +
  2026-04-23 continuation). Sustaining. Watching for 2 more.
- Mini-design in existing design doc: T-3 + T-2 both used the pattern
  successfully. 2 data points; 1 more needed for codification.

### Next (per queue)

**T-2 DONE**. Next in sequence per §7.5.10 charter-alignment:

1. **1A-iii-a-wide Step 2** — PU refactor (4 per-domain universes +
   shared hasse-registry + `elab-meta-read/write` API). Vision-advancing
   capstone of Phase 1A per D.3 §7.5.4. Significantly larger scope
   than Step 1 or T-2.
2. **1A-iii-b** — Tier 2 `atms` struct + `atms-believed` retirement
   (independent of Path T; can parallel with Step 2)
3. **1A-iii-c** — Tier 3 surface ATMS AST retirement (14-file pipeline;
   independent of Path T)
4. **Phase 1B** — Tropical fuel primitive (after 1A* complete)

Then the rest of the addendum: 1C / 1V / 2A / 2B / 2V / 3A / 3B / 3C / 3V / V.
Immediate post-addendum: main-track Phase 4 (CHAMP retirement).

### Side-quest

Update `.claude/rules/pipeline.md` — "New AST Node" checklist stale since
PM 10 (pnet-serialize.rkt added), and since several recent features (incl.
T-2's expr-Open) are internal-only and skip surface-syntax/parser/macros.

---

## Continuation (2026-04-23 continued) — Step 2 baseline/hypothesis + S2.a + S2.b rescope

### Step 2 performance baseline + hypotheses + measurement discipline (commit `3ba56387`)

Created `docs/tracking/2026-04-23_STEP2_BASELINE.md` (400+ lines) with:
- §1 Headline: meta-lifecycle 11-195× faster since PPN 4C started
- §2 bench-meta-lifecycle baseline (fresh-meta 3.45μs, solve-meta 8.53μs, read 0.205μs)
- §3 bench-alloc baseline (net-new-cell 0.56μs, struct-copy 0.01μs post-BSP-LE-Track-0)
- §4 Probe per-command verbose breakdown
- §5 Step 2 hypotheses (quantitative + VAG success criteria)
- §6 Measurement discipline (bounce-back, not auto-gate)
- §7 Deferred measurements (post-Phase-4, post-Phase-3-addendum, post-PM-12)
- §11 bench-ppn-track4c PRE0 → post-T-2 A/B (11-25% wall improvements)

**Key finding captured**: meta-lifecycle gains since PPN 4C started (M2 CHAMP path still ~40μs pending Phase 4; cell path 11-195× faster).

### Step 2 rescoped to Option B + Phase 1E added (commit `7b602bd8`)

Architectural dialogue resolved:
- **Q1 concern**: `elab-meta-*` API would be parallel to `that-*`, creating Track 4D migration debt.
- **Resolution**: Option B — use existing `elab-cell-read/write` + `compound-cell-component-ref/write` helpers. No new API.
- **Phase 1E** as dedicated follow-on (between Step 2 and Phase 1B) for storage unification with `that-*`. D.3 §7.6.16 captures 7 architectural considerations (position representation, meta-id↔position mapping, 27ns fast-path preservation, universe-cell integration, facet naming alignment, write-through semantics, Track 4D compatibility) for future 1E design cycle.
- **Measurement discipline → bounce-back**: Claude proposes per phase, user decides. Always-do (probe verbose, suite timing) vs propose-and-wait (micros, A/B, hypothesis docs).

### Step 2 S2.a infrastructure (commit `ded412db`)

Delivered:
- `compound-tagged-merge(domain-merge)` factory in decision-cell.rkt (~60 LoC)
- `meta-universe.rkt` NEW (170 lines): 4 universe-cell-id parameters + init + helpers + predicate
- `'worldview` SRE domain registered (infra-cell-sre-registrations.rkt)
- `bench-meta-lifecycle.rkt` extended with E1-E5 A/B micros

**SURPRISE** (contradicted §5 hypothesis): E4 compound-cell-component-ref = 43-52 ns/call vs E5 per-meta elab-cell-read = 113 ns/call. **Compound path is ~55% FASTER** across all universe sizes (10-500 metas). Per-meta baseline's outer-CHAMP walk is expensive; universe's compact CHAMP + hasheq-ref (13 ns!) dominates.

**Implication**: Step 2 is a READ-PATH WIN + allocation win, not just allocation. Strengthens the architectural case.

### Step 2 S2.a-followup (commit `2bab505a`)

Refactored meta-universe.rkt to lightweight imports (no type-lattice.rkt dependency). Parameter-injection pattern for per-domain merge-fns + contradiction predicates (injected by elaborator-network.rkt at module load). Breaks potential import cycle for S2.b.

Added helpers: `compound-cell-component-write`, `meta-universe-cell-id?`, `reset-meta-universe-parameters!`.

### Step 2 S2.b RESCOPED to Option S2.b-staged

Attempted full S2.b migration; reverted after scope discovery:
- Estimated 200-400 LoC per D.3 §7.5.4
- Reality: ~500-900 LoC with many more caller classes than anticipated
- Specific failure mode: `match: no matching clause for '#hasheq()` in zonk.rkt:492 via unify-core — unmigrated `prop-meta-id->cell-id` consumers treating raw hasheq as type expression

**New plan**: staged sub-phases starting with S2.b-i AUDIT (committed document enumerating every call site with migration pattern per category).

**Principle surfaced** (for DEVELOPMENT_LESSONS.org candidate):
> *Migrating a cell-allocation factory migrates the IDENTITY of those cells across the codebase.* Scope by enumerating IDENTITY CONSUMERS (propagator installations, bridge-fns, direct readers), not just READERS. The cell-id returned by a factory becomes a handle used by many propagator construction sites; rewiring its meaning ripples farther than the obvious reader consumers.

### Handoff + mempalace re-mine

Wrote `docs/tracking/handoffs/2026-04-23_PPN_4C_S2b_HANDOFF.md` (562 lines) for next session pickup at S2.b-i. Includes:
- §2.3 specific request for PPN 4C parent D.3 hot-load (cross-cutting concerns between addendum and parent)
- §5.5 cross-cutting concerns matrix (Phase 3/4/7/8/9/9b/10/11/11b/12/13 interactions)
- §5.1 S2.b-i audit spec with grep commands + category enumeration
- §4.1 positive measurement surprise (compound 55% faster)
- §4.2 scope finding
- §4.5 specific failure mode to remember
- Full tracker state + commit hash index

Re-mined `docs/` with mempalace (incremental, content-hash dedup). Captures this session's new/updated docs so next session's semantic search has current baseline + Phase 1E notes + Step 2 revisions available.

### Next (S2.b-i in fresh session)

Per handoff §7.1: hot-load all §2 docs → summarize understanding → begin S2.b-i audit → produce committed `docs/tracking/2026-04-23_STEP2_S2B_AUDIT.md`.

Session arc complete. Context transferred to handoff document.

---

## Continuation (2026-04-23 continued) — session recovery + process clarification + methodology edits

### Context recovery per HANDOFF_PROTOCOL

Hot-loaded all §2 documents from `2026-04-23_PPN_4C_S2b_HANDOFF.md` in full: D.3 addendum, PPN 4C parent D.3, 4D vision, Step 2 baseline, dailies, prior T-2 handoff, principles docs, master roadmaps, key code files (meta-universe.rkt, cell-ops.rkt, elab-network-types.rkt, hasse-registry.rkt, infra-cell-sre-registrations.rkt, elaborator-network.rkt call sites, metavar-store.rkt call sites). Ran handoff §5.1 grep commands as audit preview: `prop-meta-id->cell-id` (5 prod sites), `meta-solution/cell-id` (9 sites across 8 files), `expr-meta-cell-id` direct access (4 sites), `elab-fresh-meta` (2 prod sites + 39+ test sites). Summarized understanding back to user per §8.1.

### Process clarification — mini-design + mini-audit belong in design doc, not separate audit files or dailies (commit `d269cd1e`)

User correction: I had proposed doing "mini-design audit in dailies" for S2.b-i opening. That conflated the opening bookmark (which goes in dailies, <10 min) with actual design DECISIONS and audit findings (which persist into the design doc). Additionally, handoff §7.1's instruction to "produce committed document `docs/tracking/2026-04-23_STEP2_S2B_AUDIT.md`" was a prior-session artifact, not a standing practice.

Two corrections absorbed:

1. **Mini-design + mini-audit outcomes persist into the design doc we're working from** (D.3 §7.5.1, §7.5.8, §7.5.10, §7.5.11, §7.6.9, §7.6.14, §7.6.15 are all canonical examples). Dailies hold the commit story. Standups hold user's personal notes. Three homes in Stage 4, one purpose each.

2. **Mini-design and mini-audit are CO-DEPENDENT, not sequential**. Some design decisions need code-reality to be well-informed; some audit work needs design framing to know what to measure. Cycle between them until the phase scope is coherent.

Also pushed back on a wording overreach: I had proposed a line tying "audit findings drifting out of the design doc" to the Track 4D fragmentation thesis. That conflates documentation PROCESS with the CODE Correct-by-Construction PRINCIPLE (which is about unifying fractured implementation paths via generalized APIs — orthogonal to where docs live). Corrected in the final edit — dropped the fragmentation sentence, added instead that the broader documentation web (audit docs, critique docs, research, PIRs, roadmaps) serves distinct purposes from the intra-Stage-4 loop.

Three surgical edits to `DESIGN_METHODOLOGY.org` Stage 4 Per-Phase Protocol + Conversational Implementation Cadence: step 1 (mini-design — bookmark-in-dailies with decisions-in-design-doc), step 2 (mini-audit — findings-in-design-doc), new paragraph in Conversational Implementation Cadence naming the three Stage-4 homes + distinguishing them from the broader documentation web.

### Lessons / takeaways

- *Canonical-example references make methodology self-teaching*: each edit points to actual §X.Y subsections in D.3 that demonstrate the pattern. Future readers (including future-self) can see the practice, not just read about it.
- *Process ambiguities hide in "in the current dailies" throwaway phrases*: step 1's original wording "Produce a note in the current dailies" was technically correct for the BOOKMARK, but ambiguous enough that I extended it mentally to cover full mini-design outcomes. The edit disambiguates.
- *Co-dependence vs sequential was buried in prose*: the methodology had "step 1 tells you WHAT to measure; step 2 produces the numbers" which sounds sequential. The refined wording makes cycling explicit.
- *Single source of truth in code ≠ single source of truth in docs*: the Track 4D fragmentation thesis applies to CODE (type info in 7+ locations, maintained by discipline not structure). Documentation has intentional multiplicity — audit docs serve different purposes than design docs than dailies than PIRs. Conflating these two kinds of "single source" was a category error.

### Watching list updates

- *Process clarifications as codification data points*: this is the 3rd consecutive session where a mid-session process clarification led to a methodology edit (prior: charter-alignment 2026-04-22 → D.3 §7.5.10; per-commit dailies discipline 2026-04-22). Suggests methodology evolves through use, not just through upfront design. 3 data points → promotion candidate for DEVELOPMENT_LESSONS.org.

### Next

Proceeding through approved plan:
1. ✅ Methodology edits committed (`d269cd1e`)
2. 🔄 Move 1: post-commit git hook for auto-mine on docs changes (Phase 3 of mempalace adoption, previously deferred)
3. ⬜ Move 2: code-wing experiment (mine racket/prologos into `prologos-code` wing, run 5-8 audit queries, decide adoption)
4. ⬜ Open next working day interval (standup-2026-04-23.org + 2026-04-23_dailies.md)
5. ⬜ Return to S2.b opening per correctly-framed per-phase mini-design cadence (conversational, outcomes persist to D.3)
