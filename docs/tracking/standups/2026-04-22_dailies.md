# Dailies — 2026-04-22

## Carried forward from 2026-04-19 interval

### Completed (PPN Track 4C Phase 9+10+11 Addendum — Stage 4 partial)

Single arc across 3 sessions (2026-04-19, 2026-04-21, 2026-04-22):

- **Phase 0** closed — acceptance file `examples/2026-04-17-ppn-track4c.prologos` (commit `d28e6029`)
- **Phase 1 structural enforcement infrastructure** — 1a (lint + baseline), 1b (merge-fn-registry scaffolding), 1c (`#:domain` + cell-domains CHAMP), 1d-A/B/C/D/E (registration sweep), 1.5 (srcloc infrastructure) — commits `d28e6029` through `99d9acad`. Mini-design audit codified as Stage 4 Step 1 in DESIGN_METHODOLOGY.org.
- **Phase 9+10+11 Addendum Stage 1/2/3 design arc** — research doc (1000 lines, `de357aa1`), audit doc (800 lines, `62ce9f83`+`28208613`), D.1→D.2→D.3 design doc (scope revised per Phase 1A mini-audit finding). Lens S + Progress Tracker Placement codified.
- **Phase 1A-i** (dead code retirement) — 29 lines deleted across 2 files (commit `5cf9a262`); 85 tests pass; acceptance file clean
- **Phase 1A-ii-a** (mult/level/session cell migration to tagged-cell-value) — 25 insertions at elaborator-network.rkt:921/995/1011 (commit `7052f590`); 111 targeted tests pass
- **`'mult` SRE registration follow-up** — extended `register/minimal` with `#:contradicts?` kwarg + registered 'mult domain (commits `8b85b28a`, `dc2d2fca`); enables Phase 13 progressive classification ratchet
- **1A-iii mini-design** — Z-wide scope + Framing C (PU refactor + hasse-registry) resolved (commit `21b52027`); pre-0 behavioral probe baseline captured (commit `329d4f30`, 28 expressions, 0 errors)
- **Path T-3 mini-design** — set-union merge + Role A/B decomplection + `type-unify-or-top` helper design captured in D.3 §7.6 via conversational dialogue (no separate Stage 1-3 artifact per user direction). Commits `9c3172e0` (design) + `97897c4c` (dailies)
- **T-3 Stage 2 audit + Stage 3 design** — 4 Role B sites + 8 Role A sites + 2 SRE dispatch tables classified; subtype-lattice-merge identified as template; Role B migration pattern + two-commit ordering specified (commit `6fddc5f7`)
- **T-3 Commit A DELIVERED** — `type-unify-or-top` helper + 4 Role B site migrations in elaborator-network.rkt (commit `37aaba2b`). Zero semantic change: probe diff = 0, 129 targeted tests pass

### Suite state

- Full suite NOT run this interval (would be post-Commit-B regression check)
- Probe baseline (`data/probes/2026-04-22-1A-iii-baseline.txt`): 28 expressions, 0 errors — reference for all T-3 work
- Targeted tests green at post-Commit-A state (129 across multiple runs; 89 in separate runs during Commit B diagnostic)

### In progress

- **T-3 Commit B PAUSED** — set-union fallthrough regressed `test-union-types.rkt:234`; reverted; third accidentally-load-bearing mechanism identified at typing-propagators.rkt:1907/1919 (expr-union writes component types instead of `[Type lv]`, relies on merge→type-top→sexp-infer fallback for correct `[Type 0]` result)
- **T-3 T3-C3 systematic re-audit** — NEXT TASK per D.3 §7.6.13 enhanced audit criteria

### Pending next-steps

1. **T-3 T3-C3 re-audit** (immediate):
   - Audit contradiction-detection-as-fallback sites downstream of cells using `type-lattice-merge`
   - Audit `type-lattice-contradicts?` / `net-contradiction?` consumers
   - Fix `typing-propagators.rkt:1878-1920` expr-union writes (architectural correctness: write `[Type lv]` via `infer-level`)
   - Audit other expr-foo typing patterns for similar issues
   - Migrate newly-found Role B sites (likely Commit A extension / Commit A.2)
2. **T-3 Commit B retry** after audit closure:
   - Set-union fallthrough: `type-lattice-merge` final `[else type-top]` → `(or (try-unify-pure a b) (build-union-type-with-absorption (list a b)))`
   - Update 5 assertions in `tests/test-type-lattice.rkt` (lines 39, 72, 85, 188, 255)
   - Validate: probe diff = 0, test-union-types:234 passes, full suite regression check
3. **Post-T-3**:
   - **T-1** speculation mechanism consolidation (with-speculative-rollback simplification; many sites become unnecessary post-T-3)
   - **T-2** Map open-world realignment at typing-core.rkt:1196-1217 (build-union-type becomes redundant OR migrate to `_` open-world)
   - **1A-iii-a-wide Step 2** PU refactor (4 per-domain universes + shared hasse-registry + elab-meta-read/write API)
   - **1A-iii-b** Tier 2 atms cleanup (independent of Path T)
   - **1A-iii-c** Tier 3 surface ATMS AST retirement (14-file pipeline, independent of Path T)
4. **Phase 1B** (tropical fuel primitive), **1C** (canonical BSP fuel instance), **1V** — after Path T resolves
5. **Phase 2A/2B/2V** (orchestration unification — stratum handler registrations)
6. **Phase 3A/3B/3C/3V** (union types via ATMS + hypercube)
7. **Phase V** capstone + PIR

### Watching

- **Three accidentally-load-bearing findings as a confirmed pattern** (attempt 1 / Sub-A / Commit B, all in this addendum track). **Promotion candidate** after T3-C3 completes: principle for `DEVELOPMENT_LESSONS.org` — "Contradiction-detection-as-fallback is a hidden Role B pattern. Stage 2 audits must check downstream dependencies, not just inline predicates." Methodology update candidate for CRITIQUE_METHODOLOGY.org Lens R (Reality-check): enumerate downstream consumers, not just direct call sites.
- **PU heuristic codification candidate** — 5+ instances of N-separate cells converging to N→1 compound (decisions-state, commitments-state, scope-cell, attribute-map, worldview-cache, now per-meta universe). Lens S sub-question or design-methodology rule worth codifying: "when creating large amounts of cells, ask if this is a PU target." Waiting for 1A-iii-a-wide Step 2 to land as 6th data point before promoting.
- **"Retired" claim post-audit verification pattern** — BSP-LE Track 2 PIR's `current-speculation-stack` overclaim was 3rd instance (PPN Track 2B, BSP-LE Track 2, BSP-LE Track 2B). 3+ instances confirm pattern; promotion candidate: "Verify retirement claims by checking dispatch paths, not just parameterize usage."
- **Mini-design in current design doc (not separate artifact)** — user direction 2026-04-22 for T-3. Watching if productive vs under-documented. One data point so far; need 2-3 more before evaluating the pattern.
- **Per-commit dailies discipline** — started 2026-04-22 session. 10+ commits this session arc, each with corresponding dailies entry. Watching if it sticks across multi-session tracks (sustains into Apr 22 follow-ups); if sustained, promote to Stage 4 per-phase completion protocol.
- **Multi-hypothesis parallel diagnostic** (user teaching 2026-04-22) — applied successfully in Commit B regression (reverted only fallthrough to isolate source, 1-2 cycles vs 3+). 1 data point; watching for confirmation across tracks before codifying.

### Lessons distillation check

Scanned 2026-04-16 BSP-LE 2B PIR + past 4 dailies (2026-04-10, 2026-04-12, 2026-04-17, 2026-04-19 which spans 2026-04-19/21/22) for codification candidates:

- **Merge ≠ Unify** (T-3 finding) — lattice join (accumulate via set-union) and equality check (unify, produce contradiction on mismatch) are semantically opposite; API conflation is bug source. Candidate for DESIGN_PRINCIPLES.org or DEVELOPMENT_LESSONS.org. **Hold** — codify after T-3 lands (Commit B) as the track's central lesson.
- **Substrate conflation worse than under-reuse** (T-1 finding) — applying BSP-LE 2B branch-exploration machinery to try-rollback semantics added complexity without benefit, hid the real mechanism, later broke in confusing ways. Candidate for DEVELOPMENT_LESSONS.org. **Hold** — codify after T-1 lands (validates the finding).
- **Implementation ordering as load-bearing principle** (T-3 two-commit ordering) — ordering can be architectural, not just procedural. Commit A before Commit B prevents silent bugs across Role B sites. Candidate for DESIGN_METHODOLOGY.org Stage 4. **Hold** — codify after T-3 lands.
- **Three accidentally-load-bearing findings pattern** (see Watching). **Hold** — T3-C3 is the response; codify after.

No new entries graduated to principles this interval. All four candidates are tied to T-3 completion; codify after Commit B lands.

### Longitudinal pattern tracking

Tracking patterns with ≥3 data points toward codification:

| Pattern | Data points | Next check |
|---|---|---|
| Accidentally-load-bearing mechanism | 3 (attempt-1, Sub-A, Commit B) | Post-T3-C3 — codify |
| PU heuristic (N→1 compound) | 5+ (decisions, commitments, scope, attribute-map, worldview-cache, per-meta) | Post-1A-iii-a-wide Step 2 — codify |
| "Retired" claim overclaim | 3+ (PPN 2B, BSP-LE 2, BSP-LE 2B) | Post-T-3 — codify |
| Mini-design in existing doc | 1 (T-3) | Observation period |
| Per-commit dailies discipline | 1 session arc | Observation period |
| Multi-hypothesis parallel diagnostic | 1 application | Observation period |

## Session log

### 2026-04-22 (continuation) session — T3-C3 re-audit context restoration

**Session start**: continuation from 2026-04-22 pre-handoff session. Context restoration via HANDOFF_PROTOCOL.org Hot-Load Protocol.

**Hot-loaded** (full):
- §2.0 `HANDOFF_PROTOCOL.org`
- §2.1 Always-load: `CLAUDE.md` + `CLAUDE.local.md`, `MEMORY.md` (via system context), `DESIGN_METHODOLOGY.org`, `DESIGN_PRINCIPLES.org`, `CRITIQUE_METHODOLOGY.org`
- §2.2 Rules (via system context): on-network, structural-thinking, propagator-design, workflow, testing, pipeline, stratification, prologos-syntax
- §2.3 D.3 design doc (`docs/tracking/2026-04-21_PPN_4C_PHASE_9_DESIGN.md`) — including critical new §7.6.11/§7.6.12/§7.6.13
- §2.4 Prior art: `subtype-predicate.rkt` (subtype-lattice-merge template), `union-types.rkt` (build-union-type)
- §2.5 Type lattice + related: `type-lattice.rkt` (incl. `type-unify-or-top` helper), `elaborator-network.rkt` (4 Role B sites + cell factories 917/995/1011), `typing-propagators.rkt:1850-1950` (expr-union typing including architecturally-wrong site 1907/1919), `typing-core.rkt:440-462` (expr-union sexp fallback) + 1196-1217 (map-assoc with with-speculative-rollback)
- §2.5 Tests: `test-type-lattice.rkt` (full, 5 assertions to update at lines 39, 72, 85, 188, 255), `test-union-types.rkt:220-240` (the canary at line 234)
- §2.6 Probe: `examples/2026-04-22-1A-iii-probe.prologos` + `data/probes/2026-04-22-1A-iii-baseline.txt`
- §2.7 Dailies: `2026-04-19_dailies.md` (including 2026-04-21 and 2026-04-22 session arcs); prior handoff (`2026-04-21_PPN_4C_PHASE_1A_HANDOFF.md`)
- Propagator.rkt fallback paths: `net-cell-read:991` (TMS fallback) + `net-cell-write:1248` (TMS fallback — THE 1st accidentally-load-bearing site)

**Git state verified**: `main @ f42434ea`, working tree clean (only benchmark data + `.lsp/` artifacts modified, not touching production). Matches handoff exactly.

**Understanding summarized to user**: Session state, 3-accidentally-load-bearing pattern, settled decisions (Path T, T-3 Role A/B decomplection, Commit A delivered, Commit B paused), typing-propagators.rkt:1907/1919 architectural error identified, T3-C3 plan per §7.6.13 enhanced criteria.

**Ready for T3-C3 re-audit execution**: awaiting user go-ahead on re-audit execution plan.

### Open next working day interval (this transition)

User requested fresh working day interval at session continuation boundary. Prior dailies (`2026-04-19_dailies.md`) closed with T-3 arc captured. New standup `standup-2026-04-22.org` and dailies `2026-04-22_dailies.md` created concurrently per workflow rule.

### PPN Track 4D proposed + vision research (commit `4812892c`)

Draft research note at `docs/research/2026-04-22_ATTRIBUTE_GRAMMAR_UNIFICATION_VISION.md` (~300 lines) proposing new PPN track 4D: Attribute Grammar Substrate Unification. Motivated by the T-3 three-accidentally-load-bearing pattern as structural fingerprint of sources-of-truth fragmentation across typing/elaboration/reduction subsystems (7+ locations: syntax.rkt + typing-core.rkt + typing-propagators.rkt + elaborator-network.rkt + zonk.rkt + unify.rkt + reduction.rkt). Thesis: collapse into single declarative attribute-grammar substrate where each typing rule is a grammar production with attribute-equations compiled to propagator installations. The propagator network IS the attribute grammar runtime.

Preliminary phase structure (A grammar rule representation, B compiler, C migration, D sexp-infer retirement, E unification consolidation, F zonking as readiness stratum, G reduction as :whnf facet). Scope comparable to PPN 4C (multi-month). Prerequisites: 4C complete + T-3 landed + PM Track 12 scoping decided. Prerequisite to Track 7 (CAPSTONE user-defined grammar extensions).

Updated PPN Master (`2026-03-26_PPN_MASTER.md`) Progress Tracker + Track Details section with 4D entry; MASTER_ROADMAP.org linked.

Principle-to-codify candidate (post-T-3): "Fragmentation of sources of truth is architectural debt; accidentally-load-bearing patterns are its fingerprint. Structural fix is unification under one substrate, not more-careful-migration."

### T3-C3 full re-audit executed + classified (2026-04-22)

Per D.3 §7.6.13 enhanced audit criteria. Findings persisted in D.3 §7.6.14.

**Q3 C3 full grep classification** (every `(type-top? ...)` consumer):
- **Category A** (already migrated in Commit A, verified): 4 Role B sites
- **Category B** (NEW Role B sites, §7.6.9 missed these): 5 sites — B1/B2 write-then-check pattern (typing-propagators.rkt app fire + expr-ann); B3/B4/B5 cell merge-fn pattern (classify-inhabit, cap-type-bridge, session-type-bridge)
- **Category C** (architectural error): 1 site — typing-propagators.rkt:1878-1920 expr-union install writing component types with misplaced INFER-time branching
- **Category L** (legitimate, no change): reconstructor propagators, readiness checks, lattice internals, defense code, root fallback gate, tensor result check

**Q2 install-caller audit**: install-typing-network has ONE production caller (line 2220, top-level infer). No check-time invocation. Phase 8 Option D branching at INFER time is misplaced. Confirmed Option A2 (remove branching).

**Q1 M2 centralized helper** — `type-map-write-unified` to be added in A.2-b.
**Q5** confirmed Role B for classify-inhabit classifier×classifier (equality enforcement).
**Q6** ordering at discretion.

### T-3 Commit A.2-a DELIVERED (commit `a5a33a71`)

Architectural fix for Category C expr-union:
- Added `make-union-fire-fn` in typing-propagators.rkt (parallels make-pi-fire-fn; reads left/right :type, writes `(expr-Type (lmax ...))`)
- Replaced expr-union install case (1878-1920): install sub-expressions, install fire function with component-paths; removed worldview-bitmask branching, promote-cell-to-tagged, assumption counter, type-map-write of component types
- Removed dead `union-assumption-counter` parameter
- Removed dead atms.rkt imports (`assumption-id`, `assumption-id-n`, `solver-state-assume`, `solver-state?`)

Validation:
- Probe diff = 0 (baseline matches exactly; p3-my-map still `(Map Keyword Int | String)`, `(infer)` paths unchanged)
- 147 targeted tests pass (test-union-types, test-type-lattice, test-ppn-track4, test-sre-coverage, test-mixed-map, test-elaborator-network)
- Correct under BOTH current merge semantics AND post-Commit-B set-union (standalone-safe; independent of T-3 ordering)

Design rationale captured in commit message + D.3 §7.6.14.

Lessons this step:
- **Architectural fix was transparent** under current merge semantics — zero behavior change. This validates the fix as correct-by-architecture (it was already wrong; fixing it doesn't break anything because the wrong path was being routed around by contradiction-fallback).
- **make-pi-fire-fn was the template** — the parallelism was there all along, expr-union just didn't use it. Reveals the attribute-grammar vision: each typing rule SHOULD be this structurally-similar declarative shape.
- **Dead scaffolding removal** is clean: union-assumption-counter + atms.rkt imports all drop out cleanly once the branching code is removed. Single architectural move with multiple cleanup benefits.

### Next

Commit A.2-b: add `type-map-write-unified` centralized helper in typing-propagators.rkt; migrate B1 (app fire, line 1160) and B2 (expr-ann, line 1932) to use it.
