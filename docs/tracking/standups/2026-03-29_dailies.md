# Dailies — 2026-03-29

## Carried Forward from Previous Interval

### PPN Track 2: Surface Normalization (IN PROGRESS)
- Stage 2 audit COMPLETE (`d0ccf5c`): macros.rkt 9763 lines, 48 functions, 24 registries
- D.1 design exists: `docs/tracking/2026-03-28_PPN_TRACK2_DESIGN.md`
- 8 phases: rewrite registry → engine → registries → injection → fixpoint → L2 → retirement → benchmarks
- **Next**: Pre-0 benchmarks, then D.2 self-critique

### SRE Track 2F: COMPLETE
- Algebraic foundation: kind-variance table, unified duality, table-driven dispatch
- 383/383 GREEN. sub-relation-fn deleted. sre-relation: 5→4 fields.

### PTF Track 1: COMPLETE
- Module theory, residuation, algebraic embeddings

### PAR Track 1-2: COMPLETE
- BSP default. True parallel execution. 4.45x synthetic, 1.42x real.

### PM Track 10C: Phase 1 COMPLETE
- Work-stealing: 150s→133s. Whale split. 383 files.

### Known Gaps / Blockers
- test-structural-decomp: meta-solving via imperative state under BSP
- WS-mode solver syntax: `=` unification, `is` bridging, arithmetic
- SRE Track 2C: cell refs (zonk elimination prerequisite)

### Deferred Work
- PAR Track 2 R3-R5: closed (answered by implementation)
- PM Track 10C Phases 2-3: per-test splitting (Phase 1 captured core value)
- PPN Track 5: bilattice (elimination ordering)
- Batch worker simplification (needs registries as cells)

### Priority Ordering (from 2026-03-28)
1. **PPN Track 2** — surface normalization as propagators
2. SRE Track 2B (optional) — polarity inference
3. PPN Track 3 — parser on network
4. SRE Track 2G — algebraic domain awareness
5. PPN Track 4 / SRE Track 2C — elaboration fusion + cell refs

### Watching (medium-term patterns)
- **"Correctness infrastructure before parallelism"** — CONFIRMED (3 instances). Ready for codification.
- **"Propagator density determines parallel benefit"** — R1: 18 BSP rounds. PPN migration will increase density.
- **"Architecture ahead of implementation"** — PTF Track 1 finding. Validated by SRE Track 2F (algebraic foundation was already correct, just ad-hoc).

---

## Lessons Distillation Check

### Scanned: SRE Track 2F implementation + PTF Track 1 PIR + March 27-28 dailies

**Ready for codification**:

1. **"Correctness infrastructure before parallelism"** — 3 instances confirmed. **CODIFY** in DESIGN_PRINCIPLES.org.
2. **"Architecture ahead of implementation"** — 2 instances (PTF Track 1 found the endomorphism ring decomposition is correct but ad-hoc; SRE Track 2F made it explicit with zero behavioral change). The research investment in design methodology pays off as architectural correctness. **WATCHING** for 3rd instance.

**Already codified**:
- CALM requires fixed topology (DEVELOPMENT_LESSONS.org)
- Diagnostic protocol (workflow.md)
- Design:implementation ratio ≥1.7:1 (10-PIR longitudinal)
- Two-lens self-critique (DESIGN_METHODOLOGY.org)

---

## Today's Work

### Completed

- **PPN Track 2 Phase 0: Pre-0 benchmarks** (`a0fd523`→`6ee83ce`) — M1-M5 micro-benchmarks + A1-A4 adversarial + E1 comparative suite. Key finding: preparse invisible vs elaboration. 22-35μs/rule. Complete performance freedom. Data incorporated into design.

- **PPN Track 2 D.1→D.1c: Major design evolution** — three significant revisions:
  - D.1b (`5acff03`): CALM-compliant rewrite architecture. Replacement merge → pipeline of set-once cells. Layered Recovery Principle applied to rewrites. Grounded in DEVELOPMENT_LESSONS.org CALM invariant + EFFECTFUL_COMPUTATION_ON_PROPAGATORS.org.
  - D.1c (`a6c9349`): Parse tree as Pocket Universe. Eliminated datum layer entirely. SRE operates directly on `parse-tree-node` via `ctor-desc`. Module Theory lens: parse tree is a module over endomorphism ring of rewrite rules. Deferred items from PPN Master + Track 1 PIR incorporated: mixfix, token migration, syntax-object elimination, reader.rkt retirement.
  - Cross-linking (`56e32de`): All documents navigable. PPN Master ↔ Track 2 design bidirectional links.

- **PPN Track 2 D.2: Self-critique** (`99725f1`) — Two lenses (principles challenge + codebase reality check). 7 findings:
  - P1 (HIGH): Parse tree only has `'line` tags → tag-refinement stratum T(0) with SRE subtype refinement. Approach 3 (struct field) with Approach 4 awareness (lattice cell for Track 3+).
  - P7 (DESIGN): Form identity tradeoff — Approach 3 vs 4 extensively analyzed through PTF, PRN, SRE, Module Theory lenses. Approach 3 for Track 2, migration path to Approach 4 documented.
  - Codebase grounding: 57 reader.rkt imports (grep), `'line`/`'root` only tags (parse-reader.rkt lines 1146/1158), ~160 lines datum extraction code.

- **PPN Track 2 NTT Modeling** (`db2ce39`) — Full pipeline expressed in NTT speculative syntax. §3.12: lattice declarations, rewrite rules, tag refinements, specialized propagators, stratification, bridges. 3 NTT gaps discovered:
  1. `rewrite` form — first-class DPO rewrite rules (proposed NTT §17.1)
  2. `refine` form — monotone tag refinement (proposed NTT §17.2)
  3. `:foreach` — data-dependent instantiation (proposed NTT §17.3)
  All three proposed as NTT syntax extensions in NTT Syntax Design doc §17.

- **PPN Track 2 D.3: External critique** (`33b9a86`) — 10 findings from architectural review:
  - E2 (HIGH): Reclassified rules → 9 simple + 5 recursive + 4 specialized. `template-fn` field added to `rewrite-rule` for recursive rules (cond, do, list-lit, lseq-lit, quasiquote).
  - E7 (HIGH): V(0) priority ordering = CALM violation → promoted to sub-strata V(0,0)/V(0,1)/V(0,2) with set-once cells between them. Priority reserved for independent rule conflicts only.
  - E5 (MEDIUM): `use-propagator-preparse?` dual-path parameter. Learn from PPN Track 1 experience.
  - E9 (LOW): Source ordering via provenance. Heyting algebra error reporting connection.
  - E6 (MEDIUM): Track split rejected — one track, design complete.

### Design Decisions Made

1. **Parse tree IS the representation** — no datum layer, no compat layer, no syntax objects. SRE `ctor-desc` on `parse-tree-node` directly.
2. **CALM-compliant pipeline** — set-once cells between strata. Rewrites at stratum boundaries (Layered Recovery). No replacement merge.
3. **Tag refinement as SRE subtype** — T(0) stratum assigns form-head tags via first-token inspection. Approach 3 (field) with Approach 4 awareness (cell).
4. **V(0) sub-strata** — V(0,0) implicit-map → V(0,1) dot-access → V(0,2) infix. Data dependency enforced structurally, not by priority.
5. **9 simple + 5 recursive + 4 specialized** rule classification.
6. **Provenance for source ordering** — errors carry structural conflict information from the provenance chain.

### Lessons

- **The Pocket Universe Principle applied to parsing**: a `parse-tree-node` with RRB children IS a tree. A PVec of PVecs. Structural sharing makes rewriting efficient. The SRE decomposes into the embedded lattice — one mechanism for type decomposition AND surface normalization.
- **CALM violations hide in priority ordering**: when rules have a DATA dependency, priority ordering within one BSP round is imperative sequencing in disguise. The BSP scheduler as default would catch this as wrong results. Sub-strata are the correct-by-construction fix.
- **NTT modeling as design validation**: expressing the architecture in NTT syntax revealed 3 gaps (rewrite, refine, foreach) that the prose design missed. Every propagator track should include an NTT modeling section.
- **External critique found the recursive-rule gap**: 5 of the "14 simple rules" actually need fold-over-children. The design was over-simplifying to maintain the "all rules are data" narrative. Honest classification (9 simple + 5 recursive + 4 specialized) is more actionable.

---

## Considerations

- **PPN Track 2 Phase 1 COMPLETE** (`27e8870`) — `surface-rewrite.rkt`: 350 lines, 14 tests. Tag refinement infrastructure (38 rules, recursive into all children, `tag-expr` catch-all). Rewrite rule struct + apply-rules with priority + guard. WHY: the parse tree from PPN Track 1 has only `'line` tags — T(0) tag refinement is the bridge between indent-structural tree and form-semantic tree. The `rhs-builder` field unifies simple and recursive rules as functions (design choice from D.3 E2 — rather than two separate fields, one builder function handles both cases).
- **PPN Track 2 Phase 1b COMPLETE** (`b990292`) — Tag refinement wired into `read-all-syntax-ws` between `read-to-tree` and `read-all-forms-from-tree`. WHY: the compat layer ignores tags (extracts by token structure), so this is additive with zero behavioral change. Tags are ready for Phase 2+ rewrite rules to dispatch on. 383/383 GREEN, 122.2s.
- **PPN Track 2 Phase 2a partial** (`d1cc404`) — 5/9 simple rules implemented (let-assign, let-bracket, if, when, compose). 3 deferred to Phase 6 (dot-access, dot-key, infix-pipe — need tree-level sentinel detection). Reality check on real parse trees confirmed structure matches expectations. Key finding: bracket groups are flat tokens in tree, not nested nodes.
- **PPN Track 2 Phase 2b COMPLETE** (`99cccaa`) — 4 recursive rules (cond→nested if, do→nested let, list-literal→nested cons, lseq-literal→nested lseq-cell). Quasiquote deferred to Phase 3.
- **PPN Track 2 Phase 3 partial** (`2d3d1f7`) — Quasiquote implemented (tree walk → Datum constructors). 4 complex rules deferred to Phase 6: pipe-fusion (datum-tied), mixfix/Pratt (needs registry cells), defn-multi (datum format), session/defproc-ws (WS restructuring).
- **Phase 6 deferred work consolidated** — 8 rules total deferred from Phases 2a/3. Design doc §4 Phase 6 now has explicit table with each rule, why deferred, and what Phase 6 must do. Integration strategy: tree-native (preferred) or datum bridge (pragmatic fallback).
- **Phase 4 ALREADY DONE** (Track 7 Phase 2 — 24 cells, dual-write, cell-primary reads).
- **Phase 6a: G(0) COMPLETE** (`8778dfe`) — `group-tree-node` converts indent-tree to form-tree with bracket-grouped children. Verified on filter-xf pattern (if: 8+ → 4 children). KEY FINDING: line structure ≠ form structure. 5 levels of structure identified. G(0) bridges levels 2→3.
- **Phase 6 DESIGN FINDING** (`e0c7a7f`) — G(0) is NOT additive. It changes tree structure, changing datums, breaking preparse-expand-all calibration. Activation requires ATOMIC switch (old path OR new path, not both). Phase 6b: implement `use-propagator-preparse?` dual-path. When `#t`, G(0) + rewrite-tree + tree-level normalization. When `#f`, current path.
- **Phase 6b** is the remaining integration work: ensure tree-level pipeline handles ALL normalization cases that preparse-expand-all handles. Then atomic switch. Then 383/383 GREEN.
- **Phase 6b: Pipeline-as-cell IMPLEMENTED** (`d67094d`) — `form-pipeline-value` struct with monotone stage chain. `advance-pipeline` applies next stage transformation. `run-form-pipeline` runs to 'done. WHY: a stratified pipeline IS a lattice, a lattice can be a cell. Each form gets ONE cell. Stage progression is monotone. Independent forms parallel via BSP. No dual-path switch needed — migration is per-stage (additive).
- 30 tests total. V(1) macro expansion and V(2) spec injection are pass-through stubs.
- **SCOPE DECISION**: Pull PPN Track 3 scope (tree → surf-* directly) into Track 2. Eliminate the datum layer. WHY: the form-grouped tree already HAS the structure that parse-datum re-derives from datums. The datum layer is a lossy bottleneck. The parser is a mechanical translation from one structured representation to another — 72 functions × ~5 lines of accessor changes. We have specific context NOW on the tree structure; doing it later means rebuilding that context.
- **Phase 6c: tree-parser.rkt IN PROGRESS** (`2a7cf89`→`879d4df`) — 783 lines, 20 tests. Core forms working: def (3 variants), fn (4 variants: bracket-binder, binder+return-type, multi-binder, bare params), atoms (integers, variables, booleans, keywords, strings, type names, Nat literals, chars, holes), builtin binary ops (17: int arithmetic, generic arithmetic, comparisons), builtin unary ops (7), keyword dispatch (fn, the, suc, boolrec, cons), application (foldl surf-app). **15 stubs remaining**: defn, spec, data, trait, impl, session, defproc, defr, solver, quote, brace-group, ns, imports, exports, match. Pattern validated: mechanical translation from parser.rkt — same dispatch, same surf-* output, different input accessors.
- WHY tree→surf directly: datums are a lossy bottleneck. tree→datum→surf loses source locations, indent structure, token types, form tags. Each piece of information is destroyed then re-derived. tree→surf preserves everything. ~200 lines of datum extraction code + ~1500 lines of parser S-expression matching will eventually be eliminated.
- **Phase 6c COMPLETE** (`a3824c8`) — tree-parser.rkt: 1250 lines, 26 tests. Full core language: def, defn, fn, Pi, Sigma, arrows, match, boolrec, natrec, Eq, cons/pair, map-literal, 27 builtin ops, application. Preparse-consumed forms correctly stubbed.
- **Phase 6d: tree parser WIRED** (`e62d444`) — `use-tree-parser?` parameter. When `#t`: tree pipeline produces surf-* ASTs, preparse runs for registries only. CRITICAL FIX: surf-app must use args-LIST not nested single-arg. Simple programs work e2e: `def x := 42\neval x` → "42 : Int". defn type inference: known issue (elaborator needs expand-top-level transformations).
- **56 tests total** across surface-rewrite (30) + tree-parser (26). Old path unaffected.
- **Phase 6d MILESTONE** (`3bca61e`) — **383/383 GREEN, 7529 tests, 125.5s.** Hybrid tree parser path: preparse runs for registries + generated defs + elaboration (proven correct). Tree parser runs in parallel for validation. Zero regressions. WHY hybrid: data/trait/impl/spec consumed by preparse produce GENERATED defs. Tree parser handles def/defn/eval/check/infer + expressions. Hybrid uses preparse for elaboration while building confidence in tree parser. DIAGNOSTIC PROTOCOL: (1) defn type inference: surf-defn.param-names must be symbols, type-ast must be Pi chain with holes (not #f). (2) surf-app.args must be LIST not nested. (3) preparse-consumed forms produce generated defs that tree parser doesn't.
- **TOTAL new code**: ~2500 lines across surface-rewrite.rkt (1200, 30 tests) + tree-parser.rkt (1250, 26 tests). 56 module-level tests.
- REMAINING: incremental switchover (tree parser for def/defn/eval), Layer 2 + retirement + benchmarks
- SRE Track 2F's algebraic foundation provides the `ctor-desc` infrastructure this design builds on.
