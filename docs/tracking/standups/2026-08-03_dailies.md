# Dailies — 2026-08-03

New file for the OCapN-branch work (branch `claude/ocapn-prologos-implementation-auLxZ`).
The 07-28 dailies covers the owner's CIU T6 line on `main` and is untouched —
this session ran entirely on the feature branch and never touched `main`.

---

## 📍 STATE  (overwrite in place — always current)

- **HEAD** `829b9b9b` on `claude/ocapn-prologos-implementation-auLxZ` · **Suite** GREEN **10270 / 538 / 0** · **OCapN conformance 24/24** · working tree clean.
- **Where we are**: the OCapN third-party-handoff review backlog is CLOSED (S1+S2, S3, C1, C4, C5, X2), and `DEFERRED.md` has had a systematic probe pass — **59 of 96 sections** now either fixed, corrected, or explicitly marked verified-accurate-with-probe.
- **NEXT**: the three OPEN RULINGS below are what gate the next cluster. Everything else remaining in DEFERRED is confirmed-unstarted work.
- **Open / blockers — three owner rulings**:
  1. Should `defn` accept a SPACED parameter annotation (`[n : Nat]`)? `fn` does; `let` does not (fused only); `defn` follows `let`. If deliberate, that entry closes as documentation.
  2. Should the twelve generated `.md` exports stay in-tree at all? They cannot be regenerated here (no emacs/pandoc) and 11 of them are stale; each now carries a banner.
  3. How should the numeric-literal obligation be discharged (the float/String branch-join unsoundness)? Three candidates written up in the entry, including one that LOOKS right and is not.
- **Watching**:
  1. **"Blocked" in a DEFERRED entry has meant "not tried" five times this session** — Numerics Phase 4 (largely done), IO Library (3 of 5 phases done+tested), PUnify (5 of 7 stale), the `book/` runners (already shipped), and String Library 4a/4d (both FFI bridges the entries listed as needing unbuilt infrastructure). Probe before repeating an entry's framing.
  2. **A wrong DEFERRED entry SUPPRESSES the test that would catch a regression.** Nested constructor patterns worked for ~5 months with nothing pinning them, because the entry said they were broken. Same for Numerics Phase 4. When a probe finds an entry stale, pin the behaviour in the same commit.
  3. **Registration-by-detonation.** The `.pnet` tag tables were populated one node at a time, each added when THAT node crashed — so every family is registered exactly as far as it has hurt someone. 31 missing siblings found in one sitting, including 12 generic-arithmetic nodes sitting next to two that are registered *because they caused a months-latent crash*. Whenever a fix is applied to a family member, enumerate the family in the same commit.
  4. **The value-discarding guard class** (`(when C (parse-error …))` — `parse-error` RETURNS) had 31 sites in `parser.rkt`; 6 were live crashers, now fixed. The structural answer (make `parse-error` raise, or an error monad) is unresolved, so the shape can be reintroduced by the next guard written.
  5. **My own failure mode**: generalising from one or two failed attempts to a claim about the system. Every wrong "blocked" call had that shape. Countermeasure is cheap — try the adjacent form before writing the word.

---

## LOG  (append per commit / decision / surprise — newest at the BOTTOM)

### OCapN handoff review backlog — S1+S2, the exporter double-spend (`d9b3bc2f`)

- **Why**: a signed `desc:handoff-receive` was a BEARER TOKEN — session and side were read only to build the replay identity, never compared. And the used-set lived on `BridgeState`, which dies with the connection, so withdraw→reconnect→replay was a double-spend.
- **Surprise**: the findings doc's own claim that "fixing S1 makes S2 sound" was FALSE — a session id derives only from the two key-derived side-ids, so a peer reconnecting with the same key reproduces it. Corrected in place.
- **Cost**: 3 conformance tests, from `ocapn-identity-keypair`'s fallback not being usable as a sentinel — crypto-ffi numbers handles from 0 and the server's identity is the FIRST keypair, so 0 is the REAL handle. Added `ocapn-identity-present?`.

### S3 gift ids, C1 reservation, C4/C5 reader agreement (`a1eeaff4`, `cc9ff44e`, `7efc781d`)

- Gift ids were a counter from 0 in an exporter-GLOBAL table reachable by any handshaked peer — a shadowing attack you carry out by counting. Now 128 random bits AND first-deposit-wins.
- **Surprise**: chasing a "batch flake" in `test-error-messages` found the cross-file SPEC-STORE LEAK — `spec-store-lookup` reads the CELL first, the worker restores only the PARAMETER, and `run-ns-*` shared one prelude network. Bisected to a TWO-FILE repro. It had been re-observed by three earlier sessions; `--jobs 1 --all` makes the order deterministic.

### The exhaustive-walker family, three instances (`2df675d5`, `c4aa917c`, `10f5a080`)

- `expr-foreign-fn` as a "closed leaf" in SIX walkers; five mixfix raise sites where I first converted three; `ground->prologos-expr` filtering AST nodes through thirteen predicates with an `unknown` fallback.
- **Why it survived**: scalars always worked in the solver case, so any test written with a string literal passed. The control case is now in the test file next to the three that fail without the fix.

### The value-discarding guard sweep (`902ca588`, `486555b1`, `ccf7adb0`)

- `parse-error` RETURNS; `(unless C (parse-error …))` computes the diagnosis and throws it away. 31 sites, PROBED one at a time; 6 were live crashers (`match 5 | 0 111` → `take: contract violation`, whole file lost).
- **Surprise**: fixing one guard MOVED the crash — `defn 5 [x]` still died in `qualify-name` because a different `defn` shape took the call. Name is checked once at `parse-defn`'s entry now.
- **Not mass-converted, deliberately**: some sites fall through to a BETTER error.

### Four String Library phases shipped (`1e00ac95`, `8c50ea3f`, `4bc01a59`, `e61437ee`, `39ae3053`)

- Normalization (4b), similarity + edit distance (4c), grapheme clusters (4a), regex (4d).
- **Surprise, twice**: 4c was filed PARTIAL by me with edit distance "unwritable" — the BRACKET `let` gives sharing and it works (kitten/sitting = 3). 4a's own "Mitigation" line was the whole answer. Both were me generalising from two failed attempts.
- **Left honest**: 4d has no capture groups (a capture list needs list marshalling), patterns recompile per call, and it is Racket `regexp` not `pregexp`.

### The DEFERRED probe pass (many commits)

- **Method**: run the entry's own claim rather than reading its framing. Where stale, PIN the behaviour in the same commit.
- **Yield**: Surface Syntax 2 of 3 stale · PUnify 5 of 7 stale · IO Library 3 of 5 done+tested · Numerics Phase 4 largely done · Collections runners already shipped · the 12 named batch-isolation failures no longer reproduce.
- **Also accurate as filed, and now recorded as CHECKED**: Session Types (all 3), HKT Map partial application, `Seq` deftype straggler, Spec Phase 2, Galois `connect-domains`, Propagator-First 3e, higher-order narrowing in WS mode.
- **Systemic finding**: DEFERRED.md is not unreliable, it is UNVERIFIED — entries are written once and never re-checked, so some are years-stale and some are precisely right, and only running them tells you which.

### LET residual 1 — the fn bracket that swallows a `let` now says so (`226452ae`)

- **Why**: the residual's complaint was about the MESSAGE, and the filing said the fix "wants fn-side layout work". True of making the form work; not true of the message. `[fn [n : Int] let k := 4 …]` reported "parameters must be bare symbols", which points at `n`, which is fine.
- **Surprise**: two silent traps in a four-line predicate. `expand-let` runs over the fn's argument list BEFORE the parser sees it, so `params` holds a `($let-error msg)` marker, not the symbol `let`. And `stx->datum` is SHALLOW, so the marker's head is still a syntax object. Neither errors — the hint just fails to fire and the generic message comes out unchanged, which is indistinguishable from an edit that did not take. Cost two rebuild-and-probe cycles.
- **Fix**: match the marker, unwrap its head. Three pins — the message fires, the workaround it names actually runs, and `[fn 5 …]` still gets the generic message.
- **Residual 2 re-probed and left deferred, but now with a reason**: `classify-let-block` bails at `(< (length cont-elems) 2)` and the reader groups the deeper line into ONE element, so the forgot-body arm is unreachable for the 2-line shape. Reclassifying would declare the head binding's column a non-body position — which is a legal body position in the nested shorthand today. Owner ruling on layout semantics, not a diagnostic fix.
- **Next**: the three open rulings still gate the next cluster.

### The bare `[]` that stole another command's result — three faults in a stack (`bd1afc65`)

- **Why**: filed as "`[]` yields the WRONG command's result", with two guesses attached: "the reader is not at fault" and "start at `preparse-expand-all`". The reader WAS at fault, `preparse-expand-all` was not involved, and the entry's own control case — `[]` alone erroring, annotated "(correct)" — was the third bug rather than the baseline. A filing can be accurate about the SYMPTOM and wrong about every pointer.
- **The stack**: (1) the reader located an empty group nowhere — `wrap-stx-list` had nothing to take a range from, passed 0, `make-stx` maps 0 to `#f`, `stx-range` propagated it up; the opening bracket's token was in the caller, unused. (2) The merge keys the two parse spines against each other BY SOURCE LINE and treated line 0 as a line — 0 is the project's unknown-location sentinel, so every located-nowhere surf on one spine matched every located-nowhere surf on the other. (3) The spines DISAGREED on what an empty group means: tree said nil, `parse-datum` said "Unexpected datum: ()".
- **Surprise**: fault 2 was papering over fault 3. `def x := []` — tested, passing, believed correct — worked because the collision swapped the error surf for the tree surf. Fixing the merge key broke it on the first suite run. **Removing an accident is how you find what was riding on it**; the single suite failure was the fix reporting a pre-existing disagreement, not a regression.
- **Also broader than filed**: `def y := ()` corrupted results identically. Every empty group anywhere.
- **Test shape that mattered**: the end-to-end assertion checks COUNT, ORDER and NO-DUPLICATE together. Asserting only that `a`, `b`, `c` each appear would have passed throughout the bug — they all did, one of them twice.
- **Next**: the three open rulings still gate the next cluster.

### The HOF def seam — two lying diagnostics over one kind mismatch (`19f7a530`)

- **Why**: `reduce + 0 '[1 2 3]` gave 6 as a command and "Multiplicity violation" as a `def`. The entry was accurate throughout and its `pipeline.md` § "infer/inferQ Are Twins" pointer named the right SHAPE; the cause was one layer under it — not a missing `inferQ` arm but a COMPARISON the two passes make differently.
- **Fault 1**: a type constructor's kind is `Pi m0 Type Type` (its type argument IS erased); a spec's `{C : Type -> Type}` writes an unannotated arrow, which defaults to `mw`. `subtype?` demanded the mults be identical. It surfaced only in QTT because typing-core sees `C` as an unsolved META and meta-solving never compares multiplicities. **So the bare command "working" was never evidence** — it runs no `checkQ-top` at all.
- **Fault 2**: `is-type` ran on an UNZONKED type. The tell was inside the message: `is-type/err` renders with `pp-expr`, which DOES follow solutions, so it printed "not a valid type: **[List Int]**" — naming a valid type. **A diagnostic that pretty-prints through a resolution its own predicate did not perform will always read as nonsense; that mismatch is the signal, not noise.**
- **Fix shape worth keeping**: fault 1's repair is not new policy — `compatible 'mw 'm0` is already `#t` at every binder check. The new `subtype?` arm applies that same predicate structurally (normalize t1's mult to t2's, delegate to the existing walk), so it loosens exactly as far as `compatible` and no further. Pinned in both directions: `mw <: m1` stays false.
- **Found while probing, filed not fixed**: an unbracketed `defn` body (`defn bump [x] int+ x 1`) reads its trailing tokens as extra PARAMETERS and fails with a type mismatch naming a 3-param lambda. Documented form is bracketed; diagnostic-quality item.
- **Next**: the three open rulings still gate the next cluster.

### The removed `~N` literal now costs one command, not the file (`f75fff20`)

- **Why**: a 🔶 PARTIAL. The July fix made the raise a REPORTED error; the file was still lost, because tokenization completes before any command runs. The entry named the remedy exactly — "emit a MARKER instead of raising, the D4.P1a `parse-error`-value seat" — and that is what it took: tag the token, emit `($reader-error msg)` in `token-entry->stx`, convert in `parser.rkt` on the `$let-error` channel, add the `macros.rkt` head-symbol exclusion.
- **Surprise (silent)**: `token-pattern`'s third field is the TYPE function, not a value function. Returning a marker symbol from it RENAMES the token type, so my `token-entry->stx` arm keyed on the old name stopped firing and the token fell through two `[else]`s to a bare symbol — `~32` came out as an "Unbound variable" with no diagnostic at all. That is the same silent-fallthrough shape the `.N` ordinal-access arm at that very site is annotated for; I read the annotation and still walked into it from the other side.
- **Second, smaller**: the location moved off the message text onto the srcloc, so the existing test's `#rx"line 4"` was asserting the old DELIVERY MECHANISM rather than the fact. Assert `srcloc-line`/`srcloc-col`.
- **Doc truth**: `test-reader-robustness`'s note calling the tilde pattern "the live whole-file raiser" is now stale and was updated in the same commit — with it converted, `tokenize-string`'s validation loop is the only raising code left in the reader, and it has no production caller.
- **Next**: the three open rulings still gate the next cluster.

### `.pnet` registration gaps, found by sibling rather than by node (`7fa17a60`)

- **Why**: QTT P5 residual 2 said the Vec/Fin family had zero registrations. True, and it was NINE nodes rather than the seven filed (`expr-Vec` and `expr-Fin` themselves were also missing). Fixing it took ten minutes; probing around it took the rest.
- **Surprise**: the same landmine under three more families, and the shape is `pipeline.md`'s "fix applied to one member but not its siblings" — `expr-generic-from-int`/`from-rat` are registered BECAUSE they detonated, while the twelve arithmetic and comparison nodes beside them were not. Those are not exotic: every generic `+ - * / <` a user writes elaborates to one. Same for `int-le`/`int-mod` beside `int-lt`/`int-eq`, and Posit `sqrt`/`from-nat` where the Float lists carry `sqrt` and the Posit lists do not. 31 nodes total.
- **Method note**: each gap was VERIFIED in-process (build the node, serialize, deserialize, assert `struct?`), not inferred from reading the registration lists. That mattered — my first sweep used `dynamic-require` and produced a false positive (`expr-Symbol`, which round-trips fine).
- **What I did NOT ship, and said so**: a blanket reflective coverage test over all 345 `expr-*` structs. It reports 130 failures, and the number is untrustworthy — dummy payloads are ill-formed for the sentinel-serialized containers (champ/hset/rrb/transients), which legitimately reject them. Filed as an upper bound needing per-node triage, with the recipe, rather than as a defect count. Reporting "130 broken nodes" would have been a fabrication dressed as a measurement.
- **Test shape**: enumerate every member per family, never sample. The defect IS the per-member gap, so one-member-per-family would have passed against all 31.
- **Next**: the three open rulings still gate the next cluster.

### `vindex` computes (`2b94721a`) — QTT P5 residual 1

- **Why**: `whnf` had iota rules for `vhead`/`vtail` on a canonical `vcons` and none for `vindex`, which lived only as an `nf` congruence arm. It type-checked, multiplicity-checked, and then sat there. The residual was filed precisely so "Vec is supported now" would not be over-read — and that framing was right.
- **The one judgement call**: the congruence arm is NOT a copy of `vhead`/`vtail`'s. Those reduce only the vector, which is correct for them and wrong for `vindex`, which needs the INDEX canonical too. Copying the neighbouring shape — the obvious move — would have left `vindex(A, n, [f x], vcons …)` stuck on a computable index, and no test built from canonical indices would have noticed.
- **Test shape**: position 2 is load-bearing at both levels, because it needs the recursive arm to fire twice; an implementation handling only `fzero` passes position 0. Also pinned: a neutral index stays stuck without looping.
- **Level 3 was not optional here** — `test-reduction` builds `expr-vindex` by hand, and a rule that fires on hand-built terms can still be unreachable through the elaborator.
- **Next**: the three open rulings still gate the next cluster.

### A partially-applied foreign's type is the REMAINDER (`829b9b9b`) — QTT P5 residual 5

- **Why**: `global-env-lookup-type` returns the DECLARED Pi, which is the node's type only while `args` is empty. `reduction.rkt` appends whnf'd arguments and returns the updated node, so the type was wrong by exactly `(length args)`.
- **The entry was wrong in a useful direction**: it said "fixing it means fixing both" arms. It meant fixing ONE — `inferQ` already delegates the TYPE to `infer` (the no-drift twin pattern), so the peel lands in a single place. The twin discipline the codebase already practises had quietly made the second fix unnecessary.
- **Detail that matters**: `subst` into the codomain, not a bare unwrap. A dependent foreign signature's later domains can mention the earlier arguments.
- **Test shape**: at the arms directly, because the accumulating node is built only inside `whnf` on the hole-section path — no source program reaches it, so a behavioural test would pass with the bug in place (same reason `2df675d5`'s walker defect needed direct tests). Uses a REAL registered foreign rather than a planted env entry, and asserts the fixture found a 2-ary Pi FIRST — otherwise `expr-error` is not a Pi and the depth assertions would "pass" for the wrong reason. Twin AGREEMENT pinned at every arity, since agreement is what made the defect safe to leave.
- **Next**: the three open rulings still gate the next cluster.
