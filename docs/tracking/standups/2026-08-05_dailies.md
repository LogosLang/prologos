# Dailies — 2026-08-05

Rolled on a **topic shift**, following the precedent `2026-08-02_dailies.md` set
for an out-of-band arc. This file covers the **ARROW track**: letting `->` appear
inside identifiers (`centigrade->fahrenheit`), in a separate worktree
(`scratchpad/arrow`, branch `arrow-identifiers-b429d0`, based on `b429d038`).

⚠ **This is not a CIU T6 continuation.** `2026-07-28_dailies.md` (1096 lines)
remains the authority for CIU T6 D4 / Path Selection and is NOT copied forward
here — only the environment invariants are. Do not read the two as one arc.

Rolling also removes a structural merge hazard: `2026-07-28_dailies.md` is the
one file where this work and main's collide, because both sides append to it.
That collision was the *only* conflict when main was merged into the prelude
branch (`99321b6e`); a separate file makes it structurally impossible to recur.

---

## 📍 STATE  (overwrite in place — always current)

- **HEAD**: re-derive with `git rev-parse HEAD` — never trust a hash in prose.
  At this roll: `3735332f` (the Stage-3 design doc), 1 commit off `b429d038`.
  **NOT PUSHED** — local only.
- **Base discipline**: pinned to `b429d038` (local `main`, 2026-08-05). This
  matters — the grounding audit caught a **HEAD divergence**: it read the main
  checkout while the scoping session sat on `eager-bartik-96e686` at `a9c0c18d`,
  5 commits off a fork at `a1bcad17` that main had run **52 commits** past.
  `ident-continue?` had already drifted `:243` → `:242`. Main has touched all
  three target files since the fork (`parse-reader.rkt` ×2, `macros.rkt` ×3,
  `tree-parser.rkt` ×3). Everything is re-pinned; this worktree was created off
  main *after* that discovery.
- **Owner WIP**: the MAIN checkout's uncommitted entries (13 at last check) have
  never been touched by this arc, and must not be. All work is in worktrees.
- **Suite**: not yet run on this branch (design-doc only so far). Main's own
  gate at the base is **9847 / 482 / 0**.
- **Where we are**: Stage 3 **DONE** — design doc
  `docs/tracking/2026-08-05_0632_ARROW_IDENTIFIERS.md` (`3735332f`), owner
  rulings recorded. **P0 is SUBSUMED** (already fixed on main by `ae26f5403`'s
  general preparse-seam guard — verified at base, not re-implemented).
- **NEXT**: **P1** — the `->`-bearing identifier recognizer in
  `parse-reader.rkt`, registered above `symbol` (50), following the
  `narrow-var-annot` (96) precedent; plus a tokenizer battery asserting the
  **token stream directly**, not just end-to-end behaviour.
- **Open / blockers**: one carried risk, not blocking — `->` is a live
  **binder-region terminator** in the in-flight CIU T6 P4c broadcast machinery
  (`binder-region-terminators` = `'(:= ->)`, four consulting sites). P1 changes
  when a standalone `->` token is emitted. Verify that interaction inside P1's
  mini-audit before landing.
- **Watching**:
  1. **The base is the first thing to verify, not the last** — this arc opened
     with a scope written against a branch 52 commits stale, and the audit's
     FIRST line was the divergence. It also caught P0 as already-fixed work that
     would otherwise have been implemented twice. Re-pin before trusting any
     coordinate. (1 data point here; the project's dynamic-workflow code-state
     rule already carries the general form.)
  2. **A cited precedent can be wrong in the direction that flatters the
     design.** The LET track's fused `x:Int` was offered as the precedent for a
     glued-vs-spaced rule; it is **adjacency-blind** (`[x :Int]` behaves
     identically). The real precedent, `recognize-narrow-var-annot`, supports
     the same conclusion for a *different* reason. Check that a precedent says
     what you need it to say. (1 data point.)

---

## 📓 LOG  (append per commit — newest at BOTTOM)

### Stage 3 — the scope, and three corrections to it · `3735332f`

**Why**: `defn centigrade->fahrenheit [c:Posit32] : Posit32` fails with
`defn: expected ':', got >`. Owner reported it as new and caused by recent main
activity.

**It is not new — and the inversion is the finding.** Bisected 661 commits
(automated, one compile per step): first "bad" commit `2a7cbe45` (2026-07-02,
*loud error surfacing*). That commit did not break arrow names; it stopped the
driver **silently discarding** the parse error. Measured at `d8485097`
(2026-06-01): `defn c->f …` alone reported **0 errors while defining nothing**,
and only failed downstream with `Unbound variable`; the control `defn cToF …`
defined and evaluated. So the prior behaviour was a clean bill of health for a
file whose function did not exist. Documented in-tree since 2026-03-18
(`examples/2026-03-18-track7-acceptance.prologos:887-891`).

**Mechanism** (measured, not inferred): a mis-lex. `recognize-symbol` eats `c`,
eats `-` (which **is** in `ident-continue?`), halts at `>` (which is not). The
tokenizer is priority-order first-match-wins, **positional, no backtracking**, so
`recognize-arrow` — anchored on `-` *at the scan position* — never fires, because
that `-` is already consumed. `"c->f"` → `symbol "c-"` · `rangle ">"` ·
`symbol "f"`. The stray `rangle` is **not inert**: it pops a bracket frame, so
`[int->str x]` loses its `[` frame and errors can surface far from the cause.

**Three corrections the audit forced**, each re-verified by hand:
(1) **The rule was mis-stated.** "No surrounding whitespace" would have
mis-described **275 live sites** in lib+examples — the prefix arrow-type form
glues `->` to an opening bracket (`[-> [List A] [Option A]]`; 208 `[->`, 67
`(->`). The rule is **ident-char on BOTH sides**.
(2) **The cited precedent does not exist as framed** (see Watching 2).
(3) **P0 was already done** — the `inject-spec-into-defn` whole-file abort found
while probing is real on the stale branch but already fixed on main by
`ae26f5403`. `7d8520a0b` had separately promoted the governing lesson *"a raise
on the parse path is a WHOLE-FILE abort"* on 2026-08-03.

**Coherence is measured, and it is clean**: 6,051 `->` in the corpus, 5,547 fully
spaced, **0 right-glued in code**; all 13 left-glued sit inside `foreign` string
literals, which `recognize-string` never re-tokenizes. Glued `->` already fails
everywhere today (`spec A->B`, `| p->b`, `<Int->Bool>`), so the rule regresses
nothing. ⚠ But `>` must **never** be added to `ident-continue?`: 1,416 of 1,444
ident-glued `>` chars close an angle group.

**Surprise**: `<` DISCLOSES — which I expected to constrain this — **does not
exist in code**. One comment; D4 schedules it in P4e (⬜).

**Next**: P1.
