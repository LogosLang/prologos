# Dailies — 2026-04-30

## Carried forward (from 2026-04-26 dailies — interval ran 2026-04-26 → 2026-04-30, full)

The previous dailies ran 1667 lines covering 4 calendar days. Opening a fresh
interval. Active threads carried into this session:

### SRE Track 2I — Phase 3 ready to open

- **Status**: Phases 1, 2, 2a, 3c ✅ committed. Phase 3 (empirical SD sweep)
  is next. Mini-design already locked in
  [`SRE_TRACK2I_SD_CHECKS_DESIGN.md`](../2026-04-30_SRE_TRACK2I_SD_CHECKS_DESIGN.md)
  § Phase 3.
- **Handoff document**: [`2026-04-30_SRE_TRACK2I_PHASE3_HANDOFF.md`](../handoffs/2026-04-30_SRE_TRACK2I_PHASE3_HANDOFF.md)
  — comprehensive context transfer per HANDOFF_PROTOCOL.org. Read first
  if context drops; this dailies entry assumes the handoff has been read.
- **Phase 3 immediate next steps**: Stage 4 mini-design conversational
  checkpoint with user (mini-design IS already locked; opening surfaces
  any final calibrations before code lands), then implement
  `run-sd-sweep` per locked scope: type×equality + type×subtype only,
  using `realistic-type-atoms` base-values, `/detailed` variants for
  vacuous-vs-non-vacuous reporting, `sre-domain-meet` lookup for principled
  per-relation meet dispatch.
- **Move B+ surveillance**: Phases 2a and 3c each surfaced bonus findings.
  Per user direction, Phase 3 bonus findings go through dialogue first,
  NOT speculative commit.
- **Discussion phase pending**: out-of-band review of Phase 3 findings
  to decide whether to update declared-properties at registration sites
  (candidate: `'distributive prop-confirmed` for type×equality).
- **Phase 3.5** (deferred): pseudo-complement empirical check.
- **Phase 3b** (deferred): generator extension for session×equality and
  form×equality sweeps.
- **Phase T** (after 3, 3.5, 3b land): dedicated `test-sre-sd-properties.rkt`.

### Sister concerns filed elsewhere

- **PM Master Track 12**: `current-lattice-meta-solution-fn` retirement
  (sister callback to the subtype one Phase 3c retired — 5+ usage sites,
  ties to metavar-store internals). Phase 3c retirement template documented
  for Track 12 to adopt directly.
- **DEFERRED.md**: cross-reference for visibility from the deferred side.
- **Issue #40**: codebase-wide `with-handlers` audit (72 remaining
  instances post-Phase-2a). A/B/C/D categorization framework. SRE-adjacent
  findings preserved as starting point.
- **Issue #41**: `.prologos` acceptance-file CI gap. Two solver-output
  regressions today caught by user acceptance-file inspection; Layer-1
  CI would have caught both. Priority strengthened.

### SH (Self-Hosting) Series — newly opened

- 3 artifacts landed yesterday (`3ef7b87b`, `d5be1769`, `a88ea1bd`).
  Series tracker, path note, deep research note.
- Track 9 / PReduce flagged as critical-path linchpin. Whether it lives
  in PReductions, PRN, or graduates to its own series is open per SH
  master open-questions §7.
- Track 1 (`.pnet` network-as-value) is the smallest-scope unblocking
  move with independent value. Could open before PReduce delivers.
- NTT progress is upstream of Track 1.
- 5 follow-up Stage 1 companion notes named in the deep research note §8
  (cost-lattice design, .pnet extension architecture, native-runtime
  architecture, type-erasure boundary, bootstrap mechanics).

### PPN 4C Addendum — Phase 1C ready

D.2 hybrid pivot architecture committed; D.2 self-critique drafted with 18
findings (3 BLOCKING resolved). Pre-0 phase 100% complete (M, A, E, R, S
tiers). Phase 1C implementation can open when SRE Track 2I closes.

### Free Lattices research arc — 3 research notes landed

Companion notes for SRE (canonical form), UCS (widening/narrowing), PTF
(lattice hierarchy). Both 04-30 SRE/UCS notes have §0 errata documenting
mempalace cross-checks that surfaced load-bearing prior art the keyword
greps missed. PTF note has the worked example of Phase 3c's
distributivity-status flip in § 4.

### MASTER_ROADMAP.org is stale

User flagged 2026-04-30. Not addressed in Track 2I scope; SH master
cross-references the placeholder line. Worth a separate small pass when
opportunity arises.

---

## Watching (medium-term patterns; promote to principles when threshold met)

| Pattern | Data points | Promotion gate | Action when triggered |
|---|---|---|---|
| Move B+ pattern's payoff scales beyond defensive guards | 2 (Phase 2a malformed-compounds; Phase 3c distributivity-flip) | One more occurrence (e.g., Phase 3 surfaces another bonus, or PM 12 sister-callback retirement does) → codify in `DEVELOPMENT_LESSONS.org` as load-bearing pattern | Add named lesson: "principled refactors of off-network state injection patterns surface real algebraic findings that the scaffolding was hiding" |
| Mempalace cross-check catching draft-staleness (inverse mode) | 2 (this session: morning lattice batch + afternoon SH batch) | One more occurrence → codify in `mempalace.md` as process step + dual staleness mode | Update `.claude/rules/mempalace.md` to name both staleness modes (mempalace-stale-vs-codebase, AND draft-stale-vs-mempalace-prior-art) |
| Three-artifact batch shape (master + path-note + research-note) for series founding | 1 (SH afternoon) | If used again with similar success → codify as series-opening workflow | Add to `DESIGN_METHODOLOGY.org` Stage 1 as a recognized series-opening artifact pattern |
| Two-axis retirement framing for migration problems | 1 (SH runtime-vs-compile-time) | Look for this shape in other retirement work; promote if 2-3 instances | Pattern candidate: "name the retirement axes explicitly, retire each independently rather than attempting simultaneous retirement" |
| Algebraic posture findings can flip across track boundaries | 1 (Track 2G "not distributive" → Phase 3c "is distributive" via Track 2H union-aware merge) | Look for one more instance of "merge/meet change in track N invalidates property declaration from track N-K" → codify as discipline | Add discipline: "when a track changes a merge or meet function, re-validate algebraic-property declarations on the affected domain at Stage 4 close" |
| Per-relation registration generalizes (`merge-registry`, `meet-registry`, future `residual-registry` etc.) | 2 (Track 1B merge-registry; Phase 3c meet-registry) | One more instance → graduate to `structural-thinking.md` as the canonical per-relation-op pattern | Add to SRE-related rules: "operations parameterized by relation belong as per-relation registries on sre-domain, NOT as off-network callbacks or hardcoded dispatch" |
| Solver-output regressions silent because output is "form-shaped" | 2 (`unknown` rendering; `(solve [?path ...])`) | Issue #41 already filed; promotion gate is whether Layer-1 CI lands and demonstrates value | When #41 lands, codify in testing.md: "acceptance-file CI catches this class of regression that test suites miss" |

---

## Phase 3 mini-design opening bookmark

Stage 4 mini-design + mini-audit dialogue completed (co-dependent per protocol). Decisions persisted into design doc § Phase 3 → "Locked decisions (mini-design + mini-audit dialogue 2026-04-30)" subsection.

**Frame**:
- Design reference: design doc § Phase 3 (locked) + PTF Lattice Hierarchy note (§5.1+§5.2 unlock-table per algebraic level)
- Obligations carried: `/detailed` enrichment, `sre-domain-meet` lookup, dual sweep (equality + subtype), distinguish full-sample vs hand-picked-6, report hypothesis-fired/total ratios, NO declaration updates (Discussion phase)
- Principles in play: **Decomplection** (sweep ≠ generator → separate file); **Data Orientation** (sd-finding records → markdown table is data); **Correct-by-Construction** (per-relation meet via Phase 3c registry, no callbacks)
- Mantra check: SD checks remain off-network sample-checks (existing Track 2G scaffolding lineage; labeled, NOT new debt)
- Drift risks (named, become mid-flight checkpoints): (1) confusing finding-reporting with declaration-updating; (2) under/overclaiming sample scope; (3) burying vacuous-triple ratios; (4) Move B+ surprise → dialogue NOT speculative commit; (5) perf wall on O(N³) sweep; (6) Q3 inclusion of test-distributive was deliberate (not creep); (7) Q2 generic-vs-typed slippage if a default-atoms param sneaks in later

**Implementation shape**:
- New file `racket/prologos/sre-property-sweep.rkt`: `run-sd-sweep`, `format-sd-findings`, `sd-finding` struct
- Sweep accepts atoms parameter (caller supplies)
- Sweep runs `{distributive, sd-vee, sd-wedge}` together per `(domain, relation)`
- Findings table lands in design doc § Phase 3 Findings
- Regression test: snapshot sweep output in `test-sre-algebraic.rkt`

## Phase 4 mini-design opening bookmark

Phase 3 sweep landed (`sre-property-sweep.rkt` + `tests/test-sre-sd-properties.rkt`) but surfaced a refutation for type×subtype distributive at depth-0 with witness `(Int, Nat, type-top)`. Audit revealed sweep semantic bug: `test-distributive` and `test-sd-{vee,wedge}/detailed` in sre-core.rkt hardcode `'equality` for the join, mixing lattices when meet-fn comes from a non-equality relation. Track 2H's claim was for `(meet-subtype, join-subtype)` (same lattice).

Per drift risk #4 dialogue mandate: NOT auto-committed; surfaced to user. User direction: open Phase 4 corrective.

**Phase 4 — Sweep Semantic Correction (Scaffolding-Hides-Truth corrective)**:
- Refactor 5 functions in sre-core.rkt + ~25 callsites across 4 files to take explicit `join-fn` parameter
- Mirrors Phase 3c's `meet-fn` pattern; retires hardcoded `'equality` constant (same shape as the retired callback — off-network state injection)
- Atomic commit per pipeline.md co-migration discipline
- After fix: re-run sweep; expected all 6 findings confirmed; if type×subtype STILL refutes → escalate

**Pattern naming**: "Scaffolding-Hides-Truth" — chosen 2026-04-30 dialogue. Replaces the historical "Move B+" placeholder. The pattern: principled refactor of scaffolding (defensive guards, hardcoded constants, callback parameters) surfaces real architectural facts the scaffolding was hiding. 3 data points after Phase 4 lands (Phase 2a, 3c, 4) → graduate to `DEVELOPMENT_LESSONS.org` as separate commit.

**Phase 3 status**: 🔄 in-progress — infrastructure built, but findings table population gated on Phase 4 fixing the sweep semantics first. Phase 3 close immediately follows Phase 4 close.

## Phase 4 close — sweep semantic correction (Scaffolding-Hides-Truth #3)

**Origin**: Phase 3 wider-sample sweep refuted distributivity for type×subtype with witness `(Int, Nat, type-top)`. Audit revealed sweep semantic bug — `test-distributive` + `test-sd-{vee,wedge}/detailed` in sre-core.rkt hardcoded `'equality` for the join lookup, mixing lattices when meet-fn came from a non-equality relation.

**Fix landed**: 5 function signatures refactored to take explicit `join-fn` parameter; ~25 callsites updated atomically across sre-core.rkt + sre-property-sweep.rkt + 2 test files. Mirrors Phase 3c's `meet-fn` discipline.

**Targeted suite GREEN**: 106 tests / 4.3s across test-sre-algebraic + test-sre-track2h + test-sre-sd-properties.

**Wider-sample sweep run** (102s, 58 samples at depth-1 with binders, 195112 triples per check):

| Domain | Relation | Property | Status | Triples | Hypothesis fired | Non-vacuity |
|---|---|---|---|---|---|---|
| type | equality | distributive | refuted (witness: Pi-typed triple) | — | — | — |
| type | equality | sd-vee | confirmed | 195112 | 6814 | 3.5% |
| type | equality | sd-wedge | confirmed | 195112 | 178382 | 91.4% |
| type | subtype | distributive | refuted (same witness) | — | — | — |
| type | subtype | sd-vee | confirmed | 195112 | 6786 | 3.5% |
| type | subtype | sd-wedge | confirmed | 195112 | 178166 | 91.3% |

**Bonus discovery — F7 conjecture empirically confirmed**: Track 2H's design § Phase 7 line 107 explicitly hedged "distributivity scope (F7): conjectured but not yet verified for binder substitution." Phase 4's wider sample is the first empirical answer: distributivity refutes at the binder boundary for BOTH relations. Witness involves Pi types: `(Pi(m1, Bool, Bool), Int, Pi(m1, Int, Bool))`.

The type lattice is **SD on full domain, distributive on ground sublattice only**. Track 2H quantale framing intact; the registration at unify.rkt:96 (`'distributive prop-confirmed`) is over-broad relative to the design body's F7 scope qualifier.

**Adversarial VAG passed** with one acknowledged scope-vs-registration mismatch surfaced as Discussion-phase concern (not Phase 4 commit-blocker).

### Lessons distilled

- **Scaffolding-Hides-Truth pattern's 3rd data point** lands. Phase 2a (with-handlers) + Phase 3c (always-installed callback) + Phase 4 (hardcoded `'equality` constant) all surface real algebraic facts the scaffolding was hiding. Pattern crosses Watching-table promotion gate → graduation to DEVELOPMENT_LESSONS.org as separate commit.
- **Pattern sub-form discovered**: Scaffolding-Hides-Scope. Phase 4's hidden truth wasn't unconditional ("the lattice IS X") but conditional ("the lattice is X on sub-A, Y on sub-B"). Track 2H's design body had F7 scope qualifier; the registration declaration erased it. This sub-form goes alongside the parent in DEVELOPMENT_LESSONS.org graduation.
- **Per-relation operation dispatch via registries codified across 3 axes**: merge-registry (Track 2F), meet-registry (Phase 3c), join-fn parameter (Phase 4). Pattern: "operations parameterized by relation belong as per-relation registries / explicit parameters, NOT as off-network callbacks or hardcoded constants."
- **Quantale ≠ distributive lattice**: Track 2H's quantale framing (tensor distributes over join) is orthogonal to lattice distributivity (meet distributes over join). The conflation in unify.rkt:96 declaration was real. Worth surfacing in PTF Lattice Hierarchy note + future research note on quantale distributivity laws.

## Phase 3 close (commit `<pending>`)

Findings table populated in design doc § Phase 3 Findings. Ground sublattice (depth-0): both relations confirm distributive + SD on 216/216 triples each. Wider sample (depth-1 with binders, 58 samples, 195112 triples): both refute distributive with Pi-typed witness, both confirm SD with asymmetric non-vacuity.

Tracker `🔄 → ✅`. Discussion-phase considerations preserved as out-of-band scope with cross-references to PPN 4C (scope-awareness mechanisms / Galois bridge framing) and PTF (research-note queue: quantale distributivity laws, substructural-logic non-distributivity, frontier dependent-type distributivity recovery).

## Pattern graduation — Scaffolding-Hides-Truth + Scaffolding-Hides-Scope sub-form

3rd data point landed (Phases 2a, 3c, 4). Crossed Watching-table promotion gate. Codified in `DEVELOPMENT_LESSONS.org` § Meta-Lessons, after "Pragmatic" and before "CALM Requires Fixed Topology" — same family of "ways scaffolding hides architectural reality."

**Scaffolding-Hides-Truth** (parent pattern): scaffolding doesn't just add overhead; it FALSIFIES observations by mixing with the underlying mechanism. The principled refactor that retires it surfaces facts the scaffolding was hiding. 3 instances catalogued with /hidden truth/ summarized for each.

**Scaffolding-Hides-Scope** (sub-form): scaffolding can be a registration declaration that elides scope qualifiers the design body acknowledges. Track 2H's F7 conjecture in the design body vs unconditional `'distributive prop-confirmed` registration is the canonical instance. Surfaces the property-registry-granularity gap as a sister concern.

The historical name "Move B+" (after PPN 4C S2.c-iii corrective sub-phase) is now retired in favor of the descriptive Scaffolding-Hides-Truth.

## Cross-link extensions

- **PTF Lattice Hierarchy note §4** ([`docs/research/2026-04-30_LATTICE_HIERARCHY_AND_DISTRIBUTIVITY_FOR_PROPAGATORS.md`](../../research/2026-04-30_LATTICE_HIERARCHY_AND_DISTRIBUTIVITY_FOR_PROPAGATORS.md)): worked-example timeline extended with Phase 4 instance. Codifies posture-mismatch-with-registration as Scaffolding-Hides-Scope sub-form. Quantale-vs-distributive orthogonality clarified.
- **PTF Master** ([`docs/tracking/2026-03-28_PTF_MASTER.md`](../2026-03-28_PTF_MASTER.md)): new "Queued Research Notes" section with 3 candidates: (A) Quantale distributivity laws — known literature, lower novelty; (B) Substructural / linear logic non-distributivity at function-type boundary — direct connection to QTT; (C) Recovering distributivity for unsolved dependent types — frontier, may need synthesis from dependent type theory research. Pragmatic alternative for (C): residuate until network grounds terms.
- **PPN 4C parent design doc** ([`docs/tracking/2026-04-21_PPN_4C_PHASE_9_DESIGN.md`](../2026-04-21_PPN_4C_PHASE_9_DESIGN.md)): new "Cross-track inputs (running log)" section captures the Phase 4 finding as open consideration. Sub-lattice scope-awareness, Galois-bridge framing, property-registry granularity gap surfaced as design considerations for in-flight PPN 4C scope.
- **SH Master** ([`docs/tracking/2026-04-30_SH_MASTER.md`](../2026-04-30_SH_MASTER.md)): "Cross-track inputs" section captures Scaffolding-Hides-Truth pattern as load-bearing reason for on-network correctness arc. Off-network state injection FALSIFIES observations; principled refactor surfaces them.

## Track 2I scope extension — Phases 5-10 added (presentation arc for Prof. J.B. Nation)

User picked up deferred Phase 3.5 + 3b work + extended scope substantially. Motivation: pseudo-complement / semi-complement checks arose from in-person conversation with Prof. J.B. Nation (Freese-Ježek-Nation 1995 *Free Lattices* author — THE Nation behind Theorem 1.21 SD theorem we cite throughout PTF). Closing phase is a presentation-quality research document for next meeting.

**Phase additions** (renumbered from deferred 3.5/3b for cleaner linear scope):

| Phase | Description |
|---|---|
| 5 | Pseudo-complement family — `test-pseudo-complement` (relative; Heyting →) + `test-semi-complement` (dual) + `test-stone-identity` (conditional). |
| 6 | Free-lattice + modularity — `test-whitmans-condition` (W; FL membership; O(N⁴) sweep) + `test-modular` + `test-breadth-bound` (Jónsson-Kiefer-Nation theorem) + `test-sectional-complement` (conditional). |
| 7 | Generator extension: session domain. |
| 8 | Generator extension: form domain. |
| 9 | Comprehensive sweep + findings synthesis (all properties × all `(domain, relation)` pairs). |
| 10 | **Lattice Variety Report** — new Stage 0/1 research note suitable for sharing with Nation. Cross-linked from PTF master. |

**Q1 alignment analysis**: technical-vs-theoretic value matrix established. Highest joint value: relative pseudo-complement, Whitman's W, modularity, breadth bound. Conditional follow-ons: Stone identity (if pseudo-complement confirms), sectional complement (if modular confirms). Deferred: atomistic checks.

**Scope discipline**: each phase opens with mini-design + mini-audit (Stage 4 protocol) when reached. High-level scoping captured in design doc; full design persists per-phase via dialogue. Discussion-phase folded into Phase 10's report (deprecated as separate row).

**Timing note**: Nation meeting is soon; user direction is "work through carefully" not rush. Done right matters more than done fast.

## Phase 5 mini-design opening bookmark

**Frame**:
- Design reference: design doc § Phase 5 (high-level scoping captured 2026-04-30; expanded with Decisions subsection)
- Obligations carried: Q1 (b) disambiguation; Phase 4 calling discipline (explicit meet-fn + join-fn from same relation); Phase 9 sweep needs Phase 5 properties
- Principles in play: **Correct-by-Construction** (explicit join-fn, no hidden lookup); **Decomplection** (4 orthogonal property checks); **Data Orientation** (pc-rel-evidence struct for non-vacuity richness)
- Mantra check: off-network sample-iteration scaffolding (existing Track 2G lineage); retirement direction = property-cells migration (sister track)

**Q2 research findings**: "semi-complement" not in Nation's lexicon (WebSearch + mempalace + project-materials grep all confirm). Nation's primary term is "**relatively complemented**" — Phase 5 family expanded to include `test-relatively-complemented` (Nation's canonical term). Mitigation: present both (a) dual-pc and (b) sectional in the report; let Nation clarify.

**Adversarial CRITIQUE_METHODOLOGY pass applied** (P/R/M/S two-column): surfaced 3 refinements (atomic property-registry rename; clarifying comment on existing `type-pseudo-complement`; YAGNI on /detailed variants — only `pc-rel-evidence`); 4 honest scope-acknowledgments (granularity gap punt; off-network scaffolding; Hasse not exploited; bridges-as-Galois implicit).

**Phase 5 family (4 checks)**:
- `test-pseudo-complement-rel` (Heyting →) — primary, /detailed variant
- `test-pseudo-complement-abs` (meet-zero) — sister
- `test-relatively-complemented` (Nation's term) — primary
- `test-stone-identity` — conditional on rel confirming

**Drift risks**: 7 named; asymmetric meet/join (recurring), sample-set sensitivity, Stone gating, rename cascade, registry granularity gap inherited, YAGNI on detailed, phase-size 1h boundary (sub-phase 5a/5b/5c fallback).

## Phase 5a close — pseudo-complement (relative + absolute) + atomic rename

**Sub-phase 5a complete.** Targeted suite GREEN (114 tests / 4.2s).

**Atomic rename**: `'has-pseudo-complement` → `'has-pseudo-complement-rel` across 4 sites: `sre-core.rkt:552` (heyting implication rule), `unify.rkt:116` (Track 2H type domain decl), `form-cells.rkt:510` (Track 2H form domain decl), `tests/test-sre-algebraic.rkt` (4 callsites + comment + 1 test-case name). Co-migration discipline (Phase 4 precedent).

**New code**:
- `lattice-leq?` helper in sre-core.rkt: `(equal? (meet-fn x y) x)`
- `pc-rel-evidence` struct (parallel to `sd-evidence`): status × total-checked × hypothesis-fired × conclusion-held × witness
- `test-pseudo-complement-rel/detailed` + wrapper: empirical check for `a → b = ⋁{x : x ∧ a ≤ b}`, candidate = sample-set join, axiom verifies candidate is in the set (= is the maximum)
- `test-pseudo-complement-abs`: 1-arg variant for `¬a = a → ⊥`
- Wired into `infer-domain-properties` (`props-6`, `props-7`) + `resolve-and-report-properties` evidence map
- Clarifying comment on `subtype-predicate.rkt:309` distinguishing context-relative absolute pc (existing) from lattice-theoretic relative pc (Phase 5 NEW)

**Phase 5a finding — net-new architectural claim**: type×equality reaches **Heyting on the ground sublattice**. Previously only type×subtype was declared Heyting (Track 2H). Phase 5a's empirical check on type-samples confirms `has-pseudo-complement-rel` for type×equality; combined with Phase 3c's `distributive prop-confirmed`, the implication rule fires `distributive + has-pseudo-complement-rel ⇒ heyting`.

Implication: equality-context type cells gain Heyting machinery (precise-incompatibility error reporting via pseudo-complement) on ground sublattice. Caveat per Phase 4: ground sublattice only; binders refute distributive → Heyting reach gated to ground.

**Test update**: `tests/test-sre-algebraic.rkt:213` — heyting expected updated `prop-unknown` → `prop-confirmed` reflecting the Phase 5a finding. Test rationale comment expanded to document the ground-sublattice scope.

**Stage 4 protocol applied**: mini-design + mini-audit + adversarial CRITIQUE_METHODOLOGY pass persisted before implementation; mid-flight principles challenge active during implementation; no diagnostic-protocol snags; phase tests pass; commit immediate; dailies updated; continuing to 5b.

**5a was NOT Scaffolding-Hides-Truth shape**: the finding was DESIGNED to be surfaced (Phase 5 mini-design said "may unlock Heyting reach for type×equality"). Confirmation, not surprise. Distinct from Phase 2a/3c/4 instances.

## Phase 5b close — relatively-complemented (Nation's primary terminology)

**Sub-phase 5b complete.** Targeted suite GREEN (117 tests / 5.0s).

**New code** in sre-core.rkt:
- `test-relatively-complemented domain samples meet-fn join-fn` — for each interval `[a, b]` in samples, for each `c ∈ [a, b]`, search samples for `d ∈ [a, b]` satisfying `c ∧ d = a` AND `c ∨ d = b`. If no such `d` found, refute with witness `(a b c)`. Returns axiom-* shape (no /detailed variant; non-vacuity less obviously informative for interval-wise check).
- Wired into `infer-domain-properties` (`props-8`) + `resolve-and-report-properties` evidence map.

**Cost**: O(N⁴) worst case (intervals × c × d-search). At ground sublattice (N=6), 1296 iterations — instant. At wider sample (N≈58), 11.3M iterations — heavy but tractable. Sample-set sensitivity: may falsely refute on wider samples when true relative complement exists outside sample. Flag for interpretation in Phase 9 sweep.

**Algebraic significance**: relatively-complemented is **Nation's primary terminology** for interval-wise complementation. Connects to:
- Eq X (partition lattice): algebraic, strongly atomic, semimodular, **relatively complemented**, simple (Nation Ch4)
- Theorem 10.10 (Dilworth 1950): relatively complemented PCF lattice = direct sum of simple (Nation Ch10)
- Theorem 11.3: every geometric lattice is relatively complemented (Nation Ch11)

This is the canonical check Nation will recognize. Most likely meaning of "semi-complement" in his in-person conversation per Q2 research.

**3 new test cases** in test-sre-algebraic.rkt: untested-without-fns, axiom-* shape on type domain, registry presence.

Sub-phase 5c (Stone identity, conditional on rel-pc confirming) remains.

## Phase 5c close + Phase 5 ADVERSARIAL VAG

**Sub-phase 5c complete.** Targeted suite GREEN (122 tests / 4.3s).

**Phase 5 substantively complete** (5a + 5b + 5c).

**5c new code**:
- `compute-abs-pc-candidate` helper: returns ¬a candidate from sample-set (used by Stone identity check)
- `test-stone-identity domain samples meet-fn join-fn`: for each atom a, compute ¬a + ¬¬a, verify ¬a ∨ ¬¬a = ⊤. Returns axiom-* shape. Sample-set sensitive — atoms whose ¬a doesn't exist on sample are skipped (not refuted).
- `stone-algebra` derived property + implication rule `distributive + has-pseudo-complement-rel + stone-identity ⇒ stone-algebra`
- Wired into `infer-domain-properties` (`props-9`) + `resolve-and-report-properties`
- 5 new Phase 5c test cases

**Phase 5 adversarial VAG passed** (two-column P/R/M/S applied). Findings:
- (a) On-network: Phase 5 EXTENDS Track 2G off-network scaffolding lineage; retirement direction = property-cells migration (sister track). Honest scaffolding label inherited.
- (b) Complete: all 4 family fns + 5 implication rules + ~25 atomic rename callsites + 14 new tests. Type×equality Heyting-on-ground BENEFIT captured (not just shape).
- (c) Vision-advancing: per-relation dispatch discipline (Phase 4) extended. Nation's "relatively complemented" first-class in registry.
- (d) Drift-risks-cleared: all 7 named risks materialized as expected or didn't fire. Sub-phase 5a/5b/5c discipline kept all under 1h conversational boundary.

**Phase 5a finding NOT Scaffolding-Hides-Truth shape**: positive finding designed to surface (per mini-design). Confirmation, not surprise. Distinct classification from Phase 2a/3c/4 instances.

**Q2 research outcome**: "semi-complement" not in Nation's lexicon (WebSearch + mempalace + project-materials confirm). `test-relatively-complemented` is the canonical-term match for Nation's primary terminology (Eq X partition lattice ch4, Theorem 10.10 Dilworth ch10, Theorem 11.3 geometric lattices ch11).

**Phase 5 deliverables snapshot**:
- 4 property-test functions: `test-pseudo-complement-rel/detailed` + wrapper, `test-pseudo-complement-abs`, `test-relatively-complemented`, `test-stone-identity`
- 4 new registry symbols: `'has-pseudo-complement-rel` (renamed), `'has-pseudo-complement-abs`, `'relatively-complemented`, `'stone-identity`
- 1 derived: `'stone-algebra`
- 2 new implication rules: `distributive + has-pc-rel ⇒ heyting` (renamed), `distributive + has-pc-rel + stone-identity ⇒ stone-algebra`
- 1 new helper: `lattice-leq?`
- 1 new struct: `pc-rel-evidence`
- 1 atomic rename across 4 sites
- 1 clarifying comment on `subtype-predicate.rkt:309`

**Tracker**: Phase 5 ⬜ → ✅ on this commit.

## Phase 6 mini-design opening bookmark

**Frame**:
- Design references: design doc § Phase 6 (high-level scoping + Decisions subsection persisted); Nation Notes ch9 (modular forbidden sublattices), ch10 (Theorem 10.10 Dilworth), Theorem 5.55/6.9 (Nation 1982 — (W) + SD ⟺ FL membership), Theorem 1.21 corollary (Jónsson-Kiefer-Nation breadth-4)
- Obligations carried: Phase 4 calling discipline, Phase 5 detailed-variant pattern, Phase 9 sweep needs Phase 6 properties
- Principles in play: **Decomplection** (3+ orthogonal axiom checks); **First-Class by Default** (each property is a registry symbol); **Correct-by-Construction** (explicit join-fn parameter)
- Mantra check: same Track 2G off-network scaffolding lineage; retirement = property-cells migration (sister track)

**Q1 reconsidered — sectional vs relatively-complemented**: NOT redundant. Distinct properties under Grätzer's *General Lattice Theory*:
- Relatively complemented (Phase 5b): all intervals `[a, b]` (Nation's primary; stronger)
- Sectionally complemented: principal ideals `[⊥, b]` only (Grätzer; weaker)
- Forward implication: rel-complemented ⇒ sect-complemented; reverse fails
- Phase 6 ADDS `test-sectionally-complemented` as 4th distinct check

**Phase 6 family (4 checks)**:
- `test-modular` + `modular-evidence` /detailed
- `test-whitmans-condition` + `whitman-evidence` /detailed
- `test-breadth-bound` (parameterized via `#:max-width`, default 4)
- `test-sectionally-complemented` (NEW; weaker than rel-complemented)

**Adversarial CRITIQUE_METHODOLOGY pass surfaced**:
- Q6 Hasse exploitation: breadth-bound is the FIRST Hasse-structural property check (antichain enumeration via incomparability adjacency)
- Modular subsumes distributive: type×equality ground sublattice should auto-confirm modular via implication; non-vacuity ratio distinguishes trivial vs genuine
- Phase 6 may surface ground-vs-binder distinction BELOW distributive (SD-not-distributive vs SD-not-modular vs modular-not-distributive)
- sre-core.rkt approaches whale-file threshold post-Phase-6 (~2000 lines); sister concern, not Phase 6 scope

**Implication rules added**:
- `distributive ⇒ modular` (forward; codifies SD ⊃ modular ⊃ distributive hierarchy)
- `relatively-complemented ⇒ sectionally-complemented` (forward; principal ideals are intervals)

**Drift risks**: 7 named (asymmetric meet/join, Whitman's W non-vacuity, breadth perf, modular-distributive trivial-confirm, phase-size 1h, /detailed proliferation, file-size watch). Sub-phase 6a/6b/6c fallback ready (modular / Whitman's / breadth+sectional).

## Phase 6 close — Free-lattice membership + modularity (✅)

**Phase 6 substantively complete.** Targeted suite GREEN (134 tests / 4.5s). Single-phase land — no sub-phase fallback needed (under 1h conversational stretch).

**4 property-test functions added**:
- `test-modular` + `modular-evidence` /detailed (Dedekind 1900 modular law: `a ≤ c ⇒ a ∨ (b ∧ c) = (a ∨ b) ∧ c`; hypothesis `a ≤ c` non-vacuity rich)
- `test-whitmans-condition` + `whitman-evidence` /detailed (Whitman 1941; Nation 1982 Theorem 5.55/6.9 — FL membership criterion with SD; O(N⁴) sweep)
- `test-breadth-bound` parameterized via `#:max-width` (default 4 per Jónsson-Kiefer-Nation 1962); enumerates (k+1)-element antichains
- `test-sectionally-complemented` (Grätzer's *General Lattice Theory*; principal-ideals only — distinct from Phase 5b's `test-relatively-complemented` for all intervals)

**2 implication rules added**:
- `distributive ⇒ modular` (forward; codifies SD ⊃ modular ⊃ distributive)
- `relatively-complemented ⇒ sectionally-complemented` (forward; principal ideals are intervals)

**Phase 6 finding (immediate test): type-samples breadth = 4**. Test asserts `breadth-bound k=3 → refuted` and `k=4 → confirmed`. The 4-atom antichain (Int, Bool, Nat, String) under flat type-lattice-meet IS pairwise incomparable — confirms breadth = 4 on the ground sublattice. Aligns with Jónsson-Kiefer-Nation: SD lattices have breadth ≤ 4. Empirical sample agreement.

**Q6 Hasse structural exploitation**: `test-breadth-bound` is the FIRST Track-2I property check that uses Hasse adjacency directly (incomparability = no Hasse edge). All prior checks (Phases 1-5) were sample-iteration over equality-or-leq predicates without exploiting topological structure. Phase 6 surfaces the architectural axis "checks that exploit Hasse" vs "checks that don't" — worth flagging in Phase 10 report.

**12 new test-cases** in test-sre-algebraic.rkt covering: untested-without-fns, /detailed shape, wrapper translation, parameterized-width-with-witness, axiom-* shapes, both implication rules, all-4-registry-presence.

**Property registry now has 13 active symbols** (5 inferred-only, 4 derivable, 4 with implication rules). sre-core.rkt grew to ~1990 lines — at the whale-file-splitting threshold per testing.md guidance. Sister concern noted (could spawn `sre-property-checks.rkt`); not Phase 6 scope.

**Adversarial VAG passed** at close. (a) On-network: scaffolding lineage extended honestly; (b) Complete: 4 fns + 2 rules + 12 tests delivered; (c) Vision-advancing: SD ⊃ modular ⊃ distributive hierarchy now codified in registry; first Hasse-structural check; (d) Drift-risks-cleared: all 7 named risks tracked; sub-phase fallback unused (single-phase succeeded).

**Phase 6 substantively closes the property-check infrastructure for Track 2I**. Phases 7-8 (session/form generator extensions) and Phase 9 (comprehensive sweep) are next. Nation report (Phase 10) gets a richer matrix.

## Phase 7 mini-design opening bookmark

**Frame**:
- Design references: design doc § Phase 7 (Decisions subsection persisted with revised three-sub-phase framing); Phase 3c precedent (per-relation meet-registry); PTF §5.4 (cross-domain bridges via Galois connections)
- Obligations carried: Phase 4 calling discipline, Phase 5 detailed-variant pattern, Phase 9 sweep needs Phase 7 atoms infrastructure
- Principles in play: **Decomplection** (cross-domain atoms via param); **First-Class by Default** (cross-domain-atoms as data); **Anti-Scaffolding-Hides-Truth** (API choice rejecting auto-discovery)
- Mantra check: same off-network sample-iteration scaffolding lineage

**Audit-driven scope expansion**: original Phase 7 was "generator extension only." Audit revealed `session-lattice-meet` already exists (Track 2G, `session-lattice.rkt:122`) but `meet-registry` was never wired into `session-sre-domain`. Phase 7 expands to three atomic sub-phases:
- 7a: Add `session-meet-registry` (sister of Phase 3c) — ~15 LoC
- 7b: Generator `#:cross-domain-atoms` parameter — ~50-80 LoC
- 7c: `realistic-session-atoms` + integration tests — ~30-50 LoC

**Q1 = (A)** explicit caller-supplied cross-domain-atoms. Auto-discovery (option B) was rejected on **anti-Scaffolding-Hides-Truth grounds** — same shape as the always-installed callback we retired in Phase 3c. Caller burden (one hash literal) is the cost of honest dependency surface.

**Q2 = (c)** `(list (sess-end) (sess-svar 0))`. Sweet spot: terminal + de-Bruijn-variable. Higher svar indices reject (out-of-context for un-nested mu); pre-baked compounds reject (defeat depth semantics); empty reject (only sentinels left after filter).

**Q3 = ADDRESSABLE NOW**: meet-limitation reframed. `session-lattice-meet` exists; just needs registration. Phase 7a unlocks all Phase 5/6 checks for session×equality sweep.

**Adversarial pass surfaced**:
- Cross-domain atom flow as Galois bridge (first explicit cross-domain bridge in Track 2I) — worth Phase 10 report mention
- Anti-Scaffolding-Hides-Truth pattern scales to API-design choices — not just defensive guards / callbacks / hardcoded constants
- Branch ctors (sess-choice/sess-offer) remain uncovered (variable arity, unregistered) — sister concern
- Mu recursion may surface 4th Scaffolding-Hides-Truth instance (out-of-binder-context sess-svar bodies)

**Drift risks**: 7 named (cross-domain leakage, no session subtype relation, branch ctors unregistered, mu malformed compounds, API change cascade, phase size, sub-phase ordering — 7a meet-registry MUST land before 7c sweep is informative).

## Phase 7 close — Session domain generator extension + meet-registry (✅)

**Phase 7 substantively complete.** Targeted suite GREEN (140 tests / 4.6s).

**3 atomic sub-phases landed**:
- **7a**: Added session meet-registry. **Surfaced pre-existing duplicate-registration bug**: `phase1d-registrations.rkt` was overwriting session-sre-domain registration with a stripped-down version (load order: phase1d body runs at driver.rkt:35, session-propagators.rkt's rich registration at line 65 transitively, BUT session-propagators.rkt is not actually in the production load chain — `(grep require session-propagators)` returned 0 production matches). The phase1d-registrations.rkt entry was the canonical registration; the rich session-sre-domain in session-propagators.rkt is dead-code-for-registration. Fix: replace register/minimal call in phase1d with explicit make-sre-domain including meet-registry. Also added (now-redundant) Phase 7a meet-registry to session-propagators.rkt for future when something imports it.
- **7b**: Generator API extended with `#:cross-domain-atoms` parameter through `generate-domain-samples` + `build-atoms-by-spec` + `run-sd-sweep`. Default empty hasheq (back-compat for type-only sweeps).
- **7c**: `realistic-session-atoms = (list (sess-end) (sess-svar 0))`; integration tests for session sweep mechanism (depth-0).

**Phase 7 surfaced bug**: duplicate registration pattern — `phase1d-registrations.rkt`'s `register/minimal` calls override richer registrations. Existed pre-Phase-7; surfaced by Phase 7's session-meet-registry test that found `meet-registry: #f` despite my session-propagators.rkt edit. **This is the 4th Scaffolding-Hides-Truth instance** of the session — `register/minimal` was scaffolding hiding the actual canonical registration site. Sister concern: audit other domains in phase1d-registrations.rkt for similar override patterns.

**Phase 7 architectural surfacings**:
- **Cross-domain atom flow as first explicit Galois bridge in Track 2I** (PTF §5.4 framing). Phase 7's `#:cross-domain-atoms` makes cross-domain dependency honest at call sites.
- **Anti-Scaffolding-Hides-Truth applied to API design**: rejecting auto-discovery (option B) for caller-supplied (option A) on principle that auto-magic hides cross-domain flow — same shape as the always-installed callback retired in Phase 3c.
- **Session-propagators.rkt's rich session-sre-domain is dead code for registration purposes**: surfaced as sister concern. Either bring session-propagators.rkt into load path (potential risk) or consolidate registration into phase1d.

**6 new test-cases** in test-sre-sd-properties.rkt: meet-registry wired (Phase 7a), generate-domain-samples cross-domain-atoms accepted (7b), run-sd-sweep returns 3 findings (7c), finding shape correctness, evidence types, format-sd-findings handles session.

**Adversarial VAG passed** at close. (a) On-network: same scaffolding lineage; (b) Complete: 3 sub-phases all delivered + bug fix; (c) Vision-advancing: cross-domain atom flow first-class + anti-Scaffolding-Hides-Truth pattern reapplied; (d) Drift-risks-cleared: phase1d duplicate-registration was the surprise — caught and fixed.

**Tracker**: Phase 7 ⬜ → ✅. Phase 8 (form domain generator extension) is next.

## Phase 8 mini-design opening bookmark

**Frame**:
- Design references: Phase 7a precedent (per-relation meet-registry sister addition); PTF Pocket Universe / lattice-embedding pattern; Phase 4 Scaffolding-Hides-Truth (declared property without empirical/constructive verification)
- Obligations carried: Phase 4 calling discipline, Phase 5 detailed-variant pattern, Phase 9 sweep needs Phase 8 meet + atoms
- Principles in play: **Correct-by-Construction** (declared has-meet now backed by actual meet); **Decomplection** (meet defined alongside merge sister); **Pocket Universe pattern** (respect embedded lattice + non-lattice metadata distinction)
- Mantra check: same off-network sample-iteration scaffolding lineage

**Audit-driven scope**: Phase 8 differs structurally from Phases 6-7. form-cell has NO ctor-descs (Pocket Universe wrapper, not ctor-decomposable). Standard generator compound-generation doesn't apply. Phase 8 is **sweep on hand-picked atoms with NO compound generation** + load-bearing **definition of form-pipeline-meet and spec-cell-meet**.

**5th Scaffolding-Hides-Truth candidate**: form-cells.rkt:508 declares `'has-meet prop-confirmed` but **no form-cell-meet function exists**. Phase 8a corrects.

**Q1 (meet on Pocket Universe wrapper)**: lattice-consistent semantics per field — set-intersect on transforms, idempotent-commutative on tree-node, filter-intersection on registrations, prefer-one on source-pos, per-key intersection on type-map. Makes wrapper algebraically consistent.

**Q2 (atoms — test wrapper or embedded?)**: mix (c) — constant-metadata atoms test embedded Boolean lattice; vary-metadata atoms test wrapper preservation. Surfaces whether declared Heyting holds on wrapper or only on embedded sub-lattice. **If wrapper refutes where embedded confirms → 5th Scaffolding-Hides-Truth finding**.

**Q3 ('spec-cell)**: include as **8d**, NOT defer. Per user lean — sub-phase out immediately applicable work. Symmetric mirror of 8a-c for spec-cell domain.

**Q4 (4-sub-phase plan)**: 8a (form-pipeline-meet), 8b (realistic-form-cell-atoms), 8c (form-cell sweep + tests), 8d (spec-cell mirror).

**Adversarial pass surfaced**:
- Pocket Universe / lattice-embedding pattern made empirical for first time in Track 2I
- Wrapper-vs-embedded algebraic-claim distinction empirically separable via Q2 atom design
- Hasse hypercube exploitation deferred (form-cell transforms IS Q_9; sister concern)

**Drift risks**: 7 named (asymmetric meet/join, metadata semantic choices, wrapper algebraic refutation expected, spec-cell different struct shape, no compound generation, phase size, Hasse hypercube deferred).

## Phase 8 close — Form-cell + spec-cell meet definition + empirical sweep (✅)

**Phase 8 substantively complete.** Targeted suite GREEN (153 tests / 4.5s).

**4 atomic sub-phases landed**:
- **8a**: Defined `form-pipeline-meet` in surface-rewrite.rkt (sister of `form-pipeline-merge`); wired `form-cell-meet-registry` into `form-cell-sre-domain`. **5th Scaffolding-Hides-Truth instance corrected**: form-cells.rkt:508 declared `'has-meet prop-confirmed` but no `form-cell-meet` function existed pre-Phase-8a. Now backed by actual meet.
- **8b**: Defined `realistic-form-cell-atoms` — mix of constant-metadata atoms (vary transforms only; test embedded Boolean lattice) + vary-metadata atoms (test wrapper preservation under metadata variation). 7 atoms total per Q2 mix design.
- **8c**: Sweep + 6 integration tests on form-cell×equality at depth-0. No compound generation possible (no ctor-descs).
- **8d**: Mirror for `'spec-cell` — defined `spec-cell-meet-fn` with lattice-dual semantics (top is identity, bot absorbs, idempotent on equal, different-specs → bot). 5 atoms; 7 tests.

**API extension surfaced + landed**: `run-sd-sweep` extended with `#:include-bot-top` keyword (default `#t` for back-compat). Caught by drift-risk discipline: form-cell top-value defaults to `#f` (no real top defined); generating samples with `include-bot-top #t` includes `#f` in samples; merge function (form-pipeline-merge) requires `form-pipeline-value?` arg → contract violation. Solution: caller passes `#:include-bot-top #f` for domains where bot/top inclusion isn't safe. Sister concern: should the generator validate bot/top compatibility with merge contracts? Defer.

**Mid-flight bug caught + fixed**: spec-cell-meet-fn first draft had condition ordering bug — checked `(not (type-surf old))` BEFORE `(top? old)`, but top values ALSO have type-surf=#f. Test "top ⊓ x = x" caught it; fix reorders top-check first. Same shape as merge function's existing top-first ordering. Lesson reinforced: meet/merge condition ordering on lattices with bot+top requires top-check first when top has bot-shaped fields.

**Phase 8 architectural surfacings**:
- **Pocket Universe / lattice-embedding pattern made empirical for first time in Track 2I**. form-pipeline-value is a wrapper struct: `transforms` (Boolean powerset embedded sub-lattice) + non-lattice metadata (tree-node, registrations, source-pos, type-map). The meet operates on ALL fields with lattice-consistent semantics; whether the wrapper algebraic claims (Track 2H Heyting declaration) hold under THIS meet is the empirical question. Phase 9 sweep + Phase 10 report covers the answer.
- **5th Scaffolding-Hides-Truth instance** in the form-cell declared-without-backing pattern. Pattern keeps firing: 2a (with-handlers), 3c (callback), 4 (hardcoded constant), 7 (duplicate registration), 8 (declared-without-backing).
- **Cross-domain Boolean-on-transforms-vs-non-Boolean-on-wrapper** distinction: form-cell's transforms is a true Boolean (powerset Q_9 hypercube — could exploit per BSP-LE Track 2 hypercube primitives). Sister concern; not Phase 8 scope.
- **Hand-picked atoms necessary for non-ctor-decomposable domains**: Phase 7 added cross-domain atoms; Phase 8 establishes that domains without ctor-descs need explicit atom enumeration. Generator's compound-generation doesn't apply.

**13 new test-cases**: 8a (3: meet-registry wired, set-intersect on transforms, idempotent on equal, different-tree-nodes), 8c (3: sweep returns 3 findings, finding shape, evidence types), 8d (5: meet-registry wired, top ⊓ x, bot ⊓ x, idempotent, different specs, sweep returns findings, finding shape).

**Adversarial VAG passed** at close. (a) On-network: same scaffolding lineage; (b) Complete: 4 sub-phases all delivered + meet semantics issue caught + API extension landed; (c) Vision-advancing: 5th Scaffolding-Hides-Truth corrected + Pocket Universe pattern made empirical; (d) Drift-risks-cleared: form-cell top-value-#f surfaced as expected; spec-cell condition ordering caught.

**Tracker**: Phase 8 ⬜ → ✅. Phase 9 (Comprehensive sweep + findings synthesis) is next.

## Phase 9 mini-design opening bookmark (2026-05-07)

**Frame**:
- Design reference: design doc § Phase 9 (high-level scoping captured 2026-04-30; expanded with Decisions subsection 2026-05-07)
- Obligations carried: extend `run-sd-sweep` from 3 properties → 10; extend `format-sd-finding-row` for 4+ evidence types; produce variety-placement summary + per-finding detail tables (Phase 10 consumer)
- Principles in play: **Data Orientation** (findings as data records); **Decomplection** (Phase 9 = empirical layer; Phase 10 = synthesis); **Correct-by-Construction** (variety placements via `derive-composite-properties`)
- Mantra check: off-network sample-iteration + format renderer; same Track 2G scaffolding lineage

**Q1 = (b) full depth-1 sweep**: ~45-60 min one-time wall time. Cost concentrated on type-domain (~40 min for Whitman's W + breadth-bound + rel-complemented at N=58). Justified by Phase 10 audience — these are EXACTLY Nation's most-relevant theorems (Theorem 5.55/6.9 Nation 1982; Theorem 1.21 Jónsson-Kiefer-Nation 1962). Skipping them at depth-1 = "we didn't run your theorems on wider sample."

**Q2 = (c) two tables**: variety-placement summary (boolean per variety level) + per-finding detail (with footnoted witnesses).

**Q3 = 9a + 9b**: code then data; interpretation = Phase 10.

**Q4 = (a) raw report**: conditionals interpreted in Phase 10.

**Q5 = (b) footnoted witnesses**: keeps table readable.

**Q6 = source-org sister concern**: sre-core.rkt at ~2000 lines IS production source-organization concern, NOT testing.md whale-file rule (which applies to TEST files only). Deferred. Phase 6 audit cited testing.md incorrectly; corrected.

**Q7 = `untested-reason` field on sd-finding**: explicit "not tested (no-relation)" reporting for cases like session × subtype. Honest coverage map.

**Drift risks**: 9 named (cost concentration, table-format complexity, conditional honesty, format cascade, atom availability, potential 6th Scaffolding-Hides-Truth, source-org, witness rendering, untested clarity).

**Adversarial CRITIQUE_METHODOLOGY pass applied** (P/R/M/S two-column): no surfaced refinements requiring design change; honest scope-acknowledgments captured.

## Phase 9a close — Sweep harness extension to all 11 properties + variety-placement summary

**Sub-phase 9a complete.** Targeted suite GREEN (158 tests / 4.0s).

**New code in sre-property-sweep.rkt**:
- `all-sweep-properties` exported list of 11 algebraic properties (PTF-hierarchy-ordered: free-membership / SD / modular / distributive / Heyting / complement family / Hasse-structural)
- Extended `run-sd-sweep` with `#:properties` keyword (default: `all-sweep-properties`); `untested-reason` detection for `'no-relation` / `'no-meet-fn` / `'no-join-fn`
- New `sweep-one-property` dispatcher — handles per-property dispatch with calling-convention awareness (test-breadth-bound has 3 positional args + keyword; others have 4)
- `sd-finding` struct extended with `untested-reason` field (Q7 — honest coverage map)
- `extract-detailed-fields` helper: uniform extraction across `sd-evidence`, `pc-rel-evidence`, `modular-evidence`, `whitman-evidence` (all share parallel field shape)
- `format-sd-finding-row` extended for 4 detailed-evidence types + untested-reason rendering
- New: `format-variety-placement-summary` — Q2(c) FIRST table; rows = (domain, relation), columns = boolean variety membership across PTF hierarchy levels (SD / Modular / Distributive / Heyting / Stone / Boolean / W). Variety placement derived from raw findings via implication chains (sd-vee ∧ sd-wedge → SD; distributive ∧ pc-rel → Heyting; etc.)
- `evidence-status` helper + `group-findings-by-domain-rel` helper

**Tests**:
- 4 existing count assertions updated to `(length all-sweep-properties)` (2× 11 = 22 for type×{eq,sub}; 11 for single-relation domains) — keeps tests robust as property list evolves
- 5 new Phase 9a tests: all-sweep-properties shape; untested-reason field present; explicit `'no-relation` for session × subtype (untested case); format-variety-placement-summary shape; explicit `#:properties` override produces narrower sweep

**Drift risks materialized**:
- Initial struct definition used `[untested-reason #:auto]` — Racket `#:auto` makes field uneditable from constructor. Fixed: regular 6th positional field.
- `test-breadth-bound` has 3 positional + keyword signature (Phase 6); dispatcher fixed to handle the asymmetry.

**Adversarial mid-flight principle check**: each new code path passed structured evidence extraction (P/Decomplection); the variety-placement summary derives via implication, not hand-coded mapping (Correct-by-Construction). No drift caught requiring corrective.

Sub-phase 9b (run comprehensive sweep + capture findings) remains.

## Phase 9b close — Comprehensive lattice variety sweep ✅

**Phase 9 substantively complete.** 110 findings captured via 4 concurrent per-domain sweep processes (one per domain; type domain further split per-relation for parallelism). Wall time: ~34 min (type domain dominated; per-relation parallelization halved vs sequential).

**Mid-flight refactor**: added `--relation` flag to `tools/run-phase9-sweep.rkt` to parallelize type domain into separate equality + subtype processes. Cut effective wall time from ~60 min sequential to ~34 min concurrent. Honestly named in dailies + design-doc adversarial VAG section.

**Variety-placement matrix populated** in design doc § Phase 9 Findings. 10 rows (domain × relation × depth), 7 hierarchy columns + Whitman's W + Notes.

**Headline findings**:

1. **Whitman's W ✓ for ALL 10 (domain × relation × depth) combinations** — every Prologos lattice is FL-embeddable per Nation Theorem 5.55/6.9. Strong non-vacuity on type (83-99% on the antecedent), unified empirical claim across the system.

2. **Both type×equality AND type×subtype reach Heyting on ground sublattice** — Phase 5a prediction validated comprehensively. New beyond Track 2H's subtype-only Heyting declaration.

3. **Asymmetry at binder boundary**: type×equality × wider is **SD + modular** (not distributive); type×subtype × wider is **SD only** (modular refutes with Pi-Pi-Pi witness). NEW finding beyond Phase 4. Equality merge preserves modularity through binders; subtype's GLB meet does not.

4. **Stone identity refutes for ALL 10** — no Prologos lattice reaches Stone-algebra level. Honest negative data — UCS dispatch path for Stone-algebra is not relevant to current domains.

5. **breadth-bound (k=4) refutes at wider samples** for type×{eq,sub}, session, form-cell. Most domains exceed 4-element antichain bound at wider samples. Relevant to parallel-decomposition fan-out estimates.

6. **Per-domain placements**:
   - type × eq × ground = **Heyting**; wider = **modular SD**
   - type × sub × ground = **Heyting**; wider = **SD-only** (Phase 4 + Phase 9 finding refined)
   - session × eq = **modular but not SD** (M3-like; both depths)
   - form-cell × eq = **SD but not modular** (both depths; Reading-Speyer-Thomas FTFSDL applies)
   - spec-cell × eq = **bottom of hierarchy** (only W + breadth ✓)

7. **Pseudo-complement decoupling on session × eq**: `has-pc-rel` ✗ but `has-pc-abs` ✓. Validates Q1(b) Phase 5 disambiguation decision empirically — they DO decouple in practice.

**No 6th Scaffolding-Hides-Truth instance**: drift risk #6 anticipated potential declared-vs-empirical mismatch surfacing. None did. All empirical results align with declarations or honestly refute with witness.

**Adversarial VAG passed** at phase close (P/R/M/S two-column). The mid-flight `--relation` flag addition was the most adversarially-scrutinized item — honestly framed as design-time decision, not hidden change.

**Phase 9 substantively closed.** Phase 10 (Lattice Variety Report for Prof. Nation) is the next move — synthesis + interpretation of Phase 9 findings into Stage 0/1 research note suitable for sharing.

## Phase 10 mini-design opening bookmark (2026-05-08)

**Frame**: Track 2I closing phase — Lattice Variety Report for Prof. J.B. Nation. Synthesis + interpretation of Phase 9 empirical findings into a Stage 0/1 research note suitable for sharing externally and bringing to in-person meeting. Different shape from prior phases — synthesis doc, not implementation.

**Audience-specific calibration** (per user from prior in-person Nation meetings):
- Nation oriented to dependent types via **Pi/Sigma ↔ universal/existential quantification** framing
- Nation is **expert on Free Lattices but NOT quantales** — referred to colleagues. Report should DEEMPHASIZE quantale framing; if mentioned, mark as "open territory we're exploring."
- Meeting in person — terser is better; doc supports the conversation, doesn't replace it.

**Locked decisions**:
- Q1 = (b) main doc + raw appendix files (already captured)
- Q2 = (b) brief Prologos-vocabulary orientation
- Q3 = (c) smallest representative witness + appendix reference; brief Pi/Sigma reminder for Nation
- Q4 = (c) Mermaid diagrams (variety hierarchy + binder-boundary asymmetry)
- Q5 = (b) conversation-starter framing for open questions
- Q6 = (a) zero Prologos-internal-roadmap context; if anything, lattice-theoretics framing
- Q7 = `docs/research/2026-05-08_PROLOGOS_LATTICE_VARIETY_CATEGORIZATION.md` markdown primary, PDF render for meeting
- Q8 = (a) top-down (matrix → per-domain → questions)
- Q9 = 400-600 lines (terser; in-person meeting context)
- Q10 = (a) lead with Whitman's W ✓ across all 10 combinations

**Honest scope acknowledgments**:
- Empirical sample-set claim, not constructive proof
- Stone identity universally refutes — honest negative
- Breadth bound exceeded at wider — honest about parallel-decomposition implications

**Drift risks (9 named)**: padding / pedagogy creep; witness verbosity; over-claiming on small samples; implementation digressions; Prologos-internal vocabulary leakage; conversation-starter framing slippage; citation discipline; **quantale over-framing** (new — Q6 refinement); scope creep into deeper internal report.

**Adversarial CRITIQUE pass applied**: P/R/M/S two-column. No drift requiring design change. Quantale-deemphasize is the biggest specific adjustment from prior assumptions.

**Follow-on items captured (NOT Phase 10 scope)**:
1. Web-app interactive network topology — color-coded demo with cells/propagators/strata for in-person exploration with Nation. Pickup after Phase 10.
2. Deeper internal lattice-variety report — complementary internal-audience version with full witnesses + quantale framing + Prologos-roadmap connections. Pickup after Phase 10.

## Phase 10 close — Lattice Variety Report for Prof. J. B. Nation ✅

**Track 2I substantively complete.** Phase 10 closing artifact landed.

**Artifact**: `docs/research/2026-05-08_PROLOGOS_LATTICE_VARIETY_CATEGORIZATION.md`. **272 lines** (under the 400-600 target — appropriately compact for in-person meeting prep). Cross-linked from PTF master Research Documents table (entry 4).

**Structure** (matches Q1-Q10 locks from mini-design):
1. Headline — Whitman's W ✓ across all 10 (Nation Theorem 5.55/6.9 directly applies)
2. Orientation: Prologos vocabulary ↔ lattice-theoretic terms (Pi/Sigma ↔ universal/existential)
3. Variety-placement matrix with Mermaid hierarchy diagram + binder-boundary asymmetry diagram
4. Per-domain commentary: type, session, form-cell, spec-cell — each with key witness in compact form
5. 6 open questions framed as conversation starters (substructural-logic framing of binder-boundary modularity refutation; pseudo-complement decoupling on session; SD-vee/wedge non-vacuity asymmetry; Whitman's W universality; Stone identity universal refutation; breadth bound exceeded)
6. "What we did not measure" honest scope section (Boolean / convex-geometry / Day's doubling / variety membership beyond W+SD)
7. Methodology note + References (Freese-Ježek-Nation 1995, Nation 1982, Jónsson-Kiefer 1962, Reading-Speyer-Thomas 2019, Adaricheva-Gorbunov-Tumanov 2003, Adaricheva-Nation 2017)
8. Appendix linking to raw per-domain findings markdown

**Quantale framing deemphasized** per audience calibration (Nation referred user to colleagues for quantale follow-up; report avoids quantale-overload). One mention in Q5/Q6 only.

**Lead choice rationale**: Whitman's W ✓ across all 10 is the strongest unified claim — direct application of Nation's own theorem to a deployed system across heterogeneously-designed domains. Sets up per-domain refinements as second-tier surprises.

**Honest scope acknowledgments included**:
- Empirical sample-set claim, not constructive proof (with non-vacuity ratios surfaced for transparency)
- Stone identity universally refutes (honest negative finding)
- Breadth bound exceeded at wider samples (most domains)
- has-complement not measured (Boolean placement uniformly "—")
- Convex-geometry / Day's doubling / variety membership beyond SD+W not directly tested

**Adversarial VAG at Phase 10 close** (P/R/M/S two-column):

| Question | Catalogue | Challenge |
|---|---|---|
| (a) On-network | N/A — synthesis doc, no propagator-network design | — |
| (b) Complete | All 10 (domain × relation × depth) findings reported; 6 open questions; raw appendix linked | Did we say what we DIDN'T measure? Yes — explicit "What we did not measure" section. |
| (c) Vision-advancing | Findings inform PTF lattice-hierarchy framework empirically; cross-linked from PTF master | Does the doc serve Nation's specific interests? Tested via Q6 quantale-deemphasis decision; per-relation focus on SD + W + modularity matches his expertise. |
| (d) Drift-risks-cleared | All 9 named risks navigated. Witness verbosity controlled (Q3 (c) compact rendering). Pedagogy creep avoided (Q9 terse target met). Quantale over-framing avoided (Q6 lock honored). | Did the report's tone match in-person meeting context? Yes — short, dense, conversation-starting. |

VAG passes adversarially. No drift caught requiring corrective.

**No 7th Scaffolding-Hides-Truth instance**: synthesis doc, not implementation. Pattern surveillance N/A.

## Track 2I — SUBSTANTIVELY COMPLETE 🎉

All phases closed:

| Phase | Status | Commit (key) |
|---|---|---|
| 1 (test fns + wiring) | ✅ | `a35d5f65` |
| 2 (sample generator) | ✅ | `f241e14e` |
| 2a (Scaffolding-Hides-Truth #1) | ✅ | `1c0c012e` |
| 3c (Scaffolding-Hides-Truth #2) | ✅ | `d4e8c811` |
| 3 (sweep run + findings) | ✅ | `520d3af8` |
| 4 (Scaffolding-Hides-Truth #3) | ✅ | `34762af0` |
| 5a/b/c (pseudo-complement family) | ✅ | `b2751dd2` / `2a8fc7ea` / `627c8d18` |
| 6 (free-lattice + modularity) | ✅ | `8151ae4e` |
| 7 (session domain; Scaffolding-Hides-Truth #4) | ✅ | `b41a3c07` |
| 8 (form/spec-cell; Scaffolding-Hides-Truth #5) | ✅ | `2d25076c` |
| 9a/b (comprehensive sweep) | ✅ | `8d3257e7` / pending |
| 10 (Nation report) | ✅ | this commit |
| T (test file) | ✅ | (Phase 3 era; expanded throughout) |

**Track 2I delivered**:
- 11 algebraic-property checks first-class in SRE registry
- 4 domains with meet-registry + sample-generator support (type, session, form-cell, spec-cell)
- Per-relation operation dispatch discipline codified (merge-registry, meet-registry, explicit join-fn)
- 5 Scaffolding-Hides-Truth findings + pattern graduated to DEVELOPMENT_LESSONS.org
- Comprehensive empirical variety-placement matrix across our system
- External-facing research note for Prof. Nation
- ~150-200 test cases across test-sre-algebraic.rkt + test-sre-sd-properties.rkt

**Follow-ons captured** (not Track 2I scope; queued separately):
1. Web-app interactive network topology demo (color-coded for in-person Nation exploration)
2. Deeper internal lattice-variety report (full witnesses, quantale framing, Prologos-roadmap connections, internal-team audience)
3. PTF research notes A/B/C queued (quantale distributivity laws; substructural non-distributivity; recovering distributivity for unsolved dependent types)
4. PDF render of Phase 10 report for the meeting (markdown → PDF post-process)

## Track 2I scope reopening — Phases 11-15 added (2026-05-08)

After Phase 10's "first close," user noted that several gaps in the report's "What we did not measure" section are too interesting to leave for Nation. Reopening Track 2I with 5 new phases addressing all 5 named gaps:

| Phase | Tier | Description |
|---|---|---|
| 11 | T1 | `has-complement` empirical check (Boolean placement) |
| 12 | T1 | M3 / N5 sublattice detection (Birkhoff theorem) — concrete witnesses for distributivity refutation |
| 13 | T2 | Convex geometry / anti-exchange characterization (Adaricheva-Gorbunov-Tumanov 2003) — AGT 2003 is recommended in Nation's own lattice notes |
| 14 | T3 | Targeted congruence tests — specific candidates (multiplicity-forgetful kernel, canonical-form projection) |
| 15 | T3 | Day's doubling / inflation detection (Adaricheva-Nation 2017) — Nation's own co-authored work |

**Decisions** (Q-A through Q-D 2026-05-08 mini-design dialogue):
- Q-A = (c) all three tiers — Phases 11-15
- Q-B = (iii) extend within Track 2I, no rename
- Q-C = (1) update existing `2026-05-08_PROLOGOS_LATTICE_VARIETY_CATEGORIZATION.md` in place once Phases 11-15 land
- Q-D = per-phase mini-design discipline (Stage 4, full)

**Rationale**: Phases 11+12 (Tier 1) are cheap and directly Nation-relevant — Boolean placement + Birkhoff M3/N5 witnesses fill clean gaps in the variety-placement matrix. Phase 13 (Tier 2) connects to AGT 2003 explicitly recommended in Nation's lattice notes. Phases 14+15 (Tier 3) are more speculative but Phase 15 specifically references Nation's OWN 2017 co-authored paper.

**Track 2I "first close" framing preserved** in design-doc Phase 10 row — honest acknowledgment that we declared close at Phase 10 then reopened. Will mark "second close" after Phase 15 lands.

**Report update plan**: existing 2026-05-08 markdown gets updated in-place as Phases 11-15 land. Variety-placement matrix grows columns; "What we did not measure" section shrinks; open questions get refined.

**Honesty scope**: the report's "What we did not measure" section already updated to reflect imminent Phase 11-15 gap closure; broader frontiers (variety subclassification, Aut(L), continuity) explicitly remain out of scope.

## Track 2I — session close summary

**Substantively closed.** Commits this session (in chronological order):

1. `bd1179b1` — Phase 3 sweep infrastructure (in-progress; sweep + format + test file)
2. `cd93a7b9` — Phase 4 mini-design persistence
3. `34762af0` — Phase 4 implementation: sweep semantic correction (Scaffolding-Hides-Truth #3)
4. `520d3af8` — Phase 3 close: findings table populated
5. `f06ccd7a` — Pattern graduation: Scaffolding-Hides-Truth + Scaffolding-Hides-Scope sub-form to DEVELOPMENT_LESSONS.org
6. `52c07afd` — Cross-link extensions: PTF (note §4 + master research-note queue) + PPN 4C + SH master

**Phase status**:
- Phase 1, 2, 2a, 3c, 3, 4 ✅
- Phase T ✅ (realized early as `test-sre-sd-properties.rkt`)
- Phase 3.5 ⬜ deferred (pseudo-complement empirical check, ~30-50 LoC)
- Phase 3b ⬜ deferred (session/form domain sweeps, requires generator extension)
- Discussion 🔄 in-progress (substantive considerations recorded; may fold into PPN 4C work or spawn separate artifact)

**Goals achieved**:
- ✅ SD∨/SD∧ as first-class algebraic-property checks
- ✅ Empirical sweep machinery generic over (domain, relations, atoms, depth)
- ✅ Type lattice's algebraic posture mapped: distributive on ground sublattice; SD on full domain (binder-included)
- ✅ Track 2H F7 conjecture empirically validated for the first time
- ✅ Phase 3c distributivity-flip discovery validated on wider sample (ground sublattice — 216/216 in tests)
- ✅ Quantale-vs-lattice-distributivity orthogonality clarified (Track 2H quantale framing intact; lattice-distributivity over-broad)

**Open work** (not Track 2I scope):
- Phase 3.5 + 3b (deferred phases within Track 2I; can pick up later)
- Sister callback retirement (`current-lattice-meta-solution-fn` → PM Track 12)
- Discussion-phase considerations (8 surfaced 2026-04-30, multiple cross-cutting)
- Research-note queue at PTF (A/B/C) — picked up as Stage 0/1 work later
- Scope-awareness mechanisms (Galois bridges between sub-lattices) — open consideration in PPN 4C
- MASTER_ROADMAP.org stale (user flagged 2026-04-30; not addressed)

**Lessons distilled this session** (all already codified in DEVELOPMENT_LESSONS.org / dailies):
- Scaffolding-Hides-Truth pattern crossed promotion gate (3 data points → graduated)
- Scaffolding-Hides-Scope sub-form codified alongside (registration declarations eliding design-body scope qualifiers)
- Pattern naming discipline: descriptive over historical-accident ("Move B+" → "Scaffolding-Hides-Truth")
- "Locked decisions" framing replaced with "Decisions" — implies revisability without false permanence
- Per-relation operation dispatch via registries codified across 3 axes (merge-registry, meet-registry, join-fn parameter)
- A merge-changing track is implicitly a property-status-changing track — algebraic declarations should be re-validated, not inherited
- Quantale ≠ distributive lattice — orthogonal axes; conflation can hide in registration declarations

---

## Lessons distillation check (per workflow.md)

Scanned: most recent PIR (BSP-LE Track 2B, 2026-04-16), past 4 dailies
(2026-04-26 covers 4 calendar days; 2026-04-23, 2026-04-22, 2026-04-19).

Most distilled lessons from the last 2 weeks have already landed in
`workflow.md`, `DESIGN_METHODOLOGY.org`, `DEVELOPMENT_LESSONS.org`:
- Adversarial VAG two-column framing
- Microbench claim verification per-sub-phase
- Per-domain universe migration co-migration sites (pipeline.md)
- "Validated ≠ Deployed" gate
- Belt-and-suspenders is a blocking red flag
- Ban "pragmatic" as justification for dual paths
- NTT model REQUIRED
- Network Reality Check (3 binary questions)
- Set-latch as prime fan-in design pattern (propagator-design.md)

No new graduations needed at this interval-open. The Watching table
above tracks medium-term patterns awaiting promotion gates.

---

## Research conversation: concurrency primitives for the LLVM substrate (2026-05-02)

Long research dialogue grounding what concurrency primitives the Zig
kernel needs to reach competitive parallel BSP, given the architectural
commitments already in place (`.pnet` → LLVM directly, propagator
network IS IR, thin Zig scheduler, Pocket Universes as the unifying
sub-network primitive).

### Arc of the conversation

1. **Initial framing** — earlier turns surveyed the Spera capability /
   non-compositionality papers (filed 2026-04-23 in research/), then
   moved through Vat / DCR / Fission Codes / IPVM / UCAN territory
   for the multi-agent vision. Acknowledged a misalignment in the
   first capability research note (vocabulary conflation between OCap
   and agentic-AI senses); corrected in §1, §3.1, §8 of that note.
2. **Free Lattices research conversation** — Chapter I of the Freese-
   Nation companion (`docs/learning/freelat-companion-ch01.html`).
   Discussed Whitman's algorithm, canonical form, semidistributivity,
   continuity, the limit case. Connected to PUnify recursive descent
   (Whitman-shaped), Hyperlattice Conjecture optimality strengthening,
   "cells as elements of FL_V(X)" framing where V is the smallest
   variety containing the domain's equations. Type lattice nominated
   as the right pilot domain for canonical-form work.
3. **Performance research arc** — given `.pnet` → LLVM prototype
   shows 10,000× speedups, asked what the absolute fastest-known
   primitives are. Initially recommended MLIR; corrected after user
   pushback (MLIR splits the optimization-IS-IR property the
   super-optimizing-compiler note commits to). Accepted the
   architectural frame: direct `.pnet` → LLVM with thin Zig scheduler.
4. **Branch assessment** — read the in-flight prototype branch
   `claude/prologos-layering-architecture-Pn8M9` (collaborator's work,
   not mine). Reviewed `runtime/prologos-runtime.zig` (544 LOC
   sequential BSP), `BSP_NATIVE_SCHEDULER.md`, `KERNEL_POCKET_UNIVERSES.md`
   (Option C recommended; awaiting selection). Surfaced steering
   recommendations: cache-line cell layout from the moment cells go
   dynamic; per-(worker, PU) write logs + cell-partitioned merge;
   persistent worker pool with futex park (not thread-fork-per-round);
   sequence Sprint D before PUs land if possible. Collaborator has
   already gone PU-first per user note.
5. **PU vs scheduler interplay** — concrete question from collaborator:
   should each PU have its own scheduler? Resolved: PU is a namespace
   of state, scheduler is execution machinery. One worker pool
   process-wide, per-PU scheduling state, per-(worker, PU) write logs,
   add `pu_run_parallel` batched primitive for sibling PU concurrency.
   Worker-binding-per-round is the load-bearing contract.
6. **Research note drafted** —
   `docs/research/2026-05-02_CONCURRENCY_PRIMITIVES_LLVM_SUBSTRATE.md`
   (~5800 words, 12 sections, 25+ references). Stage 1 synthesis;
   no design commitments. Filed in `docs/research/` per pattern of
   companion SH notes. Ran 5 focused websearches before drafting
   (Chase-Lev canonical, EBR vs HP vs RCU, mimalloc internals + Zig
   FFI, Soufflé EQREL, TigerBeetle production lessons).

### Key things that crystallized in the note

- **EBR is correct here precisely because BSP gives explicit phase
  boundaries.** The "blocking" pathology of EBR (a stalled reader
  holding back reclamation) cannot occur because no reader survives
  a round boundary. The same property that makes EBR risky in general
  workloads makes it strictly better than hazard pointers for ours.
- **Lê et al. 2013 has a known integer-overflow bug** that Chase-Lev
  2005 doesn't have. Crossbeam-deque (Rust) and ConcurrentDeque (C++17)
  carry the corrected version. Port from those, not the 2013 PDF.
- **Mimalloc v3 changed its threading model** from v2 (no more
  thread-local segments). FFI integration via `@cImport` is direct;
  `mi_heap_new()` per worker for arena-style allocation.
- **TigerBeetle's deterministic simulation testing posture transfers
  to `.pnet` round-trip verification** even though their workload
  (single-threaded financial OLTP) differs from ours.
- **The Sprint D MVP I'd actually recommend** (named in §10 Q6):
  Chase-Lev deque + persistent worker pool + futex park + per-(worker,
  PU=root) write logs + cell-partitioned merge + sense-reversing
  barrier. ~800 LOC of new Zig. EBR / comptime per-tag batches /
  `pu_run_parallel` follow as Sprint D+1/D+2 gated on measurement.
- **Three calibration measurements on the current sequential prototype**
  (~2 hours work) answer most open questions about which primitives
  matter at our actual scale: per-round wall time, tag distribution,
  false-sharing microbenchmark.

### What's queued downstream of this note

- Sprint D design doc on the prototype branch — collaborator's call,
  not mine. Note is informational input.
- Calibration runs (the three measurements above) on the current
  sequential prototype — could be done before any concurrency code
  lands; would anchor primitive choices empirically.
- Companion note on PReduce / Track 9's effect on per-cell merge
  cost — when PReduce introduces tropical-quantale cost merges,
  per-cell merge becomes more expensive, which makes the cell-
  partitioned merge architecture even more load-bearing. Re-measure
  post-PReduce.
- Free-lattice canonical form work for the type-lattice pilot —
  separate research thread; orthogonal to the concurrency note. Worth
  a Stage 1 note of its own when that arc resumes.

### Capability-as-Datalog research note correction (2026-05-02)

Earlier in this session, corrected vocabulary conflation in
`2026-04-23_CAPABILITY_SAFETY_DATALOG_HYPERGRAPHS.md`:
- §1 (Vocabulary disambiguation) — clarified Spera papers use
  "capability" in agentic-AI / tool-use sense, NOT OCap sense; the
  three traditions (Spera, OCap, Prologos's effect-capability types)
  are now distinguished honestly.
- §3.1 — softened "OCap-style authority gating" to
  "OCap-influenced authority gating" with note that the theorem's
  math is independent of which capability tradition.
- §8 — executive summary updated to match.
The structural correspondence (transitive closure under conjunctive
composition rules with provenance) stands; only the vocabulary
identification was wrong. Math is unchanged.

### Watching (medium-term)

- **PReduce / Track 9 ↔ concurrency interaction**: tropical-quantale
  cost merges change the per-cell merge cost profile. Worth re-
  measuring once PReduce lands; the cell-partitioned merge
  architecture's value scales with merge cost.
- **Determinism story for `.pnet` round-trip verification**: TigerBeetle's
  DST is the gold standard. Multi-thread parallel BSP is intrinsically
  non-deterministic (work-stealing order varies). Two paths surface in
  the concurrency note §10 Q3: (a) deterministic mode that pins
  workers + steal-orders for verification only; (b) accept
  non-determinism in production but maintain deterministic
  single-threaded mode for verification. Resolve in concert with
  Track 10.
- **Free-lattice canonical form ↔ SRE engineering**: type-lattice
  pilot identified as the right place to start. Variety identification
  (which equations hold) is empirical work that precedes any algorithm
  implementation. Adding SD∨/SD∧ to the SRE algebraic-property check
  registry is the small immediate move that gets us calibrated.
