# Dailies — 2026-03-24

## Carried Forward from Previous Interval

### PM Track 8F: COMPLETE
- cell-id in expr-meta, cell-id zonk paths, resolution zonk elimination, ground-expr? cell-id
- Suite: 244.3s → 232.9s (4.7% improvement)
- PIR: `docs/tracking/2026-03-24_PM_8F_PIR.md` (`ba55f52`)
- Deferred: Phase 5 (defaults at solve-time), Phase 7 (CHAMP fallback → Track 10)

### PM Track 10: UNBLOCKED by 8F
- Module loading on-network, CHAMP fallback removal, zonk elimination, box elimination
- Absorbs: 8F Phase 5 (defaults), 8F Phase 7 (CHAMP fallback), 13 remaining zonk calls, id-map elimination
- May become Track Series given scope

### SRE Series Status
- Track 0: ✅ (form registry)
- Track 1+1B: ✅ (relation-parameterized, subtype/duality)
- Track 2: ✅ (classifier → SRE dispatch, O(1) prop:ctor-desc-tag)
- Track 2B: Pending (polarity inference — new feature, separate design)
- Tracks 3-6: Pending (trait resolution, pattern compilation, sessions, reduction)

### Known Bugs / Gaps (carried)
- 5 punify parity bugs with toggle ON
- 5 L3 parser bugs from acceptance file
- PUnify toggle flip: may be unblocked by REPL fix (needs verification)

### Process Improvements Delivered in Previous Interval
- REPL/process-string hang fixed (`29a1fad`)
- Test runner bail-out messaging improved (`4211a78`)
- 4 workflow rules: separate compile/test, 3-timeout=revert, "pre-existing" smell, full output capture
- DESIGN_METHODOLOGY.org: Stage 2 measurements, Stage 3 Pre-0 benchmarks, Stage 4 A/B + PIR
- Dead instrumentation cleanup: subtype-check-count gated (`3339fec`)

### Watching (medium-term patterns, updated)
- **D.1 estimates systematically wrong**: 3 instances (PUnify 5c, SRE T2, PM 8F). Need estimation methodology improvement — concrete measurements BEFORE design, not estimates from vision. Partially addressed by Stage 2 methodology update. Watch for recurrence.
- **Benchmark as design input**: 4 confirmed instances. CODIFIED in DESIGN_METHODOLOGY.org.
- **"Pre-existing" as process smell**: 2+ instances. CODIFIED in MEMORY.md workflow rules.
- **Dead instrumentation accumulates**: 1 instance (subtype-check-count). Watch for more. Consider periodic instrumentation audit as part of track cleanup.
- **Agent bulk edits need verification**: 1 instance (accessor/constructor confusion). Watch for recurrence.
- **Suite time trajectory**: 247.5→244.3→238→235.7→232.9s. Steady downward across PM 8F. Monitor for regression.

---

## Lessons Distillation Check

### Scanned: PM 8F PIR + March 23 dailies

**Ready for codification**:
1. "Gate debug assertions/instrumentation behind flags" — 2 instances (bvar-free?, subtype-check-count). Both caused measurable overhead in production. **Action**: Add to DEVELOPMENT_LESSONS.org.
2. "Value-equivalence ≠ identity-equivalence" — 1 instance (zonk-at-depth). Not yet 3 instances. Watching.
3. "Audit before designing" — 3 instances but already addressed by DESIGN_METHODOLOGY Stage 2 update. Done.

**Pending from PM 8F PIR §17**:
- "Gate debug assertions" → DEVELOPMENT_LESSONS.org (pending)
- "Audit before designing" → DESIGN_METHODOLOGY.org (done, `cf3a856`)

---

## Today's Work

### Completed

- **NTT Syntax Design round 5 (SRE Track 1 considerations)** — incorporated NTT insights into design
  - `:where` as catch-all for laws/properties, `property` for named groups
  - `:stratum` removed from propagator (contextual, not inherent)
  - `interface` replaces `schema` for network types
  - One-way bridge implicit from missing α or γ
  - `:scheduler` cascade scoping (outer default, inner override)
  - Fixpoint modalities: lfp, gfp, stratified, approximation, metric, mixed
  - Coinductive types (`codata`) for foreign type structure
  - NF-Narrowing as structural inference source

- **NTT Case Studies: Architecture Survey + Type Checker Deep Study**
  - Survey: 7 systems as NTT skeletons, 5 gaps found
  - Type Checker: Full NTT spec, `expr-meta` as deepest impedance mismatch
  - Key finding: SRE more central than expected — handles merge, constraints, meta→cell, zonk elimination

- **SRE Track 1+1B completion + PIR** (`9a4e8a6`)
  - Lessons distilled: non-equality relations break equality assumptions, benchmark as design input
  - Distilled to DESIGN_METHODOLOGY.org + DEVELOPMENT_LESSONS.org (`eca77b4`)

- **SRE Track 2 completion + PIR** (`70cb5f7`)
  - O(1) dispatch via `prop:ctor-desc-tag` on syntax.rkt structs
  - Classifier → SRE delegation for 10 structural cases
  - Key learning: elaborator was already store-agnostic (0 typing-core changes needed)

- **PM 8F completion + PIR** (`ba55f52`)
  - cell-id in expr-meta, cell-primary reads, zonk cell-id fast path
  - REPL/process-string hang fixed (`29a1fad`)
  - Suite: 244.3s → 232.9s (4.7% improvement)

- **Research: Hypergraph Rewriting + Propagator-Native Parsing** (`e394874`)
  - SRE decomposition IS DPO hyperedge replacement (Engelfriet-Heyker)
  - Parsing on network is natural extension of what we already built
  - PPN series sketched: 6 tracks from lattice design through incremental editing

- **Research: Tropical Optimization + Network Architecture** (`7510c1a`)
  - Tropical semirings for cost-bounded exploration (replaces fuel limits)
  - ATMS-guided search for parsing/rewriting (nogood learning)
  - Unified compiler stratification (NTT syntax sketch)
  - OE series: cross-cutting research that all other series consume

- **PPN + OE series added to Master Roadmap** (`7510c1a`)

- **Performance investigation: prelude loading tax** (no commit — analysis only)
  - Identified ~55s regression from First-Class Paths (March 20)
  - Root cause: 4 new AST nodes across 14 pipeline files → more match arms → slower prelude module elaboration
  - Bimodal: 313 files unaffected, 28 prelude-heavy tests +10-16s each
  - **Key insight**: this is Track 10's problem to solve — persistent prelude eliminates the structural tax
  - Every new AST node compounds the tax; Track 10 makes it O(1)

- **PM Track 10 Stage 2 Audit** (`c95ac6e`)
  - Comprehensive module loading infrastructure audit
  - Module loading is entirely imperative (no live network during elaboration)
  - 41-parameter parameterize scope per module load (even cache hits)
  - Prelude does NOT re-elaborate per test (cache hits) but scope creation costs compound
  - 306 `with-fresh-meta-env` calls need subnetwork scoping

- **PM Track 10 Stage 3 Design D.1** (`8b63a06`)
  - 7-phase design: network-active loading → persistent prelude → subnetwork tests → parameter elimination → 8F deferrals → snapshot retirement → verification
  - Key mechanism: CHAMP fork for subnetwork scoping
  - Performance targets: suite <180s, prelude-heavy <30s, cache-hit <0.05ms

- **Track 10 Pre-0 benchmark suite** (in progress)
  - 20+ measurements across 7 categories
  - E2e path (full `(ns bench)`) plus isolation micro-benchmarks

---

## Design Decisions

- **Stage 2 audit as separate document**: Applied the PM 8F methodology lesson — concrete code measurements in a standalone audit doc, feeding the Stage 3 design. This prevented the vision from contaminating the audit.
- **CHAMP fork as isolation mechanism**: Structural sharing via persistent data structure, not parameterize scoping. Copy-on-write guarantees isolation. O(1) fork. Composes transitively (prelude → file → command → speculation).
- **Pre-0 benchmarks BEFORE design iteration**: Applied the "benchmark as design input" methodology. The D.1 draft was written with targets; the benchmarks will validate or invalidate those targets before D.2.

## Lessons

- **"Pre-existing issue" is a process smell**: The REPL hang was dismissed for multiple sessions. Investigation cost: 10 lines, 30 minutes. Workaround cost: 2+ hours. Codified as workflow rule: file immediately, estimate workaround cost, escalate if >30min/session.
- **Prelude loading tax is structural, not algorithmic**: Adding match arms to pipeline files slows ALL module loads. The fix is architectural (persistent prelude), not tactical (reorder arms). Every new AST node compounds the debt.
- **The elaborator was already store-agnostic**: SRE Track 2 estimated 158 typing-core changes; actual was 0. The audit-first methodology caught this, but the VISION (elaborator-as-thin-walker) initially overwrote the AUDIT (elaborator-already-delegates).

## Considerations

- **PM Track 10 Design D.1-D.3** (`8b63a06` through `ab3eed3`)
  - D.1: 7-phase design grounded in Stage 2 audit
  - D.2: Pre-0 benchmarks revealed parameterize (3.4μs) and cache-hit (6.5μs) are NEGLIGIBLE — the 20s cold-start is the only real cost. `.pnet` serialization proposed.
  - D.3 self-critique (6 findings):
    1. CRITICAL: `fasl` NOT AVAILABLE in Racket 9.0 → `struct->vector` + tag dispatch (verified 40/40 modules)
    2. Gensym serialization: `symbol$$N` tagging (verified 40/40)
    3. Foreign function re-linking: `(foreign-proc name)` (22/40 modules affected)
    4. Missing serialization items: specs (614), def-locations (30,908), registry contributions
    5. Unified prelude `.pnet`: modules + registries + persistent cells in one serialized network
    6. Three-layer fork: prelude → test-file → test-case (verified via code audit: struct-copy safe, tests independent)
    7. Parameter reduction: 21 → 3 (code-verified classification of all 21 params)
    8. Test-granular scheduling: per-test work items via Places, eliminates tail effect
  - 18 open questions all resolved
  - External critique in progress

- **Lessons from Track 10 design process**:
  - Pre-0 benchmarks changed the design FUNDAMENTALLY (D.1 was wrong about the bottleneck)
  - Self-critique code audit caught 3 wrong D.2 claims (fasl, write/read, size estimate)
  - Testing serialization on REAL modules (not toy examples) found 22/40 failures from foreign procs
  - Unified .pnet vision emerged from "can registries be cells?" question — not in original design

- **PM Track 10 Phase 0** (`50169c8`): Acceptance file (43 results, 0 errors) + ns-context pipeline fix
  - ns-context struct leaks into type expressions — handled in zonk, substitution, reduction
  - IDEAL vs WORKING syntax gaps documented as learning: trait WS, pipeline parse, Pair export, length/Reducible, polymorphic zonk crash
  - 7401 tests, 241.4s, all pass

- **PM Track 10 Phase 1a** (`a96f543`): Network-active module loading
  - Single-line change: `current-prop-net-box #f → (box (make-prop-network))`
  - Modules now elaborate with live propagator network — no regressions
  - 7401 tests, 236.3s (5s faster!), all pass

- **PM Track 10 Phase 1b** (`9203476`): .pnet serialization pipeline
  - pnet-serialize.rkt: serialize/deserialize via struct->vector + write/read
  - Verified: prologos::data::nat round-trips (44 env, 22 specs, 34KB)
  - Dynamic tag table (graceful degradation for unknown structs)
  - Feature flag: current-use-pnet-cache? (default #f — disabled until tag table complete)
  - 148 unique struct tags discovered in prelude; 60 core types need registration
  - 7401 tests, 260.9s (recompilation overhead), all pass
  - **Design decision**: dynamic tag table instead of manual 326-entry table.
    Avoids pipeline-exhaustiveness problem predicted by D.3 critique.

- **PM Track 10 Phase 1b.2** (`7b8e282`): Tag table with 60+ constructors
  - `register-all-pnet-structs!` covers: zero-arg atoms, 1-5 arg structs, posit types, numeric ops, spec-entry, srcloc, ns-context, preparse-macro, trait-meta, trait-method, impl-entry
  - Dynamic fallback for unknown tags (graceful degradation)

- **PM Track 10 Phase 2a** (`05d93c4`): Foreign function provenance
  - Added `source-module` + `racket-name` fields to `expr-foreign-fn` (36 files touched)
  - Re-linking via `dynamic-require` on deserialize
  - Polyglot-ready: stores `(module-path . binding-name)` — host-agnostic
  - **Design decision**: Option A (embed provenance in struct) over Options B-C because it's the most general, most data-oriented, and scales to future polyglot hosts

- **PM Track 10 Phase 2b-2e**: Registry completeness journey
  - **Key lesson: whack-a-mole anti-pattern** — each test fix revealed the next missing registry/struct. Applied diagnostic protocol: stop fixing one-at-a-time, audit ALL registries.
  - Registry audit: 28 registry-type parameters identified across codebase. 17 non-zero in prelude. 11 zero (deferred).
  - Registries added incrementally then comprehensively:
    - Phase 2c (`1fa29f8`): Closure-free preparse (symbol lookup table) + coercion (data references)
    - Phase 2e (`31bec3a`): Expanded load-module scope — 5 registries (trait, impl, param-impl, specialization, tycon-arity). 233.5s all green with cache OFF.
    - Phase 2e (`f0b1fde`): Coercion derivation from data refs — via-functions, single-field unwrap, identity. 381/382.
    - Phase 2e (`4dead84`): Via-function coercion + bundle-registry + bundle-entry tag. 381/382.
    - Phase 2e (`e9aff76`): Capability wrapper re-linking for IO foreign functions. 381/382.
  - **Phase 2f** (`e25061a`): COMPREHENSIVE audit resolves all remaining failures
    - Added: defn-param-names (426 entries), trait-laws (7), property-store (4), functor-store (2)
    - **382/382 ALL GREEN with .pnet cache ON** 🎉

- **Serialization mechanisms delivered**:
  1. `struct->vector` + `write`/`read` + tag dispatch (zero blast radius)
  2. `(foreign-proc name)` + `dynamic-require` re-linking
  3. `symbol$$N` gensym preservation with per-module tables
  4. `(coerce-via Sub Super via-fn)` data references with cold-start derivation
  5. Capability-token wrapper for `:requires` foreign functions
  6. `preparse-expander` → symbol lookup table

- **Key lessons from Phase 2**:
  - **Audit everything first**: The comprehensive registry audit (`e25061a`) solved more in one commit than 5 incremental fixes combined. The whack-a-mole stopped when we stopped playing whack-a-mole.
  - **Serialization IS dynamic linking**: Every closure needs a reconstruction strategy. The strategy is always: store DATA from which the closure derives, derive on load.
  - **Completeness applied to process**: "Can we audit ALL registries?" was the question that broke the cycle. Not "which registry is this test missing?" but "how many registries ARE there?"
  - **The diagnostic protocol emerged**: Audit → Hypothesize → Test narrowly → Challenge by principles → Reframe. Now codified in workflow.md.

- **PM Track 10 Phase 3a** (`51e6a9e`): .pnet cache ON by default + tooling
  - pnet-compile.rkt tool, --no-pnet-cache flag, PROLOGOS_PNET_CACHE env var
  - **240.6s → 155.6s (35% improvement!)** — exceeds <200s target
  - Second run 159.8s (consistent, no gen overhead)

- **PM Track 10 Phase 3b** (`1462fd6`): fork-prop-network + with-forked-network
  - O(1) CHAMP structural sharing. Architectural foundation for three-layer model.

- **PM Track 10 Phase 3c** (`81b5c21`): Fork integrated into test-support
  - `with-forked-network` macro replaces some parameterize patterns
  - Macro hygiene fix for `current-prop-net-box` binding

- **PM Track 10 Phase 3d** (`41b67c4`): Systemic process-string fix — ALL GREEN
  - **Root cause found**: `process-string` MUTATED `current-prop-net-box` without scoping. 252 test files with bare `parameterize` (no prop-net-box) caused elab-network leaks between test cases.
  - **Diagnostic protocol applied**: Audit (252 files) → Hypothesis (box mutation leaks) → Narrow test (bare run then prelude = reproduced) → Principles (Data Orientation: scoped not mutated) → Fix at infrastructure level (process-string itself, not 252 callers)
  - **Fix**: `process-string`/`process-string-ws` now PARAMETERIZE `current-prop-net-box` when none exists (not mutate). Network scoped to the call. Zero test files changed.
  - Also: prop-network/elab-network sentinels in pnet-serialize.rkt as defense-in-depth
  - **382/382, 7401 tests, 149.0s (38% improvement from 240s baseline)**
  - **Key lesson**: The bug was in `process-string`, not in tests or `.pnet` cache. Infrastructure bugs masquerade as integration issues — fixing at the right level (one function vs 252 callers) is the Completeness principle applied.

- **PM Track 10 Phase 4** (`f896887`): Absorb PM 8F deferrals (partial)
  - defaults at solve-time ✅
  - CHAMP fallback RETAINED — sole consumer was expander.rkt (Racket phase 1)
  - 382/382, 143.9s
  - **Finding**: expander.rkt is the ONLY consumer of CHAMP fallback. Runs in Racket phase 1 where propagator network doesn't exist.

- **PM Track 10 Phase 5** (`ca1fe15`, `60f6395`): Drop `#lang prologos` support
  - **Pivot decision**: Instead of making expander.rkt network-aware (substantial phase-boundary work for 6 test files), DROP the `#lang` path entirely.
  - Deleted: expander.rkt (261), main.rkt (73), repl-support.rkt (68), sexp.rkt (18) = 420 lines
  - Deleted: 6 test-lang-* files + 21 examples/ files = 1315 total lines deleted
  - Cleaned dep-graph.rkt: removed 6 test entries + 21 example-test-map entries
  - Cleaned run-affected-tests.rkt: removed DAG reference to test-lang-01-sexp.rkt
  - **No impact on active workflows**: VSCode+LSP, process-file, test-support all unchanged. DrRacket REPL is the only loss (unused).
  - **376/376, 7364 tests, 133.5s — 44% improvement from 240s baseline** 🎉
  - **Unblocks Track 10B**: CHAMP fallback removal + zonk-final deletion now possible (sole blocker removed)
  - **Design lesson**: When an entire subsystem exists for compatibility with a workflow you've already replaced (DrRacket → VSCode+LSP, Racket module system → Prologos module system), removing the subsystem is simpler and more principled than upgrading it.

---

## Considerations

- **Track 10 Phases 0-5 COMPLETE**: 240s → 133.5s (44%). .pnet cache ON by default. `#lang prologos` dropped. All green.
- Phase 6 (parameter reduction) deferred to Track 10B — it's architectural cleanup, not user-facing improvement.

- **Track 10 PIR** (`0df3371`): A/B benchmarks show 6× warm-cache improvement (20s → 3.3s), 185× memory-cache (108ms). 44% suite improvement.

- **Track 10B Stage 2 Audit** (`35ac7c4`):
  - 133 `set-box!` sites (74 HIGH risk in metavar-store + global-env)
  - 47 real zonk call sites (not 13 as estimated — session zonk + defaults were missed)
  - 36 CHAMP fallback sites, 18 id-map sites
  - 277 `with-fresh-meta-env` sites (correctly scoped)
  - **Key surprise**: `current-sess-meta-store` is the ONLY remaining mutable hasheq. Last holdout of imperative meta storage.

- **Track 10B Stage 3 Design** (`c6a9c5e`):
  - WS-A (Foundation, 6 phases): network-always, freeze+defaults, id-map elimination, batch worker simplification, PUnify toggle flip
  - WS-B (Zonk Elimination, 5 phases): scoping audit, session meta migration, remaining 23 zonk→cell reads, zonk.rkt deletion (~1300 lines), test-granular scheduling (deferred)
  - Targets: <130s (WS-A), <120s (WS-B)

- **DEVELOPMENT_LESSONS.org** (`7feca8c`): "Data at Rest, Closures Derived on Demand" — 6 instances from Track 10, the serialization corollary of Data Orientation

- **Diagnostic protocol codified** in workflow.md: audit → hypothesize → test narrowly → challenge by principles → reframe. Anti-pattern: whack-a-mole. Two successful applications: Phase 2 registry audit + Phase 3d process-string scoping.

- **Self-describing serialization research** captured: grammar-based format (Part 0 fixed header + Part 1 grammar rules + Part 2 data). Serializer = generator, deserializer = parser — duals of the same grammar. Deferred to PPN Track 0.

- **Track 10B Pre-0 benchmarks** (`aabb664`):
  - `make-prop-network`: 11ns (trivially cheap — A1 is safe)
  - CHAMP fallback check: 1.1ns (negligible — WS-A value is architectural)
  - `zonk-at-depth 0`: 302μs — **350× slower than `zonk`** (academic: being deleted)
  - `prop-meta-id->cell-id`: 98ns vs 1.5ns direct cell-id (id-map is 65× slower)
  - `with-fresh-meta-env` full: 79μs (277 calls = 22ms/suite, not bottleneck)
  - **Design implication**: WS-A is architectural cleanup (not performance). WS-B's zonk elimination IS performance but we're deleting, not optimizing.

- **Track 10B Design iterations** (D.1→D.4):
  - D.1 (`c6a9c5e`): Initial design from audit
  - D.2 (`520b061`): Pre-0 benchmarks confirm design. Explicit A/B + cleanup + PIR phases added.
  - D.3 (`55994c9`): Self-critique — 7 findings, 4 actions. Key: driver.rkt sentinel checks (3 sites), hasmethod bare meta ID (line 625), PUnify parity bugs = process smell.
  - D.4 (`c4e9270`): External critique — 12 recommendations, 10 accepted. Phase reorder (B0→A3b), B1 split (B1a-d), type-lattice.rkt 8 missing zonk sites, acceptance file added. Targets revised to ≤134s/≤125s.

- **Research: Hypergraph Rewriting + Propagator Parsing** (`e394874`):
  - Engelfriet-Heyker theorem: parsing IS attribute evaluation IS propagator fixpoint
  - SRE structural decomposition IS hyperedge replacement (DPO rewriting)
  - PPN Series proposed: 6 tracks from lattice design through incremental editing

- **Research: Tropical Optimization + Network Architecture** (`7510c1a`):
  - Tropical semirings for cost-bounded exploration (replaces flat fuel)
  - ATMS-guided search for parsing/rewriting
  - Self-describing serialization: Part 0 (fixed header) + Part 1 (grammar rules) + Part 2 (data). Serializer = generator, deserializer = parser — grammar duals.

- **PPN + OE Series on Master Roadmap** (`7510c1a`):
  - PPN: 6 tracks (lattice design → lexer → parser → disambiguation → grammar extensions → incremental editing)
  - OE: 5 research tracks (tropical lattice → weighted parsing → cost rewriting → cost-bounded exploration → weighted constraints)

- **Track 10B Implementation — WS-A Phases**:

  - **Phase A0b** (`8e1495f`): Acceptance file — 25 sections, 143 results, 56 errors, 87 successes. Syntax gaps found: char literals, cond WS, let/do binding, `:where` on user specs, `int>` angle bracket, list-of-maps. Each gap documented as IDEAL (commented) + WORKING (uncommented).

  - **Phase A1** (`a767b50`): Network-always + scoped fresh network per call
    - `with-fresh-meta-env` creates `make-elaboration-network` (not `#f`). Every code path has a live network.
    - `process-string`/`process-string-ws`/`process-file` always scope a fresh elab-network per call via parameterize (unconditional, not sentinel-gated).
    - CHAMP boxes initialized to `(box champ-empty)`, replacing `#f`.
    - **Surprise**: Raw `make-prop-network` caused test-unify-cell-driven failure — `process-command` wraps in `elab-network`, so the box must start with `elab-network`. Fixed: `make-elaboration-network`.
    - **Lesson**: The "network-always" change and the "scoped per call" change are BOTH needed. Network-always (A1) ensures every path HAS a network. Scoped-per-call (from Track 10 Phase 3d) ensures calls DON'T LEAK state. Removing the sentinel check without keeping the scoping caused the same leak bug.
    - 376/376, 135.5s

  - **Phase A2** (`d12eacf`): zonk-final → freeze rename (15 call sites)
    - Cosmetic rename signaling intent: `freeze` = boundary operation, not elaboration-time.
    - `default-metas` at solve-time deferred to WS-B — requires post-resolution hook design.
    - **Design choice**: `freeze` and `zonk-final` are currently IDENTICAL (zonk + defaults). The rename is a signal, not a behavioral change. True `freeze` (single-pass cell-read, no tree walk) comes in WS-B when metas ARE cells.
    - 376/376, 153.4s (higher due to .pnet cache invalidation from .zo staleness check)

  - **Phase A3** (`cd0d708`): id-map elimination — cell-id direct access
    - `hasmethod-constraint-info` struct: added `dict-meta-cell-id` field (D.3 finding). Construction site in elaborator.rkt updated.
    - 3 external id-map callers → direct `expr-meta-cell-id` access: metavar-store:623 (trait-var), metavar-store:626 (dict-meta), driver:2400 (structural-meta-lookup).
    - `prop-meta-id->cell-id` now has ZERO external callers. id-map persists internally only.
    - **Pipeline exhaustiveness hit**: Adding field to `hasmethod-constraint-info` broke test-infra-cell-constraint-01.rkt (arity mismatch, 4→5). Also caused 0-tests/600s timeout when batch workers had stale .zo — the "silent batch worker crash" pattern from BSP-LE Track 0.
    - **Lesson**: Struct field additions MUST be followed by: (1) `raco make` recompile, (2) grep for ALL constructor call sites, (3) verify test .zo are fresh. The pipeline.md checklist covers this but we still hit it.
    - 376/376, 139.0s

  - **Tooling fixes** (`a014087`):
    - `pnet-compile.rkt`: resolve cache-dir from `syntax-source #'here`, not CWD. Works from any directory.
    - `run-affected-tests.rkt`: stale .zo warning when `--no-precompile` used. Summary file `last-run-summary.txt` written after every run.
    - **Design choice**: Summary file for easy inspection without re-running. Contains test count, wall time, pass/fail, first 5 error lines per failure.

  - **Transitive staleness** (`a01f8ac`): .pnet files auto-invalidated when `driver_rkt.zo` is newer. Simple timestamp comparison, O(1) per check. Prevents "stale .pnet after Racket changes."

  - **Design doc updated** (`pending`): Phase completion protocol added — every phase ends with commit + dailies + then move on. Dailies are INSIDE the phase, not after it.

---

## Considerations

- WS-A progress: A0b ✅ A1 ✅ A2 ✅ A3 ✅. Four of seven phases complete.
- Remaining WS-A: A3b (scoping audit), A4 (batch worker), A5 (PUnify toggle), A6-A7 (benchmarks, verify).
- The CWD sensitivity issue persists for `run-affected-tests.rkt` when invoked from project root with `--all` (precompile step). Workaround: always run from `racket/prologos/` or use `--no-precompile`.
- Process improvement: dailies updates are now part of each phase's completion criteria, not a separate deferred task.
- The "0 tests, 600s" silent batch worker crash from stale .zo is a RECURRING pattern (3rd instance). Now detected in 30s (dead-worker detection).

- **Process fixes** (`a014087`, `5ee1e25`, `40d0415`, `ef2e1a5`):
  - CWD-independent: pnet-compile uses syntax-source, runner sets current-directory to project-root
  - Dead-worker detection: 30s first-result timeout (was 600s). 20× faster feedback.
  - Stale .zo warning when --no-precompile used
  - Summary file (last-run-summary.txt) after every run
  - Worker count defaults to processor-count, not hardcoded 10
  - Phase completion protocol: commit → tracker → dailies → next phase

- **Phase A3b** (process audit): process-string scoping audit COMPLETE
  - 69 `set-box!` in metavar-store.rkt: ALL write to parameterized boxes (net-box, tc-net-box, hm-net-box, cstore-net-box, etc.)
  - 5 `set-box!` in global-env.rkt: ALL write to parameterized boxes
  - 9 `set-box!` in elab-speculation-bridge.rkt: ALL write to parameterized boxes (atms-box, ctx-box)
  - 3+3+3 in unify/resolution/macros: ALL parameterized
  - **Result: NO LEAKS FOUND.** The Track 10 Phase 3d prop-net-box leak was the only instance. All other boxes follow the parameterize scoping pattern correctly.
  - **Lesson**: The consistent parameterize pattern across the codebase is correct-by-construction for box isolation. The Phase 3d bug was an EXCEPTION (process-string didn't parameterize), not a pattern. The fork model is safe to proceed with.

- **Phase A4** DEFERRED to WS-B:
  - Batch worker has 11 saved values + 19 macros params (via snapshot). Fork model helps with NETWORK cells but the 19 macros registries are RACKET PARAMETERS, not cells. `restore-macros-registry-snapshot!` resets them imperatively.
  - Until macros registries become cells (WS-B), the snapshot is still needed for per-file isolation.
  - **Lesson**: The fork model's isolation is LIMITED to what's ON the network. Racket parameters are outside the fork boundary. Simplifying the batch worker requires migrating registries to cells first — it's not just a "use fork instead" swap.

- **Phase A5**: PUnify toggle flip ATTEMPTED — systemic regression
  - Flipped `current-punify-enabled?` to `#t`. Multiple failures + 1 timeout in first 188 files (test-map-bridge FAIL 97.7s, test-stdlib-01-data-04 TIMEOUT 120s).
  - The 5 known parity bugs (head, map, Pair, match, Vec) are NOT simple fixes — they cause cascading failures in prelude module loading.
  - **Decision**: Reverted to `#f`. PUnify needs a DEDICATED track with its own design cycle (not a validation phase in Track 10B). The 5 parity bugs require individual investigation with targeted test reproduction.
  - **Lesson**: "Attempt and document" was the right approach — the attempt cost 5 minutes and confirmed that PUnify is NOT ready for default status. Deferring indefinitely without attempting would have left this as an untested assumption. Now we have DATA: systemic regression, not just 5 isolated bugs.
  - Also fixed: `processor-count` import (needs `racket/future`), `completed-count` undefined in bail handler.
  - 376/376, 134.0s after revert

- **Phase A6**: A/B benchmark comparison — no performance change (confirmed)
  - All micro-benchmark numbers within noise of Pre-0 baselines
  - WS-A delivered ARCHITECTURAL value (code simplification), not performance
  - Suite wall time: 134.0s (≤134s target MET)

- **Phase A7**: Verification — 376/376, 134.0s, all pass ✅

- **WS-A COMPLETE.** Phases: A0b ✅ A1 ✅ A2 ✅ A3 ✅ A3b ✅ A4 ⏸️ A5 ✅ A6 ✅ A7 ✅

- **Phase B0**: zonk call-count measurement
  - Polymorphic command (`spec id {A} A -> A`): 14 `zonk-at-depth 0` calls + 18 `zonk` + 4 `freeze` = 36 total
  - Simple command (`def x := 42`): 2 `zonk-at-depth 0` + 4 `zonk` + 4 `freeze` = 10 total
  - At 302μs per `zonk-at-depth 0`: polymorphic command pays ~4.2ms in zonk-at-depth alone
  - **Suite estimate**: thousands of polymorphic commands × 4.2ms = multiple seconds. Deletion will show measurable improvement.
  - Counters gated behind `current-zonk-call-counts` parameter (#f = zero overhead)

- **Phase B1a**: Session meta cell infrastructure
  - `sess-meta` struct: added `cell-id` field + custom `gen:equal+hash` (same pattern as PM 8F `expr-meta`).
  - `fresh-sess-meta`: captures cell-id at creation time (from fresh-fn cell allocation).
  - Pattern matches updated across 4 files: sessions.rkt (3 sites), session-lattice.rkt (1), typing-sessions.rkt (4), tests (7).
  - **Discovery**: Session metas were ALREADY partially on-network (Track 4 Phase 3 added cell allocation). The CHAMP is primary, cell is secondary write. B1a adds cell-id to the struct so reads can go direct.
  - **Discovery**: `sess-meta-solution` already has cell-primary read path (line 2452: `elab-cell-read`). B1b (read migration) may be as simple as removing CHAMP fallback.
  - **Dead-worker detection validated**: First run hit stale .zo (struct field change) — caught in 30s instead of 600s. The detection works exactly as designed.
  - 376/376, 136.1s

- **Phase B1b**: Session meta read migration
  - Added `sess-meta-solution/cell-id` (same pattern as PM 8F `meta-solution/cell-id`).
  - `zonk-session` and `zonk-session-default` now use cell-id fast path when struct available.
  - Backward-compatible `sess-meta-solution` delegates to `/cell-id` with `#f`.
  - 376/376, 138.9s

- **Phase B1c**: Speculation verification — PASSED (no code changes)
  - test-sess-inference (28 tests) exercises session metas under TMS speculation. All pass.
  - TMS tagging in fresh-sess-meta unchanged by B1a — still reads `current-speculation-assumption`.

- **Phase B1d**: Session default timing — VERIFIED (no code changes)
  - `sess-end` defaults apply in `freeze` (boundary time), after all resolution. Timing correct.
  - D.4 concern (premature sess-end) does NOT apply — defaults are post-resolution.

- **Phase B2**: zonk elimination — **BLOCKED**
  - The 55 zonk call sites substitute solved metas into expression trees. Removing them requires that expressions DON'T CONTAIN `expr-meta` nodes — the elaborator must create cell references instead.
  - This is SRE Track 2+ scope (elaborator-on-SRE), not Track 10B scope.
  - **Root cause**: zonk exists because the elaborator puts `expr-meta` nodes in expressions. Cell-id fast paths make zonk FASTER (PM 8F), but only ELIMINATING expr-meta from expressions eliminates zonk.
  - **Lesson**: The design estimated 55 sites as "replace zonk with cell reads." But cell reads READ the cell value — they don't substitute it INTO an expression tree. Substitution (what zonk does) is a fundamentally different operation from reading. Eliminating substitution requires eliminating the NEED for substitution — which means changing how expressions are constructed.
  - B3 (zonk.rkt deletion) is also blocked — depends on B2.

- **Phase B5**: A/B benchmarks — session cell-id changes too small to measure. Suite 134-139s.
- **Phase B6**: Instrumentation cleanup — all 4 params gated behind #f. No cleanup needed.
- **Phase B7**: Verification — 376/376 verified at each phase throughout Track 10B.
- **Phase B8**: PIR COMPLETE (`2026-03-26_PM_TRACK10B_PIR.md`)
  - 16 sections per methodology. 8 PIRs cross-referenced longitudinally.
  - 5 wrong assumptions corrected. 3 longitudinal patterns identified.
  - Key finding: infrastructure tracks (PM) deliver SUBSTRATE; system migration tracks (SRE) deliver BEHAVIOR CHANGE. Zonk elimination is behavior change, correctly deferred to SRE 2C.
  - "Implicit Contracts" recommendation for infrastructure track designs.
  - "Vision contaminates audit" at 6 instances — approaching codification threshold.

**Track 10B COMPLETE.** WS-A ✅ (7/9 phases, 2 deferred). WS-B partial (B1 ✅, B2-B4 deferred to SRE 2C/10C).

- **Deferrals captured** (`a084f08`):
  - SRE Track 2C: cell refs in expressions (enables zonk elimination)
  - SRE Track PUnify: systemic parity investigation
  - PM Track 10C: per-test scheduling via Places

---

## Roadmap Vision + Series Architecture (March 26)

- **PRN/PPN distinction crystallized**:
  - **PRN** (Propagator-Rewriting-Network): THEORY series — hyperlattice rewriting as unifying formalism. Grows from application track discoveries.
  - **PPN** (Propagator-Parsing-Network): APPLICATION series — grammar/parsing on propagators. 10 tracks.
  - SRE, PReductions, BSP-LE are also PRN application series.

- **PPN Master Document created**: 10 tracks from lattice design through self-describing serialization. PPN Track 4 (elaboration as attribute evaluation) = SRE Track 2C (elaborator-on-SRE).

- **PRN Master Document created**: Theory series with confirmed findings, conjectured connections, universal primitives, open research, cross-series contribution ledger.

- **Lossy bidirectional typed grammar research**: Serialization/deserialization as grammar duals. Invariant levels (identity/structural/behavioral/value) as NTT types.

- **Priority**: PPN elevated to prerequisite for future syntax work. 14-file pipeline is biggest development friction.

- **Meta-lesson**: Build applications first → observe patterns → formalize (PRN). Don't generalize a priori.

## Considerations

- PPN Track 2 (surface normalization) may be highest-value-per-effort
- PPN Track 4 = SRE Track 2C: joint ownership needed
- "Vision contaminates audit" at 6 instances — ready for codification
- Acceptance file found 6 syntax gaps — PPN Track 7 motivation

---

## PPN Track 0 Design (March 26 continued)

- **D.1** (`8674d94`): 4 lattices, 6 bridges, NTT syntax, 9 phases
- **D.2** (`0da6ef4`): bilattice deferred (WF-LE pattern), bridge/exchange distinction, SPPF=provenance
- **D.3** (`9dd9440`): set-once tokens, hybrid value/structural parse, demand priority, adjunction verification
- **D.4** (`b570803`): external critique (12 points). Cell count estimation, Track 0→3 contract, demand satisfaction protocol, SurfaceToType reclassified as ATMS-mediated, `:set-once` NTT kind, expanded integration test.
- **Pre-0** (`8880524`): Comprehensive benchmarks. Key findings:
  - CHAMP handles 500K cells (1.5μs/cell, 728ms total). Parse-scale viable.
  - Pipeline is 99.99% elaboration. Reader + preparse = 0.005%. PPN value is ARCHITECTURAL not PERFORMANCE.
  - Quiescence scales linearly (10K propagators in 11μs). Chain propagation 500ns/step.
  - ATMS: 2-branch ambiguity 2μs, retraction 47ns. Cheap enough for parse disambiguation.
  - 4-tuple key lookup: 55-65ns (nested slightly faster). Both viable.
  - Set-union merge: 267ns (3 elements), 3μs (50 elements). Sub-microsecond for typical case.
  - **No design changes needed.** All measurements within budget.
  - **Key insight**: PPN Tracks 1-3 deliver architectural value (14-file collapse, grammar extensions, cross-domain disambiguation), not performance. Track 4 (elaboration) is where performance matters.

- **Phase 0** (`8036249`): acceptance file — 25 sections, found 5 syntax gaps (> in identifiers, PVec literal, trait method format, zonk crash on trait dict, bundle crash)
- **Phases 1-3** (`20fdeb0`): `parse-lattice.rkt` — all 4 lattice domains in one module. 27 tests, all pass. Token (set-once), Surface (derivation set-union with SPPF provenance), Demand (domain-typed, priority, Right Kan internal state), Core (trivial set-once). Phases combined because all are simple struct definitions with merge functions — no dependencies between them.
- **Phase 4** (`3d15bb8`): `parse-bridges.rkt` — 3 bridges + 2 exchanges + 1 projection. 23 tests. SurfaceToType honestly classified as ATMS-mediated.
- **Phase 5** (`86df0b4`): NTT Syntax Design §16 — parse lattices, bridges, exchanges in NTT. New `:set-once` lattice kind. Both stratification options shown (separate vs same strata).
- **Phase 6** (`b42d30f`): Integration test — 7 tests on hand-built propagator network. All lattice domains compose on the network.
- **Phase 7**: A/B benchmarks — all ops under 1μs target.
- **Phase 8**: Suite verification — 379 files, 7421 tests (+30), 150.3s, all pass. No regressions.
- **Phase 9**: PIR — 15 sections, 0 wrong assumptions (first PIR with empty §13).
  - Key finding: 1:1 design:implementation ratio from research investment (3 research docs). Smoothest track to date.
  - Cross-PIR: research investment correlates with implementation smoothness. Pre-0 was validation, not discovery.
  - "Parsing is not special" — same lattice+propagator+bridge mechanism as type inference. The PRN thesis in miniature.

**PPN Track 0 COMPLETE.** 57 new tests, 4 lattice domains, 6 bridge/exchange specs, NTT extensions.
