# Dailies — 2026-04-10

## Carried Forward from Previous Interval

### BSP-LE Track 2 — Phases 0-10 COMPLETE, D.12
- Design at D.12 (1900+ lines). Phases 4-10 implemented in single session.
- Propagator-native solver functional end-to-end: facts, single-clause, concurrent multi-clause, all goal types, tabling, config knobs.
- 397/397 files, 7750 tests, ~130s
- `:strategy :atms` routes to propagator solver; `:auto` stays DFS (parity not yet validated)

### Key Architectural Discoveries (from 2026-04-09 session)
- Per-propagator worldview bitmask: concurrent clause execution on same network
- Compound scope cells: one cell per scope, table entries ARE scope cells
- Table registry as on-network cell: pioneers self-hosting registry pattern
- BSP fire-and-collect-writes fix: net-cell-read-raw for diffing
- promote-cell-to-tagged: must update merge function (not just value)
- Tagged-cell-value entry ordering: new entries first (prepend)

### Deferred Items (Phase 11 scope)
- TMS/tagged-cell-value merge gap: union type `<Nat | Bool>` regression when TMS removed
- current-speculation-stack retirement: 2/5 consumers migrated, 3/5 retain dual-write
- DFS <-> propagator parity: :auto stays DFS pending validation
- Inert-dependent checkpoint: counter data review pending
- S(-1) lattice-narrowing cleanup: deferred pending instrumentation data

### Watching
- **TMS merge gap**: the critical blocker for full tagged-cell-value deployment. The attribute-map cell's nested hasheq speculation doesn't replicate TMS tree isolation under tagged-cell-value merge. This is not a minor issue — it affects type inference correctness.
- **Diagnostic discipline**: previous session had 5+ instances of re-running full suite for diagnostics. New rules/tooling added. Verify compliance this session.
- **Delimiter balance**: check-parens.sh added. Use after every .rkt edit.

---

## Session Log

### BSP-LE Track 2 PIR Rewrite (commit `c3649319`)

Rewrote the Track 2 PIR from 143 lines to 542 lines. Previous version was scant — missing Phases 0-3 timeline, per-phase hour:minute estimates, evaluative sections, meta-learning questions, and 10-PIR longitudinal survey. Full 16-question methodology now followed.

### BSP-LE Track 2B Design — 9 Iterations (D.1 through D.9)

**D.1** (`11ce3f80`): Initial design from Track 2 PIR findings. 5 phases, grep-backed measurements.
**D.2** (`547ef31d`): Async NAF from start, set-equality parity, expanded Phase 0.
**D.3** (`7bee0995`): Hypercube-guided optimality lens (from standup-2026-04-08 conversation).
**D.4** (`f424c2fb`): Clause selection as decision-cell narrowing — from Phase 0a adversarial findings.
**D.5** (`bb0f01c1`): Benchmark data folded in + Tier 1 optimization pipeline (4 optimizations).
**D.6** (`b4c7d647`): Executor scaling data + hypercube merge topology for BSP barriers.
**D.7** (`f10de53d`): Self-hosting lens — 5 off-network dismissals REVISED to on-network cells.
**D.8** (`383e16bd`): External critique responses (18 findings) + consistency pass.
**D.9** (`f9e31200`): Phase 1 lattice design deepened (SRE full analysis, module theory, forward composition).

### Phase 0 Investigation (Complete)

**0a** (`00be3486`): Parity harness + adversarial benchmark. 19/19 existing tests pass BOTH strategies. Adversarial found 3 divergence categories: fact-row last-write-wins, multi-clause variable binding, NAF semantic divergence.

**0b** (`7b76b496`, `0c9dfe9f`): 28 micro-benchmarks + overhead decomposition. ATMS 25x slower than DFS for single-fact. BSP scheduling 52.6%, goal installation 30.4%, allocation 15.5%. Narrowing cost negligible (0.125us).

**0c** (`3e819b08`, `70b66de7`, `91bd2e82`): Executor comparison (3×7×10 runs) — parallel executors show NO benefit for current workloads. Threads cross over at N≥128. Futures eliminated (Racket VM restriction). Thread pool prototype: channel communication (1.57us) is the floor.

### Self-Critique (commit `0a02767d`)

P/R/M three-lens analysis: 15 findings, 5 required design changes. Key: merged Phase 5a+5c into fire-once fast-path, NAF always writes to cell (info-flow invariant), self-hosting lens revised 5 off-network dismissals.

### External Critique (commit `6e616c01`)

18 findings (2 critical, 8 major). Key: GS-to-BSP transition (found to be already done — `run-to-quiescence` dispatches via parameter), discrimination cell needs producing propagator, conjunction wiring missing from NTT model, NAF deferred-spawn pattern.

### Critique Response (commit `c4615537`)

15 actions incorporated into D.8. BSP IS the architecture. DFS is alternative backend, not being retired. Conjunction convergence via parallel prefix (hypercube).

### Phase 1a Implementation — Categories 1+2 FIXED

**Step 1** (`a1df50f4`): Discrimination map extraction — `build-discrimination-data`, `position-discriminates?`, `discrimination-lookup`. Mini-audit found BSP already default (P1/R1 critique was wrong).

**Steps 2-4 WIP** (`e3f95d86`): On-network discrimination via broadcast propagators + viability cell. Representation mismatch found: `hasheq` uses `eq?` but AST structs need `equal?`. Resolved with on-network broadcast approach (user's N+1 insight: use PUnify-compatible `equal?` comparison in propagator, not hash lookup).

**Closure capture bug** (`683e2ff3`): Fire function captured `n` (installation-time network) instead of `net` (BSP snapshot). One-character fix. Added to propagator design rules (`a72bad83`).

**Domain-merge fix** (`88fd267e`): Result reading called `tagged-cell-read` without domain-merge — multiple same-bitmask entries returned first-match. Fix: pass `make-tagged-merge(scope-cell-merge)`. Category 2 (multi-clause variable binding) FIXED.

**Step 5 fact-row PU** (`b47b9787`): Per-fact-row worldview bitmask branching, same pattern as multi-clause. Category 1 (fact-row multi-result) FIXED. 398/398, 7750 tests.

### Phase 1b Implementation — Discrimination Tree

**Tree structures** (`1eae7eb8`): `discrim-node`, `discrim-leaf`, `build-discrimination-tree`, `variant-discrimination-tree`. Position scoring, recursive best-position partitioning.

**Step-think revert** (`cb4758f4`): Tree traversal for propagator installation was imperative (walk struct, branch on bound/free). User caught this — reverted to flat installation. Tree retained as data value for self-hosting/analysis. Ordering EMERGES from BSP dataflow, not from tree traversal code paths.

### Key Lessons

1. **Fire function network parameter** — fire functions MUST use their `net` parameter, never captured outer variables. Added to propagator-design.md.
2. **Domain-merge in result reading** — `tagged-cell-read` needs domain-merge for same-bitmask entries. Same class as Track 2 Phase 11 Bug #7.
3. **Step-think detection** — tree traversal for installation is imperative. Flat propagator installation with BSP-emergent ordering is the on-network approach.
4. **Discrimination map on-network** — self-hosting lens: "constants" are fixpoints after one write, still belong on-network as cells.

### Current State

- **Track 2B**: Phase 1a+1b ✅ complete. Categories 1+2 FIXED. Category 3 (NAF) is Phase 2.
- **Suite**: 398/398 files, 7750 tests, ~125s, all pass.
- **Design**: D.9, 9 iterations, 2 critique rounds, all findings resolved.
