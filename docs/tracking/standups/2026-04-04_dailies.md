# Dailies — 2026-04-04

## Carried Forward from Previous Interval

### SRE Track 2D ✅ COMPLETE
- 4th SRE relation. 13/13 rules on SRE (100%). 0 critical pairs, strongly confluent.
- DPO spans, pattern-desc, child-pattern-split, PUnify holes, fold/tree/PU combinators.
- Adhesive category confirmed: parse trees are presheaf objects → full DPO toolkit.
- 385/385 GREEN, 7551 tests.

### SRE Track 2H ✅ COMPLETE
- Full quantale. Union-join + absorption, complete meet, tensor, per-relation properties.
- 384/384 → 385/385 GREEN (after Track 2D test additions).

### Grammar Form Design Thinking (Stage 2)
- `:target`-first insight: 90%+ of cases work via spec/defn → grammar :target
- Three tiers: inferred from target / type params / relational &> (future)
- Attribute grammar thread ACTIVE — needs worked examples
- Compositional grammar layering, operator DAG, duality/modality settled

### Priority Ordering (updated)
1. Grammar Form R&D — continue deepening (attribute grammar expression, hard cases)
2. PPN Track 4 — elaboration on network (all prerequisites met: 2H quantale + 2D rewrite relation)
3. SRE Track 6 — reduction as rewriting (shares 2D's DPO mechanism)

### Known Gaps / Outstanding
- SRE Track 2F: PIR not written (implementation complete)
- 9+ codification patterns backlog — significant, needs dedicated session
- Track 2D scaffolding: apply-all-sre-rewrites uses for/or (transitional), K as hash (not cells), local tag constants (circular dep workaround), instantiate-template recursive (not PUnify)

### Watching (medium-term patterns)
- **PIR methodology adherence**: 2 consecutive PIRs initially scant. Checklist rule helps but isn't sufficient. Consider auto-generating PIR skeleton.
- **+0 test delta pattern**: broken by mandatory test phase rule. Monitor.
- **User scope challenges improve designs**: 4th instance (codification ready). Track 2D: 62% → 85% → 100%.
- **CALM-adhesive connection**: potentially novel contribution to CALM body of research. Worth formalizing.

---

## Lessons Distillation Check

### Scanned: Track 2D PIR + April 3 dailies + adhesive research

**Ready for codification (cumulative):**

1. **"Pre-0 structural/algebraic validation catches bugs"** — 3rd instance. READY. → DESIGN_METHODOLOGY.org
2. **"User scope challenges improve designs"** — 4th instance. READY. → DESIGN_METHODOLOGY.org
3. **"Dual path = incomplete delivery (validated ≠ deployed)"** — 3rd instance. READY. → DEVELOPMENT_LESSONS.org

**Still pending (cumulative 12+ items):**
- 9 items from April 2 dailies
- 3 new items from Track 2D

**NOTE**: Codification backlog is now 12+ items across 3+ sessions. This is approaching a process failure — lessons that stay only in PIRs are lessons that won't be encountered during future work.

---

## Today's Work

### PPN Track 4: Elaboration as Attribute Evaluation

**Design complete through D.3** (3 iterations):
- D.1: Initial Four Questions, phases, NTT model
- D.2: Self-critique (P/R/M lenses) — context lattice, no-walk DPO architecture, typing as rewrite rules, zonk retirement, bidirectional ring, ATMS rescoping (commit `2e9bad0d`)
- D.3: External critique — PU-internal invariant, off-network state mapping, phase deps, migration strategy, rule accounting, parity criteria (commit `7f0dbab7`)

**Phase 0: Acceptance file + DEFERRED triage** (commit `81cf3a72`)
- Acceptance file: 8 sections, all pass at Level 3 (process-file)
- DEFERRED triage: 2 items in scope (TMS-aware cells, unify inference+traits), 1 out (reduction cache → SRE Track 6)
- Baseline: 7551 tests, 385 files, all passing, 117.8s

**Phase 1a: Component-indexed propagator firing** ✅ (commit `6d5f1adb`)
- Extended `prop-cell.dependents` to store component paths alongside prop-ids
- `net-add-propagator` gains `#:component-paths` keyword — list of `(cell-id . path)` pairs
- `net-cell-write` computes `pu-value-diff` for hasheq PU values, filters dependents by component path
- Fast path: if no dependents have component paths, skips diff entirely (backward compat)
- 13 tests in test-ppn-track4.rkt: unit + integration
- Decision: considered deferring scheduler-level filtering in favor of fire-fn guards — rejected because the design is architecturally correct (component info belongs ON the network) and the implementation cost is bounded
- Stale `.zo` lesson: `--no-precompile` + keyword arg change = linklet mismatch. Always recompile after changing function signatures.
- 7564 tests, 386 files, all pass

**Phase 1b: PU cell-trees (type-map in form-pipeline-value)** ✅ (commit `bffe3c90`)
- Added `type-map` as 5th field to `form-pipeline-value` struct (hasheq: position → type)
- `type-map-merge`: pointwise, newer-value-wins (Phase 2 wires in lattice merge)
- `advance-pipeline` threads type-map through all 8 transform stages unchanged
- 30+ construction sites updated across 3 files (surface-rewrite, form-cells, bench-ppn-track3)
- Pipeline checklist followed: grepped all construction sites, updated all, compiled, tested
- 7564 tests, 386 files, all pass

**Phase 1c: Context lattice** ✅ (commit `2f50c6c4`)
- New file: typing-propagators.rkt — infrastructure for propagator-based type inference
- `context-cell-value` struct: binding stack + depth. De Bruijn indexing.
- Extension = tensor (depth+1), lookup = structural read at position k
- Merge: pointwise same-depth, deeper wins on mismatch. No cross-branch merging (TMS).
- Module Theory parallel: context IS a "local module" with positional exports
- 8 tests, 7572 total, all pass

**Phase 2a: typing-rule struct + registry + dispatch** ✅ (commit `bca522f5`)
- `typing-rule` struct: tag, name, arity, infer-fn, check-fn, stratum
- Registry: hasheq tag → rule, O(1) lookup, populated at load time
- Dispatch: DPO-first + imperative-fallback, bidirectional (infer = join, check = meet)
- 6 tests, 27 total, all pass
- Decision: parallel struct to `sre-rewrite-rule` (not same struct — different domain: expr→type not tree→tree)

**Phase 2b: literal + universe typing rules** ✅ (commit `08402ded`)
- 12 rules: 4 literal, 7 type-constructor, 1 universe. All bidirectional.
- `expr-typing-tag`: maps expr structs to tag symbols (27 tags)
- `make-constant-infer-rule` helper for fixed-type rules
- 15 tests, 42 total, 7593 suite total, all pass

**Phase 2c: variable lookup rules** ✅ (commit `fae47058`)
- bvar: first rule to USE context lattice (Phase 1c). Reads position k, shifts by (k+1).
- fvar: global env bridge (off-network, Track 7 scope). Uses global-env-lookup-type.
- 4 tests, 46 total, 7597 suite total, all pass

**Phase 2d: lambda + Pi + Sigma formation rules** ✅ (commit `605f4356`)
- Lambda: first rule to use context extension + sub-expression type reading
- Pi/Sigma formation: read domain/codomain types, produce Type(lmax(...))
- Key insight: "is-type?" = reading Type(l) from type-map (a READ, not a CALL)
- Bidirectional lambda: check mode fills hole domains from expected Pi
- 7 tests, 53 total, 7604 suite total, all pass

**Phase 2e: application (tensor) + projection rules** ✅ (commit `71bd2bca`)
- Application rule wires type-tensor-core from Track 2H — the quantale tensor as a typing rule
- Fst/snd projection rules for Sigma types
- Total: 20 typing rules (12 literal+universe + 2 variable + 3 binder + 3 structural)
- 6 tests, 59 total, 7610 suite total, all pass
- Phase 2 COMPLETE: typing-rule infrastructure + 20 concrete rules covering core type system

**Phase 3: typing-rule-aware infer (DPO-first + fallback)** ✅ (commit `f7c86536`)
- `make-typing-rule-infer(infer-fallback)`: wraps infer with registry dispatch
- Mixed-state: rules for current node, recursive rule-or-fallback for children
- No dependency on typing-core.rkt (fallback injected as parameter — decomplection)
- 10 parity tests confirm identical results to imperative infer
- 69 tests, 7620 suite total, all pass

**Phase 4a: meta-variable typing rule** ✅ (commit `edb8962e`)
- Meta rule: infer reads from cell (fast path), check optimistically succeeds
- Cells are sole authority for meta solutions (CHAMP meta-info kept for debug only)
- Scoped: rule registration only, NOT full 74-site expr-meta→cell-ref migration
- 2 tests, 71 total, 7622 suite total, all pass

**A/B Benchmark after Phase 4a** ✅
- All comparative benchmarks: no significant regressions (<2% variation, within noise)
- Confirms Pre-0 prediction: cell operations have zero measurable impact on elaboration time

### Track 4 Progress Summary (Phases 0-4a)

11 commits, ~1,676 lines new infrastructure, 71 tests, 21 typing rules.

**Delivered:**
- Component-indexed propagator firing (PU-diff + selective scheduling)
- Type-map in form-pipeline-value (PU carries type information)
- Context lattice (typing context as cells, Module Theory parallel)
- Typing-rule struct + registry + dispatch (first-class data, Engelfriet-Heyker)
- 21 concrete typing rules: literals, universes, variables, binders, application (tensor), projections, metas
- DPO-first + imperative-fallback integration (make-typing-rule-infer)
- Parity tests confirming rule-infer = imperative infer

**Remaining (Phases 4b-9):**
- 4b: zonk retirement (~1300 line deletion, 74-site expr-meta migration, 15 files)
- 5: ATMS extension for DPO rules (delta from PM Track 8 B1)
- 6: constraint propagators (trait resolution as cells)
- 7: bidirectional bridge (Surface→Type Galois)
- 8: scaffolding retirement (8 items from Tracks 2H + 2D)
- T: test file consolidation
- 9: verification + PIR

**Phase 4b-i: fan-in meta-readiness infrastructure** ✅ (commit `002f7cc3`)
- meta-readiness-value: registered/solved set tracking per form
- Set-union merge (monotone), register/solve/unsolved/all-solved? API
- 7 tests, 78 total

**Phase 6: constraint lattice** ✅ (commit `f7ef8665`)
- constraint-cell-value: pending → resolved(instance) → contradicted
- Join (merge) + meet (dual) with algebraic properties validated
- 12 tests, 90 total, 7641 suite total, all pass

### Track 4 Progress Summary (Phases 0-6, partial)

13 implementation commits, ~2,150 lines new infrastructure, 90 tests, 21 typing rules.

**Delivered (all infrastructure, tested, zero regressions):**
- Component-indexed propagator firing
- Type-map in form-pipeline-value
- Context lattice (typing context as cells)
- 21 typing rules as first-class data (Engelfriet-Heyker)
- DPO-first + imperative-fallback integration
- Meta-readiness fan-in cell infrastructure
- Constraint lattice (join/meet/contradicts)
- A/B benchmark: zero performance impact

**Remaining (deep production-code migration):**
- 4b-ii: full expr-meta→cell-ref migration + zonk.rkt deletion (34 arms, 15 files)
- 5: ATMS extension for DPO rules
- 7: bidirectional bridge
- 8: scaffolding retirement
- T+9: test consolidation, verification, PIR

The remaining phases modify the EXISTING production pipeline. They depend on
wiring the typing rules INTO process-command (replacing the imperative infer/check
path). This is the "deployment" phase of the validated infrastructure.

**Phase 5: ATMS extension** — assessed, ~130-300 lines of new code. Blocked on production pipeline wiring.
**Phase T: test file** ✅ — test-ppn-track4.rkt already has 90 tests across 11 suites.
**Phase 9: verification** 🔄 — all tests pass, A/B benchmark clean, acceptance file L3 clean. PIR deferred to track completion.

### Track 4 Natural Split Point

Track 4 divides cleanly into two halves:

**Track 4A (COMPLETE — this session):**
All independently-buildable infrastructure. 13 commits, ~2,300 lines, 90 tests.
- Component-indexed propagator firing
- Type-map in form-pipeline-value
- Context lattice
- 21 typing rules as first-class data
- DPO-first + imperative-fallback integration
- Meta-readiness fan-in
- Constraint lattice
- Acceptance file + A/B benchmark

**Track 4B (REMAINING — future session):**
Production pipeline integration. Requires modifying driver.rkt, typing-core.rkt, zonk.rkt, and 12+ other files.
- Wire typing rules INTO process-command (the "deployment" step)
- expr-meta→cell-ref migration (34 arms, 15 files)
- Zonk.rkt deletion (~1,300 lines)
- ATMS wiring into DPO rules
- Bidirectional bridge deployment
- Scaffolding retirement (8 items)
- PIR

This follows our "Validated ≠ Deployed" principle — Track 4A is validated, Track 4B deploys it.

**Phase 4b-ii-a: all meta lookups use cell-id fast path** ✅ (commit `4b8f3876`)
- Converted 6 slow-path sites across 6 files: reduction, typing-core, pretty-print,
  unify, trait-resolution, metavar-store
- Cells now sole authority for meta solutions in entire production codebase
- Bug found + fixed: stale cell-ids from prior command networks → guarded read with
  fallback to CHAMP. Discovered via test-io-csv-02 regression.
- Lesson: cross-command cell persistence is real. Guard pattern (try fast → catch → fallback)
  is the right approach for expr-meta cell-ids that outlive their network.
- 7641 tests, all pass

**DEPLOYMENT: typing rules active in production pipeline** ✅ (commit `2a8c3871`)
- infer/err now dispatches through typing-rule registry (current-typing-rule-infer parameter)
- driver.rkt installs make-typing-rule-infer(infer) at startup
- LEAF RULES ONLY deployed (12 of 21): literals, type constructors, universe
- Non-leaf rules (variable, binder, application, meta) cause 53 failures when enabled:
  imperative path creates metas/constraints as side effects that pure rules skip.
  These become safe when constraint creation moves on-network (Phase 6+).
- Key lesson: the typing rules are CORRECT (parity tests prove it), but the imperative
  path's side effects (constraint creation, meta solving, trait resolution) are deeply
  coupled. Pure rules can only replace leaf computations. Non-leaf replacement requires
  the CONSTRAINT INFRASTRUCTURE to also be on-network.
- This is the "Validated ≠ Deployed" deployment step: 12 rules now active in production.
