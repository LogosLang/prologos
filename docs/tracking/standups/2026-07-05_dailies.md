# Dailies — 2026-07-05

Fresh dailies rolled. The `2026-06-29_dailies.md` had reached ~1205 lines ending at a **stale N3d checkpoint** (6 days / many sessions behind — it was never updated through Numerics N3e→N6, the Num Series, DEMO, or CIU Track 6), and the working topic has shifted (Numerics → CIU Path Selection). This file covers the **Num-Series-founding + CIU-Track-6** arc. Prior work (Numerics N3e→N6 completion) lives in git history + the Num Track 1 design doc; the `2026-06-29` dailies remains the record through N3d.

---

## State snapshot

- **HEAD**: `35b3bc90` (CIU T6 F2/F3/F4 complete)
- **Suite**: GREEN **8530 / 448 / 0**
- **Branch**: `main` (ahead of origin; don't push unless directed)
- **Working tree**: pre-existing OWNER WIP only (42 items: `docs/standups/*.org`, `MASTER_ROADMAP.md`+`LANGUAGE_VISION.md` deletions, `examples/*.prologos`, `.claude/settings.local.json`, untracked owner files) — **leave alone, never blind-commit**

---

## Num Series founded (commit `0965daab`)

- **`2026-07-02_NUM_MASTER.md`** — home for all numerics-systems work; linked in `MASTER_ROADMAP.org` § Active Series (between FFI and SH).
  - **Track 1** = Numerics (Float / refinement-via-trait / tower / literals / ergonomics) — **N6 complete** (N1–N6; N6f reconciliation capstone done) → unblocks DEMO P1.
  - **Track 2 (proposed)** = Generic `Num` type (constraint-as-type numeric fns, `spec square Num -> Num`), seed note **`2026-07-02_GENERIC_NUM_TYPE_NOTE.md`**. Grounding-audited: homogeneous `square` tractable (constraint-as-type desugar + keyword→trait routing); **heterogeneous multi-arg = net-new** (relational `NumJoin A B C` needs fundep/output inference); **runtime already heterogeneous** (only static typing blocks). Deferred to its own track (out of focus for the demo).
- **gh issue #76** filed — a section over a fully-specced user function doesn't infer its type in a *no-expected-type* position (works in `map`/applied/annotated; fails on a bare `def := [pow 2 _]`). Includes the `[T->T]`-vs-`<T->T>` annotation footgun.
- The `pow` HoF/`map` "could not infer" report reconciled = the known **REPL session-persistence bug** (Emacs REPL loses ns/def context between evals; VSCode-after-reset works) — **not a code bug**.

## CIU Track 6 opened — Path Selection & Anonymous-Record (`Map`) typing (commit `ca4dbb73`)

- Design doc **`2026-07-05_PATH_SELECTION_RECORDS_DESIGN.md`**; added as **CIU Track 6** in `2026-03-21_CIU_MASTER.md`.
- **Origin**: `m.{a.a1 b.b1}` reported a misleading "`.{ }` retired for mixfix" error (the `.{`→`.( )` mixfix migration made the WS reader emit `$mixfix-retired` for *every* `.{`, hard-`raise`-aborting the file). Reworded to "not currently supported — postfix path-selection is under redesign" (commit `94ae750a`).
- **Locked decisions (D1–D5, §2a):**
  - **D1** — `Map` IS the anonymous **open record** type (structural/observational typing); **retire the internal `Open` type**.
  - **D2** — `Map` ⇄ `schema` interop, both directions.
  - **D3** — unify Map + schema as *one* structural-record notion (axes: named/anonymous × closed/open).
  - **D4** — home = CIU Series (relates to Track 2 dot-brace sugar + Track 3 Indexed/Keyed unification).
  - **D5** — fundamentals-first + **WS-first** (wire all the way through to WS; regression-test in WS `.prologos`, not sexp/`.rkt`).
- **Grounding audit** (run `wf_b5bda45e`, §4a):
  - `Open` minted at `elaborator.rkt:2127/2132`; it's an α-semantic wildcard threaded through check/unify/qtt.
  - schema per-field projection **WORKS** (`typing-core:1536`) — the audit's "schema-typed dot-access broken" was a **PROBE ARTIFACT** (it used non-canonical `schema :=`; the **block form** works, `alice.age → 30 : Int`). **V1 de-risked** (main-session R-lens). *Lesson: re-verify every audit "broken" finding with canonical syntax.*
  - **No row-polymorphism** anywhere (net-new); `expr-Map` carries one uniform value type (no per-key slot).
  - `expr-get` unifies PVec/Map/List at the node level but they're disjoint at the **trait** level (`Indexed {C:Type→Type}` vs `Keyed {C:Type→Type→Type}` kind-arity) → **CIU Track 3 owns the Array⇄Map unification**; reconcile, don't parallel-design.
  - Postfix juxtaposition `coll{selector}` is **feasible + cheap** — `arr[i]` (`$postfix-index`, reader adjacency) is the precedent; hook = mirror the lbrace-adjacency check → `$brace-select` sentinel + a `rewrite-dot-access` fold arm. No ambiguity.

## Fundamentals F2/F3/F4 DONE (commits `af161de7`, `142da071`, `35b3bc90`)

- **F2** — registered the 5 path AST nodes (`expr-path`/`Path`/`get-in`/`update-in`/`broadcast-get`) in `pnet-serialize` (were missing → vector-impostor crash under a stale `.pnet` cache). Verified via a two-run `core/path` repro.
- **F3** — `recognize-keyword` (`parse-reader.rkt:519`) now includes `^` → WS `:key^alias` rename tokenizes (was excluded while `ident-continue?` allowed it). Branching/rename → projected-map deferred to V4/F1.
- **F4** — `tests/test-first-class-paths.rkt` (WS-syntax coverage). **Perf lesson**: a full-prelude WS `process-file` test balloons to ~48s under 10-worker suite contention → use `:no-prelude` for language-level features (kept it ~0.6s). Broadcast `.*` needs `'[…]` (= prelude); verified via probe, deferred.

## NEXT — F1 (the core): retire `Open` → structural `Map` typing

Goal: `{:a 1}.a : Int`. Approach: **generalize schema's working per-field projection to anonymous literals**. **Three open design decisions (co-design, NOT decided):**

- **Q1 — representation**: reuse `schema-entry` as an anonymous inline record carried on the `Map` type (per D3), vs a new AST node (full pipeline-exhaustiveness cost).
- **Q2 — row-polymorphism depth**: start structural + open-by-default; defer row variables?
- **Q3 — scope split**: F1a = `{:a 1}.a : Int` + retire `Open` first; then F1b = `Map`⇄`schema` record subtyping/interop.

**Sites**: `elaborator.rkt:2127` (Open mint) · `typing-core.rkt:1509-1548` (projection arms; schema arm `:1536` = the template) · `qtt.rkt:1206-1220` (**MISSING an Open arm** — co-change per pipeline.md files 7+8) · α-semantic Open behaviors to narrow (`typing-core:2608-2609`, `qtt:2140-2141`, `unify:574-575`).

Handoff written: `docs/tracking/handoffs/2026-07-05_CIU_TRACK6_PATH_SELECTION_handoff.md`.

---

# 2026-07-06 — CIU T6 F1: co-design → D.2 → s1 (records **and** the collection reframe)

The whole F1 arc landed today: full co-design (D1–D14), a frontier-research pass, an independent adversarial critique, the D.2 revision, and the first implementation slice (F1a-s1). Running narrative + the lessons worth keeping.

## The arc, in order

1. **Grounding reframed F1 before any code.** A `grounding-audit` workflow + main-session R-lens on the actual sites produced the pivotal empirical finding: **annotated maps already project concretely** (`def am : (Map Keyword Int) := {:a 1}` → `am.a : Int` *today*). So F1's entire job is "make the *unannotated* literal's value type concrete instead of `Open`" — the projection machinery is done. This also caught that the design doc's own `~1536` "schema-projection template" pointer named the **wrong arm**: anonymous `{:a 1}` infers `(expr-Map ?km Open)` and hits the expr-Map arm at `typing-core:1512`, not the schema-fvar arm at `:1536`. **Lesson: even a doc you wrote, grounded against HEAD, can name the wrong site — the audit earns its keep.**

2. **Frontier research (D6–D9).** Owner pointed at the 2026-06-29 "Maps↔Schema as co-data" conversation; a 7-thread research workflow confirmed the architecture: rows-as-primitive (Tang OOPSLA'23 erasure-mode), `Open` = Sekiyama–Igarashi's `★` dyn tail + `&{}` negative-top (so **relocate, don't just delete**), seal-as-tabulation, one-solver row endgame (Toohey POPL'26), OSF meet-as-unification. Captured as a brief research note (D9 — engineering-grounding only, never paper-gating).

3. **Merge / refinement / codata (D10–D12).** Owner's `[merge m1 m2].a` intuition is exactly the `Concat l r u` relation; the impossibility (Wand'91 finite typings; Palsberg–Zhao NP-complete) is *only* for unconstrained-principal-simple merge — **the tweak is residuation** (fundep-directional 3-cell constraint in the one solver — the most mantra-literal thing in the design). Records = refinement types (conjunction of per-label observation facts; **no record-subtype judgment, ever** — D11). Codata is substrate-level only in F1 (observation-indexed carrier + tabulation boundary); the co-GADT/`DeepConcat` generalization homes at SRE-5.

4. **The collection reframe (D13/D14) — the owner's biggest steer.** Heterogeneous collections aren't a regression to defend against; they're a **design goal**, the positional dual of records. Probe: `@[1 "a"]` **errors today** (`expr-PVec` has one uniform elem slot, exactly like `expr-Map`'s v slot) → net-new capability. A second research workflow + owner co-design settled: **one carrier, two surface nodes, a key-domain axis** (Keyword→record, Nat→tuple, dyn-tail→dictionary/array — the 2×2). Owner-blessed Q_A–Q_E: one-carrier/two-nodes; forbid mixed-key; closed tuples v1 (variadic deferred); **tuple-by-default** (Roc-like, vs TS's array-by-default `as const` friction); end-state (b) (`Map`/`PVec` are the dyn-tailed/uniform instances of the one carrier).

5. **D.3 independent adversarial critique** (5 lens critics + adjudicator, HEAD-pinned, live probes): **4 BLOCKING + 12 SIGNIFICANT confirmed, 2 refuted.** The blockers were real and partly *mine*: B1 (my "the seed never consults annotated checking" claim was false — the term-directed check recurses to the seed at `typing-core:2424`); B2 (the Record<:Map arm is unreachable from qtt's *duplicated* fallback); B3 (Record-vs-Record unify undispositioned — het collections regress, invisible to `:no-prelude` gates); B4 (my dynamic-key degrade dropped the key check). Recorded at `2026-07-06_CIU_T6_F1_STAGE3_CRITIQUE_D3.md`.

6. **D.2** folded the collection reframe + every critique amendment + the 7-question resolutions. Uncertainties closed before committing: order-significance is **free from position-keying** (no non-commutative monoid needed for F1a); union-widening for flavor B is representable + projects today (the earlier `pvec-nth` "gap" was a `0`-vs-`0N` *index-type* mistake of mine); distributivity holds by construction.

7. **F1a-s1 implemented + committed (`7330b582`).** The `expr-Record (key-domain fields tail)` + `record-field (type presence)` carrier through 13 pipeline sites, purely additive (nothing mints it yet). Suite GREEN **8530/448/0 == baseline**. New `tests/test-record-node.rkt` (15). Design-doc §2 tracker → F1a-s1 ✅.

## Surprises / design choices worth remembering

- **The empirical reframe beat the abstract framing.** The original Q1/Q2/Q3 (representation / row-depth / scope-split) dissolved once we *ran* the code: F1 = "make the unannotated vt concrete," and the real fork is uniform-join vs per-field, not the abstract carrier menu. **Run it before theorizing about it.**
- **`Open` is formalized prior art, not debt** — it's `★`/`&{}`. That flipped D1 from "delete with scaffolding" to "relocate into the dyn tail, then delete" (D7). A nicer, more principled story.
- **One carrier, decomplected laws.** The frontier unifies the row *substrate* (Morris–McKinna monoidal rows; Ur/Web tuples-as-Nat-records; Roc same runtime rep) but *nobody unifies the surface* — Casinghino/Tjoa (ML'24) explicitly rejected reorderable tuples for order/layout reasons. So: shared carrier + key-domain tag selecting the law-set. **Active-enforcement caveat noted in the design: a law that reads the carrier without checking the key-domain tag is a latent law-crossing bug.**
- **S2 (generic-walker blindness) was demonstrable, not theoretical.** `occurs?`/`collect-meta-ids` recurse via `struct->vector` but a *list field is not a struct*, so field metas were invisible — probe confirmed, explicit Record arms fix it. Codified a candidate pipeline.md checklist add: "sub-exprs inside a non-struct container → audit every generic walker."
- **Grounding error caught by the critique**: D.1 claimed "no map `merge` exists" — it does (`lib/.../map.prologos`, right-priority, feeds `impl Lattice`). D10's *semantics* lock survived; the "no legacy" justification didn't. **Re-verify grounding claims across the whole tree, not just the obvious dir.**
- **Delegation discipline held**: grounding-audit + design-options-panel + two research workflows did the reading/enumeration in disposable context; the **design dialogue + the D.2 authoring + s1 implementation stayed main-session**. Kept main context lean across a very large arc.
- **`record-map-field-types` helper** centralized the ~7 field-recursion sites (shift/subst/zonk/nf/…) into one reconstruction point — small blast radius, uniform.

## F1a-s2 DONE — the goal slice (`{:a 1}.a : Int`) landed

**Delivered**: elaborator classifier + record-seed mint; projection (`map-get`/`get` → `record-project`); B1 (empty-record seed-check arm); B2 + the Record<:Map subsumption arm; qtt delegation; the **full map-op surface** (dissoc/keys/vals/size/has-key/nil-safe-get — pulled fwd from s3, since minting records makes these encounter records); smart-constructor helpers. Suite GREEN **8545/449/0**; acceptance **27/27** (now a suite gate, `test-f1-records-acceptance.rkt`). `{:a 1}.a : Int` ✓, `+ q.a 1 : Int` ✓ (V5), heterogeneous per-field ✓, `idm r` subsumption canary ✓, exact `assoc`/`dissoc` ✓, closed-row-miss error ✓.

### The one real regression + the design refinement it forced

The only non-display-churn failure was `postfix-index-03`: `def xs : (List (Map Keyword Nat)) := '[{:age 25N} …]`. Diagnosis took several passes and was worth it — it revealed that **D.2's "the Record→Map α is only a `check` arm; leave `unify`/`subtype-predicate` untouched" was insufficient**. The α must ALSO be reachable from (a) `subtype?` + `structural-subtype-ground?` (nested subsumption: `(List Record) <: (List Map)` recurses through the covariant List walk — which is `structural-subtype-ground?`, NOT `subtype?`, so the bridge had to go in BOTH), and (b) `classify-whnf-problem` in `unify` (polymorphic subsumption: `(cons {:age 25N} nil)` commits `cons`'s element param to the record, which meets the `(Map K V)` annotation through `unify`, not `check`). **D11 (no record<:record) is preserved** — this is the record→Map *bridge* (a record projected to its uniform-Map view, Q_E), reachable wherever a record meets a Map in a subsumption context. Pre-s2 it "worked" only because `(Map K Open)`'s `Open` unified with `Nat`; records don't. Documented in the design doc §11a.

### Surprises / lessons (s2)

- **Minting is the point of no return for annotation-satisfaction** — the moment `{:a 1}` types as a record, every Record-vs-Map flow (direct literal via B1, fvar via subsumption, nested via subtype?, polymorphic via unify) must be handled or it regresses. The atomic slice is bigger than "projection."
- **The α needs FOUR reach points, not one** (check-subsumption, qtt fallback, subtype?/structural-subtype-ground?, unify classify) — the "one local check arm" framing under-counted. Each is a different entry into subsumption.
- **`last-string` skips non-strings** — the T-2 tests' error cases can't be asserted via `last-string` (it returns the prior def line); use `(ormap prologos-error? result)` on the raw list.
- **The T-2 "Open by Design" contract file is now superseded** (D7) — its α-Open-trust tests (map-get-on-non-map-returns-none, etc.) now assert the sound static behavior (projecting a field off a scalar = type error). More sound; a real (intended) semantic change for dynamic-map idioms → use `(Map K V)` annotation or `nil-safe-get`.
- **Diagnosis discipline paid off**: the postfix regression took ~5 hypothesis-test cycles (subtype? bridge → structural-subtype-ground? bridge → unify direction). The var-vs-literal isolation probe (`def xs := zs` works, `'[…]` fails) was the key that localized it to polymorphic-instantiation-via-unify.

## Carried forward → F1a-s3 + F1a-col

s3: B3 Record-vs-Record same-shape unify classify + prelude WS test; B4 dynamic-key acceptance canaries; S3 fold/filter/map-map-vals arms + union-arm Record components; S7 rich closed-row-miss diagnostic (currently plain "Could not infer type"); S10 trait ground-expr? arm; cross-module `.pnet` canary. F1a-col: flavor A (tuple mint + classifier) + flavor B (union-widening). Owner edited the acceptance file (Posit32, foreign block, posit exprs) — the suite gate covers it.

---

## ✅ CHECKPOINT — 2026-07-06, F1a-s2 complete (resume anchor)

- **HEAD** `589fb067` · **Suite GREEN 8545 / 449 / 0** · **Working tree** = pre-existing owner WIP only (leave alone).
- **The goal is DONE**: `{:a 1}.a : Int` end-to-end (records, projection, subsumption, full map-op surface). F1a-s1 (carrier pipeline) + F1a-s2 (minting + goal) committed.
- **RESUME HANDOFF** (read first): `docs/tracking/handoffs/2026-07-06_CIU_T6_F1_records_s2_handoff.md`. Design: `docs/tracking/2026-07-06_CIU_T6_F1_STRUCTURAL_RECORDS_DESIGN.md` (§11a = the s2 design refinement: the record→Map α reaches FOUR subsumption points, not one). Memory: `ciu-t6-records`.
- **NEXT**: F1a-s3 (B3 first — needs a prelude WS test; then S3 map-op-tail arms, S7 diagnostic, cross-module `.pnet` canary) → F1a-col (tuples + flavor-B collections) → F1a.2 (Open relocation) → F1b.
- **Commits this arc**: `40fd8158` (research D6–D9) · `ead54e27` (D.1/D10–D12) · `f031e6d8` (D.3 critique) · `6d6a24ca` (Pre-0) · `8d011816` (D.2/D13–D14) · `7330b582` (s1) · `679b8d22` (dailies) · `589fb067` (s2).

## ✅ CHECKPOINT — 2026-07-06, F1a-s3 COMPLETE

- **HEAD** `d08e14bf`+docs · **Suite GREEN 8586 / 451 / 0** · acceptance **37/37** · working tree = owner WIP only.
- **s3 opened per Stage-4 protocol**: grounding audit (`wf_2d535113`, 4 facets + critic) corrected the enumeration (both union arms, check-side arms, whnf wrappers, helper extraction, S7's callers) and resolved S10 (already s2).
- **B3 `a5c546c8`** — Record-vs-Record same-shape classify case ('sub per-field goals; explicit key-domain+tail guards, forward-necessary for 'nat/'dyn); `'[{:a none} {:a [some 1]}]` : `List {:a (Option Int)}` (errored before); different-shape stays the named F1a-col regression, clean error. Prelude WS test `test-record-collections.rkt`. Known limitation documented: degenerate cross-command stale-meta force-solve (pre-existing, surfaced-not-caused, zero suite hits).
- **S3+B4 `83bd416f`** — fold/filter/map-vals Record arms in BOTH checkers (filter = dictionary-degrade; map-vals = LABEL-PRESERVING `{:n Int :s Int}`); both union arms accept Record components (Q5 filter; keyword-literal = pure lookup, no rollback); `record-value-union` + `union-record-component-vt` extracted; whnf wrappers; B4 dynamic-key canaries (Keyword→degrade, String→error). Probe caught a `map` pattern-var shadowing bug pre-test.
- **S7+canary `d08e14bf`** — rich closed-row-miss ("field :z is not present in the record {:n Int :s String}; available fields: :n :s"; 6-field cap; empty-record wording; works through computed sub-exprs) via the #70-precedent post-hoc walk; `test-record-pnet-cache.rkt` commits the F2 two-run repro (write→read→USE across the cache boundary) as a permanent test.
- **Close notes**: design doc §11b (S10 posture, non-reachability assertions incl. match-over-records probe, S7 check-path note).
- **NEXT**: F1a-s4/s5 mostly absorbed (acceptance flips happened incrementally; s5 = bench-ab vs Pre-0 baseline + PIR-lite) → **F1a-col** (flavor A tuples `@[…]` + flavor B union-widening) → F1a.2 (Open relocation) → F1b.

## Watching / lessons

- **Re-verify audit "broken" findings with canonical syntax** before trusting (schema false-alarm this session).
- **WS-first + WS `.prologos` tests** (owner standing preference; the design target for usability).
- **Options spaced into clear blocks** in design dialogue (owner, 2026-07-05; memory `design-dialogue-preference`).
- **`:no-prelude` for language-feature WS tests** (F4 perf lesson — full prelude × suite contention = ~48s).
- **Run it before theorizing** (F1, 2026-07-06) — an empirical probe (annotated maps already project concretely) reframed the whole design and dissolved the abstract Q1/Q2/Q3 framing. Ground the *behavior*, not just the code coordinates.
- **Grounding claims: verify tree-wide, not just the obvious dir** (2026-07-06) — D.1's "no map merge exists" was false (it's in `lib/.../map.prologos`); the D.3 critique caught it via a whole-tree grep + probe.
- **Generic `struct->vector` walkers are blind to list-valued struct fields** (2026-07-06 S2) — `occurs?`/`collect-meta-ids` stop at a `fields` list because a list isn't a struct. Any new node with a list-of-sub-exprs field must audit every generic walker (candidate pipeline.md checklist add).
- **One carrier, decomplected laws** (2026-07-06 D13) — unify the row substrate, split the surface + laws by a key-domain tag; watch for a law reading the carrier without checking the tag (latent law-crossing bug).
- **Delegation vs main-session held across a huge arc** (2026-07-06) — grounding-audit + design-options-panel + research workflows in disposable context; design dialogue + doc authoring + implementation main-session. Kept context lean.

## ✅ CHECKPOINT 2 (2026-07-13) — F1a-core CLOSED; F1a-col opened under D15

- **F1a-s3 complete**: B3 (`a5c546c8`), S3+B4 (`83bd416f`), S7 rich diagnostic + `.pnet` cross-module canary (`d08e14bf`). **F1a-s5 close** (`9d03aeb8`): suite GREEN **8586/451/0**, acceptance 37/37; like-for-like perf (exact Pre-0 file, post-s3 compiler): **net faster ~125→117ms** (type_check −25%; qtt +13ms = delegation cost, WATCH); bench-ab all noise-level, reference saved. PIR-lite = design doc §11c.
- **F1a-col opened**: grounding `wf_8c01845e` (seed-keyed classifier forced by the 2nd pvec producer; in-place meta widening contract-blocked at both solve entry points; `'[…]` has 4 producer sites; `record-subtypes-map?` `'nat` key-gate = must-fix; owner-intent evidence found in scratch: `@[{:a 1} {:b 2}][1.b]`).
- **D15 locked** (owner): *observational literal typing* — heterogeneous literals mint per-position rows in BOTH bracket forms; union-element list = derived α only, never minted ("the type is what the literal is observed to be"). Mechanism = literal-extent nodes (Q_col-A blessed). Display `⟨Int String⟩` (Q_col-D).
- **Path Selection = OPEN design conversation** (persisted in track doc §2a): postfix `coll[…]` path selection (supersedes braces), broadcast `coll.[…]`, result-shape crux — deep co-design before surface commitment; col ships only degenerate literal-index.
- Lesson: the probe-first + grounding-audit-per-slice discipline caught the pattern-var shadowing bug, the S10 tracker drift, and the classifier-siting fact (2nd producer) before any of them cost a debugging session.

## ✅ CHECKPOINT 3 (2026-07-14) — F1a-col-1 + col-2 LANDED; handoff written

- **col-1 ✅ `cf034b56`** — tuples: `expr-pvec-literal` node; `@[1 "a" true] : ⟨Int String Bool⟩`; unification-based homogeneity (`@[none [some 1]]` stays `PVec (Option Int)`); positional projection incl. bare-Int; the owner-scratch idiom `@[{:a 1} {:b 2}][1].b ⇒ 2 : Int`; Tuple→PVec α (union-branch aware); `'nat` key-gate must-fix. Suite 8601/452/0; acceptance 48/48.
- **col-2 ✅ `c6c3ef3a`** — lists under **D15** (observational literal typing, owner-locked): `'[{:a 1} {:b 2}] : ⟨{:a Int} {:b Int}⟩` — **the D.3 named regression CLOSED**; union = derived α only. Suite **8604/452/0**.
- **Route discovery** (the arc's big lesson): WS files lower `'[…]` via the PREPARSE sentinel, not the tree route the audit read — diagnosed by driver instrumentation after a def-route "Multiplicity violation" showed a raw cons-chain body. Fix mirrors `$vec-literal`: `$list-literal-parse` parser handoff. + qtt fallback runs elements INFER-mode → operator lists (`'[int+ int*]`, issue-#76 class) need the dedicated CHECK-mode checkQ arm (test-prim-op caught it).
- **Test-infra find**: the `.pnet` canary's leftover top-level cache file silently disabled the runner's prelude-cache regeneration since s3 → compiler-drifted caches → 10-worker dead-at-30s (twice). Canary now cleans up; caches regenerated; runner-hardening spawned as task chip `task_5e3ec9c2`. Stale-log category exercised (`test-generic-arith-02` — no source file).
- **Known v1 limit** (pinned type-only): generic folds over row-lists type-sound, value-stalls (S10-at-list-level; `List <union>` fine; α = escape hatch; CIU T3/T5 home).
- **Handoff**: `docs/tracking/handoffs/2026-07-14_CIU_T6_F1_col2_handoff.md`. Resume = **col-3** (tuple-op dispositions) → col-4 → F1a.2 → F1b. Path Selection = OPEN owner design conversation (track doc §2a) — surface not to be built until the co-design happens.

## ✅ CHECKPOINT 4 (2026-07-14, resumed session) — F1a-col-3 LANDED

- **col-3 ✅ `f6716b3a`** — Record ('nat row) arms across ALL pvec ops. EXACT-where-static: nth (record-project delegate), push/update/pop (row grows / literal in-bounds replace / drop-last), concat (ground×ground renumbered append), slice (clamped half-open sub-row), length, from-list (row IDENTITY — rows carry no container tag). Uniform-⋃ elsewhere: to-list `(List ⋃)` (escape-hatch view, deliberately NOT row-identity — would grow the pinned S10-at-list-level stall surface), fold, map (position-preserving constant-W, the map-map-vals mirror), filter `(PVec ⋃)`, dynamic index/bounds, transient `(TVec ⋃)`. Suite GREEN **8625/453/0**; acceptance 48/48; `tests/test-tuple-ops.rkt` (21, :no-prelude WS).
- **Stage-4 protocol held**: grounding-audit `wf_d09c38a7` (4 facets + critic, HEAD-pinned `64529290`) BEFORE code — it re-grepped the stale arm coordinates (arms at `typing-core:1919-2014`, not the prior audit's 1845-1933), surfaced **push/length/from-list/transient** as same-class gaps missing from the handoff's op enumeration, caught the **check-mode arms** (typing-core fold/map + qtt checkQ fold/map/filter hard-fail tuples with no α fallthrough — the issue-#76 class again), and **refuted "usage-only = no qtt work"**: the pvec echo arms (`(tu (tu-type r1) …)`) return the STALE row once ops are type-changing (the in-code §6 divergence class) → conditional delegate-to-infer landed; plus qtt to-list's resolution fallback silently echoed the row as the "List" type (parity bug, no crash) → fixed; plus checkQ conversion fallback lacked the Tuple→PVec/List α mirrors (B2 completion) → added.
- **Decisions made in-slice** (implementation-level, mirroring locked precedents; no new D-numbers): (1) **Nat-only index discipline** for pvec-* tuple arms — probe caught Int-literal bounds type-checking exactly but VALUE-stalling (pvec-* runtime is `nat-value`-only; `expr-get` has Int `index-value`) → Int literals rejected on pvec-*, `v[i]` keeps col-1's Int tolerance. (2) to-list = uniform view, NOT row-identity (S10 stall-surface argument; from-list IS row-identity — runtime becomes an rrb where every op works). (3) EXACT arms guard `closed-nat-row?` ('nat + 'closed explicit, B3 forward-necessary). (4) slice/concat renumbering inline via `make-record` with computed labels (no new helper needed — make-record sorts, callers renumber).
- **Probe-first paid again**: the Int-index value-stall and a `cond`-vs-`match` `[_ …]` compile error were both caught pre-test; the "map/filter errors" in the first probe were CORRECT rejections (f : Int→Int can't consume ⟨Int String Bool⟩) — probe misread, not code bug.
- **Suite incident (transient, logged for `task_5e3ec9c2`)**: first full-suite run mass-froze at 279/453 (~all 5130 run tests PASS, then NO completions for 600s → watchdog killed 174; the pnet canary was in flight and left its top-level artifact). Canary passes standalone (cleans up); artifact deleted; re-run healthy end-to-end **8625/453/0 in 132.4s** (normal band). Not reproduced; the leftover-artifact-on-kill window is itself a hardening argument (cleanup runs only on normal completion).
- Pre-existing noise noted: "PRELUDE DRIFT DETECTED" runner warning (manifest vs namespace.rkt; owner-WIP-adjacent, non-blocking, untouched); bare `def et := @[]` Multiplicity violation is pinned legacy behavior ("needs checking context", test-pvec.rkt:302).
- **NEXT**: **col-4** — remaining canaries (tuple-op acceptance-file sections; aliased-PVec whnf canary; prelude `pvec-from-list '[…]` surfaces in `test-record-collections.rkt`) → col close (bench-ab vs `data/benchmarks/f1a-core-post-s3-2026-07-06.json` + tracker) → F1a.2 (dyn tail + Open relocation) → F1b.

## ✅ CHECKPOINT 5 (2026-07-15) — F1a-col-4 LANDED; **F1a-col CLOSED**

- **col-4** — canaries + doc route correction + col close. Suite GREEN **8646/453/0 in 119.7s** (+16 acceptance markers, +5 prelude test-cases); acceptance **64/64**; bench gate clean.
- **Canaries**: acceptance ;;50–65 (tuple-op WS showcase: nth/push/pop/concat/slice/update EXACT; to-list/map/filter views; from-list∘to-list round-trip type-loss; **aliased-PVec whnf** ;;61–63; static-miss + Int-index-discipline errors). `test-record-collections.rkt` +5: keyword `pvec-from-list` on a row-list = row IDENTITY; from-list∘nth composition; wrapper `pvec-from-list-fn` **α-degrades DEF-BOUND rows** to `(PVec ⋃)` while **INLINE literals stay per-element-check errors** (the C2 class — route-sensitivity pinned BOTH ways); `(List ⋃)` value-reduces through prelude `length` (S10 stall = ROW-lists only).
- **Grounding audit `wf_80ef80da`** (4 facets + critic at `016251dd`) earned its keep three ways: (1) established the aliased-admission SCOPE precision — from-list/transient whnf'd PRE-col-3, so the new admission is the 11 PVec-subject arms only (a mis-attribution the col-3 framing would have baked into comments); (2) found `bench-ab --ref` is documented-but-unimplemented + the baseline path is compiler-dir-relative; (3) flagged the `pvec-from-list-fn` row behavior as a decision. On (3) the critic's adjudication (hard error) was **half-wrong** — main-session R-lens probing found the DEF-BOUND form α-degrades cleanly; the facet had probed the INLINE form (per-element check). *Lesson: when a probe result drives a pin-vs-fix decision, re-probe BOTH binding routes (inline literal vs def-bound) before deciding — literal typing is route-sensitive by design (C2/B1).*
- **Doc route correction**: dated ✏ amendments at §1 (D15 supersedes flavor-B-as-union), §3 B3 row (the "elaborator list = cons chains" claim that misled the col audit — preparse `$list-literal-parse` route documented), §4.2 (no elaborator classifier landed; literal-extent nodes + all-at-once typing; `:2448` drifted), §4.6 (delimiter resolved space-separated; col-2 DID touch preparse/parser), §9 (Flavor A/B un-deferred); new **§11d** F1a-col close notes (route discovery, dispositions, decisions, bench verdict, current coordinates + drift caveat).
- **Bench close gate**: `f1a-col-close-2026-07-15.json` (10 runs) vs `f1a-core-post-s3-2026-07-06.json`: **every program −0.6%…−13.6%** (no regression; adversarial tier −5%…−11%, CVs ≤1%). Attribution spans col-1..3 + the two test-infra commits (`c457f479` pregen hardening likely the ambient gain) — recorded as "no regression", not a col speedup.
- Between-session note: the runner hardening chip (`task_5e3ec9c2`) landed as `c457f479` and the collection-path batch flake as `016251dd` (both owner/chip sessions) — the col-3 transient mass-stall class is now structurally closed (generation stamp + recursive cache check).
- **F1a-col is CLOSED** (col-1 `cf034b56` → col-2 `c6c3ef3a` → col-3 `f6716b3a` → col-4). **NEXT**: **F1a.2** (dyn tail live + `Open` relocation/deletion, D7/D1-b — own mini-design per tracker) → F1b (erasure-mode width + seal) → F-row. Path Selection = OPEN owner design conversation (track doc §2a) — still not building `v[1.b]`/`coll.[…]` until it happens.

## ✅ CHECKPOINT 6 (2026-07-15) — F1a.2 OPENED: mini-design locked (D16–D20) + p0 COMPLETE

- **The opening ran the full protocol**: grounding audit `wf_2b58a7e2` (4 facets + critic) → design-options panel `wf_71c14484` (4 clusters, propose/adversarial-critique/synthesize) → owner co-design (Q1–Q5 answered) → **D16–D20 locked** (track doc §2a round 5) → mini-design **§12** in the design doc (disposition table, row-comparison engine grid, per-op dyn table as p1a spec, F-row pins, phase ladder p0→p1a→p1b→p2→p3). Docs `767bbcae`.
- **The audit rewrote the charter's premises**: FIVE Open producers (not 2), ~44 sites (not 38); the `unify:579` wildcard is strictly coarser than S–I C_Cons (absorbs metas WITHOUT solving — the ordering is re-made as solve-first, D16); key-domain cannot express `(Map String V)` → **expr-Map survives**, full Q_E(b) absorption deferred to one coherent carrier change; the tail goes live as the **bare `'dyn` symbol** (bounds-free ★ — zero carrier/walker/pnet churn); S9 was never implemented and is folded into D20 (`{| _}` for dynamic update-in AND dissoc).
- **Owner decisions**: D17 bare `{}` = keyword-committed empty dyn row (owner-signed semantics change; string keys = annotation escape hatch; heterogeneous key-types design space DEFERRED w/ note); D18 mixed-key seed = `(Map ?km ⋃observed)`, no global assoc-widening; D19 projection-on-dyn = fresh meta only, recording descoped to F-row (pins in §12.5); Q4 unsolved metas display (tighten later, DEFERRED.md).
- **p0 fixes `803a57e4`** — the audit's two live "Multiplicity violation" bugs root-caused inside the time-box and FIXED-BEFORE (shared machinery per the branch rule): (1) **inferQ had no `expr-meta` arm** — bare `{}`'s unsolved `?km` tu-errored through map-empty; (2) **the union-expected arms branch-split only** — a term whose inferred type IS the whole union failed both branches, so EVERY bare-union-typed def died at checkQ-top (and annotated ones at check — a THIRD site in typing-core found during verification). Whole-union conversion fallback added to both checkers, right branch rollback-wrapped, single-branch preserved. Why the suite never saw it: union results were only bare-eval'd, never def'd. `test-qtt-union-meta.rkt` (6) + acceptance ;;66–73. Suite GREEN **8660/454/0**; acceptance **72/72**.
- *Lesson (panel adjudication vs main-session probe)*: the panel critic adjudicated "prelude from-list-fn on rows = hard error" from a facet's inline-literal probe; the def-bound form α-degrades cleanly — and later the SAME shape recurred with the union fix (annotated vs unannotated paths differ). **Literal/binding route sensitivity is now a standing probe discipline: always probe inline AND def-bound, annotated AND unannotated.**
- **NEXT = p1a** (consumers-first, dead-code-safe): the row-comparison engine + §12.4 per-op dyn table + 5 helpers delegated + C_Cons arms (solve-first) + record-project dyn hook + negative differentiating probes. Then p1b (atomic mint-flip + PNET_VERSION 1→2), p2 (mechanical deletion), p3 (gate).

## ✅ CHECKPOINT 7 (2026-07-15) — F1a.2 p1a LANDED (validated, NOT deployed)

- **p1a ✅ `bec0c058`** — every §12.4 consumer surface implemented before anything mints `'dyn`. Suite GREEN **8676/455/0**; `tests/test-dyn-rows.rkt` (16 synthetic hand-built-row tests) pins the grid including the **negative differentiating probes** — rejection cases the `:579` Open wildcard structurally cannot produce, so a rejection PROVES the dyn arms are live, per the W5 watchout.
- **The p1a audit (`wf_2687a16e`) reshaped the §12.3 engine before code**: (1) the module topology forbids a single engine — subtype-predicate (which unify requires) deliberately cannot see typing-core, so the PURE fragment lives there and the ctx/meta adapters in typing-core; (2) classify's `'sub` goals are pure unify pairs (no subtype kind, NO rollback) → row-vs-Map C_Cons semantics ride the EXISTING pure α arms — and the pure helpers' knowns-only checks turned out to BE the C_ConsL absorption (comment-only change!); only row-vs-row needed a new arm; (3) "solve-first vs metas" is STRUCTURAL (all record arms both-sides-concrete; flex-rigid/flex-app catch metas) — no arm placement gymnastics. §12.4 was rewritten as the implementation-precision spec table first, then code.
- **What changed**: unify C_Cons row-vs-row arm (containment guard + shared-label `'sub` goals; dyn-dyn-same-labels rides B3 via tail-eq); `record-<:-map?`/`-elem?` meta-solving REFUSED from dyn rows; `record-value-bound` (dyn → ⋃knowns∪fresh) at the 6 typing-core + 6 qtt enumerating sites; record-project 3-leg dyn hook (literal miss → fresh meta, dynamic → bound, empty-dyn projects); union-component dyn legs (miss contributes a meta rather than silently filtering); nil-safe `<fresh|Nil>`; S7 hint tail-guard. Exports: record-value-bound/record-project/union-record-component-vt (test surface).
- Audit also verified as NO-change: pnet ('dyn symbol rides the free path), pp (`{… | _}` display shipped since s1), union-sort-key (tail in the key — dyn/closed correctly distinct), the 23 `closed-nat-row?` guards (fail-safe), B1 empty-seed arm (tail-blind = correct for the empty dyn seed, T6 resolved), map-size/has-key/keys (sound as types), assoc/dissoc-literal (tail-preserving = correct).
- **Honesty marker per the ladder**: p1a is **validated (NOT deployed)** — dead consumer code until p1b's atomic mint-flip. The tracker row says so explicitly.
- **NEXT = p1b**: the atomic mint-flip (all five producers per §12.2: `{}` → keyword dyn row [D17], mixed-key seed → `(Map ?km ⋃observed)` [D18], RVU empty → fresh meta, assoc/dissoc dynamic degrades → dyn rows keeping knowns / `{| _}`) + **PNET_VERSION 1→2 in the same commit** + acceptance canaries ;;35/;;37/;;66 flip + D17 pins flip. The point of no return.

## ✅ CHECKPOINT 8 (2026-07-15) — F1a.2 p1b LANDED: the dyn tail is LIVE; Open is no longer minted

- **p1b-pre ✅ `07c0e70f`** — `expr-map-literal (keys vals chain)` through the full pipeline, dead code until the flip. The design honesty moment: D18's ⋃observed CANNOT ride the assoc chain (the seed pre-dates entry elaboration; a fresh-meta value slot breaks heterogeneous literals — the C2 class), so the mechanism is the twice-proven D15 literal-extent recipe at the Map domain. Landed consumers-first per the ladder.
- **p1b ✅ `6a2bc9d5`** — THE atomic flip: all five producers + PNET_VERSION 1→2 in one commit. `{}` → `{ | _}` (D17; the seed's K slot stays a META — probe caught the concrete-Keyword version breaking the annotation escape hatch through B1); mixed-key literals → `expr-map-literal` (D18: `{"a" 1 "b" "x"} : (Map String <Int|String>)`); RVU's empty→Open corner DEAD (bound gives empty-closed rows a fresh meta; 15 empty-reachable callers swapped; elem-meta null guard); assoc-dynamic → `{known… | _}`; dissoc-dynamic + update-in-dynamic → `{| _}` (D20). qtt map-empty delegates the type when the v-slot is a Record (the second probe catch: checkQ-top must see the ROW, not `(Map ?km row)`).
- **The D17 payoff is visible**: `[map-assoc [map-assoc {} :a 1] :b "s"] : {:a Int :b String | _}` with exact projections — pre-flip that was `(Map Keyword Open)` and every projection came back `Open`. Unknown-field projection on dyn rows mints a fresh meta (D19; runtime miss stays a dictionary miss).
- **Gate**: acceptance **78/78** (canaries ;;35/;;37/;;66 flipped deliberately with dated notes; ;;74–79 pin the payoffs); suite **8682/455/0 in 145.1s** — ZERO unplanned churn at the point of no return; **zero caches carry the expr-Open tag** (VERSION bump verified). The `unify:579` wildcard + the 9 Open-scrutinee arms + check/checkQ Open arms are now DEAD CODE.
- Probe-first scorecard this phase: 2 real defects caught pre-commit (the seed K-slot hatch break; the qtt map-empty delegation gap) — both would have been suite failures with far muddier diagnostics.
- **NEXT = p2** (mechanical): delete all ~44 `expr-Open` sites (the node, pipeline identity arms, the wildcard, the 9 scrutinee arms, check/checkQ arms, pnet reg0!), rewrite `test-mixed-map.rkt` (hand-builds the node), record the two-role docstring history (S–I ★ + polarized `&{}`) at the deletion commit → `grep expr-Open` = 0 → p3 gate + close.

## ✅ CHECKPOINT 9 (2026-07-16) — F1a.2 CLOSED: `expr-Open` is DELETED; D1-b done

- **p2 ✅ `e4fad7d7`** — mechanical deletion, net **−77 lines**: the struct + provide + predicate, the `unify:579` wildcard, the 9 Open-scrutinee map-op arms, both check/checkQ absorption arms, the formation arms, 8 pipeline identity arms, both display arms, the pnet registration. The **two-role tombstone** at syntax.rkt records the history (S–I's ★ dyn tail → the C_Cons arms + knowns-only α; polarized `&{}` negative top → the `{| _}` empty dyn row). `record-value-union` is non-empty-by-contract (bound's empty arm mints the Q6 fresh meta). `test-mixed-map`'s hand-built Open test rewritten to the dyn-row-seed contract. `grep expr-Open` = 0 code hits. Suite **8682/455/0**; acceptance **78/78**.
- **p3 ✅ `4892fda3`** — the close bench flagged +9–19% and the diagnosis is the checkpoint's real story: a **pinned worktree at `016251dd` benched the OLD compiler in the same ambient window** — phase timings identical, cache sizes identical, **interleaved ABAB walls = parity (new marginally faster)**. The deltas were ambient drift across sequential 6-minute bench windows on a 15-hour-loaded machine (CVs 3–9% vs ≤1% at col close; suite wall recovered 146→130s as load wound down). NO code regression. *New lessons*: sequential bench invocations ≠ A/B under long-session ambient — interleave or worktree-pin; the perf-worktree is the safe `--ref` when owner WIP occupies the main tree. The union-arm refit found while chasing the false lead stayed on principle: p0 had rollback-wrapped the union right branch (a fork per successful right-branch check); restored bare, whole-union conversion on the both-fail path only.
- **F1a.2 total**: p0 `767bbcae`+`803a57e4` → p1a `bec0c058` → p1b `07c0e70f`+`6a2bc9d5` → p2 `e4fad7d7` → p3 `4892fda3`. The V1/D1 arc that opened this track ("retire `Open`") is now **complete**: literals mint rows (F1a), collections mint rows (F1a-col), the dyn tail carries the wildcard's duties (p1), and the node is gone (p2).
- **NEXT**: F1b (erasure-mode width + label-keyed depth; Map↔schema seal; the Q4/meta-refusal tightening pass; presence marks) — own Stage-3 per the tracker. F-row inherits the §12.5 pins. Path Selection = still the OPEN owner design conversation.

## ✅ CHECKPOINT 10 (2026-07-16) — F1b Stage-3 OPENED (co-design phase; no code)

- **Owner direction at opening**: F1b proper is the scope — getting anonymous records/`Map`↔`schema` right *in principle* is the cornerstone; Path Selection ergonomics + `solve`-solution-set consumption fall out downstream. New owner design note captured as a common-concern input (likely its own design consideration beside F1b): **drop the ground logic-var keys from `solve` solution rows** (today displayed underscore-appended — `'[{:known_ "value" :unknown "solution"}]` → `'[{:unknown "solution"}]`) so Path Selection over solution sets is ergonomic.
- **Opening housekeeping (workflow.md)**: MASTER_ROADMAP.org CIU status + CIU-master status/Track-6 row refreshed for the three closes (F1a-core/F1a-col/F1a.2, `expr-Open` deleted); DEFERRED.md triage — "Type Narrowing for `map-get`" (2026-02-22 Mixed-Type Maps) RESOLVED by the record work → moved to DEFERRED_COMPLETE.md; the Q4 tightening entry marked ACTIVE (F1b scope); heterogeneous key-types entry stands (opens only with Q_E(b) if the owner scopes it).
- **Grounding audit launched** (`grounding-audit` workflow, HEAD `02aad495`): six facets — schema-substrate (seal target), width/depth attachment surfaces, Q4/meta-refusal tightening surfaces, presence-marks readiness, **solve-solutions reification/typing/display** (the underscore-key site is UNGROUNDED in our docs — the audit finds it), rank≤2 HOF-stdlib boundary. Next: R-lens-verify the synthesis → design-options panel → owner co-design → D-numbers. **No F1b code before the co-design.**

## ✅ CHECKPOINT 11 (2026-07-16) — F1b grounding + options panel COMPLETE; review docs landed; F1b.0 probe bundle running

- **Grounding audit ✅** (`wf_0f364e4e`, 7 agents, all HEAD-pinned `02aad495`; main-session R-lens re-verified 11 load-bearing targets). Headline corrections: the solve `:known_` keys are the RELATION'S DECLARED PARAM NAMES (a bound-args echo feature, SIX merge surfaces + `test-bound-args-01.rkt` 38 checks) — NOT ground logic-var keys; solve results carry NO type (`expr-hole`); the annotation path `def r : Person := {:name …}` SILENTLY ACCEPTS missing fields (fill-or-error is preparse-syntactic constructor-form-only — a live soundness gap); `check-subsumption` doesn't exist as a function (the α arms live in check's conversion fallback); THREE width-failure termini (unify conv, C_Cons containment, nested atomic-merge); dormant `expr-schema`/`expr-schema-type` (no producer, pnet-unregistered); presence activation is serializer-free.
- **Design-options panel ✅** (`wf_b47e23d4`, 11 agents: 5 clusters propose+critique + synthesis). Coherent spine: the F1a row-comparison engine stays the sole comparison authority — W extends check-side (directional erasure discharge; unify equality untouched), P instruments the same arms (presence guards, `'unknown`-at-dissoc), S delegates via ONE schema→row projection (bespoke schema arms retire), T polices emitted metas (A-prime sweep mirroring `check-unresolved-trait-constraints`), Q5-A purifies reification (echo deleted). Killed options died consistently (direction smuggled into symmetric machinery; dual-path soundness). Implied ladder F1b.0–F1b.6; first green = Q5-A echo deletion (blast radius verified CLOSED). Panel also found a LIVE bug: explain's reserved `:certainty`/`:cycle` keys silently clobber same-named query vars.
- **Review docs landed** (this commit): `2026-07-16_CIU_T6_F1B_STAGE3_GROUNDING.md` (446 lines — all facet findings/gaps/R-lens verbatim) + `2026-07-16_CIU_T6_F1B_STAGE3_OPTIONS.md` (1037 lines — full options, per-option adversarial verdicts, recommendations, cross-cluster synthesis, the Q1–Q10 rulings queue).
- **F1b.0 probe bundle launched** (`wf_7d2900e9`, 8 probes): W coverage×3 + groundness rider (decides A/E-vs-C); S def-body panic asymmetry trace + champ-arm/annotation censuses + non-literal `[Person m]`/`the Person e` probes; P update-in-removal premise; T commitment-boundary trace; W unify-internal width reality. Owner co-design (Q1–Q10 → D-numbers) resumes when probe results are in.

## ✅ CHECKPOINT 12 (2026-07-16) — F1b.0 probe bundle COMPLETE: rulings collapsed; three NEW defects surfaced

- **Probe results doc landed**: `2026-07-16_CIU_T6_F1B_STAGE3_PROBES.md` (8/8 probes, `wf_7d2900e9`, HEAD `d3c37bd0`). Options doc header carries a dated ✏ collapse note.
- **What collapsed**: **W → check-fallback attachment (A/E)** — the canonical width events are barely expressible at HEAD (record types unwritable; spec-less defn params are HOLES, never call-narrowed; projection on unannotated params dies at defn); Option C's contravariant cornerstone = ZERO traffic (shape-without-benefit, the VAG class); unify-attached width would CORRUPT D15 (homogeneity probes rely on row-vs-row unify failing). **Q6's champ-arm concern dissolves** — the arm is dead code (and blanket-accepts; latent hole). **Width-partial census CLEAN** (0 live missing-required sites; the 10 selection-typed sites are width-partial BY DESIGN → subset-check obligation; aspirational commented sites already expect FILL). **T → escape-boundary** empirically supported (`:1704` transient by LWW re-commit; raw-meta leak CONFIRMED route-sensitive: open-row projection metas store verbatim + dangle silently). **P → dissoc-only activation** (B+ holds: update-in spine labels ⊇-stable on non-empty paths; guards = zero-segment runtime error + dyn tail for insert-on-miss growth).
- **THREE new defects (outside the rulings queue, in "records correct in principle" territory)**: (1) **on-network annotation-adoption soundness gap** — `(the Int {:a 1})` → `{:a 1} : Int`; inline `[use-m {:a 2 :b "x"}]` vs `(Map Keyword Int)` PASSES (ann rule adopts annotation; literal chains stay ⊥, rule-fn #f, contradiction never fires; imperative strict arms never run on top-level-expr routes — driver.rkt:584-588); (2) **panic-swallow-to-`none`** — a failed schema `:check` under projection yields `none : String` with zero error (`definitely-not-map?` exempts expr-error, NOT expr-panic; defs never nf, so def-bound violating constructors define silently) — kills any panic-desugar seal realization; (3) **update-in/get-in `definitely-not-map?` value-loss bug** — chipped as `task_92fc799d` (independent fix). Plus P2's suspicious inline-cons width-tolerance (both directions — needs its own audit) and P5's expr-champ/expr-tchamp pnet-unregistration latency.
- **Next**: owner dialogue over the collapsed rulings → D-numbers in track doc §2a → F1b mini-design section in the F1 design doc.

## ✅ CHECKPOINT 13 (2026-07-16) — F1b co-design round 6 IN PROGRESS: D21 locked (width)

- **D21 ✅ (owner-locked)** — erasure-mode width = **Option E** (expected-tail `'closed→'dyn` relaxation into existing C_Cons, under rollback, bare success path), **equality-depth** on shared fields; ONE shared predicate at check's conversion fallback + qtt mirror. Two pins (meta-residue posture; expected-closure erased — empty-closed-expected accepts anything). **Covariant depth deferred with named triggers** — incl. the owner's `Num`-supertype-schema-field example, which lands at CHECK-strength surfaces (the seal's per-field discharge), NOT the width discharge: the **boundary invariant** (width = labels only; rich per-field satisfaction = check-strength) is D21's load-bearing clause and BANS the seal-as-row-vs-row-width realization. Entails Q2 (pin strict reading stands + D15-corruption argument) + Q3 (seal needs no record-record width). Captured: track doc §2a **round 6** + design doc **§13.1** + tracker F1b row 🔄.
- Walkthrough continues: Q6 (seal shape) next — owner's Num example feeds its per-field-discharge requirement.
- **D22 ✅ (owner-locked, same session)** — the seal: (1) constructor form IS the seal (`[SchemaName e]` generalizes to non-literals; annotation + `the` routes share the discharge — one engine, four doors); (2) per-field CHECK-strength + residual scan ONCE at the seal boundary (fill/missing-required-error/closedness); selections subset-check obligation; (3) annotation-path fill-or-error signed (census-clean, own gated commit); (4) `:check` = **minimal-repair now + F1b.5 retirement pin** (owner resolved the (i)-vs-(iii) split via the decomposition: validate's `Result` IS the principled runtime channel — building another now = belt-and-suspenders; repairs = expr-panic → definitely-not-map? exemption + seal-scoped def-forcing; bridge = named scaffolding); (5) **tombstone** `expr-schema`/`expr-schema-type`/`surf-schema` (type-as-witness stands; wrapper node would fork the value representation); (6) `:default`→`'present`; (7) up-shift = schema→row projection onto the existing αs; (8) champ-arm loud retirement. Captured: track doc §2a round 6 + design doc **§13.2** (route table).
- **D23 ✅ (owner-locked, same session)** — Q4 tightening = **escape-boundary, hard error**: exploration displays metas (D19 honored); escape into a STORED type errors with def-srcloc (stored raw metas dangle + would propagate to .pnet — P7). A-prime mechanism (no second store); groundwork = structured mint sources + deepened shallow walk + ALL store paths guarded (`:1704` exempt — transient); refusal-conservatism STAYS (Q_E(b)-gated w/ probe pin); flip lands AFTER presence activation (pinned re-trigger). Captured: track doc §2a round 6 + design doc **§13.3**.
- **D24 ✅ (owner-locked, same session)** — presence: dissoc-only `'unknown` activation (update-in arm needs NO presence machinery per P6, w/ zero-segment closure + dyn tail); ONE joint grid amendment w/ D21 (acceptance-preserving guard REQUIRED); **gradient = gated-identically** (courtesy-upgrade rejected — the D22-gap unsoundness shape; marks pay via comparison precision + provenance diagnostics + the future has-key? narrowing hook); mechanics = presence-aware union absorption + sort-key tiebreak, `{:a? Int | _}` display, points-map at the struct site; close-gate bench matrix covers D21+D22+D24 together. Captured: track doc §2a round 6 + design doc **§13.4**.
- **D25 ✅ (owner-locked, same session)** — solve-shape: bound-args echo DELETED across all six merge surfaces (solutions carry query-var keys only — the owner's original target); `test-bound-args-01` inverts to a keys-ABSENT gate; explain reserved-key clobber gets the interim guard in the same window; **typed solution rows = own mini-track chartered at F1b close** (entry gates: typeable-goal fragment, ONE shared ground/free predicate, two-context audit, unbound-representation reconciliation); **solve-one → bare solution map + `none`** (never `{}` — D17 ambiguity). This is F1b's FIRST GREEN slice. Captured: track doc §2a round 6 + design doc **§13.5**.

## ✅ CHECKPOINT 14 (2026-07-17) — F1b Stage-3 CLOSED: D21–D26 all locked; the ladder stands

- **D26 ✅** — the F-carrier stub charter (tracker row + consolidated DEFERRED entry: covariant depth, unify-internal width, refusal-relax, annotation-derived bounds, heterogeneous key-types — each with entry gates) + the **route-soundness slice as a PREREQUISITE of the seal** (the ann-adoption gap hollows D22's `the Person e` door on the on-network route; inline-cons tolerance joins as audit+fix).
- **Round 6 COMPLETE — six D-numbers in one co-design day**: D21 width (E/equality-depth, Num-trigger deferral, check-strength boundary invariant) · D22 seal (constructor-form, boundary residual, :check minimal-repair + F1b.5 pin, tombstone) · D23 tightening (escape-boundary hard error; groundwork/flip split) · D24 presence (dissoc-only 'unknown; gated-identically; joint grid amendment) · D25 solve-shape (echo deleted; clobber guard; typed-rows chartered; solve-one bare-map) · D26 (above).
- **Captured**: track doc §2a round 6 (the rulings) · design doc §13.1–§13.5 (mechanics) · **§13.6 the ladder** (F1b.1 first-green → F1b.2 soundness-groundwork → F1b.3 grid amendment → F1b.4 seal → F1b.5 validate → F1b.6 posture flip → close) · **§13.7 the consolidated scaffolding ledger** (the cross-cluster watchout discharged — six mechanisms, each with its PPN/F-row retirement mapping) · DEFERRED.md consolidation (D23 re-trigger pin w/ rejected-with-reason entries; § F-carrier; typed-solution-rows + explain-restructure charter homes) · roadmap + CIU-master rows refreshed.
- **NEXT SESSION: F1b.1 (first green — the echo deletion)**. Everything decision-shaped is done; F1b.1–F1b.2 are fully specified; F1b.3+ carry their probes/canaries in §13.6. Stage-4 discipline applies from here (per-change gate, probe-first, mini-audits per slice).

## ✅ CHECKPOINT 15 (2026-07-17) — F1b.1 LANDED (first green): the echo is gone

- **F1b.1 ✅ `83784ef9`** (docs fast-follow this checkpoint). Suite GREEN **8694/455/0** (121.1s); acceptance **82/82** (;;80–84 = the solution-shape section). Net **−25 lines**.
- **What landed**: `compute-bound-args`/`-for-relation` DELETED (zero non-echo consumers; the global-env param-names registry untouched — live macros/LSP/pnet consumers); `answers->prologos-expr` bound-args param + merge gone (7 call sites simplified); **solve-one → bare solution map** (`{:y "bob"}`) — the mini-audit's load-bearing correction: it was **Option-wrapped** (not a singleton list), so D25.4 = a some-unwrap ×4 arms; `none`-on-miss was already correct; dead legacy explain arms (wf-explained-answer/answer-record — **zero producers**, critic-verified) deleted; the **D25.2 clobber guard** landed as binding-wins-on-collision over THREE reserved keys (certainty/cycle/**provenance** — the audit's widening: provenance is the LIVE one under default semantics; probe showed the clobber live pre-fix, user's binding restored post-fix).
- **Test inversion**: `test-bound-args-01` → the solution-shape negative gate, 11 cases (8 inverted-intent + bare-map/no-some gate + none-on-miss gate + 2 NEW explain cases — display-level explain coverage existed NOWHERE before — incl. the clobber probe). Targeted 14-file regression set 349 PASS.
- **Mini-audit earned its keep** (`wf_b4faf5ef`, 4 agents): the Option-wrap correction; the legacy-arms-dead finding (halved the guard surface); the provenance widening; the **owner-WIP staging-hazard map** (wfle's underscore comment lines sit INSIDE owner hunks — even `git add -p` can't separate; punify is -p-separable) → the 4 example-file comment touch-ups (comment-only, ungated) deferred to the F1b.close doc-truth sweep. NOTE for the owner: two of your uncommitted example additions (wfle `solve (lookup k "localhost")`, punify `[my-add 3N ?b] #= 8N`) will show echo-free output when re-run — that's F1b.1, not a regression.
- **Probe evidence**: baseline probe captured the echo + some-wrap + live clobber; post-edit probe = every flip exact (`'[{:y "bob"} {:y "charlie"}]` / `{:y "bob"}` / `none` / provenance-binding preserved). The stale `:who_` comment mystery resolved: explain always emitted UNsuffixed query-var keys; the wfle comment was simply wrong.
- **NEXT: F1b.2 (soundness groundwork)** — (a) the route-soundness mini-grounding → the ann-adoption fix (P1's `(the Int {:a 1})` class) + the inline-cons width-tolerance audit; (b) D23 groundwork (structured meta sources, deep scrub walk, uniform store-path scrub).

## ✅ CHECKPOINT 16 (2026-07-17) — F1b.2 LANDED (both halves): the adoption gap is closed; the raw-meta leak is closed

- **F1b.2a ✅ `3153319b`** — the route-soundness fix. The mini-audit (`wf_8390b549`, 5 agents) REWROTE the charter before code: the term position does NOT stay ⊥ under an ann (the downward write fills it — D26's pinned fix shape was unrealizable); the gap class = 16 unshadowed rule-fn-#f registrations + the whole unregistered family (literal-extent nodes, all pvec ops — probed class-generic: `(the Int @[1 2])` etc.); the cons tolerance is the SAME class through the APP rule (no ann node — killing any ann-local fix); and the critic found `def y := (the Int (match …))` PERSISTING unsound types (the contains-unsupported-qtt? checkQ skip — probe-confirmed). The fix that survived elimination: **the post-quiescence untyped-interior scan** as a 5th refusal check in `infer-on-network/err` — roster-free (the audit itself reproduced the enumeration-under-count failure mode while counting), scheduler-independent (reads the CALM fixpoint), over-approximation-safe (re-route to the imperative checker). All five probe shapes flipped to errors; sound forms pass; +2% suite wall = noise.
- **F1b.2b ✅ `1801d377`** — D23 groundwork. Structured `dyn-row-source` provenance (NINE tags/TWELVE sites — the audit corrected the four-site framing: record-value-bound is a FUNNEL); **generic reflection-based deep walkers** in zonk.rkt (metas-to-holes transformer + two detectors — zero per-node arms; the S2 lesson taken to its limit); blanket deep scrub at all SIX store sites; the `has-holes?` gate REMOVED (verdict evidence: it EXEMPTED implicit metas from scrubbing = stored them raw = a leak; every stored unsolved meta dangles by construction under per-command reset); `holes-to-metas` deliberately shallow (annotation-feature change, out of scope, documented). The P7 leak flips: `x : _ defined.`, nothing dangles, module capture clean.
- **Gates**: `test-route-soundness-01.rkt` (11 cases, incl. the stored-type-hygiene gate); acceptance ;;85–88 → **86/86**; targeted risk sets 12+14 files PASS; full suites **8706/456/0** (123.5s) and **8704/456/0** (128.7s).
- **Watch items**: (1) the `run-ns-ws-last` forked-network fixture crashes (`net-cell-reset: unknown cell`) when the re-routed imperative path speculates — fixture×speculation interaction; (2) ONE full-suite mass-stall (25 files then 600s watchdog) that did NOT reproduce (individuals + 35-file batch + controlled re-run all green) — **2nd data point** for the transient infra class first seen at F1a-col-3 (`task_5e3ec9c2` era); a 3rd occurrence warrants a dedicated infra session.
- **NEXT: F1b.3 (THE GRID AMENDMENT)** — D21's width-discharge arm + D24's presence activation as ONE co-signed §12.3/§12.4 amendment (the acceptance-preserving guard is REQUIRED; unify pin text amended in the same commit).

## ✅ CHECKPOINT 17 (2026-07-17) — F1b.3 LANDED: the grid amendment (width + presence), one co-signed commit

- **F1b.3 ✅ `352fc28b`** — D21 + D24 in one commit window per the co-sign rule. Suite GREEN **8733/457/0** (124.7s); main acceptance **89/89** (;;79 flipped to `{:a? Int :b? Int | _}` + wdd canaries); NEW clean-state width acceptance **6/6** (own gate). test-dyn-rows **31** (16 envelope + 15 new incl. the case-3 negative that guards the acceptance-preserving relaxation).
- **D21 width live**: `pick wn ww` flips ERROR→`{:a 1} : {:a Int}`; missing-label + type-conflict still reject; the empty-closed-expected differentiator accepts (keyword-scoped closure erasure). Realized exactly as audited: `#:when` static guards (no fork on hopeless events; guard-failing pairs still reach `subtype?`), rollback-wrapped C_Cons delegation, shared predicates capping the qtt mirror drift.
- **D24 presence live**: `wdd : {:p? Int :q? String | _}` — labels+types retained under dynamic dissoc; projections stay gated (fresh metas at all THREE hit sites — the audit's widening); `idm ds` accept→reject = the precision payoff working; union tiebreak repairs the STATED S1 determinism invariant; update-in mints labels+fresh-metas (composing with the F1b.2b scrub → `{:a _ :b _ | _}` stored) behind the new zero-segment runtime guard.
- **Bonus fix**: the qtt dissoc-twin divergence made `def e := [map-dissoc q :a]` fail QTT with a miscategorized Multiplicity violation — probe-caught mid-slice, repaired by delegation (both twins).
- **The day's discovery, documented not fought**: the PRE-EXISTING order-dependent union-state BSP hang is BROADER than DEFERRED knew — after the main acceptance file's accumulated state, polymorphic spec+apps AND plain annotated-lambda defs hang BOTH compilers (worktree-verified at `0bdfca22`; repros preserved). Hence the canary split. DEFERRED entry widened w/ both repros + the possible transient-mass-stall connection. **The 3rd strike calls the dedicated session.**
- **NEXT: F1b.4 (THE SEAL)** — D22: constructor-form generalization, boundary residual, selections subset-check, `:check` bridge repairs + def-forcing, tombstone, up-shift α, champ-arm retirement; annotation-path fill-or-error as its own gated commit. Opens with its mini-audit per protocol.

## 🔄 CHECKPOINT 18 (2026-07-17) — F1b.4 (THE SEAL) opened: mini-audit + 4-pre landed

- **Mini-audit ✅** (`wf_4e9c1b19`, 7 agents at `a91bf0c1`; 11 R-lens targets re-verified). Headline findings: (1) **the map-empty-vs-schema arms are LIVE, not dead** — typing-core's is the recursion BASE CASE of map-assoc-vs-schema (`:2956` → `:2984`), the qtt twin runs on EVERY annotated schema def via the always-on QTT gate → the loud retirement must CO-LAND with the seal's replacement discharge (and it IS the fill-or-error flip — D22's own-gated-commit and the co-land constraint solve each other: 4e); (2) **two-context soundness gap** — the F1b.2a scan was process-file-armed only (`init-attribute-map-cell!` = 1 caller); wrong-typed sealed literals passed SILENTLY in process-string contexts (probe: `(the Person {:name 42 :age "x"})` accepted); (3) the seal has NO AST identity (`the S e` = plain surf-ann → expr-ann; the check-vs-schema-fvar arms ARE the shared choke point); (4) up-shift genuinely absent (no Record-vs-fvar arm; fallback tail dies at subtype?); (5) tombstone list widened (+trait-resolution/union-types arms, +test-relational-types casualties; expr-schema? was never in expr?); (6) census re-confirmed — fill-or-error flips ZERO live gated markers; (7) selection arms: only map-assoc delegates (selection-BLIND, wholesale to parent); map-empty/champ-vs-selection are #t with comments falsely claiming delegation; qtt selection twins asymmetric. Owner approved M1 (universal scan) / M2 (runtime-conditional fill for non-literals) / M3 (top-level-shape def-forcing detection) + the 4-pre→4e ladder.
- **F1b.4-pre ✅ (this commit)** — the scan made UNIVERSAL, correct-by-construction: `infer-on-network/full` returns 6 values (+post-quiescence scan net [net5 holds the attribute map in BOTH modes] + attribute-cell id); the 5th refusal check reads THOSE unconditionally — no arming dependence, no per-context enumeration; `infer-on-network` stays a 4-value wrapper (zero churn at the 9 test call-sites). Probe flip exact: process-string wrong-typed seal ACCEPTED→ERROR; sound seals + plain flows unchanged (positive control). +2 suite-gated pins in `test-route-soundness-01` (13). Targeted 10-file set 253 PASS; **full suite 8735/457/0** (wall 152s — ambient; the close bench matrix measures properly).
- **NEXT: 4a** — up-shift (schema→row projection + Record-vs-schema-fvar arm) + selection subset-aware discharge + champ-arm loud retirement + qtt twins.
- **F1b.4a ✅ (this commit)** — the up-shift + arm hygiene: `schema->row` (one-level projection, all-`'present`) + `record-<:-schema?` (per-field CHECK-strength on knowns; closed? consultation; width-partial toward missing until 4e) + `record-<:-selection?` + FIVE new fallback arms in check + qtt twins (row-vs-schema/selection expectations; schema-actual vs Map [the free up-shift] and vs Record [projection feeding the D21 width machinery]). `the Person m` on a def-bound dyn row SEALS (was: inference error). **Bonus gap closed**: `field-type-satisfies?` — rows vs union-V Map annotations refused pre-4a although every field fit a branch (pre-existing, probe-isolated; single-branch case byte-identical to the old leg). **Champ arms retired LOUD** (both checkers, fail closed, comments name validate). **Design finding (probe-corrected mid-slice)**: my subset-REJECT selection arm broke test-selection-paths — the existing idiom constructs selection-typed values carrying parent fields OUTSIDE the view, because **a selection is a read-side VIEW over fuller data**; D22's "delegate against their SUBSET" scopes the 4e RESIDUAL (a selection seal REQUIRES its subset, not the parent's set), not construction-side field rejection. The corrected arms validate types against the PARENT while keeping SELECTION identity in the recursion (what 4e needs). New `tests/test-schema-seal.rkt` (9 cases incl. the view-semantics pin + wrong-typed-parent-field reject). Targeted 17-file set 370 PASS; **full suite 8744/458/0** (131.3s).
- **NEXT: 4b** — constructor generalization (`[SchemaName e]` non-literal, single-arg; runtime-conditional fill wrap per M2) → 4c (`:check` repairs + def-forcing) → 4d (tombstone) → 4e (fill-or-error + map-empty retirement co-land).
- **F1b.4b ✅ (this commit)** — the constructor form generalizes: `[Person m]` (any single non-literal arg) rewrites to `(the Person m)` and SEALS type-only through the 4a discharge; `[Person 42]` naturally rejects; multi-arg stays non-rewritten; the literal route keeps static defaults + `:check` wrapping byte-identical (probe: `[Config {:host "h"}]` still injects `:port 8080`). **M2 AMENDED (probe-refuted)**: the runtime-conditional fill wrap mis-types — the if-branch unification LOSES the filled field on dyn rows (`{:name|_}` both branches; projection→meta) and mis-types closed rows (type claims the field absent while the runtime value has it; closed-row-miss errors on fields that exist); the conditional-VALUE variant dies on closed rows (map-get on a statically-absent closed field = typing error). Root cause: **fill is TYPE-DIRECTED and preparse has no types** — non-literal fill + pred discharge land with validate's tabulation (F1b.5, the D22.4 decomposition), and the 4e residual gains the missing-defaulted-on-non-literal rule. Also fixed en route: the emitter name is `map-has-key?` (the P6 'unbound' flag explained — the probe used the un-suffixed name). +5 cases in `test-schema-seal.rkt` (14). Targeted 8-file set 190 PASS; full suite GREEN (458 files all pass; the schema-seal count label raced a comment-only mtime edit — re-verified 14/14 individually).
- **NEXT: 4c** — `:check` minimal-repair (expr-panic → `definitely-not-map?` exemption + nil-safe analog) + M3 seal-scoped def-forcing (top-level body shape `expr-ann(_, schema-fvar)` under the optional check-redex; nf + top-node panic → prologos-error BEFORE global-env-add).
- **F1b.4c ✅ (this commit)** — the `:check` minimal-repair, both halves. (1) `expr-panic` joins the `definitely-not-map?` exemptions — ONE edit kills BOTH swallow arms (map-get→`none` AND nil-safe-get→`nil` share the predicate): a violating seal under projection now displays the stuck panic instead of `none : String`. (2) M3 seal-scoped def-forcing at BOTH def commits (`seal-application-body?` shape gate: `expr-ann(_, schema-fvar)` optionally under the check beta-redex; forcing = CHECK-only, stored body stays the unreduced redex; annotated path un-registers like its QTT-failure arm; def-srcloc on the error): `def bad := [Person {:name "dave" :age 0}]` now ERRORS at commit — "panic: … (sealed definition forced at commit)" — was silent `defined.` + swallow-on-projection (the P4 matrix FLIPPED). Laziness pinned untouched outside seals (panic-bearing lambda def still silent; note: a BARE panic body was never definable — infer can't synthesize it, the initial pin construction was wrong). Named non-coverage: nested-in-container seals (the eval arm's own top-node class) = validate-era. +5 cases (`test-schema-seal.rkt` = 19). Targeted 7-file set 123 PASS; **full suite 8754/458/0** (count reconciles the 4b label race: 8744+5+5).
- **NEXT: 4d** — the tombstone (mechanical): expr-schema/expr-schema-type/surf-schema + the audited deletion list incl. trait-resolution/union-types arms + the test-relational-types casualties.
- **F1b.4d ✅ (this commit)** — the tombstone: `expr-schema`/`expr-schema-type`/`surf-schema` DELETED with the full audited arm sweep — 12 files: syntax (provides/defs→history docstring/expr? membership), substitution ×4, zonk ×6 (3 shared-line partials), reduction ×4 (whnf-trivial? member + 2 whnf arms + nf; the shared nf line's `expr-logic-var` arm was collateral-deleted by a substring match and RESTORED — caught by the residual grep + git diff, the mechanical-edit hazard check-parens exists for), pretty-print ×3, typing-core ×3, qtt ×2 (the audited arm body had drifted — repaired via git diff), trait-resolution + union-types (the audit's two extra finds), surface-syntax ×2, + the two `test-relational-types` casualties (deleted w/ tombstone notes). `grep expr-schema|surf-schema` = 0 non-tombstone hits repo-wide; the latent pnet vector-impostor hazard is GONE. Targeted 10-file set 183 PASS; **full suite 8752/458/0** (−2 = the deleted cases, exact).
- **NEXT: 4e** — THE fill-or-error commit: residual scan at the seal boundary (fill / missing-required error w/ srcloc / closedness iff `:closed` / the non-literal + selection-subset rules) + the map-empty-vs-schema retirement in BOTH checkers (co-land — it IS the flip), census-cited (zero live gated flips; new canaries + the punify-p3 aspirational uncomments).
- **F1b.4e ✅ (this commit)** — THE FLIP. (1) `check-seal-chain`: the seal-boundary walk (per-entry checks, ONE residual — no recursive re-entry; the old recursion's map-empty base #t WAS the width-partial hole, now RETIRED with `schema-seal-residual-ok?` as its replacement in the same commit, the co-land constraint satisfied). (2) Preparse default-FILL extended to the ANNOTATION routes with literal braces (`the S {…}` + `def x : S [:=] {…}` — the WS def shape is `($angle-type S)`, instrumentation-discovered): **the March aspirational payoff works** — `def c : Config := {}` → `c.host` → `"localhost"`. (3) Missing-REQUIRED errors with the NAMED field on BOTH error routes (infer/err S7-style hint + a check/err seal-msg — the def route fails through check/err, the known S7 asymmetry). (4) Non-literal rules: dyn rows ABSORB missing-required (gradual, D16); closed rows ERROR; `:closed` schema REFUSES dyn actuals (the closedness scan). (5) **SELECTION SEMANTICS CORRECTED MID-SLICE (empirically decisive)**: my requires-as-required residual broke `sel-path/nameaddr-name-gated` — one of the TEN by-design sites D22 protects (its literal omits a requires-path field entirely). The reading that preserves all ten: **selections have NO completeness residual at construction** — `:requires` is a READ-CAPABILITY declaration; selection values are partial VIEWS; completeness is the PARENT seal's business; the selection residual = closedness-only. (6) Census citation: ZERO live flips in gated surfaces (audit facet 6) — the flip surface was entirely new canaries (+9, `test-schema-seal.rkt` = 28... 27). Targeted 16-file set 376 PASS; **full suite 8761/458/0**.
- **REMAINING in F1b.4**: the clean-state seal acceptance file + gate; §13.2/§13.6 + track-doc ✏ captures (M2 refutation, view-semantics, closedness-only selection residual); then F1b.5 validate → F1b.6 posture flip → F1b.close.

## ✅ CHECKPOINT 19 (2026-07-17) — F1b.4 (THE SEAL) CLOSED: 4-pre→4f, seven commits

- **4f ✅ `1a9cd0de`** — the clean-state seal acceptance file `examples/2026-07-17-ciu-t6-f1b4-seal.prologos` (**29/29 first-run** — every marker matched the probe-verified outputs exactly) + suite gate `test-f1b4-seal-acceptance.rkt` (the width-file clone). Suite **8790/459/0**.
- **Docs captured (this commit)**: track doc §2a gains the ✏ D22 implementation-amendments block (M2 fill refutation → validate; selections = read-side views, TWO test-oracle corrections, closedness-only residual; the fill/residual mechanics as landed); design doc §13.2 close notes + §13.6 F1b.4 row ✅ (per-sub-commit record).
- **F1b.4 total**: 4-pre `eaf115d6` (scan universal) → 4a `0e6901ef` (up-shift) → 4b `dd37efd6` (constructor generalization) → 4c `7269dfb9` (:check repairs) → 4d `f77e646d` (tombstone) → 4e `4082ec70` (THE FLIP) → 4f `1a9cd0de` (acceptance). Suite arc 8733→8790; test-schema-seal.rkt 27 cases; the P1/P4/P5 probe matrices all FLIPPED.
- **NEXT: F1b.5 (VALIDATE)** — own mini-design: the Result-returning runtime tabulation face; witness-strength policy (refuse vs skip non-runtime-checkable fields); the `:check` bridge retirement decision (delegate vs assertion-sugar); inherits the named debt (non-literal fill/checks, nested-in-container seals, partial-view runtime misses). Then F1b.6 (the D23 posture flip) → F1b.close (bench matrix D21+D22+D24 interleaved/worktree-pinned, PIR, typed-rows mini-track charter, doc-truth sweep, DEFERRED triage, roadmap refresh).

## ✅ CHECKPOINT 13 (2026-07-16) — P6 value-loss bug FIXED (commit `06e10d25`)

- **The fix** (owner started the chip; landed main-session): whnf arms for `expr-get-in`/`expr-update-in` (mirror the nf arms at whnf strength; eq-preserving when stuck), `definitely-not-map?` exempts both nodes (a stuck dynamic update-in's result IS a map — degradation to `none` is always wrong; also covers nil-safe-get's analogous arm), and the nf dynamic-update-in arm normalizes its map-assoc spine (map-keys/map-size were stuck on raw spines).
- **Probe-first held**: expected-fail probe characterized all 6 symptoms pre-fix (3× `none` value loss, 2× stuck spine, 1 working control); post-fix 6/6 correct. **Semantics-preservation verified** by re-running the P6 probe fleet: empty-path whole-map-replace, insert-on-miss growth, dissoc-at-path, spine-key survival — all unchanged (those remain D20 co-design questions, deliberately NOT changed here).
- **Tests**: +5 WS regression cases in `test-first-class-paths.rkt` (the dynamic def-bound-path route — literal `#p` paths desugar statically and never mint these nodes). Targeted paths/map/record files green; **full suite 8687/455/0 in 122.4s** (+5 = the new cases).
- **Still open from P6**: the QTT inline-lambda-in-update-in "Multiplicity violation" route sensitivity (flagged, undiagnosed); the empty-path semantics question (D20/F1b co-design). Probes doc § P6 carries the ✏ fixed-note.

## ✅ CHECKPOINT 20 (2026-07-17) — F1b.5 (VALIDATE) mini-design CLOSED: D27–D29 owner-locked; the D22.4 pins discharged

- **The protocol ran full**: grounding audit `wf_1ec0797b` (8 facets @ `6f80956b`; reification gap, three-valued witness, five runtime residues, greenfield surface — all R-lens-verified main-session) → owner-directed SECOND adversarial round: options panel `wf_49cd3f40` (5 clusters propose+critique+synthesis vs DESIGN_PRINCIPLES.org) → main-session probes in parallel → owner took the surviving leans. Captured: track doc §2a **round 7 (D27–D29)** + design doc **§13.8** (partition + p0 probes + watchouts) + §13.6 row 🔄 + §13.7 rows (bridge retirement dated s3; the composed validate row w/ OWN runtime horizon) + DEFERRED walker charter.
- **The panel did real damage (the second challenge earned its keep)**: my A1 surface-code lean KILLED (M2-refuted class + witness unexpressible from surface + un-nameable scaffolding) → the `expr-validate` NODE is the shape (plan-as-data; ONE rule, ONE arm; M2 unrepresentable). My anonymous-row FieldError KILLED on a verified structural conflict (the D13 pin: rows are inferred+displayed NOT parsed — spec-inexpressible) → **E = Result S (Map Path Reason)**, nominal closed Reason, dogfooding at the AGGREGATE. Q_C risk INVERTED: the live class is ERR-polarity (predicate-exact witness fails Nat-in-Int data the seal accepted) → the **polarity table** is the load-bearing invariant, test-locked before the typing rule; my tier-2 deferral reason was factually FALSE (ctor-meta has the metadata; the logic is what's unbuilt) — rewritten honestly. D2 delegate survived alone + strengthened: **doors bake the NODE** (one engine structurally — the two-realizations scaffolding dissolves), let-skeleton emission (gate probably zero-extension, probe pinned). My "(e) covered by (i)+(ii)" scope claim REFUTED (validate is opt-in) → honest wording + ONE walker charter.
- **Main-session probes (ran while the panel deliberated; answered two of its R-targets)**: tier-3 field types are INEXPRESSIBLE-in-practice (`<Int -> Int>`, `<Int | String>` fields register but $angle-type/$pipe sentinels leak MANGLED into the datum — every construction fails) AND tier-2 constructions fail too (`(List Int)` field mints a bare `List` fvar that never unifies with the qualified prelude type; probe: a correctly-typed List Int value REJECTED). Two latent by-catch defects in records-correct-in-principle territory, invisible to F1b.4 (which only exercised primitive+nominal fields). **Ruled IN scope (D27.6, s1)** — they gate validate's own witness on container fields, and pre-repair, validate-on-champ is the ONLY door rich-field schemas have (exactly the demo shape). Also caught en route: list literals inside map literals fail everywhere (`$list-literal` unbound — pre-existing, orthogonal, unfiled).
- **The partition (§13.8)**: p0 pins+probes (9 probes incl. WS-preparse-ordering [gates A2-e attachment], Path-as-Map-key, let-skeleton gate, container-E Result polymorphism, network-free subsumption, .pnet plan staleness, two-context bake lifecycle, P-Real shapes, §3b blame-latch) → s1 Reason + polarity-tabled witness + renderer + the two field-type repairs → **s2 the node = first green** (freestanding `[validate Person champ]` — the DEMO beat; zero door changes) → s3 door flip + bridge DELETION (Validated≠Deployed honored at phase level) → s4 selections + ledger.
- **NEXT: F1b.5-p0** (the probes; then s1). After F1b.5: F1b.6 (D23 posture flip; DEFERRED re-trigger pin) → F1b.close.

## ✅ CHECKPOINT 21 (2026-07-17) — F1b.5-p0 CLOSED: all nine probes answered, both pins resolved

- **p0 ✅ (this commit)** — probes ran as 3 parallel read-only agents (attachment/lifecycle, .pnet staleness, P-Real shapes) + 5 foreground empirical probes (scratch .prologos + racket -e) + 1 direct code-read. Full record: design doc **§13.8 ✏ p0 PROBE OUTCOMES**; pin resolutions: track doc §2a round-7 ✏.
- **The two pins**: (1) **attachment = A2-e CONFIRMED** — preparse is whole-file with Pass-0 schema pre-registration (order-insensitive by construction); the dual-parser cost collapses to the precedented one-liner (datum-parser arm + tree-parser bail-list, the solve/defr pattern); bake at elaboration, cell-first lookups ONLY, loud unknown-schema error. (2) **E key = Keyword (the named fallback)** — Path failed BOTH routes (row key-domain keyword/nat ⇒ Path keys are F-carrier work; `Path` unbound in annotation position); widening story = dotted-keyword path encoding (E type never breaks); real Path keys re-trigger at F-carrier.
- **Green lights**: let-skeleton gate is body-independent (s3's emission fires both catch sites, zero extension); container-E Result generics healthy in the post-freeze shape (annotated err + spec'd fns through map-err; unwrap-or is default-FIRST); `subtype?` runs network-free incl. the tower (Nat<:Int, Int<:Rat) → the polarity table CONSUMES it; element recursion DEFERS (all demo schemas flat all-String; the demo's "accumulating applicative" ask = collect-all Map-E inside plain Result); the §3b blame-latch citation is concrete.
- **By-catches (probe-found, filed)**: NEW consolidated DEFERRED entry "cross-module schema channel" — .pnet validity is own-mtime+driver.zo only (baked defaults/checks ALREADY go stale cross-module today; validate inherits the class, adds none); schemas neither serialized nor re-registered on cache-hit (cross-module schemas are COLD-ONLY; A-hit+B-miss ⇒ silent inject/wrap no-op); `current-schema-registry` absent from 3 save/restore lists (masked by cell-first; the list insertions = s1 hygiene rider). Demo-doc stale gap-row claims → F1b.close doc-truth sweep + a coercion named-deferral.
- **Artifacts**: acceptance skeleton `examples/2026-07-17-ciu-t6-f1b5-validate.prologos` (3/3 self-verified; s1–s4 targets commented) + the M2-boundary paragraph (§13.8).
- **NEXT: F1b.5-s1** — `data Reason` module (4 variants) · the polarity table + three-valued witness (CONSUME `subtype?`; whnf-first; union some-branch; unknown-fvar never-fail) test-locked · the skip-set-empty test · `errors->list` renderer · the TWO field-type repairs (angle-sentinel processing; bare-head qualification) · the 3-list hygiene rider.

## 🔄 CHECKPOINT 22 (2026-07-17) — F1b.5-s1 IN PROGRESS: the field-type class repair LANDED (s1a)

- **s1 opened with its mini-audit** (`grounding-audit wf_194002cf`, 7 facets @ `dac6ca1d`). Headline widenings it forced: (1) the "two field-type repairs" are ONE two-list-drift CLASS defect (builtin-type-names LACKS Float32/64; schema-field-type->expr's case table LACKS Quire/List/Map/Option/…; the angle repair needs BOTH datum normalization AND new union/arrow conversion arms — none existed); (2) the guarded-resolution recipe already exists in-module (`list-type-fvar`) and preserves the pinned bare-`Address` test; (3) the witness cannot consume `subtype?` from reduction-land (the `type-lattice→reduction` cycle is real) → bake-time acceptance-tag assignment is required (module-graph-cheapest; rides the coercion-cache bake precedent); (4) `errors->list` is NOT WS-lexable + no in-language Keyword comparator exists; (5) Reason payload shapes are UNPINNED in the committed docs (the "Datum Datum" sketch never grounded — genuine s1 co-design); (6) the hygiene-rider gap is FIVE params (schema/selection/session/strategy/process), byte-identical shape, and the real surface is the macros.rkt vector pair + test-support, NOT batch-worker (opaque consumer).
- **s1a ✅ `7450c35f`** — the field-type CLASS repair, both directions. macros.rkt: `normalize-field-type-datum` (WS `<A|B>`/`<A->B>` → canonical `$union`/`$arrow` at registration; sigma refuses loud), qualify-guard for `$`/`->` sentinels, Float32/64 into builtin-type-names. typing-core.rkt: `schema-field-type->expr` gains guarded name-resolution (import-map FIRST — the bare-container-head unification fix), `$union`→right-nested expr-union, `$arrow`→expr-Pi 'mw, Quire arms. Gate: p0 tier-2/tier-3 construction probes FLIP (container+union+arrow fields all construct); acceptance skeleton 11/11 (s1 sections uncommented); +19 test cases (new test-schema-field-construction.rkt 7 L3 + schema-types 4 + schema-registry 8); **full suite GREEN 8804/460/0** (129.7s). Deferred within s1: the sub-schema-field qualification asymmetry (mitigated by lookup-schema-by-name short-name fallback; noted for the walker charter).
- **NEXT (s1 remaining)**: the witness substrate (bake-time tags + new low module + polarity table + skip-set-empty test) · the Reason stdlib module + prelude wiring (two-file edit: namespace.rkt + PRELUDE manifest — `gen-prelude --validate` FAILS at HEAD) + the `errors-to-list` renderer · the 5-param hygiene rider. Two decisions surfaced to owner (Reason payloads; renderer realization).

## ✅ CHECKPOINT 23 (2026-07-17) — F1b.5-s1 CLOSED: E + witness substrate + field-type class repair, four commits

- **s1 ✅ (four commits; full suite GREEN 8825/462/0, 145s)** — landed as spec'd + four empirically-forced widenings (design doc §13.8 ✏ s1 CLOSE). Owner rulings folded: Q1 String Reason payloads; Q2 the Keyword shim NOW.
- **s1a `7450c35f` — field-type CLASS repair**: the audit widened "two repairs" → a two-list-drift class (builtin-type-names lacked Float32/64; schema-field-type->expr's case table lacked Quire/List/Map/Option/… + had no union/arrow arm). Normalizer (`<A|B>`→`$union`, `<A->B>`→`$arrow` at registration; sigma refuses loud) + qualify-guard + guarded name-resolution (import-map FIRST — the bare-container-head unification fix). p0 tier-2/tier-3 construction probes FLIP.
- **s1b `45337221` — witness substrate**: the type-lattice→reduction cycle forced the split — `field-type->witness-tag` (typing-core, CONSUMES subtype? → acceptance set IS the subtype closure, the D28 err-polarity invariant) emits plain-data tags; `field-witness.rkt` (NEW, below reduction) interprets them. Polarity table + skip-set discipline test-LOCKED (12 cases).
- **s1c `fb751643` — Reason + renderer + Keyword shim**: `errors->list` renamed `errors-to-list` (original not WS-lexable); in-language (annotated key-lambda pins the generic inference); `keyword-ops.rkt` IR-unwrap shims fill the stdlib's missing Keyword Ord/->string; prelude = two-file edit (gen-prelude-consistent). errors-to-list renders deterministically sorted.
- **s1d `0b6e9d07` — hygiene rider**: the p0 by-catch paid as a class — 5 registry params (schema/selection/session/strategy/process) → the macros snapshot vector (19→24, batch-worker isolation) + test-support.
- **Surprises**: (1) the "two repairs" were a two-list-drift class (Float32/64 missing from builtins; Quire/container arms missing from the converter) — fixed the list pair, not the symptom; (2) schema-field-type->expr's resolution order matters — import-map resolution must come BEFORE stored-name (a bare container head is global-env-bound under its short name, so stored-first returns the bare fvar that never unifies with the qualified prelude type); (3) errors-to-list's generic inference needed an annotated key-lambda (`fn [e : (MapEntry Keyword Reason)] [entry-key e]`) — bare polymorphic entry-key blocks sort-on's A inference; (4) `errors->list` isn't WS-lexable (`<`/`>` reserved) — renamed.
- **Deferred within s1 (noted)**: the sub-schema-field qualification asymmetry (mitigated by lookup-schema-by-name short-name fallback) → walker charter; the deprecated run-ns-* capture-tuple surgery.
- **NEXT: F1b.5-s2 (THE NODE — first green)**: `expr-validate` through the full internal-node pipeline (~8 files incl. pnet regN! same commit); ONE typing rule `Result S E`; ONE reduction arm (presence → fill → witness [consume field-witness] → :check → Map-accumulate); the A2-e parse attachment + `_`-subject; freestanding `[validate Person champ]` — the DEMO beat. Then s3 (door flip + bridge deletion) → s4 (selections + ledger).

## ✅ CHECKPOINT 24 (2026-07-17) — F1b.5-s2 CLOSED: THE NODE — FIRST GREEN, the demo beat works

- **s2 ✅ (`ab468799` engine + `69cafdd6` surface; mini-audit `wf_5fc4c3eb`, 8 agents; full suite GREEN 8875/465/0; acceptance 20/20 first-run)** — `[validate SchemaName e]` end-to-end: `ok … : Result Person (Map Keyword Reason)`; `[validate Cfg {}]` fills BOTH defaults (**the non-literal fill debt DISCHARGED**); `:check` as a RECOVERABLE Result err (not panic); the HOF beat `[map [validate Person _] rows]`; err?/map-err/match consumption (the dual-arity minting contract proven); static non-map reject; loud unknown-schema. **Both retired-loud champ-arm pointer sets land TRUE. ZERO door changes** (seal 29/29 + schema-seal 28 green throughout).
- **The audit earned its keep THREE times**: (1) my check-vs-`(Map Keyword ?meta)` subject discipline was REFUTED pre-code (dyn rows fail record-<:-map?'s meta-V refusal; the map-get INFER-DISPATCH template adopted); (2) the ctor-minting "contradiction" dissolved — the runtime match is deliberately DUAL-ARITY (payload-only minting is the documented foreign.rkt contract; the real trap = PARTIAL type-args → silent stuck, now pinned); (3) the enumeration corrected (ONE infer + ONE inferQ arm only; the definitely-not-map? exemption + three zonk arms + srcloc-last surf convention all named before code).
- **Mid-slice fix (instrumentation-diagnosed)**: unannotated defn params zonk their domain metas to HOLES — `map-ish?` had to accept holes or defn-param subjects passed typing but tripped QTT as a spurious Multiplicity violation.
- **Engine shape**: validate-tabulate in reduction (collect-all; missing → type-mismatch → check-failed precedence; values nf'd AT INSERT + BEFORE witnessing; pred panic PROPAGATES, stuck pred SKIPS per err-polarity); the bake in the elaborator (two new require edges raco-proven; subst-underscore/normalize-check-pred re-homed = D29's second consumer LANDED, >-reversal golden-pinned); payload-only FQN minting; pnet regN! same-commit.
- **Tests**: test-validate-node.rkt 17 (tabulation semantics + walker contract + polarity goldens) + test-validate.rkt 13 L3 routes (def-bound QTT, nested-in-defn bail-list, HOF, closed/open, map-err∘errors-to-list) + the acceptance gate clone (20 markers). Suite arc 8825→8875.
- **By-catches (close sweep)**: `<`-preds unusable in WS files (`<` opens an angle group — pre-existing reader class; workaround `(> N _)`); match with an INLINE validate scrutinee fails inference (def-bound works); test-validate.rkt 47s > guideline; stale §13.8 champ-arm coordinates corrected (3120-3128/3137-3141).
- **NEXT: F1b.5-s3 (DOOR FLIP + BRIDGE DELETION)** — the stdlib abort fn; doors bake node∘abort in the let-skeleton; DELETE wrap-schema-checks; gate probe both catch sites; P4/;;12–;;15 matrix re-run; strengthened-door census. Then s4 (selections + ledger) → F1b.6 → F1b.close.

## ✅ CHECKPOINT 25 (2026-07-18) — F1b.5-s3 CLOSED: THE DOOR FLIP + bridge deletion, D29 deployed

- **s3 ✅ (s3a `d9986bb1` + s3b `11660e92`; mini-audit `wf_e30376b8`, 6 facets + critic @ `033454bb`; full suite GREEN 8876/465/0)** — the `:check` panic bridge is DELETED; the constructor door delegates :check to validate. Validated≠Deployed honored (no dual mechanism at close). Design doc §13.8 ✏ s3 CLOSE + §13.7 bridge row lands TRUE.
- **s3a `d9986bb1` — the abort face** (additive, ZERO door change): `expect-valid : {A} String [Result A (Map Keyword Reason)] -> A` in reason.prologos (ok unwraps; err PANICS with `render-failures` — the panic is the match ARM BODY so it surfaces top-node at both catch sites) + `render-failures`/`render-reason`/`render-entry` (single-failure BYTE PARITY with the retired wrap-schema-checks message, probe-verified identical; multi = field-sorted, `; `-joined). Prelude exposure (namespace.rkt + PRELUDE, kept in sync). test-reason +1 case (consolidated — L3 file, ~4s/prelude-load hits the ~30s watchdog). seal 29/29 + validate 20/20 intact.
- **s3b `11660e92` — THE FLIP**: `wrap-seal-validate` replaces `wrap-schema-checks` (SAME :check-presence gate → no-:check doors byte-identical). BOTH ctor doors on a :check schema: `(let __schema-check-tmp (the S {aug|arg}) (…::expect-valid "S" (validate S __schema-check-tmp)))`. LITERAL door = bridge→composition (byte-parity); NON-LITERAL door = the D29 STRENGTHENING ([Checked m] runs :check at commit; census: ZERO live :check non-literal ctors, no flip). The let skeleton keeps the arg a schema-ann → the beta-redex gate fires body-independently (the one inferred link — rewrite-specializations preserves shape — now REAL-emission-verified at both catch sites). Q_B keep-the-seal: the inner `(the S …)` still statically type-checks.
- **The empirically-forced correction (elaborator.rkt resolved-sname → ns-QUALIFIED, try-qualified-first)**: the flip exposed a LATENT s2 gap — short vs qualified schema fvars are DISTINCT types, so validate's short S failed to unify with a `def x : Schema :=` annotation (`Expected test::Strict / Got Strict`, test-schema-properties §6) AND rendered unqualified (the ;;13 display regression). Qualifying S fixes BOTH. The alternative outer-`(the S …)` fix was REFUTED (`(the Schema <app>)` is seal-special → "could not infer type"). This changes validate's DIRECT display to qualified too — s2 acceptance markers are `=>~` contains on value fragments (unaffected); test-validate.rkt's two `Result Person` regexps → `Result vt[0-9]+::Person`.
- **Census (D22.3)**: the two `:no-prelude` :check surfaces (f1b4 seal accept; test-schema-seal CHECKED preamble) gain result+reason requires — a :check ctor door is now prelude-dependent (the bake errors loud; the f1b5 file already did this). Acceptances: seal 29/29 (;;13 qualified, ;;12/14/15 preserved); f1b5 24/24 (+4 s3 markers: both doors + strengthening).
- **Probe-first held**: the door-behavior matrix (all four doors × violating/valid) captured pre- AND post-flip — caught the strengthening (non-literal silent→error), the display regression (short S), and the unification bug BEFORE the cluster run.
- **NEXT: F1b.5-s4** (selection-validate — the elaborator arm currently emits an honest "lands at F1b.5-s4" error; + the composed §13.7 ledger row + the walker charter finalized in DEFERRED). Then F1b.6 (D23 posture flip) → F1b.close.

## ✅ CHECKPOINT 26 (2026-07-18) — F1b.5-s4 CLOSED: selection-validate; F1b.5 implementation COMPLETE

- **s4 ✅ (`0f95d544`; mini-audit `wf_bd911236`, 5 facets + critic @ `5194ad92`; full suite GREEN 8885/465/0)** — `[validate SelectionName e]` works (was an honest stub). Design doc §13.8 ✏ s4 CLOSE; §13.7 validate row stamped ✅; DEFERRED walker charter + residue-(e) updated.
- **Semantics** (D22.4 amendment 2: selection = read-side VIEW; :requires = read-capability, not completeness): enforce single-segment :requires PRESENT (miss = read-capability violation → missing-required); validate every PRESENT parent field's type + :check (B1 — a view carrying a violating :age is flagged); accept extra parent fields; NO parent-completeness; closedness iff parent :closed; Result's S = the qualified SELECTION name.
- **Encoding = WIDEN THE PLAN ENTRY** (the audit's third option, beating the binary NEW-struct-field-vs-reuse framing): a 7th per-field slot `required-on-miss?`. A 6th STRUCT field breaks four positional match/ctor sites + pnet-serialize; a plan slot breaks NONE (all bind `plan` opaquely). Schema plans set it #t → schema behavior BYTE-IDENTICAL.
- **The bake unifies** schema + selection: selection borrows PARENT fields (lookup-selection-parent-schema); plan spans the FULL parent field set (plan-kws covers the parent for closedness + present fields validate); required? = #t (schema) / (kw ∈ single-segment requires) (selection). **The arm** splits the missing branch + a NEW SKIP arm (absent+no-default+not-required — naively gating the old branch fell to `(nf #f)` = garbage).
- **Empirically nothing forced a correction this slice** (the s3 pattern of a latent-bug-exposed did NOT recur) — the audit's pre-code grounding (esp. the plan-widening third option + the SKIP-arm hazard + all-stale-coordinates correction) meant the implementation landed as designed. Probe-first still earned its keep: the baseline confirmed the three view semantics + the B1 present-field-validation before the cluster run.
- **Scope**: single-segment requires; deep/wildcard (`:address.zip`/`:address.*`) defer to the walker charter (DEFERRED item 4, the SAME descent mechanism). pnet: the 7th boolean slot rides the existing registration (closed? is already a serialized boolean).
- **Tests**: test-validate-node +1 (selection skip semantics); f1b5 acceptance 24→28 (+4 s4 markers: registered / ok / extra-field-accept / missing-requires-err). Full suite 8876→8885.
- **F1b.5 (VALIDATE) IMPLEMENTATION COMPLETE** — p0→s4 all landed. The Result-returning runtime tabulation face: schemas + selections + champs; non-literal fill+:check discharged; the :check bridge deleted; collect-all Map-E; qualified-S. **NEXT: F1b.6** (the D23 escape-boundary posture flip; the DEFERRED re-trigger pin — strictly after F1b.3 presence, satisfied) → **F1b.close** (bench matrix D21+D22+D24, PIR, typed-solution-rows charter, doc-truth sweep, DEFERRED triage, roadmap/CIU-master refresh).

## ✅ CHECKPOINT 27 (2026-07-18) — F1b.6 CLOSED: D23 escape-boundary posture flip DEPLOYED

- **F1b.6 ✅ (`7bcbca69`; mini-audit `wf_5eb9af3e`, 4 facets + critic @ `43be29c4`; full suite GREEN 8887/465/0)** — the Q4 tightening's deployment half. A dyn-row POINT-PROJECTION meta escaping into a STORED type is now a HARD ERROR (def-srcloc); everything else keeps the F1b.2b scrub-to-hole. Design doc §13.6 F1b.6 ✏ CLOSE; DEFERRED re-trigger pin RESOLVED.
- **The audit resolved the spec's under-specification + corrected 3 stale coordinates**: (1) THE PARTITION (the #1 correctness surface) — the error set is the NARROW 2-kind {dyn-row-projection, dyn-row-dynamic-projection}, NOT all 10 dyn-row-* kinds; the decisive counterexample was f1b3-width's update-in marker (must keep scrubbing). Bulk-op result kinds (update-in/fold/values/…) are legitimately-dyn results. (2) SCOPE = type-LOCAL walk (not the global sweep the design's "mirror" named) — matches D23's "a type CONTAINING a projection meta," avoids over-fire on `def x := [const 5 m.c]`. (3) BOUNDARIES = the TWO def-commit boundaries only (the design's ":1704 deferral exempt" was stale — :1704 is the data-type store; the transient exempt is :1744; defr/foreign/data are no-ops under type-local).
- **Implementation**: `collect-expr-metas-deep` (zonk.rkt, a generic struct-reflection meta-collector, sibling of expr-contains-meta-deep?) + `check-escaping-projection-metas` (driver.rkt — walk the PRE-scrub stored type; error iff it holds a projection-kind meta) inserted in the def-inferred + annotated error blocks (before the scrub, def-srcloc, per-path un-register). GATING is critical: NEVER at eval/infer → exploration stays permissive.
- **Escape hatch verified**: an explicit annotation solves the meta (drops from the unsolved set → def commits); a `: _` HOLE annotation does NOT discharge (stays unsolved → still errors). Both probe-confirmed.
- **Probe-first held**: the baseline (exploration display / store-escape scrub-to-hole / escape-hatch annotation) captured all three behaviors BEFORE code; the `: _` hole gap was probe-found (led to wiring the annotated boundary, not just def-inferred).
- **Flips (updated in-slice)**: test-route-soundness-01 (the def now errors; +2 cases exploration-permissive + escape-hatch) + f1-records marker 88 (`dx : _ defined.` → the D23 error). **Must-stay-green confirmed**: update-in (f1b3-width), exploration display (f1-records 91), closed-row dynamic-key (qtt-union-meta), structural dyn-tail (f1b5-validate).
- **NEXT: F1b.close** — the LAST slice of F1b. Bench matrix (D21+D22+D24 fast paths, interleaved/worktree-pinned per the §12.7 lesson) · PIR (16 questions, checklist-first) · typed-solution-rows mini-track charter (D25.3 gates) · doc-truth sweep (the wfle `:dept_` stale comment; the punify-p3 `;;264/;;267` aspirational uncomments — they WORK now; the `<`-pred WS reader class) · DEFERRED triage · roadmap/CIU-master refresh.

## ✅ CHECKPOINT 28 (2026-07-18) — Records/maps hand-testing: 5 WS-ergonomics fixes (dogfooding foray.prologos)

Owner-driven hand-testing of the F1b records work against `lib/examples/foray.prologos` (Map sketches + schema-returning functions) — surfacing WS-mode ergonomics/wiring gaps before F1b.close. Five commits, each grounding-audited or probe-first, gated (full suite GREEN throughout), committed. SOP reaffirmed: dailies capture the narrative WHY (design choices, surprises, lessons) behind each commit's WHAT.

- **`ff31d237` — implicit-map `def` bodies parse via `process-string-ws` (the REPL/LSP path).** The owner's layout maps (`def user` / `  :name "a"` / `  :age 1`) parsed via `process-file` but FAILED interactively ("def: expected ':', got :name"). **A Level-2 vs Level-3 divergence**: `process-file` uses the merge pipeline (preparses the raw grouped syntax); the cell pipeline (`extract-surfs-from-form-cells`) routed `def` through `parse-form-tree` (no preparse) AND flattened the datum before `rewrite-implicit-map`, destroying the keyword grouping the rewrite keys on. Fix: detect implicit-map-shaped `def` on the RAW datum, route through the datum path, rewrite BEFORE flatten. **Surprise**: scoped-to-`def` was load-bearing — a broader scope false-positived on `spec … :properties` (metadata, not a map value), caught by a per-file test-count diff (test-properties 13→8). **Lesson**: instrument the actual datum (the `(def user (:name "a"))` grouping) beat guessing; the per-file test-count diff is a precise regression-hunting tool.
- **`d9ec4911` — inline dash `- :key val`.** YAML-style dash + first key on the same line. The reader leaves same-line keys FLAT while grouping continuations (`(- :name "a" (:role :super))`); `process-dash-child` re-groups the flat prefix. Strictly additive (only rescues clauses that failed to type). Owner-approved after the YAML-familiarity framing.
- **`5655a02c` — s1: dot-access / head-macros inside map-literal VALUES.** THE records blocker: `{:x [+ p1.x p2.x]}` (functions returning records) crashed/leaked. Root: `$brace-params` OPAQUE during preparse (Track 8 B3, protecting the `->` KIND arrow in binder brace-params). **Grounding audit `wf_0a2792b4`** (6 agents) + probes settled the discriminator: **map literals lead with a `:keyword`; binders lead with a bare symbol** — collision-free shape gate. Approach A: gate BOTH opacity sites (the audit found TWO, not one — the def-BODY case rides the 2nd), recurse into keyword-headed values via `preparse-map-literal-contents` (fold access sentinels across the flat value stream, then expand each element). **Surprise**: map values are a FLAT multi-sibling stream (`:a p ($dot-access x)`), so "recurse per value" was the wrong shape — the fold-then-expand handles it. R-lens confirmed spec metadata is consumed directly (never reaches the gate). String/number/computed keys = accepted v1 limitation (heterogeneous keys = own future feature).
- **`b44e73cf` — s2: fold dot-access inside mixfix `.(...)` before pratt-parse.** Orthogonal ordering bug (the audit split it out): `.(p.x + 1)` crashes at TOP LEVEL (no brace) — the mixfix expander pratt-parses before the subform dot-access rewrite, so `($dot-access x)` reaches it as a non-operator. Fix: `expand-mixfix-form` folds access sentinels first; caller re-expands residue. Unblocks the owner's `point-add` (mixfix `.(p1.x + p2.x)` body) end-to-end.
- **`9d54fe0b` — s3: odd map literal → graceful error, not a `cadr` crash.** Pre-existing robustness bug the audit surfaced: `parse-map-literal`'s odd-length guard was a value-discarding `when` (built the error, threw it away, fell through to the crashing loop). `{:a 1 :b}` now gets the graceful "even number of elements" message.

**End-to-end payoff**: both `point-add` (mixfix body) and `point-add2` (bracket body) — functions returning records over a schema — now work. **Standing non-blockers found (separate from maps/records, flagged for triage):** (1) untyped-param projection `defn f [p] p.x` → "Type mismatch" (needs p's type; the owner's `point-add` hit this via the `spec paint-add`/`defn point-add` NAME typo → bare untyped params); (2) generic `+` on an untyped param `defn f [x] [+ x 1]` → "Type mismatch" (pre-existing, standalone); (3) **dotted `ns examples.foray` is silently mis-parsed** — the `.` reads as dot-access, `.foray` is DROPPED, so the ns is just `examples` (→ `examples::Point`, and `examples.other` COLLIDES). Hierarchical `ns examples::foray` (→ `examples::foray::Point`) and flat `ns foray` work; no-ns → bare `Point`. **Also standing**: pre-existing `test-properties.rkt` batch flake (8↔13, property-test nondeterminism, ~8.5% of runs, unrelated) — DEFERRED note for the close sweep.

## ✅ CHECKPOINT 29 (2026-07-18) — Records hand-testing follow-ups: ns `.`-parse-error + sweep, inference error-messaging, DEFERRED filing

Owner-directed follow-ups to the three standing non-blockers from checkpoint 28.

- **`b0db8f3e` — dotted-`ns` parse error + legacy sweep.** `ns examples.foray` silently dropped `.foray` (the `.` is dot-access tokenization → `(ns examples ($dot-access foray))` → ns became `examples`, colliding across `examples.*` files); a prior-era `/`-then-`.` FQN convention that outlived dot-access-on-record-fields. `process-ns-declaration` now REJECTS a `.`-containing ns name with a targeted message (use `::` or a single segment). **WS-mode only** — sexp `(ns test.x)` keeps `.` as a module-path char (verified: unaffected; the reduction-perf/stdlib sexp tests didn't flake). **Census**: 34 dotted-ns `.prologos` files main-repo (114 incl. stale worktree copies); NONE suite-gated; 1 test (`test-higher-rank`, 5 inline `ns test.hrN`) WAS gated → migrated to `::` (behavior-preserving). Owner chose the SWEEP: 32 files `ns examples.X` → `examples::X` (multi-dot → `A::B::C`); 2 owner-WIP dotted files LEFT UNTOUCHED. **Lesson**: WS-vs-sexp reader divergence on `.` — the census must split by reader mode; the "foray" safety-grep false-positived on `foray-min` (a clean tracked file, correctly swept).
- **`ff813935` — clearer error for unannotated-param inference.** `defn f [p] p.x` / `defn f [x] [+ x 1]` gave a bare "Type mismatch"; now: "cannot infer the type of an unannotated parameter … annotate `[x : T]` or add a `spec`." SURGICAL at the check/err builder (typing-errors.rkt) — fires only when the checked term is a lambda with a hole domain AND inference gave up (actual = could-not-infer), so the ~6 "Type mismatch"-matching tests + all genuine mismatches are untouched (control test pins it).
- **DEFERRED filing** (this commit): the two inference soft spots as ONE class (inference on an unconstrained param), each with its named deep-fix home — projection → **F-row** (row inference from `p.x`), arithmetic → **Num Track 2** (generic `Num`). The error-message mitigation makes the annotate/spec workaround discoverable; the deep fixes are NOT records-slice work.

**Design note surfaced (not yet a task)**: post-sweep FQNs are longer (`examples::foray::Point`) — the display-verbosity of type FQNs (show bare name when unambiguous vs always-qualified) is a separate presentation-design conversation the owner may open later.

## ✅ CHECKPOINT 30 (2026-07-18) — Schema/validate STRESS-TEST → F1b.7 hardening phase opened

Owner asked for self-driven stress-testing of the records/schema/validate/selection surfaces before F1b.close. Ran an adversarial battery (`wf_9e4a15ad`, 6 cluster testers + synthesis @ `e60ba8b4`, each tester given the known-deferred map so it only surfaces genuine surprises). EVERY confirmed finding main-session reproduced before acting. Also filed owner-directed deferrals first (`e60ba8b4`): schema EXTENSION (un-named future design, owner brewing) + selection PROJECTIONS (Path-Selection-adjacent) + the nested-`validate`-descent demo-relevance note on the walker charter.

- **🔴 HIGH bug found — `:check` silently passes any non-Bool predicate.** A trait-method check (`eq?`/`le?`/`lt?`/`ge?` on Int) or a `[fn …]` check → `validate` returns `ok` on invalid data, AND the seal ctor door commits the violating value. Reproduced: `schema EqInt :n Int :check (eq? _ 7)` → `[validate EqInt {:n 8}]` = `ok {:n 8}`; `def bad := [EqInt {:n 8}]` commits. **Root cause**: check-pred baked `elaborate`-ONLY (elaborator.rkt:3142, no trait-dict resolution) → trait method stuck; tabulate arm (reduction.rkt:1354) treats "not false, not panic" as PASS — err-polarity ("skip stuck") MIS-APPLIED to `:check` eval (it governs type-witnessing, not the user's runtime assertion). Masked because every acceptance/test marker uses `(> _ 0)`-style (which normalizes to dict-free `lt`/`le`).
- **🟠 MED — non-literal ctor door drops `:default`**: `[Person m]` (def-bound) → `p.age` = `<error> : Int` where the literal door + validate fill it.
- **Rough edges (owner ruled IN SCOPE — records-usability-critical)**: bare map-ops on a nominal schema value fail (Record<:Map doesn't fire at op application; reads work, `map-keys`/`-assoc`/… don't); generic "Could not infer type" swallows specific diagnostics; `?`-suffixed keyword keys (`:active?`) mis-parse; the annotation door + `the`-form skip `:check` (parity).
- **Reassuring**: zero crashes/hangs (~120 probes); all 16 primitive witnesses sound; collect-all + `errors-to-list` + D23 matrix + up-shift all solid. Expected-limitations re-confirmed (one-level nested, union un-witnessable-branch over-accept, arrow/refined skip).
- **Captured as F1b.7 (Records/Schema Hardening)** — design doc **§13.9** (7 subphases 7a–7g + fix shapes + the 7a design decisions) + §13.6 ladder row 🔄; F1b.close GATED on 7a. Owner order: 7a (Layer B soundness guard) → 7b (Layer A trait resolution) → the usability edges 7c–7g.
- **NEXT: F1b.7a** — the Layer B guard, opening with its mini-audit + owner dialogue on (i) def-time vs runtime guard, (ii) new Reason variant vs reuse check-failed, (iii) whether def-time pulls 7b forward.

## ✅ CHECKPOINT 31 (2026-07-18) — F1b.7a LANDED: the :check soundness guard (Layer B)

- **F1b.7a ✅ `f5aaae0b`** (full suite GREEN 8914/465/0) — the stress-test HIGH bug closed. A `:check` predicate that doesn't reduce to a Bool (stuck trait method / a `[fn …]` value) no longer silently passes: validate's tabulate check-eval is now a clean four-way with a fail-closed `else` → the NEW `check-unevaluable : String` Reason. `[validate EqInt {:n 8}]` (`:check (eq? _ 7)`) → `err check-unevaluable "(eq? _ 7)"`; `def bad := [EqInt {:n 8}]` errors at commit. Design doc §13.9 ✏ 7a CLOSE + §13.6 ladder row.
- **Protocol held**: root-caused precisely (elaborator.rkt:3142 elaborate-only + reduction.rkt:1354 "not-false→pass") pre-code; the census (zero trait-method `:check` in the suite, all `>`-style) confirmed zero regressions BEFORE landing; probe-first gate proved the flip (eq?/le?/lambda → check-unevaluable; `(> _ 0)` + valid values unchanged; the door rejects with a clean message) before the suite.
- **Encoding win** (the names-as-data lesson): `check-unevaluable` rode as an index-8 names-list append — NOT a struct field → zero pnet/spine-walker touch; the one co-change was test-validate-node's hand-built names list (the New-Struct-Field-adjacent hazard, caught by the targeted run's `list-ref index 8` error, fixed test-side).
- **Owner decisions (Q1/Q2/Q3) resolved as recommended**: runtime guard now (def-time comes with 7b); a new honest Reason variant (not overload check-failed); fail-closed.
- **NEXT: F1b.7b** — Layer A: resolve trait constraints on the baked pred (elaborator.rkt:3142) so `eq?`/`le?`/`lt?`/`ge?` checks WORK (a real violation → check-failed); the 7a runtime guard stays the permanent backstop. Then 7c–7g (the usability edges).

## ✅ CHECKPOINT 32 (2026-07-18) — F1b.7b LANDED: :check comparison family + /= + :default diagnostic

- **F1b.7b ✅ `9555d324`** (grounding audit `wf_7cdee201`; full suite GREEN 8912/465/0) — the audit REFUTED my premise (the mini-audit earned its keep again): only `eq?` (+`div`/user predicates) is a genuine trait method; `le?`/`lt?`/`ge?`/`gt?` are monomorphic `Nat Nat -> Bool` (Ord's method is `compare→Ordering`, there is no generic Ord `le?`) — on Int they're a type-mismatch that sticks at reduction, NOT a dict problem. Empirically: `[le? 8 5]`→type error, `[eq? 8 7]`→false, `[le? 8N 5N]`→false.
- **Owner-ruled option (b)**: ship the cheap half now, defer genuine trait resolution. Landed: (i) normalize `le?`/`lt?`/`ge?`/`gt?` → dict-free `le`/`lt`/`ge`/`gt` (works Int+Nat; `(le? _ 100)` on Int now enforces; census empty → zero regressions); (ii) `/=` → `(not (eq ..))` (neq was unbound); (iii) the `default-unevaluable` Reason (index-9 names append; the exceptional-diagnostics ask — a stuck trait-method default reads clearly, was misleading `type-mismatch "Bool" "value"`).
- **Q_B opened + resolved by principle** (owner asked to challenge against design principles): the `:default` gap is NOT a silent-unsoundness hole (unlike 7a) — the def-route resolves it, validate catches it, unbound errors; so Correct-by-Construction is already satisfied and only the RESOLUTION defers (Decomplection: same mechanism as lever ii). The MESSAGE improvement landed now (exceptional diagnostics is a core goal).
- **Deferred + documented**: lever (ii) genuine trait-method predicate resolution + `:default` trait resolution → the traits-as-refinement note **§8** (owner's doc request — a grounded consumer of "trait resolution as a reusable service"; the candidate-A fix is grounded, the workaround is a spec'd-Bool-fn wrapper, 7a's guard is the loud backstop).
- **NEXT: 7c–7g** — the usability edges (door parity, non-literal default fill, Record<:Map map-ops, targeted diagnostics, `?`-keyword parse). Sequencing at each opener.
