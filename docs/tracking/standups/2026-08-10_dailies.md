# Dailies — 2026-08-10

Rolled from `2026-08-07_dailies.md` (**1137 lines**, 2.3× the ~400–500 cap) at
the **P4e-0 attempt-3** boundary — a genuine restart after two failed attempts,
and the roll the P4e prep slice was approved to do. Prior file is read-only
history; its LOG carries the whole P4d arc (slices 0–7, the close, Q_U19/Q_U21),
the P4e design round (**Q_U23–Q_U31**), **P4e-0 attempts 1 and 2**, the
**star-surface census**, and the abort-seam fix. STATE head below is SEEDED
(copy-forward per `DAILIES_METHODOLOGY.org`) and refreshed to current.

---

## 📍 STATE  (overwrite in place — always current)

- **HEAD**: re-derive with `git rev-parse HEAD` — never trust a hash in prose.
  At this refresh: `9cac0099` (**P4e-1a COMPLETE: all three slices SHIPPED** —
  the matrix, the Tier-O arms, and the CONSUMER; Q_U36 + Q_U37 ruled; DEFERRED
  106–112 filed), local
  `main`, MAIN CHECKOUT · suite
  **10143 / 488 / 0** (`--all --force-rerun`, `[488/488]`, 0 `FAILED:`) ·
  battery **476** (measured) · acceptance **84** markers · **26 unpushed — do
  NOT push unless directed** (MEASURE) · working tree = owner WIP only, stage
  explicit paths, never stash.
  ⚠⚠ **THE SUITE IS INTERMITTENT AND IT IS PRE-EXISTING — check
  `data/benchmarks/timings.jsonl` BEFORE any base A/B.** It records **469** runs
  with per-file counts and `all_pass`. `test-properties.rkt` is RANDOMIZED
  (`gen:integer-in`): **13 cases in 411 runs, 8 in 55**, so a ±5 total swing is
  NORMAL. And **74 of 469** runs recorded `all_pass=False` (~16%). A 5-run base
  sample missed BOTH and nearly cost slice A an attribution it did not deserve;
  one query answered in seconds what five worktree suite runs could not.
  ⚠ Do NOT compile while a suite is in flight — one run was contaminated that way
  and its failure sent the diagnosis down the wrong path first.
  ⭐ **NUMBERING is RULED** [owner 2026-08-08]: monotonic, permanent, allocated
  from the **NEXT FREE register** at the top of `DEFERRED.md` — never
  `max(heading)+1`. **Next free 113 · next free Q-label U38.**
  ⚠ **COUNT `ERROR` AS WELL AS `FAILURE`.** A raise inside a rackunit
  `test-case` reports as **ERROR**, and a `grep -c '^FAILURE'` reads it as green.
  Applied this session: the battery was checked for both, both 0.
- **Where we are**: **CIU T6 — P4e-1a is COMPLETE.** Slice A (`e7a49228`) minted
  the `postfix-star` TOKEN TYPE; slice B was reverted (DEFERRED 105); then
  1a-i (`ff9b7d81`, the arrival matrix + the rebuilt gate), 1a-ii (`dc458109`,
  the Tier-O arms), and **1a-iii (`9cac0099`, THE CONSUMER — attempt 2 per
  Q_U37; attempt 1 reverted at `bd2c3575`)**. The parked tests are LIVE:
  `xs:{a}*` / `c{a}*` reach the guided not-yet message, `[f x]*` takes Q_U35's
  refusal, glued Sigma takes the type-seat message, quote/quasiquote capture the
  star as `*`, and the fuse retired two whole-file aborts (DEFERRED 108:
  21 → 19).
  ⭐⭐ **THE ARC'S DEFINING FACT: THREE adversarial verify rounds on slice A, and
  EVERY ONE found a BLOCKING defect in my own code** — rounds 2 and 3 each found
  a defect in the FIX for the round before. Slice B's verify then found a fourth
  class. In every case the battery, all five acceptance files, the neighbourhood
  tests AND the corpus A/B were green over the defect. **Budget for the second
  and third verify, not just the first.**
- **NEXT**: **P4e-1a is COMPLETE — close the sub-phase** (D4 §5.P4e-1a records
  all three slices + both attempt records). Then P4e-1b onward per the
  sequencing: the remaining `*` semantics (the actual flatten), `.*` ravel
  (Q_U26), `*_` provenance (Q_U24) — each opening with its own mini-audit. The standing rulings:
  **Q_U36** (positive-list fuse over the PREDECESSOR — discharges census Tier-O
  O7) and **Q_U37** (the refusal is decided IN TERRITORY: fold fuses in
  selection territory, `parse-datum` owns expressions, `unwrap-angle-type` owns
  types, the quote/quasiquote lowerings own data). The matrix artifact is
  `tools/star-arrival-matrix.rkt` (11 minting carriers × 19 contexts + 5
  controls); the preparse observable is a three-way PARTITION pinned
  cell-by-cell, plus pinned snapshot sets for aborts (19), message leaks
  ({let-nested, spec-type}), and preparse raises (3).
- **Open / blockers**:
  · **DEFERRED 101** — the unspellable non-identifier carriers + the recorded
    analysis of why both attempts failed. The design gap itself.
  · **DEFERRED 103** — ✅ **RULED by Q_U32** as a language ruling. ⚠ Its
    SIBLING is NOT discharged: `compile-match-tree` aborts on a multi-form
    LITERAL arm (`| 2 3 ->` → `list-ref: contract violation`, `macros.rkt`
    **`:11333`** — coordinate re-measured, the entry said `:11313`), and it
    reproduces in **sexp** mode, so it is an IR defect.
  · **DEFERRED 104** (NEW) — a bare operator bound by `def` yields a STUCK TERM
    at ZERO errors while `let` binds it correctly. Reproduces with `+`, so it is
    the first-class-operator surface, not a star or selection defect.
  · **A CLAIMED live defect that does NOT reproduce**, recorded so it is not
    re-raised: `$bcast-step` is genuinely ABSENT from `pattern-var?`'s exclusion
    list, and an adversarial critic predicted a whole-file
    abort for a broadcast inside a `defmacro` TEMPLATE. **Three shapes probed at
    `19560a7c`: all clean at 0 errors**, including one where the macro expands
    correctly to `[PVec Int]`. The absence is real; the predicted symptom is not.
    ⚠ **NUMBER CORRECTED 2026-08-10 (measured):** the exclusion list is **20**
    entries, not 23. **23** is the count of reader-emittable `$`-symbols that
    REMAIN misclassified (of 33) — `macros.rkt`'s own comment says so. Two
    different quantities that had merged into one figure here.
    ⚠ The *class* argument survives the failed example — `$bcast-step` WAS missed
    from `access-sentinel?` and **P4c-2 closed ✅ green anyway**, caught a phase
    later. ⭐ **The census states it better than either verdict** (method finding
    10): `pipeline.md`'s documented template abort no longer reproduces, so
    `pattern-var?`'s residual is now **SILENT rather than LOUD** — a miss no
    longer announces itself. Neither "it's broken" nor "it's fine" was right.
  · **DEFERRED 105** (NEW) — R4 is NOT independently landable; the tokenizer
    repair restores the carrier TOKEN but makes the star a separate datum ITEM,
    and that count change is attempt 1's failure mode. ⚠ Its framing is CORRECTED
    in D4 §5.P4e-1: it is TWO gates, not one, and they separate.
  · **DEFERRED 106 + 107** (NEW, both PRE-EXISTING and differential-proven) —
    106: the `let` nested-shorthand binding value leaks a raw sentinel, reached
    by none of the four `rewrite-dot-access` seats; it BOUNDS Q_U35's
    "dissolves that whole blast radius". 107: carrier-plus-star in PATTERN
    position mints and is unruled — `c.a` in the same seat gives
    `h defined (arities: 1, 3)` at ZERO errors, and Q_U32's refusal never landed.
  · **DEFERRED 108** (NEW) — a trailing `*` in PATTERN or MATCH-SCRUTINEE
    position is a WHOLE-FILE ABORT: **21 of 320 cells**. Glued-vs-spaced proves
    the MINT is not implicated (a spaced `*` aborts identically, and three
    non-minting controls abort too). `pattern-pos`'s `list-ref` signature is
    DEFERRED 103's sibling. PINNED as a measured snapshot, not skipped.
  · Live DEFERRED: 31, 33, 34, 35, 39, 40-residual, 41, 44, 54, 56, 57, 58, 62,
    63, 65, 75/76, 77–84, 88, 89, 91, 92, 93–101, 104, 105, 106, 107, 108.
  · Chips out: `task_204859b9` · `task_4c00d3f0` · `task_e1c15ee6`.
- **Watching** *(carried from 2026-08-07)*:
  1. **The mini-audit / probe keeps refuting the premise — including MY OWN
     RECOMMENDATIONS, the design's own text, and now an adversarial critic's.**
     New data point 2026-08-10: the critic's headline "live defect" (V1,
     `pattern-var?` → defmacro abort) did not reproduce in three shapes.
     **PROMOTION OVERDUE** — fold into the rules tier at the next close.
  2. **The design's own enumeration under-counts** — 10th+ arc: the census found
     **24** form-level item-counting consumers where an earlier sweep found 14,
     nine of them invisible to the obvious grep.
  3. **A failing test is only evidence if it fails for the reason you claim** —
     carried. Applied this session: all four Q_U32 pins were MUTATION-TESTED
     (every expected match #t, every mutant #f) before being trusted.
  4. **A claim is only evidence at the layer you measured** — carried.
  5. **An A/B over a dirty tree measures the tree, not the change** — carried.
  6. **A green suite proves nothing for a class no file spells** — ≥3 this arc.
     **Promotion candidate alongside item 1.**
  7. **HEAD moves; handoff coordinates go stale fast** — confirmed again: the
     P4d handoff shipped stale on DEFERRED 38 within two days, and this session
     found `macros.rkt:11313` → `:11333` and three stale `quests` sites.
  8. **The MEASUREMENT can be the broken thing, not the code** — 2nd data point,
     2026-08-10: a mutation-test probe passed `(quote [2 3 4])` into Prologos
     (shell-quoting damage), so the `reduce * 1 …` leg measured `Unbound variable
     quote` and reported the pin as failing. **The pin was fine; the probe was
     broken.** Caught by printing ACTUAL output alongside the assertion instead
     of reading the boolean alone. Sibling of item 6 one level down.
  9. **WIDENING a slice mid-flight is where I introduce defects** — 3 data
     points. **Budget for the second verify, not just the first.**
 10. **NEW (1st data point) — A FACT HAS A TIMESTAMP, and a partial revert is
     how one rots quietly.** Q_U23's five recorded lexical symptoms were measured
     correctly and were stale within one arc, because `d0ac2a58` reverted the
     MINT but not the guided-error surface. Every STRUCTURAL claim survived; two
     of five SYMPTOM TEXTS did not. **Pin the mechanism, not the message** — and
     when a revert is partial, say which half came out.
 11. **NEW (1st data point) — AN AMBIENT RULE CAN GO STALE, AND NOTHING RE-READS
     IT.** `pipeline.md` § "A new sentinel, an old recognizer" says a
     `pattern-var?` miss "is a whole-file abort in a `defmacro` template" and
     names `'[1 2]` / `$list-literal` as the live example. **MEASURED at
     `7fa26ea7`: it is not.** `defmacro lit [] '[1 2]` + `[lit]` yields
     `v : [List Int]` at 0 errors, and a template holding `$dot-access` gives
     PER-COMMAND errors with `before`/`after` both surviving. `macros.rkt`'s own
     comment carries the same stale "abort TODAY" claim two lines above the
     predicate. The abort now needs the **splice** shape specifically
     (`datum-subst`'s pattern-var arm passes an unbound var through by design;
     only `datum-subst-list`'s `$var ...` arm still raises). ⚠ And that splice
     arm's shipped justification — *"a sentinel is a list HEAD followed by its
     payload, never a bare symbol followed by `...`"* — **is falsified by
     `$postfix-star`, a bare symbol**: the first counterexample to the invariant.
     `.claude/rules/*.md` is the ONLY ambient tier, so a stale entry there is
     wrong in front of every session (same shape as the broadcast-is-parallelism
     correction). **Fix the rule when 1a-ii lands.**

---

## LOG  (append per commit / decision / surprise — newest at the BOTTOM)

### (xx) Q_U32 RULED — the guard rail is PINNED BEFORE the design, and the discriminator is POSITION, not spelling — `f63612e7`

**Why.** DEFERRED 103 was named the first question of attempt 3 because, if a
bare `*` stays legal in pattern position, any mint must be pattern-position-aware
**at a stage that does not know it is in a pattern**. The owner ruled it out of
existence: refuse bare `*` in pattern position, provided the arithmetic uses are
untouched — "`* 3 5`, `* 3 _`, `[* _ 2]`, mixfix `.(4 * 5)`".

**⭐ Surprise, and it is the finding that makes the ruling implementable.** The
four named spellings are not the whole guard rail. Since Numerics N6e-E2
**operators are first-class values**, `reduce * 1 '[2 3 4]` → 24 and `let op *` /
`[op 3 4]` → 12 put a **BARE, unglued `*`** in argument and binding position. So
a refusal keyed on "a bare `*` token" would break live surface: **the
discriminator has to be POSITION.** Found by sweeping rather than by reasoning
from the four given examples — the enumeration-under-counts pattern (Watching 2),
caught this time on the input side.

**Also measured**: the ruling is BROADER than the defect. `match q | * -> 99`
yields 99 at 0 errors and `defn f | * -> 1` works — both harmless today, both
refused by Q_U32. Accepted as the simpler rule, and monotone per spec §3.6.
Bracket destructuring (`let [a *] pair`) reads as APPLICATION and is NOT in the
domain.

**Observation for the mini-design, explicitly not a ruling**: the arm loss is
observable at the **pattern compiler**, which knows it is in a pattern. So the
refusal need not sit at the reader — the same move Q_U31 used to keep a context
test out of the grouper.

**Doc-truth, each verified by RUNNING the code rather than reading a commit
message**: DEFERRED 102's glyph ⬜ → ✅ (its fix commit never touched
`DEFERRED.md`; re-ran its own reproducer — partial output, one per-command
error, `after` still defined) · Q_U23's stale symptoms corrected with the
partial-revert explanation, one bullet marked UNVERIFIED rather than confirmed
because the re-probe used a different subject · DEFERRED **104** filed, register
bumped to 105 in the same commit.

**Next.** The census extraction (part 2 of the prep), then the attempt-3
mini-design against the constraint set.

### (xxi) THE CENSUS ARTIFACT IS IN THE TREE — 38 constraints, 12 approaches, and six places the sources disagree — `ae67419f`

**Why.** The owner ruled that attempt 3 opens from a MEASURED MAP. The map lived
only in a session transcript while **three documents cited it by existence** —
D4's `#star-census` carried six summary bullets, and the ranked constraint set,
surface map, carrier table, discriminator inventory and twelve approaches were
nowhere readable. A design cannot open against an unreadable artifact, and
session transcripts are not durable storage. Recovered to
`docs/research/2026-08-09_STAR_SURFACE_CENSUS.md` (860 lines, 44 anchors).

**⭐ Surprise — the consolidation's most valuable output is the DISAGREEMENTS
table, not the constraints.** Six places the five axes and the synthesis
contradict each other, preserved rather than silently resolved. The dangerous
three are COUNT rows using different denominators where **none records its
command**: spaced bare `*` = 71 lines / 67 code sites / **76**; fused
trailing-star idents = 306 (incl. comments) / 171 / **170**. Any of those could
have been cited as fact by attempt 3. Also: `split-star-lexeme` call sites 4 vs
5, never adjudicated · the discriminator IDs collide with the Tier-D constraint
IDs *and* the two axes number `close-type` differently · the synthesis says
"Eleven distinct approaches" and enumerates twelve.

**⭐⭐ The census independently reached — and stated better than I did — the
`pattern-var?` question this session opened.** An adversarial critic predicted a
live whole-file abort for a broadcast inside a `defmacro` TEMPLATE, from
`$bcast-step`'s genuine absence from the 23-sentinel exclusion list. I probed
three shapes: all clean at 0 errors, so I recorded "does not reproduce." The
census's method finding 10 gets it right: `pipeline.md`'s documented template
abort no longer reproduces, so **`pattern-var?`'s residual is now SILENT rather
than LOUD** — a miss no longer announces itself. Not a live defect; a *quieter*
hazard than the documented one. **Neither "it's broken" nor "it's fine" was the
right answer.**

**Verified before landing, not trusted**: R1–R12 present in the source ·
constraint IDs A1–A10 / B1–B3 / C1–C4 present · the carrier arithmetic present ·
the "Eleven vs twelve" contradiction genuine rather than a consolidation
artifact · **zero `file.rkt:NNN` citations** (the drift rule).

**Next.** The attempt-3 mini-design, opened against `#constraints` — with the
three count rows re-measured before any of them is cited.

### (xxii) SLICE A SHIPPED — the `postfix-star` token type, after THREE verify rounds each finding a blocking defect in my own code — `e7a49228`

**Why.** [Q_U33] ruled a token-TYPE mint: a `*` byte-adjacent to a preceding
CLOSER is re-typed at `disambiguate-tokens`. COUNT-PRESERVING and DATUM-INVISIBLE,
which is the whole structural difference from attempt 1.

**⭐⭐ Surprise — three rounds, three blocking defects, and rounds 2 and 3 each found
a defect in the FIX for the round before.** R1: the mint fired on LIVE INFIX
MULTIPLICATION (`.((1 + 2)*(3 + 4))` → 21 at 0 errors) — a DESIGN problem, ruled by
[Q_U34]; plus `a >>* b` (the lookback read the pass's INPUT and saw a token
`compose-merge?` had consumed) and `1 >* 2` (a bare `>` is `rangle` whether or not
it closes anything). R2: the frame stack pushed **4 of 9** opener types while
popping all 3 closers, so `'[` `@[` `~[` `#{` `.{` were each a NET POP that ate the
mixfix frame — **this file's own 31d27c83 class, committed 280 lines below the
comment that names it**. R3: the mirror — a STRAY closer over-popped.

**⭐ The fix that mattered was DELETING a list, not extending it.** The opener
enumeration is now defined ONCE and consumed by every frame stack in the file,
including the two twins whose disagreement the file records BY COMMIT HASH.
Hand-copied opener lists in `parse-reader.rkt`: **0**.

**⭐ And R3's finding outlasts its bug**: the authority for mixfix extent is
`group-items`, NOT `make-bracket-depth-rrb` — the latter over-pops too, so
*mirroring it faithfully reproduced its bug*. "It mirrors the authoritative one"
was true and insufficient.

**My own instruments shared the implementation's blind spot**: a 17-row probe
written specifically to hunt frame leaks missed the 4-of-9 defect, because like the
code and like the tests it only exercised `(` `[` `{`. **An instrument written from
the implementation inherits its assumptions** — derive guard rows from the
AUTHORITATIVE enumeration.

**Next.** Slice B, the tokenizer repair.

### (xxiii) SLICE B — BUILT, VERIFIED, ⛔ REVERTED; R4 is not independently landable — `0e007864`

**Why.** R4 relaxed both digit arms so `xs:0*` / `x.0*` stop destroying their own
carriers. It WORKED at the token layer — six of seven carriers minting, battery 468
green, acceptance 84/84, every slice-A gate holding.

**⛔ And it was a SILENT-WRONG-ANSWER regression.** 4 tokens → 3 makes the star a
separate datum ITEM, and item counts are load-bearing: `def w2 := [take2 p.0*]`
went from a loud `Too many arguments` at HEAD to **`w2 : Int defined.` / `10` at
zero errors**, the written flatten discarded and the `*` filling a parameter slot.
A `defr` fact row registered 2 rows instead of 4; a `bundle` invented a type
parameter. **Attempt 1's failure mode, one slice later, in a narrower aperture.**

**⭐⭐ Surprise, and it is the finding: CONTAINMENT IS NOT A SAFETY PROPERTY.** A
verify axis measured **1,943/1,943 glued ≡ HEAD-spaced** and offered it as *"the
argument that makes the count change payable"*. Category error — token-stream
containment says HEAD *could* have produced this stream **from different source
text**, never that the written source's MEANING is preserved. `xs:0 *` and `xs:0*`
agreeing **IS** the defect. That axis CLEARED the slice; the adjudicator overturned
the clearance and found a fourth defect none of the three skeptics had.

**Why the obvious fix does not apply**: the identifier band is count-preserving
because it FUSES, the star riding inside the symbol. The ordinal band cannot —
`$postfix-index`'s payload must be NUMERIC, so a `.0*` lexeme yields `#f`.

**Next.** Semantics first (owner), for the carriers slice A already delivers.

### (xxiv) TWO CORRECTIONS TO MY OWN COMMITS, and one was inside a doc-truth commit — `0306ed72` · `832d9ae2`

**Why.** A doc-truth commit "corrected" a `star-step-trigger?` reference by
asserting the symbol *"has never existed in the tree"*. **False.**
`git show bfba68d5` has it defined, exported and called, plus a call site in
`surface-rewrite.rkt`; the revert `d0ac2a58` removed the DEFINITION and left the
REFERENCE — a dangling pointer, not an aspirational name.

**⭐ Surprise — I diagnosed a stale comment by READING the tree and inferring
intent, inside a commit whose entire purpose was correcting comments that asserted
unmeasured things.** A symbol's absence at HEAD says nothing about whether it ever
existed; `git log` answers that in one command. **Check the history before
declaring a symbol imaginary.**

**And a second**: **Q_U34 was CITED SIX TIMES — in shipped code comments, tests and
a commit message — while D4's register still read "next free U34"**, i.e. the label
was never allocated and the ruling had no entry. Slice A shipped citing a ruling
that did not exist as a record. Both now recorded, with [Q_U35] (`*` after a
non-selection expression is REFUSED, owner).

**Next.** P4e-1.

### (xxv) P4e-1 OPENED — the mint is INERT by construction, and the arrival inventory is ELEVEN contexts wide — `1476734b`

**Why.** Slice A's datum-invisibility — the property that kept the corpus A/B clean
— is also why the mint has **no consumer**. Measured: the carriers give `Could not
infer type`, byte-identical to their spaced controls, so there is **zero observable
difference** between glued and spaced today. Both out-of-band channels are dead:
syntax properties AND srcloc are destroyed by preparse for `cfg{a}*` / `xs:{a}*` on
a `def :=` RHS while surviving at command position — either would work for half the
carriers and **silently fail on the surface users actually write**.

**⭐⭐ Surprise — DEFERRED 105's framing was wrong, and correcting it is what makes
semantics-first viable.** 105 recorded "the datum representation must be decided" as
ONE gate. It is TWO: for slice A's carriers the star **already occupies a datum
item**, so renaming its VALUE is COUNT-PRESERVING and never engages what killed
slice B; for `xs:0*` / `x.0*` the star has **no item at all**. Four carriers clear,
two blocked — a partition, not one wall.

**⚠⚠ The inventory, GENERATED not read: 40 of 44 spellings mint across ELEVEN
contexts.** Only the four mixfix ones do not — Q_U34's gate working. So the rename
puts a sentinel into eleven contexts at once. [Q_U35] is what makes that tractable.
Generated over a matrix rather than read off the code, **because the last three
blocking defects in this arc were all bad enumerations**.

**⚠ Measurement note**: rackunit ABORTS a test-case at its first failed check, so
"2 FAILURES" meant 2 failing test-CASES, not 2 assertions — the later checks never
ran. Do not read a failure count as an assertion count.

**Next.** Implement P4e-1a: uncomment the two parked test-cases, emit
`$postfix-star` at `token-entry->stx`, fuse at `rewrite-dot-access`. **Tier-O
obligations first, `pattern-var?` foremost.**

### (xxvi) P4e-1a OPENED — Q_U36 rules the fuse POSITIVE-LIST, and the inventory it was scoped against turned out to be a 21% SUB-FRAME — `7fa26ea7`

Stage-4 open: mini-design + `grounding-audit` (`wf_9bbe5f5a-9e0`, 4 facets +
completeness critic), cycled per the co-dependence the methodology describes —
and the audit's numbers reshaped the design twice before any code.

**Why** the owner's sequencing ruling came first: `<(x : Nat)* Nat>` is legal
today, elaborates to `[Sigma Nat Nat]`, and MINTS `postfix-star`. `star-symbol?`
is `(memq s '(* $star))` — it accepts `$star` but not `$postfix-star`. So the
rename ARMS the regression; the refusal is part of this change, not adjacent to
it. Probe-verified: `parse-datum` on `($angle-type (x : Nat) $postfix-star Nat)`
returns a `surf-app` chain where today it returns `surf-sigma`.

**Surprise 1 — I had the sequencing wrong inside the same turn I proposed it.**
I proposed the Sigma refusal as slice ONE. It cannot be: Q_U31 has **two**
identity families, and family 2 (`param-type->angle-type`'s `(memq '* ptype)`)
has its `$angle-type` SYNTHESIZED AT PREPARSE — so a reader-seated refusal
structurally cannot reach it. The refusal must RIDE the rename, which is the one
change that makes glued stars nameable at both seats. Owner re-ruled; the slice
went 4 → 3, with the last necessarily atomic.

**Surprise 2 — the blast radius was a 21% sub-frame, and the census had already
said so.** "40 of 44 across eleven contexts" is TRUE and regenerates exactly —
of a **4 × 11** frame. Measured surface: **10 carriers × 19 contexts = 190 of
280 cells**. The six uncounted carriers mint for a principled reason (`]` `}`
`)` are all in `group-closer-types`). The census's MISS 3 said *"the population
is 11, not 4"* **one day earlier**, and D4 cites that census as "the design's
input". ⚠ The generating matrix **did not exist in the tree** — `1476734b`
touched two files — so the number was unfalsifiable at HEAD. That is *Watching
2* firing again, at the exact section the phase opens on.

**Fix** — [Q_U36](../2026-07-28_CIU_T6_PATH_SELECTION_D4.md#q-u36): the fuse arm
is a POSITIVE list over the PREDECESSOR. It converts the under-count from a
defect generator into a gate-coverage obligation, and it DISCHARGES the census's
⭐BLOCKING Tier-O **O7** (conditional/recursive membership — the shape that killed
slice B) by never asking whether the star is a member. O7 was never carried into
P4e-1's OWED list.

**Surprise 3 — two "new" defects are PRE-EXISTING, and the differential is what
proved it.** `let k c{a}*` leaks a raw sentinel; so does `let k c.a`, with no
star. Carrier+star in pattern position drops an arm; `c.a` in the same seat gives
`h defined (arities: 1, 3)` at **ZERO errors** — worse, and star-free. Filed
**DEFERRED 106 + 107** rather than fixed (*Watching 9*). ⚠ 106 BOUNDS Q_U35's
"dissolves that whole blast radius": true only where the fuse seat runs, and
there are exactly four `rewrite-dot-access` seats.

**Also corrected, each measured**: the slice-A pin's harmlessness argument names
a DEAD path (`merge-preparse-and-tree-parser`'s tree leg is gone — its conclusion
is right, its mechanism false) · DEFERRED's "`star-step-trigger?` has no
definition anywhere in the tree" — `0306ed72` corrected that in `parser.rkt` and
**the correction never reached `DEFERRED.md`** · `pattern-var?` has **20**
exclusions, not the 23 this file said (23 is the *residual*, a different
quantity) · and the ambient `pipeline.md` abort claim does not reproduce
(*Watching 11*).

**Next**: slice **1a-i** — land a durable matrix generator (10 × 19) and widen
the LIVE leak gate from its 6 rows, which covers 6 of 190 cells and uses 2 of 10
carriers. No behaviour change; the gate is currently green *vacuously* and arms
on 1a-iii's rename.

### (xxvii) SLICE 1a-i — the arrival matrix is in the tree, and the verify proved my gate's OBSERVABLE was wrong, not just narrow — `ff9b7d81`

**Why** the slice exists: D4's "40 of 44 across ELEVEN contexts" was generated by
a script that was never committed, so at HEAD it could not be re-derived,
extended or audited. `tools/star-arrival-matrix.rkt` is now that artifact, and
both the dump and the battery READ it rather than copying it.

**Surprise 1 — the claim was right and the frame was small.** 40-of-44
reproduces exactly, as a sub-frame of **11 carriers × 19 contexts = 209**. The
census had already said "the population is 11, not 4" *one day earlier*, in the
document D4 cites as the design's input.

**⭐⭐ Surprise 2, and it is the slice's lesson — I MEASURED THE WRONG THING.**
The gate grepped user-visible output for `$postfix-star`. The adversarial verify
planted the sentinel in each cell and ran my own gate over it: it could fire in
**72 of 190** cells, and in the rest clean and planted output was BYTE-IDENTICAL.
The reason is structural — an unconsumed sentinel is an EXTRA DATUM, so the form
fails on SHAPE (arity, parity, parse) long before anything renders the symbol.
`quasiquote-body` renders only the TYPE, so no message could ever reach it.
**Fix**: the invariant is *no unconsumed `$postfix-star` survives PREPARSE into
the datum*. Measured, and stated rather than rounded up — **datum 187/220 ·
message 92/220 · union 208/220**, complementary rather than nested, 12 residual
blind cells in `let-bracket`(8) / `quasiquote-body`(3) / `let-nested`(1), which
is DEFERRED 106's own seat.

**Surprise 3 — five defects in my own instrument, four found by the verify.**
A `defmacro-tmpl` row that never registered a macro (a bare `[f 1]*` is TWO
template datums) so `datum-subst` never ran — the exact Tier-O path the row
existed to cover — while 33 output lines made it look healthy, *and my comment
asserted the opposite*. Whole contexts skipped when a batch raised (28 cells
dropped, 21 abort). An `(check-true (pair? msgs))` that was unreachable. A mint
pin asserting cardinality where its stronger twin sat 20 lines above. And one I
caught myself: `star-cell-source` took a STRING while every caller passed the
RECORD — `format`'s `~a` hid it, every source was malformed, and every count and
both mutation tests still agreed.

**Surprise 4 — a pre-existing WHOLE-FILE ABORT class, found only because the
instrument became well-formed.** 21 of 320 cells raise; the glued-vs-spaced
differential rules the mint out (a *spaced* `*` aborts identically, and three
non-minting controls abort too). Filed **DEFERRED 108**; `pattern-pos`'s
`list-ref` signature is DEFERRED 103's sibling. Pinned as a measured snapshot so
a fix turns the battery red and claims the win.

**Also**: a shared-fixture temp-file leak (`run-ws-pre-raw` skipped `delete-file`
on the raise path; **192 stale files** had accumulated, now `dynamic-wind`, 0),
and my own cost comment's arithmetic was wrong ~2× — corrected in place rather
than deleted, because asserting a number instead of measuring it is this arc's
recurring failure and deleting the evidence hides the pattern.

**Gate**: battery 465 → 466 · file 74 s → 79 s (memoizing a duplicated batch pass
paid for the new work) · neighbourhood 322 green across 7 files · all three pins
mutation-tested, each red for the reason its name claims.

**Next**: slice **1a-ii** — the Tier-O sentinel arms, landed INERT (dead code
until 1a-iii), `pattern-var?` first and preferably by the polarity inversion the
file itself asks for.

### (xxviii) SLICE 1a-ii — the Tier-O arms are TWO LINES, and I reversed myself twice getting there — `dc458109`

**Why so small**: the inventory was generated rather than assumed, and it said
*no arm needed* far more often than *arm*. `access-sentinel?` and the
`reader-forms.rkt` head sets, `flatten-ws-datum`, and both preparse lists all
require a PAIR — a bare atom can never reach them. That is [Q_U36](../2026-07-28_CIU_T6_PATH_SELECTION_D4.md#q-u36)'s
positive-list ruling arriving as a coverage dividend rather than as extra work.

**Surprise 1 — the handoff's premise was stale.** It carried `pattern-var?` as
"a miss is a whole-file abort in a `defmacro` template". Not any more:
DEFERRED 3 was discharged at `446070fc` by moving `datum-subst` onto the
BINDINGS, and **both `pipeline.md` (AMBIENT) and `macros.rkt`'s own comment were
wrong about it**. Measured: `defmacro lit [] '[1 2]` + `[lit]` → `v : [List Int]`
at 0 errors. `pipeline.md` corrected in this slice (*Watching 11* discharged).
The residual raise is `datum-subst-list`'s SPLICE arm alone — **unreachable from
WS** (`...` reads as `$rest` while the arm tests `'...`) and G2-degraded to a
per-command error in sexp. So the exposure was a leaked internal NAME, not an
abort — and the fix was sized to that rather than to the scarier claim.

**⭐⭐ Surprise 2 — I reversed my own work twice, and both reversals came from
checking rather than reasoning.**
1. I fixed the splice arm STRUCTURALLY (require the binding to exist), because
   its stated invariant — *"a sentinel is a list HEAD …, never a bare symbol
   followed by `...`"* — **is** falsified by `$postfix-star`, the first
   bare-symbol sentinel. The neighbourhood run turned `test-defmacro.rkt`'s
   "the SPLICE branch keeps its unbound error" **RED**. That raise is a RULING
   (`446070fc` states it in writing); only its rationale was false.
   ⭐ **A falsified rationale does not license reversing the decision it was
   offered for.** Grep for an existing pin before "completing" someone's fix.
2. I gave `pp-datum` an arm rendering the sentinel as `*`. The adversarial verify
   showed that every CONSUMED marker deliberately has **no** arm and prints its
   internal name, because for a consumed marker *appearing there IS the defect* —
   `*` would make a leak indistinguishable from correct output inside `expand`,
   the exact tool someone debugging the star would reach for.

**Also caught by the verify**: a comment of mine asserting the opposite of the
code it shipped with (it survived reversal 1), and a third test that was vacuous
— it claimed to test a sexp defmacro leak while using the WS fixture on a source
containing no star and no sentinel. Replaced with the consequence that IS real
and was unpinned: the exclusion crosses onto the PATTERN side (which `macros.rkt`
declares out of scope), so a macro whose PARAMETER is named `$postfix-star`
registers and then silently never matches.

**Recorded, not closed**: `pol8-bad-head-error` (parser.rkt) and
`process-ns-declaration`'s segment message (namespace.rkt) both render a bare
atom into user text and would print the sentinel's name. Neither is reachable
from any star context in the matrix, so 1a-i's gate cannot see them.

**Gate**: battery 466 → 469 · track file 72 s · neighbourhood 287 green across 6
files. Both production arms mutation-tested — and note the exclusion's pin fails
as an **ERROR**, not a FAILURE, which is why the discipline says count both.

**Next**: slice **1a-iii**, the atomic one — rename + gate disjunct + the Q_U36
positive-list fuse + Q_U35's refusal + Q_U31's refusal at BOTH identity families,
then uncomment the two parked tests. It cannot be split: the live leak gate arms
the instant `token-entry->stx` emits the symbol.

### (xxix) SLICE 1a-iii SHIPPED — the mint has its consumer, Q_U37 ruled it territory-scoped, and round 2's verify caught the rename leaking through error ECHOES — `bd2c3575` · `9cac0099`

**The feature**: `xs:{a}*` / `c{a}*` reach the guided not-yet message; `[f x]*`
takes Q_U35's refusal; the glued Sigma takes the type-seat message at BOTH
identity families; quote/quasiquote capture the star as the `*` the user wrote.
The parked tests are LIVE. And the fuse **retired two whole-file aborts**
(DEFERRED 108: 21 → 19).

**⭐⭐ Q_U37 [owner]** — attempt 1 (the refusal-at-preparse marker) was built,
verified, REVERTED: one wrong assumption ("every star is the fold's business"),
two measured symptoms (the marker PRE-EMPTED the Sigma seat, so Q_U31's refusal
shipped structurally unreachable; and it broke the quasiquote lowering). The
ruling: the fold fuses in selection territory and otherwise LEAVES THE STAR for
the seat that owns the territory — Q_U34's own logic extended one layer in.
Slice 1a-i's blanket preparse invariant was RESTATED as a three-way PARTITION
(consumed/bare/gone), pinned cell-by-cell; the boolean version had been wrong
twice in opposite directions, and the second time it FORCED the implementation
into the marker. **The instrument shaped the code; the ruling unwound it.**

**Surprise — round 2's verify found the rename leaking where no gate looks.**
The def seam and the fn binder error both ECHO source datums into messages, and
the rename silently changed the echoed content from `*` to `$postfix-star` —
off-matrix seats, structurally invisible to the leak pin. Fixed via
`unmint-star-for-echo` (echoes render the USER'S spelling — the same
capture-fidelity argument as the quasiquote arm, and the OPPOSITE of pp-datum's
deliberate internal-name tell). Also caught: the family-2 pin was VACUOUS (its
`|defn requires` alternation passed on a star-independent message — an
instrument hedged into its own blind spot) and the arm's comment asserted a
route that dies before reaching it. The live route is the SPEC spelling; the
pin now targets it, and the previously-unkilled fifth mutant kills.

**Also**: the type-seat message is now ANGLE-GENERIC (unions were getting advice
about a product type they never wrote) · the preparse-RAISED set is a pinned
snapshot (3 cells, all reproduce star-free) · DEFERRED **110** (pipe-terminal
fused star — `expand-pipe-block`'s unwrap ambiguity, pre-existing, pinned AS an
accident like Q_U19's) · **111** (the raw-datum-echoing error-site CLASS — two
fixed, census unclaimed; 106/109 are the known unfixed members) · **112**
(`$facts-sep`, star-free, rel-territory).

**Gate**: battery 469 → 476 · neighbourhood 497/14 files · acceptance 84/84 by
diff · **full suite 10143 / 488 / 0** · five mutations kill.

**Next**: close P4e-1a, then P4e-1b per the sequencing (the actual flatten
semantics), with Q_U24/Q_U26 queued behind it.
