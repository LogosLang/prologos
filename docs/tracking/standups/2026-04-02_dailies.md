# Dailies — 2026-04-02

## Carried Forward from Previous Interval

### PPN Track 3 ✅ COMPLETE
- ALL top-level forms ON-NETWORK via tree-parser dispatch (§11 tree-canonical pivot)
- Cell infrastructure: per-form cells, dependency-set Pocket Universe, spec cells
- Single-parser architecture: parse-datum as expression parser, called from on-network dispatch
- parser.rkt effectively retired from WS form dispatch
- 383/383 GREEN, 7491 tests, ~133s. A/B: zero regression.
- PIR written with §11 addendum.

### Priority Ordering (from Master Roadmap)
1. SRE Track 2H — type lattice redesign (prerequisite for PPN 4)
2. PPN Track 4 — elaboration on network (consumes Track 3's per-form cells)
3. Grammar Form research (speculative design)

### Known Gaps / Blockers
- test-structural-decomp: meta-solving via imperative state under BSP
- WS-mode solver syntax: `=` unification, `is` bridging
- Type lattice not distributive (SRE Track 2H scope)
- Preparse registration dependency (PM series migrates to persistent cells)

### Deferred Work
- Session tree-parser stubs: datum conversion handles correctly, dedicated implementations future
- parser.rkt full deletion: blocked on sexp path retirement (PM module-loading-on-network)

### Watching (medium-term patterns)
- **Batch-worker parameter isolation**: 2nd instance (current-source-str). Pipeline.md checklist may need update.
- **"One canonical representation" challenges**: Track 3 pivoted twice (datum→tree, then whole-node datum conversion). The architecture is correct now but the path was long.

---

## Lessons Distillation Check

### Scanned: PPN Track 3 PIR + §11 addendum + April 1 dailies

**Ready for codification:**

1. **"Tree-canonical: on-network IS canonical, datums derived"** — Track 3 §11 lesson. The datum-canonical path was architecturally wrong (kept parsing off-network). Tree-canonical with datum derivation is correct. **→ DESIGN_PRINCIPLES.org**
2. **"Whole-node datum conversion bridges tree-parser and parse-datum"** — The architectural pattern: convert raw tree-node to datum, apply WS normalizations, call parse-datum. Gives on-network dispatch with proven expression parsing. **→ PATTERNS_AND_CONVENTIONS.org**
3. **"D:I ratio < 1:1 correlates with more bugs"** — 3rd confirmation (Track 3: 0.6:1 ratio, 6 bugs). **→ DESIGN_METHODOLOGY.org**
4. **"Batch-worker parameter isolation"** — 2nd instance. New parameters must be added to batch-worker parameterize block. **→ pipeline.md checklist**

**Still pending from prior sessions (5 items):**
- "Merge/hybrid over wholesale replacement" — 5 instances
- "Integration phases find all the bugs" — 8 of 11 PIRs
- "Wrong assumptions at boundaries" — 9 of 11 PIRs
- "Vision contaminates audit" — 6 instances
- "Resist algorithmic thinking: look for the lattice" — 3+ instances

**NOTE**: 9 patterns total flagged READY across multiple sessions. Codification backlog is significant. Should be prioritized.

---

## Today's Work

### Completed

1. **PPN Track 3 §12: SRE integration** (commits `0d5de638` → `b057a60d`)
   - S1: FormCell domain registered. Property inference confirms Heyting (7 properties).
   - S3: SpecCell domain registered. **Inference caught REAL bug**: top absorption ordering in merge. Fixed: top checks before bot checks, canonical top value for commutativity.
   - S4: 5 ctor-descs registered (surf-def/defn/eval/check/narrow). Roundtrip validated.
   - S5: Initially deferred to Track 4 — INCORRECT deferral. Parse-level propagator wiring IS Track 3 scope. Restored.
   - S6: All domains pass inference.
   - A/B: zero regression (14 programs, 3 concurrent batches).
   - PIR updated with §12 addendum documenting TWO critical process failures: (a) implementation deviated from design without acknowledgment, (b) PIR didn't catch the gap.
   - 3 new MUST-codify lessons: SRE registration mandatory, PIR scope against design, Completeness challenge on deferrals.

2. **SRE Track 2H Stage 2 audit** (commit `b30a7b83`)
   - 11-section audit of type-lattice.rkt (448 lines), subtype-predicate.rkt (226 lines), union type infrastructure, SRE integration.
   - Key findings:
     - Both equality-merge and subtype-merge produce type-top for incomparable types (no union).
     - Union type infrastructure EXISTS (expr-union, build-union-type, flatten-union, ACI normalization) but only used for map value widening.
     - try-intersect-pure only covers Pi/Sigma — missing 10+ constructors.
     - Function duplication between type-lattice.rkt and unify.rkt (3 functions) with active drift risk.
     - Per-relation property declarations not supported — properties are per-domain only.
   - Algebraic targets for subtype ordering: distributive + has-pseudo-complement → Heyting.
   - Document: `docs/tracking/2026-04-02_SRE_TRACK2H_STAGE2_AUDIT.md`

3. **Test regression fix** (commit `0b626e3a`)
   - **Real failure**: test-ctor-registry.rkt expected exact count 28, got 33 after PPN Track 3 §12 added 5 form-cell ctor-descs. Fixed: `>=` instead of exact count (module load order dependent).
   - **Stale dep-graph**: 3 entries for non-existent test files (test-functor-ws, test-pipe-compose-e2e-02, test-reducible) removed from both test-deps and test-prologos-deps.
   - **Stale failure logs**: 9 logs from Mar 25-28 cleaned from data/benchmarks/failures/.
   - **Process failure noted**: PPN Track 3 §12 should have run a full regression suite but didn't. The ctor-desc count test should have been caught immediately.
   - 383/383 GREEN, 7491 tests, 136.2s.

4. **SRE Track 2H: Design** (commits `7b9b8570` → `f8369678`)
   - D.1: 8-phase design with NTT model, mathematical grounding
   - Tensor pulled into scope (user challenge): full quantale, not half a semiring (`a9dc9112`)
   - Pre-0 benchmarks: 30 tests, 5 tiers. whnf dominates at ~300μs — no perf risk from unions (`a8023d58`)
   - **whnf fast-path** (independent): 625× speedup for atoms, 3.5% suite improvement (`92006da8`)
   - D.3 self-critique (3 lenses): R1 blocker (circular dep), M3 win (core/scaffolding tensor split)
   - D.5 external critique (12 findings): F1 bot-on-failure, F7 Heyting scoped to ground, F11 merge phases
   - Vision Alignment Gate codified from PPN Track 3 deviations (`621e4e89`)
   - Semiring structure documented for PPN Track 4 (`f712422a`)
   - ATMS speculation retirement documented (`51f5d9d4`)

5. **SRE Track 2H: Implementation** (commits `44a53651` → `19e165e2`)
   - Acceptance file: 6 sections, baseline passes (`44a53651`)
   - Phase 1: union-types.rkt extracted, 3 duplicate functions eliminated (`7a91db47`)
   - Phase 2: subtype-lattice-merge redesigned — unions for incomparable, bot/top handling, absorption atomic (`8bb7af3d`)
   - Phase 3: try-intersect-pure extended to 11 constructors via ctor-registry (`0ba64c3b`)
   - Phase 4: type-tensor-core (bot-on-failure) + type-tensor-distribute (scaffolding) (`0edee767`)
   - Phase 5: Tensor-aware expr-app elaboration (`493cfc68`)
   - Phase 6: Per-relation property declarations, operations field, 13 sites migrated (`bba6f7ab`)
   - Phase 7: **Distributivity achieved** — V4 0/512 (was 412). Subtype callback + meet distributes over unions + sort key fix (`5735b9e8`)
   - Phase 8: Pseudo-complement scaffolding (`19e165e2`)
   - 384/384 GREEN throughout, 7526 tests (after test file added)

6. **SRE Track 2H: PIR + follow-up** (commits `61553175` → `7050e49b`)
   - PIR written (21 sections, full 16-question methodology after rewrite)
   - 35 persistent regression tests added including F7 binder-type distributivity (`47e8d401`)
   - make-sre-domain keyword constructor — retires L4 positional debt, all 13 sites migrated (`7050e49b`)
   - 4 scaffolding items documented on PPN Master for Track 4 retirement (`9d087f0a`)
   - PIR methodology checklist rule codified (`dea5d414`)
   - Phase completion blocking checklist codified (`129dee8d`)

7. **SRE Track 2D: Stage 2 audit** (commit `c26fe96e`)
   - Mapped PPN 2-3's 12 rewrite rules to DPO/SRE concepts
   - 3 layers: rule registry (data), pipeline (on-network), rhs-builders (imperative)
   - 6 simple rewrites directly liftable; 3 recursive need fold combinator
   - Key gap: implicit interface K (positional children, not named sub-cells)
   - Monotonicity resolves via existing pipeline — no new stratification needed
   - SRE Master updated: Track 2F → ✅ (PIR outstanding), Track 2D linked

---

## Considerations

- 9+ patterns flagged READY for codification — growing backlog. Dedicated codification session recommended.
- SRE Track 2H COMPLETE. Track 2D audit done — ready for design scope discussion.
- Sequence: SRE 2D → Grammar Form R&D → PPN Track 4
- SRE Track 2F has no PIR — outstanding documentation gap.
- 4 scaffolding items in PPN Master for Track 4 retirement.

