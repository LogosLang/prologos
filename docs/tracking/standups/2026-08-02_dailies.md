# Dailies — 2026-08-02

Rolled from `2026-07-28_dailies.md` (**214 KB**, far past the ~400–500 line cap)
on a **topic shift**: this file covers an OUT-OF-BAND arc in a separate worktree
(`.claude/worktrees/sleepy-tharp-263e0f`, branch `sleepy-tharp-263e0f`, based on
`ab55a99b`) — the dual-spine parser merge defect, filed by CIU T6 D4.P4c-4's
mini-audit at `388af899` precisely so it would NOT be discovered mid-slice.

⚠ **This is not a CIU T6 continuation.** The prior file's STATE head remains the
authority for CIU T6 D4 / Path Selection and is NOT copied forward here — only
the environment invariants are. Do not read the two as one arc.

---

## 📍 STATE  (overwrite in place — always current)

- **HEAD**: re-derive with `git rev-parse HEAD` — never trust a hash in prose. At
  this refresh: **`2d7813ef`** on branch `sleepy-tharp-263e0f`, **22 commits**
  ahead of its base `ab55a99b`. Local `main` is at `a1bcad17` (2 commits ahead of
  the same base). **NOT merged, NOT pushed.** ⚠ `docs/tracking/DEFERRED.md`
  conflicts with main — both sides appended at EOF; trivial (keep both), but not
  a clean merge.
- **Suite**: **9819 / 482 files / 0 failures**. Baseline at branch point was
  9799/479; the delta is exactly this arc's three new test files.
- **Corpus gate**: 163 `.prologos` files, full-OUTPUT A/B (not error counts —
  see LOG). Total errors **359 = 359** vs the `ab55a99b` baseline, **no file
  changed its error count**. Two deltas, both explained in the defect note §0.
- **Working tree**: clean. The MAIN checkout's ~41 owner-WIP entries were never
  touched — verified at close. (One slip mid-arc: I edited the defect note in the
  main checkout by path instead of the worktree, and reverted it immediately; see
  Watching 3.)
- **Where we are**: the `loc->line` defect is **CLOSED**. The merge key was broken
  **three** ways (the filed note named two, and its suggested fix was actively
  harmful); the tree spine has won **0 of 5,171 corpus forms, ever**; correcting
  the key REGRESSES the corpus (359→724 errors, 32 test files fail). So the spine
  is now held shut **on purpose** at an admission gate, pinned by a
  verified-to-trip test. Instrument built (`tools/spine-census.rkt`) and the
  follow-on architecture question is measured, not argued.
- **⭐ OWNER RULING 2026-08-03 — FINISH THE JOB.** Retiring the merge is not a new
  decision: it is **PPN Track 3 Phase 7's own designed deliverable**, shipped
  half-done. Phase 7's design says verbatim *"Delete:
  `merge-preparse-and-tree-parser`, `merge-form`, source-line-keyed identity
  matching, `tree-by-line` hash building. Total ~80 lines from driver.rkt."*
  `40d07caa` landed only the first half (form cells wired IN); the deletion never
  happened, and the comment *"Phase 7 will switch process-command to read from
  these cells"* was written in that same commit — its own unfinished second half.
  ✅ **THE TREE LEG IS GONE — `2d7813ef`.** The merge is now the preparse
  pass-through + `consumed-form-residue?` filter, keeping the form-cell block.
  Gates: corpus A/B full-output vs a `git archive` pin at `f0bce056` — 359 = 359
  errors, 26 = 26 aborts, **zero differing lines after normalising generated-name
  counters**; suite 9819/482/0.

  ⚠ **AND THE HISTORY WAS NOT WHAT THE HANDOFF SAID.** Phase 7's row read
  `✅ … No merge` for four months and was wrong twice: (a) at `5d3b597c` only
  `process-string-ws` moved to cells — `process-file` never left the merge; (b)
  `19d9f8aea` (Rel T1 SC, 2026-07-20) reverted the string path too, because
  cell-pipeline-only **dropped preparse-macro support** (`solver`/`schema`/
  `defmacro` broke in REPL/LSP), removing `extract-surfs-from-form-cells`'s last
  caller — which is WHY the form cells are write-only. Phase 7's second half was
  shipped and then **reverted for a reason that is still unsolved**.

  **NEXT = (B) delete the legacy `parse-*-tree` family** (~1,971 lines / 33 fns).
  Now unblocked: after `2d7813ef` `parse-form-tree` is unreachable from production
  — only its own recursion, its `module+ test`, `tools/spine-census.rkt`, and the
  caller-less `extract-surfs-from-form-cells`. ⚠ Deleting it means deciding
  `spine-census.rkt`'s legacy mode AND **not** taking
  `extract-surfs-from-form-cells` as collateral — it is Phase 7's intended surf
  source. **The step AFTER that is not wiring**: making the form cells READ means
  solving the preparse-macro problem that caused the `19d9f8aea` revert.
  ⚠ **The merge function itself is LOAD-BEARING** — it returns the surf list
  `process-surfs` consumes. "Retire" means reduce, not delete.
- **Superseded framing (kept for the record)**: the census
  established that the tree spine's datum path is not a parser at all: it feeds
  `parse-datum`, the SAME parser preparse uses. So the system has **one parser and
  two readers**, and the merge compares a parser against itself. The real option is
  **UNIFICATION** (one parser, two readers, delete the 1,971-line legacy family,
  retire the merge — any residual difference is a READER bug and belongs in a test).
  Agreement: **29 % legacy / 82 % datum / 98 % raw-datum / 99 % after the
  where-pass fix**. `DEFERRED.md` § "Dual-spine parser merge" is reframed
  accordingly. Do NOT land `current-raw-node` ahead of the ruling — with the gate
  shut it is unobservable ("nothing behind the switch", P4c-3).
- **Issue #69 CLOSED** on GitHub 2026-08-03 (both halves verified at HEAD; last
  residue `16203dbe`), with the verification in the close comment.
- **⚠ THE FENCE — what must NOT move.** Three layers are routinely conflated;
  this arc touched only the third. (1) the reader's 5 parse cells
  (`parse-reader.rkt`) = PPN Track 1's deliverable, scoped by 4D §12. (2) the
  per-form cells (`form-cells.rkt`) = PPN Track 3's deliverable and the intended
  REPLACEMENT for the merge. (3) the merge's tree leg = gated off. **(1) and (2)
  stay, precisely BECAUSE they are unwired** — write-only is the "cells landed,
  propagator wiring unbuilt" state PPN Master row 3 documents, not abandonment.
  I called (2) "dead weight" earlier in this arc; that was WRONG and was the one
  genuinely dangerous characterisation. **PPN 4C is elaborator-side and unaffected
  either way**; neither 4C nor 4D mentions the merge machinery at all.
- **Open / blockers**: none blocking. Two coordination notes: (1) **CIU T6
  D4.P4c-4 needs no change** — its recorded assumption is now a stated invariant
  rather than an accident; I deliberately did NOT edit the D4 design doc, since
  it is under active work in the main checkout. (2) A **PRELUDE drift** chip is
  running independently (pre-existing, unrelated to this arc).
- **Watching** (patterns, not yet codified):
  0. **⭐ A PLAUSIBLE EXPLANATION IS NOT A MEASUREMENT.** I asserted a "structural
     98 % ceiling" in THREE documents as fact; it was reasoning, and running the
     experiment refuted it in one command (a skipped pass, not missing
     information). Same shape as the `<`-hint gap: a comment asserting why
     something could not happen, load-bearing for an omission, invalidated by a
     later fix and never re-checked. **When a residual gets an explanation, run
     the experiment that would falsify it BEFORE writing it down.**
  1. **The instrument is the thing most likely to be wrong** — 4 data points this
     arc alone: a regex census that said 292/292 clean (missed `surf-narrow`), a
     comparator that manufactured ~500 false divergences, a timing probe that
     reported a confident `0 ms`, and an error-COUNT corpus gate that could not
     see whole-file aborts or silent value flips. Every one produced a *plausible*
     number. **Verify the instrument before believing the measurement** — near
     rule-threshold; `reader-corpus-ab.rkt` already encodes a tripwire for exactly
     this ("THE FALSE ZERO").
  2. **"Fix the obvious thing" was wrong three times running** — the note's arm
     swap (harmful), the srcloc-struct conversion (regresses), flipping
     `[else tree-surf]` (leaves the *unguarded* branch as the only live one). In
     a system this old, the obvious fix is load-bearing for something.
  3. **Worktree path discipline**: editing by absolute path silently targets the
     MAIN checkout. Caught once here. Prefer paths relative to the worktree root.

---

## 📓 LOG  (append-only, newest at BOTTOM)

### The merge key was defused THREE ways, and enabling it regresses — `d4e32398`

**Why**: the filed note (`388af899`) named two defects and proposed swapping two
`cond` arms. **Surprise**: there are three, and the third decides everything —
the two spines number lines on **different bases** (tree 0-based, preparse
1-based), which was in nobody's enumeration. So the note's own fix does not
enable the merge, it **MISPAIRS form N with form N+1**: 176 of 242 hits (73 %)
were mispairings, and end-to-end on three `def`s it leaves the first one
**unbound**. **Fix**: make the defusal deliberate at the key rather than
accidental. **Next**: measure whether a *correct* key is even wanted.

### Closed the note — three defects, the proposal was harmful — `002273af`

**Why**: the note scoped the work; it should carry the outcome. Preserved §1–§5
**as filed** with the two errors marked inline rather than edited away — the
shape of the miss is the lesson. **Surprise**: §1's "arm 2 yields the COLUMN" is
also wrong; it yields a hardcoded literal `0`, so `tree-by-line` held exactly ONE
real key. **Next**: classify *why* a correct key regresses.

### 14 defects across 4 layers — and I corrected my own gate — `b3e146ff`

**Why**: "one systematic cause or many" decides repairable-vs-not. **Surprise**:
many, and two are INPUT-CONTRACT defects no arm repair reaches (the tree
re-derives numbers from lexeme strings, losing exactness — `1.5f32` becomes
1.5e32; and it parses UNEXPANDED source). **Fix**: filed the census. **Also**: my
own "359→724" **understated** it — two files clean at HEAD abort entirely, and an
error-COUNT gate cannot see that or the 6 silent value flips. Recorded so the
next person does not reach for the cheaper gate.

### Closed the string-mode hole + pinned the invariant — `8f437a5c`

**Why**: asked where my own fix does *not* apply. **Surprise**: it didn't cover
`process-string-ws` at all — that path carries a srcloc *struct with line 0*,
truthy, so the list arm never fired. **Fix**: reject line 0 outright — *line 0 is
not a line, it is the UNKNOWN sentinel* — a better-motivated guard than the one I
shipped, guarding on whether the position is KNOWN rather than how it is
REPRESENTED. **Also**: `d4e32398` shipped **+0 tests** against this project's own
rule; added the tripwire, verified to trip.

### Moved the guard to ADMISSION — `385d3ec1`

**Why**: the adversarial synthesis said the guard was in the wrong place, and it
was right — while its OWN counter-argument ("inverting leaves the spine
permanently unexercised") does not survive the measurement, since the spine has
*never* been exercised. **Surprise**: the root was **two propositions sharing one
`cond`** — an identity-key hygiene rule (true regardless of the spine) and a
parse-fitness claim. Position-knowledge was only ever a PROXY, which is why an
*independently correct* change defeated it. **Fix**: split them. **Not**
`merge-form`'s `[else]`: every other arm already returns preparse, so flipping
makes it a constant function — and the error branch reads `tree-by-line`
directly, so flipping would leave the spine's only live role being its
**unguarded** one. **Measured**: the now-dead tree parse costs 47–872 µs against
0.41–4.23 s wall (0.003–0.09 %) — removing it is not a win, so it stays.

### Recorded the guard's-home ruling — `37d46ed8` · gates line corrected — `47d23775`

**Why**: the doc still described the key-based guard and called the criticism
unresolved. **Next**: build the instrument the ruling needs.

### spine-census — the two "modes" are two ARCHITECTURES — `0ae2ec86`

**Why**: a build-failing oracle would be red from birth and contingent on an
unmade decision; a CENSUS is useful under both answers. **Surprise**:
`parse-eval-tree-for-cell` is **not a parser** — it converts tree→datum→
`parse-datum`, the *same* parser preparse uses. So legacy = a second PARSER
(1,971 lines, where all 14 defects live), datum = the tree as a **READER**.
Measured 29 % vs 82 % agreement. **Fix**: two instrument bugs caught BEFORE
trusting any number — positional masking (`surf-narrow` is the 1-of-377
exception) and surf-only recursion (~500 FALSE divergences of 1,039). **Also**: a
bare top-level expression is NOT `surf-eval`, so only ~54 % of forms are
merge-eligible at all.

### raw-datum: 29 % → 82 % → 98 %, residual is ONE class — `9d1511a4`

**Why**: 82 % is not 100 %; find the gap. **Fix**: isolated it by diffing the two
**datum streams** rather than reading code — `tree-node->stx-elements` flattens
then RE-GROUPS, and the `$…` sentinels are minted from the bracket TOKENS, which
the grouped tree has already consumed (`flatten-with-boundaries` drops the child
node's TAG). So `($list-literal 1 2 3)` becomes `(1 2 3)`. Feeding the UNGROUPED
node via `current-raw-node` — **the designed hook**, which the merge binds to
`#f` — fixes every one: **98 %**. **Surprise**: the residual 26 are NOT gensym
noise as I claimed one message earlier; `$Add-A` vs `x` is an implicit
**dictionary binder** from cross-form spec context that per-form expansion cannot
see. So 98 % is a **ceiling**, not a waypoint.

### Pinned the census comparator, both bugs verified to trip — `eec12ea2`

**Why**: a number from an unverified instrument is worse than no number, because
it gets acted on. **Fix**: regression tests for both instrument bugs plus **four
positive controls** — every masking assertion is satisfied by a comparator
hardwired to "equivalent", so one case must assert a real difference IS caught.
Re-introduced each bug and confirmed FAIL. Suite 9809/481/0.

### Filed the seven residuals, decision-first — `b06d3702`

**Why**: untriaged deferred items rot. Ordered with the OWNER RULING first
because everything else is downstream. Two entries record where I was **wrong**
(the 98 % ceiling; the unguarded error branch), because the correction is the
useful part. **Next**: the ruling.

### Issue #69 — `<` was the ONE comparison with no hint — `16203dbe`

**Why**: asked whether #69 is closable. **Verified** both halves fixed at HEAD:
the reader no longer swallows (`[filter [< _ 3] xs]` + `def c := 3` reads as TWO
forms; a real `<Int -> Int>` still mints `$angle-type`), and the driver no longer
silently drops (N6e-E5.2). **Surprise**: `unbound-op-hint-table` had `<=`, `>=`,
`>` but **not `<`** — excluded by a comment saying "bare `<` never reaches
elaboration (issue #69(a) reader tokenization)". True when written; **#69(a)'s own
fix is what made it false.** So the token the issue is NAMED AFTER was the only
comparison answering with a bare "Unbound variable". Nothing failed, because
nothing was watching. **Also**: surfaced a proxy assertion in test-mixfix-02
(string-matching `"Unbound"` as a stand-in for the error type) — now asserts the
type, which is immune to message evolution.

### The "structural ceiling" was a MISSING PASS — `41697413`

**Why**: the one fact that decides unification — can the tree front-end get
whole-file expansion context? **Surprise**: it already HAS it. Running the two
expanders back to back with registries populated (exactly the tree spine's state)
showed `-single` doing the spec injection fine and merely leaving `where (-> A (->
A A))` **un-discharged**. `maybe-inject-where` is called twice in `-all` and was
called **zero** times in `-single`. **Fix**: mirror the pass — 26 → 14
divergences, 98 % → 99 %, the whole `$Dict-A` class gone. **This refutes my own
diagnosis twice over** (first "gensym noise", then "structural ceiling"), and I
had written the second into three documents as fact. **Invisible** because the two
expanders feed different spines and the tree spine is not admitted — it would have
detonated on commissioning, attributed to the wrong subsystem. Gates: suite
9819/482/0, corpus **zero diff**, errors 359 = 359.

### Reframed the decision around UNIFICATION; corrected the ceiling — `e2337e88`

**Why**: I had asserted the ceiling in the census header, DEFERRED and the dailies.
**Fix**: the census header now carries the correction plus its general form — *a
plausible explanation for a residual is not a measurement of it; run the
experiment*. DEFERRED item 1 is reframed from "commission vs retire" to
**unification** (one parser, two readers) now that the datum path is known not to
be a parser; item 3 re-points at the ACTUAL open question — the remaining 14 —
with an explicit warning not to assume they are structural, since that assumption
has now been wrong twice on this exact question.

### Guarded the `expand` command — my own fix gave it a raise path — `7e387472`

**Why**: an adversarial workflow re-derived the where-pass finding independently
and then caught what I missed. **Surprise**: `preparse-expand-single` can now
RAISE, and `driver.rkt`'s `(expand datum)` calls it unwrapped — before the fix it
could not reach the raise at all. **Fix**: guard the call site, NOT `-single` —
swallowing there would put it back out of step with `-all`. **Also**: my
"26 → 14" was measured wrong (an `--mode all` baseline vs a `--mode raw-datum`
result); true same-mode delta **23 → 14**, verified against a `git archive` pin.

### Settled the residual — 14 → 7 under process isolation — `43b5ed72`

**Why**: half the residual was suspected pollution. **Confirmed**: isolation
removes exactly 7 rows, ALL from `pairs-and-options.prologos`, which reports ZERO
alone. **Surprise**: isolation also ADDS 12 crashes, because batching lets earlier
files register what later ones need — so **neither run is "the truth" alone**;
isolated = CONFIRMED, batched-only = SUSPECT, crash-alone = UNMEASURABLE. Tool
committed rather than left in scratchpad.

### rewrite-implicit-map must precede flatten — 7 → 4 — `52e99e1f`

**Why**: one of the two cheap wins. **Surprise**: the rewrite DOES run downstream
(`preparse-expand-subforms`) but by then `flatten-ws-datum` has destroyed the
keyword grouping it keys on, so it silently no-ops. `form-cells.rkt` already had
this right and said why; this arm had drifted. **Also**: verified the read side —
the form-cell pipeline is write-only END TO END (0 reads of either map, 0 callers
of `form-cell-ref`, no consumer of `form-pipeline-value` outside its module).

### Which side does PPN build off — settled, and it inverts the risk — `899e4cac`

**Why**: the owner's concern that killing the wrong side would damage PPN 4C/4D.
**Surprise**: **PPN Track 3 Phase 7's own design says to DELETE the merge** — so
retirement is not a departure from the plan, it IS the plan, left half-done. And
the risk points the OTHER way: the form cells I had called "dead weight" are
Track 3's deliverable and the merge's intended replacement. **Ruling**: finish
the job.

### Phase 7's second half — the tree leg is GONE — `2d7813ef`

**Why**: the owner ruling. Deleted `tree-surfs` / `tree-by-line` / `merge-form` /
the admission gate / both lookups; kept the reader, the FORM-CELL block, and the
`consumed-form-residue?` filter (−195/+69 driver.rkt). The function stays —
"retire" meant REDUCE; it still returns the surf list `process-surfs` consumes.

**⭐ Surprise — the history was not what the handoff said, and it is worse in a
useful way.** The tracker row read `✅ … extract-surfs-from-form-cells produces
ALL surfs. No merge.` for four months and was wrong **twice**: (a) at `5d3b597c`
the cell pipeline was wired into `process-string-ws` ONLY — `process-file`, the
primary design target, never left the merge, so "No merge" was true of one of two
paths; (b) `19d9f8aea` (Rel T1 SC, 2026-07-20) then reverted the string path to
the merge as well, because cell-pipeline-only **dropped preparse-macro support**
(`solver`/`schema`/`defmacro` broke in REPL/LSP). That revert removed
`extract-surfs-from-form-cells`'s last caller — which is exactly WHY the form
cells are write-only. So Phase 7 was not merely "half-shipped"; its second half
was shipped, then **reverted for a real reason that is still unsolved**.

**Consequence for whoever finishes it**: the remaining step is NOT wiring. Making
the form cells READ means solving the preparse-macro problem that caused the
revert. Recorded at Track 3 §Phase 7 status + DEFERRED, so the next attempt does
not walk into the same wall.

**Also removed deliberately, not disabled**: issue #69(b)'s "recovery-first" half
— the unguarded `(or tree-match s)`, where the QTT bypass rode in. #69(b)'s other
half (error surfs not silently dropped) stands. DEFERRED 4 closed by removal.

**⭐ Unlocks step (B)**: `parse-form-tree` is now unreachable from production —
only its own recursion, its `module+ test`, `tools/spine-census.rkt`, and the
caller-less `extract-surfs-from-form-cells`. The ~1,971-line legacy family is
deletable as a standalone change (DEFERRED 6 unblocked).

**Gates**: corpus A/B on FULL OUTPUT, baseline `git archive`-pinned at `f0bce056`,
both legs on the SAME 163 inputs — errors **359 = 359**, aborts **26 = 26**, no
file changed its error count, and **ZERO differing lines after normalising
generated-name counters**. The only raw diff is gensym/meta numbering, because the
tree leg no longer allocates. Suite **9819 / 482 / 0**.

**Next**: (B) delete the legacy family — but decide `spine-census.rkt`'s legacy
mode and protect `extract-surfs-from-form-cells` (it is Phase 7's intended surf
source, not collateral).
