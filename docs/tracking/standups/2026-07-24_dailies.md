# Dailies — 2026-07-24

Rolled from `2026-07-19_dailies.md` (472 lines — at the ~400–500 soft cap, plus
a topic shift: the Rel T1 aspect arc closed and the work is now the SUB spin-out
close + the POL roster). Prior file is read-only history; its LOG carries the
A/B/C/D/B3/POL/SUB entries. STATE head below is SEEDED (copy-forward, per
`DAILIES_METHODOLOGY.org`) and refreshed to current — the prior head had drifted
into accumulation (multiple "Where we are" strata), which the methodology names
as an anti-pattern; this one is a single current projection.

---

## 📍 STATE  (overwrite in place — always current; per `DAILIES_METHODOLOGY.org`)

- **HEAD** `2e8fc846` · **Suite** GREEN **470 files / 9019 / 0** · **Working tree**: owner WIP only (modified standups/examples, deleted `MASTER_ROADMAP.md` + `LANGUAGE_VISION.md`, untracked LATTICE_/LAVAMOAT_/pldi/qauntale_outputs/quantale-research) — stage ONLY my files, NO Co-Authored-By, **NEVER `git stash`** (two owner stashes + owner WIP).
- **Where we are**: **Rel Track 1**, POL roster + the SUB spin-out. Aspects **A · B · C · D ✅**; **B3** 🔄 (B3.0 ✅ `67d96a0d`, B3.1 ✅ `0d34fa7e`, **B3.2 open**); **POL** 🔄 (.2/.4/.5/.10 ✅, **.6 .3 .7 .8 .9 open**, .1 ruled doc-only); **SUB spin-out** ✅ through the fix (R ✅ (D) · SUB.1 `f19d6f56` · SUB.2 ADOPTED `6323587e` · SUB.3a `7ea49168` · SUB.3b `036b59f7` · hot-scan `8ec5e507`), **SUB.close open**.
- **NEXT**: **SUB.close** (doc-truth on the defect doc §2 · lesson promotions · quiet-window suite re-confirm) → **POL.6** (fused `x:Int` in `defn` params, the C.b.2 last mile) → B3.2 → POL.3 (+ the POL.1 bag-semantics doc line) → POL.7 → POL.8 → POL.9 co-design → **X.close (Stage-5 PIR gates the track ✅)**.
- **Open / blockers**: none blocking. Named-and-owned deferrals: A.2b/A.4 DFS-routing scaffolds + Aspect-B on-network row computation → **BSP-LE Track 3**; C.d runtime `?x:Int` → **UCS Track 6**; Q_A–Q_D + R2 + `distinct` + recipe-style def liveness → **Rel T2 "The Fact Store"**; `[head rows].field` polymorphic-head inference → **CIU T6 Path Selection** (cross-reference, don't fix here). POL.9 needs owner co-design before implementation (grammar ambiguity vs function application).
- **Watching**:
  1. **Suite test-count is NONDETERMINISTIC** (9003↔9019 run-to-run, data-driven case gen) — the gate is **0 failures across 470 files**, never the count.
  2. **Missing-arm walker disease — the strongest promotion candidate (7+ instances, ready NOW).** Every hand-armed AST walker in the pipeline has silently skipped node kinds: `shift`/`subst` (champ as closed leaf), `nf` (skipped champ while descending its rrb sibling 10 lines later), `narrow-subst-bvars` (`[_ expr]` catch-all), zonk ×3, `occurs?`, `uses-bvar0?`, `conv-nf`. The failure mode is always SILENT (wrong answer, 0 errors). The **structural** answer now has 3 uses in-tree: generic transparent-struct rebuild (`struct-info` + `struct-type-make-constructor`) as the fallback, plus — when arms are needed for perf — the reflective twin retained as a **differential oracle** with poison planted in every armed field position. Promote to `pipeline.md` (which today carries only the checklist discipline) at SUB.close.
  3. **Bench-methodology trio (3 data points in one session)**: (a) workload-poisoned synthetic (bignum blowup — caught via `sample` showing `big_add_pos`); (b) `touch FILE && raco make` does NOT recompile (SHA short-circuit → a false negative that nearly killed the compile-limit win) — already codified in `testing.md`; (c) ambient drift invalidates sequential wall-clock A/B (re-confirmed; interleaved same-process micro is the sanctioned instrument). Promote (a)+(c) alongside (b) at SUB.close.
  4. **Un-arm'd node → spurious "Multiplicity violation" — 3rd data point** (F1a.2 `def m0 := {}`; POL.5 `def := solve(…)`; `def := [validate …]`, found by the SUB.1 probe and still live). Promotion due at X.close; the third instance is also an open POL-adjacent defect.
  5. **Premise-refutation cascade at 9** (from the prior file) + a new entry earned this session: **"a green full suite gates correctness"** — the suite was green 470/0 with a live silent-wrong-answer bug; only a failing-test-first discipline caught it. Always probe/instrument before locking a premise.
  6. **"Reduction DEPTH is the axis; check whnf before inventing taxonomy"** (2 data points: the eager-`nf` failure, the whnf success). Promote on a third.

---

## LOG  (append per commit / decision / surprise — newest at the BOTTOM)

### New working-day interval opened — standup + dailies pair  (2026-07-24, docs commit)

- **Why**: the prior pair dates to 2026-07-19 and its dailies hit the roll trigger on both counts (472 lines; topic shift from the aspect arc to the SUB close + POL remainder).
- **Carried forward**: the STATE head above, compacted to a single current projection; the six Watching entries with their data-point counts (two of them — the walker disease and the bench trio — are at promotion threshold and are scheduled into SUB.close, which is the next slice).
- **Next**: SUB.close, then POL.6 per the Stage-4 per-phase protocol.
