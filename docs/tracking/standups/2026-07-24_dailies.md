# Dailies — 2026-07-24

Rolled from `2026-07-19_dailies.md` (472 lines — at the ~400–500 soft cap, plus
a topic shift: the Rel T1 aspect arc closed and the work is now the SUB spin-out
close + the POL roster). Prior file is read-only history; its LOG carries the
A/B/C/D/B3/POL/SUB entries. STATE head below is SEEDED (copy-forward, per
`DAILIES_METHODOLOGY.org`) and refreshed to current — the prior head had drifted
into accumulation (multiple "Where we are" strata), which the methodology names
as an anti-pattern; this one is a single current projection.

---

## 📍 STATE  (overwrite in place — always current; per `DAILIES_METHODOLOGY.org`)

- **HEAD** `3386c89a` (X.close **Batch B** docs; code `b8e01255` Q_N1) · **Suite** GREEN **471 files / 9144 / 0** · **Working tree**: owner WIP only (modified standups/examples, deleted `MASTER_ROADMAP.md` + `LANGUAGE_VISION.md`, untracked LATTICE_/LAVAMOAT_/pldi/qauntale_outputs/quantale-research/TRACK8_DESIGN/CRITIQUE_METHODOLOGY.md) — stage ONLY my files, **verify with `git diff --cached --name-status`** (a `^[AM]` grep hides D-rows and I committed two owner deletions once — reverted `2c4609f6`), NO Co-Authored-By, **NEVER `git stash`** (two owner stashes + owner WIP).
- **Where we are**: **Rel Track 1 is ✅ COMPLETE** — all aspects (A·B·C·D·B3), the POL roster (.1–.10), the SUB spin-out (BVAR half), **X.close shipped incl. the Stage-5 PIR** (`docs/tracking/2026-07-25_REL_T1_PIR.md`), and the PIR's own follow-up **Batch A** (test gates) + **Batch B** (three rulings). Roadmap + Rel Master flipped ✅.
- **NEXT**: **Batch C+D** — (C) fix the LIVE `def := [validate …]` spurious-multiplicity instance (3rd of its class, same one-arm shape as POL.5); (D) the overdue lesson promotions to `DEVELOPMENT_LESSONS.org`: **"user scope challenges improve designs"** (6 instances, flagged READY 2026-04-03, never done), **"mini-audit refutes the design's own premise"**, the **un-arm'd-node → spurious-multiplicity class** (3 instances), and a "design time is invisible to git" note in `POST_IMPLEMENTATION_REVIEW.org`. Then **Group E** stays properly deferred: **PM Track 12/12B** (the architectural answer to the 7-instance two-context class + POL.9 Q_D slice 2, captured at 12B §11), the **SUB META half**, the **POL.9b def-seam gaps**, `grammar.ebnf` (ride with UCS T6).
- **Open / blockers**: none blocking. Owner-sequenced next track is open: **Rel T2 "The Fact Store"** · **CIU T6 Path Selection** (unblocked — typed rows landed) · back to the **DEMO** through-line. Named-and-owned deferrals unchanged: BSP-LE T3 (A.2b/A.4 scaffolds + on-network row computation) · UCS T6 (C.d, + `grammar.ebnf`) · Rel T2 (Q_A–Q_D, R2, `distinct`) · CIU T6 Path Selection (`[head rows].field`).
- **Watching**:
  1. **Suite test-count is NONDETERMINISTIC** — the gate is **0 failures across 471 files**, never the count.
  2. ~~Missing-arm walker disease~~ — **PROMOTED** to `pipeline.md` § "Exhaustive Walkers" (`32f0d250`).
  3. ~~Bench-methodology trio~~ — **PROMOTED** to `testing.md` (`32f0d250`).
  4. **Un-arm'd node → spurious "Multiplicity violation" — 3 instances, one STILL LIVE** (`def := [validate …]`). **This is Batch C** — fix the instance AND promote the class; the structural direction is `pipeline.md`'s Exhaustive-Walkers answer applied to `inferQ` (a missing arm should degrade to imprecision, not a false failure).
  5. **Premise-refutation cascade — the COUNT was broken** (four inconsistent authoritative values). **Q_N3 defined the boundary** in `POST_IMPLEMENTATION_REVIEW.org` q13: recorded-beforehand + shown-false-by-evidence + changed-the-work = refutation; everything else is a surprise, uncounted. **State it ONCE at close with the enumeration.** Expect it to shrink (~11, not ~29–32).
  6. **"Reduction DEPTH is the axis; check whnf before inventing taxonomy"** (2 data points). Promote on a third.
  7. **NEW — "the pattern already existed; we didn't reach for it."** Batch A's acceptance gate had **four** prior instances in the immediately preceding track. The gap was discipline, not tooling. Watch for recurrence; if it appears again, the response is a checklist item in the acceptance-file methodology, not a new mechanism.
  8. **NEW — probing before ruling changes the RULING, not just the facts.** Q_N1's three keywords looked symmetric and were not (`is` errored / `not` was silently useless / only `=` had working behavior to lose). Cost arguments made without probing are shaped by the framing, not the evidence.

---

## LOG  (append per commit / decision / surprise — newest at the BOTTOM)

### New working-day interval opened — standup + dailies pair  (2026-07-24, docs commit)

- **Why**: the prior pair dates to 2026-07-19 and its dailies hit the roll trigger on both counts (472 lines; topic shift from the aspect arc to the SUB close + POL remainder).
- **Carried forward**: the STATE head above, compacted to a single current projection; the six Watching entries with their data-point counts (two of them — the walker disease and the bench trio — are at promotion threshold and are scheduled into SUB.close, which is the next slice).
- **Next**: SUB.close, then POL.6 per the Stage-4 per-phase protocol.

### SUB.close — doc-truth, two lesson promotions, and a RETRACTED perf claim  (2026-07-25, docs commit)

The spin-out's close. Three deliverables plus one correction I did not expect to be writing.

- **Doc-truth (defect doc §2.0)**: the §2 traversal table described pre-fix HEAD; it now carries a per-row post-fix reading. The load-bearing distinction: the fix did NOT add champ arms — under ruling (D) it made the *existing* skips **correct** by removing the only thing that made them wrong (`nf` no longer constructs an open container). **New finding, named not fixed: the META half is still open.** `zonk` ×3 + `occurs?` skip on *metas*, which NbE says nothing about; post-fix reachability is **UNVERIFIED** (one surface probe typed/displayed correctly — that shows only that route doesn't reach them). `occurs?` is the higher-stakes one (unsound occur-check ⇒ cyclic solutions). Recorded in DEFERRED.md as its own slice with the probe recipe.
- **Promotions landed**: (1) **`pipeline.md` § "Exhaustive Walkers"** — the missing-arm disease (7+ in-tree instances, all silent) + the 3-step structural answer (generic transparent-struct rebuild as fallback → binder inventory from `shift` → differential oracle when arms are needed for perf) + the red-flag list. This is the pattern the whole SUB arc kept re-deriving. (2) **`testing.md`** — bench workload-validity (the bignum-poisoned synthetic, caught by `sample`, not by reading the generator) + the ambient/interleaved re-confirmation + "never compile while a timing suite is in flight."
- **⚠ RETRACTION (§4.3)**: I claimed the hot scan would cut "~8-11 s → ~1-2 s" of suite time. That was an extrapolation from one micro and **the data does not support it**. The quiet-window pair came back 198.6/204.2 s — *slower* than the pre-scan memo runs (184.7/189.4). I tested the obvious mechanism (armed walk pessimizing un-armed-heavy terms, which pay arm dispatch before the fallback) and **refuted it**: armed wins both shapes (6.87× armed-heavy, **1.57× un-armed-heavy**). So the scan cannot be the cause — the suite figure is **ambient**: identical code varied 5.6–8.5 s run-to-run, and the evening drifted monotonically 173 → 185 → 204 across ~6 h of continuous suite running. **Consequence**: the scan is justified by the micro and by being semantically free, NOT by a suite number; and the **SUB.3a "+6.4 %" figure inherits the same caveat** — re-measure from a cold machine at X.close.
- **Why this matters more than the number**: the failure mode here is exactly the one `testing.md` warns about, and I walked into it by reporting a wall delta measured hours apart as if it were attributable. The interleaved micro was right the whole time; the suite wall was never the instrument for a change this size.
- **Next**: POL.6 (fused `x:Int` in `defn` params — the C.b.2 last mile), Stage-4 per-phase protocol.

### Rel T1 POL.6 FIXED — fused `x:Int` in defn params (`0f6dc98c`)  (2026-07-25, code commit)

The owner's last standup repro. `defn my-square [x:Int] : Int` → `Int -> Int`, computes.

- **Root, instrumented not inferred** (the standing Watching-2b lesson earned its keep again): a temporary `eprintf` on the real `process-file` path showed WS delivers the param list as the TWO elements `x` + `:Int` — *the exact shape C.b.2 already handles* — but a `defn`'s params never route through `parse-binder`. `parse-defn-bare-params` mapped `binder-info` over the RAW elements, so the function silently became **2-ary with a parameter named `:Int`**, both hole-typed. The owner-visible "cannot infer the type of an unannotated parameter" was the *downstream symptom of a silently wrong arity* — the arity-lenience family again, in a third guise (after the solve cases POL.4 fixed).
- **Fix shape (DRY over duplicate-and-drift)**: extracted THREE shared fused primitives (`fused-type-annot?` / `fused-annot->type-surf` / `split-fused-symbol`) and rerouted C.b.2's three `parse-binder` arms through them, then folded the defn param list through the same. One implementation serves both paths — the alternative (a second fused parser in the defn path) is precisely the `recognize-keyword`-vs-`ident-continue?` drift class `prologos-syntax.md` warns about.
- **Ordering bug caught in my own first draft**: the chained-reject arm has to precede the 2-element fused arm, or `x:Int:Even` consumes the first annotation and treats the tail as a parameter. Also fixed a no-op `when` error-propagation in the same pass.
- **Bonus capability, verified not assumed**: MIXED fused+bare (`[a:Int b]`) now works wherever bidirectional inference can supply the bare param (`[a:Int b] : Int` returning `b` infers `b : Int`) — **strictly more capable than the spaced form**, which hard-errors on mixed. I checked before deciding whether to reject mixed; it works, so it stays. Mixed+arithmetic still hits the pre-existing unannotated-param inference limit (identical to all-bare params; same family as the DEFERRED F-row item).
- **Test-infra note**: two of my new tests initially errored because `cb1-run-ws` returns raw error STRUCTS for failing programs — the existing `cc-result-strings` helper is the converter. Also hit a runner false alarm: a lone 40 s test file trips the "DEAD WORKERS — no results after 30 s" heuristic; paired with any second file it reports normally (verified both ways, plus a clean direct `require`). Worth remembering before diagnosing a phantom crash.
- **Gates**: targeted 60/0; acceptance 0 errors; suite **470 / 9025 / 0**. +9 tests in the C-arc file.
- **Next**: B3.2 (coinductive display refinement) → POL.3 (+ the POL.1 doc line) → POL.7 → POL.8 → POL.9 co-design → X.close.

### Rel T1 B3.2 LANDED — display-time coinductive refinement; **B3 COMPLETE** (`df428f7e`)  (2026-07-25, code commit)

The owner's headline aspect is now whole: B3.1 gave rule solves static row types; B3.2 adds the coinductive half at the display seam.

- **What it does**: **SHARPEN** — `solve (mixed 1 v)` on a heterogeneous fact column echoes `{:v String}` instead of `{:v String | Int}` (exact for THIS result set), while a genuinely heterogeneous result keeps the union. **FILL** — a hole field takes the observed type of the values present.
- **The division is visible and test-pinned**: `def r := solve (mixed 1 v)` announces `{:v String | Int}` (the STORED static type, which governs composition) while echoing `r` shows `{:v String}`. Display-only, at the eval arm only — deliberately not on def/defr, whose announced type is the type they store.
- **Surprise (a real regression, caught by the suite not by inspection)**: my first cut matched ANY bare record, so an ordinary **dyn-tailed** record value got its holes re-filled — `{:a 1, :b 2} : {:a _ :b _ | _}` became `{:a Int :b Int | _}`, breaking `test-f1b3-width-acceptance`. Those holes are F1b.3's *deliberate* degrade (a dyn tail means the field set is unknown), so refilling them re-asserts knowledge the type system just discarded. Fix = the **CLOSED-tail gate**, which is also the principled scope: D-B3.6 says solve rows are always closed. Named cost: explain rows are 'dyn-tailed, so they aren't refined.
- **Honest scope finding**: **FILL has no reachable surface case today.** B3.1's walker is thorough enough that a hole field almost always means "nothing binds this var" — whose runtime value is an unbound echo, so the hole correctly stays. The one shape that would reach it is blocked by an adjacent defect (below). The FILL path is unit-pinned so it's correct the day that lands — rather than shipping an untested branch or pretending the case exists.
- **Adjacent PRE-EXISTING defect surfaced + deferred, not chased**: unifying a relational var with a **collection literal** yields the runtime value `unknown` while the static type derives correctly — `'[{:m unknown, :x 1}] : [List {:m {:a Int} :x Int}]`. That is the one place the static row type and the actual row provably DISAGREE (the CbC agreement B3.0 protected), and it's adjacent to the DEMO's `:from` record loading. DEFERRED.md entry with the probe.
- **Design/API note**: observation reuses B2's `observe-column-type` kernel, and uses plain `infer` — which RETURNS `expr-error` for an unobservable value, so the skip is structural with **no `with-handlers`** (the VAG red flag avoided by construction). A refinable?-field gate runs first, so fully-concrete row types cost one shallow scan and no row walk.
- **Gates**: targeted 30/0; acceptance 0 errors; suite **470 / 9040 / 0**. +11 tests (5 e2e, 5 unit + the concrete-untouched gate).
- **Next**: POL.3 (declaration-order keys) + the POL.1 bag-semantics doc line — they share the row-assembly seam → POL.7 → POL.8 → POL.9 co-design → X.close.

### Rel T1 POL.3 FIXED — declaration-order keys for solve echoes (`b0e3da9c`)  (2026-07-25, code commit)

- **Realization**: display-only at the eval echo seam (beside B3.2's refiner) — rows are champs, hash-ordered BY CONSTRUCTION, and the declaration order lives in the goal's positional query vars, which the echo seam still holds. Goal-apps via the B0 kernel (minus POL.2 anons); anonymous `rel` via param order (at the AST stage that's C.b.1's raw 3-list, not param-info — both accepted after a probe caught the contract violation); explain metadata keys follow the query keys. The renderer only handles shapes it FULLY recognizes and falls back to pp-expr otherwise — the echo must never drop or reshape an element.
- **Anti-alphabetical pin**: `defr za [?z ?a]` → `{:z 1, :a 2}` — declaration order, provably not sorting.
- **Caught at the suite gate, fixed before commit**: the first cut rendered EMPTY results as `'[]` where they have always displayed `nil` — three e2e pins (negation-ground-fails, empty-facts, ground-query-miss) flagged it; fix = fall back on zero rows. Plus one progress flip: the F1 records acceptance marker ;;84 pinned the old hash order.
- **Named limitation**: `def r := solve (…)` then `r` echoes through an fvar — no goal in hand → hash order (test-pinned as the fallback). Carrying order IN the row is Rel T2 representation territory. The row TYPE stays canonically sorted (the expr-Record field invariant is load-bearing for unification — untouched).
- **Gates**: targeted 150/0 (4 files); acceptance 0 errors; suite **470 / 9046 / 0**. +6 tests.
- **Next**: POL.7 (single-line `|` facts) → POL.8 (implicit rule-clause groups) → POL.9 co-design → X.close (which also carries the POL.1 bag-semantics doc line).

### POL.9 co-design SETTLED — the paren-goal design (owner + Claude, prose round)  (2026-07-25, decision entry)

The owner proposed the alternative that won: **parens make goals** — `(foo x)` = implicit solve, `foo x`/`[foo x]` = application — superseding the seed's registry-lookup idea.

- **The argument that settled it**: free-ordering REQUIRES syntactic category-decidability. Registry-lookup makes the phrase's CATEGORY pend on name resolution (un-typeable, retroactively re-categorized — incompatible with the free-ordering north star); parens keep category static so only the BINDING residuates — the residuation pattern the driver's demand loop already implements for forward-referenced defs. The owner's residuate-until-defined instinct for (a) was right and needed exactly this to be well-posed.
- **Rulings**: Q_A paren-goals adopted · Q_B defn/defr namespaces DISJOINT at registration (second registration errors, pointing at the first — kills the silent-wrong-reading hazard, keeps Curry-style unification open) · Q_C scope = top level + def RHS, NOT defn bodies (function purity vs fact-store dependence = its own future design round) · Q_D unknown-relation error first, demand-loop retry as fast-follow · Q_E conjunction-by-juxtaposition parked for the POL.8 grammar round.
- **Named costs, accepted eyes-open**: the institutionalized WS-vs-sexp divergence on `(f x)` (the Lisp-muscle-memory hazard — watch in sexp-mode test authoring) + one character of semantic weight (echo self-corrects).
- **Also noted**: it completes the existing `()` delimiter reservation rather than adding a rule; the whole B/POL machinery (typed rows, decl-order keys, POL.4 diagnostics, POL.10 def snapshots) lights up under the sugar.
- **Next**: POL.7 (single-line `|` facts — WS-Impact first) → POL.8 → POL.9 implementation → X.close.

### Rel T1 POL.7 FIXED — single-line `|` facts (`f55988cc`)  (2026-07-25, code commit)

The owner's digits example works verbatim. Two premises REFUTED by the opening probes (the premise-refutation cascade earns entries 10+11):

- **Watching-3 is STALE for exact multiples**: arity-chunking already existed in `parse-defr-body` (`|| 5 3` on unary = two correct rows). The REAL trap was the **partial remainder** — 5 terms at arity 2 silently registered a dead row no query could match (the true source of "spurious empty results"). Now a loud error naming counts + the `|` remedy.
- **`|` was not inert**: `$pipe` flowed into `parse-datum` as a garbage TERM — `|| 0 | 1 | 2` silently yielded five rows with interleaved `unknown`s. Now: pipes = explicit rows, exact arity per segment, empty segments error, continuation lines supported; ONE shared splitter for both content sites (no drift twin).
- **Bonus pre-existing fix**: WS-mode defr body errors returned as `(list err)` slipped the callers' bare `prologos-error?` check → the ugly "Cannot elaborate: #(struct…)" wrap. Sexp arms already returned bare; aligned. Every defr body diagnostic now displays cleanly and the file continues (per-command containment verified).
- **Also**: my binary probe's `pair` relation name collided with the `pair` builtin and echoed unevaluated — a probe-authoring trap worth remembering (builtin names in probes), not a code defect.
- **Gates**: targeted 61/0; acceptance 0 errors; suite **470 / 9054 / 0**. +8 tests.
- **Next**: POL.8 (implicit rule-clause groups — layout-based parenless goals in defr bodies; interacts with the settled POL.9 grammar) → POL.9 impl → X.close.

### Handoff written for the POL.8 → POL.9-impl → X.close session  (2026-07-25, docs commit)

- **Artifact**: `docs/tracking/handoffs/2026-07-25_REL_T1_POL8_handoff.md`, §1–§6 per `HANDOFF_PROTOCOL.org`. Owner will seed a fresh context from its Relay Note.
- **Owner ask honored**: the hot-load list points at **BOTH dailies** — `2026-07-19` (the aspect arc A/B/C/D/B3 + early POL, read-only history, carrying the *reasoning* behind every settled decision) and `2026-07-24` (SUB + POL remainder + the POL.9 co-design) — so the next session gets the full scope and the lessons, not just the resume point.
- **Also corrected while writing it**: the STATE head's NEXT line had gone stale (it still listed POL.3/POL.7 as upcoming, and called POL.9 co-design pending) — the head is the relay-note seed, so a stale head propagates into the next session. Fixed to POL.8 → POL.9 impl → X.close.
- **§4 carries 12 surprises**, the highest-value being: a green suite is not a correctness gate for walker bugs; the suite wall could not resolve a ~5 s change (and a perf claim was retracted on that basis); `touch` ≠ recompile; instrument the parse rather than infer reader shape; and POL.7's refutation of a standing Watching entry.
- **Next session's first move**: POL.8 (implicit rule-clause groups) with its WS-Impact analysis, then POL.9 implementation (design settled), then X.close with the Stage-5 PIR that gates the track ✅.

### Rel T1 POL.8 FIXED — implicit rule-clause groups, layout-based parenless goals (`1c3c8848`)  (2026-07-25, code commit)

All three owner standup forms work verbatim; both spellings stay legal. The co-design round (Q1 same-column siblings · Q2 head-token rule · Q2a diagnostic · Q5 lenient-parens/loud-bare · Q6 flat-arm uniformity · Q_E RULED OUT at top level — a bare conjunctive query has no projection list, `rel [vars] &> …` is the spelling) and the implementation landed in one session.

- **The load-bearing surprise (grounding `wf_69f4dc4c-137` + a live eprintf probe)**: the "WS flattens defr bodies, no column info" premise — the POL.6/POL.7-era working model — is **REFUTED**. Per-token line/column SURVIVES to `parse-defr-body`, and the reader's indent grouping already builds most of the target structure: the owner's nested-`not` form arrived correctly grouped as `(not (= c 4))` BEFORE any change. Only the sibling-vs-deeper cut for lines hanging directly off the `&>` line is srcloc-only (identical datum, C6-vs-C9 columns). The implementation is therefore a parser-level structural grammar + column arithmetic at exactly that one boundary — no reader changes, no session-Offer risk.
- **The srcloc cliff, detected not suffered**: a preparse rewrite anywhere inside a defr (e.g. dot-access in a goal argument) strips ALL inner srclocs (macros.rkt defr arm's `equal?` guard — probe-confirmed, the audit's one inferred link). Detection is clean because a real `&>` can never sit at column 0: degraded + all-paren parses exactly as before; degraded + parenless gets a guiding error instead of a silent mis-grouping. The L2 `process-string-ws` double-parse (tree-spine surf discarded by the merge — defr has no `same-form-type?` arm) arrives fully stripped and is harmless; named as a FUTURE-TRAP in §8 (adding a defr arm to the merge would flip the L2 winner to the stripped surf).
- **A design ruling met an implementation impossibility, resolved honestly**: Q5 asked "lenient on paren groups; loud on bare column misalignment" — but a paren goal and a bare ≥2-token line are INDISTINGUISHABLE post-reader (same `wrap-stx-list`, no origin sentinel — C11). So loud-on-misalignment lands on single-token lines, and a misaligned multi-token bare line parses as the sibling it was meant to be (the only sensible reading). Named cost: a multi-token continuation of the `&>`-line goal is not expressible — parenthesize (single-token continuations work, and deeper lines under a goal on its OWN line nest structurally at the reader for free).
- **Bonus from the census**: the bare form today was NOT loud-broken as assumed — it silently registers garbage (`goal-desc 'app 'unknown`) that errors only at first solve; a never-queried relation carries it with 0 errors. Post-POL.8 every mis-shape is loud at registration (mis-indent/Q2a/bad-head/degraded diagnostics with real columns). The flat arm also gained clause-splitting on further `$clause-sep` tokens (previously a silent garbage goal) — sexp multi-clause single-line defr now works.
- **Gates**: targeted 6-file battery 145/0 pre-tests; grown `test-rel-t1-pol.rkt` 58/0 (+16, incl. L2-path pins); acceptance ;;29–34 added, 0 errors; **full suite 470 / 9070 / 0**.
- **Next**: POL.9 implementation (Phase 0 census) → X.close.

### Rel T1 POL.9a LANDED — implicit solve, the top-level leg (`ddf29351`)  (2026-07-25, code commit)

`(fruit-not-of-color f "red")` ≡ `solve (…)` at top level; the composed owner example — `(rel [fruit]` + POL.8 parenless clauses — returns the right rows. Sub-phased 9a (top-level) / 9b (def-RHS) / 9c (Q_B gates) after the grounding audit showed the def leg is a two-pipeline problem of its own.

- **Phase-0 census REFUTED the design's additivity expectation for one head class**: `(dbl 3)` was live, working paren-application in WS (6 : Int) — so the flip to the guiding diagnostic is the named cost (1) *realized*, not avoided. Census: relation + unknown heads errored before (additive); the corpus's only non-keyword-head paren command is a def-RHS ctor line (9b); the 9082/0 suite empirically closed the untestable census hole (no WS fixture relied on paren-application). Also caught by the audit's critic: my census called `type-of` a preparse macro — it has NO handler anywhere (track10b's lines are already broken); a census-classification defect worth remembering.
- **Mechanism**: a NEW syntax property at the reader's lparen wrap (native sexp reader never attaches it ⇒ sexp application untouched BY CONSTRUCTION — the institutionalized divergence, now test-pinned); preparse's bare-form rebuild made property-preserving (4-arg `datum->syntax` — one arg); `parse-toplevel-datum` at exactly the five driver command-map sites (Q_C scope structurally — nested parse-datum untouched); the produced surf is byte-identical to explicit `solve`, so B1/B2/B3 typed rows, POL.3 key order, POL.4 arity, POL.10 snapshots all light up under the sugar unmodified.
- **The POL.8×POL.9 collision, found by probe**: the settled anonymous-query spelling `(rel [x] &> …)` PARENS the body — and parens suspend indent grouping, so POL.8's parenless clauses arrived flat and collapsed into one 6-ary garbage goal. Fix = `regroup-flat-lines-by-layout`: reconstruct the reader's per-line nested shape from token srclocs and feed the ONE shared clause grammar. Bonus: multi-clause anonymous rel inside parens is now a working DISJUNCTION (a second flat `$clause-sep` previously parsed as a silent garbage goal).
- **Solve-seam classification with an honest wrinkle**: all five unknown-relation sites (incl. the plain-`error` propagator site that CRASHED files under `:atms` routing — now converted) classify the head: Pi-typed → "foo is a function — application is written [foo …]"; other bound values → guiding wording; and a case my first wording got WRONG — a gate-rejected defr leaves its name env-bound (the env write precedes the A.3/C.c gates), so "bound as a value" misled; detected via the stored `expr-defr` value → "its defr failed to register (see the earlier error)". Surfaced by punify-p3's pre-existing floundering rejections (4 errors there are A.3-era, only reworded).
- **Test-pollution trap (fixture lesson)**: a sexp-mode `spec dbl` in one test leaked through the shared prelude spec registry into a LATER test's WS `defn dbl [x:Int]` ("has both a spec and inline annotations") — cross-test name hygiene matters even with per-call forked networks.
- **Gates**: targeted 7-file battery 174/0; POL file 70/0 (+12); acceptance ;;35–36 (corrected at X.close — 37/38 never existed), 0 errors; **full suite 470 / 9082 / 0**.
- **Next**: POL.9b (def-RHS, two-pipeline + the ctor line fix) → POL.9c (Q_B local-only gates, xor/singleton canaries) → X.close.

### Rel T1 POL.9b LANDED — the def-RHS leg (`7ce02760`)  (2026-07-25, code commit)

`def r := (fruit-not-of-color f "red")` binds the rows; `def r := (rel [f] &> parenless…)` keeps full POL.8 layout inside the RHS. The := spelling is byte-equivalent to explicit `def := solve (…)` (test-pinned).

- **The two-pipeline problem, solved with a category-disagreement merge preference**: the tree def surf wins on L3 (audit C9) and the tree spine can't recover srclocs/properties (no source text on the legacy path — `pos->line-col` on "" would crash). Instead of a tree leg: the preparse := rewrite carries the RHS ELEMENT's stx through (splice-if-unchanged — inner layout survives — else a 4-arg same-headed rebuild), and merge-form prefers the preparse surf EXACTLY when preparse parsed the body as solve while the tree parsed it as application. Explicit solve parses as solve on both spines → merge unchanged.
- **The gate caught a real over-reach, and the fix sharpened the design**: my first cut dispatched parse-def on any marked RHS — the suite failed on transducer/pipe-compose fixtures that write sexp-style paren-defs INSIDE WS files (`(def x : T (cons …))`). That's the IR spelling embedded in WS — application semantics are the institutionalized reading. Fix: the := rewrite STAMPS the RHS element (`'prologos-defrhs-command`); parse-def dispatches only on stamped elements. The := surface is what makes the RHS command position — now structurally, not by discipline. 5 cases healed, every := test green.
- **Parity, probed both ways**: the def-seam failures for bad heads ("Expression is not a valid type" — typing dies before the guiding runtime diagnostic can fire) and row-type def annotations are PRE-EXISTING and identical under the explicit solve spelling. Pinned with a message-equality test; both → DEFERRED at X.close.
- **Corpus**: punify-p3's `def matrix := (cons …)` → `[cons …]` (the convention's own spelling); the file keeps exactly its 4 pre-existing A.3 errors and matrix binds `List [List Int]`.
- **Gates**: targeted 8-file battery 247/0; POL file 77/0 (+7); acceptance ;;39–40, 0 errors; **full suite 470 / 9089 / 0**.
- **Next**: POL.9c (Q_B local-only registration gates) → X.close.

### Rel T1 POL.9c LANDED — Q_B disjoint namespaces; **the POL roster CLOSES** (`08c3f187`)  (2026-07-25, code commit)

- **The gate**: cross-kind re-registration errors pointing at the first (`defr area` over `defn area`; `def speed :=` over `defr speed`; multi-arity `defn` base over a prior defr). Same-kind stays legal (re-defr, LWW def-over-def, REPL iteration).
- **Local-only fell out of the architecture**: `module-network-lookup-status` already checks the module's own cell-id-map before cascading imports — the gate is just its FIRST ARM factored out (`module-network-lookup-local`). Refer-imported names are invisible to it BY CONSTRUCTION, so the prelude `xor`/`singleton` shadowing precedent stays legal without any special-casing (canary-tested).
- **The 9a finding became load-bearing**: a defr name's env value IS its expr-defr body — that's the kind discriminator, no new registry needed.
- **Named gap, eyes open**: the ambient multi-defn registry has no module provenance, so defr-AGAINST-a-prior-multi-defn is ungated (gating it would break prelude-name shadowing — `nth` etc. are prelude multi-defns); the reverse direction IS gated at process-def-group.
- **Gates**: POL file 84/0 (+7); acceptance 0 errors; **full suite 470 / 9096 / 0**.
- **Next**: **X.close** — the roster is done; bench matrix (COLD machine) · DEFERRED triage · doc-truth · memory fold · the Stage-5 PIR that flips Rel T1 ✅.

### Rel T1 X.close COMPLETE — bench matrix, adversarial gap audit, and the PIR; **the track flips ✅**  (2026-07-25)

The closing phase, and the Stage-5 PIR that gates the roadmap tick: `docs/tracking/2026-07-25_REL_T1_PIR.md`.

- **Bench matrix, three tiers, baselines worktree-pinned** (never `git stash` here). **Tier 1 (deterministic counters — the primary instrument, ambient-immune)** gave the two cleanest results of the arc: the **POL syntax layer is FREE** (POL spelling vs the byte-equivalent classic spelling at HEAD → *every counter identical*), and **typed rows cost NOTHING at the solver layer** (backtracks 13→13, unifies 84→84, cell_allocs 552→552; the whole cost is typing, +120% infer_steps). The NAF fix's +56% backtracks is the only solver-layer increase and it is the price of correctness — the baseline was cheap and *wrong* (8 rows where 4 are correct). **Tier 2 (interleaved wall)**: the track is net **−2.3% / −1.8%** on the two relational workloads despite that. **Tier 3**: the SUB.3a retraction is now COMPLETE — measured against the *immediately prior commit*, the "+6.4%" figure **does not reproduce** (−0.1% / −0.4%). It was ambient, exactly as SUB.close suspected.
- **The baseline runs doubled as a correctness artifact**: at pre-track, `solve (unlicensed lv)` returns all 8 vehicles and top-level `not` goals echo unevaluated and rows type as `: _`; at HEAD, 4 correct rows, evaluated NAF, and `List {:lv String}`. Three headline deliverables visible in one program.
- **The adversarial gap audit earned its keep, and it cost me.** The PIR-gathering workflow's gap facet + completeness critic found **12 verified gaps**, including two over-claims *of mine*: I wrote "acceptance ;;35-38" for POL.9a when **markers 37/38 never existed**, and "+13 tests" when it was +12 — in the design doc, the dailies AND the commit message. Both mutable copies corrected and annotated. It also caught that **POL.1's only deliverable was never shipped** (the row was ✅; `grep "bag semantics"` = 0) — now written into `prologos-syntax.md`.
- **The most uncomfortable finding**: the acceptance file is **gated by nothing** (no test references it, no golden, `compare-golden-for-file` has zero callers) and the entire POL cluster is **Level-2 only** (0 `process-file` vs 84 `run-ns-ws-last`) — for *syntax* features, which `testing.md` explicitly mandates L3 for. The three sibling test files all do call `process-file`; the pattern was available and simply not applied. L3 coverage for POL therefore rests on an ungated file. Filed as action item #1 (~20 lines to close).
- **A live hazard found by probing rather than reading**: `(not (blocked "c"))` at top level is **Bool negation of a stuck goal term** — 0 errors, useless answer — because `not`/`=`/`is` are keywords and ride POL.9's keyword exclusion incidentally. Documented + filed with three options; it needs an owner ruling because it interacts with functional `not` on Bool.
- **Doc-truth**: `prologos-syntax.md` (the file every session auto-loads) had **zero** mention of the POL surfaces — the highest-leverage gap, now fixed. Corrected the overstated "sub-ms vs 30ms SQLite" and "CHAMP tables go further than first-argument indexing" claims in both vision docs against the measured artifact (there was **no index of any kind** before D.2.d). Retired `REL_MASTER.md` (stale by an entire track) and the seed note's `&>` label (the item design §4 reserved for X.close). Fixed `substitution.rkt:264`'s false comment — the exact shape the walker rule *this track wrote* red-flags.
- **PIR highlights**: 45% of production churn (POL + SUB) was **outside the Stage-3 decomposition** — it came from the owner using the language, which the design process did not and arguably could not generate. The premise-refutation tally is **broken** (four mutually inconsistent authoritative counts; ~29–32 by exhaustive enumeration vs 11 published) — the discrepancy is itself the finding. Two-context boundary bugs reach **7 instances**; codification has failed and PM Track 12 is the overdue architectural answer. Longitudinal: lessons get promoted *within* the track that learns them and stall *between* tracks.
- **Next**: owner sequences — **Rel T2 "The Fact Store"** charter, **CIU T6 Path Selection** (now unblocked by typed rows), or the DEMO through-line.

### X.close Batch A — the test-gate gaps closed (`bb45d2a0`)  (2026-07-25)

The first batch of PIR action items: pure test work, no behavior change. Suite **471 files / 9,136 / 0** (+1 file, +40 tests).

- **The acceptance file is now GATED** — `tests/test-rel-t1-acceptance.rkt`, 32 cases: 0-errors (naming *which* command fails), every marker verified, and a marker-index RANGE CHECK. Previously it was referenced by no test at all; its "0 errors" was a per-phase hand-check, i.e. a discipline rather than a gate.
- **Running `--check` for the first time found MORE than the audit did.** Beyond the ~13 prose markers, the **POL.8 and POL.9 markers were MISNUMBERED** — off by one and by two, because appending a `defr` shifts every later result index (`;;29` claimed the disjunction rows and pointed at `fruit-color : _ defined.`). All markers rewritten against actual output: **30/30 pass, was 5/28**. This is the **third** over-claim in this track's acceptance bookkeeping, and it survived for exactly the reason the gap existed — nothing ran `--check`. The gap demonstrated itself while being closed.
- **The mechanism was not new — it existed FOUR times over** (`test-f1-records-acceptance`, `-f1b3-width-`, `-f1b4-seal-`, `-f1b5-validate-`), all from the immediately preceding track. So the honest framing is a **discipline gap, not a tooling gap**: the pattern was available, recent, and simply not reached for. Worth remembering when the next track writes an acceptance file.
- **SC finally has its regression test** — three cases on the exact `process-string-ws` path it fixed (named solver config + `solve-with`, inline overrides, `:semantics`). It had shipped citing "130 tests pass", which was pre-existing regression evidence, not a pin.
- **Four untested branches pinned**: empty `&>` clause · a multi-token bare line at the goal column reading as a SIBLING (the Q5 residual — now deliberate, not incidental) · `raise-unknown-relation-error` branch 3 (bound as a non-function value; 1 and 2 were covered) · POL.4's multi-arity `arity=#f` path, a watchout named when POL.4 landed and never pinned.
- **The POL.8 merge FUTURE-TRAP is pinned** with an L2 layout canary — a flipped merge winner loses column info and the nesting collapses, so it fails at the point of change.
- **Still open, honestly**: `test-rel-t1-pol.rkt` is still Level-2 throughout, so the POL cluster's L3 coverage runs *through* the acceptance gate rather than through cases of its own; and the POL parser internals still have zero unit tests. Both remain filed.
- **Next**: the Batch B rulings (POL.9 `not`/`=`/`is`; Q_D slice 2 scope; the refutation/surprise boundary), then Batch C+D.

### X.close Batch B — the three rulings (`b8e01255` + docs)  (2026-07-25)

Owner ruled all three; each landed with its record.

- **Q_N1 — the goal keywords take the implicit solve (option A).** The defect was a CATEGORY CONFLATION: `paren-goal-stx?` excluded all parser keywords except `rel`, but that exclusion exists to protect EXPRESSION forms and says nothing about goals — `not`/`=`/`is` rode it incidentally. The fix is **derived, not listed**: `goal-keywords = {rel, not, =, is}` comes from `run-solve-goal`'s dispatch set, so the parse-level set cannot drift from what the engine can actually solve, and `guard`/`cut` stay out because a top-level solve doesn't dispatch them either. It also retroactively explains `rel`'s old special-case as the first instance of a general rule rather than a one-off.
- **Probing before ruling changed the ruling's shape.** I had framed the three as one group; the probe **split them**: `is` ERRORED (purely additive), `not` produced a stuck term with ZERO errors (the silent-useless shape), and only `=` had working behavior to lose. Corpus cost tree-wide: one line — `narrowing-demo.prologos`'s `(= ?x 5)`, already used as a query, now answering as a unify goal. Had I not probed I'd have argued the cost symmetrically and wrongly.
- **Q_N2 — Q_D slice 2 routed to PM Track 12B §11**, per the owner's instinct that it smelled like Free Ordering. mempalace found the track immediately (`2026-06-06_PM_TRACK12B_FREE_ORDERING_ON_NETWORK.md`), cross-checked live. It is a *precise* fit: POL.9's load-bearing argument — *free ordering requires syntactic CATEGORY-decidability, so only the BINDING residuates* — is 12B's own thesis, and slice 2 is the exact sibling of 12B §7 Q3 (multi-defn registry). **The Rel design's "fast-follow" sizing was WRONG**, and §11 records why with grounding: `residuation-demand-name` keys on global-env fvar `'pending`, but a not-yet-seen `defr` name is `'absent` — relations are a second namespace needing new machinery. Two adjacent items resolve there too (POL.9c's ungated 4th direction; the `current-relation-store` two-context instance).
- **Q_N3 — the refutation/surprise boundary is now defined** in `POST_IMPLEMENTATION_REVIEW.org` q13: a REFUTATION needs a premise **recorded beforehand**, shown false **by evidence**, that **changed the work**; everything else is a surprise, recorded but uncounted. Plus the procedural half — **state the count once at close with the enumeration**, since the running mid-flight tally is exactly what double-booked slot 10. Expect it to SHRINK the number and let it: under the strict rule this track's ~29–32 reclassifies mostly to surprises, validating the published ~11 rather than inflating it.
- **Gates**: POL file 100/0; acceptance gate green; **full suite 471 / 9,144 / 0**.
- **Next**: Batch C+D (the live `def := [validate …]` multiplicity instance + the overdue lesson promotions), or whatever the owner sequences.
