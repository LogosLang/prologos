# Claude Daily Summary: 2026-03-09

Covers all work since 2026-03-07_dailies.md was created (which covered through pattern defn + Phase 1a definitional trees).

**Interval**: Mar 7 dailies/standup creation → Mar 9 standup creation

---

## Work This Interval

### Research: Propagator Networks as Model Checkers (commit `93b2e98`)

Committed the research document from the Mar 7 discussion. ~865 lines, 13 sections formalizing the structural isomorphism between propagator-based session type checking and CTL model checking through the modal μ-calculus.

---

### Research: Narrowing + Search Heuristics + FL Narrowing Design (commits `111ba69`, `0a3ead4`, `9add767`)

Three research/design documents:

1. **Narrowing, Constraint Propagation, and Search Heuristics** (~490 lines) — survey of FL narrowing, CLP narrowing, and ECLiPSe's configurable search.
2. **FL Narrowing Phase 0-1 Design Revision** (~780 lines) — `=` as universal unification, implicit narrowing via `?`, constraints from types, `spec` as solution schema, 11 design decisions (D1-D11).
3. **Phase 2-3 Abstract Interpretation + Static Analysis Design** (~680 lines) — interval abstract domain, termination analysis, confluence analysis, 0-CFA, search heuristics, global constraints.

**External critique review** (commit `9add767`): Accepted 6/10 concerns (domain-bounded fuel, lazy confluence, demand-driven 0-CFA, global BB cost, narrowing+effects, length-interval domain). Rejected 4 (property-map, propagation strength axis, constraint block syntax, widening thresholds). New design decisions DD-8, DD-9.

---

### FL Narrowing Phase 1: Core Narrowing Implementation

**Phase 1a: Definitional Tree Extraction** (commit `99e56a8`)
- New module: `definitional-tree.rkt` (~200 lines)
- 4 struct types: `dt-rule`, `dt-branch`, `dt-or`, `dt-exempt`
- `extract-definitional-tree` from `defn` multi-clause AST
- 33 tests (unit + integration + WS-mode)

**Phase 1b: Term Lattice** (commit `4a7e215`)
- New module: `term-lattice.rkt`
- `bot/Var/Ctor/top` lattice with unification-based merge
- Used as narrowing variable cells

**Phase 1c: Narrowing Propagator** (commit `77f4a5d`)
- New module: `narrowing.rkt`
- Given function + definitional tree + cells → installs propagators
- Residuation by default (propagator waits), narrowing on demand

**Phase 1d: DT-Guided Narrowing Search** (commit `36831ed`)
- ATMS `amb` branching guided by definitional tree `dt-branch` nodes
- Propagate→branch→propagate loop with `run-to-quiescence`

**Phase 1e: WS-Mode Narrowing Syntax** (commit `3062b4c`)
- `?`-prefixed variables trigger narrowing
- WS reader integration tests

---

### FL Narrowing Phase 2: Abstract Interpretation + Static Analysis

**Phase 2a: Interval Abstract Domain** (commit `6301d69`)
- Galois connections between concrete terms and abstract intervals
- Cross-domain propagators via `net-add-cross-domain-propagator`
- Handles Nat/Int/PosInt infinite constructor universes

**Phase 2b: Size-Based Termination Analysis** (commit `bae2fb4`)
- LJBA criterion for compile-time termination guarantees
- `termination-analysis.rkt`

**Phase 2c: Critical Pair / Confluence Analysis** (commit `3d3c0ce`)
- Church-Rosser checking via critical pair detection
- `confluence-analysis.rkt`
- Lazy fast-path for inductively sequential functions (no Or-branches)

---

### FL Narrowing Phase 3: Advanced Extensions

**Phase 3a: 0-CFA Auto-Defunctionalization** (commit `cdb6176`)
- `cfa-analysis.rkt` — module-level 0-CFA, demand-driven on `?`-prefixed HO args
- Auto-defunctionalization for higher-order narrowing

**Phase 3b: Configurable Search Heuristics** (commit `fdda370`)
- 3 orthogonal axes: variable ordering, value selection, search strategy
- `search-heuristics.rkt`, `search-strategies.rkt`
- Solver config extensions

**Phase 3c: Global Constraints + Branch-and-Bound** (commit `16dd1ac`)
- `global-constraints.rkt`
- All-different, cumulative, and other global constraints
- Branch-and-bound optimization as propagator

---

### FL Narrowing PIR + Capstone Demo (commits `550bbd4`, `92d4e0c`, `1734cc6`)

Post-Implementation Review documenting all 11 sub-phases. Feature capstone demo added as mandatory Phase 5 step. Capstone demo showing end-to-end narrowing.

---

### Pattern-Based `defn` Clauses + Head-Tail Patterns (commits `8402619`, `92166a9`, `96bd62e`)

Haskell/Erlang-style pattern matching at function definition level:
- `| [pattern...] -> body` clause syntax
- Head-tail `[a b | rest]` → `cons(a, cons(b, rest))` desugaring
- Spec integration: `datum->surf-type` builds surface type AST from raw datums
- `holes-to-metas` / `unsolved-metas-to-holes` for pattern functions with inferred return types
- Pattern clauses and arity clauses coexist within same `defn`
- 22 tests in `test-pattern-defn-01.rkt`

**Files modified**: surface-syntax.rkt, parser.rkt, macros.rkt, driver.rkt, expander.rkt

---

### Unified Pattern Matching (commits `99f97ef`..`1ad02e8`)

Rich patterns (constructors, literals, nested, head-tail, wildcards) available in both `match` arms and `defn` params/arms — same compilation path through `compile-match-tree`.

- `test-unified-match-01.rkt`: 38 tests
- `unified-matching.prologos` demo file
- Grammar docs updated

---

### Int Literal Patterns + Guard Clauses + Cond Macro (commits `8df17c1`..`d0b880f`)

Three new surface features:

**Int Literal Patterns** (commit `e16015b`):
- Bare `0`/`1`/`42`/`-1` in patterns → `'int-lit` kind (distinct from `'numeric` Nat)
- Compiles to nested `surf-boolrec` with `surf-int-eq` guards (no new AST nodes)
- New `compile-int-dispatch` in macros.rkt

**Guard Clauses** (commit `ec039dc`):
- `| pattern when guard-expr -> body` syntax
- `guard` field added to `match-pattern-arm` struct
- Guard checked after pattern match; falls through on failure

**Cond Macro** (commit `8df17c1`):
- `cond | guard -> body | ...` → nested `if` (preparse macro)
- Registered via `register-preparse-macro!`

**Test + Fix Commits**:
- `ccfafbe`: 47 new tests across 3 files + QTT hole handling fix
- `bcb6f83`: Performance regression fix — `pattern-is-simple-flat?` now handles `'wildcard` kind, restoring fast path for simple flat constructor patterns (850s → 186s)
- `bcb6f83`: Test expectation fixes — `{map` → `{:` regex in `count-answers` (6 files), empty CHAMP map `{}` pp fix (test-map)
- `d0b880f`: Grammar docs, demo file, tracking doc

---

### Library Improvements

- **Sigma pair projection rename** (commit `5feaeb9`): `first`/`second` → `fst`/`snd` to free names for collection ops
- **Generic collection functions** (commit `7c04a89`): `first`/`second`/`rest` as generic ops on sequences
- **Narrowing ground args fix** (commit `7fdf1cd`): Ground constructor arguments handled correctly in narrowing
- **DEFERRED update** (commit `ebacd75`): WS narrowing surface gaps documented

---

### Whale Test Splits (commits `936b045`, `626b1af`)

Split 10 test files exceeding 30s wall time into `-01`/`-02` halves:

| Original | Split Into | Tests |
|----------|-----------|-------|
| test-pattern-defn-01 | -01 (14) + -02 (14) | 28 |
| test-char-string | -01 (19) + -02 (19) | 38 |
| test-narrow-syntax-01 | -01 + -02 | 24 |
| test-mixfix | -01 (36) + -02 (29) | 65 |
| test-union-find-integration | -01 (6) + -02 (3) | 9 |
| test-implicit-map | -01 (11) + -02 (10) | 21 |
| test-pipe-compose-e2e-01 | -01 (5) + -03 (4) | 9 |
| test-narrowing-search-01 | -01 (15) + -02 (7) | 22 |
| test-dot-access | -01 (14) + -02 (7) | 21 |
| test-cfa-analysis-01 | -01 (14) + -02 (9) | 23 |

Added `test-reduction-perf-02-01.rkt` (51s) to skip list. Updated `dep-graph.rkt`.
Slowest files now 25-33s (down from 31-51s).

---

## Metrics

| Metric           | Mar 7 dailies close | Current |
|------------------|---------------------|---------|
| Tests            | 6080                | 6639    |
| Test files       | 312                 | 343     |
| Failures         | 0                   | 0       |
| Wall time        | 176.9s              | 188.0s  |
| Skipped tests    | 2                   | 3       |
| New modules      | —                   | 11 (FL narrowing) + constraint-cell |
| Research docs    | —                   | 4 new + 2 revised |
| Commits          | —                   | ~55     |

---

## Key Technical Insights

- **Performance regression pattern**: Adding new pattern kinds (`'int-lit`) to pattern compilation required updating `pattern-is-simple-flat?` to recognize `'wildcard` as well as `'var` — missing this caused the fast path to fall through to the slow `compile-match-tree` path for ALL patterns (850s → 186s regression).
- **Test output format drift**: `count-answers` regex `\{map` failed silently when narrowing output changed to `{:key value}` format. Tests returned 0 answers instead of failing — a reminder that "passes with 0" is a dangerous test pattern.
- **Whale file splitting**: Fixture compilation (shared `define-values`) dominates test time — splitting below ~19 tests per file yields diminishing returns since both halves pay the same fixture cost.
- **Concurrent agent edits**: 10 parallel split agents editing `dep-graph.rkt` caused "file modified since read" errors. Sequential dep-graph updates would be safer for future splits.

---

### First-Class Traits: Stage 3 Design + Implementation

**Design Document** (commit `2d6e0db`):
- `2026-03-09_FIRST_CLASS_TRAITS_STAGE3_DESIGN.md` (~1200 lines)
- 12-phase roadmap for `:over`/`:method` trait polymorphism, trait universes, row variables
- 5 dependency levels, risk analysis, error catalog (E3001-E3010)

**Phase 1: `first-class` keyword** (commit `6825d38`):
- `first-class` keyword designates traits as first-class (can be passed as values)
- Design principle: `FIRST_CLASS_BY_DEFAULT.md`

**Phase 2a: Static generic operator resolution** (commit `44539b1`)
**Phase 2b: `constraint-cell.rkt`** (commit `dc98b9b`)
**Phase 2c: Constraint propagators P1-P4** (commit `2ca66d2`)

**Phase 3a: HasMethod constraint + projection** (commit `a5c465d`):
- `:over P` / `:method P eq? : A A -> Bool` spec syntax
- `hasmethod-constraint-info` struct in metavar-store
- `parse-hasmethod-spec` + `:over`/`:method` keyword handling in macros.rkt
- `$hm-METHOD` evidence params: detected in lambda elaboration, create direct where-method-entries
- `resolve-hasmethod-constraints!` with search-based P discovery via `find-trait-with-method`
- `project-method` for single-method (identity) and multi-method (sigma fst/snd chain) traits
- `trait-expr->name` helper
- Wired at 4 sites in expander.rkt + 5 sites in driver.rkt
- 12 tests in test-hasmethod-01.rkt

**Key debugging insight**: The driver uses propagator cell-path callbacks for standard trait resolution and never calls `resolve-trait-constraints!` explicitly. HasMethod constraints use a separate map requiring explicit `resolve-hasmethod-constraints!` calls. The initial implementation only wired this in `expander.rkt` (used by `#lang prologos`), not `driver.rkt` (used by `process-string` in tests/REPL). This caused evidence metas to remain unsolved despite the constraint being registered and the search logic working correctly.

**Phase 2d: ATMS multi-candidate dispatch** (commit `9faecd2`):
- When `resolve-generic-narrowing` can't resolve to a single trait impl (e.g., `+` on untyped `?`-variables), falls through to `resolve-generic-narrowing-candidates` which returns all remaining candidates
- `run-narrowing` in `reduction.rkt` handles `'multi-dispatch` sentinel: tries each candidate independently via `run-narrowing-search`, collects solutions with `remove-duplicates`
- Extracted `candidate->func-name` helper (was inline in `resolve-generic-narrowing`)
- 10 tests in `test-constraint-amb-01.rkt`
- Design: simple enumerate-and-merge, no full ATMS amb needed (each candidate attempt is independent)

**Key design decisions**:
1. Evidence as implicit parameter, not new AST node — reuses existing implicit insertion infrastructure
2. Search-based P discovery: trait dict types are expanded (e.g., `Nat -> Nat -> Bool` not `(Eq Nat)`), so unification can't discover P. Instead, `find-trait-with-method` searches all traits for one matching the method name + having an impl for the type args.
3. `$brace-params` wrapper needed for sexp-mode `:over`/`:method` keywords (WS reader handles this natively)
4. Evidence type tokens wrapped as compound lists to survive `split-on-arrow-datum/mult`

---

## Considerations

- The FL narrowing implementation is the largest single feature built in Prologos to date: 11 sub-phases, ~3,800 new lines, ~720 tests, 11 new modules. It unifies functional evaluation and logic programming on the propagator substrate — `defn` functions can now be queried as relations via narrowing.
- The Int patterns + guard clauses + cond macro complete the "equational style" surface syntax story. Users can now write `defn fib [n : Int] : Int | 0 -> 0 | 1 -> 1 | n -> ...` — a significant ergonomic milestone.
- Test suite growth from 6080 → 6528 (+448 tests) with wall time increase from 176.9s → 190.4s (+13.5s, +7.6%) reflects the whale splits keeping parallelism healthy despite more tests.
- The 3 skipped tests (2 perf + 1 fuel exhaustion) total ~243s — keeping them out of the default suite saves significant CI time.
